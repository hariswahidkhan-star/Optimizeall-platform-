# Academy course packs v2 — work in progress (staging snapshot)

Snapshot of course packs being upgraded/written to the v2 format (lecture scripts on every lesson, deeper lessons,
`lastReviewed`, `tools`). These files are NOT loaded by the API (only `backend/src/OptimizeAll.Api/Modules/Learning/Catalog/*.json`
is embedded). They move into the catalog once the v2 engine/validator lands; this folder is then removed.
