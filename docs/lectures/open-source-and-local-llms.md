# Open-Weight and Local AI: Run, Choose and Deploy Your Own Models — lecture production script

Course `open-source-and-local-llms` · pack v1 · reviewed 2026-09 · level advanced · category ai

16 lectures · 19,023 narration words · planned runtime 2:17:49 (≈ 136 min at 140 wpm)

> Generated by `scripts/export-lecture-scripts.py` from the course pack — edit the pack, not this file. Production workflow: [README](README.md).

## Voice direction

Conversational expert, second person, ~140 wpm; precise and technical, calm confidence; spell out code identifiers when read aloud; audience level: advanced. Pause ~0.5 s between scenes; no music under narration (added in edit).

## Lectures

1. [Open-weight vs closed models: what "open" really means](#1-open-weight-vs-closed-models-what-open-really-means)
2. [Licenses decoded: Apache-2.0, MIT, Llama and custom terms](#2-licenses-decoded-apache-2-0-mit-llama-and-custom-terms)
3. [Model families in 2026 and how to shortlist](#3-model-families-in-2026-and-how-to-shortlist)
4. [Quantization: GGUF, AWQ, GPTQ, FP8 and MXFP4](#4-quantization-gguf-awq-gptq-fp8-and-mxfp4)
5. [VRAM and memory sizing: the formula](#5-vram-and-memory-sizing-the-formula)
6. [Hardware choices: laptops, workstations, servers and cloud GPUs](#6-hardware-choices-laptops-workstations-servers-and-cloud-gpus)
7. [Ollama hands-on: models, Modelfiles and the API](#7-ollama-hands-on-models-modelfiles-and-the-api)
8. [llama.cpp and LM Studio: control and convenience](#8-llama-cpp-and-lm-studio-control-and-convenience)
9. [Local models for real work: structured output, tools and reliability](#9-local-models-for-real-work-structured-output-tools-and-reliability)
10. [Serving at scale with vLLM](#10-serving-at-scale-with-vllm)
11. [Choosing a serving engine: SGLang, TGI, TensorRT-LLM and friends](#11-choosing-a-serving-engine-sglang-tgi-tensorrt-llm-and-friends)
12. [On-device AI and small language models](#12-on-device-ai-and-small-language-models)
13. [Privacy, data residency and securing self-hosted models](#13-privacy-data-residency-and-securing-self-hosted-models)
14. [Benchmarking responsibly: leaderboards, evals and honest comparisons](#14-benchmarking-responsibly-leaderboards-evals-and-honest-comparisons)
15. [Hybrid routing between local and API models](#15-hybrid-routing-between-local-and-api-models)
16. [Capstone: a private RAG assistant on a laptop or server](#16-capstone-a-private-rag-assistant-on-a-laptop-or-server)

### 1. Open-weight vs closed models: what "open" really means

Lesson `open-weight-vs-closed-models` · module “The open-weight landscape” · target 9 min · 13 scenes · 1267 words · planned 9:11 (words ≈ 9:11) · status: **to produce**

| Term | Say |
|---|---|
| Ollama | oh-LAH-mah |
| Qwen | chwen |
| gpt-oss | G P T O S S |
| GPU | G P U |

| # | Time | Sec | Narration | On screen | Visual |
|---|---|---|---|---|---|
| 1 | 0:00 | 34 | Imagine your company's most sensitive document. A contract, a patient file, a payroll sheet. Now imagine asking an AI to summarize it, and knowing for certain that not a single word ever leaves the room. That is the promise of open-weight models. In this course you will learn to choose them, run them, size the hardware, serve them at scale and build a private assistant. Let's start by getting the words right, because the words drive the decisions. | Open-weight AI: what "open" really means<br>• Choose, run and deploy your own models<br>• Start with the right vocabulary | Slow push-in on a locked server rack in a dim room, a single status LED pulsing. Title card fades in over it. |
| 2 | 0:34 | 55 | Here is an analogy that makes the whole decision easier. Using a closed model through an API is like taking a taxi. You get a great driver, you pay per trip, and you never worry about fuel, servicing or insurance. But you cannot choose the route engine, the company can change its prices, and your conversation happens inside someone else's car. Running an open-weight model is like owning a car. It costs money up front, you handle the servicing, and it may not be the fastest car on the road. But it goes where you want, when you want, and nobody else is in the passenger seat. Most businesses end up with both: a car for daily trips and a taxi for the occasional long journey. | Analogy: taxi vs owning a car<br>• API model = taxi: pay per trip, no maintenance<br>• Open weights = own car: upfront cost, full control<br>• Most teams use both | Two-column layout. Left: a taxi icon slides in with three labels fading in one by one (pay per trip, no servicing, their car). Right: a car in a private garage with labels (upfront cost, you service it, your route). A final line draws between them labeled hybrid. |
| 3 | 1:29 | 49 | There are three categories. Closed models are only available through a vendor's API or app. You never see the weights, and the vendor decides when versions change or retire. Open-weight models let you download the trained parameters, the actual numbers that make the model work, under a license. And open source AI, in the strict sense used by the Open Source Initiative, goes further: enough information about data and code that others could study and rebuild the system. Most of the famous "open" models, Llama, Gemma, Qwen, Mistral, DeepSeek, Phi and gpt-oss, are open-weight. Keep that distinction in your pocket. It matters when a lawyer or auditor asks what exactly you are using. | Three categories<br>• Closed: API only, vendor controls versions<br>• Open-weight: download parameters under a license<br>• Open source AI: weights plus data and code details | Three-column animated diagram: a cloud with a padlock, a download arrow into a file icon labeled "weights", and a transparent box showing weights, data and code. Labels appear as each is named. |
| 4 | 2:18 | 33 | So what do you actually gain? First, data control. Prompts and documents stay on your laptop, your server or your private cloud. Second, version stability. The file you tested is the file you run, with no silent update on a Tuesday afternoon. Third, a different cost shape. You pay for hardware and electricity instead of per token. Fourth, customization, from fine-tuning to quantization. And fifth, offline use, on a ship, in a factory or on a phone. | What you gain<br>• Data control<br>• Version stability<br>• Fixed cost shape instead of per token<br>• Customization and offline use | Five icons appear in sequence on a clean grid: shield, pinned version tag, flat cost line vs rising line, wrench, airplane-mode symbol. |
| 5 | 2:51 | 30 | Now the honest part. The strongest closed models still tend to lead on the hardest reasoning and long agentic tasks, though the gap moves every few months and depends heavily on the task. You also take on operations: uptime, scaling, patches, monitoring. The safety filters an API vendor wraps around its model are now your responsibility. And "free tokens" is a myth. Idle GPUs and engineering hours cost real money. | What you give up<br>• Some peak capability on the hardest tasks<br>• Operations become your job<br>• Safety layers become your job<br>• Hidden costs: people and idle hardware | Balance scale animation: left pan fills with "control" icons, right pan with "ops", "safety", "people" weights. The scale settles roughly level. |
| 6 | 3:21 | 40 | Here is a simple frame I use with clients. Four questions. One, data: must any of this data stay inside our boundary? Two, quality: on our own test set, does an open model hit the bar, maybe with retrieval or fine-tuning? Three, volume and latency: is traffic steady and high enough that owning inference pays off? Four, capability: can we operate this safely? A yes on data pulls you toward open weights. A no on quality pulls you toward an API, at least for that task. Most teams land on a hybrid. | The four questions<br>• Data: must it stay inside?<br>• Quality: does it pass our eval?<br>• Volume and latency: does owning pay off?<br>• Capability: can we operate it? | Decision flowchart built step by step, four diamond nodes leading to three end boxes labeled Local, API and Hybrid. |
| 7 | 4:01 | 42 | Let's run it on a real-style example. A real-estate agency in Dubai wants an assistant that summarizes tenancy contracts full of passport and Emirates ID numbers. Data: sensitive, clients expect it to stay in the country. Quality: on sixty redacted contracts, a mid-size open model with retrieval gets fifty-five right, a frontier API gets fifty-seven. These numbers are illustrative, but the pattern is common. Volume: about four hundred questions a day in office hours, easy for one workstation GPU. Operations: their IT partner already runs servers. Decision: self-host, and route unusual clause questions to a human. | Worked example: Dubai agency (illustrative)<br>• Sensitive IDs in contracts<br>• Open model 55/60 vs API 57/60<br>• ~400 queries a day<br>• Decision: self-host plus human escalation | Stylized office dashboard mock-up with four cards filling in: data sensitivity red, quality bars 55 vs 57, a traffic curve peaking midday, a server icon. "Illustrative numbers" label in the corner. |
| 8 | 4:43 | 36 | Your hands-on step is tiny but important. Install Ollama, then run one small model from your terminal and ask it a question. Then list your installed models and look at the size. That file on your disk is the model. No account, no API key, no network once it is downloaded. The exact commands are in the lesson text. From here we will learn to read licenses, pick model families, squeeze models onto your hardware and serve them to a whole team. | Hands-on<br>• Install Ollama<br>• ollama run a small model<br>• ollama list: the file is the model | Screen recording of a terminal: typing an ollama run command, the download progress bar, then a two-sentence answer streaming, followed by ollama list showing the model size. |
| 9 | 5:19 | 49 | Let me give you a simple example first, before the business one. A freelance translator in Lahore gets client documents that are under a non-disclosure agreement. Each week she translates about forty pages and wants help drafting first versions. The data rule is strict: nothing may leave her machine. Quality: a mid-size open model gets her first drafts to a level she can edit quickly. Volume: small and steady. Operations: she only needs one laptop and a runner like Ollama. Four questions, four answers, and the decision is obvious. Local, on her laptop. Notice she did not need a benchmark leaderboard to decide. She needed her own documents and one afternoon of testing. | Simple example: freelance translator<br>• NDA documents: must stay local<br>• Quality: drafts easy to edit<br>• Small, steady volume<br>• Decision: local on her laptop | Single laptop centered on screen. Four small cards animate in around it in sequence: Data (red lock), Quality (green tick), Volume (small bar), Ops (one laptop). The word LOCAL stamps onto the laptop at the end. |
| 10 | 6:08 | 44 | Three mistakes to avoid. First, calling a model open source in a policy or a contract when it is really open-weight with use restrictions. Words in contracts matter. Second, testing an open model only on your very hardest task, deciding it is not good enough, and missing the eighty percent of routine work where it would be perfect. Test across your real task mix. Third, forgetting the operations bill. Someone has to patch the server, watch the dashboards and plan upgrades, and that person's time belongs in your cost comparison. If you avoid those three, you are already ahead of most teams. | Three mistakes to avoid<br>• Saying open source when you mean open-weight<br>• Testing only the hardest task<br>• Ignoring the operations bill | Three warning cards flip over one at a time, each with a simple line icon: a contract, a single difficult puzzle piece, and a wrench next to a clock. |
| 11 | 6:52 | 42 | Try this now. Open a note and write down one task you did this week where you pasted company information into an AI tool. Ask yourself the four questions for that one task: does the data need to stay inside, does a smaller model reach the quality bar, is the volume steady, and could someone on your team operate it? If you answered yes to data and yes to capability, that task is your first candidate for a local model. Keep the note. By the end of module three you will have that task running locally. | Try this now<br>• Pick one task from this week<br>• Answer the four questions<br>• Yes to data + capability → first local candidate | A note-taking card animates open. Four checkbox lines appear one per second. Two of them tick green and a highlighted banner slides up reading "first local candidate". |
| 12 | 7:34 | 66 | Watch me do it. I will take one real workload and run the four questions live. The workload: summarizing supplier contracts for a procurement team in Karachi. First, data. I open a sample contract and highlight what is sensitive: bank details, pricing, a named contact. Verdict: it should stay inside. Second, quality. I pull ten redacted contracts, run them through a local eight billion model and a cloud model, and score each summary against a three-point checklist: parties, value, renewal date. The local model gets nine of ten fully right, the cloud model ten. Third, volume: about sixty contracts a day, steady. Fourth, capability: the IT team already runs a Linux server. I write the decision in one line on the note: local, with a human check on renewal dates, revisit in three months. That took me about forty minutes, and now the decision has evidence behind it instead of opinions. | Watch me do it<br>1 Mark sensitive fields<br>2 Score 10 contracts, local vs cloud<br>3 Check volume + capability<br>4 Write a one-line decision | Screen recording: a contract PDF with three fields highlighted in yellow; then a spreadsheet with ten rows and two score columns filling in (local 9/10, cloud 10/10); finally a note app where a one-line decision is typed. |
| 13 | 8:40 | 31 | Quick recap. Most open models are open-weight, not fully open source. They give you control, stability, customization and offline use, in exchange for some peak capability and a lot of operational responsibility. Decide with four questions: data, quality, volume, capability. Your next step: list three AI workloads in your organization and label each one local, API or hybrid. We will come back to that list at the end of the course. | Recap<br>• Open-weight is not the same as open source<br>• Control vs operations trade-off<br>• Next: label three workloads local, API or hybrid | Summary card with three checkmarks animating in, then a notepad graphic with three blank lines labeled Workload 1, 2, 3. |

<details><summary>Narration per scene (paste into ElevenLabs)</summary>

Scene 1 — Open-weight AI: what "open" really means

```text
Imagine your company's most sensitive document. A contract, a patient file, a payroll sheet. Now imagine asking an AI to summarize it, and knowing for certain that not a single word ever leaves the room. That is the promise of open-weight models. In this course you will learn to choose them, run them, size the hardware, serve them at scale and build a private assistant. Let's start by getting the words right, because the words drive the decisions.
```

Scene 2 — Analogy: taxi vs owning a car

```text
Here is an analogy that makes the whole decision easier. Using a closed model through an API is like taking a taxi. You get a great driver, you pay per trip, and you never worry about fuel, servicing or insurance. But you cannot choose the route engine, the company can change its prices, and your conversation happens inside someone else's car. Running an open-weight model is like owning a car. It costs money up front, you handle the servicing, and it may not be the fastest car on the road. But it goes where you want, when you want, and nobody else is in the passenger seat. Most businesses end up with both: a car for daily trips and a taxi for the occasional long journey.
```

Scene 3 — Three categories

```text
There are three categories. Closed models are only available through a vendor's API or app. You never see the weights, and the vendor decides when versions change or retire. Open-weight models let you download the trained parameters, the actual numbers that make the model work, under a license. And open source AI, in the strict sense used by the Open Source Initiative, goes further: enough information about data and code that others could study and rebuild the system. Most of the famous "open" models, Llama, Gemma, Qwen, Mistral, DeepSeek, Phi and gpt-oss, are open-weight. Keep that distinction in your pocket. It matters when a lawyer or auditor asks what exactly you are using.
```

Scene 4 — What you gain

```text
So what do you actually gain? First, data control. Prompts and documents stay on your laptop, your server or your private cloud. Second, version stability. The file you tested is the file you run, with no silent update on a Tuesday afternoon. Third, a different cost shape. You pay for hardware and electricity instead of per token. Fourth, customization, from fine-tuning to quantization. And fifth, offline use, on a ship, in a factory or on a phone.
```

Scene 5 — What you give up

```text
Now the honest part. The strongest closed models still tend to lead on the hardest reasoning and long agentic tasks, though the gap moves every few months and depends heavily on the task. You also take on operations: uptime, scaling, patches, monitoring. The safety filters an API vendor wraps around its model are now your responsibility. And "free tokens" is a myth. Idle GPUs and engineering hours cost real money.
```

Scene 6 — The four questions

```text
Here is a simple frame I use with clients. Four questions. One, data: must any of this data stay inside our boundary? Two, quality: on our own test set, does an open model hit the bar, maybe with retrieval or fine-tuning? Three, volume and latency: is traffic steady and high enough that owning inference pays off? Four, capability: can we operate this safely? A yes on data pulls you toward open weights. A no on quality pulls you toward an API, at least for that task. Most teams land on a hybrid.
```

Scene 7 — Worked example: Dubai agency (illustrative)

```text
Let's run it on a real-style example. A real-estate agency in Dubai wants an assistant that summarizes tenancy contracts full of passport and Emirates ID numbers. Data: sensitive, clients expect it to stay in the country. Quality: on sixty redacted contracts, a mid-size open model with retrieval gets fifty-five right, a frontier API gets fifty-seven. These numbers are illustrative, but the pattern is common. Volume: about four hundred questions a day in office hours, easy for one workstation GPU. Operations: their IT partner already runs servers. Decision: self-host, and route unusual clause questions to a human.
```

Scene 8 — Hands-on

```text
Your hands-on step is tiny but important. Install Ollama, then run one small model from your terminal and ask it a question. Then list your installed models and look at the size. That file on your disk is the model. No account, no API key, no network once it is downloaded. The exact commands are in the lesson text. From here we will learn to read licenses, pick model families, squeeze models onto your hardware and serve them to a whole team.
```

Scene 9 — Simple example: freelance translator

```text
Let me give you a simple example first, before the business one. A freelance translator in Lahore gets client documents that are under a non-disclosure agreement. Each week she translates about forty pages and wants help drafting first versions. The data rule is strict: nothing may leave her machine. Quality: a mid-size open model gets her first drafts to a level she can edit quickly. Volume: small and steady. Operations: she only needs one laptop and a runner like Ollama. Four questions, four answers, and the decision is obvious. Local, on her laptop. Notice she did not need a benchmark leaderboard to decide. She needed her own documents and one afternoon of testing.
```

Scene 10 — Three mistakes to avoid

```text
Three mistakes to avoid. First, calling a model open source in a policy or a contract when it is really open-weight with use restrictions. Words in contracts matter. Second, testing an open model only on your very hardest task, deciding it is not good enough, and missing the eighty percent of routine work where it would be perfect. Test across your real task mix. Third, forgetting the operations bill. Someone has to patch the server, watch the dashboards and plan upgrades, and that person's time belongs in your cost comparison. If you avoid those three, you are already ahead of most teams.
```

Scene 11 — Try this now

```text
Try this now. Open a note and write down one task you did this week where you pasted company information into an AI tool. Ask yourself the four questions for that one task: does the data need to stay inside, does a smaller model reach the quality bar, is the volume steady, and could someone on your team operate it? If you answered yes to data and yes to capability, that task is your first candidate for a local model. Keep the note. By the end of module three you will have that task running locally.
```

Scene 12 — Watch me do it

```text
Watch me do it. I will take one real workload and run the four questions live. The workload: summarizing supplier contracts for a procurement team in Karachi. First, data. I open a sample contract and highlight what is sensitive: bank details, pricing, a named contact. Verdict: it should stay inside. Second, quality. I pull ten redacted contracts, run them through a local eight billion model and a cloud model, and score each summary against a three-point checklist: parties, value, renewal date. The local model gets nine of ten fully right, the cloud model ten. Third, volume: about sixty contracts a day, steady. Fourth, capability: the IT team already runs a Linux server. I write the decision in one line on the note: local, with a human check on renewal dates, revisit in three months. That took me about forty minutes, and now the decision has evidence behind it instead of opinions.
```

Scene 13 — Recap

```text
Quick recap. Most open models are open-weight, not fully open source. They give you control, stability, customization and offline use, in exchange for some peak capability and a lot of operational responsibility. Decide with four questions: data, quality, volume, capability. Your next step: list three AI workloads in your organization and label each one local, API or hybrid. We will come back to that list at the end of the course.
```

</details>

### 2. Licenses decoded: Apache-2.0, MIT, Llama and custom terms

Lesson `licenses-decoded` · module “The open-weight landscape” · target 8 min · 13 scenes · 1160 words · planned 8:25 (words ≈ 8:24) · status: **to produce**

| Term | Say |
|---|---|
| Apache | uh-PATCH-ee |
| MIT | M I T |
| YAML | YAM-ul |
| GGUF | G G U F |
| MAU | M A U |

| # | Time | Sec | Narration | On screen | Visual |
|---|---|---|---|---|---|
| 1 | 0:00 | 34 | Picture this. Your team spends six weeks building on the model that topped your benchmark. Launch review arrives, legal reads the license, and the answer is no. Six weeks gone. It happens more than you would think. In this lesson you will learn to read model licenses like a practitioner, know which families use which terms right now, and keep a simple register so this never catches you out. One note first: this is practical guidance, not legal advice. | Licenses decoded<br>• License first, then quality<br>• Know the current terms by family<br>• Keep a model register | Animated calendar flipping through six weeks, then a red "blocked" stamp landing on a launch checklist. |
| 2 | 0:34 | 44 | Think of a model license like a lease on a shop. Some leases say: use it for anything, just keep the landlord's sign on the wall. That is roughly Apache or MIT. Others say: you can run your business here, but you must display our brand at the entrance, name your shop after us, follow our house rules, and if you become enormous you need to renegotiate. That is closer to a community license like Llama's. Neither lease is bad. But you would never sign a lease without reading it, and you should never ship a model without reading its license. | Analogy: a shop lease<br>• Apache/MIT: keep the sign on the wall<br>• Community license: branding, naming, house rules, size limit<br>• Read before you sign | Split screen of two shopfronts. Left shop has a small plaque labeled "notice". Right shop shows a large brand banner, a rulebook on the counter and a height marker labeled "700M". Labels animate in as each clause is mentioned. |
| 3 | 1:18 | 41 | Start with the permissive two. Apache 2.0 lets you use, modify and distribute, including commercially. You keep the copyright and license notices, mark significant changes, and pass on any NOTICE file. It also includes an explicit patent grant from contributors. MIT is even shorter: keep the notice, and do almost anything, but there is no explicit patent clause. Right now, gpt-oss, Gemma 4, the Mistral 3 family and most Qwen three and three point five open weights use Apache 2.0. DeepSeek and Microsoft's Phi-4 family use MIT. Always confirm on the specific model card. | Permissive licenses<br>• Apache-2.0: gpt-oss, Gemma 4, Mistral 3, most Qwen3/3.5<br>• MIT: DeepSeek, Phi-4<br>• Keep notices; verify each release | Two side-by-side cards titled Apache-2.0 and MIT, each listing permissions with green ticks and obligations with amber dots. Family names slide under the correct card. |
| 4 | 1:59 | 43 | Now Meta's Llama 4 Community License. It is free for most commercial use, with conditions. If your products had more than seven hundred million monthly active users on the release date, you need a separate license from Meta. You must show "Built with Llama", and derivative model names must start with the word Llama. And you must follow the Acceptable Use Policy, which says that for the multimodal Llama 4 models, rights are not granted to individuals or companies based in the European Union, though end users of a product built on them are not covered by that restriction. | Llama 4 Community License<br>• Special license above 700M MAU<br>• "Built with Llama" + names start with "Llama"<br>• Acceptable Use Policy<br>• EU limit on multimodal models | Document-style animation highlighting four clauses one by one with a marker effect; a small map of Europe dims when the multimodal clause is highlighted. |
| 5 | 2:42 | 33 | Here is the trap. Licenses belong to a specific release, not to a brand. Earlier Gemma versions used Google's own terms, and Gemma 4 moved to Apache. Some older Qwen sizes used Alibaba's own licenses. And when someone uploads a quantized copy of a model, it inherits the original license. They cannot relabel it. So "we use Qwen" is not an answer to a license question. "We use this exact model file, under this license text" is. | The release, not the brand<br>• Gemma 1–3 custom terms; Gemma 4 Apache-2.0<br>• Some older Qwen sizes: custom licenses<br>• Quantized copies inherit the original license | Family tree diagram with brand at the top and individual releases as leaves, each leaf coloured by license type. A GGUF file icon attaches to one leaf and takes its color. |
| 6 | 3:15 | 32 | Three more checks beyond the weights. First, outputs: some terms restrict using a model's outputs to train competing models, which matters if you plan to distil. Second, provenance: download from the publisher or a trusted mirror, and make sure the license file is included. Third, regulation: data protection law and rules like the EU AI Act apply to your use case, whatever the license says. Apache does not make an unlawful use lawful. | Three more checks<br>• Output-use terms (distillation)<br>• Provenance and license files<br>• Regulation applies regardless of license | Three stacked checklist cards with icons: a funnel for outputs, a package with a seal for provenance, a gavel for regulation. |
| 7 | 3:47 | 38 | Let's triage a real-style case. A Lahore start-up sells a WhatsApp support assistant to retailers across Pakistan and the Gulf, and plans to sign an Irish customer next year. They shortlist a Llama 4 model, a Qwen three point five model and gpt-oss twenty B. They are nowhere near seven hundred million users, but Llama would require attribution in their product, and their legal team wants zero ambiguity for future EU customers. So they shortlist the two Apache models and keep Llama only as a benchmark reference. | Worked example: Lahore SaaS<br>• Candidates: Llama 4, Qwen3.5, gpt-oss-20b<br>• Future EU customer<br>• Shortlist: the Apache-2.0 models | Map animation from Lahore to the Gulf and then to Dublin, followed by three model cards where two receive a green shortlist badge. |
| 8 | 4:25 | 31 | Your hands-on tool is a model register: one small YAML file per model you deploy. It records the exact model, the source, the license, extra policies, attribution and naming rules, geographic limits, whether outputs can train other models, who reviewed it and when to review again. The template is in the lesson. It takes ten minutes to fill in and it turns license review from a memory test into a checklist. | Hands-on: model register<br>• One YAML file per model<br>• License, policies, attribution, geography<br>• Reviewer and next review date | Code editor screen recording showing the YAML register file being filled field by field, with the reviewed_on and next_review lines highlighted at the end. |
| 9 | 4:56 | 37 | Here is a simple example. A developer in Manchester builds an internal meeting-notes summarizer for her own ten-person company using gpt-oss twenty B. The license is Apache 2.0. Her obligations are light: keep the license and notice files if she ever redistributes the weights, and follow OpenAI's usage policy. She is not redistributing anything, she is running it internally. So her register entry takes five minutes, and the answer is a clear yes. Simple cases should be simple. The register just proves you checked. | Simple example: internal summarizer<br>• gpt-oss-20b, Apache-2.0<br>• Internal use, no redistribution<br>• Obligations: notices if redistributed, usage policy<br>• Register entry: 5 minutes | Single register card on screen with fields populating one per beat: model, license, use, redistribution: no, decision: approved. A green approved stamp lands at the end. |
| 10 | 5:33 | 41 | A few pitfalls I see again and again. Teams assume a family has one license, then upgrade to a new release with different terms without anyone noticing. Engineers download a quantized copy from a stranger's repository where the license file is simply missing. Designers forget the attribution line in the product interface and the documentation. And people read Apache 2.0 as permission to ignore responsible-use policies and the law. The fix for all four is the same: make the register entry a required gate before any model reaches staging, and re-check it on every upgrade. | Pitfalls<br>• One license per family? No<br>• Missing license files in re-uploads<br>• Forgotten attribution<br>• Permissive license is not a legal shield | Four red flags pop up on a timeline from download to production; a green gate labeled "register entry" slides in front of the staging stage. |
| 11 | 6:14 | 33 | Try this now. Pick one model you are curious about, open its model card, and find three things: the exact license name, whether there is a separate acceptable use or usage policy, and whether there are any attribution or naming rules. Write them into the YAML template from the lesson. If you cannot find one of them within five minutes, that itself is a finding. Flag it for review before anyone builds on that model. | Try this now<br>• Open one model card<br>• Find: license, usage policy, attribution/naming<br>• Fill the YAML register<br>• Can't find it in 5 minutes? Flag it | Browser-style frame showing a generic model card; three highlight boxes animate over license, policy and attribution sections, then the values fly into a YAML file on the right. |
| 12 | 6:47 | 67 | Watch me do it. I will fill in a register entry for a model from scratch. I open the model's page and scroll to the license section. It says Apache 2.0, so I type that in. Next I search the page for the words usage policy and find a link to the publisher's policy; I paste the link into additional policies. Then I check for attribution rules: Apache asks me to keep the license and notice files if I redistribute, so I note that. Naming rules: none. Geographic restrictions: I search for European Union and region, and find nothing, so I write none found, re-check on upgrade. Finally, I note how we will use it: internal only, a quantized copy built from the official weights. I add the reviewer and a review date six months out. Seven fields, eight minutes, and the next person who asks can we use this gets an answer immediately. | Watch me do it<br>1 Find the license<br>2 Search for a usage policy<br>3 Attribution + naming rules<br>4 Geographic limits<br>5 Use, reviewer, review date | Split screen: left, a generic model card scrolling with search highlights on "license" and "policy"; right, the YAML register file with fields filling in one by one; a timer in the corner reads 8 minutes. |
| 13 | 7:54 | 31 | To recap. Check licenses before benchmarks. Apache and MIT are permissive but still carry notices and sometimes usage policies. Llama's community license adds a user threshold, branding and naming rules, and an acceptable use policy with an EU limit on multimodal models. Licenses belong to releases, and quantized copies inherit them. Your next step: fill in a register entry for one model you are considering, and link the license text itself. | Recap<br>• License before benchmark<br>• Know Apache, MIT and Llama terms<br>• Next: write one register entry | Recap card with three icons, then a blank YAML file with a blinking cursor. |

<details><summary>Narration per scene (paste into ElevenLabs)</summary>

Scene 1 — Licenses decoded

```text
Picture this. Your team spends six weeks building on the model that topped your benchmark. Launch review arrives, legal reads the license, and the answer is no. Six weeks gone. It happens more than you would think. In this lesson you will learn to read model licenses like a practitioner, know which families use which terms right now, and keep a simple register so this never catches you out. One note first: this is practical guidance, not legal advice.
```

Scene 2 — Analogy: a shop lease

```text
Think of a model license like a lease on a shop. Some leases say: use it for anything, just keep the landlord's sign on the wall. That is roughly Apache or MIT. Others say: you can run your business here, but you must display our brand at the entrance, name your shop after us, follow our house rules, and if you become enormous you need to renegotiate. That is closer to a community license like Llama's. Neither lease is bad. But you would never sign a lease without reading it, and you should never ship a model without reading its license.
```

Scene 3 — Permissive licenses

```text
Start with the permissive two. Apache 2.0 lets you use, modify and distribute, including commercially. You keep the copyright and license notices, mark significant changes, and pass on any NOTICE file. It also includes an explicit patent grant from contributors. MIT is even shorter: keep the notice, and do almost anything, but there is no explicit patent clause. Right now, gpt-oss, Gemma 4, the Mistral 3 family and most Qwen three and three point five open weights use Apache 2.0. DeepSeek and Microsoft's Phi-4 family use MIT. Always confirm on the specific model card.
```

Scene 4 — Llama 4 Community License

```text
Now Meta's Llama 4 Community License. It is free for most commercial use, with conditions. If your products had more than seven hundred million monthly active users on the release date, you need a separate license from Meta. You must show "Built with Llama", and derivative model names must start with the word Llama. And you must follow the Acceptable Use Policy, which says that for the multimodal Llama 4 models, rights are not granted to individuals or companies based in the European Union, though end users of a product built on them are not covered by that restriction.
```

Scene 5 — The release, not the brand

```text
Here is the trap. Licenses belong to a specific release, not to a brand. Earlier Gemma versions used Google's own terms, and Gemma 4 moved to Apache. Some older Qwen sizes used Alibaba's own licenses. And when someone uploads a quantized copy of a model, it inherits the original license. They cannot relabel it. So "we use Qwen" is not an answer to a license question. "We use this exact model file, under this license text" is.
```

Scene 6 — Three more checks

```text
Three more checks beyond the weights. First, outputs: some terms restrict using a model's outputs to train competing models, which matters if you plan to distil. Second, provenance: download from the publisher or a trusted mirror, and make sure the license file is included. Third, regulation: data protection law and rules like the EU AI Act apply to your use case, whatever the license says. Apache does not make an unlawful use lawful.
```

Scene 7 — Worked example: Lahore SaaS

```text
Let's triage a real-style case. A Lahore start-up sells a WhatsApp support assistant to retailers across Pakistan and the Gulf, and plans to sign an Irish customer next year. They shortlist a Llama 4 model, a Qwen three point five model and gpt-oss twenty B. They are nowhere near seven hundred million users, but Llama would require attribution in their product, and their legal team wants zero ambiguity for future EU customers. So they shortlist the two Apache models and keep Llama only as a benchmark reference.
```

Scene 8 — Hands-on: model register

```text
Your hands-on tool is a model register: one small YAML file per model you deploy. It records the exact model, the source, the license, extra policies, attribution and naming rules, geographic limits, whether outputs can train other models, who reviewed it and when to review again. The template is in the lesson. It takes ten minutes to fill in and it turns license review from a memory test into a checklist.
```

Scene 9 — Simple example: internal summarizer

```text
Here is a simple example. A developer in Manchester builds an internal meeting-notes summarizer for her own ten-person company using gpt-oss twenty B. The license is Apache 2.0. Her obligations are light: keep the license and notice files if she ever redistributes the weights, and follow OpenAI's usage policy. She is not redistributing anything, she is running it internally. So her register entry takes five minutes, and the answer is a clear yes. Simple cases should be simple. The register just proves you checked.
```

Scene 10 — Pitfalls

```text
A few pitfalls I see again and again. Teams assume a family has one license, then upgrade to a new release with different terms without anyone noticing. Engineers download a quantized copy from a stranger's repository where the license file is simply missing. Designers forget the attribution line in the product interface and the documentation. And people read Apache 2.0 as permission to ignore responsible-use policies and the law. The fix for all four is the same: make the register entry a required gate before any model reaches staging, and re-check it on every upgrade.
```

Scene 11 — Try this now

```text
Try this now. Pick one model you are curious about, open its model card, and find three things: the exact license name, whether there is a separate acceptable use or usage policy, and whether there are any attribution or naming rules. Write them into the YAML template from the lesson. If you cannot find one of them within five minutes, that itself is a finding. Flag it for review before anyone builds on that model.
```

Scene 12 — Watch me do it

```text
Watch me do it. I will fill in a register entry for a model from scratch. I open the model's page and scroll to the license section. It says Apache 2.0, so I type that in. Next I search the page for the words usage policy and find a link to the publisher's policy; I paste the link into additional policies. Then I check for attribution rules: Apache asks me to keep the license and notice files if I redistribute, so I note that. Naming rules: none. Geographic restrictions: I search for European Union and region, and find nothing, so I write none found, re-check on upgrade. Finally, I note how we will use it: internal only, a quantized copy built from the official weights. I add the reviewer and a review date six months out. Seven fields, eight minutes, and the next person who asks can we use this gets an answer immediately.
```

Scene 13 — Recap

```text
To recap. Check licenses before benchmarks. Apache and MIT are permissive but still carry notices and sometimes usage policies. Llama's community license adds a user threshold, branding and naming rules, and an acceptable use policy with an EU limit on multimodal models. Licenses belong to releases, and quantized copies inherit them. Your next step: fill in a register entry for one model you are considering, and link the license text itself.
```

</details>

### 3. Model families in 2026 and how to shortlist

Lesson `model-families-and-selection` · module “The open-weight landscape” · target 9 min · 14 scenes · 1205 words · planned 8:43 (words ≈ 8:44) · status: **to produce**

| Term | Say |
|---|---|
| MoE | M O E, mixture of experts |
| Qwen | chwen |
| Ministral | mini-STRAL |
| Phi | fye |
| MXFP4 | M X F P four |
| JSONL | jason lines |

| # | Time | Sec | Narration | On screen | Visual |
|---|---|---|---|---|---|
| 1 | 0:00 | 29 | Every week a new open model claims the top spot somewhere. If you chase leaderboards you will never ship. So in this lesson you will learn the handful of dimensions that decide whether a model fits your job, get a snapshot of the major families as of September twenty twenty-six, and walk away with a four-step shortlist method and a tiny evaluation harness you can run today. | Model families and how to shortlist<br>• Dimensions, not leaderboards<br>• 2026 family snapshot<br>• Four-step method + harness | Fast montage of abstract leaderboard rows reshuffling, then freeze and blur, replaced by a clean checklist titled "Dimensions". |
| 2 | 0:29 | 40 | Choosing a model is a lot like hiring. You would not hire the candidate with the most impressive CV for every role. For a receptionist role you care about languages, manners and reliability. For an analyst role, reasoning. And you always run your own interview, because a CV is written to impress. Model cards and leaderboards are CVs. Your evaluation set is the interview. And just like hiring, the best candidate is often not the most senior one, it is the one who does this particular job well at a sensible cost. | Analogy: hiring for a role<br>• Leaderboards = CVs<br>• Your eval set = the interview<br>• Best fit, not most senior | Three candidate cards slide in with CV-style bullet points. An interview panel icon appears; each card receives a score from a small eval checklist, and the mid-size card is highlighted as the hire. |
| 3 | 1:09 | 45 | First, size. As a rough guide, one to four billion parameters suits phones and simple tasks, seven to fifteen billion suits laptops and many business tasks, twenty to forty billion gives workstation-class quality, and above a hundred billion is server territory. Second, dense versus mixture of experts. A dense model uses every parameter for every token. A mixture-of-experts model routes each token through a few experts. So it has total parameters, which must fit in memory, and active parameters, which drive speed. gpt-oss one-twenty B has about one hundred seventeen billion total but only about five billion active per token. Big memory, fast generation. | Size and architecture<br>• 1–4B phone · 7–15B laptop · 20–40B workstation · 100B+ server<br>• Dense: all parameters per token<br>• MoE: total (memory) vs active (speed) | Diagram: a dense block lighting up entirely for each token, next to an MoE grid of 16 small blocks where only 2 light up per token. Labels "total" and "active" appear. |
| 4 | 1:54 | 30 | The other dimensions: reasoning mode, where thinking variants trade speed for better multi-step answers. Context length, where the advertised maximum is not the same as quality at that length. Modalities: text only, or images and audio too. Languages: if your users write Arabic, Urdu, or Roman Urdu mixed with English, test those specifically. Tool calling and structured output, which matter for agents. And license, which we covered last lesson. | More dimensions<br>• Reasoning mode<br>• Real vs advertised context<br>• Modalities and languages<br>• Tool calling, structured output, license | Radar chart with eight axes filling in for an example model; each axis label pulses as it is mentioned. |
| 5 | 2:24 | 43 | Now the snapshot, and please treat it as a snapshot. OpenAI's gpt-oss, twenty B and one-twenty B, are Apache licensed reasoning models shipped natively quantized. Google's Gemma 4 spans edge sizes to around thirty-one billion, now under Apache. Alibaba's Qwen three and three point five cover tiny to huge, mostly Apache. DeepSeek publishes very large mixture-of-experts reasoning models under MIT. Mistral 3 includes the Large 3 mixture-of-experts and the Ministral 3 edge models, all Apache. Meta's Llama 4 uses its community license. And Microsoft's Phi-4 family covers small models under MIT. Many other publishers ship strong open weights too. | Snapshot: September 2026<br>• gpt-oss · Gemma 4 · Qwen3/3.5 · DeepSeek<br>• Mistral 3 · Llama 4 · Phi-4<br>• Verify every model card | Grid of seven neutral family cards (text only, no logos), each flipping to show size range and license. A "snapshot, verify" stamp in the corner. |
| 6 | 3:07 | 33 | Here is the method. Step one, filter on hard constraints: license, languages, modalities, the memory you can afford, and the context you actually need. Step two, pick three to five candidates across sizes, one small, one mid, one large. Step three, run your own evaluation set, thirty to a hundred real tasks, at the exact quantization you will deploy. Step four, choose the smallest model that passes, then measure latency and cost on the real hardware. | Four-step shortlist<br>1 Filter on hard constraints<br>2 Pick 3–5 candidates across sizes<br>3 Evaluate on your tasks at deploy precision<br>4 Choose the smallest that passes | Funnel animation: dozens of dots enter, constraints filter to five, evaluation filters to two, and one small dot is highlighted at the bottom. |
| 7 | 3:40 | 35 | Example. A Manchester agency drafts social posts in English and Arabic for Gulf clients and needs clean JSON campaign briefs. Constraints: permissive license, strong Arabic, a twenty-four gigabyte GPU per workstation. They test four candidates on forty English and forty Arabic prompts rated by bilingual editors, plus thirty JSON prompts. In this illustrative run, two models tie within noise, one breaks JSON too often and drops out, and they pick the smaller of the rest. That is the discipline in action. | Worked example: Manchester agency (illustrative)<br>• English + Arabic drafts, JSON briefs<br>• 24 GB GPU budget<br>• 4 candidates → smallest passing model | Split screen: English and Arabic post drafts on the left, a JSON validator on the right showing green and red ticks; a results table fades in with one row highlighted. |
| 8 | 4:15 | 33 | The harness in your lesson is deliberately small. It talks to any OpenAI-compatible local server, which Ollama, llama dot cpp, LM Studio and vLLM all provide. It reads tasks from a JSON lines file, runs each candidate at temperature zero, checks for required keywords, and prints pass rate and median latency. Keyword checks are crude, so for real decisions you will add human ratings or a model judge, which we cover in the benchmarking lesson. | Hands-on harness<br>• Any OpenAI-compatible local server<br>• tasks.jsonl with must_include keywords<br>• Pass rate + median latency per model | Screen recording: VS Code with eval_shortlist.py, then a terminal printing two result lines for two models. |
| 9 | 4:48 | 43 | Watch for three quiet failures. The first is evaluating a model at full precision on a borrowed cloud GPU, then deploying a four-bit version on a laptop and never re-testing. Quantization can change behavior, so evaluate what you ship. The second is trusting a public leaderboard that never tested your language or your task. The third is the chat template. Every model expects its conversation formatted in a specific way, and a runtime that applies the wrong template will quietly make a good model look mediocre. If one candidate looks strangely weak, check the template before you drop it. | Quiet failures<br>• Evaluating full precision, shipping 4-bit<br>• Leaderboards without your language or task<br>• Wrong chat template | Three short vignettes: a precision slider dropping from 16 to 4 bits, a leaderboard with a missing language column, and a chat transcript whose role markers are scrambled then corrected. |
| 10 | 5:31 | 39 | A simple example before the agency one. A café chain in Karachi wants to classify customer feedback from its app into five buckets: taste, service, price, cleanliness and other. English and Roman Urdu. Constraints: runs on one small office PC with a sixteen gigabyte GPU. Candidates: a small Qwen model, Gemma 4's smaller variant, and Phi-4-mini. They label forty real comments, run the harness, and two models score almost the same. They pick the smaller one because it answers in under a second. Three candidates, forty examples, one afternoon. | Simple example: café feedback (illustrative)<br>• 5 buckets, English + Roman Urdu<br>• 16 GB GPU budget<br>• 3 small candidates, 40 labeled comments<br>• Pick the smaller of two ties | Five bucket icons across the top; comment bubbles drop into them. Below, a three-row results table fills in with scores and latency; the smallest model row turns green. |
| 11 | 6:10 | 34 | Finally, plan for change. Whatever you choose today will be overtaken within months. So make switching cheap. Keep your prompts, evaluation set and harness independent of any one model. Use OpenAI-compatible endpoints so swapping is a configuration change. And schedule a quarterly re-shortlist: run the same evaluation on two or three new candidates and switch only when the evidence says so. That is how you benefit from the fast pace of open models without being whipped around by it. | Plan for change<br>• Model-independent prompts and evals<br>• OpenAI-compatible endpoints<br>• Quarterly re-shortlist on the same eval | Timeline with quarterly markers; at each marker, two or three new model cards are tested against the same evaluation box, and one occasionally replaces the current model. |
| 12 | 6:44 | 27 | Try this now. Write down your hard constraints for one task: license, languages, modalities, memory budget and context length. Then list three candidate models of different sizes that meet them. Do not evaluate yet. Just the shortlist. You will be surprised how many models drop out at the constraint step, and how much time that saves before you run a single test. | Try this now<br>• Write 5 hard constraints<br>• List 3 candidates of different sizes<br>• Notice how many drop out early | A funnel graphic; five constraint labels appear on its rim one by one, and a crowd of model dots enters the top, with only three dots exiting the bottom. |
| 13 | 7:11 | 63 | Watch me do it. My task: classify customer reviews for an e-commerce seller into five topics, in English and Arabic. Constraints first: permissive license, Arabic support, a sixteen gigabyte GPU. I pull three candidates of different sizes with Ollama, a small, a mid-size and a larger quantized model. I open my tasks file: forty reviews, each with the expected topic. I run the harness from the lesson against all three. The output prints pass rates and median latency. The small model gets thirty-one of forty, the mid-size thirty-seven, the larger thirty-eight but twice as slow. I read the three failures of the mid-size model: two are genuinely ambiguous reviews, one is a sarcastic Arabic comment. I decide on the mid-size model, add the sarcastic case to the tasks file for future comparisons, and write the result into a small table with license, pass rate and latency. | Watch me do it<br>1 Constraints first<br>2 Pull 3 sizes<br>3 Run 40 tasks<br>4 Read the failures<br>5 Pick the smallest that passes | Terminal recording: three ollama pull commands, then the harness printing three result lines (31/40, 37/40, 38/40 with latencies); a failures view highlights one Arabic review; a results table is saved. |
| 14 | 8:14 | 29 | Recap. Pick models on dimensions, not headlines. Remember that mixture-of-experts models need memory for all their parameters but compute with a few. Know the major families, and verify every model card. Shortlist three to five, evaluate on your own tasks at deployment precision, and choose the smallest that passes. Your next step: write thirty real tasks from your job and run the harness on two local models. | Recap<br>• Dimensions over leaderboards<br>• Smallest model that passes<br>• Next: 30 tasks, two models, one table | Recap card, then an empty shortlist table with columns Model, License, Pass rate, Latency. |

<details><summary>Narration per scene (paste into ElevenLabs)</summary>

Scene 1 — Model families and how to shortlist

```text
Every week a new open model claims the top spot somewhere. If you chase leaderboards you will never ship. So in this lesson you will learn the handful of dimensions that decide whether a model fits your job, get a snapshot of the major families as of September twenty twenty-six, and walk away with a four-step shortlist method and a tiny evaluation harness you can run today.
```

Scene 2 — Analogy: hiring for a role

```text
Choosing a model is a lot like hiring. You would not hire the candidate with the most impressive CV for every role. For a receptionist role you care about languages, manners and reliability. For an analyst role, reasoning. And you always run your own interview, because a CV is written to impress. Model cards and leaderboards are CVs. Your evaluation set is the interview. And just like hiring, the best candidate is often not the most senior one, it is the one who does this particular job well at a sensible cost.
```

Scene 3 — Size and architecture

```text
First, size. As a rough guide, one to four billion parameters suits phones and simple tasks, seven to fifteen billion suits laptops and many business tasks, twenty to forty billion gives workstation-class quality, and above a hundred billion is server territory. Second, dense versus mixture of experts. A dense model uses every parameter for every token. A mixture-of-experts model routes each token through a few experts. So it has total parameters, which must fit in memory, and active parameters, which drive speed. gpt-oss one-twenty B has about one hundred seventeen billion total but only about five billion active per token. Big memory, fast generation.
```

Scene 4 — More dimensions

```text
The other dimensions: reasoning mode, where thinking variants trade speed for better multi-step answers. Context length, where the advertised maximum is not the same as quality at that length. Modalities: text only, or images and audio too. Languages: if your users write Arabic, Urdu, or Roman Urdu mixed with English, test those specifically. Tool calling and structured output, which matter for agents. And license, which we covered last lesson.
```

Scene 5 — Snapshot: September 2026

```text
Now the snapshot, and please treat it as a snapshot. OpenAI's gpt-oss, twenty B and one-twenty B, are Apache licensed reasoning models shipped natively quantized. Google's Gemma 4 spans edge sizes to around thirty-one billion, now under Apache. Alibaba's Qwen three and three point five cover tiny to huge, mostly Apache. DeepSeek publishes very large mixture-of-experts reasoning models under MIT. Mistral 3 includes the Large 3 mixture-of-experts and the Ministral 3 edge models, all Apache. Meta's Llama 4 uses its community license. And Microsoft's Phi-4 family covers small models under MIT. Many other publishers ship strong open weights too.
```

Scene 6 — Four-step shortlist

```text
Here is the method. Step one, filter on hard constraints: license, languages, modalities, the memory you can afford, and the context you actually need. Step two, pick three to five candidates across sizes, one small, one mid, one large. Step three, run your own evaluation set, thirty to a hundred real tasks, at the exact quantization you will deploy. Step four, choose the smallest model that passes, then measure latency and cost on the real hardware.
```

Scene 7 — Worked example: Manchester agency (illustrative)

```text
Example. A Manchester agency drafts social posts in English and Arabic for Gulf clients and needs clean JSON campaign briefs. Constraints: permissive license, strong Arabic, a twenty-four gigabyte GPU per workstation. They test four candidates on forty English and forty Arabic prompts rated by bilingual editors, plus thirty JSON prompts. In this illustrative run, two models tie within noise, one breaks JSON too often and drops out, and they pick the smaller of the rest. That is the discipline in action.
```

Scene 8 — Hands-on harness

```text
The harness in your lesson is deliberately small. It talks to any OpenAI-compatible local server, which Ollama, llama dot cpp, LM Studio and vLLM all provide. It reads tasks from a JSON lines file, runs each candidate at temperature zero, checks for required keywords, and prints pass rate and median latency. Keyword checks are crude, so for real decisions you will add human ratings or a model judge, which we cover in the benchmarking lesson.
```

Scene 9 — Quiet failures

```text
Watch for three quiet failures. The first is evaluating a model at full precision on a borrowed cloud GPU, then deploying a four-bit version on a laptop and never re-testing. Quantization can change behavior, so evaluate what you ship. The second is trusting a public leaderboard that never tested your language or your task. The third is the chat template. Every model expects its conversation formatted in a specific way, and a runtime that applies the wrong template will quietly make a good model look mediocre. If one candidate looks strangely weak, check the template before you drop it.
```

Scene 10 — Simple example: café feedback (illustrative)

```text
A simple example before the agency one. A café chain in Karachi wants to classify customer feedback from its app into five buckets: taste, service, price, cleanliness and other. English and Roman Urdu. Constraints: runs on one small office PC with a sixteen gigabyte GPU. Candidates: a small Qwen model, Gemma 4's smaller variant, and Phi-4-mini. They label forty real comments, run the harness, and two models score almost the same. They pick the smaller one because it answers in under a second. Three candidates, forty examples, one afternoon.
```

Scene 11 — Plan for change

```text
Finally, plan for change. Whatever you choose today will be overtaken within months. So make switching cheap. Keep your prompts, evaluation set and harness independent of any one model. Use OpenAI-compatible endpoints so swapping is a configuration change. And schedule a quarterly re-shortlist: run the same evaluation on two or three new candidates and switch only when the evidence says so. That is how you benefit from the fast pace of open models without being whipped around by it.
```

Scene 12 — Try this now

```text
Try this now. Write down your hard constraints for one task: license, languages, modalities, memory budget and context length. Then list three candidate models of different sizes that meet them. Do not evaluate yet. Just the shortlist. You will be surprised how many models drop out at the constraint step, and how much time that saves before you run a single test.
```

Scene 13 — Watch me do it

```text
Watch me do it. My task: classify customer reviews for an e-commerce seller into five topics, in English and Arabic. Constraints first: permissive license, Arabic support, a sixteen gigabyte GPU. I pull three candidates of different sizes with Ollama, a small, a mid-size and a larger quantized model. I open my tasks file: forty reviews, each with the expected topic. I run the harness from the lesson against all three. The output prints pass rates and median latency. The small model gets thirty-one of forty, the mid-size thirty-seven, the larger thirty-eight but twice as slow. I read the three failures of the mid-size model: two are genuinely ambiguous reviews, one is a sarcastic Arabic comment. I decide on the mid-size model, add the sarcastic case to the tasks file for future comparisons, and write the result into a small table with license, pass rate and latency.
```

Scene 14 — Recap

```text
Recap. Pick models on dimensions, not headlines. Remember that mixture-of-experts models need memory for all their parameters but compute with a few. Know the major families, and verify every model card. Shortlist three to five, evaluate on your own tasks at deployment precision, and choose the smallest that passes. Your next step: write thirty real tasks from your job and run the harness on two local models.
```

</details>

### 4. Quantization: GGUF, AWQ, GPTQ, FP8 and MXFP4

Lesson `how-quantization-works` · module “Quantization and hardware sizing” · target 8 min · 13 scenes · 1182 words · planned 8:34 (words ≈ 8:34) · status: **to produce**

| Term | Say |
|---|---|
| GGUF | G G U F |
| AWQ | A W Q |
| GPTQ | G P T Q |
| FP8 | F P eight |
| MXFP4 | M X F P four |
| KV | K V |
| llama.cpp | llama dot C P P |
| vLLM | V L L M |

| # | Time | Sec | Narration | On screen | Visual |
|---|---|---|---|---|---|
| 1 | 0:00 | 31 | Here is a party trick that is actually an engineering superpower. Take a model file that weighs sixteen gigabytes and shrink it to about five, then watch it run faster, while answering almost as well. That is quantization. In this lesson you will learn what it really does, decode the alphabet soup of GGUF, AWQ, GPTQ, FP8 and MXFP4, and pick the right level for your hardware with evidence rather than guesswork. | Quantization<br>• Shrink the model, keep most of the quality<br>• Decode GGUF, AWQ, GPTQ, FP8, MXFP4<br>• Choose a level with evidence | A large glowing cube labeled 16 GB compresses smoothly into a smaller cube labeled about 5 GB, with a speedometer needle rising beside it. |
| 2 | 0:31 | 43 | Here is the analogy I like best. Imagine a photo with millions of colors. Now save it with a smaller palette of, say, two hundred and fifty-six colors. From arm's length, it looks almost the same, and the file is a fraction of the size. Look very closely at a sunset gradient, and you might see some banding. Quantization does the same to a model's weights. Eight bits is a rich palette, almost indistinguishable. Four bits is a careful smaller palette, still very good. Two bits is a poster with a handful of colors: recognizable, but you lose detail. | Analogy: color palettes<br>• 16-bit = full color photo<br>• 8-bit = rich palette, near identical<br>• 4-bit = smaller palette, still good<br>• 2-bit = poster: detail lost | The same abstract gradient image shown four times in a row, each with fewer color bands; a file-size label under each shrinks as the palette shrinks. |
| 3 | 1:14 | 37 | The idea is simple. Models are trained with each weight stored in sixteen bits, two bytes. Quantization stores each weight in fewer bits, eight, six, five or four. Here is how it keeps quality. Weights are grouped into small blocks, maybe thirty-two values each. For every block you keep one precise scale number, and for every weight a small integer. At runtime you multiply them back together. Because every block has its own scale, one unusual weight does not ruin the precision of the rest. | How it works<br>• Weights grouped into small blocks<br>• One precise scale per block<br>• Low-bit integer per weight<br>• weight ≈ scale × integer | Animated grid of 32 coloured numbers collapsing into one scale value and 32 small integers; an arrow shows multiplication restoring approximate values. |
| 4 | 1:51 | 31 | Why does smaller mean faster? When a model generates a token, it has to read almost all of its weights from memory. On most hardware, that memory read is the bottleneck, not the arithmetic. So if every weight takes a quarter of the bytes, each token needs a quarter of the reading. That is why a four-bit model on the same laptop often streams text noticeably faster than the sixteen-bit original. | Why smaller is faster<br>• Each token reads most weights<br>• Memory bandwidth is the bottleneck<br>• Fewer bytes per weight → faster tokens | Diagram of a memory chip feeding a processor through a pipe; the pipe carries fat blocks at 16-bit, then four times as many thin blocks at 4-bit in the same time. |
| 5 | 2:22 | 51 | Now the formats. GGUF is the single-file format used by llama dot cpp, Ollama and LM Studio. You will see names like Q eight zero, Q six K, Q five K M and Q four K M, which are different quantization recipes. AWQ protects the small set of weights that matter most, measured on sample activations, before going to four bits. GPTQ quantizes layer by layer and compensates the rounding error. Both are popular for GPU servers like vLLM. FP8 is an eight-bit floating-point format that recent data-center GPUs run natively. And MXFP4 is a four-bit microscaling format, the one gpt-oss ships in, which is why the one-twenty B model fits on a single eighty gigabyte GPU. | The formats<br>• GGUF: Q8_0, Q6_K, Q5_K_M, Q4_K_M (local)<br>• AWQ and GPTQ: 4-bit GPU serving<br>• FP8: native on recent GPUs<br>• MXFP4: gpt-oss native weights | Five labeled tiles arranged by where they run: a laptop zone containing GGUF, a server zone containing AWQ, GPTQ and FP8, and a special tile MXFP4 linked to a single 80 GB GPU icon. |
| 6 | 3:13 | 43 | How do you choose? For local use, start with this ladder. Q eight zero is near-original at half the size. Q six K and Q five K M are the sweet spot if memory allows. Q four K M is the common laptop default and works well for models around seven billion parameters and up. Three bits and below are for emergencies: when a bigger model would not otherwise fit, and only after testing. And here is the surprising part. A bigger model at four bits often beats a smaller model at eight bits. Often, not always. So you measure. | The local ladder<br>• Q8_0: near-original<br>• Q6_K / Q5_K_M: sweet spot<br>• Q4_K_M: common default<br>• ≤ 3-bit: only after testing | Vertical ladder graphic with rungs labeled by quant level, each showing file size shrinking and a small quality bar dipping slightly then sharply at the bottom rungs. |
| 7 | 3:56 | 43 | Let's fit a real-style case. An engineering consultancy in Riyadh wants a thirty-two billion parameter model on twenty-four gigabyte workstations. At sixteen bits the weights alone are about sixty-four gigabytes. No chance. At eight bits, thirty-two. Still no. At Q four K M, roughly eighteen to twenty gigabytes, which fits with room for a working context. They test it against a fourteen billion model at eight bits on fifty of their own questions. In their illustrative test, the bigger four-bit model wins on hard technical questions, the smaller one is quicker, so they use each where it fits. | Worked example: Riyadh consultancy (illustrative)<br>• 32B at 16-bit ≈ 64 GB ✗<br>• 8-bit ≈ 32 GB ✗<br>• Q4_K_M ≈ 18–20 GB ✓ on 24 GB<br>• Test vs 14B at Q8_0 | Three memory bars against a 24 GB line: two overflow in red, the third fits in green with a small KV cache slice on top. |
| 8 | 4:39 | 27 | Remember the other memory user: the KV cache. It stores attention information for every token in the conversation, for every user at once. With long documents and many users, it can outgrow the weights. Most runtimes can quantize the KV cache too, often to eight bits, with a small quality trade-off. We will put exact numbers on this in the next lesson. | Don't forget the KV cache<br>• Grows with context length<br>• Grows with concurrent users<br>• Can be quantized (e.g. 8-bit) | Stacked bar showing weights as a fixed block and KV cache growing upward as a context slider and a user-count slider increase. |
| 9 | 5:06 | 42 | A simple example. You have a laptop with sixteen gigabytes of memory and you want to run an eight billion parameter model. At sixteen bits, the weights alone are about sixteen gigabytes, so it will not fit alongside your operating system. At eight bits, about eight gigabytes: it fits, but tightly. At Q four K M, roughly five gigabytes: it fits comfortably with room for a decent context. So you try Q four K M first, run twenty of your own prompts, and if the answers look as good as the eight-bit version, you keep it. | Simple example: 8B on a 16 GB laptop<br>• 16-bit ≈ 16 GB ✗<br>• 8-bit ≈ 8 GB (tight)<br>• Q4_K_M ≈ 5 GB ✓<br>• Compare 20 prompts | A laptop outline with a 16 GB memory bar. Three model blocks try to slot in one after another: the first overflows red, the second fits tightly amber, the third fits green with space left. |
| 10 | 5:48 | 32 | Your hands-on option: make a quantization yourself with llama dot cpp. Convert a model to a sixteen-bit GGUF, optionally compute an importance matrix on text that looks like your real traffic, quantize to Q four K M, then measure perplexity and, more importantly, run your task evaluation. The commands are in the lesson. The pitfalls: trusting perplexity alone, pushing tiny models to very low bits, and mixing up which files belong to which runtime. | Hands-on + pitfalls<br>• convert → imatrix → quantize → evaluate<br>• Perplexity is not enough<br>• Tiny models degrade fast at low bits | Terminal screen recording: four commands run in sequence with progress bars; final output shows a perplexity number next to a task-score table. |
| 11 | 6:20 | 32 | Try this now. In Ollama or LM Studio, look up one model you use and find which quantization you are actually running. Ollama's show command will tell you, and LM Studio displays it next to the download. Many people are surprised to learn they have been running a four-bit model all along. Then download one level higher, run the same five prompts on both, and write down whether you can tell the difference. | Try this now<br>• Check which quant you are running<br>• Download one level higher<br>• Same 5 prompts on both<br>• Can you tell the difference? | Terminal panel showing an ollama show output with the quantization line highlighted, followed by a two-column comparison card for five prompts with tick marks. |
| 12 | 6:52 | 68 | Watch me do it. I have one model in two quantizations, Q four K M and Q eight zero, both downloaded as GGUF files. First I check sizes on disk: roughly five gigabytes versus eight and a half. Then I run llama bench on both with the same settings, and write down prompt processing and generation speed. The four-bit version generates noticeably faster on my laptop. Now quality. I run the same twenty prompts from my own work through both, at temperature zero, and paste the outputs side by side in a sheet. I score each pair: same quality, four-bit worse, or four-bit better. Result: seventeen the same, two slightly worse, one oddly better. The two worse ones are long numeric tables, where small errors crept in. So my decision: Q four K M for everyday drafting, Q eight zero for the table-heavy report workflow. Size, speed and task quality, all measured in under an hour. | Watch me do it<br>1 Compare file sizes<br>2 llama-bench speeds<br>3 Same 20 prompts, side by side<br>4 Score pairs<br>5 Choose per workflow | Terminal shows ls with two file sizes, then llama-bench output with pp and tg columns for both files; a spreadsheet with 20 paired outputs gets colored tags: same, worse, better. |
| 13 | 8:00 | 34 | Recap. Quantization stores weights in fewer bits, so models get smaller and usually faster, with a small precision cost. GGUF is for local runtimes; AWQ, GPTQ and FP8 are for GPU servers; MXFP4 is how gpt-oss ships. Q four K M is a sensible default, but a table of size, speed and task score is what makes the decision. Your next step: run two quant levels of one model on twenty of your tasks and fill in that table. | Recap<br>• Fewer bits → smaller and faster<br>• Right format for the right runtime<br>• Next: compare two quant levels on your tasks | Recap card and an empty comparison table with columns Quant, Size, Tokens per second, Task score. |

<details><summary>Narration per scene (paste into ElevenLabs)</summary>

Scene 1 — Quantization

```text
Here is a party trick that is actually an engineering superpower. Take a model file that weighs sixteen gigabytes and shrink it to about five, then watch it run faster, while answering almost as well. That is quantization. In this lesson you will learn what it really does, decode the alphabet soup of GGUF, AWQ, GPTQ, FP8 and MXFP4, and pick the right level for your hardware with evidence rather than guesswork.
```

Scene 2 — Analogy: color palettes

```text
Here is the analogy I like best. Imagine a photo with millions of colors. Now save it with a smaller palette of, say, two hundred and fifty-six colors. From arm's length, it looks almost the same, and the file is a fraction of the size. Look very closely at a sunset gradient, and you might see some banding. Quantization does the same to a model's weights. Eight bits is a rich palette, almost indistinguishable. Four bits is a careful smaller palette, still very good. Two bits is a poster with a handful of colors: recognizable, but you lose detail.
```

Scene 3 — How it works

```text
The idea is simple. Models are trained with each weight stored in sixteen bits, two bytes. Quantization stores each weight in fewer bits, eight, six, five or four. Here is how it keeps quality. Weights are grouped into small blocks, maybe thirty-two values each. For every block you keep one precise scale number, and for every weight a small integer. At runtime you multiply them back together. Because every block has its own scale, one unusual weight does not ruin the precision of the rest.
```

Scene 4 — Why smaller is faster

```text
Why does smaller mean faster? When a model generates a token, it has to read almost all of its weights from memory. On most hardware, that memory read is the bottleneck, not the arithmetic. So if every weight takes a quarter of the bytes, each token needs a quarter of the reading. That is why a four-bit model on the same laptop often streams text noticeably faster than the sixteen-bit original.
```

Scene 5 — The formats

```text
Now the formats. GGUF is the single-file format used by llama dot cpp, Ollama and LM Studio. You will see names like Q eight zero, Q six K, Q five K M and Q four K M, which are different quantization recipes. AWQ protects the small set of weights that matter most, measured on sample activations, before going to four bits. GPTQ quantizes layer by layer and compensates the rounding error. Both are popular for GPU servers like vLLM. FP8 is an eight-bit floating-point format that recent data-center GPUs run natively. And MXFP4 is a four-bit microscaling format, the one gpt-oss ships in, which is why the one-twenty B model fits on a single eighty gigabyte GPU.
```

Scene 6 — The local ladder

```text
How do you choose? For local use, start with this ladder. Q eight zero is near-original at half the size. Q six K and Q five K M are the sweet spot if memory allows. Q four K M is the common laptop default and works well for models around seven billion parameters and up. Three bits and below are for emergencies: when a bigger model would not otherwise fit, and only after testing. And here is the surprising part. A bigger model at four bits often beats a smaller model at eight bits. Often, not always. So you measure.
```

Scene 7 — Worked example: Riyadh consultancy (illustrative)

```text
Let's fit a real-style case. An engineering consultancy in Riyadh wants a thirty-two billion parameter model on twenty-four gigabyte workstations. At sixteen bits the weights alone are about sixty-four gigabytes. No chance. At eight bits, thirty-two. Still no. At Q four K M, roughly eighteen to twenty gigabytes, which fits with room for a working context. They test it against a fourteen billion model at eight bits on fifty of their own questions. In their illustrative test, the bigger four-bit model wins on hard technical questions, the smaller one is quicker, so they use each where it fits.
```

Scene 8 — Don't forget the KV cache

```text
Remember the other memory user: the KV cache. It stores attention information for every token in the conversation, for every user at once. With long documents and many users, it can outgrow the weights. Most runtimes can quantize the KV cache too, often to eight bits, with a small quality trade-off. We will put exact numbers on this in the next lesson.
```

Scene 9 — Simple example: 8B on a 16 GB laptop

```text
A simple example. You have a laptop with sixteen gigabytes of memory and you want to run an eight billion parameter model. At sixteen bits, the weights alone are about sixteen gigabytes, so it will not fit alongside your operating system. At eight bits, about eight gigabytes: it fits, but tightly. At Q four K M, roughly five gigabytes: it fits comfortably with room for a decent context. So you try Q four K M first, run twenty of your own prompts, and if the answers look as good as the eight-bit version, you keep it.
```

Scene 10 — Hands-on + pitfalls

```text
Your hands-on option: make a quantization yourself with llama dot cpp. Convert a model to a sixteen-bit GGUF, optionally compute an importance matrix on text that looks like your real traffic, quantize to Q four K M, then measure perplexity and, more importantly, run your task evaluation. The commands are in the lesson. The pitfalls: trusting perplexity alone, pushing tiny models to very low bits, and mixing up which files belong to which runtime.
```

Scene 11 — Try this now

```text
Try this now. In Ollama or LM Studio, look up one model you use and find which quantization you are actually running. Ollama's show command will tell you, and LM Studio displays it next to the download. Many people are surprised to learn they have been running a four-bit model all along. Then download one level higher, run the same five prompts on both, and write down whether you can tell the difference.
```

Scene 12 — Watch me do it

```text
Watch me do it. I have one model in two quantizations, Q four K M and Q eight zero, both downloaded as GGUF files. First I check sizes on disk: roughly five gigabytes versus eight and a half. Then I run llama bench on both with the same settings, and write down prompt processing and generation speed. The four-bit version generates noticeably faster on my laptop. Now quality. I run the same twenty prompts from my own work through both, at temperature zero, and paste the outputs side by side in a sheet. I score each pair: same quality, four-bit worse, or four-bit better. Result: seventeen the same, two slightly worse, one oddly better. The two worse ones are long numeric tables, where small errors crept in. So my decision: Q four K M for everyday drafting, Q eight zero for the table-heavy report workflow. Size, speed and task quality, all measured in under an hour.
```

Scene 13 — Recap

```text
Recap. Quantization stores weights in fewer bits, so models get smaller and usually faster, with a small precision cost. GGUF is for local runtimes; AWQ, GPTQ and FP8 are for GPU servers; MXFP4 is how gpt-oss ships. Q four K M is a sensible default, but a table of size, speed and task score is what makes the decision. Your next step: run two quant levels of one model on twenty of your tasks and fill in that table.
```

</details>

### 5. VRAM and memory sizing: the formula

Lesson `vram-sizing-math` · module “Quantization and hardware sizing” · target 9 min · 13 scenes · 1226 words · planned 8:53 (words ≈ 8:53) · status: **to produce**

| Term | Say |
|---|---|
| VRAM | V RAM |
| KiB | kibibytes |
| GiB | gibibytes |
| GQA | G Q A |
| config.json | config dot jason |

| # | Time | Sec | Narration | On screen | Visual |
|---|---|---|---|---|---|
| 1 | 0:00 | 37 | A client once asked me to approve a purchase order for a GPU server. I asked one question: how many users, at what context length? Nobody knew. Ten minutes with a calculator showed the card they wanted could serve about a third of their team at the context they needed. In this lesson you will learn that ten-minute calculation: how much memory a model needs, how much context you can offer, how many users fit on one card, and roughly how fast it will talk. | Memory sizing: the formula<br>• Will it fit?<br>• How much context, how many users?<br>• How fast will it generate? | A purchase order document on a desk with a calculator next to it; numbers tick up on the calculator and the order gets a "revise" sticky note. |
| 2 | 0:37 | 42 | Think of GPU memory as a restaurant. The weights are the kitchen: a fixed space you need no matter how many guests arrive. The KV cache is the tables: every guest needs a table, and a guest with a long meal, a long conversation, needs a bigger table. Overhead is the corridors and storage. You can have the best kitchen in town, but if you only have room for three tables, you can only serve three guests at a time. Sizing is simply checking that the kitchen, the tables and the corridors all fit in the building. | Analogy: a restaurant<br>• Weights = kitchen (fixed)<br>• KV cache = tables (per guest, grows with conversation)<br>• Overhead = corridors<br>• All must fit in the building | Floor plan animation of a restaurant: a kitchen block appears first, then tables appear one by one for guests, stretching longer for long conversations, until the floor plan fills and a "full" sign lights. |
| 3 | 1:19 | 37 | Here is the whole formula. Total memory is roughly weights plus KV cache plus overhead. Weights in bytes equal parameters times effective bits per weight, divided by eight. KV cache equals two, for keys and values, times layers, times key-value heads, times head dimension, times bytes per value, times tokens in context, times concurrent sequences. And overhead covers the runtime and buffers, often a gigabyte or three, but measure it. You will find layers, key-value heads and head dimension in the model's config dot json file. | total ≈ weights + KV cache + overhead<br>• weights ≈ params × bits ÷ 8<br>• KV ≈ 2 × layers × kv_heads × head_dim × bytes × tokens × users<br>• overhead: measure | The formula builds term by term on a dark board; each term links by a thin line to the matching field in a config.json snippet on the right. |
| 4 | 1:56 | 34 | Two details trip people up. First, use key-value heads, not attention heads. Most modern models use grouped-query attention, where many attention heads share fewer key-value heads, and that makes the cache much smaller. Second, use the effective bits. Q four K M is roughly four and a half to five bits per weight once you count the scales. And for mixture-of-experts models, weights memory uses the total parameters, all experts, even though only a few run per token. | Details that matter<br>• Use KV heads (GQA), not attention heads<br>• Use effective bits (Q4_K_M ≈ 4.5–5)<br>• MoE: total parameters for memory | Diagram of eight query heads sharing two key-value heads with arrows converging; then a small label shows 4 bits plus scale overhead equals roughly 4.8 bits. |
| 5 | 2:30 | 45 | Let's do the laptop example together, with illustrative numbers. An eight billion parameter model with thirty-two layers, eight key-value heads and a head dimension of one twenty-eight. At Q four K M the weights are about four point eight gigabytes. The KV cache per token is two times thirty-two times eight times one twenty-eight times two bytes. That is a hundred and twenty-eight kibibytes per token. At eight thousand tokens, one gibibyte. At thirty-two thousand, four. Add overhead, and a thirty-two thousand token context needs around ten to eleven gigabytes. Comfortable at eight k on a sixteen gigabyte laptop, tight at thirty-two k. | Laptop example (illustrative)<br>• Weights (Q4_K_M) ≈ 4.8 GB<br>• KV ≈ 128 KiB per token<br>• 8k ctx ≈ 1 GiB · 32k ctx ≈ 4 GiB<br>• Total at 32k ≈ 10–11 GB | Animated stacked bar filling up against a 16 GB laptop line: weights block, KV block growing as a context slider moves from 8k to 32k, overhead cap on top. |
| 6 | 3:15 | 52 | Now a team server. A Karachi fintech wants a thirty-two billion model for twenty analysts, each with eight thousand tokens of context. With a model of sixty-four layers, the KV cache is about two hundred fifty-six kibibytes per token. Twenty users times eight thousand tokens is about one hundred sixty-four thousand tokens, around forty gibibytes of cache. With eight-bit weights of thirty-two gigabytes, you are near seventy-six gigabytes. Too tight for an eighty gigabyte card. Switch to four-bit weights and an eight-bit KV cache and you are around forty-two gigabytes. Now it fits comfortably. Real servers allocate the cache on demand, so real capacity is often better, but you size for the worst case you care about and then load-test. | Team server (illustrative)<br>• 20 users × 8k tokens<br>• FP8 weights + FP16 KV ≈ 76 GB: too tight<br>• 4-bit weights + 8-bit KV ≈ 42 GB: fits<br>• Then load-test | Two side-by-side stacked bars against an 80 GB line: the first nearly touches it in amber, the second sits well below in green. Twenty small user icons above both. |
| 7 | 4:07 | 44 | Sizing is half the story. The other half is speed. When generating for a single user, a good ceiling is memory bandwidth divided by the bytes read per token. A card with about a thousand gigabytes per second of bandwidth, reading a model of about five gigabytes, tops out around two hundred tokens a second in theory, and real numbers are lower. That is also why mixture-of-experts models feel fast: they read only the active experts. Reading your prompt, called prefill, is different. It is compute-bound, which is why a very long prompt takes a moment before the first word appears. | Speed ceiling<br>• tokens/s ≲ bandwidth ÷ bytes per token<br>• ~1,000 GB/s ÷ ~5 GB ≈ 200 tok/s ceiling (illustrative)<br>• MoE reads only active experts<br>• Prefill is compute-bound | A pipe from memory to compute with a flow-rate gauge; the formula appears beside it. Then a second phase shows a burst of parallel arrows labeled prefill before a steady stream labeled decode. |
| 8 | 4:51 | 36 | The lesson includes a small Python calculator. Download just the model's config file, run it for three scenarios, solo, team and peak, and then confirm with what your runtime actually reports. Ollama's ps command, the llama dot cpp load log, and vLLM's startup log all tell you real memory use. Common mistakes: using attention heads instead of key-value heads, forgetting concurrency, planning to use a hundred percent of memory, and promising the maximum advertised context when users need a fraction of it. | Hands-on + pitfalls<br>• vram_calc.py with config.json<br>• Confirm with runtime reports<br>• Avoid: wrong heads, no concurrency, 100% memory | Screen recording of running vram_calc.py three times with different arguments, then a split view with a vLLM startup log line reporting KV cache capacity highlighted. |
| 9 | 5:27 | 39 | One more quick calculation, a tiny one, so you can do it in your head. A three billion parameter model at eight bits needs about three gigabytes for weights. Suppose its KV cache is about a hundred kilobytes per token. For four thousand tokens and one user, that is about four hundred megabytes. Add a gigabyte of overhead. Total: about four and a half gigabytes. That fits on almost any modern laptop GPU or a recent phone-class device with enough memory. You just sized a model in thirty seconds. | Quick mental math (illustrative)<br>• 3B at 8-bit ≈ 3 GB<br>• KV ≈ 100 KB/token × 4k ≈ 0.4 GB<br>• + ~1 GB overhead<br>• ≈ 4.5 GB total | Three number tiles slide in and stack into a bar labeled 4.5 GB, which settles under a device outline with a green fit tick. |
| 10 | 6:06 | 35 | Here is how to present sizing to a decision-maker. One slide, three scenarios: solo use, normal team load and peak. For each, show weights, cache and overhead against the card's memory, with a clear green or red. Add the expected tokens per second and the maximum users at your chosen context. Then state the assumptions: model, quantization, context and concurrency. When someone asks what if we double the users, you can answer in a minute, because the formula is right there. | Presenting sizing<br>• One slide, three scenarios<br>• Stacked bars vs card memory<br>• Tokens/s and max users<br>• State your assumptions | A single presentation slide with three stacked bars against a memory line, green and red markers, and an assumptions box in the corner. |
| 11 | 6:41 | 34 | Try this now. Pick a model you use, find its config file on its model page, and write down three numbers: number of layers, key-value heads and head dimension. Multiply two times layers times key-value heads times head dimension times two bytes. That is your KV cache per token. Multiply by the context length you actually need. Compare it with the size of the weights. Most people are shocked at how big the cache gets at long context. | Try this now<br>• Find layers, KV heads, head_dim<br>• 2 × L × H × D × 2 bytes = KV/token<br>• × your context length<br>• Compare with weights | A config.json snippet with three fields highlighted in sequence; the numbers fly into the formula line and a result appears next to a weights bar for comparison. |
| 12 | 7:15 | 67 | Watch me do it. I open the config file of a fourteen billion parameter model. I find forty-eight hidden layers, eight key-value heads and a head dimension of one twenty-eight. Weights first: at Q four K M, about fourteen billion times four point eight bits, divided by eight, which is about eight point four gigabytes. KV cache per token: two times forty-eight times eight times one twenty-eight times two bytes, which is one hundred ninety-six thousand six hundred and eight bytes, about a hundred ninety-two kibibytes. My team wants eight thousand tokens each, for four people at once: that is thirty-two thousand tokens, about six gigabytes of cache. Add two gigabytes of overhead: about sixteen and a half gigabytes. My card has twenty-four. It fits, with headroom. Then I start the server and compare with the reported memory: within ten percent. All numbers here are illustrative, but the method is exactly what you will use. | Watch me do it (illustrative)<br>• 14B at Q4_K_M ≈ 8.4 GB<br>• KV ≈ 192 KiB/token<br>• 4 users × 8k ≈ 6 GB<br>• + 2 GB overhead ≈ 16.5 GB on a 24 GB card | Config.json on the left with three fields highlighted; a calculator panel on the right fills each formula step; a stacked bar settles under a 24 GB line; finally a runtime log line shows measured memory for comparison. |
| 13 | 8:22 | 31 | Recap. Total memory is weights plus KV cache plus overhead. Weights scale with parameters and bits. The KV cache scales with layers, key-value heads, head size, context and concurrent users. Speed is bounded by bandwidth divided by bytes per token. Estimate on paper, then confirm on hardware. Your next step: run the calculator for a model you are considering and write down the maximum users and context one card can support. | Recap<br>• weights + KV + overhead<br>• KV grows with context × users<br>• Next: size one model for three scenarios | Recap card, then a filled three-row scenario table with solo, team and peak, and a green tick on the chosen configuration. |

<details><summary>Narration per scene (paste into ElevenLabs)</summary>

Scene 1 — Memory sizing: the formula

```text
A client once asked me to approve a purchase order for a GPU server. I asked one question: how many users, at what context length? Nobody knew. Ten minutes with a calculator showed the card they wanted could serve about a third of their team at the context they needed. In this lesson you will learn that ten-minute calculation: how much memory a model needs, how much context you can offer, how many users fit on one card, and roughly how fast it will talk.
```

Scene 2 — Analogy: a restaurant

```text
Think of GPU memory as a restaurant. The weights are the kitchen: a fixed space you need no matter how many guests arrive. The KV cache is the tables: every guest needs a table, and a guest with a long meal, a long conversation, needs a bigger table. Overhead is the corridors and storage. You can have the best kitchen in town, but if you only have room for three tables, you can only serve three guests at a time. Sizing is simply checking that the kitchen, the tables and the corridors all fit in the building.
```

Scene 3 — total ≈ weights + KV cache + overhead

```text
Here is the whole formula. Total memory is roughly weights plus KV cache plus overhead. Weights in bytes equal parameters times effective bits per weight, divided by eight. KV cache equals two, for keys and values, times layers, times key-value heads, times head dimension, times bytes per value, times tokens in context, times concurrent sequences. And overhead covers the runtime and buffers, often a gigabyte or three, but measure it. You will find layers, key-value heads and head dimension in the model's config dot json file.
```

Scene 4 — Details that matter

```text
Two details trip people up. First, use key-value heads, not attention heads. Most modern models use grouped-query attention, where many attention heads share fewer key-value heads, and that makes the cache much smaller. Second, use the effective bits. Q four K M is roughly four and a half to five bits per weight once you count the scales. And for mixture-of-experts models, weights memory uses the total parameters, all experts, even though only a few run per token.
```

Scene 5 — Laptop example (illustrative)

```text
Let's do the laptop example together, with illustrative numbers. An eight billion parameter model with thirty-two layers, eight key-value heads and a head dimension of one twenty-eight. At Q four K M the weights are about four point eight gigabytes. The KV cache per token is two times thirty-two times eight times one twenty-eight times two bytes. That is a hundred and twenty-eight kibibytes per token. At eight thousand tokens, one gibibyte. At thirty-two thousand, four. Add overhead, and a thirty-two thousand token context needs around ten to eleven gigabytes. Comfortable at eight k on a sixteen gigabyte laptop, tight at thirty-two k.
```

Scene 6 — Team server (illustrative)

```text
Now a team server. A Karachi fintech wants a thirty-two billion model for twenty analysts, each with eight thousand tokens of context. With a model of sixty-four layers, the KV cache is about two hundred fifty-six kibibytes per token. Twenty users times eight thousand tokens is about one hundred sixty-four thousand tokens, around forty gibibytes of cache. With eight-bit weights of thirty-two gigabytes, you are near seventy-six gigabytes. Too tight for an eighty gigabyte card. Switch to four-bit weights and an eight-bit KV cache and you are around forty-two gigabytes. Now it fits comfortably. Real servers allocate the cache on demand, so real capacity is often better, but you size for the worst case you care about and then load-test.
```

Scene 7 — Speed ceiling

```text
Sizing is half the story. The other half is speed. When generating for a single user, a good ceiling is memory bandwidth divided by the bytes read per token. A card with about a thousand gigabytes per second of bandwidth, reading a model of about five gigabytes, tops out around two hundred tokens a second in theory, and real numbers are lower. That is also why mixture-of-experts models feel fast: they read only the active experts. Reading your prompt, called prefill, is different. It is compute-bound, which is why a very long prompt takes a moment before the first word appears.
```

Scene 8 — Hands-on + pitfalls

```text
The lesson includes a small Python calculator. Download just the model's config file, run it for three scenarios, solo, team and peak, and then confirm with what your runtime actually reports. Ollama's ps command, the llama dot cpp load log, and vLLM's startup log all tell you real memory use. Common mistakes: using attention heads instead of key-value heads, forgetting concurrency, planning to use a hundred percent of memory, and promising the maximum advertised context when users need a fraction of it.
```

Scene 9 — Quick mental math (illustrative)

```text
One more quick calculation, a tiny one, so you can do it in your head. A three billion parameter model at eight bits needs about three gigabytes for weights. Suppose its KV cache is about a hundred kilobytes per token. For four thousand tokens and one user, that is about four hundred megabytes. Add a gigabyte of overhead. Total: about four and a half gigabytes. That fits on almost any modern laptop GPU or a recent phone-class device with enough memory. You just sized a model in thirty seconds.
```

Scene 10 — Presenting sizing

```text
Here is how to present sizing to a decision-maker. One slide, three scenarios: solo use, normal team load and peak. For each, show weights, cache and overhead against the card's memory, with a clear green or red. Add the expected tokens per second and the maximum users at your chosen context. Then state the assumptions: model, quantization, context and concurrency. When someone asks what if we double the users, you can answer in a minute, because the formula is right there.
```

Scene 11 — Try this now

```text
Try this now. Pick a model you use, find its config file on its model page, and write down three numbers: number of layers, key-value heads and head dimension. Multiply two times layers times key-value heads times head dimension times two bytes. That is your KV cache per token. Multiply by the context length you actually need. Compare it with the size of the weights. Most people are shocked at how big the cache gets at long context.
```

Scene 12 — Watch me do it (illustrative)

```text
Watch me do it. I open the config file of a fourteen billion parameter model. I find forty-eight hidden layers, eight key-value heads and a head dimension of one twenty-eight. Weights first: at Q four K M, about fourteen billion times four point eight bits, divided by eight, which is about eight point four gigabytes. KV cache per token: two times forty-eight times eight times one twenty-eight times two bytes, which is one hundred ninety-six thousand six hundred and eight bytes, about a hundred ninety-two kibibytes. My team wants eight thousand tokens each, for four people at once: that is thirty-two thousand tokens, about six gigabytes of cache. Add two gigabytes of overhead: about sixteen and a half gigabytes. My card has twenty-four. It fits, with headroom. Then I start the server and compare with the reported memory: within ten percent. All numbers here are illustrative, but the method is exactly what you will use.
```

Scene 13 — Recap

```text
Recap. Total memory is weights plus KV cache plus overhead. Weights scale with parameters and bits. The KV cache scales with layers, key-value heads, head size, context and concurrent users. Speed is bounded by bandwidth divided by bytes per token. Estimate on paper, then confirm on hardware. Your next step: run the calculator for a model you are considering and write down the maximum users and context one card can support.
```

</details>

### 6. Hardware choices: laptops, workstations, servers and cloud GPUs

Lesson `hardware-choices` · module “Quantization and hardware sizing” · target 9 min · 14 scenes · 1256 words · planned 9:07 (words ≈ 9:06) · status: **to produce**

| Term | Say |
|---|---|
| UPS | U P S |
| TCO | T C O |
| MLX | M L X |
| FLOPS | flops |

| # | Time | Sec | Narration | On screen | Visual |
|---|---|---|---|---|---|
| 1 | 0:00 | 31 | Two companies buy the same expensive GPU. One runs it flat out serving hundreds of staff and pays it back in months. The other runs it five percent of the time and quietly burns money for two years. Same chip, opposite outcomes. In this lesson you will learn the five hardware tiers for running models, the trade-offs that actually matter, and a simple total cost sketch so your purchase is defensible. | Hardware choices<br>• Five tiers<br>• Trade-offs that matter<br>• A 24-month cost sketch | Split screen of two identical GPU boxes: one with a busy utilization graph near the top, the other with a flat line near zero and coins trickling away. |
| 2 | 0:31 | 40 | Buying AI hardware is like choosing vehicles for a delivery business. A motorbike is cheap and nimble, perfect for one parcel at a time: that is a laptop. A van carries a team's worth of parcels: that is a workstation GPU. A truck fleet moves a whole city's deliveries: that is a data-center cluster. Buying a truck to deliver one parcel a day wastes money, and sending a motorbike to move a warehouse fails. Match the vehicle to the load, and remember fuel, insurance and parking, which here are power, cooling and people. | Analogy: delivery vehicles<br>• Motorbike = laptop<br>• Van = workstation GPU<br>• Truck fleet = data-center cluster<br>• Running costs = power, cooling, people | Three vehicle icons line up left to right with growing size; parcel counts animate above each, and a small fuel gauge icon appears under each labeled power and cooling. |
| 3 | 1:11 | 46 | Tier one is CPU only: slow generation, but fine for small models, embeddings and background batch work. Tier two is Apple silicon, where the GPU shares a large pool of unified memory, great for individuals and prototypes. Tier three is a consumer NVIDIA card with sixteen to thirty-two gigabytes, perfect for one developer running models up to about thirty billion at four bits. Tier four is a workstation GPU with around forty-eight to ninety-six gigabytes, the sweet spot for a team server. And tier five is data-center accelerators and cloud GPUs for high concurrency and giant models. Specs change fast, so check current vendor sheets. | Five tiers<br>1 CPU only<br>2 Apple silicon (unified memory)<br>3 Consumer GPU (16–32 GB)<br>4 Workstation GPU (48–96 GB class)<br>5 Data-center / cloud | Five-step staircase diagram with a simple device silhouette on each step, from a small PC to a server rack, and a memory label under each. |
| 4 | 1:57 | 26 | The first trade-off is capacity versus bandwidth. Capacity decides whether a model fits. Bandwidth decides how fast it talks. A machine with a huge but slower memory pool can load a big model, yet generate more slowly than a smaller, faster GPU running a smaller model. So always ask two questions: will it fit, and how fast will it read. | Capacity vs bandwidth<br>• Capacity: will it fit?<br>• Bandwidth: how fast will it generate?<br>• Ask both, every time | Two containers: a wide tank with a thin outlet pipe and a smaller tank with a wide outlet; water flow speed visibly differs. |
| 5 | 2:23 | 35 | Mixture-of-experts models bend the rules. They need memory for every expert, but only a few experts work on each token. That makes high-memory machines, and setups that keep some experts in regular system memory, surprisingly usable for one or a few users. For many concurrent users, though, dedicated GPUs with a batching server still win. Multi-GPU setups can split a model across cards, but for one team server, one bigger card is usually simpler to run than several small ones. | MoE and multi-GPU<br>• MoE: big memory, light compute per token<br>• Offloading works for low concurrency<br>• One bigger card beats several small ones for simplicity | Animated MoE grid with experts partly in a GPU box and partly in a RAM box; then a two-GPU split with a thick interconnect cable highlighted. |
| 6 | 2:58 | 33 | Now the unglamorous parts that decide real outcomes. Power and cooling: a high-end workstation under load draws serious power, and in a Karachi or Riyadh summer, poor cooling means silent throttling. Plan a UPS and airflow. Cloud versus on-premise: cloud avoids upfront cost and scales for launches, on-premise wins with steady use, strict data locality and predictable bills. If data must stay in-country, choose a cloud region that meets that requirement, not just the cheapest one. | The unglamorous parts<br>• Power, cooling, UPS<br>• Cloud: flexible, no capex<br>• On-prem: steady use, locality, predictable cost<br>• Region for residency, not just price | A thermometer rising beside a GPU with a throughput gauge dropping; then a map with highlighted cloud regions and an in-country pin. |
| 7 | 3:31 | 37 | Three buyers, three answers. A solo consultant in London needs a private writing assistant: a high-memory Apple silicon laptop runs a fourteen to thirty-two billion model quietly on battery. A thirty-person agency in Lahore needs a shared assistant for about fifteen concurrent users: one workstation GPU, a batching server, and a UPS, with occasional cloud burst. A bank in Riyadh with hundreds of users and a strict on-premise policy needs a small cluster of data-center GPUs behind its own gateway, with a formal capacity plan. | Three buyers<br>• London consultant: high-memory laptop<br>• Lahore agency: one workstation GPU + vLLM<br>• Riyadh bank: on-prem data-center cluster | Three panels with simple illustrated settings: a laptop at a café table, a small office with a tower PC and a UPS, a secure server room with access badge reader. |
| 8 | 4:08 | 35 | Your hands-on tool is a twenty-four month cost sketch. On one side, hardware, power at your local tariff, cooling and UPS, admin hours, and resale value. On the other, cloud hourly rates times hours, storage, any committed-use discount, and admin time. Divide each by expected volume to get cost per thousand requests, and compare it with an API model at the same quality. If the API is cheaper and your data rules allow it, keep using the API until volume grows. | Hands-on: 24-month TCO<br>• On-prem vs cloud vs API<br>• Include power, cooling, admin time<br>• Compare cost per 1,000 requests | Spreadsheet-style screen with two columns filling in, then a bar chart comparing cost per 1,000 requests for on-prem, cloud and API. |
| 9 | 4:43 | 40 | A simple example. A two-person design studio in Dubai wants a private assistant for writing proposals. Two users, rarely at the same time, a fourteen billion model at four bits is plenty. Their options: a high-memory Apple silicon laptop they would buy anyway, or a cloud GPU rented by the hour. The laptop wins: it already exists in their budget, runs quietly, and keeps client proposals in the office. A GPU server would sit idle most of the day. Sometimes the right hardware decision is to buy nothing new at all. | Simple example: two-person studio<br>• 2 users, low concurrency<br>• 14B at 4-bit<br>• High-memory laptop wins<br>• Server would sit idle | Two-column comparison card: laptop vs cloud GPU, with rows for cost, noise, data location and utilization; ticks animate into the laptop column. |
| 10 | 5:23 | 43 | One more practical tip before you buy anything: rent first. Most cloud providers let you rent the exact GPU class you are considering by the hour. Spend a day running your real prompts at your expected concurrency, with the same engine and quantization you plan to use, and record throughput, latency and memory. That one day of rental often costs less than a single wrong component, and it turns your purchase request from a guess into a measured plan. If data rules prevent you from using real prompts in the cloud, use synthetic prompts with the same length and structure. | Rent before you buy<br>• Rent the target GPU class for a day<br>• Real prompts, real concurrency, same engine<br>• Measure throughput, latency, memory<br>• Synthetic prompts if data cannot leave | Calendar showing one highlighted day with a cloud GPU icon, then a results sheet with three measured numbers feeding into a purchase request form. |
| 11 | 6:06 | 52 | A question I get all the time: should we buy one big GPU or two smaller ones? For most single-team servers, one bigger card is simpler. The whole model fits in one place, you avoid splitting layers across cards, and there is one thing to monitor and cool. Two smaller cards make sense when the model does not fit on any single card you can buy, or when you want two independent replicas for resilience. And a second common question: is a Mac good enough for our team? For a few people using it one at a time, often yes. For twenty people hitting it at once, a dedicated GPU with a batching server will serve them far better. | FAQ<br>• One big GPU or two small? Usually one big, simpler<br>• Two cards: model won't fit, or replicas<br>• Mac for a team? Few users yes; many users → GPU server | Two Q&A cards flip in sequence; the first shows one large GPU vs two small GPUs with a "simpler" tick, the second shows a laptop with a few users vs a server with many. |
| 12 | 6:58 | 32 | Try this now. Write down your expected number of concurrent users at peak, and the context length they really need. Then place yourself on the five tiers. If you land on tier two or three, you probably do not need a server yet. If you land on tier four or five, your next step is to rent that exact GPU class in the cloud for a day and measure, before you buy anything. | Try this now<br>• Peak concurrent users?<br>• Real context length?<br>• Place yourself on the five tiers<br>• Tier 4–5: rent for a day first | The five-tier staircase from earlier reappears; a marker pin drops onto one step based on two slider inputs labeled users and context. |
| 13 | 7:30 | 67 | Watch me do it. A thirty-person office asks me what to buy. I start with numbers, not shopping. Peak concurrent users: I check their chat tool logs and see about twelve at the busiest hour. Context: most documents fit in eight thousand tokens. I run the sizing calculator for a fourteen billion model at four bits: comfortably inside a forty-eight gigabyte workstation card, with room for more users. Next I rent that GPU class in the cloud for an afternoon, serve the model with vLLM, and replay fifty real prompts at twelve concurrent requests. Latency looks good. Then the cost sketch: workstation price, power at their local tariff, a UPS, and two hours a month of admin, over twenty-four months, divided by expected requests. I compare with their current API bill. The workstation pays for itself in the second year, and it keeps client files in the office. That is the recommendation I send. | Watch me do it<br>1 Peak users from logs<br>2 Size the model<br>3 Rent + replay 50 prompts<br>4 24-month cost vs API<br>5 Recommend | Screen sequence: a chat-tool usage chart with a peak marker; the sizing calculator output; a cloud console showing a rented GPU; a latency chart; a spreadsheet cost comparison with a crossover month highlighted. |
| 14 | 8:37 | 30 | Recap. Start from sizing and the workload. Capacity decides fit, bandwidth decides speed. Unified memory is great for individuals, batching GPU servers are best for teams. Mixture-of-experts models need memory more than compute. And judge purchases on twenty-four month cost per thousand requests, including power, cooling and people. Your next step: fill in the cost template for two options and compare them with an API you already use. | Recap<br>• Fit and speed<br>• Right tier for the concurrency<br>• Next: 24-month TCO for two options | Recap card with three icons: memory chip, gauge, and calculator. |

<details><summary>Narration per scene (paste into ElevenLabs)</summary>

Scene 1 — Hardware choices

```text
Two companies buy the same expensive GPU. One runs it flat out serving hundreds of staff and pays it back in months. The other runs it five percent of the time and quietly burns money for two years. Same chip, opposite outcomes. In this lesson you will learn the five hardware tiers for running models, the trade-offs that actually matter, and a simple total cost sketch so your purchase is defensible.
```

Scene 2 — Analogy: delivery vehicles

```text
Buying AI hardware is like choosing vehicles for a delivery business. A motorbike is cheap and nimble, perfect for one parcel at a time: that is a laptop. A van carries a team's worth of parcels: that is a workstation GPU. A truck fleet moves a whole city's deliveries: that is a data-center cluster. Buying a truck to deliver one parcel a day wastes money, and sending a motorbike to move a warehouse fails. Match the vehicle to the load, and remember fuel, insurance and parking, which here are power, cooling and people.
```

Scene 3 — Five tiers

```text
Tier one is CPU only: slow generation, but fine for small models, embeddings and background batch work. Tier two is Apple silicon, where the GPU shares a large pool of unified memory, great for individuals and prototypes. Tier three is a consumer NVIDIA card with sixteen to thirty-two gigabytes, perfect for one developer running models up to about thirty billion at four bits. Tier four is a workstation GPU with around forty-eight to ninety-six gigabytes, the sweet spot for a team server. And tier five is data-center accelerators and cloud GPUs for high concurrency and giant models. Specs change fast, so check current vendor sheets.
```

Scene 4 — Capacity vs bandwidth

```text
The first trade-off is capacity versus bandwidth. Capacity decides whether a model fits. Bandwidth decides how fast it talks. A machine with a huge but slower memory pool can load a big model, yet generate more slowly than a smaller, faster GPU running a smaller model. So always ask two questions: will it fit, and how fast will it read.
```

Scene 5 — MoE and multi-GPU

```text
Mixture-of-experts models bend the rules. They need memory for every expert, but only a few experts work on each token. That makes high-memory machines, and setups that keep some experts in regular system memory, surprisingly usable for one or a few users. For many concurrent users, though, dedicated GPUs with a batching server still win. Multi-GPU setups can split a model across cards, but for one team server, one bigger card is usually simpler to run than several small ones.
```

Scene 6 — The unglamorous parts

```text
Now the unglamorous parts that decide real outcomes. Power and cooling: a high-end workstation under load draws serious power, and in a Karachi or Riyadh summer, poor cooling means silent throttling. Plan a UPS and airflow. Cloud versus on-premise: cloud avoids upfront cost and scales for launches, on-premise wins with steady use, strict data locality and predictable bills. If data must stay in-country, choose a cloud region that meets that requirement, not just the cheapest one.
```

Scene 7 — Three buyers

```text
Three buyers, three answers. A solo consultant in London needs a private writing assistant: a high-memory Apple silicon laptop runs a fourteen to thirty-two billion model quietly on battery. A thirty-person agency in Lahore needs a shared assistant for about fifteen concurrent users: one workstation GPU, a batching server, and a UPS, with occasional cloud burst. A bank in Riyadh with hundreds of users and a strict on-premise policy needs a small cluster of data-center GPUs behind its own gateway, with a formal capacity plan.
```

Scene 8 — Hands-on: 24-month TCO

```text
Your hands-on tool is a twenty-four month cost sketch. On one side, hardware, power at your local tariff, cooling and UPS, admin hours, and resale value. On the other, cloud hourly rates times hours, storage, any committed-use discount, and admin time. Divide each by expected volume to get cost per thousand requests, and compare it with an API model at the same quality. If the API is cheaper and your data rules allow it, keep using the API until volume grows.
```

Scene 9 — Simple example: two-person studio

```text
A simple example. A two-person design studio in Dubai wants a private assistant for writing proposals. Two users, rarely at the same time, a fourteen billion model at four bits is plenty. Their options: a high-memory Apple silicon laptop they would buy anyway, or a cloud GPU rented by the hour. The laptop wins: it already exists in their budget, runs quietly, and keeps client proposals in the office. A GPU server would sit idle most of the day. Sometimes the right hardware decision is to buy nothing new at all.
```

Scene 10 — Rent before you buy

```text
One more practical tip before you buy anything: rent first. Most cloud providers let you rent the exact GPU class you are considering by the hour. Spend a day running your real prompts at your expected concurrency, with the same engine and quantization you plan to use, and record throughput, latency and memory. That one day of rental often costs less than a single wrong component, and it turns your purchase request from a guess into a measured plan. If data rules prevent you from using real prompts in the cloud, use synthetic prompts with the same length and structure.
```

Scene 11 — FAQ

```text
A question I get all the time: should we buy one big GPU or two smaller ones? For most single-team servers, one bigger card is simpler. The whole model fits in one place, you avoid splitting layers across cards, and there is one thing to monitor and cool. Two smaller cards make sense when the model does not fit on any single card you can buy, or when you want two independent replicas for resilience. And a second common question: is a Mac good enough for our team? For a few people using it one at a time, often yes. For twenty people hitting it at once, a dedicated GPU with a batching server will serve them far better.
```

Scene 12 — Try this now

```text
Try this now. Write down your expected number of concurrent users at peak, and the context length they really need. Then place yourself on the five tiers. If you land on tier two or three, you probably do not need a server yet. If you land on tier four or five, your next step is to rent that exact GPU class in the cloud for a day and measure, before you buy anything.
```

Scene 13 — Watch me do it

```text
Watch me do it. A thirty-person office asks me what to buy. I start with numbers, not shopping. Peak concurrent users: I check their chat tool logs and see about twelve at the busiest hour. Context: most documents fit in eight thousand tokens. I run the sizing calculator for a fourteen billion model at four bits: comfortably inside a forty-eight gigabyte workstation card, with room for more users. Next I rent that GPU class in the cloud for an afternoon, serve the model with vLLM, and replay fifty real prompts at twelve concurrent requests. Latency looks good. Then the cost sketch: workstation price, power at their local tariff, a UPS, and two hours a month of admin, over twenty-four months, divided by expected requests. I compare with their current API bill. The workstation pays for itself in the second year, and it keeps client files in the office. That is the recommendation I send.
```

Scene 14 — Recap

```text
Recap. Start from sizing and the workload. Capacity decides fit, bandwidth decides speed. Unified memory is great for individuals, batching GPU servers are best for teams. Mixture-of-experts models need memory more than compute. And judge purchases on twenty-four month cost per thousand requests, including power, cooling and people. Your next step: fill in the cost template for two options and compare them with an API you already use.
```

</details>

### 7. Ollama hands-on: models, Modelfiles and the API

Lesson `ollama-hands-on` · module “Running models locally” · target 9 min · 14 scenes · 1204 words · planned 8:43 (words ≈ 8:43) · status: **to produce**

| Term | Say |
|---|---|
| Ollama | oh-LAH-mah |
| Modelfile | model file |
| num_ctx | num context |
| VPN | V P N |
| TLS | T L S |
| Caddy | caddy |

| # | Time | Sec | Narration | On screen | Visual |
|---|---|---|---|---|---|
| 1 | 0:00 | 33 | In the next ten minutes you will go from an empty laptop to a branded, team-ready AI assistant that never sends a word to the cloud. We will use Ollama, the most popular local model runner. You will learn the five commands that matter, the one setting that silently breaks long documents, how to package your own assistant, how to call it from code, and how to share it without accidentally publishing it to the internet. | Ollama hands-on<br>• Five commands<br>• The context-length gotcha<br>• Modelfiles, API, safe sharing | Clean desktop screen, a terminal opens, and a progress bar downloads a model; title text slides in. |
| 2 | 0:33 | 40 | Think of Ollama as an app store and a power socket in one. The library is the app store: you browse, pull, and the model lands on your disk. The local server is the socket: once a model is plugged in, any app on your machine can draw from it through a standard plug, the API. And a Modelfile is like a preset on a kitchen appliance. You set the temperature, the timer and the recipe once, save it with a name, and everyone in the house gets the same result every time. | Analogy: app store + power socket<br>• Library = app store<br>• Local API = standard socket<br>• Modelfile = saved appliance preset | Three icons animate in sequence: a storefront with model boxes, a wall socket with a plug connecting an app window, and an appliance dial with a saved preset label. |
| 3 | 1:13 | 36 | The five commands. Ollama pull downloads a model. Ollama run starts a chat. Ollama list shows what you have and how big it is. Ollama show tells you the details, including quantization, template and license. And my favorite diagnostic, ollama ps, shows what is loaded and whether it sits fully on your GPU. If you see part of it on the CPU, it did not fit, and it will be slower. Pick a smaller model, a smaller quantization, or a shorter context. | Five commands<br>• pull · run · list · show<br>• ps: check GPU vs CPU split<br>• CPU share = did not fit | Terminal screen recording running each command; the ps output row showing a percentage split between CPU and GPU is highlighted. |
| 4 | 1:49 | 32 | Now the gotcha that catches almost everyone. A model might support a very long context, but the runtime gives it a smaller default to save memory. So you paste a twenty-page contract, and the model cheerfully answers as if page one never existed. It was truncated. The fix is to set the context length on purpose, per request, in a Modelfile, or server-wide. Just remember from the sizing lesson: more context means more memory. | The context gotcha<br>• Default context < model maximum<br>• Long inputs get truncated silently<br>• Set num_ctx deliberately | A long document scrolling into a funnel; the first pages fall off the edge in gray. Then a dial labeled num_ctx turns up and the whole document fits. |
| 5 | 2:21 | 36 | A Modelfile packages your assistant. It names a base model, sets parameters like temperature and context length, and adds a system prompt. For a fictional Dubai travel agency, the system prompt says: answer in the language of the question, English or Arabic, say when you are unsure, and never invent prices or visa rules. One command, ollama create, turns it into a named model everyone on the team runs identically. You can also point a Modelfile at a GGUF file you built yourself. | Modelfile<br>• FROM base model<br>• PARAMETER temperature, num_ctx<br>• SYSTEM prompt with rules<br>• ollama create → a named assistant | Code editor showing the Modelfile lines appearing one by one, then a terminal running ollama create and ollama run with a bilingual answer streaming. |
| 6 | 2:57 | 36 | To use it from software, Ollama listens on port one one four three four on your machine. There is a native API with chat and embedding endpoints, and an OpenAI-compatible API under slash v one. That second one is the quiet superpower. Code written for the OpenAI software kit can talk to your local model by changing two things: the base address and the model name. Later in the course, that exact trick is how we route between local and cloud models. | Calling it from code<br>• Native API: /api/chat, /api/embed<br>• OpenAI-compatible: /v1<br>• Switch base URL + model name | Diagram of one Python app with a switch; one path goes to localhost port 11434, the other to a cloud icon, with only two config lines changing. |
| 7 | 3:33 | 35 | Now safety. By default, Ollama only listens to your own machine. You can open it to the network, but its API has no built-in login. Security researchers regularly find exposed model servers on the internet. So if you share it, put it behind a reverse proxy that requires authentication and encryption, restrict it to your office network or VPN, and never expose the raw port publicly. Also pin a specific model tag rather than latest, so an update never surprises you. | Share it safely<br>• No built-in authentication<br>• Reverse proxy with auth + TLS<br>• VPN or office network only<br>• Pin model tags | Network diagram: laptop server behind a padlocked proxy box, inside an office boundary; a red crossed-out arrow shows direct internet access being blocked. |
| 8 | 4:08 | 32 | Here is the whole thing working. The fictional travel agency installs Ollama on a desktop with a sixteen gigabyte GPU, creates its assistant with a sixteen thousand token context, and puts it behind the office VPN with a password-protected proxy. Eight agents use a simple web chat that talks to the OpenAI-compatible endpoint. Passport scans never leave the building. When a question needs current visa rules, the assistant says to check the official portal. | Worked example: travel agency<br>• 16 GB GPU desktop<br>• Modelfile with 16k context<br>• VPN + authenticated proxy<br>• Data stays in the office | Office floor plan animation: eight desks connect by dotted lines to one desktop box inside a shaded boundary labeled Office network. |
| 9 | 4:40 | 36 | A simple example. A teacher in Leeds wants a private assistant that turns lesson notes into five quiz questions. She installs Ollama, pulls a small model, and writes a four-line Modelfile: the base model, a low temperature, an eight thousand token context, and a system prompt saying always return five multiple-choice questions with answers. She runs ollama create quiz maker, and from then on, one command turns any lesson note into a quiz. No accounts, no uploads, no student data leaving her laptop. | Simple example: quiz maker<br>• Small model + 4-line Modelfile<br>• Low temperature, 8k context<br>• System prompt: 5 MCQs with answers<br>• ollama create quiz-maker | Code panel shows a four-line Modelfile typing itself; a terminal below runs ollama create, then a quiz output streams in with five numbered questions. |
| 10 | 5:16 | 36 | Two small habits make Ollama deployments far easier to support. First, keep your Modelfiles in version control alongside the application code, so a change to the system prompt or context length is reviewed like any other change. Second, keep a short runbook: which model tag is approved, how to update it, how to check ollama ps, how to restart the service, and who to call. When the person who set it up goes on holiday, the runbook is what keeps the assistant running. | Two habits<br>• Modelfiles in version control<br>• A one-page runbook: approved tag, update, check, restart, contact | Git history view showing a Modelfile diff with a changed num_ctx line, then a one-page runbook document with five headings. |
| 11 | 5:52 | 47 | Two questions people ask after their first day with Ollama. First: why is the first answer slow and the rest fast? Because the model loads from disk into memory on the first request. Ollama keeps it loaded for a while afterwards, and you can adjust how long with its keep alive setting, so during office hours you can keep it warm. Second: can I use my own fine-tuned model? Yes. If you have a GGUF file, a Modelfile that points FROM that file turns it into a named model, and Ollama can also attach supported adapters. Just record it in your model register like any other model. | FAQ<br>• First answer slow? Model loading; keep it warm with keep_alive<br>• Own fine-tuned model? FROM your GGUF, or attach an adapter<br>• Register it | Two Q&A cards: a loading bar followed by fast responses with a "keep_alive" dial; a Modelfile snippet showing FROM ./my-model.gguf. |
| 12 | 6:39 | 32 | Try this now. Install Ollama, pull one small model, and run ollama ps while it is answering a question. Look at the processor column. If it says one hundred percent GPU, great. If it shows a CPU share, try a smaller model or a shorter context and watch the change. Then write a Modelfile for a task you do every week, give it a name, and use it for the rest of the week. | Try this now<br>• Pull a small model<br>• Watch ollama ps while it answers<br>• Fix any CPU share<br>• Create a named Modelfile for a weekly task | Terminal split view: left pane streaming an answer, right pane showing ollama ps refreshing with the processor column highlighted. |
| 13 | 7:11 | 65 | Watch me do it. I open a terminal and type ollama pull, then a small model tag. While it downloads, I create a file called Modelfile. First line: FROM and the model tag. Then PARAMETER temperature zero point two, and PARAMETER num context sixteen thousand three hundred eighty-four. Then a SYSTEM block: you are the internal assistant for our support team; answer in the language of the question; say when you are unsure. I save it and run ollama create support assistant with the file. Now ollama run support assistant, and I paste a long refund policy plus a question about the last paragraph. It answers correctly, which tells me the context setting worked. In a second terminal I run ollama ps: one hundred percent GPU. Finally I call it from Python with the OpenAI client, changing only the base address and the model name. Ten minutes, start to finish. | Watch me do it<br>1 ollama pull<br>2 Write the Modelfile<br>3 ollama create<br>4 Test a long document<br>5 ollama ps → 100% GPU<br>6 Call from Python | Terminal recording in two panes: left shows pull progress, Modelfile editing and ollama create; right shows ollama ps with the processor column at 100% GPU; final frame shows a Python script returning an answer. |
| 14 | 8:16 | 27 | Recap. Ollama pulls, manages and serves models locally. Check ollama ps for GPU fit. Set context length deliberately. Package assistants with Modelfiles. Use the OpenAI-compatible endpoint to reuse code. And never expose it without authentication. Your next step: write a Modelfile for an assistant in your own domain and call it from Python with both clients. The code is in the lesson. | Recap<br>• Pull, run, ps, create<br>• Set context, pin tags, add auth<br>• Next: your own Modelfile + Python call | Recap checklist with ticks, ending on a code editor showing both Python snippets side by side. |

<details><summary>Narration per scene (paste into ElevenLabs)</summary>

Scene 1 — Ollama hands-on

```text
In the next ten minutes you will go from an empty laptop to a branded, team-ready AI assistant that never sends a word to the cloud. We will use Ollama, the most popular local model runner. You will learn the five commands that matter, the one setting that silently breaks long documents, how to package your own assistant, how to call it from code, and how to share it without accidentally publishing it to the internet.
```

Scene 2 — Analogy: app store + power socket

```text
Think of Ollama as an app store and a power socket in one. The library is the app store: you browse, pull, and the model lands on your disk. The local server is the socket: once a model is plugged in, any app on your machine can draw from it through a standard plug, the API. And a Modelfile is like a preset on a kitchen appliance. You set the temperature, the timer and the recipe once, save it with a name, and everyone in the house gets the same result every time.
```

Scene 3 — Five commands

```text
The five commands. Ollama pull downloads a model. Ollama run starts a chat. Ollama list shows what you have and how big it is. Ollama show tells you the details, including quantization, template and license. And my favorite diagnostic, ollama ps, shows what is loaded and whether it sits fully on your GPU. If you see part of it on the CPU, it did not fit, and it will be slower. Pick a smaller model, a smaller quantization, or a shorter context.
```

Scene 4 — The context gotcha

```text
Now the gotcha that catches almost everyone. A model might support a very long context, but the runtime gives it a smaller default to save memory. So you paste a twenty-page contract, and the model cheerfully answers as if page one never existed. It was truncated. The fix is to set the context length on purpose, per request, in a Modelfile, or server-wide. Just remember from the sizing lesson: more context means more memory.
```

Scene 5 — Modelfile

```text
A Modelfile packages your assistant. It names a base model, sets parameters like temperature and context length, and adds a system prompt. For a fictional Dubai travel agency, the system prompt says: answer in the language of the question, English or Arabic, say when you are unsure, and never invent prices or visa rules. One command, ollama create, turns it into a named model everyone on the team runs identically. You can also point a Modelfile at a GGUF file you built yourself.
```

Scene 6 — Calling it from code

```text
To use it from software, Ollama listens on port one one four three four on your machine. There is a native API with chat and embedding endpoints, and an OpenAI-compatible API under slash v one. That second one is the quiet superpower. Code written for the OpenAI software kit can talk to your local model by changing two things: the base address and the model name. Later in the course, that exact trick is how we route between local and cloud models.
```

Scene 7 — Share it safely

```text
Now safety. By default, Ollama only listens to your own machine. You can open it to the network, but its API has no built-in login. Security researchers regularly find exposed model servers on the internet. So if you share it, put it behind a reverse proxy that requires authentication and encryption, restrict it to your office network or VPN, and never expose the raw port publicly. Also pin a specific model tag rather than latest, so an update never surprises you.
```

Scene 8 — Worked example: travel agency

```text
Here is the whole thing working. The fictional travel agency installs Ollama on a desktop with a sixteen gigabyte GPU, creates its assistant with a sixteen thousand token context, and puts it behind the office VPN with a password-protected proxy. Eight agents use a simple web chat that talks to the OpenAI-compatible endpoint. Passport scans never leave the building. When a question needs current visa rules, the assistant says to check the official portal.
```

Scene 9 — Simple example: quiz maker

```text
A simple example. A teacher in Leeds wants a private assistant that turns lesson notes into five quiz questions. She installs Ollama, pulls a small model, and writes a four-line Modelfile: the base model, a low temperature, an eight thousand token context, and a system prompt saying always return five multiple-choice questions with answers. She runs ollama create quiz maker, and from then on, one command turns any lesson note into a quiz. No accounts, no uploads, no student data leaving her laptop.
```

Scene 10 — Two habits

```text
Two small habits make Ollama deployments far easier to support. First, keep your Modelfiles in version control alongside the application code, so a change to the system prompt or context length is reviewed like any other change. Second, keep a short runbook: which model tag is approved, how to update it, how to check ollama ps, how to restart the service, and who to call. When the person who set it up goes on holiday, the runbook is what keeps the assistant running.
```

Scene 11 — FAQ

```text
Two questions people ask after their first day with Ollama. First: why is the first answer slow and the rest fast? Because the model loads from disk into memory on the first request. Ollama keeps it loaded for a while afterwards, and you can adjust how long with its keep alive setting, so during office hours you can keep it warm. Second: can I use my own fine-tuned model? Yes. If you have a GGUF file, a Modelfile that points FROM that file turns it into a named model, and Ollama can also attach supported adapters. Just record it in your model register like any other model.
```

Scene 12 — Try this now

```text
Try this now. Install Ollama, pull one small model, and run ollama ps while it is answering a question. Look at the processor column. If it says one hundred percent GPU, great. If it shows a CPU share, try a smaller model or a shorter context and watch the change. Then write a Modelfile for a task you do every week, give it a name, and use it for the rest of the week.
```

Scene 13 — Watch me do it

```text
Watch me do it. I open a terminal and type ollama pull, then a small model tag. While it downloads, I create a file called Modelfile. First line: FROM and the model tag. Then PARAMETER temperature zero point two, and PARAMETER num context sixteen thousand three hundred eighty-four. Then a SYSTEM block: you are the internal assistant for our support team; answer in the language of the question; say when you are unsure. I save it and run ollama create support assistant with the file. Now ollama run support assistant, and I paste a long refund policy plus a question about the last paragraph. It answers correctly, which tells me the context setting worked. In a second terminal I run ollama ps: one hundred percent GPU. Finally I call it from Python with the OpenAI client, changing only the base address and the model name. Ten minutes, start to finish.
```

Scene 14 — Recap

```text
Recap. Ollama pulls, manages and serves models locally. Check ollama ps for GPU fit. Set context length deliberately. Package assistants with Modelfiles. Use the OpenAI-compatible endpoint to reuse code. And never expose it without authentication. Your next step: write a Modelfile for an assistant in your own domain and call it from Python with both clients. The code is in the lesson.
```

</details>

### 8. llama.cpp and LM Studio: control and convenience

Lesson `llama-cpp-and-lm-studio` · module “Running models locally” · target 9 min · 14 scenes · 1200 words · planned 8:41 (words ≈ 8:42) · status: **to produce**

| Term | Say |
|---|---|
| llama.cpp | llama dot C P P |
| GBNF | G B N F |
| jinja | JIN-jah |
| lms | L M S |
| MLX | M L X |
| -ngl | N G L |

| # | Time | Sec | Narration | On screen | Visual |
|---|---|---|---|---|---|
| 1 | 0:00 | 36 | Ollama is the easy button. But sometimes you need to open the hood: force a model to return perfect JSON, squeeze a model onto a machine that is slightly too small, or hand a friendly app to colleagues who never touch a terminal. That is where llama dot cpp and LM Studio come in. In this lesson you will run the llama dot cpp server with sensible flags, constrain output with a schema, tune performance, and set up LM Studio for non-technical teams. | llama.cpp and LM Studio<br>• Control vs convenience<br>• Server flags, schemas, tuning<br>• A friendly app for non-engineers | A car hood opening to reveal a clean engine diagram labeled llama.cpp, then a sleek dashboard labeled LM Studio. |
| 2 | 0:36 | 35 | If Ollama is an automatic car, llama dot cpp is a manual with a full dashboard. You control every gear: how many layers go on the GPU, how the context is split, which template is used, even which tokens the model is allowed to choose. LM Studio is the showroom. You walk in, try models side by side, see which ones fit your garage, and drive one home without reading the manual. Different people in the same company need different vehicles. | Analogy: manual car + showroom<br>• llama.cpp = manual with full dashboard<br>• LM Studio = showroom test drive<br>• Different people, different vehicles | Left: a car dashboard with labeled dials (GPU layers, context, template, grammar). Right: a showroom floor with model cars on display and a "fits your garage" badge on one. |
| 3 | 1:11 | 38 | llama dot cpp is the open-source engine under a lot of local AI tools. Its server takes a model, either a local GGUF file or one fetched directly from a Hugging Face repository, and exposes an OpenAI-compatible API. The flags you will use most: minus c for context size, minus n g l for how many layers go to the GPU, parallel for how many requests run at once, jinja to use the model's own chat template, and api key to require a key on every request. | llama-server essentials<br>• -m or -hf: model source<br>• -c context · -ngl GPU layers<br>• --parallel slots · --jinja template<br>• --api-key | Terminal screen recording launching llama-server with each flag highlighted in turn and a short tooltip label appearing beside it. |
| 4 | 1:49 | 32 | One subtle point. The context you set is shared across parallel slots. Thirty-two thousand tokens of context with four slots gives each request about eight thousand. If you promised users thirty-two thousand each, you will need thirty-two thousand times four, and the memory to match. And always use the jinja option, or the right template, when you rely on tool calling. A wrong template is the number one reason local tool calls look broken. | Watch out<br>• Context is divided across slots<br>• 32k ÷ 4 slots ≈ 8k each<br>• Use --jinja for tool calling | A 32k bar splitting into four equal 8k segments labeled slot 1 to 4; then a template icon snapping into place next to a tool-call bubble. |
| 5 | 2:21 | 32 | Now a genuinely powerful feature: constrained output. You can give the server a JSON schema, or a grammar, and the decoder will only choose tokens that keep the output valid. The model literally cannot produce broken JSON. For a support-intent classifier, you define an intent field that must be one of refund, delivery, product question or other, plus an optional order number. The format is guaranteed. Whether the label is correct still needs evaluation. | Constrained output<br>• JSON schema or grammar<br>• Invalid tokens are blocked<br>• Format guaranteed, correctness still measured | Animation of a token stream where invalid tokens are greyed out and rejected, and only schema-valid tokens pass into a neat JSON object. |
| 6 | 2:53 | 33 | Performance knobs. Put all layers on the GPU if they fit. If not, partial offload lets a too-big model run, just slower. Flash attention and an eight-bit KV cache save memory at long context. For CPU inference, match threads to performance cores. Speculative decoding, where a small draft model proposes tokens and the big model checks them, can speed some workloads. But do not copy flags from forums. Run llama bench on your own machine and compare. | Performance knobs<br>• Full GPU offload if it fits<br>• Flash attention, 8-bit KV cache<br>• Threads = performance cores<br>• Benchmark with llama-bench | Mixing-desk style sliders labeled GPU layers, KV type, threads, draft model; a tokens-per-second meter moves as sliders change. |
| 7 | 3:26 | 38 | LM Studio is the friendly face. You search for a model, and it tells you which versions fit your memory. You chat, compare two models side by side, and when you are ready, start a local server that speaks the OpenAI format on port twelve thirty-four. There is also a command-line tool, l m s, for scripting. On Apple silicon it runs both GGUF and MLX models. It is perfect for letting a marketing or operations team try local models on real work before anyone buys a server. | LM Studio<br>• Discover: fit-aware model downloads<br>• Side-by-side chat comparison<br>• Local server on port 1234<br>• lms CLI | Screen recording of a desktop app: searching a model, a green "fits" indicator, split-screen chat, then toggling the local server on. |
| 8 | 4:04 | 36 | A real-style example. A UK charity handles safeguarding notes that must never leave the device. Case workers use LM Studio on their laptops with one small approved model and an IT setup guide. The data team runs llama server on an internal Linux box, with an API key, firewall rules and a JSON schema that extracts risk categories straight into their case system. IT records both in the model register and pins model files by checksum. Convenience for people, control for pipelines. | Worked example: UK charity<br>• Case workers: LM Studio, approved model<br>• Data team: llama-server + JSON schema<br>• IT: register + checksums | Two-lane diagram: laptops with a desktop app on one lane, a Linux server with a schema icon feeding a case-management database on the other, both inside a secure boundary. |
| 9 | 4:40 | 37 | A simple example of constrained output. A small online bookshop wants to tag incoming emails with one of four labels: order status, return, recommendation request or other. Without constraints, the model sometimes replies with a friendly sentence instead of a label. With a JSON schema that allows only those four strings, every single reply is one of the four. The shop's automation never breaks on a surprise sentence again. They still check a sample each week to make sure the labels are right, not just valid. | Simple example: bookshop email tags<br>• 4 allowed labels<br>• Without schema: occasional chatty replies<br>• With schema: always a valid label<br>• Still sample-check correctness | Email icons flow into a model box; without a schema some outputs are speech bubbles (red), then a schema filter slides in and all outputs become neat label chips (green). |
| 10 | 5:17 | 35 | How do you decide between them for a given team? Ask who will operate it and what it feeds. If a person is chatting and exploring, LM Studio's interface and fit-aware downloads save hours. If software depends on the output, a pipeline, a database, an integration, you want the llama dot cpp server, or a bigger engine, with fixed flags, an API key and a schema. Many organizations use both, with the same approved model files, recorded in the same register. | Which one when?<br>• People exploring → LM Studio<br>• Software depending on output → llama-server<br>• Same approved files, same register | Decision fork graphic: a person icon leading to a desktop app, a gear icon leading to a server, both drawing from one shared "approved models" folder. |
| 11 | 5:52 | 46 | A common question: if Ollama and LM Studio both use llama dot cpp underneath, why would I ever use it directly? Three reasons. You get new features and model support first, sometimes weeks earlier. You get precise control over flags like context per slot, cache types and grammars. And you get a tiny, dependency-light server that is easy to put in a container on almost any hardware. Another question: are GGUF files from LM Studio and Ollama interchangeable? The GGUF format is shared, so a GGUF file generally works in any llama dot cpp based runtime, though each app manages its own storage and naming. | FAQ<br>• Why use llama.cpp directly? New features first, precise flags, tiny server<br>• GGUF interchangeable? Format is shared across llama.cpp runtimes | Q&A cards animate: three small benefit icons for llama.cpp, then a single GGUF file icon connecting to three runtime boxes. |
| 12 | 6:38 | 25 | Try this now. Start llama server with an API key and a small model, and send it one request with a JSON schema for a task you care about. Then send the same request without the schema, ten times, and count how many answers you could parse. That tiny experiment will convince your team faster than any slide. | Try this now<br>• llama-server + API key<br>• Same request ×10 with and without schema<br>• Count parseable answers | Two tally counters side by side labeled "with schema" and "without schema", incrementing as ten requests animate through each. |
| 13 | 7:03 | 68 | Watch me do it. I start llama server with a GGUF model, a context of sixteen thousand, all layers on the GPU, two parallel slots, the jinja template flag and an API key from an environment variable. I check the health endpoint: it says ok. Now I send a classification request for an email that says my order arrived broken, I want my money back, with a JSON schema that allows only four intents. The response comes back as clean JSON: intent refund, order number included. I send the same request ten times without the schema and count: eight parseable, two chatty answers. Next I switch to LM Studio for a colleague. I search the same model, and the app shows which quantization fits her laptop. She downloads it, compares it side by side with another model on three of her real emails, then turns on the local server. Two tools, two audiences, same model file. | Watch me do it<br>1 llama-server with key + template<br>2 Health check<br>3 Schema request → clean JSON<br>4 Without schema: 8/10 parseable<br>5 LM Studio for a colleague | Terminal shows llama-server startup flags and a curl to the health endpoint; a JSON response panel; a tally of 10 unconstrained requests; then an LM Studio-style desktop app with a fit indicator and a side-by-side chat. |
| 14 | 8:11 | 30 | Recap. llama dot cpp gives you control: flags, schemas, performance tuning and a small OpenAI-compatible server. Remember context is split across slots, and templates matter for tools. LM Studio gives people a friendly way to explore and serve models locally. Your next step: serve a model with an API key and a JSON schema for one classification task from your work, then benchmark two quantizations with llama bench. | Recap<br>• Control: llama.cpp<br>• Convenience: LM Studio<br>• Next: schema-constrained server + llama-bench | Recap card with two columns, Control and Convenience, each with three ticks. |

<details><summary>Narration per scene (paste into ElevenLabs)</summary>

Scene 1 — llama.cpp and LM Studio

```text
Ollama is the easy button. But sometimes you need to open the hood: force a model to return perfect JSON, squeeze a model onto a machine that is slightly too small, or hand a friendly app to colleagues who never touch a terminal. That is where llama dot cpp and LM Studio come in. In this lesson you will run the llama dot cpp server with sensible flags, constrain output with a schema, tune performance, and set up LM Studio for non-technical teams.
```

Scene 2 — Analogy: manual car + showroom

```text
If Ollama is an automatic car, llama dot cpp is a manual with a full dashboard. You control every gear: how many layers go on the GPU, how the context is split, which template is used, even which tokens the model is allowed to choose. LM Studio is the showroom. You walk in, try models side by side, see which ones fit your garage, and drive one home without reading the manual. Different people in the same company need different vehicles.
```

Scene 3 — llama-server essentials

```text
llama dot cpp is the open-source engine under a lot of local AI tools. Its server takes a model, either a local GGUF file or one fetched directly from a Hugging Face repository, and exposes an OpenAI-compatible API. The flags you will use most: minus c for context size, minus n g l for how many layers go to the GPU, parallel for how many requests run at once, jinja to use the model's own chat template, and api key to require a key on every request.
```

Scene 4 — Watch out

```text
One subtle point. The context you set is shared across parallel slots. Thirty-two thousand tokens of context with four slots gives each request about eight thousand. If you promised users thirty-two thousand each, you will need thirty-two thousand times four, and the memory to match. And always use the jinja option, or the right template, when you rely on tool calling. A wrong template is the number one reason local tool calls look broken.
```

Scene 5 — Constrained output

```text
Now a genuinely powerful feature: constrained output. You can give the server a JSON schema, or a grammar, and the decoder will only choose tokens that keep the output valid. The model literally cannot produce broken JSON. For a support-intent classifier, you define an intent field that must be one of refund, delivery, product question or other, plus an optional order number. The format is guaranteed. Whether the label is correct still needs evaluation.
```

Scene 6 — Performance knobs

```text
Performance knobs. Put all layers on the GPU if they fit. If not, partial offload lets a too-big model run, just slower. Flash attention and an eight-bit KV cache save memory at long context. For CPU inference, match threads to performance cores. Speculative decoding, where a small draft model proposes tokens and the big model checks them, can speed some workloads. But do not copy flags from forums. Run llama bench on your own machine and compare.
```

Scene 7 — LM Studio

```text
LM Studio is the friendly face. You search for a model, and it tells you which versions fit your memory. You chat, compare two models side by side, and when you are ready, start a local server that speaks the OpenAI format on port twelve thirty-four. There is also a command-line tool, l m s, for scripting. On Apple silicon it runs both GGUF and MLX models. It is perfect for letting a marketing or operations team try local models on real work before anyone buys a server.
```

Scene 8 — Worked example: UK charity

```text
A real-style example. A UK charity handles safeguarding notes that must never leave the device. Case workers use LM Studio on their laptops with one small approved model and an IT setup guide. The data team runs llama server on an internal Linux box, with an API key, firewall rules and a JSON schema that extracts risk categories straight into their case system. IT records both in the model register and pins model files by checksum. Convenience for people, control for pipelines.
```

Scene 9 — Simple example: bookshop email tags

```text
A simple example of constrained output. A small online bookshop wants to tag incoming emails with one of four labels: order status, return, recommendation request or other. Without constraints, the model sometimes replies with a friendly sentence instead of a label. With a JSON schema that allows only those four strings, every single reply is one of the four. The shop's automation never breaks on a surprise sentence again. They still check a sample each week to make sure the labels are right, not just valid.
```

Scene 10 — Which one when?

```text
How do you decide between them for a given team? Ask who will operate it and what it feeds. If a person is chatting and exploring, LM Studio's interface and fit-aware downloads save hours. If software depends on the output, a pipeline, a database, an integration, you want the llama dot cpp server, or a bigger engine, with fixed flags, an API key and a schema. Many organizations use both, with the same approved model files, recorded in the same register.
```

Scene 11 — FAQ

```text
A common question: if Ollama and LM Studio both use llama dot cpp underneath, why would I ever use it directly? Three reasons. You get new features and model support first, sometimes weeks earlier. You get precise control over flags like context per slot, cache types and grammars. And you get a tiny, dependency-light server that is easy to put in a container on almost any hardware. Another question: are GGUF files from LM Studio and Ollama interchangeable? The GGUF format is shared, so a GGUF file generally works in any llama dot cpp based runtime, though each app manages its own storage and naming.
```

Scene 12 — Try this now

```text
Try this now. Start llama server with an API key and a small model, and send it one request with a JSON schema for a task you care about. Then send the same request without the schema, ten times, and count how many answers you could parse. That tiny experiment will convince your team faster than any slide.
```

Scene 13 — Watch me do it

```text
Watch me do it. I start llama server with a GGUF model, a context of sixteen thousand, all layers on the GPU, two parallel slots, the jinja template flag and an API key from an environment variable. I check the health endpoint: it says ok. Now I send a classification request for an email that says my order arrived broken, I want my money back, with a JSON schema that allows only four intents. The response comes back as clean JSON: intent refund, order number included. I send the same request ten times without the schema and count: eight parseable, two chatty answers. Next I switch to LM Studio for a colleague. I search the same model, and the app shows which quantization fits her laptop. She downloads it, compares it side by side with another model on three of her real emails, then turns on the local server. Two tools, two audiences, same model file.
```

Scene 14 — Recap

```text
Recap. llama dot cpp gives you control: flags, schemas, performance tuning and a small OpenAI-compatible server. Remember context is split across slots, and templates matter for tools. LM Studio gives people a friendly way to explore and serve models locally. Your next step: serve a model with an API key and a JSON schema for one classification task from your work, then benchmark two quantizations with llama bench.
```

</details>

### 9. Local models for real work: structured output, tools and reliability

Lesson `local-tools-and-structured-output` · module “Running models locally” · target 8 min · 14 scenes · 1183 words · planned 8:35 (words ≈ 8:34) · status: **to produce**

| Term | Say |
|---|---|
| Pydantic | pie-DAN-tick |
| JSON | jason |
| SGLang | S G Lang |
| keep_alive | keep alive |

| # | Time | Sec | Narration | On screen | Visual |
|---|---|---|---|---|---|
| 1 | 0:00 | 28 | A chatbot that is right most of the time is a demo. A component that is right, parseable and traceable every time is a product. In this lesson you will turn a local model into something other software can rely on: guaranteed structured output, tool calling, and the reliability patterns that stop two a.m. pages. Everything runs locally, and the code is in the lesson. | Local models for real work<br>• Structured output<br>• Tool calling<br>• Reliability patterns | A playful chat bubble transforms into a solid building block that snaps into a larger software diagram with CRM and ticketing icons. |
| 2 | 0:28 | 38 | Here is an analogy for structured output. Asking a model for JSON in plain words is like asking a new employee to fill in a paper form by hand. Most of the time it is fine, but sometimes a box is skipped or someone writes outside the lines. A schema with constrained decoding is a web form with dropdowns and required fields. The person can only submit valid entries. It does not guarantee they chose the right option, but it guarantees the form is complete and machine-readable. | Analogy: paper form vs web form<br>• Prompt-only JSON = handwritten form<br>• Constrained schema = web form with dropdowns<br>• Valid ≠ correct | Left: a paper form with a skipped box circled in red. Right: a clean web form with dropdown fields and a disabled submit button that becomes active once all required fields are filled. |
| 3 | 1:06 | 36 | There are three levels of structured output. Level one, prompt-only: you ask nicely for JSON, and it works until it does not. Level two, JSON mode: valid JSON, but not necessarily your fields. Level three, constrained decoding: you hand the runtime a schema and it only allows tokens that fit. Ollama does this with its format parameter, llama dot cpp with schemas and grammars, vLLM and SGLang with guided decoding. Use level three whenever you can, and still validate on your side. | Three levels of structure<br>1 Prompt-only: fragile<br>2 JSON mode: valid JSON, any shape<br>3 Constrained decoding: your schema, guaranteed | Three stacked steps with increasing solidity: a dotted outline, a solid outline, then a filled block stamped with a schema icon. |
| 4 | 1:42 | 39 | Here is the pattern in Python. You describe the lead you want with a Pydantic model: name, company, country, budget, and an intent that must be demo, pricing, support, partnership or other. You pass the schema to Ollama, set temperature to zero, then validate the result. Try it on an email like this: Ayesha from a textile firm in Faisalabad wants pricing for twenty-five seats and maybe a demo. Notice the ambiguity. Pricing or demo? The schema cannot decide that. Your labeling guide must, and your tests must check it. | Pydantic + Ollama format<br>• Schema defines fields and allowed intents<br>• temperature 0<br>• Validate, then evaluate<br>• Ambiguity needs a labeling rule | Code editor showing the Lead class, then an email card with phrases highlighted in two colors for "pricing" and "demo", and a question mark between them. |
| 5 | 2:21 | 37 | Tool calling works locally with the same loop you know from cloud APIs. The model sees tool descriptions, decides to call one, say get order status with order five five three one. Your code checks the tool name against an allow-list, validates the arguments, runs the function, and sends the result back. Then the model writes the final answer. The Ollama Python library can even build the tool description from your function's signature and docstring. Just make sure the model's chat template supports tools. | Tool-calling loop<br>• Model proposes a call<br>• You allow-list and validate<br>• Execute, return the result<br>• Model writes the answer | Circular loop diagram with four labeled arrows; a small gate icon labeled allow-list sits between "model proposes" and "execute". |
| 6 | 2:58 | 39 | Now reliability. Use temperature zero for extraction and classification. If validation fails, retry once with the error message, then route to a human. Set client timeouts, because a busy local server can stall. Keep the model warm during business hours, since the first request after idle has to load it from disk. Log the model tag and quantization with every output so you can trace a regression. And remember, prompt injection works locally too. An email can contain instructions. Keep tools read-only where possible and confirm anything with side effects. | Reliability patterns<br>• temperature 0 · retry with repair · human fallback<br>• Timeouts · warm models<br>• Log model tag + quant<br>• Least-privilege tools | Checklist animation with six items ticking; a small shield icon appears beside the last one. |
| 7 | 3:37 | 35 | A worked example. An online store in Jeddah gets Arabic and English emails. A local eight billion model on an office GPU extracts intent, order number and language with a schema. A read-only tool fetches order status. Refunds are never executed automatically; they become drafts for a human agent. After two weeks, on three hundred labeled emails, the team sees, and these are illustrative, ninety-four percent correct intents, zero malformed outputs, and a median latency around one and a half seconds. | Worked example: Jeddah store (illustrative)<br>• Arabic + English emails<br>• Schema extraction + read-only tool<br>• Refunds → human drafts<br>• 94% intent accuracy, 0 parse errors | Pipeline animation: email icons in two scripts flow into a local GPU box, out come JSON cards, one branch to an order API, another to a human review inbox. |
| 8 | 4:12 | 25 | Your hands-on exercise is a gold-set loop. Define a schema for one extraction task. Label fifty real examples by hand. Run the model at temperature zero. Compute accuracy per field and read the ten worst errors. Fix your labeling guide or prompt, run again, compare. That loop, not the model name, is what makes local AI trustworthy. | Hands-on: the gold-set loop<br>1 Schema<br>2 Label 50 examples<br>3 Run at temperature 0<br>4 Per-field accuracy + worst 10<br>5 Fix and rerun | Circular five-step loop diagram with numbered nodes and a small accuracy chart rising after each lap. |
| 9 | 4:37 | 42 | A simple example of tool calling. A fitness studio in Riyadh wants its local assistant to answer, what time is the next yoga class? The model does not know the timetable, so you give it one read-only tool, get next class, with a class type argument. The model calls get next class with yoga, your code looks it up in the booking system, returns seven p m, and the model replies: the next yoga class is at seven this evening. The model never touches the booking system directly. Your code does, with only the permission to read. | Simple example: studio timetable<br>• Tool: get_next_class(class_type)<br>• Model proposes call → code reads timetable<br>• Answer: "7 pm tonight"<br>• Read-only permission | Sequence diagram with three lanes: user, model, booking system. Arrows animate: question → tool call → lookup → result → answer, with a small read-only lock on the booking lane. |
| 10 | 5:19 | 38 | Let me show you a failure that schemas do not catch. A support email says: I was charged twice, and the parcel never arrived. Is that a refund intent or a delivery intent? The model returns perfectly valid JSON with intent set to delivery. Parse succeeds, accuracy fails. The fix is not a better model. It is a labeling rule, for example, money issues take priority, plus a test case for exactly this pattern in your gold set. Most accuracy gains in production come from sharpening these rules. | Valid but wrong<br>• "Charged twice and parcel never arrived"<br>• Valid JSON, wrong intent<br>• Fix: a priority rule + a test case | An email card with two highlighted phrases, a JSON output with intent "delivery" and a green parse tick next to a red accuracy cross; then a rule card "money issues take priority". |
| 11 | 5:57 | 40 | A question worth answering: do small local models handle tool calling reliably? Increasingly yes, but it varies by model and by template. The practical approach is to test tool calling as part of your evaluation, with ten or twenty requests that should call a tool and ten that should not. Measure both: did it call the right tool with valid arguments, and did it avoid calling tools when it should just answer? If a model struggles, give it fewer, clearer tools with good descriptions, or route tool-heavy requests to a stronger model. | FAQ: are small models good at tools?<br>• Varies by model and template<br>• Test: should-call and should-not-call cases<br>• Fewer, clearer tools · or route to a stronger model | Two columns of test requests labeled "should call" and "should not call"; tick and cross marks fill in as results arrive. |
| 12 | 6:37 | 27 | Try this now. Take one form you fill in by hand at work, maybe a lead intake or an expense note, and turn its fields into a Pydantic model. Then feed the local model three real examples with the schema and temperature zero. Count how many fields are right. That is your first gold-set measurement, and it takes about twenty minutes. | Try this now<br>• Turn a real form into a Pydantic model<br>• 3 real examples, temperature 0<br>• Count correct fields | A paper form morphs into a Python class on screen, field by field; three example cards pass through and a small per-field accuracy table appears. |
| 13 | 7:04 | 68 | Watch me do it. I write a Pydantic class for a sales lead: name, company, country, budget and an intent from a fixed list. I pass its schema to Ollama's format parameter with temperature zero, and send three real, anonymized emails. The first comes back perfect. The second has budget empty, correct, because the email did not mention one. The third labels intent as demo, but my labeling guide says pricing wins when both are mentioned. So I add one sentence to the system prompt about that priority, rerun, and it now says pricing. Next I add a read-only tool, get order status, and ask where is order five five three one. The model proposes the call, my code checks the name against an allow-list, runs it, and returns the result, and the model answers in one sentence. I log the model tag and quantization with every output. Twenty minutes, and it behaves like a component. | Watch me do it<br>1 Pydantic schema → format<br>2 3 real emails<br>3 Fix one priority rule<br>4 Add a read-only tool<br>5 Allow-list + log model tag | Code editor with the Lead class; terminal outputs three JSON objects, one highlighted amber then turning green after a prompt edit; a sequence diagram animates the tool call through an allow-list gate. |
| 14 | 8:12 | 23 | Recap. Constrain output with schemas, but remember valid is not the same as correct. Tool calling works locally with an allow-list and validation. Build reliability with low temperature, retries, timeouts, warm models, logging and least privilege. Your next step: build one schema-constrained extractor for your work and measure it on fifty labeled examples. | Recap<br>• Schema ≠ correctness<br>• Allow-list tools<br>• Next: extractor + 50-example gold set | Recap card with three icons: schema, tool, checklist. |

<details><summary>Narration per scene (paste into ElevenLabs)</summary>

Scene 1 — Local models for real work

```text
A chatbot that is right most of the time is a demo. A component that is right, parseable and traceable every time is a product. In this lesson you will turn a local model into something other software can rely on: guaranteed structured output, tool calling, and the reliability patterns that stop two a.m. pages. Everything runs locally, and the code is in the lesson.
```

Scene 2 — Analogy: paper form vs web form

```text
Here is an analogy for structured output. Asking a model for JSON in plain words is like asking a new employee to fill in a paper form by hand. Most of the time it is fine, but sometimes a box is skipped or someone writes outside the lines. A schema with constrained decoding is a web form with dropdowns and required fields. The person can only submit valid entries. It does not guarantee they chose the right option, but it guarantees the form is complete and machine-readable.
```

Scene 3 — Three levels of structure

```text
There are three levels of structured output. Level one, prompt-only: you ask nicely for JSON, and it works until it does not. Level two, JSON mode: valid JSON, but not necessarily your fields. Level three, constrained decoding: you hand the runtime a schema and it only allows tokens that fit. Ollama does this with its format parameter, llama dot cpp with schemas and grammars, vLLM and SGLang with guided decoding. Use level three whenever you can, and still validate on your side.
```

Scene 4 — Pydantic + Ollama format

```text
Here is the pattern in Python. You describe the lead you want with a Pydantic model: name, company, country, budget, and an intent that must be demo, pricing, support, partnership or other. You pass the schema to Ollama, set temperature to zero, then validate the result. Try it on an email like this: Ayesha from a textile firm in Faisalabad wants pricing for twenty-five seats and maybe a demo. Notice the ambiguity. Pricing or demo? The schema cannot decide that. Your labeling guide must, and your tests must check it.
```

Scene 5 — Tool-calling loop

```text
Tool calling works locally with the same loop you know from cloud APIs. The model sees tool descriptions, decides to call one, say get order status with order five five three one. Your code checks the tool name against an allow-list, validates the arguments, runs the function, and sends the result back. Then the model writes the final answer. The Ollama Python library can even build the tool description from your function's signature and docstring. Just make sure the model's chat template supports tools.
```

Scene 6 — Reliability patterns

```text
Now reliability. Use temperature zero for extraction and classification. If validation fails, retry once with the error message, then route to a human. Set client timeouts, because a busy local server can stall. Keep the model warm during business hours, since the first request after idle has to load it from disk. Log the model tag and quantization with every output so you can trace a regression. And remember, prompt injection works locally too. An email can contain instructions. Keep tools read-only where possible and confirm anything with side effects.
```

Scene 7 — Worked example: Jeddah store (illustrative)

```text
A worked example. An online store in Jeddah gets Arabic and English emails. A local eight billion model on an office GPU extracts intent, order number and language with a schema. A read-only tool fetches order status. Refunds are never executed automatically; they become drafts for a human agent. After two weeks, on three hundred labeled emails, the team sees, and these are illustrative, ninety-four percent correct intents, zero malformed outputs, and a median latency around one and a half seconds.
```

Scene 8 — Hands-on: the gold-set loop

```text
Your hands-on exercise is a gold-set loop. Define a schema for one extraction task. Label fifty real examples by hand. Run the model at temperature zero. Compute accuracy per field and read the ten worst errors. Fix your labeling guide or prompt, run again, compare. That loop, not the model name, is what makes local AI trustworthy.
```

Scene 9 — Simple example: studio timetable

```text
A simple example of tool calling. A fitness studio in Riyadh wants its local assistant to answer, what time is the next yoga class? The model does not know the timetable, so you give it one read-only tool, get next class, with a class type argument. The model calls get next class with yoga, your code looks it up in the booking system, returns seven p m, and the model replies: the next yoga class is at seven this evening. The model never touches the booking system directly. Your code does, with only the permission to read.
```

Scene 10 — Valid but wrong

```text
Let me show you a failure that schemas do not catch. A support email says: I was charged twice, and the parcel never arrived. Is that a refund intent or a delivery intent? The model returns perfectly valid JSON with intent set to delivery. Parse succeeds, accuracy fails. The fix is not a better model. It is a labeling rule, for example, money issues take priority, plus a test case for exactly this pattern in your gold set. Most accuracy gains in production come from sharpening these rules.
```

Scene 11 — FAQ: are small models good at tools?

```text
A question worth answering: do small local models handle tool calling reliably? Increasingly yes, but it varies by model and by template. The practical approach is to test tool calling as part of your evaluation, with ten or twenty requests that should call a tool and ten that should not. Measure both: did it call the right tool with valid arguments, and did it avoid calling tools when it should just answer? If a model struggles, give it fewer, clearer tools with good descriptions, or route tool-heavy requests to a stronger model.
```

Scene 12 — Try this now

```text
Try this now. Take one form you fill in by hand at work, maybe a lead intake or an expense note, and turn its fields into a Pydantic model. Then feed the local model three real examples with the schema and temperature zero. Count how many fields are right. That is your first gold-set measurement, and it takes about twenty minutes.
```

Scene 13 — Watch me do it

```text
Watch me do it. I write a Pydantic class for a sales lead: name, company, country, budget and an intent from a fixed list. I pass its schema to Ollama's format parameter with temperature zero, and send three real, anonymized emails. The first comes back perfect. The second has budget empty, correct, because the email did not mention one. The third labels intent as demo, but my labeling guide says pricing wins when both are mentioned. So I add one sentence to the system prompt about that priority, rerun, and it now says pricing. Next I add a read-only tool, get order status, and ask where is order five five three one. The model proposes the call, my code checks the name against an allow-list, runs it, and returns the result, and the model answers in one sentence. I log the model tag and quantization with every output. Twenty minutes, and it behaves like a component.
```

Scene 14 — Recap

```text
Recap. Constrain output with schemas, but remember valid is not the same as correct. Tool calling works locally with an allow-list and validation. Build reliability with low temperature, retries, timeouts, warm models, logging and least privilege. Your next step: build one schema-constrained extractor for your work and measure it on fifty labeled examples.
```

</details>

### 10. Serving at scale with vLLM

Lesson `vllm-production-serving` · module “Serving at scale” · target 9 min · 14 scenes · 1222 words · planned 8:51 (words ≈ 8:51) · status: **to produce**

| Term | Say |
|---|---|
| vLLM | V L L M |
| PagedAttention | paged attention |
| TTFT | T T F T, time to first token |
| ITL | I T L |
| p95 | P ninety-five |
| Prometheus | pro-MEE-thee-us |
| LoRA | LOR-ah |

| # | Time | Sec | Narration | On screen | Visual |
|---|---|---|---|---|---|
| 1 | 0:00 | 32 | Your pilot was a hit. Twelve people loved the local assistant. Now the whole company wants it, plus an application that fires hundreds of requests at once. The desktop runner that was perfect for one person starts to queue, stall and time out. This lesson is about serving at scale with vLLM: why it is fast, how to launch it properly, which flags to tune, and how to measure performance like an operator. | Serving at scale with vLLM<br>• From one user to hundreds<br>• Why it is fast<br>• Flags, metrics, capacity | Animation of a single-lane road turning into a multi-lane highway, with small request cars merging smoothly. |
| 2 | 0:32 | 39 | Think of a busy restaurant again. A desktop runner is a single chef cooking one order at a time from start to finish. vLLM is a professional kitchen with a head chef who starts new dishes the moment a burner frees up, shares prep work between similar orders, and never leaves a stove idle. That is continuous batching and prefix caching. And PagedAttention is smart shelving: instead of reserving a whole shelf for each order, ingredients go wherever there is space. Same ingredients, same chefs, far more dishes per hour. | Analogy: a professional kitchen<br>• Continuous batching = new dish when a burner frees<br>• Prefix caching = shared prep<br>• PagedAttention = smart shelving | Top-down kitchen animation: a single chef cooking one order sequentially, then transforming into a line of burners where new orders slot in immediately; shelves reorganize into small labeled bins. |
| 3 | 1:11 | 35 | Two ideas make vLLM fast. The first is PagedAttention. Instead of reserving one huge block of memory per request for context it might never use, vLLM stores the attention cache in small pages allocated on demand, like virtual memory in an operating system. Less waste, more requests per GPU. The second is continuous batching. Requests join and leave the running batch at every step, so a short answer never waits for a long one, and the GPU never sits idle. | Why vLLM is fast<br>• PagedAttention: KV cache in pages, on demand<br>• Continuous batching: join and leave every step | Left: memory grid with large half-empty reserved blocks; right: the same grid packed with small pages. Then a conveyor belt where items enter and exit at every tick. |
| 4 | 1:46 | 28 | On top of that you get prefix caching, which reuses work for shared prompt beginnings like a long system prompt or the same retrieved document. You get quantized checkpoints such as FP8, AWQ and GPTQ, tensor parallelism across GPUs, structured outputs, tool calling, and serving many fine-tuned adapters on one base model. And it speaks the OpenAI API, so your existing code barely changes. | Production features<br>• Prefix caching<br>• FP8 / AWQ / GPTQ<br>• Tensor parallelism<br>• Structured output, tools, multi-LoRA<br>• OpenAI-compatible API | Feature tiles assembling around a central engine icon; the last tile shows a code snippet with only the base URL changing. |
| 5 | 2:14 | 45 | Launching is one command: vllm serve, the model name, a maximum context length, a GPU memory budget and an API key. Or use the official Docker image, pinned to a specific tag. Then watch the startup log. vLLM tells you how much memory it allocated for the cache and how many requests it can hold at full context. That single line is your capacity plan. Two flags matter most. Max model length: set it to what users need, not the model's maximum, because every extra token of allowed context reduces concurrency. And GPU memory utilization: lower it if something else shares the card. | Launch and tune<br>• vllm serve MODEL --max-model-len --gpu-memory-utilization --api-key<br>• Read the KV capacity log line<br>• Context you need, not the maximum | Terminal recording of the vllm serve command, scrolling logs, and a highlighted log line about KV cache size and maximum concurrency. |
| 6 | 2:59 | 32 | Now measure like an operator. Users feel two things: time to first token, how long before text appears, and inter-token latency, how smoothly it streams. Operators also watch throughput and the ninety-fifth percentile, because averages hide the unhappy users. vLLM includes a benchmarking command that replays prompts at a target rate and reports all of these. And it exposes Prometheus metrics, queue length, cache usage and token throughput, ready for dashboards and alerts. | Measure it<br>• TTFT: time to first token<br>• ITL: inter-token latency<br>• Throughput + p95/p99<br>• vllm bench · /metrics | Dashboard mock-up with four panels: TTFT histogram, ITL line, throughput counter, queue length gauge, with a p95 marker line. |
| 7 | 3:31 | 43 | Let's walk through an insurer in Abu Dhabi, with illustrative numbers. A hundred and twenty claims adjusters, around forty at peak. They serve a thirty-two billion model at FP8 on one eighty gigabyte GPU with a twelve thousand token context. They replay five hundred real, redacted prompts at two, four and eight requests per second. At four per second, the ninety-fifth percentile first token is under two seconds. Fine. At eight, it passes six seconds. Not fine. So one GPU handles normal peaks, and a second replica handles month-end surges. Alerts fire when the queue or cache usage runs hot. | Worked example: Abu Dhabi insurer (illustrative)<br>• 32B FP8, 1 × 80 GB GPU, 12k context<br>• 4 req/s: p95 TTFT < 2 s ✓<br>• 8 req/s: p95 TTFT > 6 s ✗<br>• Add a replica for surges | Line chart of p95 TTFT against request rate with a threshold line; the curve crosses it between 4 and 8. A second server icon appears labeled month-end. |
| 8 | 4:14 | 36 | Before you call it production, run the checklist. Authentication and TLS, and network isolation. Pinned model revision and container tag, recorded in your register. Health checks. Metrics, dashboards and alerts. A load test before launch and after every model change. Rate limits per client at the gateway. And logging that respects your privacy policy. Three classic mistakes: maximum context set to the model limit, a so-called load test with one request at a time, and an unauthenticated server because it is only internal. | Production checklist<br>• Auth, TLS, isolation<br>• Pinned versions + register<br>• Health, metrics, alerts<br>• Load test every change<br>• Rate limits, privacy-safe logs | Checklist sliding in line by line, then three red cross icons beside the three classic mistakes. |
| 9 | 4:50 | 39 | A simple example. A ten-person accountancy firm in Birmingham runs vLLM on one workstation GPU with an eight billion model. They set maximum context to eight thousand tokens because their documents are short after retrieval. At startup, the log says the cache can hold dozens of eight-thousand-token requests at once, far more than ten staff will ever send together. They run a quick benchmark at two requests per second, see first tokens in well under a second, and they are done. Not every deployment needs a capacity war room. | Simple example: 10-person firm<br>• 8B model, 1 workstation GPU<br>• max-model-len 8k<br>• Log: capacity >> 10 users<br>• Quick benchmark: done | Office icon with ten desks connected to one GPU box; a log line animates in showing capacity, then a small benchmark chart with a flat, low latency line. |
| 10 | 5:29 | 38 | A word on scaling out. When one GPU is not enough, you have two options. Scale up: a bigger card, or tensor parallelism across several cards in one machine, for models that do not fit. Or scale out: several identical replicas behind a load balancer, for more concurrent users. For most business workloads, replicas are simpler, more resilient, and let you do rolling upgrades, one replica at a time, with no downtime. Decide your scale-out trigger in advance, for example, queue length above a threshold for ten minutes. | Scale up vs scale out<br>• Up: bigger card or tensor parallel (model too big)<br>• Out: replicas behind a load balancer (more users)<br>• Rolling upgrades<br>• Pre-agreed scale-out trigger | Two diagrams: one large server box split into GPU slices, and three identical smaller boxes behind a load balancer icon; a queue gauge crosses a dashed threshold line. |
| 11 | 6:07 | 38 | A frequent question: how many users can one GPU serve? It depends on four things: model size and precision, the context length you allow, how long answers are, and how often people actually send requests. Ten people who each send a request every few minutes generate far less load than ten people hammering a chat window. The honest answer is to measure: replay realistic traffic at increasing request rates and find the point where your latency target breaks. That number, not a guess, goes into your capacity plan. | FAQ: users per GPU?<br>• Depends on model, context, answer length, request rate<br>• Users ≠ load<br>• Replay traffic, find the latency breaking point | Line chart of p95 latency against request rate with a horizontal target line; a marker shows the breaking point and a label reads "capacity". |
| 12 | 6:45 | 28 | Try this now. If you have access to any NVIDIA GPU, even in a notebook environment, launch vLLM with a small model and a maximum context of four thousand tokens. Find the log line that reports the KV cache capacity. Then restart with sixteen thousand and read it again. Watching that number shrink is the fastest way to understand why context limits matter for concurrency. | Try this now<br>• vllm serve a small model at 4k context<br>• Read the KV capacity log line<br>• Restart at 16k and compare | Two terminal snapshots side by side with the capacity log line highlighted; a number animates from larger to smaller as context grows. |
| 13 | 7:13 | 70 | Watch me do it. I launch vllm serve with an eight billion model, maximum context eight thousand, memory utilization zero point nine and an API key. While it starts, I watch the log and find the line reporting KV cache capacity and maximum concurrency. I note both numbers. Then I run the benchmark command, replaying two hundred prompts from our support logs at two, four and eight requests per second. For each rate I copy the median and ninety-fifth percentile time to first token and inter-token latency into a table. At four requests per second, first tokens arrive in about a second at p ninety-five. At eight, queueing pushes it past four seconds. I open the metrics endpoint and point a dashboard at it, with an alert when queue length stays high for five minutes. Then I write the capacity note: one GPU handles up to four requests per second within target; add a replica beyond that. Illustrative numbers, real method. | Watch me do it<br>1 vllm serve + read capacity line<br>2 Benchmark at 3 rates<br>3 Record p50/p95<br>4 Dashboard + alert<br>5 One-paragraph capacity note | Terminal with vllm serve logs scrolling and one capacity line highlighted; a benchmark results table filling in for three rates; a dashboard with a queue-length panel and an alert rule; a short capacity note document. |
| 14 | 8:23 | 28 | Recap. vLLM is built for throughput under concurrency thanks to PagedAttention and continuous batching. Tune maximum context and memory first, and read the capacity line in the log. Measure first-token and inter-token latency at realistic load, watch the ninety-fifth percentile, and monitor metrics. Your next step: serve a model, benchmark it at three request rates with your own prompts, and write a one-page capacity note. | Recap<br>• PagedAttention + continuous batching<br>• Tune context, read capacity<br>• Next: 3-rate benchmark + capacity note | Recap card, then a blank one-page capacity note template with headings Model, Hardware, Rates tested, p95, Decision. |

<details><summary>Narration per scene (paste into ElevenLabs)</summary>

Scene 1 — Serving at scale with vLLM

```text
Your pilot was a hit. Twelve people loved the local assistant. Now the whole company wants it, plus an application that fires hundreds of requests at once. The desktop runner that was perfect for one person starts to queue, stall and time out. This lesson is about serving at scale with vLLM: why it is fast, how to launch it properly, which flags to tune, and how to measure performance like an operator.
```

Scene 2 — Analogy: a professional kitchen

```text
Think of a busy restaurant again. A desktop runner is a single chef cooking one order at a time from start to finish. vLLM is a professional kitchen with a head chef who starts new dishes the moment a burner frees up, shares prep work between similar orders, and never leaves a stove idle. That is continuous batching and prefix caching. And PagedAttention is smart shelving: instead of reserving a whole shelf for each order, ingredients go wherever there is space. Same ingredients, same chefs, far more dishes per hour.
```

Scene 3 — Why vLLM is fast

```text
Two ideas make vLLM fast. The first is PagedAttention. Instead of reserving one huge block of memory per request for context it might never use, vLLM stores the attention cache in small pages allocated on demand, like virtual memory in an operating system. Less waste, more requests per GPU. The second is continuous batching. Requests join and leave the running batch at every step, so a short answer never waits for a long one, and the GPU never sits idle.
```

Scene 4 — Production features

```text
On top of that you get prefix caching, which reuses work for shared prompt beginnings like a long system prompt or the same retrieved document. You get quantized checkpoints such as FP8, AWQ and GPTQ, tensor parallelism across GPUs, structured outputs, tool calling, and serving many fine-tuned adapters on one base model. And it speaks the OpenAI API, so your existing code barely changes.
```

Scene 5 — Launch and tune

```text
Launching is one command: vllm serve, the model name, a maximum context length, a GPU memory budget and an API key. Or use the official Docker image, pinned to a specific tag. Then watch the startup log. vLLM tells you how much memory it allocated for the cache and how many requests it can hold at full context. That single line is your capacity plan. Two flags matter most. Max model length: set it to what users need, not the model's maximum, because every extra token of allowed context reduces concurrency. And GPU memory utilization: lower it if something else shares the card.
```

Scene 6 — Measure it

```text
Now measure like an operator. Users feel two things: time to first token, how long before text appears, and inter-token latency, how smoothly it streams. Operators also watch throughput and the ninety-fifth percentile, because averages hide the unhappy users. vLLM includes a benchmarking command that replays prompts at a target rate and reports all of these. And it exposes Prometheus metrics, queue length, cache usage and token throughput, ready for dashboards and alerts.
```

Scene 7 — Worked example: Abu Dhabi insurer (illustrative)

```text
Let's walk through an insurer in Abu Dhabi, with illustrative numbers. A hundred and twenty claims adjusters, around forty at peak. They serve a thirty-two billion model at FP8 on one eighty gigabyte GPU with a twelve thousand token context. They replay five hundred real, redacted prompts at two, four and eight requests per second. At four per second, the ninety-fifth percentile first token is under two seconds. Fine. At eight, it passes six seconds. Not fine. So one GPU handles normal peaks, and a second replica handles month-end surges. Alerts fire when the queue or cache usage runs hot.
```

Scene 8 — Production checklist

```text
Before you call it production, run the checklist. Authentication and TLS, and network isolation. Pinned model revision and container tag, recorded in your register. Health checks. Metrics, dashboards and alerts. A load test before launch and after every model change. Rate limits per client at the gateway. And logging that respects your privacy policy. Three classic mistakes: maximum context set to the model limit, a so-called load test with one request at a time, and an unauthenticated server because it is only internal.
```

Scene 9 — Simple example: 10-person firm

```text
A simple example. A ten-person accountancy firm in Birmingham runs vLLM on one workstation GPU with an eight billion model. They set maximum context to eight thousand tokens because their documents are short after retrieval. At startup, the log says the cache can hold dozens of eight-thousand-token requests at once, far more than ten staff will ever send together. They run a quick benchmark at two requests per second, see first tokens in well under a second, and they are done. Not every deployment needs a capacity war room.
```

Scene 10 — Scale up vs scale out

```text
A word on scaling out. When one GPU is not enough, you have two options. Scale up: a bigger card, or tensor parallelism across several cards in one machine, for models that do not fit. Or scale out: several identical replicas behind a load balancer, for more concurrent users. For most business workloads, replicas are simpler, more resilient, and let you do rolling upgrades, one replica at a time, with no downtime. Decide your scale-out trigger in advance, for example, queue length above a threshold for ten minutes.
```

Scene 11 — FAQ: users per GPU?

```text
A frequent question: how many users can one GPU serve? It depends on four things: model size and precision, the context length you allow, how long answers are, and how often people actually send requests. Ten people who each send a request every few minutes generate far less load than ten people hammering a chat window. The honest answer is to measure: replay realistic traffic at increasing request rates and find the point where your latency target breaks. That number, not a guess, goes into your capacity plan.
```

Scene 12 — Try this now

```text
Try this now. If you have access to any NVIDIA GPU, even in a notebook environment, launch vLLM with a small model and a maximum context of four thousand tokens. Find the log line that reports the KV cache capacity. Then restart with sixteen thousand and read it again. Watching that number shrink is the fastest way to understand why context limits matter for concurrency.
```

Scene 13 — Watch me do it

```text
Watch me do it. I launch vllm serve with an eight billion model, maximum context eight thousand, memory utilization zero point nine and an API key. While it starts, I watch the log and find the line reporting KV cache capacity and maximum concurrency. I note both numbers. Then I run the benchmark command, replaying two hundred prompts from our support logs at two, four and eight requests per second. For each rate I copy the median and ninety-fifth percentile time to first token and inter-token latency into a table. At four requests per second, first tokens arrive in about a second at p ninety-five. At eight, queueing pushes it past four seconds. I open the metrics endpoint and point a dashboard at it, with an alert when queue length stays high for five minutes. Then I write the capacity note: one GPU handles up to four requests per second within target; add a replica beyond that. Illustrative numbers, real method.
```

Scene 14 — Recap

```text
Recap. vLLM is built for throughput under concurrency thanks to PagedAttention and continuous batching. Tune maximum context and memory first, and read the capacity line in the log. Measure first-token and inter-token latency at realistic load, watch the ninety-fifth percentile, and monitor metrics. Your next step: serve a model, benchmark it at three request rates with your own prompts, and write a one-page capacity note.
```

</details>

### 11. Choosing a serving engine: SGLang, TGI, TensorRT-LLM and friends

Lesson `choosing-a-serving-engine` · module “Serving at scale” · target 8 min · 14 scenes · 1151 words · planned 8:20 (words ≈ 8:20) · status: **to produce**

| Term | Say |
|---|---|
| SGLang | S G Lang |
| RadixAttention | radix attention |
| TensorRT-LLM | tensor R T L L M |
| TGI | T G I |
| ROCm | rock M |

| # | Time | Sec | Narration | On screen | Visual |
|---|---|---|---|---|---|
| 1 | 0:00 | 30 | Every few months someone posts a chart claiming a new serving engine is three times faster. Sometimes it is, for their traffic, on their hardware. The question is whether it is faster for yours. In this lesson you will get a clear map of the main engines as of September twenty twenty-six, a five-question decision guide, and a small bake-off script to test engines with your own prompts. | Choosing a serving engine<br>• The landscape<br>• A five-question guide<br>• Your own bake-off | A bar chart with a dramatic "3x faster" label slowly zooms out to reveal a tiny footnote reading "on our workload". |
| 2 | 0:30 | 37 | Choosing a serving engine is like choosing a courier company. They all deliver parcels. One has the biggest network and works almost everywhere. One is especially fast on repeat routes to the same addresses. One is a specialist with custom vans that are fastest on one type of road but take longer to set up. And one is a local courier that goes anywhere, even without roads. You pick based on your routes and your parcels, and you test with real deliveries before signing a contract. | Analogy: courier companies<br>• Biggest network = vLLM<br>• Fast on repeat routes = SGLang prefix reuse<br>• Custom vans = TensorRT-LLM<br>• Goes anywhere = llama.cpp | Map with four courier route overlays appearing one by one in different line styles; a stack of parcels labeled "your traffic" is placed at the center. |
| 3 | 1:07 | 40 | The main options. vLLM is the default for GPU serving: broad model and hardware support and a huge community. SGLang is a fast runtime whose RadixAttention reuses shared prompt prefixes automatically, and it is strong for multi-turn chat, agents and structured output. TensorRT-LLM squeezes maximum performance from NVIDIA GPUs, with more build complexity. The llama dot cpp server runs almost anywhere. Ollama is the simplest to manage. And Hugging Face's TGI, once very popular, was moved into maintenance mode in late twenty twenty-five, with Hugging Face pointing new deployments to vLLM or SGLang. | The landscape (Sep 2026)<br>• vLLM: default GPU choice<br>• SGLang: prefix reuse, structured output<br>• TensorRT-LLM: NVIDIA peak<br>• llama.cpp / Ollama: anywhere, simple<br>• TGI: maintenance mode | Six neutral text tiles (no logos) arranged on a map by hardware reach and throughput; the TGI tile has a small amber "maintenance" tag. |
| 4 | 1:47 | 33 | Here is what RadixAttention buys you. Imagine a support assistant where every request starts with the same three-thousand-token system prompt and often the same popular policy documents. SGLang keeps the attention cache for those shared beginnings in a tree and reuses it, so it only computes each user's unique part. vLLM has prefix caching too; the implementations differ, and so do the workloads where each shines. That is exactly why you test on your own traffic. | Shared prefixes<br>• Same system prompt + popular docs<br>• Reuse cached work, compute only the unique suffix<br>• Both engines cache prefixes: test yours | Tree diagram: a thick trunk labeled shared system prompt, branches for popular documents, and many small leaves for individual user questions, with only the leaves lighting up as computed. |
| 5 | 2:20 | 39 | Five questions decide it. One, hardware: NVIDIA data-center cards open every option, AMD needs a check of current support, Apple and CPUs point to llama dot cpp. Two, model support: does the engine handle your exact architecture, quantization, tool-call format and reasoning output? Three, traffic shape: long shared prefixes, or many small independent requests? Four, team skills: do you have GPU and Kubernetes operators, or should you start with Docker or a managed endpoint? Five, longevity: active releases and security fixes. Do not start new work on maintenance-mode software. | Five questions<br>1 Hardware<br>2 Model support<br>3 Traffic shape<br>4 Team skills<br>5 Longevity | Five numbered cards in a row, each flipping to reveal a small icon: chip, model file, traffic graph, people, calendar. |
| 6 | 2:59 | 29 | Remember you do not always have to run GPUs yourself. Managed endpoints and inference clouds host open-weight models behind an API. You keep the benefits of open weights, like version choice and no lock-in to one model vendor, but your data now travels to that provider. So check the region and the data terms, especially if you operate under Saudi, Emirati, Pakistani or UK data protection rules. | Managed option<br>• Open weights, someone else runs GPUs<br>• Data goes to the provider<br>• Check region + data terms | Diagram of an application calling a hosted endpoint in a cloud, with a region pin and a document icon labeled data terms. |
| 7 | 3:28 | 34 | Now the bake-off. Same model, same precision, same context limit, same prompts, same request rates, on both engines. The lesson includes a short async Python script that streams responses from each engine at a fixed concurrency and prints median and ninety-fifth percentile time to first token. For rigorous numbers, use each engine's own benchmark tool. For a quick apples-to-apples feel with your real prompts, this is perfect, and because both speak the OpenAI API, switching is just a URL. | Bake-off rules<br>• Same model, precision, context<br>• Same prompts and rates<br>• Compare p50/p95 TTFT, throughput, errors | Split terminal: two engines receiving identical streams of requests; result lines print side by side with p50 and p95 values. |
| 8 | 4:02 | 30 | A migration story. A Manchester e-commerce firm has run TGI since twenty twenty-four. They want newer models that it will not support. So they stand vLLM up beside TGI behind their gateway, mirror five percent of traffic for two weeks, compare quality and latency, then switch. Because both expose OpenAI-style endpoints, the application change is a configuration flip. That is the pattern for any engine change: shadow, compare, switch. | Worked example: migrating off TGI<br>• Stand up vLLM alongside<br>• Mirror 5% of traffic for 2 weeks<br>• Compare, then switch via config | Gateway diagram with traffic split: a thick arrow to the old engine and a thin mirrored arrow to the new one, then the thickness swaps. |
| 9 | 4:32 | 34 | A simple example. A university IT team in Islamabad runs an internal Q and A bot on one GPU. Every request starts with the same long policy document. They try vLLM with prefix caching on and SGLang with its default prefix reuse, same model, same precision, same prompts. For their traffic, both give similar first-token times, and vLLM fits their existing Docker setup better, so they pick vLLM. The bake-off took one afternoon and ended a two-week debate. | Simple example: university Q&A<br>• Same long policy prefix on every request<br>• vLLM prefix caching vs SGLang<br>• Similar results → pick what fits ops<br>• One afternoon ends the debate | Two engine boxes receiving identical request streams; a results table appears with nearly equal bars, and a Docker-shaped icon tips the decision to one side. |
| 10 | 5:06 | 35 | One more consideration people forget: how the engine handles the model's special outputs. Reasoning models may emit a thinking section before the answer. Tool-calling models format calls in family-specific ways. Engines use parsers to turn these into clean API fields, and if the parser for your model is missing or buggy, your application sees garbled tool calls or leaked reasoning text. Before committing, test your exact model's tool calls and reasoning output through each engine's API, not just plain chat. | Check the parsers<br>• Reasoning sections<br>• Family-specific tool-call formats<br>• Missing parser → garbled calls or leaked thinking<br>• Test tools + reasoning through the API | Split view: raw model output with a thinking block and a tool call in a custom format, passing through a parser box into clean JSON fields on the other side. |
| 11 | 5:41 | 39 | People often ask: will I be locked into one engine? Much less than you might fear. Because vLLM, SGLang, llama dot cpp and most managed endpoints expose OpenAI-compatible APIs, your application code rarely changes. The real switching costs are operational: container images, monitoring dashboards, flags and runbooks. Keep those documented and your engine becomes a replaceable component. Another question: should small teams even run their own engine? If you have steady volume, data residency needs or cost pressure, yes. If not, a managed endpoint may be the better first step. | FAQ<br>• Lock-in? Low for app code (OpenAI-compatible)<br>• Real costs: images, dashboards, flags, runbooks<br>• Small teams: self-host for volume/residency/cost; else managed | An application box connected by a single standard plug to interchangeable engine boxes; a side panel lists operational items as the real switching cost. |
| 12 | 6:20 | 24 | Try this now. List the three questions that would decide your engine choice today: your hardware, your exact model and features like tool calling, and whether your traffic shares long prefixes. Write one sentence per question. If any answer is I do not know, that is the first thing to find out before you compare engines. | Try this now<br>• Hardware?<br>• Exact model + tool calling/reasoning needs?<br>• Shared long prefixes?<br>• Unknowns first | Three question cards flip over to reveal short answers; one card shows a question mark and pulses to indicate "find out first". |
| 13 | 6:44 | 66 | Watch me do it. I start vLLM on port eight thousand and SGLang on port thirty thousand, same model, same precision, same maximum context. I check both health endpoints. Then I prepare one hundred real prompts, most of which share our long system prompt, and run the bake-off script at sixteen concurrent requests against each engine. It prints median and ninety-fifth percentile time to first token. I repeat at thirty-two concurrent requests. Next I test what the lesson warned about: tool calls. I send ten prompts that should trigger our lookup tool and check both engines return properly parsed tool calls. One engine needs a specific parser flag for this model family; I add it and rerun. Finally I write the table: engine, first-token latency at both loads, error count, tool-call success, and a sentence on which one fits our Docker setup. The decision takes an afternoon and ends the debate. | Watch me do it<br>1 Two engines, identical settings<br>2 100 real prompts at 16 and 32 concurrency<br>3 Test tool-call parsing<br>4 Fix parser flag<br>5 Decision table | Two terminal panes each launching an engine; a load script printing results for both; a tool-call test grid with ticks and one cross fixed after a flag change; a final comparison table. |
| 14 | 7:50 | 30 | Recap. vLLM is the sensible default for GPU serving, SGLang shines with shared prefixes and structured output, TensorRT-LLM for peak NVIDIA performance, llama dot cpp for anywhere else, and TGI only for existing deployments you plan to migrate. Decide on hardware, model support, traffic, skills and longevity, then prove it with a fair bake-off. Your next step: run the bake-off on your hardware with a hundred of your prompts. | Recap<br>• Right engine for hardware and traffic<br>• Avoid maintenance-mode for new work<br>• Next: a fair bake-off | Recap card with the five-question icons in a row and a trophy icon marked "your traffic". |

<details><summary>Narration per scene (paste into ElevenLabs)</summary>

Scene 1 — Choosing a serving engine

```text
Every few months someone posts a chart claiming a new serving engine is three times faster. Sometimes it is, for their traffic, on their hardware. The question is whether it is faster for yours. In this lesson you will get a clear map of the main engines as of September twenty twenty-six, a five-question decision guide, and a small bake-off script to test engines with your own prompts.
```

Scene 2 — Analogy: courier companies

```text
Choosing a serving engine is like choosing a courier company. They all deliver parcels. One has the biggest network and works almost everywhere. One is especially fast on repeat routes to the same addresses. One is a specialist with custom vans that are fastest on one type of road but take longer to set up. And one is a local courier that goes anywhere, even without roads. You pick based on your routes and your parcels, and you test with real deliveries before signing a contract.
```

Scene 3 — The landscape (Sep 2026)

```text
The main options. vLLM is the default for GPU serving: broad model and hardware support and a huge community. SGLang is a fast runtime whose RadixAttention reuses shared prompt prefixes automatically, and it is strong for multi-turn chat, agents and structured output. TensorRT-LLM squeezes maximum performance from NVIDIA GPUs, with more build complexity. The llama dot cpp server runs almost anywhere. Ollama is the simplest to manage. And Hugging Face's TGI, once very popular, was moved into maintenance mode in late twenty twenty-five, with Hugging Face pointing new deployments to vLLM or SGLang.
```

Scene 4 — Shared prefixes

```text
Here is what RadixAttention buys you. Imagine a support assistant where every request starts with the same three-thousand-token system prompt and often the same popular policy documents. SGLang keeps the attention cache for those shared beginnings in a tree and reuses it, so it only computes each user's unique part. vLLM has prefix caching too; the implementations differ, and so do the workloads where each shines. That is exactly why you test on your own traffic.
```

Scene 5 — Five questions

```text
Five questions decide it. One, hardware: NVIDIA data-center cards open every option, AMD needs a check of current support, Apple and CPUs point to llama dot cpp. Two, model support: does the engine handle your exact architecture, quantization, tool-call format and reasoning output? Three, traffic shape: long shared prefixes, or many small independent requests? Four, team skills: do you have GPU and Kubernetes operators, or should you start with Docker or a managed endpoint? Five, longevity: active releases and security fixes. Do not start new work on maintenance-mode software.
```

Scene 6 — Managed option

```text
Remember you do not always have to run GPUs yourself. Managed endpoints and inference clouds host open-weight models behind an API. You keep the benefits of open weights, like version choice and no lock-in to one model vendor, but your data now travels to that provider. So check the region and the data terms, especially if you operate under Saudi, Emirati, Pakistani or UK data protection rules.
```

Scene 7 — Bake-off rules

```text
Now the bake-off. Same model, same precision, same context limit, same prompts, same request rates, on both engines. The lesson includes a short async Python script that streams responses from each engine at a fixed concurrency and prints median and ninety-fifth percentile time to first token. For rigorous numbers, use each engine's own benchmark tool. For a quick apples-to-apples feel with your real prompts, this is perfect, and because both speak the OpenAI API, switching is just a URL.
```

Scene 8 — Worked example: migrating off TGI

```text
A migration story. A Manchester e-commerce firm has run TGI since twenty twenty-four. They want newer models that it will not support. So they stand vLLM up beside TGI behind their gateway, mirror five percent of traffic for two weeks, compare quality and latency, then switch. Because both expose OpenAI-style endpoints, the application change is a configuration flip. That is the pattern for any engine change: shadow, compare, switch.
```

Scene 9 — Simple example: university Q&A

```text
A simple example. A university IT team in Islamabad runs an internal Q and A bot on one GPU. Every request starts with the same long policy document. They try vLLM with prefix caching on and SGLang with its default prefix reuse, same model, same precision, same prompts. For their traffic, both give similar first-token times, and vLLM fits their existing Docker setup better, so they pick vLLM. The bake-off took one afternoon and ended a two-week debate.
```

Scene 10 — Check the parsers

```text
One more consideration people forget: how the engine handles the model's special outputs. Reasoning models may emit a thinking section before the answer. Tool-calling models format calls in family-specific ways. Engines use parsers to turn these into clean API fields, and if the parser for your model is missing or buggy, your application sees garbled tool calls or leaked reasoning text. Before committing, test your exact model's tool calls and reasoning output through each engine's API, not just plain chat.
```

Scene 11 — FAQ

```text
People often ask: will I be locked into one engine? Much less than you might fear. Because vLLM, SGLang, llama dot cpp and most managed endpoints expose OpenAI-compatible APIs, your application code rarely changes. The real switching costs are operational: container images, monitoring dashboards, flags and runbooks. Keep those documented and your engine becomes a replaceable component. Another question: should small teams even run their own engine? If you have steady volume, data residency needs or cost pressure, yes. If not, a managed endpoint may be the better first step.
```

Scene 12 — Try this now

```text
Try this now. List the three questions that would decide your engine choice today: your hardware, your exact model and features like tool calling, and whether your traffic shares long prefixes. Write one sentence per question. If any answer is I do not know, that is the first thing to find out before you compare engines.
```

Scene 13 — Watch me do it

```text
Watch me do it. I start vLLM on port eight thousand and SGLang on port thirty thousand, same model, same precision, same maximum context. I check both health endpoints. Then I prepare one hundred real prompts, most of which share our long system prompt, and run the bake-off script at sixteen concurrent requests against each engine. It prints median and ninety-fifth percentile time to first token. I repeat at thirty-two concurrent requests. Next I test what the lesson warned about: tool calls. I send ten prompts that should trigger our lookup tool and check both engines return properly parsed tool calls. One engine needs a specific parser flag for this model family; I add it and rerun. Finally I write the table: engine, first-token latency at both loads, error count, tool-call success, and a sentence on which one fits our Docker setup. The decision takes an afternoon and ends the debate.
```

Scene 14 — Recap

```text
Recap. vLLM is the sensible default for GPU serving, SGLang shines with shared prefixes and structured output, TensorRT-LLM for peak NVIDIA performance, llama dot cpp for anywhere else, and TGI only for existing deployments you plan to migrate. Decide on hardware, model support, traffic, skills and longevity, then prove it with a fair bake-off. Your next step: run the bake-off on your hardware with a hundred of your prompts.
```

</details>

### 12. On-device AI and small language models

Lesson `on-device-and-small-models` · module “Edge AI, privacy and honest benchmarking” · target 8 min · 14 scenes · 1154 words · planned 8:20 (words ≈ 8:22) · status: **to produce**

| Term | Say |
|---|---|
| MLX | M L X |
| LiteRT | lite R T |
| MediaPipe | media pipe |
| WebGPU | web G P U |
| ONNX | onyx |
| Ministral | mini-STRAL |

| # | Time | Sec | Narration | On screen | Visual |
|---|---|---|---|---|---|
| 1 | 0:00 | 30 | Here is a quiet revolution. The phone in your pocket can now run a language model that rewrites messages, classifies requests and extracts data with no network, no per-request cost and no data leaving the device. In this lesson you will meet the small model families, the runtimes on Apple, Android, browsers and Windows, the design patterns that make small models shine, and how to evaluate them on real devices. | On-device AI and small models<br>• Families and runtimes<br>• Design patterns<br>• Evaluating on real devices | Close-up of a phone in airplane mode; a text bubble transforms into a neat structured card, with a small "processed on device" badge. |
| 2 | 0:30 | 37 | Think of small models as specialist tools in a toolbox. A big frontier model is a skilled general contractor who can do almost anything, but you pay for every hour and they have to travel to your site. A small on-device model is a good screwdriver: it lives in your pocket, costs nothing each time you use it, and does one job very well. You would not ask a screwdriver to build a house. But you also would not call a contractor to tighten one screw. | Analogy: toolbox<br>• Frontier model = general contractor<br>• Small model = screwdriver in your pocket<br>• Right tool for the job | Left: a contractor icon with a travel route and an hourly meter. Right: a single screwdriver icon in a pocket with a "zero cost per use" tag. A screw appears and the screwdriver highlights. |
| 3 | 1:07 | 35 | Small language models, roughly half a billion to eight billion parameters, trade breadth of knowledge and deep reasoning for four things: privacy, zero marginal cost, offline operation and very low latency. For narrow jobs, classification, extraction, short rewrites, autocomplete, routing, a well-chosen small model is often all you need. Current families include Gemma 4's edge sizes, Mistral's Ministral 3, Microsoft's Phi-4-mini, small Qwen models, and at the top of edge territory, gpt-oss twenty B. As always, check the model cards. | Small models<br>• ≈0.5–8B parameters<br>• Privacy, zero marginal cost, offline, low latency<br>• Gemma 4 edge · Ministral 3 · Phi-4-mini · small Qwen · gpt-oss-20b | Four icons for privacy, cost, offline and speed orbit a small chip; model family names appear as plain text labels around it. |
| 4 | 1:42 | 39 | Where do they run? On Apple devices, MLX runs open models on Apple silicon, and Apple's Foundation Models framework gives apps access to the on-device model behind Apple Intelligence. On Android, Google offers Gemini Nano through ML Kit on supported devices, and LiteRT and MediaPipe run open models like Gemma. In the browser, Chrome has built-in AI APIs backed by Gemini Nano, and WebLLM and Transformers dot js run open models on WebGPU. On Windows, there is ONNX Runtime and Microsoft's Foundry Local. Availability shifts, so check current docs. | Runtimes<br>• Apple: MLX, Foundation Models framework<br>• Android: Gemini Nano (ML Kit), LiteRT/MediaPipe<br>• Browser: built-in AI APIs, WebLLM, Transformers.js<br>• Windows: ONNX Runtime, Foundry Local | Four device silhouettes, laptop, phone, browser window and PC, each with a plain-text runtime label appearing beneath. |
| 5 | 2:21 | 30 | Built-in models versus shipping your own. A built-in model means your users download nothing extra. But you do not control the version, and it depends on device and region. Shipping an open model gives you control and consistency, at the cost of app size and update management. Many apps use both: the built-in model where available, their own small model elsewhere, and a cloud fallback with user consent. | Built-in vs your own<br>• Built-in: no download, no version control<br>• Your own: control, bigger app<br>• Often: both + cloud fallback | Two-column comparison card with a toggle switching between Built-in and Bundled, and a cloud icon linked by a dotted line labeled "with consent". |
| 6 | 2:51 | 34 | Five patterns make small models shine. One, on-device first, with a cloud fallback when confidence is low, and the user's permission. Two, narrow the task: tight instructions, a fixed label set, constrained output. Three, retrieval, not memorization: pair the model with a local search index. Four, fine-tune for the job, because a small model trained on a few thousand examples of one task can beat a much bigger general model on that task. Five, respect battery and heat. | Five patterns<br>1 On-device first, cloud fallback<br>2 Narrow the task<br>3 Local retrieval<br>4 Task fine-tuning<br>5 Battery and heat | Five numbered tiles arranged in a pentagon around a phone icon, each lighting up as mentioned. |
| 7 | 3:25 | 31 | A worked example. A beverage distributor in Saudi Arabia sends reps to shops in remote areas with patchy coverage. Their tablet app turns voice-note transcripts into structured visit reports, shop, stock issues, next order, in Arabic and English, using a small model fine-tuned on three thousand reviewed reports. It runs offline in seconds and syncs when the tablet reconnects. Big analytical questions go to a cloud assistant back at head office. | Worked example: field sales in KSA<br>• Offline voice notes → structured reports<br>• Arabic + English<br>• Fine-tuned small model, syncs later<br>• Big questions go to the cloud | Map with scattered shop pins in a desert region, a tablet producing structured report cards offline, then a sync arrow when a signal icon appears. |
| 8 | 3:56 | 33 | The hands-on uses MLX on a Mac: one command to generate text with a four-bit small model, and a tiny Python classifier that forces one label from a fixed list and maps anything unexpected to other. Never trust free text from a small model. Then evaluate properly. Test on the devices your users actually own, often mid-range phones, over a twenty-minute session. Measure latency, memory, battery drain and heat, plus task accuracy on a gold set. | Hands-on + evaluation<br>• mlx_lm.generate + a fixed-label classifier<br>• Map unknown outputs to "other"<br>• Test on real devices, 20-minute sessions<br>• Latency, memory, battery, heat, accuracy | Screen recording of a terminal running mlx_lm.generate, then a Python output showing a label; followed by a device test dashboard with battery and temperature graphs. |
| 9 | 4:29 | 33 | A simple example. A note-taking app wants to suggest three tags for every note a user writes. Tags come from the user's own existing list. That is a narrow task, a fixed label set, short input, and mistakes are harmless because the user can remove a tag with one tap. A small model on the phone does it instantly, offline, for free, and the note never leaves the device. This is the sweet spot for on-device AI. | Simple example: note tagging<br>• Narrow task, fixed label set<br>• Harmless, easy-to-fix mistakes<br>• Instant, offline, free, private | Phone mock-up with a note being typed; three tag chips animate in below it, and one is removed with a tap to show easy correction. |
| 10 | 5:02 | 37 | Shipping a model inside an app raises practical questions. Model files can be several gigabytes, so download them after install, on Wi-Fi, with a progress indicator and a resume option. Check free storage first. Plan updates: a new model version is a new download, so version it and allow rollback. And remember older devices. In many markets, including large parts of Pakistan and the wider region, mid-range and older phones dominate, so decide a minimum device spec and give everyone else the cloud fallback with consent. | Shipping models in apps<br>• Download after install, on Wi-Fi, resumable<br>• Check free storage<br>• Versioned updates + rollback<br>• Minimum device spec + cloud fallback | Phone screen mock-up showing a download progress bar for an AI model with a Wi-Fi icon, followed by a device compatibility table with green, amber and red rows. |
| 11 | 5:39 | 41 | A question from product teams: how do we update an on-device model safely? Treat it like any app release. Version the model file, ship it to a small percentage of users first, compare accuracy and crash or battery metrics against the previous version, and keep the ability to roll back. Also keep the app working if the new model has not downloaded yet. And a second question: can small models handle Arabic or Urdu? Some do reasonably, many do not. Test your languages early, because language coverage is often the deciding factor for small models. | FAQ<br>• Updating on-device models: version, staged rollout, compare, rollback<br>• Keep app working before download<br>• Arabic/Urdu? Test early; often the deciding factor | Phone icons receiving a new model version in a small group first, with metric cards comparing versions; then a language test card with Arabic and Urdu samples. |
| 12 | 6:20 | 26 | Try this now. Look at your product or your daily work and list three tasks that are narrow, frequent and low-risk: things like tagging, short rewrites or routing. For each one, write the fixed set of outputs it should produce. If you can write that set in one line, the task is probably a good fit for a small model. | Try this now<br>• List 3 narrow, frequent, low-risk tasks<br>• Write each task's fixed output set<br>• One line? Good small-model fit | Checklist card with three rows; each row gets a short output set typed in, and a small chip icon appears beside rows that fit on one line. |
| 13 | 6:46 | 64 | Watch me do it. On a Mac, I install mlx lm and run a four-bit, four billion parameter model with a one-line prompt to rewrite a message politely. It answers in about a second. Now a real task: classify thirty support messages into billing, delivery, technical or other. I run the Python classifier from the lesson, which forces one label and maps anything unexpected to other. It gets twenty-six right. Three misses are messages mentioning two problems; I add a priority rule to the system prompt and it gets twenty-eight. Then I check the device side: I watch memory use in the activity monitor, around three gigabytes, and run the classifier on a loop for twenty minutes while noting battery and temperature. Everything stays reasonable. Last step, I note what would happen on an older device, and decide the cloud fallback threshold for users below our minimum spec. | Watch me do it<br>1 mlx_lm quick test<br>2 Classify 30 messages<br>3 Add a priority rule<br>4 20-minute device test<br>5 Set fallback rules | Terminal with mlx_lm.generate output; a small results table 26/30 → 28/30; an activity-monitor-style memory graph and a battery curve during a 20-minute loop; a note defining the fallback threshold. |
| 14 | 7:50 | 30 | Recap. Small models win on privacy, cost, offline use and latency for narrow tasks. Choose between built-in platform models and your own, and often use both. Narrow the task, add local retrieval, fine-tune when needed, and always test on real target devices. Your next step: pick one narrow task from your product, run a small model on your own device, and measure accuracy on thirty examples plus latency and memory. | Recap<br>• Narrow tasks, small models<br>• Built-in vs bundled vs cloud<br>• Next: one task, 30 examples, real device | Recap card with a phone icon and three ticks. |

<details><summary>Narration per scene (paste into ElevenLabs)</summary>

Scene 1 — On-device AI and small models

```text
Here is a quiet revolution. The phone in your pocket can now run a language model that rewrites messages, classifies requests and extracts data with no network, no per-request cost and no data leaving the device. In this lesson you will meet the small model families, the runtimes on Apple, Android, browsers and Windows, the design patterns that make small models shine, and how to evaluate them on real devices.
```

Scene 2 — Analogy: toolbox

```text
Think of small models as specialist tools in a toolbox. A big frontier model is a skilled general contractor who can do almost anything, but you pay for every hour and they have to travel to your site. A small on-device model is a good screwdriver: it lives in your pocket, costs nothing each time you use it, and does one job very well. You would not ask a screwdriver to build a house. But you also would not call a contractor to tighten one screw.
```

Scene 3 — Small models

```text
Small language models, roughly half a billion to eight billion parameters, trade breadth of knowledge and deep reasoning for four things: privacy, zero marginal cost, offline operation and very low latency. For narrow jobs, classification, extraction, short rewrites, autocomplete, routing, a well-chosen small model is often all you need. Current families include Gemma 4's edge sizes, Mistral's Ministral 3, Microsoft's Phi-4-mini, small Qwen models, and at the top of edge territory, gpt-oss twenty B. As always, check the model cards.
```

Scene 4 — Runtimes

```text
Where do they run? On Apple devices, MLX runs open models on Apple silicon, and Apple's Foundation Models framework gives apps access to the on-device model behind Apple Intelligence. On Android, Google offers Gemini Nano through ML Kit on supported devices, and LiteRT and MediaPipe run open models like Gemma. In the browser, Chrome has built-in AI APIs backed by Gemini Nano, and WebLLM and Transformers dot js run open models on WebGPU. On Windows, there is ONNX Runtime and Microsoft's Foundry Local. Availability shifts, so check current docs.
```

Scene 5 — Built-in vs your own

```text
Built-in models versus shipping your own. A built-in model means your users download nothing extra. But you do not control the version, and it depends on device and region. Shipping an open model gives you control and consistency, at the cost of app size and update management. Many apps use both: the built-in model where available, their own small model elsewhere, and a cloud fallback with user consent.
```

Scene 6 — Five patterns

```text
Five patterns make small models shine. One, on-device first, with a cloud fallback when confidence is low, and the user's permission. Two, narrow the task: tight instructions, a fixed label set, constrained output. Three, retrieval, not memorization: pair the model with a local search index. Four, fine-tune for the job, because a small model trained on a few thousand examples of one task can beat a much bigger general model on that task. Five, respect battery and heat.
```

Scene 7 — Worked example: field sales in KSA

```text
A worked example. A beverage distributor in Saudi Arabia sends reps to shops in remote areas with patchy coverage. Their tablet app turns voice-note transcripts into structured visit reports, shop, stock issues, next order, in Arabic and English, using a small model fine-tuned on three thousand reviewed reports. It runs offline in seconds and syncs when the tablet reconnects. Big analytical questions go to a cloud assistant back at head office.
```

Scene 8 — Hands-on + evaluation

```text
The hands-on uses MLX on a Mac: one command to generate text with a four-bit small model, and a tiny Python classifier that forces one label from a fixed list and maps anything unexpected to other. Never trust free text from a small model. Then evaluate properly. Test on the devices your users actually own, often mid-range phones, over a twenty-minute session. Measure latency, memory, battery drain and heat, plus task accuracy on a gold set.
```

Scene 9 — Simple example: note tagging

```text
A simple example. A note-taking app wants to suggest three tags for every note a user writes. Tags come from the user's own existing list. That is a narrow task, a fixed label set, short input, and mistakes are harmless because the user can remove a tag with one tap. A small model on the phone does it instantly, offline, for free, and the note never leaves the device. This is the sweet spot for on-device AI.
```

Scene 10 — Shipping models in apps

```text
Shipping a model inside an app raises practical questions. Model files can be several gigabytes, so download them after install, on Wi-Fi, with a progress indicator and a resume option. Check free storage first. Plan updates: a new model version is a new download, so version it and allow rollback. And remember older devices. In many markets, including large parts of Pakistan and the wider region, mid-range and older phones dominate, so decide a minimum device spec and give everyone else the cloud fallback with consent.
```

Scene 11 — FAQ

```text
A question from product teams: how do we update an on-device model safely? Treat it like any app release. Version the model file, ship it to a small percentage of users first, compare accuracy and crash or battery metrics against the previous version, and keep the ability to roll back. Also keep the app working if the new model has not downloaded yet. And a second question: can small models handle Arabic or Urdu? Some do reasonably, many do not. Test your languages early, because language coverage is often the deciding factor for small models.
```

Scene 12 — Try this now

```text
Try this now. Look at your product or your daily work and list three tasks that are narrow, frequent and low-risk: things like tagging, short rewrites or routing. For each one, write the fixed set of outputs it should produce. If you can write that set in one line, the task is probably a good fit for a small model.
```

Scene 13 — Watch me do it

```text
Watch me do it. On a Mac, I install mlx lm and run a four-bit, four billion parameter model with a one-line prompt to rewrite a message politely. It answers in about a second. Now a real task: classify thirty support messages into billing, delivery, technical or other. I run the Python classifier from the lesson, which forces one label and maps anything unexpected to other. It gets twenty-six right. Three misses are messages mentioning two problems; I add a priority rule to the system prompt and it gets twenty-eight. Then I check the device side: I watch memory use in the activity monitor, around three gigabytes, and run the classifier on a loop for twenty minutes while noting battery and temperature. Everything stays reasonable. Last step, I note what would happen on an older device, and decide the cloud fallback threshold for users below our minimum spec.
```

Scene 14 — Recap

```text
Recap. Small models win on privacy, cost, offline use and latency for narrow tasks. Choose between built-in platform models and your own, and often use both. Narrow the task, add local retrieval, fine-tune when needed, and always test on real target devices. Your next step: pick one narrow task from your product, run a small model on your own device, and measure accuracy on thirty examples plus latency and memory.
```

</details>

### 13. Privacy, data residency and securing self-hosted models

Lesson `privacy-residency-and-security` · module “Edge AI, privacy and honest benchmarking” · target 8 min · 14 scenes · 1165 words · planned 8:26 (words ≈ 8:27) · status: **to produce**

| Term | Say |
|---|---|
| PDPL | P D P L |
| SDAIA | sed-AYE-ah |
| DIFC | D I F C |
| ADGM | A D G M |
| OWASP | OH-wasp |
| SHA-256 | shah two fifty-six |
| HIPAA | HIP-ah |

| # | Time | Sec | Narration | On screen | Visual |
|---|---|---|---|---|---|
| 1 | 0:00 | 33 | Here is a sentence I hear a lot: it is local, so it is private. It is a comforting sentence, and it is often wrong. In this lesson you will see why local hosting is the start of privacy, not the end, how data residency rules drive open-weight adoption in the Gulf, South Asia, the UK and beyond, and how to secure the model supply chain and the running system. This is practical guidance, not legal advice. | Privacy, residency and security<br>• Local ≠ automatically private<br>• Residency rules<br>• Supply chain and runtime security | A sturdy front door with a padlock, then the camera pans to reveal open windows labeled logs, backups and traces. |
| 2 | 0:33 | 34 | Here is an analogy. Hosting a model locally is like moving your documents from a courier service into your own building. That is a real improvement. But if your building has open windows, an unlocked filing room and a photocopier that sends copies to another country, the documents are not safe just because they are inside. Privacy is the whole building: doors, windows, filing rooms, copiers and the people with keys. The model server is just one room. | Analogy: your own building<br>• Local hosting = documents inside<br>• Open windows = logs, traces, backups<br>• Privacy = the whole building | Cutaway building illustration: a server room in the middle; windows labeled logs, traces and backups open one by one, then close as controls are applied. |
| 3 | 1:07 | 46 | Residency is one of the biggest reasons organizations choose open weights. In Europe and the UK, GDPR governs personal data and transfers. Saudi Arabia's Personal Data Protection Law, overseen by SDAIA, has rules on processing and cross-border transfers. The UAE has a federal data protection law, plus separate regimes in free zones like DIFC and ADGM, and sector rules in health and finance. In Pakistan, comprehensive data protection legislation has been in development for years, so check its status, and regulators like the State Bank set requirements for regulated firms. Hosting in-country can make a use case feasible. It does not remove your other obligations. | Residency drivers<br>• EU/UK: GDPR, UK GDPR<br>• KSA: PDPL (SDAIA)<br>• UAE: federal PDPL, DIFC, ADGM, sector rules<br>• Pakistan: legislation in progress; sector rules<br>• Other obligations remain | Stylized world map highlighting the UK, EU, Saudi Arabia, UAE and Pakistan, each with a small document icon labeled with the regime name. |
| 4 | 1:53 | 32 | So map where data actually goes. For a local assistant: the chat front end, the gateway, the inference server's logs, the embedding model and vector database, your observability traces, your backups, and even model update channels. Seven hops. For each one decide retention, encryption and who can access it. The classic mistake is sending full prompts to a tracing tool hosted in another country, which quietly undoes the whole point of hosting locally. | Seven hops<br>• Front end · gateway · inference logs<br>• Embeddings + vector DB<br>• Traces · backups · update channels<br>• Retention, encryption, access per hop | Data-flow diagram drawn step by step with seven nodes; one arrow to a foreign-flagged cloud labeled traces turns red. |
| 5 | 2:25 | 37 | Now the model supply chain. Download from the official publisher, and read the organization name carefully, because look-alike repositories exist. Prefer the safetensors format. Older pickle-based checkpoints can execute code when loaded. Do not set trust remote code to true unless you have read the code it will run. Pin files by their SHA two fifty-six hash, and verify on every deploy. And remember a model can be deliberately trained to misbehave on a trigger phrase, so choose reputable publishers and evaluate before you ship. | Supply chain<br>• Official publisher, check the org name<br>• safetensors, not pickle<br>• No untrusted remote code<br>• Pin by SHA-256<br>• Evaluate before deploy | Package moving along a conveyor through four inspection gates labeled source, format, code, hash; one look-alike package is rejected at the first gate. |
| 6 | 3:02 | 33 | Then secure the running system. The OWASP Top Ten for LLM applications is a great checklist. Prompt injection: a document or email can carry instructions, so keep tools least-privilege and confirm side effects. Sensitive information disclosure: enforce document permissions at retrieval time using your identity system, never with a line in the system prompt. Unbounded consumption: rate limits and token caps. Improper output handling: treat model output as untrusted input. And network exposure: authentication, TLS, firewalls. | Runtime security (OWASP LLM Top 10)<br>• Prompt injection → least privilege<br>• Permissions at retrieval time<br>• Rate limits, token caps<br>• Output is untrusted input<br>• Auth, TLS, firewalls | Layered shield diagram around a model icon, each layer labeled with one control, with example threat arrows bouncing off. |
| 7 | 3:35 | 39 | A worked example. A hospital group in the UAE wants clinicians to query guidelines and discharge summaries, and patient data must stay in its own infrastructure. They run vLLM and a vector database in their own data center. They turn off prompt logging in the inference server, keep redacted traces for thirty days, enforce patient-record permissions at retrieval through the existing identity system, verify model hashes in their deployment pipeline, and complete a privacy impact assessment before rollout. Clinicians see a banner reminding them to check outputs against the source. | Worked example: UAE hospital group<br>• vLLM + vector DB on-prem<br>• No prompt logging; redacted traces, 30 days<br>• Retrieval-time permissions<br>• Hash-verified models + impact assessment | Secure building diagram with internal components connected; small badges appear on each: no-log, redact, permissions, hash check, assessment. |
| 8 | 4:14 | 24 | The hands-on script downloads a model file pinned to a specific commit, computes its SHA two fifty-six hash, and refuses to deploy if it does not match your register. The Transformers snippet shows safe defaults: pinned revision, safetensors on, remote code off. Add both to your deployment pipeline and a whole class of supply-chain problems disappears. | Hands-on<br>• Pinned download + SHA-256 check<br>• use_safetensors=True<br>• trust_remote_code=False | Screen recording of a script running: download progress, hash computed, a green "Verified" line; then a second run with a tampered file showing a red refusal. |
| 9 | 4:38 | 30 | A simple example. A small dental clinic in Sharjah runs a local model to draft appointment reminder messages. They check three hops. The front end: the reception PC, with disk encryption on. The inference server: prompt logging turned off. Backups: encrypted and kept in the clinic's approved storage. Three hops, three decisions, twenty minutes. Their data-flow map is a single page, and it is enough for a practice their size. | Simple example: dental clinic<br>• Front end: encrypted PC<br>• Inference: no prompt logs<br>• Backups: encrypted, approved storage<br>• One-page map | A simple three-node data-flow diagram draws itself; each node gets a small badge (lock, no-log, encrypted backup) as it is mentioned. |
| 10 | 5:08 | 35 | Do not forget the humans. Write a short acceptable-use note for staff: what data may go into the assistant, what must not, how to report a problem, and that outputs must be checked. Train people with real examples from their own work. And agree an incident process: if the assistant exposes something it should not, who switches it off, who investigates, and who informs affected people or regulators if required. Controls in code work best alongside clear expectations for people. | The human layer<br>• Staff acceptable-use note<br>• Training with real examples<br>• Incident process: switch off, investigate, notify | Three simple cards: a one-page policy, a small workshop scene with abstract figures around a screen, and a flowchart with a red stop button leading to investigate and notify steps. |
| 11 | 5:43 | 42 | A question I hear from leadership: if we self-host, do we still need a data protection impact assessment? Often yes. Self-hosting changes where data goes, but not what you are doing with it. If the use involves sensitive data, large-scale processing or decisions about people, an assessment is usually expected, and it is also a useful design tool. A second question: is a model file itself personal data? Usually the weights are not treated as a database of personal data, but models can memorize training data, which is why training data choices and memorization tests matter. | FAQ<br>• Self-hosted → still need an impact assessment? Often yes<br>• Are weights personal data? Usually not a database, but memorization risk exists | Two Q&A cards; the first shows a clipboard labeled impact assessment next to an on-prem server, the second shows a model file with a small magnifier revealing a faint memorized text snippet. |
| 12 | 6:25 | 25 | Try this now. Take one AI tool your team already uses and ask one question: where do the prompts get logged? Check the tool's settings or documentation, and your own infrastructure. Write the answer down with a retention period. If nobody knows, you have found your first privacy task, and it is probably the most important one. | Try this now<br>• Pick one AI tool<br>• Where are prompts logged?<br>• How long are they kept?<br>• Unknown = first privacy task | Magnifying glass sweeps over a simplified settings screen and stops on a "logging" section; a sticky note appears with retention period and owner fields. |
| 13 | 6:50 | 66 | Watch me do it. I take our internal policy assistant and map its data flow on a whiteboard. Hop one, the web front end: I check browser storage and server logs, and find chat history kept for ninety days; I note it. Hop two, the proxy: access logs only, no bodies, good. Hop three, the inference server: debug logging was on; I turn it off. Hop four, the vector database: document chunks include salary bands, so I confirm permission filters are applied at retrieval and test with a non-HR account; restricted chunks never come back. Hop five, tracing: prompts were going to a hosted tool abroad; I switch to redacted traces stored in-country. Hop six, backups: encrypted, retained thirty days. Hop seven, update channels: I disable telemetry in the runner. Finally I run the hash check script on the deployed model file. It matches the register. Seven hops, two fixes, one afternoon. | Watch me do it<br>1 Map seven hops<br>2 Turn off debug prompt logs<br>3 Test retrieval permissions<br>4 Redact + localize traces<br>5 Verify model hash | Whiteboard diagram with seven nodes drawn in sequence; two nodes turn red then green after fixes; a terminal runs the hash-check script and prints Verified. |
| 14 | 7:56 | 30 | Recap. Local hosting avoids sending data to a model vendor, but logs, vectors, traces and backups still need controls. Residency rules often make self-hosting the enabling choice. Secure the supply chain with official sources, safetensors, no untrusted remote code and hash pinning. Enforce permissions at retrieval. Your next step: draw the seven-hop data-flow map for one AI system you run or plan, with retention, encryption and access for each hop. | Recap<br>• Map every hop<br>• Secure the supply chain<br>• Next: your seven-hop data-flow map | Recap card, then a blank seven-node diagram template ready to fill in. |

<details><summary>Narration per scene (paste into ElevenLabs)</summary>

Scene 1 — Privacy, residency and security

```text
Here is a sentence I hear a lot: it is local, so it is private. It is a comforting sentence, and it is often wrong. In this lesson you will see why local hosting is the start of privacy, not the end, how data residency rules drive open-weight adoption in the Gulf, South Asia, the UK and beyond, and how to secure the model supply chain and the running system. This is practical guidance, not legal advice.
```

Scene 2 — Analogy: your own building

```text
Here is an analogy. Hosting a model locally is like moving your documents from a courier service into your own building. That is a real improvement. But if your building has open windows, an unlocked filing room and a photocopier that sends copies to another country, the documents are not safe just because they are inside. Privacy is the whole building: doors, windows, filing rooms, copiers and the people with keys. The model server is just one room.
```

Scene 3 — Residency drivers

```text
Residency is one of the biggest reasons organizations choose open weights. In Europe and the UK, GDPR governs personal data and transfers. Saudi Arabia's Personal Data Protection Law, overseen by SDAIA, has rules on processing and cross-border transfers. The UAE has a federal data protection law, plus separate regimes in free zones like DIFC and ADGM, and sector rules in health and finance. In Pakistan, comprehensive data protection legislation has been in development for years, so check its status, and regulators like the State Bank set requirements for regulated firms. Hosting in-country can make a use case feasible. It does not remove your other obligations.
```

Scene 4 — Seven hops

```text
So map where data actually goes. For a local assistant: the chat front end, the gateway, the inference server's logs, the embedding model and vector database, your observability traces, your backups, and even model update channels. Seven hops. For each one decide retention, encryption and who can access it. The classic mistake is sending full prompts to a tracing tool hosted in another country, which quietly undoes the whole point of hosting locally.
```

Scene 5 — Supply chain

```text
Now the model supply chain. Download from the official publisher, and read the organization name carefully, because look-alike repositories exist. Prefer the safetensors format. Older pickle-based checkpoints can execute code when loaded. Do not set trust remote code to true unless you have read the code it will run. Pin files by their SHA two fifty-six hash, and verify on every deploy. And remember a model can be deliberately trained to misbehave on a trigger phrase, so choose reputable publishers and evaluate before you ship.
```

Scene 6 — Runtime security (OWASP LLM Top 10)

```text
Then secure the running system. The OWASP Top Ten for LLM applications is a great checklist. Prompt injection: a document or email can carry instructions, so keep tools least-privilege and confirm side effects. Sensitive information disclosure: enforce document permissions at retrieval time using your identity system, never with a line in the system prompt. Unbounded consumption: rate limits and token caps. Improper output handling: treat model output as untrusted input. And network exposure: authentication, TLS, firewalls.
```

Scene 7 — Worked example: UAE hospital group

```text
A worked example. A hospital group in the UAE wants clinicians to query guidelines and discharge summaries, and patient data must stay in its own infrastructure. They run vLLM and a vector database in their own data center. They turn off prompt logging in the inference server, keep redacted traces for thirty days, enforce patient-record permissions at retrieval through the existing identity system, verify model hashes in their deployment pipeline, and complete a privacy impact assessment before rollout. Clinicians see a banner reminding them to check outputs against the source.
```

Scene 8 — Hands-on

```text
The hands-on script downloads a model file pinned to a specific commit, computes its SHA two fifty-six hash, and refuses to deploy if it does not match your register. The Transformers snippet shows safe defaults: pinned revision, safetensors on, remote code off. Add both to your deployment pipeline and a whole class of supply-chain problems disappears.
```

Scene 9 — Simple example: dental clinic

```text
A simple example. A small dental clinic in Sharjah runs a local model to draft appointment reminder messages. They check three hops. The front end: the reception PC, with disk encryption on. The inference server: prompt logging turned off. Backups: encrypted and kept in the clinic's approved storage. Three hops, three decisions, twenty minutes. Their data-flow map is a single page, and it is enough for a practice their size.
```

Scene 10 — The human layer

```text
Do not forget the humans. Write a short acceptable-use note for staff: what data may go into the assistant, what must not, how to report a problem, and that outputs must be checked. Train people with real examples from their own work. And agree an incident process: if the assistant exposes something it should not, who switches it off, who investigates, and who informs affected people or regulators if required. Controls in code work best alongside clear expectations for people.
```

Scene 11 — FAQ

```text
A question I hear from leadership: if we self-host, do we still need a data protection impact assessment? Often yes. Self-hosting changes where data goes, but not what you are doing with it. If the use involves sensitive data, large-scale processing or decisions about people, an assessment is usually expected, and it is also a useful design tool. A second question: is a model file itself personal data? Usually the weights are not treated as a database of personal data, but models can memorize training data, which is why training data choices and memorization tests matter.
```

Scene 12 — Try this now

```text
Try this now. Take one AI tool your team already uses and ask one question: where do the prompts get logged? Check the tool's settings or documentation, and your own infrastructure. Write the answer down with a retention period. If nobody knows, you have found your first privacy task, and it is probably the most important one.
```

Scene 13 — Watch me do it

```text
Watch me do it. I take our internal policy assistant and map its data flow on a whiteboard. Hop one, the web front end: I check browser storage and server logs, and find chat history kept for ninety days; I note it. Hop two, the proxy: access logs only, no bodies, good. Hop three, the inference server: debug logging was on; I turn it off. Hop four, the vector database: document chunks include salary bands, so I confirm permission filters are applied at retrieval and test with a non-HR account; restricted chunks never come back. Hop five, tracing: prompts were going to a hosted tool abroad; I switch to redacted traces stored in-country. Hop six, backups: encrypted, retained thirty days. Hop seven, update channels: I disable telemetry in the runner. Finally I run the hash check script on the deployed model file. It matches the register. Seven hops, two fixes, one afternoon.
```

Scene 14 — Recap

```text
Recap. Local hosting avoids sending data to a model vendor, but logs, vectors, traces and backups still need controls. Residency rules often make self-hosting the enabling choice. Secure the supply chain with official sources, safetensors, no untrusted remote code and hash pinning. Enforce permissions at retrieval. Your next step: draw the seven-hop data-flow map for one AI system you run or plan, with retention, encryption and access for each hop.
```

</details>

### 14. Benchmarking responsibly: leaderboards, evals and honest comparisons

Lesson `benchmarking-responsibly` · module “Edge AI, privacy and honest benchmarking” · target 8 min · 14 scenes · 1138 words · planned 8:16 (words ≈ 8:15) · status: **to produce**

| Term | Say |
|---|---|
| EleutherAI | ee-LOO-ther A I |
| promptfoo | prompt foo |
| bootstrap | boot strap |

| # | Time | Sec | Narration | On screen | Visual |
|---|---|---|---|---|---|
| 1 | 0:00 | 32 | A new model tops the leaderboard on Monday. By Wednesday your CEO asks why you are not using it. By Friday you have switched, and by the next Monday your Urdu extraction accuracy has quietly dropped. Sound familiar? In this lesson you will learn why public benchmarks mislead, how to build an evaluation you can trust, how to use AI judges without fooling yourself, and how to be statistically honest about small differences. | Benchmarking responsibly<br>• Why leaderboards mislead<br>• Evaluations you can trust<br>• Honest statistics | Calendar animation Monday to Friday with a trophy icon, then a small accuracy graph dipping on the following Monday. |
| 2 | 0:32 | 36 | Here is an analogy. Public benchmarks are like restaurant reviews. They help you pick which places to try. But the reviewer may have visited on a good day, ordered different dishes, and cared about ambience more than you do. Before you book your wedding dinner, you go and taste the actual menu. Your own evaluation set is that tasting. And a good tasting is fair: same dishes, same night, same people tasting, and enough dishes that one lucky plate does not decide it. | Analogy: restaurant reviews<br>• Benchmarks = reviews<br>• Your eval = tasting the actual menu<br>• Fair tasting: same dishes, enough samples | Row of star-rating cards for three restaurants; then a tasting table appears with identical plates in front of each, and a scorecard fills in. |
| 3 | 1:08 | 33 | Five reasons benchmarks mislead. Contamination: test questions leak into training data. Saturation: top models cluster near the ceiling, so differences are noise. Mismatch: benchmarks rarely match your task, language or format. Configuration drift: scores depend on prompts, sampling, reasoning effort, quantization and templates, and vendor numbers often use settings you will not run. And arena-style votes measure what anonymous users like in open chat, which may reward length and style more than correctness for your job. | Why benchmarks mislead<br>• Contamination<br>• Saturation<br>• Mismatch<br>• Configuration drift<br>• Preference ≠ correctness | Five warning icons appear around a leaderboard table, each connecting to a row with a thin red line. |
| 4 | 1:41 | 36 | Instead, build your own evaluation with four ingredients. A representative set of fifty to three hundred real examples, stratified by type and difficulty, in your users' languages, with a hidden holdout you never tune on. Clear scoring: exact match or field accuracy where possible, a rubric with anchored levels for open-ended work, and pairwise comparisons when absolute scores are hard. Deployment-realistic settings: the same quantization, template, prompt and runtime you will ship. And operational metrics: latency, throughput, memory and cost per task. | Your evaluation<br>• 50–300 real, stratified examples + holdout<br>• Clear scoring and rubrics<br>• Deployment settings<br>• Latency, throughput, memory, cost | Four puzzle pieces labeled dataset, scoring, settings and ops snapping together into a square labeled "evaluation you can trust". |
| 5 | 2:17 | 35 | AI judges are useful for scale, but they have habits. Position bias: they favor the first answer. Verbosity bias: they favor longer answers. Self-preference: they favor their own family's style. So swap the order and average. Give the judge a rubric and a reference answer, and ask for a brief justification before the score. Use a judge from a different family when you can. And calibrate: have humans grade fifty items and check that the judge agrees before you trust it. | LLM-as-judge<br>• Biases: position, verbosity, self-preference<br>• Swap order, rubric + reference<br>• Different family judge<br>• Calibrate against 50 human grades | A judge icon with three small bias labels floating around it; then order-swapping arrows and a scatter plot showing judge scores vs human scores. |
| 6 | 2:52 | 33 | Now statistical honesty. With a hundred examples, eighty-two percent versus seventy-eight percent is often not a real difference. Use a paired comparison: on how many items did A beat B, and B beat A? And compute a confidence interval with a simple bootstrap, which resamples your items thousands of times to see how much the difference moves. If the interval crosses zero, you cannot tell them apart with this sample. The script is in the lesson. | Statistical honesty<br>• Paired wins and losses<br>• Bootstrap 95% interval<br>• Crosses zero → not distinguishable | Histogram of bootstrapped differences forming a bell shape, with a shaded 95% interval straddling a vertical zero line. |
| 7 | 3:25 | 36 | A worked example with illustrative numbers. A Karachi bank's internal assistant runs a mid-size open model. A newer model tops several leaderboards. The team runs its two hundred fifty item evaluation across English, Urdu and Roman Urdu, covering policy questions, summaries and extraction, at the deployment quantization. The new model is better on English summaries, indistinguishable on policy questions, worse on Roman Urdu extraction, and twenty percent slower. Decision: keep the current model for extraction and pilot the new one for summaries. | Worked example: Karachi bank (illustrative)<br>• 250 items: English, Urdu, Roman Urdu<br>• Better: English summaries<br>• Same: policy Q&A · Worse: Roman Urdu extraction<br>• Split decision by task | Grouped bar chart by task and language with confidence whiskers; one bar group highlighted green, one gray, one red. |
| 8 | 4:01 | 30 | Tools help. EleutherAI's evaluation harness runs standard benchmarks against local models. Frameworks like promptfoo and Inspect, from the UK AI Security Institute, run your own task suites with rubrics and model-graded checks. Your serving engine's benchmark command covers latency and throughput. Whatever you use, version the dataset, prompts, settings and results together, and write a one-page report: the question, setup, data, results with intervals, judge method and the decision. | Tools + the report<br>• lm-evaluation-harness · promptfoo · Inspect<br>• Engine benchmark CLI<br>• Version everything<br>• One-page evaluation report | A folder structure animation showing datasets, prompts, configs and results versioned together, then a one-page report template filling in. |
| 9 | 4:31 | 36 | A simple example of statistical honesty. You test two models on twenty questions. Model A gets seventeen right, model B gets fifteen. Is A better? Look at the pairs: on sixteen questions both got the same result, on three A won, and on one B won. Three wins to one on twenty questions is not strong evidence. Add eighty more questions, and if A keeps winning, you have a real difference. If it evens out, you have saved yourself a pointless migration. | Simple example: 17 vs 15 of 20<br>• Same result on 16 items<br>• A wins 3, B wins 1<br>• Not strong evidence yet<br>• Add 80 more items | A 20-cell grid where each cell shows A and B results; matching cells fade gray, and the four differing cells glow, three for A and one for B. |
| 10 | 5:07 | 35 | How often should you re-evaluate? Every time something changes: a new model, a new quantization, a new engine version, a new prompt, or a new chat template. And on a schedule, say monthly, even if nothing changed on your side, because your users' questions drift. Automate it. Keep the evaluation set in version control, run it in your deployment pipeline, and block a release if key metrics drop beyond an agreed tolerance. Then evaluation becomes a habit, not a project. | When to re-evaluate<br>• Any change: model, quant, engine, prompt, template<br>• Monthly for drift<br>• Automate in the pipeline<br>• Block releases on regressions | CI pipeline diagram with stages build, evaluate, deploy; the evaluate stage turns red on a metric drop and the deploy stage is greyed out. |
| 11 | 5:42 | 41 | A question from busy teams: how many evaluation examples do we really need? For a quick screen between very different models, thirty to fifty real examples can reveal large gaps. To detect smaller differences, a few points, you need more, often a few hundred, and a paired comparison. A practical rule: start with fifty, look at the confidence interval, and add examples until the interval is narrow enough for the decision you are making. And always stratify, so small but important groups, like a second language, have enough examples to be judged on their own. | FAQ: how many examples?<br>• 30–50: spot large gaps<br>• Few hundred: detect small differences<br>• Grow until the interval fits the decision<br>• Stratify key groups | A confidence interval bar narrowing as the sample-size counter grows from 50 to 300; a stratified bar shows a separate interval for a second language. |
| 12 | 6:23 | 26 | Try this now. Take any comparison you have made between two AI tools, even an informal one. Write down how many examples you used and whether both tools saw the same examples with the same settings. If the answer is fewer than thirty, or different settings, treat your conclusion as a hunch, not a result, and plan a fair re-test. | Try this now<br>• Recall a past comparison<br>• How many examples? Same settings?<br>• < 30 or unfair → hunch, not result | A small form with two fields, sample size and same settings, filling in; a label flips from "result" to "hunch" when the threshold is not met. |
| 13 | 6:49 | 65 | Watch me do it. I have fifty tasks from our work, in English and Urdu, each with a rubric. I run two local models on all fifty at deployment settings, same quantization and template. Then I score them, but blind: a script shuffles the outputs and hides which model wrote which. I use a judge model with the rubric, swapping answer order, and I also score fifteen items myself to check the judge agrees; it matches on thirteen. I save per-item scores to two files and run the paired bootstrap script. Output: model A ahead by a small margin, but the interval crosses zero. Not distinguishable. Then I split by language: in Urdu, model B is clearly better. So the decision is not A or B overall, it is B for Urdu-heavy tasks. I write the one-page report with setup, data, results with intervals, judge calibration and the decision. | Watch me do it<br>1 50 tasks, deployment settings<br>2 Blind, order-swapped judging<br>3 Calibrate on 15 items<br>4 Paired bootstrap<br>5 Split by language → decide | Screen recording: a shuffling script hides model names; a judge scoring log with swapped orders; a calibration mini-table 13/15; the bootstrap output with an interval crossing zero; a language-split bar chart; the one-page report. |
| 14 | 7:54 | 22 | Recap. Leaderboards spot candidates; your evaluation decides. Use representative data at deployment settings, keep a holdout, calibrate AI judges, and report confidence intervals, not just point scores. Your next step: score two local models on fifty of your tasks, run the paired bootstrap script, and write a one-page evaluation report. | Recap<br>• Leaderboards propose, your eval decides<br>• Intervals, not just scores<br>• Next: 50 tasks, 2 models, 1 report | Recap card with a magnifying glass over a small table and a check mark. |

<details><summary>Narration per scene (paste into ElevenLabs)</summary>

Scene 1 — Benchmarking responsibly

```text
A new model tops the leaderboard on Monday. By Wednesday your CEO asks why you are not using it. By Friday you have switched, and by the next Monday your Urdu extraction accuracy has quietly dropped. Sound familiar? In this lesson you will learn why public benchmarks mislead, how to build an evaluation you can trust, how to use AI judges without fooling yourself, and how to be statistically honest about small differences.
```

Scene 2 — Analogy: restaurant reviews

```text
Here is an analogy. Public benchmarks are like restaurant reviews. They help you pick which places to try. But the reviewer may have visited on a good day, ordered different dishes, and cared about ambience more than you do. Before you book your wedding dinner, you go and taste the actual menu. Your own evaluation set is that tasting. And a good tasting is fair: same dishes, same night, same people tasting, and enough dishes that one lucky plate does not decide it.
```

Scene 3 — Why benchmarks mislead

```text
Five reasons benchmarks mislead. Contamination: test questions leak into training data. Saturation: top models cluster near the ceiling, so differences are noise. Mismatch: benchmarks rarely match your task, language or format. Configuration drift: scores depend on prompts, sampling, reasoning effort, quantization and templates, and vendor numbers often use settings you will not run. And arena-style votes measure what anonymous users like in open chat, which may reward length and style more than correctness for your job.
```

Scene 4 — Your evaluation

```text
Instead, build your own evaluation with four ingredients. A representative set of fifty to three hundred real examples, stratified by type and difficulty, in your users' languages, with a hidden holdout you never tune on. Clear scoring: exact match or field accuracy where possible, a rubric with anchored levels for open-ended work, and pairwise comparisons when absolute scores are hard. Deployment-realistic settings: the same quantization, template, prompt and runtime you will ship. And operational metrics: latency, throughput, memory and cost per task.
```

Scene 5 — LLM-as-judge

```text
AI judges are useful for scale, but they have habits. Position bias: they favor the first answer. Verbosity bias: they favor longer answers. Self-preference: they favor their own family's style. So swap the order and average. Give the judge a rubric and a reference answer, and ask for a brief justification before the score. Use a judge from a different family when you can. And calibrate: have humans grade fifty items and check that the judge agrees before you trust it.
```

Scene 6 — Statistical honesty

```text
Now statistical honesty. With a hundred examples, eighty-two percent versus seventy-eight percent is often not a real difference. Use a paired comparison: on how many items did A beat B, and B beat A? And compute a confidence interval with a simple bootstrap, which resamples your items thousands of times to see how much the difference moves. If the interval crosses zero, you cannot tell them apart with this sample. The script is in the lesson.
```

Scene 7 — Worked example: Karachi bank (illustrative)

```text
A worked example with illustrative numbers. A Karachi bank's internal assistant runs a mid-size open model. A newer model tops several leaderboards. The team runs its two hundred fifty item evaluation across English, Urdu and Roman Urdu, covering policy questions, summaries and extraction, at the deployment quantization. The new model is better on English summaries, indistinguishable on policy questions, worse on Roman Urdu extraction, and twenty percent slower. Decision: keep the current model for extraction and pilot the new one for summaries.
```

Scene 8 — Tools + the report

```text
Tools help. EleutherAI's evaluation harness runs standard benchmarks against local models. Frameworks like promptfoo and Inspect, from the UK AI Security Institute, run your own task suites with rubrics and model-graded checks. Your serving engine's benchmark command covers latency and throughput. Whatever you use, version the dataset, prompts, settings and results together, and write a one-page report: the question, setup, data, results with intervals, judge method and the decision.
```

Scene 9 — Simple example: 17 vs 15 of 20

```text
A simple example of statistical honesty. You test two models on twenty questions. Model A gets seventeen right, model B gets fifteen. Is A better? Look at the pairs: on sixteen questions both got the same result, on three A won, and on one B won. Three wins to one on twenty questions is not strong evidence. Add eighty more questions, and if A keeps winning, you have a real difference. If it evens out, you have saved yourself a pointless migration.
```

Scene 10 — When to re-evaluate

```text
How often should you re-evaluate? Every time something changes: a new model, a new quantization, a new engine version, a new prompt, or a new chat template. And on a schedule, say monthly, even if nothing changed on your side, because your users' questions drift. Automate it. Keep the evaluation set in version control, run it in your deployment pipeline, and block a release if key metrics drop beyond an agreed tolerance. Then evaluation becomes a habit, not a project.
```

Scene 11 — FAQ: how many examples?

```text
A question from busy teams: how many evaluation examples do we really need? For a quick screen between very different models, thirty to fifty real examples can reveal large gaps. To detect smaller differences, a few points, you need more, often a few hundred, and a paired comparison. A practical rule: start with fifty, look at the confidence interval, and add examples until the interval is narrow enough for the decision you are making. And always stratify, so small but important groups, like a second language, have enough examples to be judged on their own.
```

Scene 12 — Try this now

```text
Try this now. Take any comparison you have made between two AI tools, even an informal one. Write down how many examples you used and whether both tools saw the same examples with the same settings. If the answer is fewer than thirty, or different settings, treat your conclusion as a hunch, not a result, and plan a fair re-test.
```

Scene 13 — Watch me do it

```text
Watch me do it. I have fifty tasks from our work, in English and Urdu, each with a rubric. I run two local models on all fifty at deployment settings, same quantization and template. Then I score them, but blind: a script shuffles the outputs and hides which model wrote which. I use a judge model with the rubric, swapping answer order, and I also score fifteen items myself to check the judge agrees; it matches on thirteen. I save per-item scores to two files and run the paired bootstrap script. Output: model A ahead by a small margin, but the interval crosses zero. Not distinguishable. Then I split by language: in Urdu, model B is clearly better. So the decision is not A or B overall, it is B for Urdu-heavy tasks. I write the one-page report with setup, data, results with intervals, judge calibration and the decision.
```

Scene 14 — Recap

```text
Recap. Leaderboards spot candidates; your evaluation decides. Use representative data at deployment settings, keep a holdout, calibrate AI judges, and report confidence intervals, not just point scores. Your next step: score two local models on fifty of your tasks, run the paired bootstrap script, and write a one-page evaluation report.
```

</details>

### 15. Hybrid routing between local and API models

Lesson `hybrid-routing-local-and-api` · module “Hybrid routing and capstone” · target 8 min · 14 scenes · 1152 words · planned 8:22 (words ≈ 8:21) · status: **to produce**

| Term | Say |
|---|---|
| CNIC | C N I C |
| IBAN | EYE-ban |
| LiteLLM | lite L L M |
| NDA | N D A |

| # | Time | Sec | Narration | On screen | Visual |
|---|---|---|---|---|---|
| 1 | 0:00 | 33 | Here is the question most teams eventually face: local or cloud? And the best answer is usually: both, per request. In this lesson you will build a router, a small piece of software that decides which model handles each request based on data sensitivity, task difficulty, cost, latency and availability. You will learn the four routing strategies, how to detect sensitive data locally, what counts as confidence, and how to prove where your data went. | Hybrid routing<br>• Local and cloud, per request<br>• Four strategies<br>• Sensitivity, confidence, proof | A traffic junction animation: requests arrive as small envelopes and a signal box sends them down a local lane or a cloud lane. |
| 2 | 0:33 | 33 | Here is an analogy for routing. Think of a hospital triage desk. Routine cases go to the general clinic, which is fast and inexpensive. Complex cases go to specialists. And some cases, like anything involving a child's records, follow special rules no matter how simple they look. A router is your triage desk for AI requests. The golden rule of triage applies too: the special rules are checked first, before anyone thinks about speed or cost. | Analogy: hospital triage<br>• Routine → general clinic (local)<br>• Complex → specialist (strong API)<br>• Special rules checked first (sensitivity) | Triage desk illustration: patient icons arrive; a red rule sign is checked first, then arrows direct them to a clinic door or a specialist door. |
| 3 | 1:06 | 36 | Four strategies. Sensitivity-based: anything with regulated or confidential data stays local. Task-based: simple jobs like classification and extraction go local; complex drafting and multi-step reasoning go to a stronger API model. Cascade: try the cheap local model first and escalate if validation fails or it abstains. And availability fallback: if the API is down, use the local model with a reduced-quality notice. Combine them in a fixed order. Sensitivity first, because it is a hard rule. Then task, then cascade and fallback. | Four strategies (in this order)<br>1 Sensitivity: hard rule<br>2 Task type<br>3 Cascade: cheap first, escalate<br>4 Availability fallback | Vertical stack of four filters; an envelope drops through each, and a sensitive envelope is caught at the first filter and diverted to a locked local box. |
| 4 | 1:42 | 34 | How do you detect sensitive data? In layers. Source metadata: anything from the HR system or patient portal is sensitive by definition. Patterns: regular expressions for identifiers like Pakistani CNIC numbers, Emirates ID numbers, bank account numbers, emails and phone numbers. A small local classifier for confidential topics. And a visible keep-this-private toggle for users. The golden rule: the check itself must run locally. Sending text to the cloud to ask whether it is sensitive has already exposed it. | Detecting sensitivity (locally)<br>• Source metadata<br>• ID patterns: CNIC, Emirates ID, IBAN, email<br>• Local classifier<br>• User privacy toggle | Four detector icons scanning a message; matched patterns glow and redact into gray boxes, all within a boundary labeled "inside your network". |
| 5 | 2:16 | 30 | What about confidence? Language models do not give you a trustworthy confidence number by default. Use observable signals instead. Did the output pass schema validation and business rules? Do two samples agree? Did retrieval find strong matches or nothing relevant? And did the model explicitly say it does not know, which your prompt should allow? Any of those can trigger escalation, if the data is allowed to leave. | Confidence signals<br>• Validation passed?<br>• Two samples agree?<br>• Strong retrieval matches?<br>• Explicit "I don't know" | Dashboard with four indicator lights; when one turns amber, an arrow labeled escalate appears, gated by a small "data allowed?" check. |
| 6 | 2:46 | 37 | The router in the lesson is about forty lines of Python. Two OpenAI-compatible clients, one local and one cloud, with keys and model names from environment variables. A sensitivity check with patterns and source rules. Hard tasks go to the cloud, with a local fallback if the cloud fails. Everything else goes local, escalating on explicit abstention. And every request logs its route, without the sensitive content, so you can measure and prove behavior. Gateways like LiteLLM can implement the same logic as configuration. | Hands-on: router.py<br>• Local + cloud OpenAI-compatible clients<br>• Sensitivity first<br>• Cloud for hard tasks, local fallback<br>• Log the route | Code editor scrolling through router.py with the four decision branches highlighted in turn, then a log output listing routes. |
| 7 | 3:23 | 35 | Let's talk money. Your monthly bill is the sum, across routes, of requests times average tokens times the price per token, plus your fixed local infrastructure. If most of your traffic is simple and moves to a local model you already run, and the hard minority goes to a stronger API, total cost and data exposure both fall while quality on the hard tasks improves. Use your own traffic mix and current price sheets. Vendor examples rarely match your reality. | Routing economics<br>• Σ (requests × tokens × price) per route<br>• + fixed local infrastructure<br>• Simple → local, hard → API<br>• Use your own mix and current prices | Stacked cost bars before and after routing; the after bar shows a smaller API slice and a fixed local slice, labeled illustrative. |
| 8 | 3:58 | 38 | A worked example. A Lahore agency serves UK and Gulf clients. Confidential briefs from workspaces flagged under an NDA stay local, always. Social captions go to the local model. Quarterly strategy decks, for clients who approved cloud processing, go to a stronger API model. During an API outage, requests fall back to local with a banner. After a month, in this illustrative case, about four in five requests run locally, API spend falls compared with the old all-API setup, and no confidential brief ever leaves the office network. | Worked example: Lahore agency (illustrative)<br>• NDA briefs: local only<br>• Captions: local<br>• Approved strategy decks: API<br>• Outage: local + banner<br>• ~78% local, lower API spend | Pie chart splitting into local and API slices, with a padlock icon on the NDA segment and a small outage lightning icon on the fallback path. |
| 9 | 4:36 | 35 | A simple example. A two-person accounting practice in Manchester uses a local model for anything containing client names or bank details, and a cloud model for general questions like explain this new tax rule in plain English. Their router is just one rule: if the message contains a client name from their list or anything that looks like an account number, stay local. Everything else can go to the cloud. One rule, tested with ten fake examples, and they sleep better. | Simple example: two-person practice<br>• One rule: client names or account numbers → local<br>• General questions → cloud<br>• Tested with 10 fake examples | Single decision diamond with two exits; ten test envelopes pass through, sensitive ones turning red and going local, others going to a cloud icon. |
| 10 | 5:11 | 31 | There is also a user experience side to routing. Tell people, simply, where their request was processed when it matters, for example a small label saying processed on our servers or processed by an external provider. Offer the keep-this-private toggle where users handle sensitive work. And when a fallback lowers quality, say so. Transparency builds trust, and it also supports the disclosure expectations that AI regulations and data protection rules increasingly set. | Routing UX<br>• Show where it was processed<br>• Keep-this-private toggle<br>• Say when quality is reduced<br>• Supports disclosure expectations | Chat interface mock-up with a small label under an answer reading "Processed on our servers", a privacy toggle in the header, and a subtle banner reading "Reduced quality mode". |
| 11 | 5:42 | 41 | A question that comes up in every routing project: will users notice when they are switched between models? Sometimes, in tone and style. To keep the experience consistent, share one system prompt and output format across routes, test both routes on the same evaluation set, and review a sample of each route's answers side by side. Another question: how often should routing rules change? Review them monthly with the route log in front of you. If a local model has improved, move more traffic to it. If a route's quality drops, tighten the rules. | FAQ<br>• Will users notice switching? Share prompts and formats; test both routes<br>• How often to change rules? Monthly, with the route log | Two answer cards from different routes shown side by side with matching formatting; a monthly calendar icon next to a route distribution pie chart. |
| 12 | 6:23 | 21 | Try this now. Write your first three routing rules in plain English, in priority order. Rule one must be about sensitive data. Then write five fake requests that should trigger rule one, using made-up identifiers, and five that should not. Those ten requests are your first canary test set. | Try this now<br>• 3 routing rules, sensitivity first<br>• 5 fake sensitive requests<br>• 5 non-sensitive requests<br>• = your canary set | Numbered rule list types itself, followed by ten envelope icons labeled with fake IDs sorting into two columns. |
| 13 | 6:44 | 65 | Watch me do it. I open the router script and set the local base address, the cloud gateway address and the model names from environment variables. I add our organization's rule first: anything from the HR source stays local. Then I write twenty canary requests with fake identifiers: fake CNIC numbers, fake Emirates ID numbers, fake emails, mixed into ordinary questions. I run them all through the router and print only the route for each. Nineteen go local correctly. One slips to the cloud: an ID number written with spaces instead of dashes. I update the pattern to allow both, rerun, and all twenty stay local. Then I simulate a cloud outage by pointing the cloud address at nothing; hard tasks fall back to local with the right route label, and no sensitive canary leaves. Finally I check the route log shows counts per route without any request content. | Watch me do it<br>1 Configure routes from env vars<br>2 20 canary requests<br>3 Catch a spaced ID → fix pattern<br>4 Simulate outage<br>5 Check the route log | Code editor with router settings; a terminal prints 20 route lines, one highlighted red; the regex is edited; rerun shows all green; an outage toggle flips and fallback labels appear; a route-count table. |
| 14 | 7:49 | 33 | Three pitfalls, then recap. Never run the sensitivity check in the cloud. Make sure sensitivity rules beat fallback rules, or an outage will quietly send private data out. And share prompts across routes so tone stays consistent. To recap: route per request, sensitivity first, observable confidence signals, and log every route. Your next step: adapt the router to three request types from your work, seed twenty fake-ID canary requests, and prove none reach the cloud. | Pitfalls + recap<br>• Local sensitivity check<br>• Sensitivity beats fallback<br>• Shared prompts across routes<br>• Next: 20 canary requests, zero on cloud | Recap card; then a small test report showing 20 canary requests, all routed local, with a green zero next to "cloud". |

<details><summary>Narration per scene (paste into ElevenLabs)</summary>

Scene 1 — Hybrid routing

```text
Here is the question most teams eventually face: local or cloud? And the best answer is usually: both, per request. In this lesson you will build a router, a small piece of software that decides which model handles each request based on data sensitivity, task difficulty, cost, latency and availability. You will learn the four routing strategies, how to detect sensitive data locally, what counts as confidence, and how to prove where your data went.
```

Scene 2 — Analogy: hospital triage

```text
Here is an analogy for routing. Think of a hospital triage desk. Routine cases go to the general clinic, which is fast and inexpensive. Complex cases go to specialists. And some cases, like anything involving a child's records, follow special rules no matter how simple they look. A router is your triage desk for AI requests. The golden rule of triage applies too: the special rules are checked first, before anyone thinks about speed or cost.
```

Scene 3 — Four strategies (in this order)

```text
Four strategies. Sensitivity-based: anything with regulated or confidential data stays local. Task-based: simple jobs like classification and extraction go local; complex drafting and multi-step reasoning go to a stronger API model. Cascade: try the cheap local model first and escalate if validation fails or it abstains. And availability fallback: if the API is down, use the local model with a reduced-quality notice. Combine them in a fixed order. Sensitivity first, because it is a hard rule. Then task, then cascade and fallback.
```

Scene 4 — Detecting sensitivity (locally)

```text
How do you detect sensitive data? In layers. Source metadata: anything from the HR system or patient portal is sensitive by definition. Patterns: regular expressions for identifiers like Pakistani CNIC numbers, Emirates ID numbers, bank account numbers, emails and phone numbers. A small local classifier for confidential topics. And a visible keep-this-private toggle for users. The golden rule: the check itself must run locally. Sending text to the cloud to ask whether it is sensitive has already exposed it.
```

Scene 5 — Confidence signals

```text
What about confidence? Language models do not give you a trustworthy confidence number by default. Use observable signals instead. Did the output pass schema validation and business rules? Do two samples agree? Did retrieval find strong matches or nothing relevant? And did the model explicitly say it does not know, which your prompt should allow? Any of those can trigger escalation, if the data is allowed to leave.
```

Scene 6 — Hands-on: router.py

```text
The router in the lesson is about forty lines of Python. Two OpenAI-compatible clients, one local and one cloud, with keys and model names from environment variables. A sensitivity check with patterns and source rules. Hard tasks go to the cloud, with a local fallback if the cloud fails. Everything else goes local, escalating on explicit abstention. And every request logs its route, without the sensitive content, so you can measure and prove behavior. Gateways like LiteLLM can implement the same logic as configuration.
```

Scene 7 — Routing economics

```text
Let's talk money. Your monthly bill is the sum, across routes, of requests times average tokens times the price per token, plus your fixed local infrastructure. If most of your traffic is simple and moves to a local model you already run, and the hard minority goes to a stronger API, total cost and data exposure both fall while quality on the hard tasks improves. Use your own traffic mix and current price sheets. Vendor examples rarely match your reality.
```

Scene 8 — Worked example: Lahore agency (illustrative)

```text
A worked example. A Lahore agency serves UK and Gulf clients. Confidential briefs from workspaces flagged under an NDA stay local, always. Social captions go to the local model. Quarterly strategy decks, for clients who approved cloud processing, go to a stronger API model. During an API outage, requests fall back to local with a banner. After a month, in this illustrative case, about four in five requests run locally, API spend falls compared with the old all-API setup, and no confidential brief ever leaves the office network.
```

Scene 9 — Simple example: two-person practice

```text
A simple example. A two-person accounting practice in Manchester uses a local model for anything containing client names or bank details, and a cloud model for general questions like explain this new tax rule in plain English. Their router is just one rule: if the message contains a client name from their list or anything that looks like an account number, stay local. Everything else can go to the cloud. One rule, tested with ten fake examples, and they sleep better.
```

Scene 10 — Routing UX

```text
There is also a user experience side to routing. Tell people, simply, where their request was processed when it matters, for example a small label saying processed on our servers or processed by an external provider. Offer the keep-this-private toggle where users handle sensitive work. And when a fallback lowers quality, say so. Transparency builds trust, and it also supports the disclosure expectations that AI regulations and data protection rules increasingly set.
```

Scene 11 — FAQ

```text
A question that comes up in every routing project: will users notice when they are switched between models? Sometimes, in tone and style. To keep the experience consistent, share one system prompt and output format across routes, test both routes on the same evaluation set, and review a sample of each route's answers side by side. Another question: how often should routing rules change? Review them monthly with the route log in front of you. If a local model has improved, move more traffic to it. If a route's quality drops, tighten the rules.
```

Scene 12 — Try this now

```text
Try this now. Write your first three routing rules in plain English, in priority order. Rule one must be about sensitive data. Then write five fake requests that should trigger rule one, using made-up identifiers, and five that should not. Those ten requests are your first canary test set.
```

Scene 13 — Watch me do it

```text
Watch me do it. I open the router script and set the local base address, the cloud gateway address and the model names from environment variables. I add our organization's rule first: anything from the HR source stays local. Then I write twenty canary requests with fake identifiers: fake CNIC numbers, fake Emirates ID numbers, fake emails, mixed into ordinary questions. I run them all through the router and print only the route for each. Nineteen go local correctly. One slips to the cloud: an ID number written with spaces instead of dashes. I update the pattern to allow both, rerun, and all twenty stay local. Then I simulate a cloud outage by pointing the cloud address at nothing; hard tasks fall back to local with the right route label, and no sensitive canary leaves. Finally I check the route log shows counts per route without any request content.
```

Scene 14 — Pitfalls + recap

```text
Three pitfalls, then recap. Never run the sensitivity check in the cloud. Make sure sensitivity rules beat fallback rules, or an outage will quietly send private data out. And share prompts across routes so tone stays consistent. To recap: route per request, sensitivity first, observable confidence signals, and log every route. Your next step: adapt the router to three request types from your work, seed twenty fake-ID canary requests, and prove none reach the cloud.
```

</details>

### 16. Capstone: a private RAG assistant on a laptop or server

Lesson `capstone-private-rag-assistant` · module “Hybrid routing and capstone” · target 8 min · 14 scenes · 1158 words · planned 8:22 (words ≈ 8:23) · status: **to produce**

| Term | Say |
|---|---|
| RAG | rag |
| NumPy | NUM-pie |
| pgvector | P G vector |
| Qdrant | Q drant |
| Chroma | KROH-mah |

| # | Time | Sec | Narration | On screen | Visual |
|---|---|---|---|---|---|
| 1 | 0:00 | 30 | This is where everything comes together. You are going to build a private assistant that answers questions from your organization's documents, cites its sources, admits when it does not know, respects who is allowed to see what, and runs entirely on a laptop or internal server. Then you will prove it works with an evaluation report. Licenses, sizing, local runtimes, structured output, security, benchmarking: all of it shows up here. | Capstone: private RAG assistant<br>• Cites sources, abstains, respects permissions<br>• Fully local<br>• Proven with an evaluation report | Montage of earlier course visuals (license card, memory bar, terminal, shield, chart) converging into one assistant window. |
| 2 | 0:30 | 33 | Here is how to picture a RAG assistant. It is an open-book exam. The model is a bright student who does not memorize your company's documents. Retrieval hands the student the right pages from the library, only from shelves that student is allowed to read. Citations are the student writing page numbers next to every claim. And abstention is the student saying, honestly, this is not in the book. Your evaluation is the examiner checking all four. | Analogy: an open-book exam<br>• Retrieval = right pages, allowed shelves only<br>• Citations = page numbers per claim<br>• Abstention = "not in the book" | Exam desk illustration: a student icon receives three pages from a library cart with a locked shelf in the background; each answer line gets a small page-number tag. |
| 3 | 1:03 | 34 | Here is the architecture. Documents are loaded, split into chunks, and turned into vectors by a local embedding model. Each chunk keeps metadata, including which groups may see it. When a user asks a question, you first filter chunks to what that user may access, then embed the question and find the closest chunks. Those chunks go into a prompt as numbered sources, and a local chat model answers with citations, or says the answer is not there. | Architecture<br>• Load → chunk → embed → index with access groups<br>• Permission filter → search top-k<br>• Numbered sources → local model → cited answer or "not found" | Pipeline diagram drawn left to right: documents, chunks, vectors, index with small lock tags, then a question flowing through a filter, search, prompt and answer with citation markers. |
| 4 | 1:37 | 34 | Before coding, write a short design doc. Which chat model, at what quantization, and its license and hash. Which embedding model, tested on your languages. How you chunk: start around five to eight hundred words with overlap, splitting on headings. Where vectors live: for a few thousand chunks, a NumPy matrix saved to disk is plenty; move to a vector database like pgvector, Qdrant or Chroma when you need scale and filtering. And the hardware, sized with your calculator. | Design doc<br>• Chat model + quant + license + hash<br>• Embedding model tested on your languages<br>• Chunking strategy<br>• Index choice<br>• Hardware sizing | A one-page document template with five headings filling in with short answers. |
| 5 | 2:11 | 40 | The reference code is about eighty lines with Ollama and NumPy. Build index reads your markdown files, chunks them, embeds them and saves vectors plus metadata. Retrieve filters by the user's groups first, then ranks by cosine similarity and drops weak matches below a threshold. Answer builds a prompt with numbered sources, tells the model to answer only from them, cite like bracket one, treat source text as data, and reply not found otherwise. Any answer without a citation is rejected. Check your embedding model's card for any required query prefixes. | Reference implementation<br>• build_index: chunk, embed, save<br>• retrieve: permission filter → similarity → threshold<br>• answer: numbered sources, cite, NOT_FOUND<br>• Reject uncited answers | Code editor walkthrough of private_rag.py with three function names highlighted in sequence, then terminal output showing a cited answer and a sources list. |
| 6 | 2:51 | 31 | Now the part most people skip: evaluation. Write at least forty questions. Twenty-five answerable, each with the expected source and key facts. Ten that the documents cannot answer, where the assistant must decline. And five permission tests, where a user without access must not see restricted content. Measure retrieval hit rate, answer correctness, citation faithfulness, abstention accuracy, permission leaks, which must be zero, and ninety-fifth percentile latency on your target hardware. | Evaluation set (≥ 40)<br>• 25 answerable · 10 unanswerable · 5 permission<br>• Hit rate · correctness · citation faithfulness<br>• Abstention · leaks = 0 · p95 latency | Three coloured stacks of question cards labeled answerable, unanswerable and permission, feeding into a metrics table that fills in. |
| 7 | 3:22 | 36 | Here is what good looks like, with illustrative numbers. A Dubai logistics firm indexes a hundred and forty procedures in English and Arabic. First run: Arabic retrieval hit rate is only seventy-eight percent. Diagnosis: the embedding model is weak in Arabic. They swap to a multilingual embedding model and the hit rate rises to ninety-three. Abstention improves after they retrieve fewer chunks and raise the similarity threshold. Twelve permission tests, zero leaks. Then they move to vLLM and Qdrant for a sixty-person rollout. | What good looks like (illustrative)<br>• Arabic hit rate 78% → 93% after embedding swap<br>• Better abstention: lower k, higher threshold<br>• 0 leaks in 12 tests<br>• Scale-up: vLLM + Qdrant | Before-and-after bar chart for Arabic hit rate, then a small architecture upgrade diagram swapping a laptop icon for a server and database icon. |
| 8 | 3:58 | 30 | Your deliverables: the design doc, working code with a readme, model register entries for both models, the seven-hop data-flow map, a one-page evaluation report with the five worst failures analyzed, and a short note on what you would change for fifty users. Avoid the classic traps: only testing answerable questions, permissions in the prompt, forgetting to re-index when documents change, and a context window too small for your sources. | Deliverables<br>• Design doc · code + README<br>• Register entries · data-flow map<br>• Evaluation report + 5 worst failures<br>• Scale-up note | Checklist of six deliverables ticking off, with four small warning icons for the traps along the side. |
| 9 | 4:28 | 41 | A simple example to test your build. Put three documents in the folder: a leave policy, a returns policy and a travel expense policy. Mark the travel policy as finance only. Ask as a support user: how many days do customers have to return an item? You should get an answer citing the returns policy. Ask: what is the daily meal allowance on business trips? As a support user you should get I could not find this, because the travel policy is restricted. Ask as a finance user, and it should answer with a citation. | Simple test: 3 documents<br>• Returns question → cited answer<br>• Travel allowance as support → "not found"<br>• Same question as finance → cited answer | Three document icons, one with a finance-only lock. Two user avatars ask the same question; one receives a "not found" card, the other a cited answer card. |
| 10 | 5:09 | 31 | A few tips to get strong results quickly. Clean your documents first: remove duplicate versions and outdated policies, because retrieval cannot tell which one is current. Add titles and dates to chunk metadata and show them in citations, so users can judge freshness. Start with four retrieved chunks and a moderate similarity threshold, then tune both using your evaluation set, not intuition. And read twenty real answers yourself before trusting any metric. | Tips for strong results<br>• Remove duplicates and outdated versions<br>• Titles and dates in citations<br>• Tune k and threshold on the eval set<br>• Read 20 answers yourself | Document stack being deduplicated with older versions fading out; then an answer card showing citations with titles and dates, and two tuning sliders labeled k and threshold. |
| 11 | 5:40 | 40 | A capstone question I get often: how do we keep the index fresh when documents change every week? Build re-indexing into your document workflow. When a policy is updated, the new version replaces the old one in the index, and the old version is removed, not just added alongside. Store the document's date and version in chunk metadata, show it in citations, and run a small nightly job that compares the document folder with the index. Stale or duplicate versions are one of the most common causes of wrong answers in real deployments. | FAQ: keeping the index fresh<br>• Replace, don't just add<br>• Date + version in metadata and citations<br>• Nightly folder-vs-index check | Document folder and index side by side; an updated document replaces its older entry in the index, and a nightly clock icon triggers a comparison scan. |
| 12 | 6:20 | 25 | Try this now. Before writing any code, write your first ten evaluation questions: six answerable, two unanswerable, two permission tests. Put the expected source file next to each answerable one. Building the test before the system keeps you honest, and you will reuse these ten questions every time you change the model, the chunking or the embedding. | Try this now<br>• 10 questions before any code<br>• 6 answerable (with source) · 2 unanswerable · 2 permission<br>• Reuse on every change | A spreadsheet-style table fills with ten rows color-coded into three groups, with a source column populated for the first six. |
| 13 | 6:45 | 67 | Watch me do it. I put twenty-two markdown policy files in the documents folder and write the access file: most documents available to all, two restricted to HR. I run build index; it embeds about three hundred chunks in under a minute. Now my evaluation file: twenty-five answerable questions, ten unanswerable, five permission tests. I run them all. Retrieval finds the right source for twenty-two of twenty-five. Nine of ten unanswerable questions correctly return not found. All five permission tests pass. I look at the three retrieval misses: two are about a policy that has an older duplicate in the folder. I delete the old version, rebuild, and the hit rate rises to twenty-four. The one unanswerable failure was a confident answer from a loosely related chunk, so I raise the similarity threshold slightly and rerun: ten of ten. I paste the metrics into the report template with the five worst failures explained. | Watch me do it<br>1 22 docs + access file<br>2 Build index<br>3 Run 40 eval questions<br>4 Remove duplicate → rebuild<br>5 Tune threshold → report | Folder view of 22 files and an acl.json; terminal shows build_index progress; an evaluation table fills with hit, abstention and permission columns; a duplicate file is deleted; metrics update; the report template fills in. |
| 14 | 7:52 | 30 | Congratulations. You can now choose open-weight models with licenses in mind, size hardware with a formula, run models locally and at scale, secure the stack, benchmark honestly, route between local and cloud, and ship a private assistant with evidence that it works. Your next step is the capstone itself. Pick a folder of at least twenty approved documents, write your forty questions, and build it. Then take the final exam. | Course recap<br>• Choose, size, run, serve, secure, evaluate, route<br>• Next: build the capstone, then the final exam | Journey map animation retracing the six modules as milestones, ending at a flag labeled Capstone. |

<details><summary>Narration per scene (paste into ElevenLabs)</summary>

Scene 1 — Capstone: private RAG assistant

```text
This is where everything comes together. You are going to build a private assistant that answers questions from your organization's documents, cites its sources, admits when it does not know, respects who is allowed to see what, and runs entirely on a laptop or internal server. Then you will prove it works with an evaluation report. Licenses, sizing, local runtimes, structured output, security, benchmarking: all of it shows up here.
```

Scene 2 — Analogy: an open-book exam

```text
Here is how to picture a RAG assistant. It is an open-book exam. The model is a bright student who does not memorize your company's documents. Retrieval hands the student the right pages from the library, only from shelves that student is allowed to read. Citations are the student writing page numbers next to every claim. And abstention is the student saying, honestly, this is not in the book. Your evaluation is the examiner checking all four.
```

Scene 3 — Architecture

```text
Here is the architecture. Documents are loaded, split into chunks, and turned into vectors by a local embedding model. Each chunk keeps metadata, including which groups may see it. When a user asks a question, you first filter chunks to what that user may access, then embed the question and find the closest chunks. Those chunks go into a prompt as numbered sources, and a local chat model answers with citations, or says the answer is not there.
```

Scene 4 — Design doc

```text
Before coding, write a short design doc. Which chat model, at what quantization, and its license and hash. Which embedding model, tested on your languages. How you chunk: start around five to eight hundred words with overlap, splitting on headings. Where vectors live: for a few thousand chunks, a NumPy matrix saved to disk is plenty; move to a vector database like pgvector, Qdrant or Chroma when you need scale and filtering. And the hardware, sized with your calculator.
```

Scene 5 — Reference implementation

```text
The reference code is about eighty lines with Ollama and NumPy. Build index reads your markdown files, chunks them, embeds them and saves vectors plus metadata. Retrieve filters by the user's groups first, then ranks by cosine similarity and drops weak matches below a threshold. Answer builds a prompt with numbered sources, tells the model to answer only from them, cite like bracket one, treat source text as data, and reply not found otherwise. Any answer without a citation is rejected. Check your embedding model's card for any required query prefixes.
```

Scene 6 — Evaluation set (≥ 40)

```text
Now the part most people skip: evaluation. Write at least forty questions. Twenty-five answerable, each with the expected source and key facts. Ten that the documents cannot answer, where the assistant must decline. And five permission tests, where a user without access must not see restricted content. Measure retrieval hit rate, answer correctness, citation faithfulness, abstention accuracy, permission leaks, which must be zero, and ninety-fifth percentile latency on your target hardware.
```

Scene 7 — What good looks like (illustrative)

```text
Here is what good looks like, with illustrative numbers. A Dubai logistics firm indexes a hundred and forty procedures in English and Arabic. First run: Arabic retrieval hit rate is only seventy-eight percent. Diagnosis: the embedding model is weak in Arabic. They swap to a multilingual embedding model and the hit rate rises to ninety-three. Abstention improves after they retrieve fewer chunks and raise the similarity threshold. Twelve permission tests, zero leaks. Then they move to vLLM and Qdrant for a sixty-person rollout.
```

Scene 8 — Deliverables

```text
Your deliverables: the design doc, working code with a readme, model register entries for both models, the seven-hop data-flow map, a one-page evaluation report with the five worst failures analyzed, and a short note on what you would change for fifty users. Avoid the classic traps: only testing answerable questions, permissions in the prompt, forgetting to re-index when documents change, and a context window too small for your sources.
```

Scene 9 — Simple test: 3 documents

```text
A simple example to test your build. Put three documents in the folder: a leave policy, a returns policy and a travel expense policy. Mark the travel policy as finance only. Ask as a support user: how many days do customers have to return an item? You should get an answer citing the returns policy. Ask: what is the daily meal allowance on business trips? As a support user you should get I could not find this, because the travel policy is restricted. Ask as a finance user, and it should answer with a citation.
```

Scene 10 — Tips for strong results

```text
A few tips to get strong results quickly. Clean your documents first: remove duplicate versions and outdated policies, because retrieval cannot tell which one is current. Add titles and dates to chunk metadata and show them in citations, so users can judge freshness. Start with four retrieved chunks and a moderate similarity threshold, then tune both using your evaluation set, not intuition. And read twenty real answers yourself before trusting any metric.
```

Scene 11 — FAQ: keeping the index fresh

```text
A capstone question I get often: how do we keep the index fresh when documents change every week? Build re-indexing into your document workflow. When a policy is updated, the new version replaces the old one in the index, and the old version is removed, not just added alongside. Store the document's date and version in chunk metadata, show it in citations, and run a small nightly job that compares the document folder with the index. Stale or duplicate versions are one of the most common causes of wrong answers in real deployments.
```

Scene 12 — Try this now

```text
Try this now. Before writing any code, write your first ten evaluation questions: six answerable, two unanswerable, two permission tests. Put the expected source file next to each answerable one. Building the test before the system keeps you honest, and you will reuse these ten questions every time you change the model, the chunking or the embedding.
```

Scene 13 — Watch me do it

```text
Watch me do it. I put twenty-two markdown policy files in the documents folder and write the access file: most documents available to all, two restricted to HR. I run build index; it embeds about three hundred chunks in under a minute. Now my evaluation file: twenty-five answerable questions, ten unanswerable, five permission tests. I run them all. Retrieval finds the right source for twenty-two of twenty-five. Nine of ten unanswerable questions correctly return not found. All five permission tests pass. I look at the three retrieval misses: two are about a policy that has an older duplicate in the folder. I delete the old version, rebuild, and the hit rate rises to twenty-four. The one unanswerable failure was a confident answer from a loosely related chunk, so I raise the similarity threshold slightly and rerun: ten of ten. I paste the metrics into the report template with the five worst failures explained.
```

Scene 14 — Course recap

```text
Congratulations. You can now choose open-weight models with licenses in mind, size hardware with a formula, run models locally and at scale, secure the stack, benchmark honestly, route between local and cloud, and ship a private assistant with evidence that it works. Your next step is the capstone itself. Pick a folder of at least twenty approved documents, write your forty questions, and build it. Then take the final exam.
```

</details>
