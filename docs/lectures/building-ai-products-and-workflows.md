# Building AI Products & Workflows — lecture production script

Course `building-ai-products-and-workflows` · pack v2 · reviewed 2026-09 · level advanced · category ai

18 lectures · 21,805 narration words · planned runtime 2:38:05 (≈ 156 min at 140 wpm)

> Generated by `scripts/export-lecture-scripts.py` from the course pack — edit the pack, not this file. Production workflow: [README](README.md).

## Voice direction

Conversational expert, second person, ~140 wpm; precise and technical, calm confidence; spell out code identifiers when read aloud; audience level: advanced. Pause ~0.5 s between scenes; no music under narration (added in edit).

## Lectures

1. [Finding high-value AI use cases](#1-finding-high-value-ai-use-cases)
2. [Scoping and defining success](#2-scoping-and-defining-success)
3. [Rapid prototyping of AI features](#3-rapid-prototyping-of-ai-features)
4. [Prototyping with AI coding tools and app builders](#4-prototyping-with-ai-coding-tools-and-app-builders)
5. [Build vs buy (and blend)](#5-build-vs-buy-and-blend)
6. [Choosing models and an architecture that can change](#6-choosing-models-and-an-architecture-that-can-change)
7. [Workflow orchestration patterns for AI features](#7-workflow-orchestration-patterns-for-ai-features)
8. [Integrating AI into existing systems](#8-integrating-ai-into-existing-systems)
9. [Data and privacy architecture for AI features](#9-data-and-privacy-architecture-for-ai-features)
10. [Security threats and vendor risk for AI features](#10-security-threats-and-vendor-risk-for-ai-features)
11. [Evaluation-driven development](#11-evaluation-driven-development)
12. [Monitoring AI features in production](#12-monitoring-ai-features-in-production)
13. [Unit economics of AI features](#13-unit-economics-of-ai-features)
14. [UX patterns for AI features](#14-ux-patterns-for-ai-features)
15. [Designing agentic product features users can trust](#15-designing-agentic-product-features-users-can-trust)
16. [Operational AI governance for product teams](#16-operational-ai-governance-for-product-teams)
17. [AI regulation for product teams (2026 update)](#17-ai-regulation-for-product-teams-2026-update)
18. [Driving adoption and scaling from pilot to portfolio](#18-driving-adoption-and-scaling-from-pilot-to-portfolio)

### 1. Finding high-value AI use cases

Lesson `finding-high-value-use-cases` · module “Finding and scoping high-value use cases” · target 9 min · 15 scenes · 1279 words · planned 9:16 (words ≈ 9:16) · status: **to produce**

| # | Time | Sec | Narration | On screen | Visual |
|---|---|---|---|---|---|
| 1 | 0:00 | 33 | Most AI projects don't fail because the model is weak. They fail because someone picked the wrong problem. A chatbot for a question nobody asks. An impressive demo for a task that happens twice a year. In this lesson you'll learn a repeatable way to find the work where AI creates real value, score it honestly, and choose your first projects with confidence, whether you run a five-person agency or a department in a large company. | Finding high-value AI use cases<br>• Problems first, models second<br>• A repeatable discovery method<br>• Scoring and choosing | Layout: A dartboard with darts scattered on the wall around it, labelled 'AI projects chosen by hype'. Then a single dart lands in the centre labelled 'valuable, frequent, painful work'. Timing (33s): 0-3s 'Finding high-value AI use cases' heading fades in; 3-27s elements build in narration order; 27-33s hold final frame. |
| 2 | 0:33 | 29 | Here's an analogy. Choosing AI use cases is like choosing where to open a new shop. You don't pick the street with the prettiest buildings. You count footfall, look at what people already buy, and check the rent. Frequency is footfall. Time spent is what people already pay in effort. Feasibility is the rent. The prettiest idea, a flashy chatbot, often sits on an empty street. | Analogy: choosing a shop location<br>• Frequency = footfall<br>• Time spent = existing demand<br>• Feasibility = the rent | Layout: A street map with two shop sites: one on a pretty but empty street (few walking dots), one on a busy street (many walking dots). Labels 'frequency', 'demand', 'feasibility' attach to the busy site. Timing (29s): 0-3s 'Analogy: choosing a shop location' heading fades in; 3-23s elements build in narration order; 23-29s hold final frame. |
| 3 | 1:02 | 44 | Start with problems, not models. The best candidates are work that's valuable, frequent and painful. Then ask whether AI is the right tool. Language and multimodal models are strongest with unstructured information like emails, documents, tickets and calls; with transforming language, such as drafting, summarising, translating and classifying; with judgement at scale where a human can review; with search and synthesis across sources; and with repetitive steps that have too much variation for old rules-based automation. They're weaker when you need guaranteed exactness without checking, when there's little data or context, or when stakes are high and there's no room for review. | Where AI creates value<br>• Unstructured information<br>• Language transformation<br>• Reviewable judgement at scale<br>• Search, synthesis, varied repetition | Layout: five icons animate onto a grid: an envelope pile, a pen rewriting text, a checklist with a reviewer, a magnifying glass over documents, a repeating arrow with small variations. Timing (44s): 0-3s 'Where AI creates value' heading fades in; 3-38s elements build in narration order; 38-44s hold final frame. |
| 4 | 1:46 | 32 | Here's the method. Map workflows, not departments. Walk through a real process step by step, like handling a supplier invoice or answering a sales enquiry, and note volume, time, errors and frustrations. For each step, ask whether it's language-heavy, repetitive, rules-with-exceptions or search-heavy. Estimate value: time saved times volume, faster response, better quality, revenue or reduced risk. Estimate feasibility: data availability, integration difficulty, the accuracy you need and the risk if it's wrong. | Discovery method<br>1 Map a workflow step by step<br>2 Flag AI-suited steps<br>3 Estimate value<br>4 Estimate feasibility | Layout: A horizontal swimlane diagram of an 'enquiry to proposal' workflow. Steps highlight one by one with small tags: 'language-heavy', 'repetitive', 'search-heavy'. Timing (32s): 0-3s 'Discovery method' heading fades in; 3-26s elements build in narration order; 26-32s hold final frame. |
| 5 | 2:18 | 44 | Now plot candidates on a value and feasibility grid. High value and high feasibility are quick wins: start there, because they build credibility and teach your team. High value but low feasibility are strategic bets: plan carefully and de-risk with pilots. Low value and high feasibility are nice-to-haves, only worth it if nearly free. And low value, low feasibility, you avoid. To find the numbers, ask simple questions. How often does this happen? How long does it take, and who does it? What happens when it's done badly? Would a good draft that a human edits save most of the time? | Value–feasibility grid<br>• Quick wins: start here<br>• Strategic bets: stage and pilot<br>• Nice-to-have • Avoid | Layout: A 2x2 grid with quadrant labels. Sticky notes representing use cases slide into quadrants; the top-right 'Quick wins' quadrant glows. Timing (44s): 0-3s 'Value–feasibility grid' heading fades in; 3-38s elements build in narration order; 38-44s hold final frame. |
| 6 | 3:02 | 33 | A simple example first. A three-person accounting firm notices every client email asking for a copy of last year's return takes about ten minutes: find the file, check identity, attach, reply. It happens around twenty times a week. That's over three hours weekly on a frequent, clear, low-judgement task, where a drafted reply with the right file attached, checked by a person, saves most of the time. Not glamorous, but an ideal first use case. | Simple example: 'send me last year's return'<br>• ~20 times a week × 10 minutes<br>• Clear, frequent, low judgement<br>• Draft + attach, human checks | Layout: A tally counter clicks up to 20 on a weekly calendar; a stopwatch shows 10 minutes per request; the total '3+ hours/week' appears in a highlighted box. Timing (33s): 0-3s 'Simple example: 'send me last year's return'' heading fades in; 3-27s elements build in narration order; 27-33s hold final frame. |
| 7 | 3:35 | 44 | Let's see it work. A digital agency serving e-commerce clients in the UK, UAE and Pakistan maps three workflows. Monthly client reports eat hours of analyst time, and AI can draft commentary from exported data for analysts to review: a quick win. New business proposals are bespoke, but AI can draft sections from past proposals: medium value, medium feasibility. Automated media buying could be valuable, but it's high risk and dependent on platform data: a strategic bet for later, with approval gates. They start with reports, measure hours saved and client feedback, and use the result to fund the next project. | Worked example: a three-country agency<br>• Reports → quick win<br>• Proposals → medium<br>• Media buying → strategic bet, later | Layout: three workflow cards (Reports, Proposals, Media buying) placed on the grid; a coin icon flows from the Reports card to fund the Proposals card. Timing (44s): 0-3s 'Worked example: a three-country agency' heading fades in; 3-38s elements build in narration order; 38-44s hold final frame. |
| 8 | 4:19 | 34 | Let's add illustrative numbers to the agency. Monthly reports take analysts about six hours per client across twenty clients, around a hundred and twenty hours a month. If AI drafts the commentary and analysts review for ninety minutes each, that's about thirty hours, saving roughly ninety analyst hours a month. The proposal assistant might save a third of that. Media buying could be worth more, but only after months of data work. The grid made the order obvious. | Business example (illustrative)<br>• Reports: ~120 h/month → ~30 h<br>• ~90 analyst hours saved<br>• Proposals: smaller win; media buying: later | Layout: three horizontal bars for the three workflows showing hours saved; reports bar longest; a 'later' tag on media buying; the value–feasibility grid shrinks into the corner. Timing (34s): 0-3s 'Business example (illustrative)' heading fades in; 3-28s elements build in narration order; 28-34s hold final frame. |
| 9 | 4:53 | 39 | Watch for red flags. AI-for-everything initiatives with no named process owner. Use cases chosen because they're impressive rather than frequent. No way to measure success. Tasks where errors are costly and outputs can't be checked. And automating a broken process instead of fixing it first. The best ideas usually come from the people who do the work, so run short sessions where they show you their most repetitive, frustrating tasks with real examples. You'll find better candidates, spot edge cases early, and build the goodwill that adoption depends on. | Red flags<br>• No owner • Impressive but rare<br>• Unmeasurable • Uncheckable, costly errors<br>• Automating a broken process | Layout: five red flag icons pop up over a mock project board; then a small workshop scene with abstract figures around a table showing sticky notes of real tasks. Timing (39s): 0-3s 'Red flags' heading fades in; 3-33s elements build in narration order; 33-39s hold final frame. |
| 10 | 5:32 | 37 | In the lesson's hands-on section you'll find an interview prompt that turns rough notes into a structured task list, quoting evidence and writing unknown instead of guessing. Then a short Python script scores a use-case backlog from a spreadsheet, combining hours per week with agreed scores for value, data readiness, integration ease, error tolerance and risk, and prints a ranked list with an illustrative monthly value. Agree the weights with leadership, keep the file shared, and re-score every quarter, because model capabilities and costs change fast. | Hands-on in the lesson<br>• Interview-to-task-list prompt<br>• Backlog scoring script<br>• Re-score quarterly | Layout: screen recording style: a CSV opens with columns name, freq_per_week, minutes, scores; a terminal prints a ranked list with scores and hours per week. Timing (37s): 0-3s 'Hands-on in the lesson' heading fades in; 3-31s elements build in narration order; 31-37s hold final frame. |
| 11 | 6:09 | 29 | A few more mistakes worth naming. Asking leadership for use cases instead of the people doing the work. Counting a task's total time without noticing that most of it is waiting, not working. Scoring every idea as high value because nobody wants to disappoint colleagues. And forgetting data readiness: an idea that needs data you don't have is a data project first and an AI project second. | More mistakes<br>• Asking only leadership<br>• Confusing waiting time with work time<br>• Everything scored 'high value'<br>• Ignoring data readiness | Layout: four small cards with icons (boardroom, hourglass, inflated balloon, empty database) flip to show the correction on their backs. Timing (29s): 0-3s 'More mistakes' heading fades in; 3-23s elements build in narration order; 23-29s hold final frame. |
| 12 | 6:38 | 28 | How will you know you picked well? Within the first few weeks of a pilot, the people doing the work say it saves them time, unprompted. The volume assumptions in your scoring turn out roughly right. And the measured time saved is in the same range as your estimate. If the estimate was wildly off, revisit your scoring method before picking the next use case. | How you'll know you picked well<br>• Users report time saved, unprompted<br>• Volume assumptions hold<br>• Measured savings ≈ estimate | Layout: A three-item checklist with green ticks; a small comparison bar 'estimated 3.2 h/week' vs 'measured 2.9 h/week'. Timing (28s): 0-3s 'How you'll know you picked well' heading fades in; 3-22s elements build in narration order; 22-28s hold final frame. |
| 13 | 7:06 | 68 | Watch me do it. First, I paste twenty minutes of interview notes from our operations lead into the interview prompt. The output is a table: task, trigger, frequency, minutes, who, inputs, output, pain and sensitivity. Two numbers say unknown, so I ask a follow-up question and fill them in. Next, I save three rows into usecases dot csv and score value, data readiness, integration ease, error tolerance and risk from one to five with the team. Then I run the script. It computes hours per week from frequency times minutes, normalises that to a one to five time score, flips risk so low risk scores high, and blends everything with the weights. It prints a ranked list: client report commentary on top, about six hours a week, then invoice chasing, then proposal drafts. Finally, I check the top item against the red flags, name the head of client services as owner, and book a scoping session. | Watch me do it: from notes to ranked backlog<br>• Interview prompt → task table<br>• usecases.csv + team scores<br>• Script: hours, weights, rank<br>• Red-flag check + owner | Layout: three stages left to right: notes pasted into a prompt, a CSV with scores, a terminal ranked list. Each stage highlights as narrated; the top row gets an 'Owner assigned' badge. Timing (68s): 0-3s 'Watch me do it: from notes to ranked backlog' heading fades in; 3-62s elements build in narration order; 62-68s hold final frame. |
| 14 | 8:14 | 34 | To recap: start from valuable, frequent, painful work. AI is strongest with unstructured information, language, reviewable judgement and search. Map workflows, estimate value and feasibility, plot them on the grid, begin with quick wins and stage the strategic bets. Your next step is to map one workflow in your organisation step by step, score three candidate steps, and place them on the grid. In the next lesson, we'll turn your top candidate into a tight scope with success metrics. | Recap<br>• Problems first<br>• Map, score, prioritise<br>• Next step: map one workflow and score three steps | Layout: recap card with the grid and a highlighted quick-win note; 'Next: Scoping and success metrics' title card. Timing (34s): 0-3s 'Recap' heading fades in; 3-28s elements build in narration order; 28-34s hold final frame. |
| 15 | 8:48 | 28 | Try this now. Book a twenty-minute conversation with someone whose work you know is repetitive. Ask them to show you, not tell you, their most frequent task, and time it. Then run the interview prompt on your notes and put three candidate steps into the scoring script or sheet. By the end, you'll have your first scored backlog, grounded in real work instead of assumptions. | Try this now (45 minutes)<br>• 20-minute 'show me' session<br>• Time the most frequent task<br>• Score three candidate steps | Layout: A calendar invite '20 min: show me your most frequent task'; a stopwatch; a small backlog table with three scored rows. Timing (28s): 0-3s 'Try this now (45 minutes)' heading fades in; 3-22s elements build in narration order; 22-28s hold final frame. |

<details><summary>Narration per scene (paste into ElevenLabs)</summary>

Scene 1 — Finding high-value AI use cases

```text
Most AI projects don't fail because the model is weak. They fail because someone picked the wrong problem. A chatbot for a question nobody asks. An impressive demo for a task that happens twice a year. In this lesson you'll learn a repeatable way to find the work where AI creates real value, score it honestly, and choose your first projects with confidence, whether you run a five-person agency or a department in a large company.
```

Scene 2 — Analogy: choosing a shop location

```text
Here's an analogy. Choosing AI use cases is like choosing where to open a new shop. You don't pick the street with the prettiest buildings. You count footfall, look at what people already buy, and check the rent. Frequency is footfall. Time spent is what people already pay in effort. Feasibility is the rent. The prettiest idea, a flashy chatbot, often sits on an empty street.
```

Scene 3 — Where AI creates value

```text
Start with problems, not models. The best candidates are work that's valuable, frequent and painful. Then ask whether AI is the right tool. Language and multimodal models are strongest with unstructured information like emails, documents, tickets and calls; with transforming language, such as drafting, summarising, translating and classifying; with judgement at scale where a human can review; with search and synthesis across sources; and with repetitive steps that have too much variation for old rules-based automation. They're weaker when you need guaranteed exactness without checking, when there's little data or context, or when stakes are high and there's no room for review.
```

Scene 4 — Discovery method

```text
Here's the method. Map workflows, not departments. Walk through a real process step by step, like handling a supplier invoice or answering a sales enquiry, and note volume, time, errors and frustrations. For each step, ask whether it's language-heavy, repetitive, rules-with-exceptions or search-heavy. Estimate value: time saved times volume, faster response, better quality, revenue or reduced risk. Estimate feasibility: data availability, integration difficulty, the accuracy you need and the risk if it's wrong.
```

Scene 5 — Value–feasibility grid

```text
Now plot candidates on a value and feasibility grid. High value and high feasibility are quick wins: start there, because they build credibility and teach your team. High value but low feasibility are strategic bets: plan carefully and de-risk with pilots. Low value and high feasibility are nice-to-haves, only worth it if nearly free. And low value, low feasibility, you avoid. To find the numbers, ask simple questions. How often does this happen? How long does it take, and who does it? What happens when it's done badly? Would a good draft that a human edits save most of the time?
```

Scene 6 — Simple example: 'send me last year's return'

```text
A simple example first. A three-person accounting firm notices every client email asking for a copy of last year's return takes about ten minutes: find the file, check identity, attach, reply. It happens around twenty times a week. That's over three hours weekly on a frequent, clear, low-judgement task, where a drafted reply with the right file attached, checked by a person, saves most of the time. Not glamorous, but an ideal first use case.
```

Scene 7 — Worked example: a three-country agency

```text
Let's see it work. A digital agency serving e-commerce clients in the UK, UAE and Pakistan maps three workflows. Monthly client reports eat hours of analyst time, and AI can draft commentary from exported data for analysts to review: a quick win. New business proposals are bespoke, but AI can draft sections from past proposals: medium value, medium feasibility. Automated media buying could be valuable, but it's high risk and dependent on platform data: a strategic bet for later, with approval gates. They start with reports, measure hours saved and client feedback, and use the result to fund the next project.
```

Scene 8 — Business example (illustrative)

```text
Let's add illustrative numbers to the agency. Monthly reports take analysts about six hours per client across twenty clients, around a hundred and twenty hours a month. If AI drafts the commentary and analysts review for ninety minutes each, that's about thirty hours, saving roughly ninety analyst hours a month. The proposal assistant might save a third of that. Media buying could be worth more, but only after months of data work. The grid made the order obvious.
```

Scene 9 — Red flags

```text
Watch for red flags. AI-for-everything initiatives with no named process owner. Use cases chosen because they're impressive rather than frequent. No way to measure success. Tasks where errors are costly and outputs can't be checked. And automating a broken process instead of fixing it first. The best ideas usually come from the people who do the work, so run short sessions where they show you their most repetitive, frustrating tasks with real examples. You'll find better candidates, spot edge cases early, and build the goodwill that adoption depends on.
```

Scene 10 — Hands-on in the lesson

```text
In the lesson's hands-on section you'll find an interview prompt that turns rough notes into a structured task list, quoting evidence and writing unknown instead of guessing. Then a short Python script scores a use-case backlog from a spreadsheet, combining hours per week with agreed scores for value, data readiness, integration ease, error tolerance and risk, and prints a ranked list with an illustrative monthly value. Agree the weights with leadership, keep the file shared, and re-score every quarter, because model capabilities and costs change fast.
```

Scene 11 — More mistakes

```text
A few more mistakes worth naming. Asking leadership for use cases instead of the people doing the work. Counting a task's total time without noticing that most of it is waiting, not working. Scoring every idea as high value because nobody wants to disappoint colleagues. And forgetting data readiness: an idea that needs data you don't have is a data project first and an AI project second.
```

Scene 12 — How you'll know you picked well

```text
How will you know you picked well? Within the first few weeks of a pilot, the people doing the work say it saves them time, unprompted. The volume assumptions in your scoring turn out roughly right. And the measured time saved is in the same range as your estimate. If the estimate was wildly off, revisit your scoring method before picking the next use case.
```

Scene 13 — Watch me do it: from notes to ranked backlog

```text
Watch me do it. First, I paste twenty minutes of interview notes from our operations lead into the interview prompt. The output is a table: task, trigger, frequency, minutes, who, inputs, output, pain and sensitivity. Two numbers say unknown, so I ask a follow-up question and fill them in. Next, I save three rows into usecases dot csv and score value, data readiness, integration ease, error tolerance and risk from one to five with the team. Then I run the script. It computes hours per week from frequency times minutes, normalises that to a one to five time score, flips risk so low risk scores high, and blends everything with the weights. It prints a ranked list: client report commentary on top, about six hours a week, then invoice chasing, then proposal drafts. Finally, I check the top item against the red flags, name the head of client services as owner, and book a scoping session.
```

Scene 14 — Recap

```text
To recap: start from valuable, frequent, painful work. AI is strongest with unstructured information, language, reviewable judgement and search. Map workflows, estimate value and feasibility, plot them on the grid, begin with quick wins and stage the strategic bets. Your next step is to map one workflow in your organisation step by step, score three candidate steps, and place them on the grid. In the next lesson, we'll turn your top candidate into a tight scope with success metrics.
```

Scene 15 — Try this now (45 minutes)

```text
Try this now. Book a twenty-minute conversation with someone whose work you know is repetitive. Ask them to show you, not tell you, their most frequent task, and time it. Then run the interview prompt on your notes and put three candidate steps into the scoring script or sheet. By the end, you'll have your first scored backlog, grounded in real work instead of assumptions.
```

</details>

### 2. Scoping and defining success

Lesson `scoping-and-success-metrics` · module “Finding and scoping high-value use cases” · target 9 min · 15 scenes · 1195 words · planned 8:41 (words ≈ 8:40) · status: **to produce**

| Term | Say |
|---|---|
| YAML | yam-el |

| # | Time | Sec | Narration | On screen | Visual |
|---|---|---|---|---|---|
| 1 | 0:00 | 34 | AI for customer support is not a project. It's a theme. And themes are where budgets go to die, because nobody can say when they're done or whether they worked. In this lesson you'll turn a theme into a scope, choose how much autonomy the AI should have, define success in three layers plus guardrails, and write kill criteria before you start. You'll leave with a one-page brief that makes the build smaller and the launch decision obvious. | Scoping AI work<br>• Themes vs scopes<br>• The AI's role<br>• Metrics, baselines, kill criteria | Layout: A cloud labelled 'AI for customer support' condenses into a crisp index card with a clear sentence, bullet boundaries and a target. Timing (34s): 0-3s 'Scoping AI work' heading fades in; 3-28s elements build in narration order; 28-34s hold final frame. |
| 2 | 0:34 | 30 | Here's an analogy. Starting an AI project without a scope is like hiring a builder and saying, make the house better. You'll get something, you'll pay for it, and you won't agree on whether it's finished. A scope is the drawing with measurements. It tells everyone what's being built, what isn't, and how you'll know it's done. It feels slower on day one and saves weeks by day thirty. | Analogy: 'make the house better'<br>• No drawing = no agreement on done<br>• A scope is the measured drawing | Layout: A builder figure looks puzzled at a vague note 'make it better'; the note is replaced by an architectural drawing with dimensions and a 'done when' box. Timing (30s): 0-3s 'Analogy: 'make the house better'' heading fades in; 3-24s elements build in narration order; 24-30s hold final frame. |
| 3 | 1:04 | 35 | Compare the theme with this scope. Draft first replies to order-status and returns emails, in English and Arabic, for human agents to review and send, reducing median handling time without lowering quality scores. Now you know the inputs, the languages, the channel, who's in control, and what success means. Write every AI project as a one-page brief: problem, users, what's in scope and explicitly out, the AI's role, success metrics, risks, data, how you'll evaluate, and a single accountable owner. | The one-page AI brief<br>• Problem, users, scope in and out<br>• AI's role, success, risks<br>• Data, evaluation, owner | Layout: An index card expands into a one-page brief template with ten labelled sections filling in one after another. Timing (35s): 0-3s 'The one-page AI brief' heading fades in; 3-29s elements build in narration order; 29-35s hold final frame. |
| 4 | 1:39 | 36 | Next, choose the AI's role. The same use case can run at four levels of autonomy. Assist: AI suggests, and a human decides and acts. Automate with review: AI acts, but a human approves before it takes effect. Automate with audit: AI acts, and humans review samples afterwards. Full automation: AI acts and only exceptions escalate. Choose based on the cost of errors, how easily errors are spotted, volume and user trust. Most first versions belong at assist or automate with review. | Choosing the AI's role<br>1 Assist<br>2 Automate with review<br>3 Automate with audit<br>4 Full automation | Layout: A four-step staircase; a small figure stands on step 2 with a sign 'Most v1s start here'. Factors orbit: error cost, detectability, volume, trust. Timing (36s): 0-3s 'Choosing the AI's role' heading fades in; 3-30s elements build in narration order; 30-36s hold final frame. |
| 5 | 2:15 | 37 | Define success in three layers. The business outcome is why the project exists: handling time, conversion, cost per case, revenue or satisfaction. Quality metrics cover accuracy, groundedness, policy compliance and tone, measured by evaluation and review. Adoption and experience cover usage, how much people edit AI drafts, satisfaction and time to complete. Then add guardrail metrics that must not get worse, like complaint rates, escalations and fairness across user groups. Set targets before launch. We'll see how it goes makes every result look like success. | Three layers + guardrails<br>• Business outcome<br>• Quality<br>• Adoption and experience<br>• Guardrails that must not worsen | Layout: A layered cake diagram: bottom 'Business outcome', middle 'Quality', top 'Adoption'; a protective fence around it labelled 'Guardrail metrics'. Timing (37s): 0-3s 'Three layers + guardrails' heading fades in; 3-31s elements build in narration order; 31-37s hold final frame. |
| 6 | 2:52 | 37 | You can only claim improvement against a baseline. Before launch, measure today's process: time per task, error rate, satisfaction, cost. If the numbers don't exist, spend a week or two collecting them; they'll be the most persuasive figures in your launch review. And define good enough by comparing with the current human process and the cost of errors. Internal drafts reviewed by experts can be useful at moderate accuracy. Customer-facing automated replies need a much higher bar, and some categories should be excluded entirely. | Baselines and 'good enough'<br>• Measure today first<br>• Compare with the human process<br>• Match the bar to the cost of errors | Layout: A before-and-after bar chart with the 'before' bar dashed and labelled 'measure this first'. A dial marked 'required accuracy' moves higher when a 'customer-facing' toggle is switched on. Timing (37s): 0-3s 'Baselines and 'good enough'' heading fades in; 3-31s elements build in narration order; 31-37s hold final frame. |
| 7 | 3:29 | 32 | A simple example before the insurance one. A café chain wants AI for customer reviews. Theme: use AI on reviews. Scope: every Monday, summarise last week's Google and delivery-app reviews per branch into three positives, three complaints and one suggested action, for branch managers to read. Out of scope: replying to reviews. Success: managers read it and act on at least one item a week. One paragraph, and suddenly everyone knows what to build. | Simple example: café reviews<br>• Theme → weekly per-branch summary<br>• Out: replying to reviews<br>• Success: one action per week | Layout: A vague sticky note 'AI for reviews' transforms into a structured card with 'Every Monday', 'per branch', '3 + 3 + 1', 'Out of scope: replies'. Timing (32s): 0-3s 'Simple example: café reviews' heading fades in; 3-26s elements build in narration order; 26-32s hold final frame. |
| 8 | 4:01 | 37 | Here's a brief in action. An insurance broker scopes a document assistant. Problem: brokers spend significant time extracting key terms from policy schedules. In scope: fifteen defined fields from PDF schedules from their top ten insurers, English only. Out of scope: advice to customers, handwritten documents, other languages. AI's role: extract with page citations while the broker reviews side by side. Success: faster extraction, field accuracy above an agreed threshold, and better broker satisfaction. Guardrail: no increase in quote errors. Owner: head of broking operations. | Worked example: insurance broker<br>• 15 fields, top 10 insurers, English<br>• Extract with citations, broker reviews<br>• Accuracy threshold + no rise in quote errors | Layout: side-by-side mock-up: a PDF policy schedule on the left with highlighted fields; an extraction panel on the right with page-number citations and a 'Confirm' button. Timing (37s): 0-3s 'Worked example: insurance broker' heading fades in; 3-31s elements build in narration order; 31-37s hold final frame. |
| 9 | 4:38 | 29 | Illustrative numbers for the broker. Baseline measurement showed about thirty-five minutes per policy schedule and a few quote errors a month traced to extraction. The target was under fifteen minutes with field accuracy of at least ninety-five percent on a hundred and twenty test documents. The pilot reached about twelve minutes and ninety-six percent. Because the targets were written down first, the launch decision took one meeting. | Business example (illustrative)<br>• Baseline ~35 min per schedule<br>• Target <15 min, ≥95% accuracy<br>• Pilot ~12 min, ~96% → launch | Layout: a target line chart with a dashed 'target' band; the pilot result dot lands inside it; a meeting icon labelled 'decision: 1 meeting'. Timing (29s): 0-3s 'Business example (illustrative)' heading fades in; 3-23s elements build in narration order; 23-29s hold final frame. |
| 10 | 5:07 | 36 | Now write kill criteria as well as success criteria: the conditions under which you'll stop or pivot. For example, accuracy plateaus below the threshold after two improvement rounds, or edit rates show drafts aren't saving time after six weeks. Pre-committing prevents zombie projects. In the hands-on section you'll store the brief as a YAML file your evaluation harness and dashboards can read, and use a sceptical AI reviewer prompt that hunts for ambiguities, metrics without baselines, and unaddressed risks before anyone builds. | Kill criteria + hands-on<br>• Decide in advance when to stop<br>• Brief as YAML config<br>• AI 'sceptical reviewer' prompt | Layout: A YAML file scrolls in an editor with 'kill_criteria' highlighted; a reviewer panel lists three critiques with quoted lines. Timing (36s): 0-3s 'Kill criteria + hands-on' heading fades in; 3-30s elements build in narration order; 30-36s hold final frame. |
| 11 | 5:43 | 27 | Common scoping mistakes. Leaving out the out-of-scope list, so the project grows every week. Choosing only adoption metrics, which can rise while quality falls. Setting targets after launch. Forgetting guardrail metrics, so a faster process quietly produces more complaints. And naming a committee as owner. If nobody's name is on it, nobody will stop it when the kill criteria are met. | Common mistakes<br>• No out-of-scope list<br>• Adoption-only metrics<br>• Targets set after launch<br>• No guardrails<br>• A committee as owner | Layout: A scope document with red annotations appearing in the margins next to missing sections; a final annotation circles 'Owner: TBC'. Timing (27s): 0-3s 'Common mistakes' heading fades in; 3-21s elements build in narration order; 21-27s hold final frame. |
| 12 | 6:10 | 26 | How will you know your scoping worked? Engineers and designers can estimate the work from the brief without a meeting. The evaluation set follows directly from the scope. At the launch review, the decision is quick, because targets and kill criteria were agreed in advance. And six weeks in, nobody is arguing about what the project was supposed to do. | How you'll know it worked<br>• Estimates from the brief alone<br>• Eval set follows from scope<br>• Quick launch decision<br>• No arguments at week six | Layout: four quick vignettes: an engineer giving an estimate from a document, an eval spreadsheet generated from scope lines, a launch meeting ending early, a calm team calendar at week six. Timing (26s): 0-3s 'How you'll know it worked' heading fades in; 3-20s elements build in narration order; 20-26s hold final frame. |
| 13 | 6:36 | 63 | Watch me do it. I open ai-brief dot yaml. First, feature, version and owner. Next, problem and users in one line each. Then scope in: PDF schedules from our top ten insurers, fifteen fields, English. Scope out: advice, handwritten documents, other languages. AI role: extract with review. Success: the primary metric is median minutes per document, with the baseline measured in week zero; quality thresholds for field accuracy and citation rate; and two adoption metrics. Guardrails: no increase in customer-reported quote errors. Kill criteria: accuracy below threshold after two iterations, or no time saving after six weeks. Then I paste the file into the sceptical reviewer prompt. It flags three things: no definition of how minutes are measured, no owner for the evaluation set, and no plan for insurer format changes. I add a timing method, name an evaluation owner and add a monthly format check. | Watch me do it: ai-brief.yaml<br>• Scope in / out, AI role<br>• Metrics with baselines<br>• Kill criteria<br>• Reviewer finds 3 gaps → fixed | Layout: YAML file centre, sections highlighting in order; a reviewer panel slides in from the right listing three quoted gaps; each gap is resolved with a new YAML line. Timing (63s): 0-3s 'Watch me do it: ai-brief.yaml' heading fades in; 3-57s elements build in narration order; 57-63s hold final frame. |
| 14 | 7:39 | 32 | To recap: turn themes into scopes with clear in and out boundaries. Choose the AI's autonomy by error cost, detectability, volume and trust. Define business, quality, adoption and guardrail metrics, measure a baseline and write kill criteria. Your next step is to write a one-page brief for your top use case, including out-of-scope items and kill criteria, and run it past the sceptical reviewer prompt. Next module, we'll prototype it cheaply on real data. | Recap<br>• Scope, role, metrics, baseline, kill criteria<br>• Next step: write and stress-test your brief | Layout: recap card with the brief template; 'Next module: Prototyping and build vs buy' title card. Timing (32s): 0-3s 'Recap' heading fades in; 3-26s elements build in narration order; 26-32s hold final frame. |
| 15 | 8:11 | 30 | Try this now. Take your top use case from the last lesson and fill in the one-page brief: problem, users, scope in, scope out, the AI's role, three layers of metrics, a guardrail, kill criteria and one named owner. Then paste it into the sceptical reviewer prompt and fix the three most important issues it finds. Thirty minutes, and you'll have a brief you can actually build from. | Try this now (30 minutes)<br>• Fill the one-page brief<br>• Run the sceptical reviewer prompt<br>• Fix the top three issues | Layout: A brief template fills section by section; a reviewer panel lists three issues; each issue gets a strike-through as the brief updates. Timing (30s): 0-3s 'Try this now (30 minutes)' heading fades in; 3-24s elements build in narration order; 24-30s hold final frame. |

<details><summary>Narration per scene (paste into ElevenLabs)</summary>

Scene 1 — Scoping AI work

```text
AI for customer support is not a project. It's a theme. And themes are where budgets go to die, because nobody can say when they're done or whether they worked. In this lesson you'll turn a theme into a scope, choose how much autonomy the AI should have, define success in three layers plus guardrails, and write kill criteria before you start. You'll leave with a one-page brief that makes the build smaller and the launch decision obvious.
```

Scene 2 — Analogy: 'make the house better'

```text
Here's an analogy. Starting an AI project without a scope is like hiring a builder and saying, make the house better. You'll get something, you'll pay for it, and you won't agree on whether it's finished. A scope is the drawing with measurements. It tells everyone what's being built, what isn't, and how you'll know it's done. It feels slower on day one and saves weeks by day thirty.
```

Scene 3 — The one-page AI brief

```text
Compare the theme with this scope. Draft first replies to order-status and returns emails, in English and Arabic, for human agents to review and send, reducing median handling time without lowering quality scores. Now you know the inputs, the languages, the channel, who's in control, and what success means. Write every AI project as a one-page brief: problem, users, what's in scope and explicitly out, the AI's role, success metrics, risks, data, how you'll evaluate, and a single accountable owner.
```

Scene 4 — Choosing the AI's role

```text
Next, choose the AI's role. The same use case can run at four levels of autonomy. Assist: AI suggests, and a human decides and acts. Automate with review: AI acts, but a human approves before it takes effect. Automate with audit: AI acts, and humans review samples afterwards. Full automation: AI acts and only exceptions escalate. Choose based on the cost of errors, how easily errors are spotted, volume and user trust. Most first versions belong at assist or automate with review.
```

Scene 5 — Three layers + guardrails

```text
Define success in three layers. The business outcome is why the project exists: handling time, conversion, cost per case, revenue or satisfaction. Quality metrics cover accuracy, groundedness, policy compliance and tone, measured by evaluation and review. Adoption and experience cover usage, how much people edit AI drafts, satisfaction and time to complete. Then add guardrail metrics that must not get worse, like complaint rates, escalations and fairness across user groups. Set targets before launch. We'll see how it goes makes every result look like success.
```

Scene 6 — Baselines and 'good enough'

```text
You can only claim improvement against a baseline. Before launch, measure today's process: time per task, error rate, satisfaction, cost. If the numbers don't exist, spend a week or two collecting them; they'll be the most persuasive figures in your launch review. And define good enough by comparing with the current human process and the cost of errors. Internal drafts reviewed by experts can be useful at moderate accuracy. Customer-facing automated replies need a much higher bar, and some categories should be excluded entirely.
```

Scene 7 — Simple example: café reviews

```text
A simple example before the insurance one. A café chain wants AI for customer reviews. Theme: use AI on reviews. Scope: every Monday, summarise last week's Google and delivery-app reviews per branch into three positives, three complaints and one suggested action, for branch managers to read. Out of scope: replying to reviews. Success: managers read it and act on at least one item a week. One paragraph, and suddenly everyone knows what to build.
```

Scene 8 — Worked example: insurance broker

```text
Here's a brief in action. An insurance broker scopes a document assistant. Problem: brokers spend significant time extracting key terms from policy schedules. In scope: fifteen defined fields from PDF schedules from their top ten insurers, English only. Out of scope: advice to customers, handwritten documents, other languages. AI's role: extract with page citations while the broker reviews side by side. Success: faster extraction, field accuracy above an agreed threshold, and better broker satisfaction. Guardrail: no increase in quote errors. Owner: head of broking operations.
```

Scene 9 — Business example (illustrative)

```text
Illustrative numbers for the broker. Baseline measurement showed about thirty-five minutes per policy schedule and a few quote errors a month traced to extraction. The target was under fifteen minutes with field accuracy of at least ninety-five percent on a hundred and twenty test documents. The pilot reached about twelve minutes and ninety-six percent. Because the targets were written down first, the launch decision took one meeting.
```

Scene 10 — Kill criteria + hands-on

```text
Now write kill criteria as well as success criteria: the conditions under which you'll stop or pivot. For example, accuracy plateaus below the threshold after two improvement rounds, or edit rates show drafts aren't saving time after six weeks. Pre-committing prevents zombie projects. In the hands-on section you'll store the brief as a YAML file your evaluation harness and dashboards can read, and use a sceptical AI reviewer prompt that hunts for ambiguities, metrics without baselines, and unaddressed risks before anyone builds.
```

Scene 11 — Common mistakes

```text
Common scoping mistakes. Leaving out the out-of-scope list, so the project grows every week. Choosing only adoption metrics, which can rise while quality falls. Setting targets after launch. Forgetting guardrail metrics, so a faster process quietly produces more complaints. And naming a committee as owner. If nobody's name is on it, nobody will stop it when the kill criteria are met.
```

Scene 12 — How you'll know it worked

```text
How will you know your scoping worked? Engineers and designers can estimate the work from the brief without a meeting. The evaluation set follows directly from the scope. At the launch review, the decision is quick, because targets and kill criteria were agreed in advance. And six weeks in, nobody is arguing about what the project was supposed to do.
```

Scene 13 — Watch me do it: ai-brief.yaml

```text
Watch me do it. I open ai-brief dot yaml. First, feature, version and owner. Next, problem and users in one line each. Then scope in: PDF schedules from our top ten insurers, fifteen fields, English. Scope out: advice, handwritten documents, other languages. AI role: extract with review. Success: the primary metric is median minutes per document, with the baseline measured in week zero; quality thresholds for field accuracy and citation rate; and two adoption metrics. Guardrails: no increase in customer-reported quote errors. Kill criteria: accuracy below threshold after two iterations, or no time saving after six weeks. Then I paste the file into the sceptical reviewer prompt. It flags three things: no definition of how minutes are measured, no owner for the evaluation set, and no plan for insurer format changes. I add a timing method, name an evaluation owner and add a monthly format check.
```

Scene 14 — Recap

```text
To recap: turn themes into scopes with clear in and out boundaries. Choose the AI's autonomy by error cost, detectability, volume and trust. Define business, quality, adoption and guardrail metrics, measure a baseline and write kill criteria. Your next step is to write a one-page brief for your top use case, including out-of-scope items and kill criteria, and run it past the sceptical reviewer prompt. Next module, we'll prototype it cheaply on real data.
```

Scene 15 — Try this now (30 minutes)

```text
Try this now. Take your top use case from the last lesson and fill in the one-page brief: problem, users, scope in, scope out, the AI's role, three layers of metrics, a guardrail, kill criteria and one named owner. Then paste it into the sceptical reviewer prompt and fix the three most important issues it finds. Thirty minutes, and you'll have a brief you can actually build from.
```

</details>

### 3. Rapid prototyping of AI features

Lesson `rapid-prototyping` · module “Prototyping and build vs buy” · target 9 min · 15 scenes · 1263 words · planned 9:09 (words ≈ 9:09) · status: **to produce**

| Term | Say |
|---|---|
| CSV | C S V |

| # | Time | Sec | Narration | On screen | Visual |
|---|---|---|---|---|---|
| 1 | 0:00 | 36 | AI features are strangely hard to judge on paper. A prompt that looks obviously right falls apart on real, messy inputs. A use case that sounds marginal turns out to be the thing everyone uses. So the cheapest way to make good AI product decisions is to prototype, fast, on real data. In this lesson you'll climb a four-rung prototyping ladder, learn what each rung should teach you, and run a spreadsheet evaluation that can kill or confirm an idea in days. | Rapid prototyping<br>• Why paper plans fail for AI<br>• The four-rung ladder<br>• The spreadsheet evaluation | Layout: A polished slide deck labelled 'AI idea' dissolves when real messy emails are thrown at it; a simple prototype sheet survives and shows scores. Timing (36s): 0-3s 'Rapid prototyping' heading fades in; 3-30s elements build in narration order; 30-36s hold final frame. |
| 2 | 0:36 | 24 | Why prototype before planning in detail? Here's an analogy. Chefs don't write a full menu and print it before tasting the dishes. They cook small batches, taste, adjust and only then commit. AI features are the same. A prompt that sounds perfect often tastes wrong on real inputs. Prototyping is tasting early, while changes are cheap. | Analogy: taste before you print the menu<br>• Small batches first<br>• Adjust while it's cheap | Layout: A chef figure tasting from a small pan, adjusting seasoning; a printed menu stays blank until a green tick appears on the tasting card. Timing (24s): 0-3s 'Analogy: taste before you print the menu' heading fades in; 3-18s elements build in narration order; 18-24s hold final frame. |
| 3 | 1:00 | 47 | Here's the ladder. Rung one, a chat prototype, takes hours: try the task by hand in an AI assistant with ten to twenty real examples and refine the prompt. This alone kills or confirms many ideas. Rung two, prompt plus spreadsheet evaluation, takes days: run a fixed prompt over fifty to a hundred real inputs and have experts grade the outputs. Rung three, a clickable or no-code prototype, puts it in front of real users in context. Rung four is a pilot: a limited production version with logging and evaluation. Each rung costs roughly ten times the one below, so climb only when the lower rung shows promise. | The prototyping ladder<br>1 Chat prototype (hours)<br>2 Spreadsheet evaluation (days)<br>3 Clickable/no-code prototype<br>4 Pilot (weeks) | Layout: A ladder with four rungs, each labelled with its time and a cost multiplier badge (x1, x10, x100, x1000). A figure climbs only after a green light appears on the rung below. Timing (47s): 0-3s 'The prototyping ladder' heading fades in; 3-41s elements build in narration order; 41-47s hold final frame. |
| 4 | 1:47 | 39 | At every rung you're answering five questions. Feasibility: is output quality good enough on real, messy inputs? Value: do users save time or produce better work, and do they want it? Workflow fit: where does it slot into existing tools? Risks: what failure modes appear, how often, and can you detect them? And cost signals: tokens per task, latency and review time. Write the evidence you need to move up before you start, for example at least seventy percent of drafts rated four or better by two senior reviewers. | What each rung must answer<br>• Feasibility • Value • Workflow fit<br>• Risks • Cost signals<br>• Set the evidence bar in advance | Layout: five gauges on a dashboard labelled Feasibility, Value, Fit, Risk, Cost; a threshold line on the Feasibility gauge marked '70% rated 4+'. Timing (39s): 0-3s 'What each rung must answer' heading fades in; 3-33s elements build in narration order; 33-39s hold final frame. |
| 5 | 2:26 | 41 | Prototypes only tell the truth with realistic inputs, but prototyping often happens before security reviews. Balance that. Use approved tools for any sensitive data. Anonymise or synthesise data where you can, while checking synthetic data isn't unrealistically clean. And start with public or low-sensitivity examples. Remember too that the most valuable outputs of prototyping aren't the demo. They're a tested, versioned prompt, an evaluation set of real inputs with expert grades, a documented list of failure modes, and real user feedback. All of that carries straight into the production build or the vendor evaluation. | Real data, safely<br>• Approved tools only for sensitive data<br>• Anonymise or synthesise with care<br>• Keep the assets: prompt, eval set, failure modes | Layout: A data card passes through a 'redact' filter before entering a prototype box; beneath, four asset icons are collected into a folder labelled 'carry forward'. Timing (41s): 0-3s 'Real data, safely' heading fades in; 3-35s elements build in narration order; 35-41s hold final frame. |
| 6 | 3:07 | 33 | A simple example of rung one. A recruitment agency wonders if AI can turn messy interview notes into a clean candidate summary. A recruiter pastes five real, anonymised sets of notes into an approved assistant with a draft prompt. Two summaries are great, two miss salary expectations, one invents a qualification. In twenty minutes they learn the prompt needs a field list and a rule about never adding facts. That's rung one doing its job. | Simple example: interview notes<br>• 5 real notes, one prompt<br>• 2 great, 2 missing fields, 1 invented fact<br>• Learned in 20 minutes | Layout: five note cards feed into a chat window; five summary cards come out with badges: two green, two amber 'missing salary', one red 'invented qualification'. Timing (33s): 0-3s 'Simple example: interview notes' heading fades in; 3-27s elements build in narration order; 27-33s hold final frame. |
| 7 | 3:40 | 44 | Here's a worked example. An agency tests whether AI can draft proposal sections. In the chat prototype, five past briefs show drafts are generic unless the model gets case studies and service descriptions, so they build a context pack. In the spreadsheet evaluation, thirty past briefs run through the prompt and senior staff score relevance and accuracy. Most drafts are acceptable first drafts, but case-study selection often fails. A simple form prototype then gets heavy use for three weeks while they measure edit time. Decision: pilot, with retrieval over the case-study library to fix selection. Total spend before any engineering: modest. | Worked example: proposal assistant<br>• Chat → context pack<br>• 30-brief evaluation → selection failures<br>• Form prototype → pilot with retrieval | Layout: A timeline with three stages; each shows a small artefact (chat window, spreadsheet with scores, simple web form) and a key finding bubble. Timing (44s): 0-3s 'Worked example: proposal assistant' heading fades in; 3-38s elements build in narration order; 38-44s hold final frame. |
| 8 | 4:24 | 30 | Illustrative numbers for the proposal assistant. Thirty past briefs were scored by two senior staff: about seventy percent rated four or five for relevance, but case-study selection was wrong in roughly a third. In the three-week form prototype, account managers used it for most proposals and median drafting time for the approach section fell from about ninety minutes to about forty. That evidence, not the demo, got the pilot funded. | Business example (illustrative)<br>• ~70% of drafts rated 4–5<br>• Case-study selection wrong ~1/3<br>• Drafting ~90 → ~40 minutes | Layout: a grading spreadsheet with a score histogram; a highlighted 'wrong case study' column; a time bar shrinking from 90 to 40 minutes. Timing (30s): 0-3s 'Business example (illustrative)' heading fades in; 3-24s elements build in narration order; 24-30s hold final frame. |
| 9 | 4:54 | 30 | Avoid four mistakes. Demoing on hand-picked easy examples. Testing only with the builders instead of real users. Treating a prototype as production, with no logging, security or evaluation. And falling in love with the demo while ignoring the evaluation data. Early rungs rarely need engineers. Product managers, analysts and domain experts can run chat prototypes and spreadsheet evaluations themselves, which keeps iteration fast and puts domain judgement at the centre. | Common mistakes<br>• Cherry-picked demos<br>• No real users<br>• Prototype treated as production<br>• Ignoring the data | Layout: four warning cards; then a scene of a domain expert (abstract figure) at a laptop running the evaluation without an engineer. Timing (30s): 0-3s 'Common mistakes' heading fades in; 3-24s elements build in narration order; 24-30s hold final frame. |
| 10 | 5:24 | 37 | The hands-on section gives you a spreadsheet evaluation runner. It reads real inputs from a CSV, runs a versioned prompt through the Claude API with polite back-off on rate limits, and writes outputs to a new CSV with empty columns for relevance, accuracy, failure mode and grader. You'll also see when to use a batch API for big runs at lower cost, and grading tips: a written rubric, two graders on the first twenty rows, and short failure-mode labels that become your improvement backlog. | Hands-on in the lesson<br>• CSV in → model → CSV out for grading<br>• Versioned prompt files, back-off on rate limits<br>• Batch runs for large sets | Layout: code editor showing the run_one function and CSV writer; then a spreadsheet opens with outputs and empty grade columns being filled by a grader. Timing (37s): 0-3s 'Hands-on in the lesson' heading fades in; 3-31s elements build in narration order; 31-37s hold final frame. |
| 11 | 6:01 | 28 | How will you know a prototype phase succeeded? Not by how impressive the demo looked. By whether you hit the evidence bar you set in advance, whether you now have a documented list of failure modes, and whether you have a clear decision: climb to the next rung, change approach, or stop. A prototype that ends with let's see is a prototype that didn't finish. | How you'll know it succeeded<br>• Evidence bar met (or not)<br>• Failure modes documented<br>• A clear decision: climb, change, stop | Layout: A decision card with three buttons 'Climb', 'Change', 'Stop'; one lights up; beneath, a documented failure-mode list with four entries. Timing (28s): 0-3s 'How you'll know it succeeded' heading fades in; 3-22s elements build in narration order; 22-28s hold final frame. |
| 12 | 6:29 | 27 | One more practical point: speed of feedback. The best prototype teams shorten the loop between trying something and seeing whether it worked. That means a small, fixed set of real inputs you can re-run in minutes, graders who are available that week, and a place where results live side by side across prompt versions. If each iteration takes days, you'll stop iterating. | Keep the loop fast<br>• Small fixed input set<br>• Graders available this week<br>• Versions side by side | Layout: A loop arrow labelled 'try → run → grade → learn' spins faster; a spreadsheet shows columns v1, v2, v3 with scores side by side. Timing (27s): 0-3s 'Keep the loop fast' heading fades in; 3-21s elements build in narration order; 21-27s hold final frame. |
| 13 | 6:56 | 67 | Watch me do it. I open the evaluation runner. First, the system prompt loads from a versioned file, proposal draft v three, so every output records which prompt made it. Next, run one calls the API; on a rate-limit error it waits five, ten, twenty, then forty seconds before giving up, and on any other API error it records the status code instead of crashing. Then the main block reads inputs dot csv and writes outputs with columns for relevance, accuracy, failure mode and grader, left empty. I run it on thirty briefs. It takes a few minutes and produces outputs v three dot csv. I open it in a spreadsheet, share it with two senior staff, and ask each to grade the first twenty rows independently. We compare, agree on the rubric where we disagreed, and they grade the rest. Finally, I sort by failure mode, and wrong case study is the biggest group. | Watch me do it: evaluation runner<br>• Versioned prompt file<br>• Back-off on rate limits<br>• CSV with empty grade columns<br>• Two graders, then sort failures | Layout: code left, spreadsheet right. Highlight PROMPT_VERSION, the retry loop, then the CSV writer; the spreadsheet fills with outputs, two grader colours, then sorts by 'failure_mode'. Timing (67s): 0-3s 'Watch me do it: evaluation runner' heading fades in; 3-61s elements build in narration order; 61-67s hold final frame. |
| 14 | 8:03 | 37 | To recap: prototype to test feasibility, value, fit, risk and cost on real inputs. Climb the ladder one rung at a time with an evidence bar set in advance. Use real data safely, and keep the durable assets: prompt, evaluation set, failure modes and user evidence. Your next step is to run a chat prototype on fifteen real examples, record the failure modes, and write the threshold for moving to rung two. Next, we'll see how AI coding tools make rung three faster than ever. | Recap<br>• Ladder with evidence gates<br>• Durable assets over demos<br>• Next step: 15-example chat prototype | Layout: recap card; 'Next: Prototyping with AI coding tools' title card. Timing (37s): 0-3s 'Recap' heading fades in; 3-31s elements build in narration order; 31-37s hold final frame. |
| 15 | 8:40 | 29 | Try this now. Pick your scoped use case and collect fifteen real, safe examples. Run them through an approved AI assistant with your first prompt. For each, write one line: good, fixable, or failed, and why. Group the failures into two or three types, then write the evidence bar you'd need to see before moving to rung two. That's your prototype plan, done in under an hour. | Try this now (under an hour)<br>• 15 real examples, one prompt<br>• Label: good / fixable / failed<br>• Group failures; set the evidence bar | Layout: fifteen small cards arrange into three columns 'good', 'fixable', 'failed'; a sticky note 'Move to rung 2 if ≥ 70% good' appears. Timing (29s): 0-3s 'Try this now (under an hour)' heading fades in; 3-23s elements build in narration order; 23-29s hold final frame. |

<details><summary>Narration per scene (paste into ElevenLabs)</summary>

Scene 1 — Rapid prototyping

```text
AI features are strangely hard to judge on paper. A prompt that looks obviously right falls apart on real, messy inputs. A use case that sounds marginal turns out to be the thing everyone uses. So the cheapest way to make good AI product decisions is to prototype, fast, on real data. In this lesson you'll climb a four-rung prototyping ladder, learn what each rung should teach you, and run a spreadsheet evaluation that can kill or confirm an idea in days.
```

Scene 2 — Analogy: taste before you print the menu

```text
Why prototype before planning in detail? Here's an analogy. Chefs don't write a full menu and print it before tasting the dishes. They cook small batches, taste, adjust and only then commit. AI features are the same. A prompt that sounds perfect often tastes wrong on real inputs. Prototyping is tasting early, while changes are cheap.
```

Scene 3 — The prototyping ladder

```text
Here's the ladder. Rung one, a chat prototype, takes hours: try the task by hand in an AI assistant with ten to twenty real examples and refine the prompt. This alone kills or confirms many ideas. Rung two, prompt plus spreadsheet evaluation, takes days: run a fixed prompt over fifty to a hundred real inputs and have experts grade the outputs. Rung three, a clickable or no-code prototype, puts it in front of real users in context. Rung four is a pilot: a limited production version with logging and evaluation. Each rung costs roughly ten times the one below, so climb only when the lower rung shows promise.
```

Scene 4 — What each rung must answer

```text
At every rung you're answering five questions. Feasibility: is output quality good enough on real, messy inputs? Value: do users save time or produce better work, and do they want it? Workflow fit: where does it slot into existing tools? Risks: what failure modes appear, how often, and can you detect them? And cost signals: tokens per task, latency and review time. Write the evidence you need to move up before you start, for example at least seventy percent of drafts rated four or better by two senior reviewers.
```

Scene 5 — Real data, safely

```text
Prototypes only tell the truth with realistic inputs, but prototyping often happens before security reviews. Balance that. Use approved tools for any sensitive data. Anonymise or synthesise data where you can, while checking synthetic data isn't unrealistically clean. And start with public or low-sensitivity examples. Remember too that the most valuable outputs of prototyping aren't the demo. They're a tested, versioned prompt, an evaluation set of real inputs with expert grades, a documented list of failure modes, and real user feedback. All of that carries straight into the production build or the vendor evaluation.
```

Scene 6 — Simple example: interview notes

```text
A simple example of rung one. A recruitment agency wonders if AI can turn messy interview notes into a clean candidate summary. A recruiter pastes five real, anonymised sets of notes into an approved assistant with a draft prompt. Two summaries are great, two miss salary expectations, one invents a qualification. In twenty minutes they learn the prompt needs a field list and a rule about never adding facts. That's rung one doing its job.
```

Scene 7 — Worked example: proposal assistant

```text
Here's a worked example. An agency tests whether AI can draft proposal sections. In the chat prototype, five past briefs show drafts are generic unless the model gets case studies and service descriptions, so they build a context pack. In the spreadsheet evaluation, thirty past briefs run through the prompt and senior staff score relevance and accuracy. Most drafts are acceptable first drafts, but case-study selection often fails. A simple form prototype then gets heavy use for three weeks while they measure edit time. Decision: pilot, with retrieval over the case-study library to fix selection. Total spend before any engineering: modest.
```

Scene 8 — Business example (illustrative)

```text
Illustrative numbers for the proposal assistant. Thirty past briefs were scored by two senior staff: about seventy percent rated four or five for relevance, but case-study selection was wrong in roughly a third. In the three-week form prototype, account managers used it for most proposals and median drafting time for the approach section fell from about ninety minutes to about forty. That evidence, not the demo, got the pilot funded.
```

Scene 9 — Common mistakes

```text
Avoid four mistakes. Demoing on hand-picked easy examples. Testing only with the builders instead of real users. Treating a prototype as production, with no logging, security or evaluation. And falling in love with the demo while ignoring the evaluation data. Early rungs rarely need engineers. Product managers, analysts and domain experts can run chat prototypes and spreadsheet evaluations themselves, which keeps iteration fast and puts domain judgement at the centre.
```

Scene 10 — Hands-on in the lesson

```text
The hands-on section gives you a spreadsheet evaluation runner. It reads real inputs from a CSV, runs a versioned prompt through the Claude API with polite back-off on rate limits, and writes outputs to a new CSV with empty columns for relevance, accuracy, failure mode and grader. You'll also see when to use a batch API for big runs at lower cost, and grading tips: a written rubric, two graders on the first twenty rows, and short failure-mode labels that become your improvement backlog.
```

Scene 11 — How you'll know it succeeded

```text
How will you know a prototype phase succeeded? Not by how impressive the demo looked. By whether you hit the evidence bar you set in advance, whether you now have a documented list of failure modes, and whether you have a clear decision: climb to the next rung, change approach, or stop. A prototype that ends with let's see is a prototype that didn't finish.
```

Scene 12 — Keep the loop fast

```text
One more practical point: speed of feedback. The best prototype teams shorten the loop between trying something and seeing whether it worked. That means a small, fixed set of real inputs you can re-run in minutes, graders who are available that week, and a place where results live side by side across prompt versions. If each iteration takes days, you'll stop iterating.
```

Scene 13 — Watch me do it: evaluation runner

```text
Watch me do it. I open the evaluation runner. First, the system prompt loads from a versioned file, proposal draft v three, so every output records which prompt made it. Next, run one calls the API; on a rate-limit error it waits five, ten, twenty, then forty seconds before giving up, and on any other API error it records the status code instead of crashing. Then the main block reads inputs dot csv and writes outputs with columns for relevance, accuracy, failure mode and grader, left empty. I run it on thirty briefs. It takes a few minutes and produces outputs v three dot csv. I open it in a spreadsheet, share it with two senior staff, and ask each to grade the first twenty rows independently. We compare, agree on the rubric where we disagreed, and they grade the rest. Finally, I sort by failure mode, and wrong case study is the biggest group.
```

Scene 14 — Recap

```text
To recap: prototype to test feasibility, value, fit, risk and cost on real inputs. Climb the ladder one rung at a time with an evidence bar set in advance. Use real data safely, and keep the durable assets: prompt, evaluation set, failure modes and user evidence. Your next step is to run a chat prototype on fifteen real examples, record the failure modes, and write the threshold for moving to rung two. Next, we'll see how AI coding tools make rung three faster than ever.
```

Scene 15 — Try this now (under an hour)

```text
Try this now. Pick your scoped use case and collect fifteen real, safe examples. Run them through an approved AI assistant with your first prompt. For each, write one line: good, fixable, or failed, and why. Group the failures into two or three types, then write the evidence bar you'd need to see before moving to rung two. That's your prototype plan, done in under an hour.
```

</details>

### 4. Prototyping with AI coding tools and app builders

Lesson `prototyping-with-ai-coding-tools` · module “Prototyping and build vs buy” · target 9 min · 15 scenes · 1277 words · planned 9:15 (words ≈ 9:15) · status: **to produce**

| Term | Say |
|---|---|
| AGENTS.md | agents dot M D |
| SSO | S S O |
| v0 | vee zero |

| # | Time | Sec | Narration | On screen | Visual |
|---|---|---|---|---|---|
| 1 | 0:00 | 36 | Two years ago, putting a clickable AI prototype in front of real users meant waiting weeks for engineering time. Today, an operations lead can describe a tool in plain English and have a working version by the afternoon. That's wonderful for learning, and dangerous if nobody notices the prototype quietly became a production system with no login and client data in someone's personal account. In this lesson you'll learn to prototype fast with AI coding tools and app builders, without shipping accidents. | Prototyping with AI coding tools<br>• Rung 3 is now cheap<br>• Spec first<br>• A productionisation gate | Layout: A split timeline: 'then' shows a calendar with weeks crossed off waiting for engineering; 'now' shows a clock reading a few hours and a working app mock-up. Timing (36s): 0-3s 'Prototyping with AI coding tools' heading fades in; 3-30s elements build in narration order; 30-36s hold final frame. |
| 2 | 0:36 | 27 | Here's an analogy. AI coding tools are like a brilliant, very fast contractor who takes every instruction literally and never asks, are you sure? Ask for a shed and you'll get a shed by lunchtime, including whatever they assumed about the foundations, the wiring and who holds the keys. The spec is where you tell them about the foundations and the keys. | Analogy: the fast, literal contractor<br>• Builds exactly what you say<br>• Fills gaps with assumptions<br>• The spec covers foundations and keys | Layout: A contractor robot builds a shed in fast-forward; X-ray overlay reveals questionable wiring and a key left under the doormat; a spec document then corrects both. Timing (27s): 0-3s 'Analogy: the fast, literal contractor' heading fades in; 3-21s elements build in narration order; 21-27s hold final frame. |
| 3 | 1:03 | 43 | There are two families of tools. AI coding agents, such as Claude Code, OpenAI Codex, GitHub Copilot's agent features and Cursor, work inside a code repository: they read files, run commands and make changes you review. Prompt-to-app builders, like v0, Lovable, Bolt and Replit's agent, generate and host a whole app from a description. Pick by need. A clickable UI to test a workflow suits a builder. An internal tool against real APIs suits a coding agent in your own repository. Integration-heavy automation suits no-code workflow tools. And testing the AI behaviour itself still belongs in scripts and spreadsheets. | Two tool families<br>• AI coding agents in your repo<br>• Prompt-to-app builders<br>• Pick by need; evaluate AI separately | Layout: two panels: a code editor with an agent sidebar proposing a diff; a browser-based builder turning a text prompt into an app preview. Timing (43s): 0-3s 'Two tool families' heading fades in; 3-37s elements build in narration order; 37-43s hold final frame. |
| 4 | 1:46 | 44 | These tools do exactly what you describe, so the spec is everything. Write one page: the goal, the users, must-haves, must-nots, the success signal you'll judge it by, and what's out of scope. Must-nots are where the value hides: no public URL, company sign-in required, no API keys in the browser, client data retained for thirty days only. Then ask the tool to propose a plan before writing any code, review it, and let it build in small steps you can test. In a repository, keep an AGENTS dot M D or similar instruction file with commands, conventions and never-do rules. | Spec first<br>• Goal, users, must-haves, must-nots<br>• Success signal, out of scope<br>• Plan before code; small tested steps | Layout: A one-page spec document with the 'Must not' section highlighted; then a chat where the tool returns a numbered plan and the user edits one step. Timing (44s): 0-3s 'Spec first' heading fades in; 3-38s elements build in narration order; 38-44s hold final frame. |
| 5 | 2:30 | 45 | Here's the trap. A prototype that looks production-ready often has no authentication, secrets in the code, no logging, no tests and a data model nobody designed. Builders' defaults may decide where your client data lives. And personal accounts end up owning business-critical tools. So treat everything as a throwaway until it passes a gate. Useful prompts help: ask the tool to review the codebase as a security engineer and list every place a secret or personal data could leak, quoting file and line, without fixing anything yet. Then ask it to write tests for the main journey, including timeouts, empty input and Arabic text. | The hidden risks<br>• No auth, exposed secrets, no logs<br>• Builder defaults decide data location<br>• Personal accounts own business tools | Layout: A polished app screen; an X-ray overlay reveals a hard-coded key, a public URL badge and a database icon labelled 'hosted by builder'. Timing (45s): 0-3s 'The hidden risks' heading fades in; 3-39s elements build in narration order; 39-45s hold final frame. |
| 6 | 3:15 | 44 | The productionisation gate is a one-page checklist. Is the success signal met, with data? Has the AI behaviour been evaluated on at least fifty real cases? Is access limited with company sign-in? Are there no secrets in code or browser bundles? Has data classification been checked and retention set? Is every AI call logged with IDs, versions, latency and cost? Do provider failures show a clear fallback? Has an engineer read the code, with tests for the core path? Is there a named owner and a kill switch? Is it in the AI register? Then decide: pilot as-is, fix first, or rebuild. | The gate<br>• Evidence + AI evaluation<br>• Auth, secrets, data, logging, errors<br>• Code review, ownership, register<br>• Decide: pilot / fix / rebuild | Layout: A checklist card with items ticking green; two items turn red; a decision stamp swings to 'Fix first'. Timing (44s): 0-3s 'The gate' heading fades in; 3-38s elements build in narration order; 38-44s hold final frame. |
| 7 | 3:59 | 32 | A simple example. A marketing manager asks a prompt-to-app builder for a tool that turns a product URL into three ad headlines. It works in minutes. But checking the result shows the builder stored the API key in the browser code, where anyone can see it. The fix is a spec line, no keys in the frontend, and asking the tool to move the call to a server function. Small line, big difference. | Simple example: a headline generator<br>• Works in minutes<br>• API key exposed in the browser<br>• One spec line fixes it | Layout: A mini app mock-up generating three headlines; a browser dev-tools panel slides open revealing a highlighted key string; a spec line 'No API keys in the frontend' stamps over it. Timing (32s): 0-3s 'Simple example: a headline generator' heading fades in; 3-26s elements build in narration order; 26-32s hold final frame. |
| 8 | 4:31 | 44 | Here's how it plays out. An agency operations lead in Karachi, not an engineer, builds a campaign-brief intake tool in an afternoon with an app builder. Twelve account managers use it for two weeks, and adoption is strong. The gate then finds the app stores full briefs, including client budgets, in a builder-hosted database with a public URL. The team keeps what they learned, the UX and the prompts, and an engineer rebuilds the backend in the agency's own environment with sign-in and thirty-day retention in three days, using a coding agent guided by the original spec. Fast learning, contained risk. | Worked example: intake tool<br>• Built in an afternoon; strong adoption<br>• Gate finds public URL + stored budgets<br>• Keep UX and prompts; rebuild backend in 3 days | Layout: A small office scene; an app goes live; a warning banner 'public URL + stored budgets'; then a rebuilt architecture diagram with SSO and a retention clock. Timing (44s): 0-3s 'Worked example: intake tool' heading fades in; 3-38s elements build in narration order; 38-44s hold final frame. |
| 9 | 5:15 | 33 | Illustrative numbers for the Karachi intake tool. The first version was built in about four hours and used by twelve account managers for over two hundred briefs in two weeks. The gate found seven issues, two serious. The rebuild took three days with a coding agent guided by the original spec, kept the UX unchanged so nobody had to relearn it, and added sign-in and thirty-day retention. Total cost was a fraction of a traditional build. | Business example (illustrative)<br>• Built in ~4 hours; 200+ briefs<br>• Gate: 7 issues, 2 serious<br>• Rebuild in 3 days, same UX | Layout: a timeline with 'build 4h', 'use 2 weeks', 'gate', 'rebuild 3 days'; the gate step shows 7 issue dots, 2 red; the UX thumbnail stays identical before and after. Timing (33s): 0-3s 'Business example (illustrative)' heading fades in; 3-27s elements build in narration order; 27-33s hold final frame. |
| 10 | 5:48 | 25 | Avoid four pitfalls. Letting a builder's defaults decide where client data lives. Personal accounts owning prototypes that become critical. Skipping the spreadsheet evaluation because the interface feels finished. And endless polishing: a prototype's job is evidence, not beauty. The hands-on section gives you a spec template, the full gate checklist and the two review prompts, ready to copy. | Pitfalls + hands-on<br>• Defaults, personal accounts<br>• Skipped evaluation, endless polish<br>• Spec template, gate, review prompts | Layout: four pitfall icons; then the spec template, gate checklist and prompt cards slide into a folder labelled 'Hands-on'. Timing (25s): 0-3s 'Pitfalls + hands-on' heading fades in; 3-19s elements build in narration order; 19-25s hold final frame. |
| 11 | 6:13 | 27 | How will you know this approach is working for your team? Prototypes reach real users in days, not weeks. Every prototype has a spec, an owner and a gate decision on record. None of them hold client data outside approved systems. And the lessons, the UX that worked and the prompts, flow into production builds instead of being thrown away with the prototype. | How you'll know it's working<br>• Days to real users<br>• Spec, owner, gate decision for each<br>• No data outside approved systems<br>• Learning carried into production | Layout: A small portfolio board of prototype cards, each with 'spec', 'owner', 'gate: pilot/fix/rebuild' tags; arrows carry 'UX' and 'prompts' labels into a 'production' box. Timing (27s): 0-3s 'How you'll know it's working' heading fades in; 3-21s elements build in narration order; 21-27s hold final frame. |
| 12 | 6:40 | 25 | A quick word on ownership. Prototypes built in personal accounts on free tiers are the most common way small companies lose control of client data and working tools. Create business accounts for the tools your team uses, add at least two admins, and set a rule that any prototype touching business data lives there from day one. | Ownership rule<br>• Business accounts, not personal<br>• At least two admins<br>• Business data → business accounts only | Layout: two account cards: 'personal free account' fades out with a padlock-broken icon; 'business workspace' with two admin avatars and a shield appears. Timing (25s): 0-3s 'Ownership rule' heading fades in; 3-19s elements build in narration order; 19-25s hold final frame. |
| 13 | 7:05 | 67 | Watch me do it. I open the prototype spec. Goal: account managers paste a brief and get an approach section. Must have: the form, a call to our draft endpoint, three case studies shown, copy to doc, feedback and logging. Must not: no stored client data beyond the log, no public URL, no keys in the frontend. Success signal: sixty percent use in weeks two and three. I give it to a coding agent and ask for a plan first. The plan proposes storing briefs in a database for history, which breaks a must-not, so I strike it. It builds in small steps; after each, I test. Then I run the security review prompt. It reports one issue: an error message echoes the full brief. I ask it to fix that and write tests for timeout, empty brief and an Arabic brief. Finally, I fill in the gate: two items need fixing, then pilot. | Watch me do it: spec → plan → gate<br>• Must-nots in the spec<br>• Strike the plan step that breaks one<br>• Security review prompt<br>• Gate: fix two, then pilot | Layout: spec document left, agent plan centre, gate checklist right. A plan line is struck through in red; a review finding card appears; two gate items turn from red to green. Timing (67s): 0-3s 'Watch me do it: spec → plan → gate' heading fades in; 3-61s elements build in narration order; 61-67s hold final frame. |
| 14 | 8:12 | 32 | To recap: AI coding tools and app builders make in-context prototypes cheap. Choose by need, write a one-page spec with must-nots and a success signal, build in small reviewed steps, and pass a productionisation gate before real users or data. Your next step is to write a prototype spec for your top use case, build the thinnest version, and run the gate on it. Next, we'll decide whether to build, buy or blend. | Recap<br>• Spec → small steps → gate<br>• Evidence, not beauty<br>• Next step: spec, build thin, run the gate | Layout: recap card; 'Next: Build vs buy' title card. Timing (32s): 0-3s 'Recap' heading fades in; 3-26s elements build in narration order; 26-32s hold final frame. |
| 15 | 8:44 | 31 | Try this now. Write a one-page spec for your top use case using the template, with at least three must-nots. Give it to an AI coding tool or app builder and ask for a plan first, not code. Review the plan against your must-nots. Then build the thinnest version and run the productionisation gate on it, noting every no. Even if you stop there, you'll know exactly what a production version needs. | Try this now (1–2 hours)<br>• One-page spec with 3 must-nots<br>• Ask for a plan first<br>• Build thin; run the gate | Layout: A spec document, then a numbered plan returned by a tool, then a checklist gate with several ticks and two red 'no' items highlighted. Timing (31s): 0-3s 'Try this now (1–2 hours)' heading fades in; 3-25s elements build in narration order; 25-31s hold final frame. |

<details><summary>Narration per scene (paste into ElevenLabs)</summary>

Scene 1 — Prototyping with AI coding tools

```text
Two years ago, putting a clickable AI prototype in front of real users meant waiting weeks for engineering time. Today, an operations lead can describe a tool in plain English and have a working version by the afternoon. That's wonderful for learning, and dangerous if nobody notices the prototype quietly became a production system with no login and client data in someone's personal account. In this lesson you'll learn to prototype fast with AI coding tools and app builders, without shipping accidents.
```

Scene 2 — Analogy: the fast, literal contractor

```text
Here's an analogy. AI coding tools are like a brilliant, very fast contractor who takes every instruction literally and never asks, are you sure? Ask for a shed and you'll get a shed by lunchtime, including whatever they assumed about the foundations, the wiring and who holds the keys. The spec is where you tell them about the foundations and the keys.
```

Scene 3 — Two tool families

```text
There are two families of tools. AI coding agents, such as Claude Code, OpenAI Codex, GitHub Copilot's agent features and Cursor, work inside a code repository: they read files, run commands and make changes you review. Prompt-to-app builders, like v0, Lovable, Bolt and Replit's agent, generate and host a whole app from a description. Pick by need. A clickable UI to test a workflow suits a builder. An internal tool against real APIs suits a coding agent in your own repository. Integration-heavy automation suits no-code workflow tools. And testing the AI behaviour itself still belongs in scripts and spreadsheets.
```

Scene 4 — Spec first

```text
These tools do exactly what you describe, so the spec is everything. Write one page: the goal, the users, must-haves, must-nots, the success signal you'll judge it by, and what's out of scope. Must-nots are where the value hides: no public URL, company sign-in required, no API keys in the browser, client data retained for thirty days only. Then ask the tool to propose a plan before writing any code, review it, and let it build in small steps you can test. In a repository, keep an AGENTS dot M D or similar instruction file with commands, conventions and never-do rules.
```

Scene 5 — The hidden risks

```text
Here's the trap. A prototype that looks production-ready often has no authentication, secrets in the code, no logging, no tests and a data model nobody designed. Builders' defaults may decide where your client data lives. And personal accounts end up owning business-critical tools. So treat everything as a throwaway until it passes a gate. Useful prompts help: ask the tool to review the codebase as a security engineer and list every place a secret or personal data could leak, quoting file and line, without fixing anything yet. Then ask it to write tests for the main journey, including timeouts, empty input and Arabic text.
```

Scene 6 — The gate

```text
The productionisation gate is a one-page checklist. Is the success signal met, with data? Has the AI behaviour been evaluated on at least fifty real cases? Is access limited with company sign-in? Are there no secrets in code or browser bundles? Has data classification been checked and retention set? Is every AI call logged with IDs, versions, latency and cost? Do provider failures show a clear fallback? Has an engineer read the code, with tests for the core path? Is there a named owner and a kill switch? Is it in the AI register? Then decide: pilot as-is, fix first, or rebuild.
```

Scene 7 — Simple example: a headline generator

```text
A simple example. A marketing manager asks a prompt-to-app builder for a tool that turns a product URL into three ad headlines. It works in minutes. But checking the result shows the builder stored the API key in the browser code, where anyone can see it. The fix is a spec line, no keys in the frontend, and asking the tool to move the call to a server function. Small line, big difference.
```

Scene 8 — Worked example: intake tool

```text
Here's how it plays out. An agency operations lead in Karachi, not an engineer, builds a campaign-brief intake tool in an afternoon with an app builder. Twelve account managers use it for two weeks, and adoption is strong. The gate then finds the app stores full briefs, including client budgets, in a builder-hosted database with a public URL. The team keeps what they learned, the UX and the prompts, and an engineer rebuilds the backend in the agency's own environment with sign-in and thirty-day retention in three days, using a coding agent guided by the original spec. Fast learning, contained risk.
```

Scene 9 — Business example (illustrative)

```text
Illustrative numbers for the Karachi intake tool. The first version was built in about four hours and used by twelve account managers for over two hundred briefs in two weeks. The gate found seven issues, two serious. The rebuild took three days with a coding agent guided by the original spec, kept the UX unchanged so nobody had to relearn it, and added sign-in and thirty-day retention. Total cost was a fraction of a traditional build.
```

Scene 10 — Pitfalls + hands-on

```text
Avoid four pitfalls. Letting a builder's defaults decide where client data lives. Personal accounts owning prototypes that become critical. Skipping the spreadsheet evaluation because the interface feels finished. And endless polishing: a prototype's job is evidence, not beauty. The hands-on section gives you a spec template, the full gate checklist and the two review prompts, ready to copy.
```

Scene 11 — How you'll know it's working

```text
How will you know this approach is working for your team? Prototypes reach real users in days, not weeks. Every prototype has a spec, an owner and a gate decision on record. None of them hold client data outside approved systems. And the lessons, the UX that worked and the prompts, flow into production builds instead of being thrown away with the prototype.
```

Scene 12 — Ownership rule

```text
A quick word on ownership. Prototypes built in personal accounts on free tiers are the most common way small companies lose control of client data and working tools. Create business accounts for the tools your team uses, add at least two admins, and set a rule that any prototype touching business data lives there from day one.
```

Scene 13 — Watch me do it: spec → plan → gate

```text
Watch me do it. I open the prototype spec. Goal: account managers paste a brief and get an approach section. Must have: the form, a call to our draft endpoint, three case studies shown, copy to doc, feedback and logging. Must not: no stored client data beyond the log, no public URL, no keys in the frontend. Success signal: sixty percent use in weeks two and three. I give it to a coding agent and ask for a plan first. The plan proposes storing briefs in a database for history, which breaks a must-not, so I strike it. It builds in small steps; after each, I test. Then I run the security review prompt. It reports one issue: an error message echoes the full brief. I ask it to fix that and write tests for timeout, empty brief and an Arabic brief. Finally, I fill in the gate: two items need fixing, then pilot.
```

Scene 14 — Recap

```text
To recap: AI coding tools and app builders make in-context prototypes cheap. Choose by need, write a one-page spec with must-nots and a success signal, build in small reviewed steps, and pass a productionisation gate before real users or data. Your next step is to write a prototype spec for your top use case, build the thinnest version, and run the gate on it. Next, we'll decide whether to build, buy or blend.
```

Scene 15 — Try this now (1–2 hours)

```text
Try this now. Write a one-page spec for your top use case using the template, with at least three must-nots. Give it to an AI coding tool or app builder and ask for a plan first, not code. Review the plan against your must-nots. Then build the thinnest version and run the productionisation gate on it, noting every no. Even if you stop there, you'll know exactly what a production version needs.
```

</details>

### 5. Build vs buy (and blend)

Lesson `build-vs-buy` · module “Prototyping and build vs buy” · target 9 min · 15 scenes · 1193 words · planned 8:39 (words ≈ 8:39) · status: **to produce**

| Term | Say |
|---|---|
| SSO | S S O |
| MCP | M C P |

| # | Time | Sec | Narration | On screen | Visual |
|---|---|---|---|---|---|
| 1 | 0:00 | 37 | Every AI feature eventually hits the same fork in the road. Do we use the AI already inside our tools, buy a specialised product, build our own on model APIs, or mix them? Choose wrong, and you either spend a year building a commodity, or lock your most differentiating process inside someone else's roadmap. In this lesson you'll learn the four paths, the criteria that decide between them, the hidden costs on both sides, and how to run a vendor bake-off on your own data. | Build, buy or blend<br>• Four paths<br>• Decision criteria<br>• Hidden costs<br>• Vendor bake-off | Layout: A road splitting into four labelled lanes: 'AI in existing tools', 'Specialised product', 'Build on APIs', 'Blend'. A small car pauses at the junction. Timing (37s): 0-3s 'Build, buy or blend' heading fades in; 3-31s elements build in narration order; 31-37s hold final frame. |
| 2 | 0:37 | 29 | Here's an analogy. Build versus buy is like deciding whether to cook, order in, or do both. For your signature dish, the one guests come for, you cook it yourself. For bread, you buy from a good bakery. Nobody bakes their own bread just to prove they can. Your differentiating workflows are the signature dish. Commodity features, like meeting notes or basic FAQs, are the bread. | Analogy: cook, order in, or both<br>• Signature dish = differentiating workflow<br>• Bread = commodity features | Layout: A kitchen scene: a chef cooking a signature dish labelled 'our pricing logic'; a bakery bag labelled 'meeting notes' arriving by delivery; a plate combining both. Timing (29s): 0-3s 'Analogy: cook, order in, or both' heading fades in; 3-23s elements build in narration order; 23-29s hold final frame. |
| 3 | 1:06 | 33 | Path one: use AI features in tools you already have. Office suites, CRMs, help desks and analytics platforms now ship them. Path two: buy a specialised AI product for a specific job, like contract review, support automation or meeting notes. Path three: build on model APIs, combining models, retrieval, tools and your own interface. Path four: blend. Buy the commodity parts, build the differentiating parts, and connect them through integrations, increasingly through open standards like MCP. | Four paths<br>1 AI in existing tools<br>2 Specialised product<br>3 Build on model APIs<br>4 Blend via integrations and MCP | Layout: four tiles with simple icons: a familiar app window with a sparkle, a boxed product, building blocks, and puzzle pieces connected by a plug labelled MCP. Timing (33s): 0-3s 'Four paths' heading fades in; 3-27s elements build in narration order; 27-33s hold final frame. |
| 4 | 1:39 | 40 | How do you decide? Ask whether the capability is a differentiator for you or table stakes. Whether a good product exists for your exact need. How specific your data and workflows are. Whether you have engineering and AI operations capacity. How fast you need value. How much control you need over data, models and roadmap. And what the unit economics look like at your volume, because per-seat pricing can become expensive at scale. No single question decides it. Weigh them together, and agree the weights before you score, so nobody reverse-engineers the answer. | Decision criteria<br>• Differentiator or table stakes?<br>• Product fit, data specificity<br>• Team capacity, time to value<br>• Control, unit economics | Layout: A decision table animates row by row with arrows leaning left ('buy') or right ('build'); a weights panel locks before scores appear. Timing (40s): 0-3s 'Decision criteria' heading fades in; 3-34s elements build in narration order; 34-40s hold final frame. |
| 5 | 2:19 | 33 | Both sides hide costs. Building means evaluation, monitoring and maintenance that never stop, keeping up with model changes, security and compliance work, UX work to make outputs trustworthy, and on-call ownership. The first prototype is a small fraction of total cost. Buying means integration and data plumbing, pricing that grows with adoption, workflows bending to the tool, vendor roadmap and pricing changes, data terms to scrutinise, and lock-in that makes exporting prompts, data and configurations hard. | Hidden costs<br>• Build: evals, maintenance, model churn, on-call<br>• Buy: integration, usage pricing, lock-in, data terms | Layout: two icebergs side by side labelled Build and Buy; the visible tips are small ('prototype', 'licence'), while the large underwater parts list the hidden costs. Timing (33s): 0-3s 'Hidden costs' heading fades in; 3-27s elements build in narration order; 27-33s hold final frame. |
| 6 | 2:52 | 47 | Evaluate vendors like a benchmark, not a demo. Run your own evaluation set through a trial. Check security and privacy: certifications, data residency, retention, whether data trains models, and sub-processors. Check controls like single sign-on, roles and audit logs. Ask which underlying models they use and how they announce changes. Examine pricing, caps and exit terms. And check integrations and support in your regions and languages. Here's a real-world shaped case. A regional e-commerce company tested its help desk's built-in AI agent against a custom build on two hundred real tickets. The built-in agent handled common English intents well, but struggled with Arabic tone and their custom order data. | Evaluate vendors on your data<br>• Trial with your eval set<br>• Security, privacy, controls<br>• Model transparency, commercials, fit | Layout: A bake-off scoreboard with two contestants and 200 ticket icons flowing through each; results split by language and intent, showing Arabic and order-data rows in amber for the built-in agent. Timing (47s): 0-3s 'Evaluate vendors on your data' heading fades in; 3-41s elements build in narration order; 41-47s hold final frame. |
| 7 | 3:39 | 27 | A simple example. A ten-person accounting firm wants AI meeting notes. Their video-call platform already includes AI summaries, their clients accept it, and the data terms are fine. Building their own would take weeks and deliver nothing better. Decision: use the built-in feature, and spend the saved time on something that actually differentiates them, like drafting client tax-planning memos from their own templates. | Simple example: meeting notes<br>• Built-in feature is good enough<br>• Data terms acceptable<br>• Save effort for differentiating work | Layout: A meeting app window with a built-in 'AI summary' panel; a crossed-out 'build our own' plan; an arrow redirecting effort to a 'tax-planning memo' card. Timing (27s): 0-3s 'Simple example: meeting notes' heading fades in; 3-21s elements build in narration order; 21-27s hold final frame. |
| 8 | 4:06 | 38 | Their decision? Blend. The help desk's AI handles triage and English FAQs. The company builds a small service that exposes order data as tools, which the help desk's AI can call, because the vendor supports external tool integrations. Differentiating logic stays in-house, commodity parts are bought. And they design for replaceability: prompts, evaluation sets, retrieval indexes and business logic live in assets they own, connected through standard interfaces, so they can switch components when something better appears. Revisit the decision at every renewal and major model shift. | Worked example: blend<br>• Buy: triage + English FAQs<br>• Build: order-data tools<br>• Own the assets; revisit at renewal | Layout: architecture diagram: a vendor help-desk box calling a company-owned 'order tools' service; a folder of owned assets (prompts, evals, indexes) sits beneath with a lock icon. Timing (38s): 0-3s 'Worked example: blend' heading fades in; 3-32s elements build in narration order; 32-38s hold final frame. |
| 9 | 4:44 | 31 | Illustrative numbers for the e-commerce blend. On two hundred real tickets, the built-in agent resolved about seventy percent of English FAQs well, but only about forty percent of Arabic and order-specific tickets. The blend, with order tools the help desk's AI can call, lifted the order-specific group to about seventy-five percent. Building only the order tools took two engineers about three weeks, versus the months a full custom assistant would have needed. | Business example (illustrative)<br>• Built-in: ~70% English FAQs, ~40% Arabic/order<br>• Blend: order tickets ~75%<br>• ~3 weeks vs months for full build | Layout: grouped bar chart with three ticket groups and two bars each ('built-in', 'blend'); a small calendar comparing '3 weeks' with 'months'. Timing (31s): 0-3s 'Business example (illustrative)' heading fades in; 3-25s elements build in narration order; 25-31s hold final frame. |
| 10 | 5:15 | 30 | In the hands-on section you'll find a weighted decision matrix in a few lines of Python, where the quality row must come from your own evaluation, a step-by-step vendor bake-off on one to two hundred real cases, and a nine-question due-diligence questionnaire covering model changes, training on your data, retention, residency in places like the UK, EU, UAE and KSA, certifications, admin controls, integrations including MCP, commercials and evidence. | Hands-on in the lesson<br>• Weighted decision matrix<br>• Vendor bake-off on real cases<br>• 9-question due diligence | Layout: terminal printing three option scores with 'Blend' on top; then a questionnaire document with nine numbered questions. Timing (30s): 0-3s 'Hands-on in the lesson' heading fades in; 3-24s elements build in narration order; 24-30s hold final frame. |
| 11 | 5:45 | 28 | Common mistakes. Deciding from a sales demo instead of your own evaluation set. Comparing a vendor's price with only your developers' time, ignoring maintenance. Ignoring exit terms until you want to leave. Building because it's more interesting. And forgetting the blend option, which is often the right answer: buy the commodity, build the parts that make you different, and connect them through open standards. | Common mistakes<br>• Demos over your data<br>• Ignoring maintenance cost<br>• Exit terms read too late<br>• Building for fun<br>• Forgetting 'blend' | Layout: five small icons with red crosses: a shiny demo screen, a partial cost bar, a locked exit door, a toy robot, and a missing puzzle piece labelled 'blend'. Timing (28s): 0-3s 'Common mistakes' heading fades in; 3-22s elements build in narration order; 22-28s hold final frame. |
| 12 | 6:13 | 30 | How will you know you decided well? Twelve months later, the bought parts are quietly doing their job without surprise costs, the built parts are where you've invested your improvements, and you could swap a vendor in weeks if you had to, because your prompts, evaluation sets and business logic live in assets you own. If any of that isn't true, revisit the decision at the next renewal. | How you'll know you decided well<br>• No surprise costs<br>• Investment goes into what you built<br>• Vendor swap possible in weeks | Layout: A 12-month timeline with checkpoints; a vendor card being unplugged and replaced by another while a folder of owned assets stays locked in place. Timing (30s): 0-3s 'How you'll know you decided well' heading fades in; 3-24s elements build in narration order; 24-30s hold final frame. |
| 13 | 6:43 | 57 | Watch me do it. I open the decision matrix. First, the weights: quality on our evaluation gets a quarter, differentiation fit a fifth, data control fifteen percent, and the rest shared. I agree these with the operations director before scoring anything. Next, the scores for three options. The quality scores come from our two-hundred-ticket bake-off, not demos. I run it, and the blend scores highest, then build, then the built-in agent. Then I open the due diligence questionnaire and send it to the help desk vendor. Two answers come back vague: whether we can pin model versions, and the exact sub-processor list. I ask again in writing and note both in the decision record. Finally, I write the recommendation: blend, with three reasons, and a renewal date when we'll revisit it. | Watch me do it: matrix + due diligence<br>• Weights agreed first<br>• Quality from the bake-off<br>• Run → blend wins<br>• Vague answers chased in writing | Layout: weights table left locking with a padlock icon; option scores right; terminal prints ranked options; an email thread panel highlights two vague answers. Timing (57s): 0-3s 'Watch me do it: matrix + due diligence' heading fades in; 3-51s elements build in narration order; 51-57s hold final frame. |
| 14 | 7:40 | 32 | To recap: there are four paths: existing tools, specialised products, building on APIs, or blending. Weigh differentiation, fit, data specificity, capacity, time, control and economics. Count the hidden costs on both sides. Evaluate vendors on your own data, and design for replaceability. Your next step is to fill in the decision matrix for one use case and recommend build, buy or blend with three reasons. Next module: choosing models and designing the architecture. | Recap<br>• Four paths, weighed criteria<br>• Your data decides, not demos<br>• Next step: fill the matrix for one use case | Layout: recap card; 'Next module: Architecture and orchestration' title card. Timing (32s): 0-3s 'Recap' heading fades in; 3-26s elements build in narration order; 26-32s hold final frame. |
| 15 | 8:12 | 27 | Try this now. Pick one AI feature you're considering. Fill in the weighted decision matrix for three options: existing tool, specialised product, and build or blend. Leave the quality row empty until you've run at least twenty real cases through each option you can access. Then send the nine due-diligence questions to your top vendor and note which answers are vague. | Try this now (1 hour + vendor reply)<br>• Score three options<br>• Quality from 20 real cases<br>• Send the 9 due-diligence questions | Layout: A decision matrix with the quality row greyed out until a '20 cases run' badge appears; an email icon sends a numbered questionnaire. Timing (27s): 0-3s 'Try this now (1 hour + vendor reply)' heading fades in; 3-21s elements build in narration order; 21-27s hold final frame. |

<details><summary>Narration per scene (paste into ElevenLabs)</summary>

Scene 1 — Build, buy or blend

```text
Every AI feature eventually hits the same fork in the road. Do we use the AI already inside our tools, buy a specialised product, build our own on model APIs, or mix them? Choose wrong, and you either spend a year building a commodity, or lock your most differentiating process inside someone else's roadmap. In this lesson you'll learn the four paths, the criteria that decide between them, the hidden costs on both sides, and how to run a vendor bake-off on your own data.
```

Scene 2 — Analogy: cook, order in, or both

```text
Here's an analogy. Build versus buy is like deciding whether to cook, order in, or do both. For your signature dish, the one guests come for, you cook it yourself. For bread, you buy from a good bakery. Nobody bakes their own bread just to prove they can. Your differentiating workflows are the signature dish. Commodity features, like meeting notes or basic FAQs, are the bread.
```

Scene 3 — Four paths

```text
Path one: use AI features in tools you already have. Office suites, CRMs, help desks and analytics platforms now ship them. Path two: buy a specialised AI product for a specific job, like contract review, support automation or meeting notes. Path three: build on model APIs, combining models, retrieval, tools and your own interface. Path four: blend. Buy the commodity parts, build the differentiating parts, and connect them through integrations, increasingly through open standards like MCP.
```

Scene 4 — Decision criteria

```text
How do you decide? Ask whether the capability is a differentiator for you or table stakes. Whether a good product exists for your exact need. How specific your data and workflows are. Whether you have engineering and AI operations capacity. How fast you need value. How much control you need over data, models and roadmap. And what the unit economics look like at your volume, because per-seat pricing can become expensive at scale. No single question decides it. Weigh them together, and agree the weights before you score, so nobody reverse-engineers the answer.
```

Scene 5 — Hidden costs

```text
Both sides hide costs. Building means evaluation, monitoring and maintenance that never stop, keeping up with model changes, security and compliance work, UX work to make outputs trustworthy, and on-call ownership. The first prototype is a small fraction of total cost. Buying means integration and data plumbing, pricing that grows with adoption, workflows bending to the tool, vendor roadmap and pricing changes, data terms to scrutinise, and lock-in that makes exporting prompts, data and configurations hard.
```

Scene 6 — Evaluate vendors on your data

```text
Evaluate vendors like a benchmark, not a demo. Run your own evaluation set through a trial. Check security and privacy: certifications, data residency, retention, whether data trains models, and sub-processors. Check controls like single sign-on, roles and audit logs. Ask which underlying models they use and how they announce changes. Examine pricing, caps and exit terms. And check integrations and support in your regions and languages. Here's a real-world shaped case. A regional e-commerce company tested its help desk's built-in AI agent against a custom build on two hundred real tickets. The built-in agent handled common English intents well, but struggled with Arabic tone and their custom order data.
```

Scene 7 — Simple example: meeting notes

```text
A simple example. A ten-person accounting firm wants AI meeting notes. Their video-call platform already includes AI summaries, their clients accept it, and the data terms are fine. Building their own would take weeks and deliver nothing better. Decision: use the built-in feature, and spend the saved time on something that actually differentiates them, like drafting client tax-planning memos from their own templates.
```

Scene 8 — Worked example: blend

```text
Their decision? Blend. The help desk's AI handles triage and English FAQs. The company builds a small service that exposes order data as tools, which the help desk's AI can call, because the vendor supports external tool integrations. Differentiating logic stays in-house, commodity parts are bought. And they design for replaceability: prompts, evaluation sets, retrieval indexes and business logic live in assets they own, connected through standard interfaces, so they can switch components when something better appears. Revisit the decision at every renewal and major model shift.
```

Scene 9 — Business example (illustrative)

```text
Illustrative numbers for the e-commerce blend. On two hundred real tickets, the built-in agent resolved about seventy percent of English FAQs well, but only about forty percent of Arabic and order-specific tickets. The blend, with order tools the help desk's AI can call, lifted the order-specific group to about seventy-five percent. Building only the order tools took two engineers about three weeks, versus the months a full custom assistant would have needed.
```

Scene 10 — Hands-on in the lesson

```text
In the hands-on section you'll find a weighted decision matrix in a few lines of Python, where the quality row must come from your own evaluation, a step-by-step vendor bake-off on one to two hundred real cases, and a nine-question due-diligence questionnaire covering model changes, training on your data, retention, residency in places like the UK, EU, UAE and KSA, certifications, admin controls, integrations including MCP, commercials and evidence.
```

Scene 11 — Common mistakes

```text
Common mistakes. Deciding from a sales demo instead of your own evaluation set. Comparing a vendor's price with only your developers' time, ignoring maintenance. Ignoring exit terms until you want to leave. Building because it's more interesting. And forgetting the blend option, which is often the right answer: buy the commodity, build the parts that make you different, and connect them through open standards.
```

Scene 12 — How you'll know you decided well

```text
How will you know you decided well? Twelve months later, the bought parts are quietly doing their job without surprise costs, the built parts are where you've invested your improvements, and you could swap a vendor in weeks if you had to, because your prompts, evaluation sets and business logic live in assets you own. If any of that isn't true, revisit the decision at the next renewal.
```

Scene 13 — Watch me do it: matrix + due diligence

```text
Watch me do it. I open the decision matrix. First, the weights: quality on our evaluation gets a quarter, differentiation fit a fifth, data control fifteen percent, and the rest shared. I agree these with the operations director before scoring anything. Next, the scores for three options. The quality scores come from our two-hundred-ticket bake-off, not demos. I run it, and the blend scores highest, then build, then the built-in agent. Then I open the due diligence questionnaire and send it to the help desk vendor. Two answers come back vague: whether we can pin model versions, and the exact sub-processor list. I ask again in writing and note both in the decision record. Finally, I write the recommendation: blend, with three reasons, and a renewal date when we'll revisit it.
```

Scene 14 — Recap

```text
To recap: there are four paths: existing tools, specialised products, building on APIs, or blending. Weigh differentiation, fit, data specificity, capacity, time, control and economics. Count the hidden costs on both sides. Evaluate vendors on your own data, and design for replaceability. Your next step is to fill in the decision matrix for one use case and recommend build, buy or blend with three reasons. Next module: choosing models and designing the architecture.
```

Scene 15 — Try this now (1 hour + vendor reply)

```text
Try this now. Pick one AI feature you're considering. Fill in the weighted decision matrix for three options: existing tool, specialised product, and build or blend. Leave the quality row empty until you've run at least twenty real cases through each option you can access. Then send the nine due-diligence questions to your top vendor and note which answers are vague.
```

</details>

### 6. Choosing models and an architecture that can change

Lesson `choosing-models-and-architecture` · module “Architecture, models and orchestration” · target 9 min · 15 scenes · 1227 words · planned 8:55 (words ≈ 8:53) · status: **to produce**

| Term | Say |
|---|---|
| Vertex AI | vertex A I |
| gpt-oss | G P T O S S |
| LiteLLM | lite L L M |
| p95 | P ninety-five |

| # | Time | Sec | Narration | On screen | Visual |
|---|---|---|---|---|---|
| 1 | 0:00 | 36 | The model you pick today will not be the model you use next year. Providers release new models several times a year, retire old ones and change prices. If your product has one model name hard-wired into every prompt and code path, every change becomes a project. In this lesson you'll learn the main model options and their trade-offs, how to choose per task using your own evaluation, and an architecture that lets you switch models in a day instead of a quarter. | Choosing models<br>• Models as replaceable components<br>• Options and trade-offs<br>• Architecture that can change | Layout: A product diagram where a single model block is welded in place with a red 'hard-coded' label; it transforms into a plug-in slot with several interchangeable model cartridges. Timing (36s): 0-3s 'Choosing models' heading fades in; 3-30s elements build in narration order; 30-36s hold final frame. |
| 2 | 0:36 | 29 | Here's an analogy. Choosing models is like staffing a team. You wouldn't hire a senior partner to sort the post, or an intern to negotiate a merger. You match the person to the task, and you organise the office so people can change roles without rebuilding the building. Small models sort the post, frontier models handle the hard thinking, and your architecture lets you reassign them easily. | Analogy: staffing a team<br>• Match capability to the task<br>• Design so roles can change | Layout: An office floor plan with desks labelled 'sorting', 'drafting', 'complex analysis'; different-sized figures move between desks without the walls changing. Timing (29s): 0-3s 'Analogy: staffing a team' heading fades in; 3-23s elements build in narration order; 23-29s hold final frame. |
| 3 | 1:05 | 50 | Here are the main options. Frontier models from first-party APIs, like the Claude, GPT and Gemini families, offer the highest capability on hard reasoning, coding and agentic work, plus rich features such as tools, structured outputs, caching and batch processing. The same models are often available through cloud platforms, like Amazon Bedrock, Google Cloud's Vertex AI and Microsoft Foundry, which helps with existing contracts, regional hosting and private networking, though feature availability can differ. Smaller, faster tiers are cheap and quick for classification, extraction and routing. And open-weight models, from families like Llama, Mistral, Qwen, Gemma, DeepSeek and gpt-oss, give you self-hosting and data control, at the cost of running infrastructure and safety yourself. | The options<br>• Frontier APIs<br>• Same models via cloud platforms<br>• Smaller fast tiers<br>• Open-weight, self-hosted | Layout: four quadrants with simple icons: a lightning bolt (frontier), a cloud (platforms), a small fast arrow (small tiers), an open padlock over a server (open-weight). Timing (50s): 0-3s 'The options' heading fades in; 3-44s elements build in narration order; 44-50s hold final frame. |
| 4 | 1:55 | 39 | Choose per task, not per company. Define the task's quality bar from your brief and evaluation set. Shortlist two to four models across tiers, including a cheaper one. Run your evaluation set on each and record quality by tag, median and slow-tail latency, cost per successful outcome and failure modes. Then tune the settings that matter. Many current models expose an effort or reasoning control. Lower effort often holds quality on simple routes at much lower cost and latency. Higher effort pays off on hard ones. Measure, don't guess. | Choosing per task<br>• Quality bar → shortlist → evaluate<br>• Latency, cost per outcome, failures<br>• Tune effort/reasoning settings | Layout: A comparison table with three model columns and rows for quality, p95 latency, cost per outcome; an 'effort' slider changes the numbers live. Timing (39s): 0-3s 'Choosing per task' heading fades in; 3-33s elements build in narration order; 33-39s hold final frame. |
| 5 | 2:34 | 33 | Don't forget data and regional constraints. Where is data processed? For how long is it retained? Do you need a cloud platform in a specific region, or self-hosting? A bank in Riyadh and a creator tool in London may make different choices for the same task. And decide per route. A support product might use a small model to classify, a frontier model at medium effort to draft, and an overnight batch job for summaries. | Constraints and routes<br>• Processing region, retention, hosting<br>• Different routes, different models | Layout: A routing diagram: incoming ticket splits to 'classify: small model', 'draft: frontier, medium effort', 'nightly summaries: batch'. Timing (33s): 0-3s 'Constraints and routes' heading fades in; 3-27s elements build in narration order; 27-33s hold final frame. |
| 6 | 3:07 | 41 | Now the architecture. Your product features call a thin AI service layer that you own: prompts, tools, schemas, routing, retries and logging. Beneath it sits a model gateway handling keys, budgets, rate limits, fallbacks and usage tracking. Beneath that, providers, cloud platforms or self-hosted models. Keep prompts and schemas in version-controlled files. Route by configuration so switching a model is a config change plus an evaluation run. Pin and record versions in every log. And re-run evaluations before any switch, even an upgrade, because newer models can change tone, length, formatting and tool habits. | Architecture<br>• Thin AI service layer (yours)<br>• Model gateway: keys, budgets, fallbacks<br>• Config routing, pinned versions, evals before switching | Layout: three stacked layers: 'Product features', 'AI service layer', 'Model gateway', over a row of provider boxes; a config file icon feeds the gateway. Timing (41s): 0-3s 'Architecture' heading fades in; 3-35s elements build in narration order; 35-41s hold final frame. |
| 7 | 3:48 | 31 | A simple example. A creator agency has two AI routes. One tags incoming brand emails into five categories. The other drafts detailed campaign proposals. They test a small, fast model and a frontier model on fifty emails and ten briefs. For tagging, both score the same, and the small one is far cheaper and faster. For proposals, the frontier model is clearly better. Two routes, two models, decided in an afternoon. | Simple example: two routes<br>• Tagging: small model = frontier model<br>• Proposals: frontier model clearly better<br>• Route each accordingly | Layout: two lanes: 'tag emails' with two model icons showing equal scores and different cost tags; 'draft proposals' with the frontier model scoring higher; routing arrows split accordingly. Timing (31s): 0-3s 'Simple example: two routes' heading fades in; 3-25s elements build in narration order; 25-31s hold final frame. |
| 8 | 4:19 | 39 | Here's a worked example. A UAE travel company's support copilot used one frontier model at maximum settings for everything. Evaluation by route showed classification was just as good on a small model, and drafting held quality at medium effort for routine requests. After routing, with high effort only for complaint escalations, median latency and cost per resolved ticket both fell substantially with no drop in quality scores. When the provider later released a new model, they switched one route, re-ran the suite and rolled it out in a day. | Worked example: travel support copilot<br>• Small model for classification<br>• Medium effort for routine drafts<br>• New model adopted in a day | Layout: before-and-after bars for latency and cost per ticket; a calendar showing 'new model released' and 'rolled out' one day apart. Timing (39s): 0-3s 'Worked example: travel support copilot' heading fades in; 3-33s elements build in narration order; 33-39s hold final frame. |
| 9 | 4:58 | 32 | Illustrative numbers for the travel copilot. Before routing, median latency was about nine seconds and cost per resolved ticket was the biggest line in the AI budget. After routing, classification took under a second, routine drafts about four seconds, and cost per resolved ticket fell by more than half. Quality scores on four hundred test tickets stayed within a point. When the new model arrived, one config line changed and the suite ran overnight. | Business example (illustrative)<br>• Latency ~9s → ~4s<br>• Cost per ticket down >50%<br>• Quality within a point<br>• Upgrade: one config line | Layout: before/after latency bars; a cost-per-ticket bar halving; a routes.yaml line changing model name with an overnight moon icon beside the eval suite. Timing (32s): 0-3s 'Business example (illustrative)' heading fades in; 3-26s elements build in narration order; 26-32s hold final frame. |
| 10 | 5:30 | 29 | The hands-on section shows a routes file that maps each task to a provider, model, effort level and fallback, and a short Python function that calls the primary route, falls back on rate limits, server errors or connection failures, and returns which model actually answered, along with usage. Model IDs are examples current at the time of writing, so always take them from the provider's model list. | Hands-on in the lesson<br>• routes.yaml per task<br>• Primary + fallback with effort settings<br>• Record which model answered | Layout: code editor showing routes.yaml and the call() function; a simulated 429 error triggers a fallback arrow. Timing (29s): 0-3s 'Hands-on in the lesson' heading fades in; 3-23s elements build in narration order; 23-29s hold final frame. |
| 11 | 5:59 | 24 | Common mistakes. Using one model everywhere at maximum settings. Choosing by public benchmark instead of your own evaluation. Hard-coding model names across the codebase. Ignoring where data is processed. Treating newer as automatically better. And forgetting to log which model and version produced each output, so you can't explain a change in behaviour when it happens. | Common mistakes<br>• One model, max settings, everywhere<br>• Public benchmarks over your evals<br>• Hard-coded model names<br>• Ignoring data location<br>• Not logging versions | Layout: A codebase view with the same model string highlighted in dozens of files; a benchmark trophy with a question mark; a log line missing its 'model' field. Timing (24s): 0-3s 'Common mistakes' heading fades in; 3-18s elements build in narration order; 18-24s hold final frame. |
| 12 | 6:23 | 27 | How will you know your model choices and architecture are right? Each route has a named model, a documented reason and evaluation results. Switching a route takes a configuration change and an evaluation run, not a code change. Cost per successful outcome is tracked per route. And when a provider announces a deprecation, you know within minutes which routes are affected. | How you'll know it's right<br>• Each route: model + reason + evals<br>• Config switch, not code change<br>• Cost per outcome by route<br>• Deprecations mapped in minutes | Layout: A routes table with columns route, model, reason, eval score, cost per outcome; a 'deprecation notice' card highlights two rows instantly. Timing (27s): 0-3s 'How you'll know it's right' heading fades in; 3-21s elements build in narration order; 21-27s hold final frame. |
| 13 | 6:50 | 64 | Watch me do it. I open routes dot yaml. Classify ticket uses a small model with two hundred max tokens. Draft reply uses a frontier model at medium effort, with a fallback to another model. Next, the call function loads the route config. For the primary and then the fallback, it builds the request, adds output config effort if the route sets one, and calls the API. On rate-limit, server or connection errors it moves to the fallback; any other error surfaces immediately. It returns the text, the model that actually answered, usage and the stop reason. I run the draft route while simulating a rate-limit on the primary, and the result shows the fallback model's name, which I log. Finally, I change classify ticket to a different small model in the YAML, run the evaluation suite on that route only, and compare accuracy and latency before merging. | Watch me do it: config routing<br>• routes.yaml per task<br>• effort via output_config<br>• Fallback on transient errors<br>• Log which model answered | Layout: YAML top-left, Python bottom-left, output right. Highlight the effort line, then the except clause; output shows 'model: fallback' after a simulated 429; an eval comparison table appears. Timing (64s): 0-3s 'Watch me do it: config routing' heading fades in; 3-58s elements build in narration order; 58-64s hold final frame. |
| 14 | 7:54 | 34 | To recap: models are components. Know the options, choose per route using your own evaluation and effort settings, respect data constraints, and build a thin service layer and gateway with versioned prompts and config routing. Your next step is to list the AI routes in one product or workflow, shortlist two models for each, and write a routes entry with a fallback. For deeper integration work, see the Integrating AI Platforms course. Next, orchestrating multi-step workflows. | Recap<br>• Per-route choice, measured<br>• Replaceable by design<br>• Next step: write your routes file | Layout: recap card; 'Go deeper: Integrating AI Platforms: Claude, OpenAI, Gemini and Open Models via API' course tile; 'Next: Workflow orchestration patterns' title card. Timing (34s): 0-3s 'Recap' heading fades in; 3-28s elements build in narration order; 28-34s hold final frame. |
| 15 | 8:28 | 27 | Try this now. List every place your product or workflow calls an AI model. Group them into routes, like classify, extract, draft and summarise. For each route, write the current model, the quality bar and one cheaper candidate. Then write a routes file entry for the two most expensive routes, with a fallback, and schedule a short evaluation to test the cheaper candidate. | Try this now (30 minutes)<br>• Inventory your AI calls<br>• Group into routes<br>• routes.yaml for the top 2 + fallback | Layout: A list of AI call sites collapses into four route groups; a YAML file appears with two entries and fallback lines highlighted. Timing (27s): 0-3s 'Try this now (30 minutes)' heading fades in; 3-21s elements build in narration order; 21-27s hold final frame. |

<details><summary>Narration per scene (paste into ElevenLabs)</summary>

Scene 1 — Choosing models

```text
The model you pick today will not be the model you use next year. Providers release new models several times a year, retire old ones and change prices. If your product has one model name hard-wired into every prompt and code path, every change becomes a project. In this lesson you'll learn the main model options and their trade-offs, how to choose per task using your own evaluation, and an architecture that lets you switch models in a day instead of a quarter.
```

Scene 2 — Analogy: staffing a team

```text
Here's an analogy. Choosing models is like staffing a team. You wouldn't hire a senior partner to sort the post, or an intern to negotiate a merger. You match the person to the task, and you organise the office so people can change roles without rebuilding the building. Small models sort the post, frontier models handle the hard thinking, and your architecture lets you reassign them easily.
```

Scene 3 — The options

```text
Here are the main options. Frontier models from first-party APIs, like the Claude, GPT and Gemini families, offer the highest capability on hard reasoning, coding and agentic work, plus rich features such as tools, structured outputs, caching and batch processing. The same models are often available through cloud platforms, like Amazon Bedrock, Google Cloud's Vertex AI and Microsoft Foundry, which helps with existing contracts, regional hosting and private networking, though feature availability can differ. Smaller, faster tiers are cheap and quick for classification, extraction and routing. And open-weight models, from families like Llama, Mistral, Qwen, Gemma, DeepSeek and gpt-oss, give you self-hosting and data control, at the cost of running infrastructure and safety yourself.
```

Scene 4 — Choosing per task

```text
Choose per task, not per company. Define the task's quality bar from your brief and evaluation set. Shortlist two to four models across tiers, including a cheaper one. Run your evaluation set on each and record quality by tag, median and slow-tail latency, cost per successful outcome and failure modes. Then tune the settings that matter. Many current models expose an effort or reasoning control. Lower effort often holds quality on simple routes at much lower cost and latency. Higher effort pays off on hard ones. Measure, don't guess.
```

Scene 5 — Constraints and routes

```text
Don't forget data and regional constraints. Where is data processed? For how long is it retained? Do you need a cloud platform in a specific region, or self-hosting? A bank in Riyadh and a creator tool in London may make different choices for the same task. And decide per route. A support product might use a small model to classify, a frontier model at medium effort to draft, and an overnight batch job for summaries.
```

Scene 6 — Architecture

```text
Now the architecture. Your product features call a thin AI service layer that you own: prompts, tools, schemas, routing, retries and logging. Beneath it sits a model gateway handling keys, budgets, rate limits, fallbacks and usage tracking. Beneath that, providers, cloud platforms or self-hosted models. Keep prompts and schemas in version-controlled files. Route by configuration so switching a model is a config change plus an evaluation run. Pin and record versions in every log. And re-run evaluations before any switch, even an upgrade, because newer models can change tone, length, formatting and tool habits.
```

Scene 7 — Simple example: two routes

```text
A simple example. A creator agency has two AI routes. One tags incoming brand emails into five categories. The other drafts detailed campaign proposals. They test a small, fast model and a frontier model on fifty emails and ten briefs. For tagging, both score the same, and the small one is far cheaper and faster. For proposals, the frontier model is clearly better. Two routes, two models, decided in an afternoon.
```

Scene 8 — Worked example: travel support copilot

```text
Here's a worked example. A UAE travel company's support copilot used one frontier model at maximum settings for everything. Evaluation by route showed classification was just as good on a small model, and drafting held quality at medium effort for routine requests. After routing, with high effort only for complaint escalations, median latency and cost per resolved ticket both fell substantially with no drop in quality scores. When the provider later released a new model, they switched one route, re-ran the suite and rolled it out in a day.
```

Scene 9 — Business example (illustrative)

```text
Illustrative numbers for the travel copilot. Before routing, median latency was about nine seconds and cost per resolved ticket was the biggest line in the AI budget. After routing, classification took under a second, routine drafts about four seconds, and cost per resolved ticket fell by more than half. Quality scores on four hundred test tickets stayed within a point. When the new model arrived, one config line changed and the suite ran overnight.
```

Scene 10 — Hands-on in the lesson

```text
The hands-on section shows a routes file that maps each task to a provider, model, effort level and fallback, and a short Python function that calls the primary route, falls back on rate limits, server errors or connection failures, and returns which model actually answered, along with usage. Model IDs are examples current at the time of writing, so always take them from the provider's model list.
```

Scene 11 — Common mistakes

```text
Common mistakes. Using one model everywhere at maximum settings. Choosing by public benchmark instead of your own evaluation. Hard-coding model names across the codebase. Ignoring where data is processed. Treating newer as automatically better. And forgetting to log which model and version produced each output, so you can't explain a change in behaviour when it happens.
```

Scene 12 — How you'll know it's right

```text
How will you know your model choices and architecture are right? Each route has a named model, a documented reason and evaluation results. Switching a route takes a configuration change and an evaluation run, not a code change. Cost per successful outcome is tracked per route. And when a provider announces a deprecation, you know within minutes which routes are affected.
```

Scene 13 — Watch me do it: config routing

```text
Watch me do it. I open routes dot yaml. Classify ticket uses a small model with two hundred max tokens. Draft reply uses a frontier model at medium effort, with a fallback to another model. Next, the call function loads the route config. For the primary and then the fallback, it builds the request, adds output config effort if the route sets one, and calls the API. On rate-limit, server or connection errors it moves to the fallback; any other error surfaces immediately. It returns the text, the model that actually answered, usage and the stop reason. I run the draft route while simulating a rate-limit on the primary, and the result shows the fallback model's name, which I log. Finally, I change classify ticket to a different small model in the YAML, run the evaluation suite on that route only, and compare accuracy and latency before merging.
```

Scene 14 — Recap

```text
To recap: models are components. Know the options, choose per route using your own evaluation and effort settings, respect data constraints, and build a thin service layer and gateway with versioned prompts and config routing. Your next step is to list the AI routes in one product or workflow, shortlist two models for each, and write a routes entry with a fallback. For deeper integration work, see the Integrating AI Platforms course. Next, orchestrating multi-step workflows.
```

Scene 15 — Try this now (30 minutes)

```text
Try this now. List every place your product or workflow calls an AI model. Group them into routes, like classify, extract, draft and summarise. For each route, write the current model, the quality bar and one cheaper candidate. Then write a routes file entry for the two most expensive routes, with a fallback, and schedule a short evaluation to test the cheaper candidate.
```

</details>

### 7. Workflow orchestration patterns for AI features

Lesson `workflow-orchestration-patterns` · module “Architecture, models and orchestration” · target 9 min · 16 scenes · 1233 words · planned 8:56 (words ≈ 8:56) · status: **to produce**

| Term | Say |
|---|---|
| idempotent | eye-dem-potent |

| # | Time | Sec | Narration | On screen | Visual |
|---|---|---|---|---|---|
| 1 | 0:00 | 31 | Agents get the headlines, but most AI features that quietly save businesses hours every day are workflows. Fixed steps, defined in code or a workflow tool, with a model doing specific jobs inside them. They're predictable, testable and cheap. In this lesson you'll learn the five workflow patterns that show up everywhere, how to choose between them, and the production engineering, gates, retries, durable execution and batch processing, that makes them reliable. | Workflow orchestration<br>• Five recurring patterns<br>• Choosing and combining<br>• Production engineering | Layout: A glamorous 'AI agent' spotlight on stage; backstage, a tidy conveyor-belt workflow does most of the work with small model icons at certain stations. Timing (31s): 0-3s 'Workflow orchestration' heading fades in; 3-25s elements build in narration order; 25-31s hold final frame. |
| 2 | 0:31 | 32 | Here's an analogy. A workflow is like a restaurant kitchen's line. Orders come in, a station preps, another cooks, another plates, and the pass checks each plate before it leaves. Routing is the head chef deciding which station gets the order. Parallelisation is several cooks working at once. The evaluator-optimiser is the pass, sending a plate back to be fixed. The model is a talented cook at some stations, not the whole kitchen. | Analogy: the kitchen line<br>• Stations = steps<br>• Head chef = routing<br>• The pass = evaluator<br>• Model = a cook, not the kitchen | Layout: A top-down kitchen line animation: an order ticket enters, moves station to station, is checked at the pass, and one plate is sent back once before leaving. Timing (32s): 0-3s 'Analogy: the kitchen line' heading fades in; 3-26s elements build in narration order; 26-32s hold final frame. |
| 3 | 1:03 | 27 | Why does this matter for your product? Because workflows are where most of the reliability, cost savings and speed come from. Agents grab attention, but a well-designed workflow with two focused model calls and good checks will usually beat a clever agent on cost, predictability and ease of testing. Learn these patterns and you'll ship more features that actually stay working. | Why it matters<br>• Workflows deliver most reliable value<br>• Focused calls + checks beat clever agents<br>• Easier to test and maintain | Layout: A comparison scale: left pan 'workflow' with icons for cost, predictability, testability outweighs right pan 'agent' for a routine task. Timing (27s): 0-3s 'Why it matters' heading fades in; 3-21s elements build in narration order; 21-27s hold final frame. |
| 4 | 1:30 | 34 | Pattern one: prompt chaining. Break a task into steps, each a focused model call, with gates between them. Extract facts from a brief, then draft, then check the draft against brand rules. Each step is easier alone, and gates stop bad data flowing forward. Pattern two: routing. Classify the input first, then send it down a specialised path. Billing, technical and cancellation tickets each get their own prompt, model or team, and easy routes can use cheaper models. | Patterns 1–2<br>• Prompt chaining with gates<br>• Routing by input type | Layout: A chain of three boxes with small gate icons between them; then a router diamond splitting tickets into three coloured lanes. Timing (34s): 0-3s 'Patterns 1–2' heading fades in; 3-28s elements build in narration order; 28-34s hold final frame. |
| 5 | 2:04 | 41 | Pattern three: parallelisation. Run independent calls at the same time. Sectioning splits a task, like reviewing each clause of a contract in parallel. Voting runs the same task several times to boost confidence, like flagging risky content only when most runs agree. Pattern four: orchestrator-workers. A model decides the sub-tasks based on the input, delegates them and combines results. It's useful when you can't predict the sub-tasks, and it's where workflows start to become agents. Pattern five: evaluator-optimiser. One call produces, another evaluates against clear criteria and feeds back, for a limited number of rounds. | Patterns 3–5<br>• Parallelisation: sectioning or voting<br>• Orchestrator-workers<br>• Evaluator-optimiser (bounded) | Layout: three diagrams: parallel arrows merging; a lead node spawning variable numbers of workers; a two-node loop with a round counter '1/2'. Timing (41s): 0-3s 'Patterns 3–5' heading fades in; 3-35s elements build in narration order; 35-41s hold final frame. |
| 6 | 2:45 | 30 | Choose by situation. Known steps that are easier separately: chaining. Distinct categories of input: routing. Independent sub-tasks, or a need for confidence: parallelisation. Sub-tasks you can't know in advance: orchestrator-workers. Clear quality criteria where iteration helps, like translating marketing copy into Arabic and checking tone and meaning against a rubric: evaluator-optimiser. And combine them. Route first, chain within each route, and put an evaluator on the final step. | Choosing and combining<br>• Match pattern to situation<br>• Route → chain → evaluate | Layout: A decision table fades into a combined diagram: router at the left, chains in each lane, evaluator loop at the end. Timing (30s): 0-3s 'Choosing and combining' heading fades in; 3-24s elements build in narration order; 24-30s hold final frame. |
| 7 | 3:15 | 33 | A simple example of chaining with a gate. Step one: extract the order number and complaint type from a customer email. Gate: is the order number in the right format and does it exist? If not, send it to a person. Step two: draft a reply using the order's details. Gate: does the draft mention a refund amount? If so, route it for approval. Two steps, two gates, and each problem is caught exactly where it starts. | Simple example: two steps, two gates<br>• Extract → check order exists<br>• Draft → check for refund amounts<br>• Problems caught where they start | Layout: A two-step chain with gate icons after each step; one email fails the first gate and diverts to a person; another passes both and continues. Timing (33s): 0-3s 'Simple example: two steps, two gates' heading fades in; 3-27s elements build in narration order; 27-33s hold final frame. |
| 8 | 3:48 | 36 | Now the engineering. Put gates in code between steps: schema validation, business rules, length limits and required citations, and route failures to review. Pass structured outputs between steps. Make each step idempotent and retryable, and save step outputs so a retry resumes instead of restarting. For long or human-in-the-loop workflows, use durable execution: a workflow engine, a queue with workers, or a no-code platform with wait and approval steps. A workflow waiting two days for a manager must survive a server restart. | Production engineering<br>• Gates in code<br>• Structured outputs between steps<br>• Idempotent retries, saved step outputs<br>• Durable execution for waits | Layout: A workflow run paused at an 'awaiting approval' node with a calendar showing two days; a server restarts and the run resumes from the same node. Timing (36s): 0-3s 'Production engineering' heading fades in; 3-30s elements build in narration order; 30-36s hold final frame. |
| 9 | 4:24 | 36 | Two more practices. Batch processing: for non-urgent bulk work, like extracting facts from five hundred briefs overnight, batch endpoints are often discounted, and results come back keyed by an ID you choose, not in order. And observability: one trace per workflow run, with a span per step, so when something fails you can see exactly which step and why. Also, back up model-based checks with deterministic ones. If a caption must contain hashtag ad, a line of code can check that with certainty. | Batch + observability<br>• Batch non-urgent bulk steps<br>• One trace per run, span per step<br>• Deterministic checks back up model checks | Layout: A night-time scene with a batch job icon processing a stack of briefs; a trace view with one red span; a code line 'assert "#ad" in caption'. Timing (36s): 0-3s 'Batch + observability' heading fades in; 3-30s elements build in narration order; 30-36s hold final frame. |
| 10 | 5:00 | 30 | A deeper business example, illustrative. A London creator-management agency runs this caption workflow for about three hundred sponsored posts a month. The check step catches a missing disclosure or banned claim in roughly one draft in twelve, and the deterministic check catches the few the model misses. Account managers now review drafts that are almost always compliant, and time per sponsored post fell from around forty minutes to fifteen. | Business example (illustrative)<br>• ~300 sponsored posts/month<br>• Check step catches ~1 in 12<br>• ~40 → ~15 minutes per post | Layout: a funnel of 300 drafts; one in twelve diverted at a 'check' gate; a clock beside the funnel shrinks from 40 to 15 minutes. Timing (30s): 0-3s 'Business example (illustrative)' heading fades in; 3-24s elements build in narration order; 24-30s hold final frame. |
| 11 | 5:30 | 32 | The hands-on section builds a creator-marketing workflow: extract campaign facts from a brief with a structured output, gate them in code, draft Instagram captions that avoid banned claims and include disclosure when required, then check them with a second structured call, retrying at most twice before handing to a human. You'll also see how to send the extraction step through a batch endpoint for five hundred briefs overnight, matching results by custom ID. | Hands-on in the lesson<br>• Extract → gate → draft → check (bounded)<br>• Human review at the end<br>• Batch variant for bulk runs | Layout: code editor scrolling through step_extract, gate_facts, step_draft and step_check; the output reads 'ready_for_human_review'. Timing (32s): 0-3s 'Hands-on in the lesson' heading fades in; 3-26s elements build in narration order; 26-32s hold final frame. |
| 12 | 6:02 | 27 | Common mistakes. Using one giant prompt to do extraction, drafting and checking all at once. Putting no checks between steps. Retrying failed steps from the very beginning, so earlier work is repeated and sometimes duplicated. Running long waits inside a single process that dies on restart. And evaluating only the final output, so you can't see which step caused a failure. | Common mistakes<br>• One giant prompt<br>• No gates<br>• Restart from scratch on retry<br>• Long waits in fragile processes<br>• Final-output-only evaluation | Layout: A single overloaded prompt box bursting; a chain without gates letting a red item flow to the end; a restart arrow looping to step one. Timing (27s): 0-3s 'Common mistakes' heading fades in; 3-21s elements build in narration order; 21-27s hold final frame. |
| 13 | 6:29 | 28 | How will you know your workflow is well orchestrated? Each step has its own pass rate you can see. Failures show up at the gate where they start, not at the end. A crash or restart doesn't lose or duplicate work. Human waits don't block anything else. And cost per completed run is stable as volume grows, with batch processing used where speed isn't needed. | How you'll know it's working<br>• Per-step pass rates<br>• Failures caught at their gate<br>• No lost or duplicated work<br>• Stable cost per run | Layout: A workflow diagram where each step shows a small pass-rate badge (98%, 94%, 99%); a restart event icon shows the run resuming from step three. Timing (28s): 0-3s 'How you'll know it's working' heading fades in; 3-22s elements build in narration order; 22-28s hold final frame. |
| 14 | 6:57 | 63 | Watch me do it. I open the workflow code. First, two schemas: BriefFacts with brand, audience, key messages, banned claims and whether disclosure is required, and Check with a pass or fail verdict and a list of problems. Step extract parses the brief into BriefFacts. The gate raises an error if there are no key messages, which routes the brief to a human. Step draft writes three captions using the messages, avoiding banned claims and starting with the disclosure when required. Step check parses a verdict. Then run loops at most twice: if the check passes, it returns ready for human review; if not, it redrafts with the problems as guidance. After two failures it returns needs human. I run it on a skincare brief with a banned claim, clinically proven. The first draft fails, the second passes, and the status is ready for human review. | Watch me do it: chain with gates<br>• BriefFacts + Check schemas<br>• gate_facts() → human route<br>• Draft, check, redraft (max 2)<br>• 'ready_for_human_review' | Layout: a horizontal chain diagram top, code below. Each step node lights up as its function is highlighted; the check node flashes red once, then green, and the output status appears. Timing (63s): 0-3s 'Watch me do it: chain with gates' heading fades in; 3-57s elements build in narration order; 57-63s hold final frame. |
| 15 | 8:00 | 29 | To recap: most AI features are workflows. Use chaining, routing, parallelisation, orchestrator-workers and evaluator-optimiser deliberately, and combine them. Make them production-grade with gates, structured outputs, idempotent retries, durable execution, batching and traces. Your next step is to map one AI feature as a workflow, naming each step's pattern, gate and human checkpoint, and build the first two steps. Next, integrating AI into the systems you already run. | Recap<br>• Five patterns, combined<br>• Gates, durability, traces<br>• Next step: map and build two steps | Layout: recap card; 'Next: Integrating AI into existing systems' title card. Timing (29s): 0-3s 'Recap' heading fades in; 3-23s elements build in narration order; 23-29s hold final frame. |
| 16 | 8:29 | 27 | Try this now. Take one AI feature and draw it as boxes: each step, its pattern, the gate after it, and where a person approves. Mark any step that could run in parallel and any that could run overnight in a batch. Then build the first two steps with structured outputs and one gate in code, and run ten real inputs through them. | Try this now (45 minutes)<br>• Draw steps, patterns, gates, approvals<br>• Mark parallel and batch candidates<br>• Build steps 1–2 + one gate | Layout: A whiteboard-style diagram draws itself with labelled boxes and gate icons; two boxes glow as 'built'; ten input dots flow through them. Timing (27s): 0-3s 'Try this now (45 minutes)' heading fades in; 3-21s elements build in narration order; 21-27s hold final frame. |

<details><summary>Narration per scene (paste into ElevenLabs)</summary>

Scene 1 — Workflow orchestration

```text
Agents get the headlines, but most AI features that quietly save businesses hours every day are workflows. Fixed steps, defined in code or a workflow tool, with a model doing specific jobs inside them. They're predictable, testable and cheap. In this lesson you'll learn the five workflow patterns that show up everywhere, how to choose between them, and the production engineering, gates, retries, durable execution and batch processing, that makes them reliable.
```

Scene 2 — Analogy: the kitchen line

```text
Here's an analogy. A workflow is like a restaurant kitchen's line. Orders come in, a station preps, another cooks, another plates, and the pass checks each plate before it leaves. Routing is the head chef deciding which station gets the order. Parallelisation is several cooks working at once. The evaluator-optimiser is the pass, sending a plate back to be fixed. The model is a talented cook at some stations, not the whole kitchen.
```

Scene 3 — Why it matters

```text
Why does this matter for your product? Because workflows are where most of the reliability, cost savings and speed come from. Agents grab attention, but a well-designed workflow with two focused model calls and good checks will usually beat a clever agent on cost, predictability and ease of testing. Learn these patterns and you'll ship more features that actually stay working.
```

Scene 4 — Patterns 1–2

```text
Pattern one: prompt chaining. Break a task into steps, each a focused model call, with gates between them. Extract facts from a brief, then draft, then check the draft against brand rules. Each step is easier alone, and gates stop bad data flowing forward. Pattern two: routing. Classify the input first, then send it down a specialised path. Billing, technical and cancellation tickets each get their own prompt, model or team, and easy routes can use cheaper models.
```

Scene 5 — Patterns 3–5

```text
Pattern three: parallelisation. Run independent calls at the same time. Sectioning splits a task, like reviewing each clause of a contract in parallel. Voting runs the same task several times to boost confidence, like flagging risky content only when most runs agree. Pattern four: orchestrator-workers. A model decides the sub-tasks based on the input, delegates them and combines results. It's useful when you can't predict the sub-tasks, and it's where workflows start to become agents. Pattern five: evaluator-optimiser. One call produces, another evaluates against clear criteria and feeds back, for a limited number of rounds.
```

Scene 6 — Choosing and combining

```text
Choose by situation. Known steps that are easier separately: chaining. Distinct categories of input: routing. Independent sub-tasks, or a need for confidence: parallelisation. Sub-tasks you can't know in advance: orchestrator-workers. Clear quality criteria where iteration helps, like translating marketing copy into Arabic and checking tone and meaning against a rubric: evaluator-optimiser. And combine them. Route first, chain within each route, and put an evaluator on the final step.
```

Scene 7 — Simple example: two steps, two gates

```text
A simple example of chaining with a gate. Step one: extract the order number and complaint type from a customer email. Gate: is the order number in the right format and does it exist? If not, send it to a person. Step two: draft a reply using the order's details. Gate: does the draft mention a refund amount? If so, route it for approval. Two steps, two gates, and each problem is caught exactly where it starts.
```

Scene 8 — Production engineering

```text
Now the engineering. Put gates in code between steps: schema validation, business rules, length limits and required citations, and route failures to review. Pass structured outputs between steps. Make each step idempotent and retryable, and save step outputs so a retry resumes instead of restarting. For long or human-in-the-loop workflows, use durable execution: a workflow engine, a queue with workers, or a no-code platform with wait and approval steps. A workflow waiting two days for a manager must survive a server restart.
```

Scene 9 — Batch + observability

```text
Two more practices. Batch processing: for non-urgent bulk work, like extracting facts from five hundred briefs overnight, batch endpoints are often discounted, and results come back keyed by an ID you choose, not in order. And observability: one trace per workflow run, with a span per step, so when something fails you can see exactly which step and why. Also, back up model-based checks with deterministic ones. If a caption must contain hashtag ad, a line of code can check that with certainty.
```

Scene 10 — Business example (illustrative)

```text
A deeper business example, illustrative. A London creator-management agency runs this caption workflow for about three hundred sponsored posts a month. The check step catches a missing disclosure or banned claim in roughly one draft in twelve, and the deterministic check catches the few the model misses. Account managers now review drafts that are almost always compliant, and time per sponsored post fell from around forty minutes to fifteen.
```

Scene 11 — Hands-on in the lesson

```text
The hands-on section builds a creator-marketing workflow: extract campaign facts from a brief with a structured output, gate them in code, draft Instagram captions that avoid banned claims and include disclosure when required, then check them with a second structured call, retrying at most twice before handing to a human. You'll also see how to send the extraction step through a batch endpoint for five hundred briefs overnight, matching results by custom ID.
```

Scene 12 — Common mistakes

```text
Common mistakes. Using one giant prompt to do extraction, drafting and checking all at once. Putting no checks between steps. Retrying failed steps from the very beginning, so earlier work is repeated and sometimes duplicated. Running long waits inside a single process that dies on restart. And evaluating only the final output, so you can't see which step caused a failure.
```

Scene 13 — How you'll know it's working

```text
How will you know your workflow is well orchestrated? Each step has its own pass rate you can see. Failures show up at the gate where they start, not at the end. A crash or restart doesn't lose or duplicate work. Human waits don't block anything else. And cost per completed run is stable as volume grows, with batch processing used where speed isn't needed.
```

Scene 14 — Watch me do it: chain with gates

```text
Watch me do it. I open the workflow code. First, two schemas: BriefFacts with brand, audience, key messages, banned claims and whether disclosure is required, and Check with a pass or fail verdict and a list of problems. Step extract parses the brief into BriefFacts. The gate raises an error if there are no key messages, which routes the brief to a human. Step draft writes three captions using the messages, avoiding banned claims and starting with the disclosure when required. Step check parses a verdict. Then run loops at most twice: if the check passes, it returns ready for human review; if not, it redrafts with the problems as guidance. After two failures it returns needs human. I run it on a skincare brief with a banned claim, clinically proven. The first draft fails, the second passes, and the status is ready for human review.
```

Scene 15 — Recap

```text
To recap: most AI features are workflows. Use chaining, routing, parallelisation, orchestrator-workers and evaluator-optimiser deliberately, and combine them. Make them production-grade with gates, structured outputs, idempotent retries, durable execution, batching and traces. Your next step is to map one AI feature as a workflow, naming each step's pattern, gate and human checkpoint, and build the first two steps. Next, integrating AI into the systems you already run.
```

Scene 16 — Try this now (45 minutes)

```text
Try this now. Take one AI feature and draw it as boxes: each step, its pattern, the gate after it, and where a person approves. Mark any step that could run in parallel and any that could run overnight in a batch. Then build the first two steps with structured outputs and one gate in code, and run ten real inputs through them.
```

</details>

### 8. Integrating AI into existing systems

Lesson `integrating-ai-into-existing-systems` · module “Architecture, models and orchestration” · target 8 min · 15 scenes · 1184 words · planned 8:37 (words ≈ 8:35) · status: **to produce**

| Term | Say |
|---|---|
| HMAC | H mac |
| FastAPI | fast A P I |
| MCP | M C P |

| # | Time | Sec | Narration | On screen | Visual |
|---|---|---|---|---|---|
| 1 | 0:00 | 32 | An AI feature that can't read your CRM or write into your help desk is a demo. Integration is where most of the real engineering happens, and where most reliability problems hide: duplicate drafts, timeouts on busy days, jobs that silently vanish. In this lesson you'll learn the integration patterns that work, the reliability rules specific to AI calls, how MCP changes who builds what, and you'll see a webhook-to-queue-to-model worker you can adapt. | Integrating AI into your stack<br>• Patterns<br>• Reliability rules<br>• MCP's role<br>• A worked worker | Layout: An AI box floating alone labelled 'demo'; cables then connect it to CRM, help desk, order system and data warehouse icons, and it becomes 'product'. Timing (32s): 0-3s 'Integrating AI into your stack' heading fades in; 3-26s elements build in narration order; 26-32s hold final frame. |
| 2 | 0:32 | 28 | Here's an analogy. Integrating AI is like adding a new specialist to a busy hospital. Their skill matters, but so do the handovers: how patients are referred to them, how results get back into the patient record, what happens if they're off sick, and how nobody gets treated twice by mistake. Webhooks, queues, retries and idempotency are the hospital's handover procedures for your AI. | Analogy: a new hospital specialist<br>• Referrals in, results back to the record<br>• Cover when they're unavailable<br>• Nobody treated twice | Layout: A hospital corridor diagram: referral slips flow into a specialist room, results flow back to a records cabinet, a 'cover rota' board, and a duplicate slip is rejected at the door. Timing (28s): 0-3s 'Analogy: a new hospital specialist' heading fades in; 3-22s elements build in narration order; 22-28s hold final frame. |
| 3 | 1:00 | 42 | Five patterns cover most needs. A synchronous API call, where the user waits, for interactive features that must answer in seconds. An asynchronous job on a queue, for anything slow, bulky or retry-prone, like summaries or document processing. Webhooks in and out, for SaaS tools like help desks, CRMs and form builders. Scheduled batches, for reports and nightly enrichment. And tools exposed to models, often through MCP servers, for assistants and agents. Most real features combine several: a webhook triggers a queued job, the worker calls a model with tools, and the result is written back. | Five patterns<br>• Sync call • Queue + worker<br>• Webhooks • Scheduled batch<br>• Tools / MCP | Layout: five small architecture thumbnails appear, then merge into one combined flow: webhook → queue → worker → model (with tools) → write-back. Timing (42s): 0-3s 'Five patterns' heading fades in; 3-36s elements build in narration order; 36-42s hold final frame. |
| 4 | 1:42 | 32 | Now the reliability rules. Verify inbound webhooks by checking signatures and timestamps, respond quickly, and do slow work asynchronously. Make everything idempotent, because the same event may arrive twice: record processed event IDs. Retry only retryable errors, like rate limits, overloads and connection failures, with exponential back-off and a little randomness. Don't retry invalid requests. After the last retry, park the job in a dead-letter queue for a human, rather than dropping it. | Reliability rules 1–4<br>• Verify webhooks, respond fast<br>• Idempotency by event ID<br>• Retry the retryable, with back-off<br>• Dead-letter, don't drop | Layout: A webhook envelope passes a signature scanner; a duplicate envelope bounces off a 'seen' list; retries spaced increasingly apart on a timeline; a failed job lands in a 'dead-letter' tray. Timing (32s): 0-3s 'Reliability rules 1–4' heading fades in; 3-26s elements build in narration order; 26-32s hold final frame. |
| 5 | 2:14 | 32 | Three more rules. Write back safely, using the target system's API with least-privilege credentials, and prefer drafts, notes or suggestions over overwriting records. Keep AI out of the transaction path, so a failed AI step never blocks a customer's order or an agent's ticket. And trace across systems by carrying one correlation ID from the inbound event, through the model call, to the write-back. When something goes wrong, you'll find it in minutes. | Reliability rules 5–7<br>• Least-privilege write-back; drafts first<br>• AI off the transaction path<br>• One correlation ID end to end | Layout: An order checkout flow continuing smoothly while an AI side-branch shows a failure icon; a correlation ID tag travels through each system box. Timing (32s): 0-3s 'Reliability rules 5–7' heading fades in; 3-26s elements build in narration order; 26-32s hold final frame. |
| 6 | 2:46 | 36 | MCP changes who builds what. Instead of wiring every system into every AI application, the team that owns a system can publish one MCP server, and every compatible assistant, IDE or agent platform can use it under the host's consent and permission controls. For many organisations that means a small set of well-designed, least-privilege servers for core systems like the CRM, orders and the knowledge base, maintained by their owners. The same security rules still apply: allow-lists, scoped credentials, gated writes and logs. | MCP as an integration layer<br>• System owners publish once<br>• Many AI apps reuse<br>• Same security rules apply | Layout: three system owners each publishing a server tile (CRM, Orders, Knowledge); multiple AI app icons connect to the same tiles through a consent gate. Timing (36s): 0-3s 'MCP as an integration layer' heading fades in; 3-30s elements build in narration order; 30-36s hold final frame. |
| 7 | 3:22 | 31 | A simple example of idempotency. Your form tool sends a webhook when someone requests a quote. Occasionally, it sends the same webhook twice. Without protection, your AI drafts two quotes and emails the customer twice. With idempotency, your service records each event ID the first time it's processed. When the duplicate arrives, it sees the ID and does nothing. One line of logic, and a whole class of embarrassing errors disappears. | Simple example: the double webhook<br>• Same event arrives twice<br>• Without IDs: two quotes sent<br>• With IDs: duplicate ignored | Layout: two identical webhook envelopes arrive; the first passes a 'seen IDs' list and gets processed; the second is stamped 'duplicate — ignored' and falls away. Timing (31s): 0-3s 'Simple example: the double webhook' heading fades in; 3-25s elements build in narration order; 25-31s hold final frame. |
| 8 | 3:53 | 43 | Here's a real-world shaped story. A Saudi e-commerce brand connects its help desk to an AI drafting service. At first, the help desk calls the AI synchronously. On a big sale day, rate limits cause timeouts, the help desk retries its webhooks, and agents find duplicate drafts on tickets. The fix: verify and queue webhooks, dedupe by event ID, back off on rate limits, write drafts as internal notes, and send failures to a dead-letter queue watched by the support lead. The next sale day: no duplicates, no blocked tickets, and a handful of parked jobs handled by hand. | Worked example: sale-day spike<br>• Sync calls → timeouts + duplicates<br>• Queue, dedupe, back-off, notes, dead-letter<br>• Next sale day: clean | Layout: traffic spike graph; before: red duplicate draft icons multiplying; after: a smooth queue with a small dead-letter tray holding three items. Timing (43s): 0-3s 'Worked example: sale-day spike' heading fades in; 3-37s elements build in narration order; 37-43s hold final frame. |
| 9 | 4:36 | 32 | Illustrative numbers for the sale-day story. On the first sale day, the help desk received roughly ten times normal volume; about one ticket in eight got a duplicate draft and dozens timed out. On the next sale day, with queueing, dedupe and back-off, there were zero duplicates, drafts arrived within a couple of minutes even at peak, and just seven jobs landed in the dead-letter queue, all handled by hand within the hour. | Business example (illustrative)<br>• ~10× volume on sale day<br>• Before: ~1 in 8 duplicated<br>• After: 0 duplicates, 7 dead-lettered | Layout: two sale-day traffic graphs stacked; the top overlays red duplicate markers; the bottom is clean with a small tray labelled '7 dead-lettered'. Timing (32s): 0-3s 'Business example (illustrative)' heading fades in; 3-26s elements build in narration order; 26-32s hold final frame. |
| 10 | 5:08 | 35 | In the hands-on section you'll read a compact FastAPI service. It verifies a help desk webhook with an HMAC signature, ignores duplicate event IDs, queues the job and responds immediately. A worker drafts a reply with the Claude API, retrying rate-limit, server and connection errors with exponential back-off and jitter, never retrying bad requests, and dead-letters anything that still fails. In production, you'd swap the in-memory queue and set for durable services and add structured logs with the correlation ID. | Hands-on in the lesson<br>• Signed webhook → queue → worker<br>• Retries with back-off; dead-letter<br>• Swap in durable queue and storage | Layout: code editor showing the webhook handler and draft_with_retries; a terminal prints 'queued', 'duplicate_ignored' and one 'dead-letter' line. Timing (35s): 0-3s 'Hands-on in the lesson' heading fades in; 3-29s elements build in narration order; 29-35s hold final frame. |
| 11 | 5:43 | 26 | Common mistakes. Calling the AI synchronously inside a customer's checkout or ticket submission. Retrying every error, including invalid requests that will never succeed. No dead-letter queue, so failures vanish. Overwriting records directly instead of creating drafts or notes. Using one powerful API key for everything. And no correlation ID, so tracing a problem across three systems takes a day. | Common mistakes<br>• AI in the checkout path<br>• Retrying invalid requests<br>• No dead-letter queue<br>• Overwriting records<br>• One powerful key<br>• No correlation ID | Layout: A system diagram with six red warning markers placed on the relevant connections and boxes. Timing (26s): 0-3s 'Common mistakes' heading fades in; 3-20s elements build in narration order; 20-26s hold final frame. |
| 12 | 6:09 | 28 | How will you know your integration is solid? Duplicates never produce duplicate actions. Rate-limit spikes cause delays, not failures. Every failed job ends up somewhere a person can see it. Core business flows keep working when the AI provider is down. And you can follow one customer's request from the inbound event to the written-back result in a single search, using its correlation ID. | How you'll know it's solid<br>• No duplicate actions<br>• Spikes cause delays, not failures<br>• Every failure visible<br>• Core flows survive AI outages<br>• One-search tracing | Layout: A test scenario board with five rows passing: duplicate test, spike test, failure visibility, outage drill, trace search, each with a green tick. Timing (28s): 0-3s 'How you'll know it's solid' heading fades in; 3-22s elements build in narration order; 22-28s hold final frame. |
| 13 | 6:37 | 66 | Watch me do it. I open app dot py. First, the webhook route reads the raw body and computes an HMAC with our shared secret. If it doesn't match the signature header, it returns four-oh-one. Next, it parses the event and checks the event ID against the processed set; a duplicate returns duplicate ignored. Otherwise it puts the event on the queue and returns queued immediately. Then the worker. It takes a job and calls draft with retries. That function catches rate-limit, server and connection errors and sleeps with exponential back-off plus jitter, but re-raises bad request errors, because retrying won't fix them. On success, the worker would write an internal draft back to the help desk with the event ID as correlation ID, and marks the event processed. On failure, it prints a dead-letter line. I send the same test webhook twice. The first returns queued, the second duplicate ignored. | Watch me do it: app.py<br>• HMAC signature check<br>• Dedupe by event_id<br>• Queue + worker<br>• Back-off; don't retry bad requests | Layout: code left, terminal right. Highlight the HMAC lines, the processed check, then the except clauses; the terminal prints 'queued' then 'duplicate_ignored' for the same event. Timing (66s): 0-3s 'Watch me do it: app.py' heading fades in; 3-60s elements build in narration order; 60-66s hold final frame. |
| 14 | 7:43 | 28 | To recap: pick integration patterns deliberately and combine them. Verify, dedupe, retry sensibly, dead-letter, write back with least privilege, keep AI off the transaction path and trace end to end. Use MCP to publish system integrations once, securely. Your next step is to draw the integration for one AI feature, marking where idempotency, retries and dead-lettering happen. Next module: data, privacy and security architecture. | Recap<br>• Patterns + seven reliability rules<br>• MCP as the integration layer<br>• Next step: draw your integration | Layout: recap card; 'Next module: Data, privacy and security' title card. Timing (28s): 0-3s 'Recap' heading fades in; 3-22s elements build in narration order; 22-28s hold final frame. |
| 15 | 8:11 | 26 | Try this now. Pick one AI feature and draw its integration: the trigger, whether it's synchronous or queued, the model call, any tools, the write-back and the failure path. Mark where idempotency, retries and dead-lettering happen, and which credentials each step uses. If any of those marks are missing, that's your first fix, before you add any new AI capability. | Try this now (20 minutes)<br>• Draw trigger → queue → model → write-back<br>• Mark idempotency, retries, dead-letter<br>• Note each step's credentials | Layout: A hand-drawn integration diagram; small coloured stickers labelled 'idempotency', 'retry', 'dead-letter', 'key: read-only' attach to parts of it; one missing sticker spot glows. Timing (26s): 0-3s 'Try this now (20 minutes)' heading fades in; 3-20s elements build in narration order; 20-26s hold final frame. |

<details><summary>Narration per scene (paste into ElevenLabs)</summary>

Scene 1 — Integrating AI into your stack

```text
An AI feature that can't read your CRM or write into your help desk is a demo. Integration is where most of the real engineering happens, and where most reliability problems hide: duplicate drafts, timeouts on busy days, jobs that silently vanish. In this lesson you'll learn the integration patterns that work, the reliability rules specific to AI calls, how MCP changes who builds what, and you'll see a webhook-to-queue-to-model worker you can adapt.
```

Scene 2 — Analogy: a new hospital specialist

```text
Here's an analogy. Integrating AI is like adding a new specialist to a busy hospital. Their skill matters, but so do the handovers: how patients are referred to them, how results get back into the patient record, what happens if they're off sick, and how nobody gets treated twice by mistake. Webhooks, queues, retries and idempotency are the hospital's handover procedures for your AI.
```

Scene 3 — Five patterns

```text
Five patterns cover most needs. A synchronous API call, where the user waits, for interactive features that must answer in seconds. An asynchronous job on a queue, for anything slow, bulky or retry-prone, like summaries or document processing. Webhooks in and out, for SaaS tools like help desks, CRMs and form builders. Scheduled batches, for reports and nightly enrichment. And tools exposed to models, often through MCP servers, for assistants and agents. Most real features combine several: a webhook triggers a queued job, the worker calls a model with tools, and the result is written back.
```

Scene 4 — Reliability rules 1–4

```text
Now the reliability rules. Verify inbound webhooks by checking signatures and timestamps, respond quickly, and do slow work asynchronously. Make everything idempotent, because the same event may arrive twice: record processed event IDs. Retry only retryable errors, like rate limits, overloads and connection failures, with exponential back-off and a little randomness. Don't retry invalid requests. After the last retry, park the job in a dead-letter queue for a human, rather than dropping it.
```

Scene 5 — Reliability rules 5–7

```text
Three more rules. Write back safely, using the target system's API with least-privilege credentials, and prefer drafts, notes or suggestions over overwriting records. Keep AI out of the transaction path, so a failed AI step never blocks a customer's order or an agent's ticket. And trace across systems by carrying one correlation ID from the inbound event, through the model call, to the write-back. When something goes wrong, you'll find it in minutes.
```

Scene 6 — MCP as an integration layer

```text
MCP changes who builds what. Instead of wiring every system into every AI application, the team that owns a system can publish one MCP server, and every compatible assistant, IDE or agent platform can use it under the host's consent and permission controls. For many organisations that means a small set of well-designed, least-privilege servers for core systems like the CRM, orders and the knowledge base, maintained by their owners. The same security rules still apply: allow-lists, scoped credentials, gated writes and logs.
```

Scene 7 — Simple example: the double webhook

```text
A simple example of idempotency. Your form tool sends a webhook when someone requests a quote. Occasionally, it sends the same webhook twice. Without protection, your AI drafts two quotes and emails the customer twice. With idempotency, your service records each event ID the first time it's processed. When the duplicate arrives, it sees the ID and does nothing. One line of logic, and a whole class of embarrassing errors disappears.
```

Scene 8 — Worked example: sale-day spike

```text
Here's a real-world shaped story. A Saudi e-commerce brand connects its help desk to an AI drafting service. At first, the help desk calls the AI synchronously. On a big sale day, rate limits cause timeouts, the help desk retries its webhooks, and agents find duplicate drafts on tickets. The fix: verify and queue webhooks, dedupe by event ID, back off on rate limits, write drafts as internal notes, and send failures to a dead-letter queue watched by the support lead. The next sale day: no duplicates, no blocked tickets, and a handful of parked jobs handled by hand.
```

Scene 9 — Business example (illustrative)

```text
Illustrative numbers for the sale-day story. On the first sale day, the help desk received roughly ten times normal volume; about one ticket in eight got a duplicate draft and dozens timed out. On the next sale day, with queueing, dedupe and back-off, there were zero duplicates, drafts arrived within a couple of minutes even at peak, and just seven jobs landed in the dead-letter queue, all handled by hand within the hour.
```

Scene 10 — Hands-on in the lesson

```text
In the hands-on section you'll read a compact FastAPI service. It verifies a help desk webhook with an HMAC signature, ignores duplicate event IDs, queues the job and responds immediately. A worker drafts a reply with the Claude API, retrying rate-limit, server and connection errors with exponential back-off and jitter, never retrying bad requests, and dead-letters anything that still fails. In production, you'd swap the in-memory queue and set for durable services and add structured logs with the correlation ID.
```

Scene 11 — Common mistakes

```text
Common mistakes. Calling the AI synchronously inside a customer's checkout or ticket submission. Retrying every error, including invalid requests that will never succeed. No dead-letter queue, so failures vanish. Overwriting records directly instead of creating drafts or notes. Using one powerful API key for everything. And no correlation ID, so tracing a problem across three systems takes a day.
```

Scene 12 — How you'll know it's solid

```text
How will you know your integration is solid? Duplicates never produce duplicate actions. Rate-limit spikes cause delays, not failures. Every failed job ends up somewhere a person can see it. Core business flows keep working when the AI provider is down. And you can follow one customer's request from the inbound event to the written-back result in a single search, using its correlation ID.
```

Scene 13 — Watch me do it: app.py

```text
Watch me do it. I open app dot py. First, the webhook route reads the raw body and computes an HMAC with our shared secret. If it doesn't match the signature header, it returns four-oh-one. Next, it parses the event and checks the event ID against the processed set; a duplicate returns duplicate ignored. Otherwise it puts the event on the queue and returns queued immediately. Then the worker. It takes a job and calls draft with retries. That function catches rate-limit, server and connection errors and sleeps with exponential back-off plus jitter, but re-raises bad request errors, because retrying won't fix them. On success, the worker would write an internal draft back to the help desk with the event ID as correlation ID, and marks the event processed. On failure, it prints a dead-letter line. I send the same test webhook twice. The first returns queued, the second duplicate ignored.
```

Scene 14 — Recap

```text
To recap: pick integration patterns deliberately and combine them. Verify, dedupe, retry sensibly, dead-letter, write back with least privilege, keep AI off the transaction path and trace end to end. Use MCP to publish system integrations once, securely. Your next step is to draw the integration for one AI feature, marking where idempotency, retries and dead-lettering happen. Next module: data, privacy and security architecture.
```

Scene 15 — Try this now (20 minutes)

```text
Try this now. Pick one AI feature and draw its integration: the trigger, whether it's synchronous or queued, the model call, any tools, the write-back and the failure path. Mark where idempotency, retries and dead-lettering happen, and which credentials each step uses. If any of those marks are missing, that's your first fix, before you add any new AI capability.
```

</details>

### 9. Data and privacy architecture for AI features

Lesson `data-and-privacy-architecture` · module “Data, privacy and security architecture” · target 9 min · 15 scenes · 1203 words · planned 8:42 (words ≈ 8:43) · status: **to produce**

| Term | Say |
|---|---|
| GDPR | G D P R |
| DPIA | D P I A |
| CNIC | C N I C |
| DPA | D P A |

| # | Time | Sec | Narration | On screen | Visual |
|---|---|---|---|---|---|
| 1 | 0:00 | 32 | Every AI feature is a data pipeline in disguise. A customer's message goes into your backend, gets combined with documents from your CRM, travels to a model provider, comes back as an answer, and leaves copies in logs, analytics and evaluation sets along the way. Most AI privacy incidents come from flows nobody drew. In this lesson you'll map those flows, apply privacy principles to them, check provider terms, and build a redaction gateway. | Data and privacy architecture<br>• Map every flow<br>• Privacy principles for AI<br>• Provider terms<br>• Risk-reducing patterns | Layout: An animated data flow: a message bubble travels through boxes (Backend, Retrieval, Provider, Output) while ghostly copies drop into boxes labelled Logs, Analytics, Eval set. Timing (32s): 0-3s 'Data and privacy architecture' heading fades in; 3-26s elements build in narration order; 26-32s hold final frame. |
| 2 | 0:32 | 28 | Here's an analogy. Personal data in an AI feature is like water in a building's plumbing. You need to know where every pipe goes, which taps are for drinking water and which aren't, where the shut-off valves are, and where leaks could pool unseen, like under the floor. Logs and evaluation sets are those hidden pools. The data flow map is your plumbing diagram. | Analogy: plumbing<br>• Know every pipe<br>• Shut-off valves = controls<br>• Hidden pools = logs and eval sets | Layout: A building cross-section with blue pipes; a hidden puddle under the floor labelled 'logs' glows; shut-off valve icons labelled 'redaction', 'retention' appear on pipes. Timing (28s): 0-3s 'Analogy: plumbing' heading fades in; 3-22s elements build in narration order; 22-28s hold final frame. |
| 3 | 1:00 | 40 | Start with a diagram. User input to your backend, retrieval from internal sources, prompt assembly, the model provider, output checks, back to the user, and the side branch into logs, analytics and evaluation stores. For every arrow, ask what data travels, how sensitive it is, where it's processed and stored, for how long, and who can access it. Then apply your data classification, public, internal, confidential, restricted, and decide which classes may flow to which AI services. Special categories like health, biometric and financial data carry stricter rules in many jurisdictions. | Map the flow<br>• Every arrow: what, how sensitive, where, how long, who<br>• Classify data<br>• Decide which classes may reach which services | Layout: the flow diagram from scene 1 freezes; each arrow gets a small label card with five fields. A classification legend (public, internal, confidential, restricted) colours the arrows. Timing (40s): 0-3s 'Map the flow' heading fades in; 3-34s elements build in narration order; 34-40s hold final frame. |
| 4 | 1:40 | 43 | Now apply the privacy principles. Have a lawful basis and be transparent, under laws such as UK and EU GDPR and the data protection laws of the UAE, Saudi Arabia, Pakistan and elsewhere as applicable. Minimise: send only what the task needs, often an ID and a few fields. Limit purpose: don't reuse data collected for one purpose to train for another without grounds. Set retention for prompts, outputs and logs. Make sure you can find and delete a person's data, including in logs, evaluation sets and memory. And know where providers process data, since cross-border transfer rules may apply. | Privacy principles for AI<br>• Lawful basis, transparency<br>• Minimisation, purpose limitation<br>• Retention, rights, cross-border transfers | Layout: six small shield icons, each labelled with a principle, arrange around the data flow diagram. Timing (43s): 0-3s 'Privacy principles for AI' heading fades in; 3-37s elements build in narration order; 37-43s hold final frame. |
| 5 | 2:23 | 31 | Check provider terms carefully. Is your data used to train their models? Many business and API offerings default to no, but verify. What are the retention periods for inputs and outputs, and can you shorten them? Where is data processed, and who are the sub-processors? What certifications and audit reports exist? Is there a data processing agreement? Enterprise plan is not an answer to these questions, it's a reason to ask them. | Provider terms checklist<br>• Training use<br>• Retention and options<br>• Residency and sub-processors<br>• Certifications, DPA | Layout: A contract document with five highlighted clauses; a magnifying glass moves over 'training', 'retention', 'region', 'sub-processors', 'DPA'. Timing (31s): 0-3s 'Provider terms checklist' heading fades in; 3-25s elements build in narration order; 25-31s hold final frame. |
| 6 | 2:54 | 38 | Six architecture patterns reduce risk. A redaction gateway masks personal data before prompts leave your environment and restores it only where needed. Permission-aware retrieval filters by the user's access rights, so the AI never sees what the user couldn't open. Tenant isolation separates each client's data, indexes, memories and logs. Secrets never appear in prompts or logs. Private or self-hosted models are used for the most sensitive workloads, where risk justifies the operational effort. And log hygiene: log what you need, masked, access-controlled and deleted on schedule. | Risk-reducing patterns<br>• Redaction gateway • Permission-aware retrieval<br>• Tenant isolation • Secrets outside prompts<br>• Private models where justified • Log hygiene | Layout: architecture diagram with each pattern appearing as a component: a filter box before the provider, a lock on the retrieval box, separate coloured vaults per tenant, a key icon kept outside the prompt. Timing (38s): 0-3s 'Risk-reducing patterns' heading fades in; 3-32s elements build in narration order; 32-38s hold final frame. |
| 7 | 3:32 | 30 | A simple example. A small estate agency adds an AI assistant that drafts replies to tenant emails. Tenants mention phone numbers, flat numbers and sometimes health issues affecting repairs. The team decides: redact phone numbers and ID numbers before the model call, keep tenant health details out of logs, keep logs for thirty days, and add a sentence to their privacy notice. Four decisions, made in an hour, before launch. | Simple example: tenant emails<br>• Redact phones and IDs<br>• No health details in logs<br>• 30-day log retention<br>• Privacy notice updated | Layout: A tenant email passes through a redaction filter with phone number blurred; a log file shows a '30 days' timer; a privacy notice gets one new highlighted line. Timing (30s): 0-3s 'Simple example: tenant emails' heading fades in; 3-24s elements build in narration order; 24-30s hold final frame. |
| 8 | 4:02 | 40 | Here's how it plays out. A company builds an assistant for employees' HR questions. Mapping shows questions may include health and family details. So version one retrieves only from HR policies, no individual records. Identifiers are redacted in logs, which are kept for a short, defined period for quality review. A clear notice explains how questions are processed, and sensitive topics escalate to a human HR partner. The provider's business terms confirm no training on inputs and offer regional processing, and the privacy team signs off a data protection impact assessment before launch. | Worked example: HR assistant<br>• Policies only, no personal records<br>• Redacted logs, short retention<br>• Notice + human escalation<br>• DPIA before launch | Layout: An HR chat mock-up with a small 'How we process your questions' link; a sensitive-topic message triggers a 'Connect me with HR' button. Timing (40s): 0-3s 'Worked example: HR assistant' heading fades in; 3-34s elements build in narration order; 34-40s hold final frame. |
| 9 | 4:42 | 30 | Illustrative numbers for the HR assistant. In the first quarter it answered around four thousand questions. The redaction step masked identifiers in about one question in seven. Logs were deleted after thirty days as planned, and when one employee asked for their data to be removed, the team found and deleted it from logs and the evaluation sample in under an hour, because the data flow map listed every store. | Business example (illustrative)<br>• ~4,000 questions per quarter<br>• ~1 in 7 had identifiers masked<br>• Deletion request done in <1 hour | Layout: data flow diagram with store icons; a deletion request card travels through 'logs', 'eval sample', 'memory', ticking each; a timer shows 'under 1 hour'. Timing (30s): 0-3s 'Business example (illustrative)' heading fades in; 3-24s elements build in narration order; 24-30s hold final frame. |
| 10 | 5:12 | 32 | The hands-on section gives you a minimal redaction gateway in Python, with patterns for emails, phone numbers, UAE Emirates ID and Pakistani CNIC formats and card-like numbers, replacing each with a placeholder and keeping the mapping locally. It's a baseline, not a guarantee, so pair it with entity detection and test on real multilingual data. You'll also see log hygiene written as configuration, and the record of processing every AI feature should have. | Hands-on in the lesson<br>• redact() and restore() with placeholders<br>• Log policy as config<br>• Record of processing per feature | Layout: terminal showing a sentence with a phone number and ID transformed into '[PHONE_3fa2c1]' placeholders; then a YAML logging policy on screen. Timing (32s): 0-3s 'Hands-on in the lesson' heading fades in; 3-26s elements build in narration order; 26-32s hold final frame. |
| 11 | 5:44 | 26 | Common mistakes. Assuming the enterprise plan settles every privacy question. Logging full prompts with personal data forever, because debugging is easier. Building evaluation sets from production data without review. Retrieval that ignores document permissions. Forgetting to include logs and memories when someone asks for their data to be deleted. And involving the privacy team only a week before launch. | Common mistakes<br>• 'Enterprise plan' as the answer<br>• Full prompts logged forever<br>• Unreviewed eval data<br>• Permission-blind retrieval<br>• Deletion misses logs<br>• Late privacy review | Layout: six cards fall onto a desk each with a small icon and a red stamp; the last card shows a calendar with 'privacy review: launch minus 7 days' crossed out. Timing (26s): 0-3s 'Common mistakes' heading fades in; 3-20s elements build in narration order; 20-26s hold final frame. |
| 12 | 6:10 | 28 | How will you know your data architecture is in good shape? Every AI feature has an up-to-date data flow and record of processing. You can answer a customer security questionnaire in hours, not weeks. A deletion request covers logs, evaluation sets and memory, and you've tested it. And a spot check of logs finds no raw personal data where your policy says it shouldn't be. | How you'll know it's in shape<br>• Current flow + record per feature<br>• Security questionnaires in hours<br>• Tested deletion across all stores<br>• Clean log spot checks | Layout: A records folder per feature; a questionnaire marked 'done in 3 hours'; a deletion test ticking off 'database', 'logs', 'eval set', 'memory'. Timing (28s): 0-3s 'How you'll know it's in shape' heading fades in; 3-22s elements build in narration order; 22-28s hold final frame. |
| 13 | 6:38 | 63 | Watch me do it. I open the redaction gateway. First, the patterns: email, phone, the UAE Emirates ID format, the Pakistani CNIC format and card-like numbers. Next, redact loops through each pattern; for every match it creates a placeholder with the label and a short random ID, stores the original in a mapping, and substitutes it in the text. Restore puts back only the labels you allow, email by default. I run it on the sample sentence. The output replaces the phone number and ID with placeholders, and the mapping holds the originals locally, never sent to the provider. Then I open the logging config. Prompts and outputs are stored redacted only, retention is thirty days, access is limited to two groups, and evaluation samples need privacy review with a monthly cap. Finally, I add a row to the record of processing for this feature. | Watch me do it: redaction + log policy<br>• Patterns incl. Emirates ID, CNIC<br>• Placeholders + local mapping<br>• restore(allowed=...)<br>• Logging config as code | Layout: code top, output bottom, YAML right. The sample sentence transforms with placeholders appearing; the mapping dictionary shows originals in a locked panel; the YAML highlights retention_days. Timing (63s): 0-3s 'Watch me do it: redaction + log policy' heading fades in; 3-57s elements build in narration order; 57-63s hold final frame. |
| 14 | 7:41 | 32 | To recap: map every data flow, including logs and evaluation stores. Apply classification, lawful basis, minimisation, purpose limitation, retention and rights. Check provider terms properly. And use redaction, permission-aware retrieval, tenant isolation, secret hygiene and log discipline. This isn't legal advice, so involve your privacy team early. Your next step is to draw the data flow for one AI feature, annotate every arrow, and fix the riskiest one. Next: security threats and vendor risk. | Recap<br>• Map, classify, minimise, retain, delete<br>• Architecture patterns<br>• Next step: draw and annotate one flow | Layout: recap card with the annotated flow diagram; 'Next: Security and vendor risk' title card. Timing (32s): 0-3s 'Recap' heading fades in; 3-26s elements build in narration order; 26-32s hold final frame. |
| 15 | 8:13 | 29 | Try this now. Pick one AI feature and draw its data flow on a single page, including logs, analytics and evaluation stores. Annotate every arrow with what data moves, its sensitivity, where it's processed, how long it's kept and who can access it. Circle the riskiest arrow and write one change that reduces the risk, like redaction or shorter retention. Share it with your privacy lead. | Try this now (30 minutes)<br>• Draw the full flow incl. logs<br>• Annotate every arrow<br>• Fix the riskiest one | Layout: A one-page flow diagram with annotation tags on each arrow; a red circle around one arrow; a note 'Add redaction before provider call'. Timing (29s): 0-3s 'Try this now (30 minutes)' heading fades in; 3-23s elements build in narration order; 23-29s hold final frame. |

<details><summary>Narration per scene (paste into ElevenLabs)</summary>

Scene 1 — Data and privacy architecture

```text
Every AI feature is a data pipeline in disguise. A customer's message goes into your backend, gets combined with documents from your CRM, travels to a model provider, comes back as an answer, and leaves copies in logs, analytics and evaluation sets along the way. Most AI privacy incidents come from flows nobody drew. In this lesson you'll map those flows, apply privacy principles to them, check provider terms, and build a redaction gateway.
```

Scene 2 — Analogy: plumbing

```text
Here's an analogy. Personal data in an AI feature is like water in a building's plumbing. You need to know where every pipe goes, which taps are for drinking water and which aren't, where the shut-off valves are, and where leaks could pool unseen, like under the floor. Logs and evaluation sets are those hidden pools. The data flow map is your plumbing diagram.
```

Scene 3 — Map the flow

```text
Start with a diagram. User input to your backend, retrieval from internal sources, prompt assembly, the model provider, output checks, back to the user, and the side branch into logs, analytics and evaluation stores. For every arrow, ask what data travels, how sensitive it is, where it's processed and stored, for how long, and who can access it. Then apply your data classification, public, internal, confidential, restricted, and decide which classes may flow to which AI services. Special categories like health, biometric and financial data carry stricter rules in many jurisdictions.
```

Scene 4 — Privacy principles for AI

```text
Now apply the privacy principles. Have a lawful basis and be transparent, under laws such as UK and EU GDPR and the data protection laws of the UAE, Saudi Arabia, Pakistan and elsewhere as applicable. Minimise: send only what the task needs, often an ID and a few fields. Limit purpose: don't reuse data collected for one purpose to train for another without grounds. Set retention for prompts, outputs and logs. Make sure you can find and delete a person's data, including in logs, evaluation sets and memory. And know where providers process data, since cross-border transfer rules may apply.
```

Scene 5 — Provider terms checklist

```text
Check provider terms carefully. Is your data used to train their models? Many business and API offerings default to no, but verify. What are the retention periods for inputs and outputs, and can you shorten them? Where is data processed, and who are the sub-processors? What certifications and audit reports exist? Is there a data processing agreement? Enterprise plan is not an answer to these questions, it's a reason to ask them.
```

Scene 6 — Risk-reducing patterns

```text
Six architecture patterns reduce risk. A redaction gateway masks personal data before prompts leave your environment and restores it only where needed. Permission-aware retrieval filters by the user's access rights, so the AI never sees what the user couldn't open. Tenant isolation separates each client's data, indexes, memories and logs. Secrets never appear in prompts or logs. Private or self-hosted models are used for the most sensitive workloads, where risk justifies the operational effort. And log hygiene: log what you need, masked, access-controlled and deleted on schedule.
```

Scene 7 — Simple example: tenant emails

```text
A simple example. A small estate agency adds an AI assistant that drafts replies to tenant emails. Tenants mention phone numbers, flat numbers and sometimes health issues affecting repairs. The team decides: redact phone numbers and ID numbers before the model call, keep tenant health details out of logs, keep logs for thirty days, and add a sentence to their privacy notice. Four decisions, made in an hour, before launch.
```

Scene 8 — Worked example: HR assistant

```text
Here's how it plays out. A company builds an assistant for employees' HR questions. Mapping shows questions may include health and family details. So version one retrieves only from HR policies, no individual records. Identifiers are redacted in logs, which are kept for a short, defined period for quality review. A clear notice explains how questions are processed, and sensitive topics escalate to a human HR partner. The provider's business terms confirm no training on inputs and offer regional processing, and the privacy team signs off a data protection impact assessment before launch.
```

Scene 9 — Business example (illustrative)

```text
Illustrative numbers for the HR assistant. In the first quarter it answered around four thousand questions. The redaction step masked identifiers in about one question in seven. Logs were deleted after thirty days as planned, and when one employee asked for their data to be removed, the team found and deleted it from logs and the evaluation sample in under an hour, because the data flow map listed every store.
```

Scene 10 — Hands-on in the lesson

```text
The hands-on section gives you a minimal redaction gateway in Python, with patterns for emails, phone numbers, UAE Emirates ID and Pakistani CNIC formats and card-like numbers, replacing each with a placeholder and keeping the mapping locally. It's a baseline, not a guarantee, so pair it with entity detection and test on real multilingual data. You'll also see log hygiene written as configuration, and the record of processing every AI feature should have.
```

Scene 11 — Common mistakes

```text
Common mistakes. Assuming the enterprise plan settles every privacy question. Logging full prompts with personal data forever, because debugging is easier. Building evaluation sets from production data without review. Retrieval that ignores document permissions. Forgetting to include logs and memories when someone asks for their data to be deleted. And involving the privacy team only a week before launch.
```

Scene 12 — How you'll know it's in shape

```text
How will you know your data architecture is in good shape? Every AI feature has an up-to-date data flow and record of processing. You can answer a customer security questionnaire in hours, not weeks. A deletion request covers logs, evaluation sets and memory, and you've tested it. And a spot check of logs finds no raw personal data where your policy says it shouldn't be.
```

Scene 13 — Watch me do it: redaction + log policy

```text
Watch me do it. I open the redaction gateway. First, the patterns: email, phone, the UAE Emirates ID format, the Pakistani CNIC format and card-like numbers. Next, redact loops through each pattern; for every match it creates a placeholder with the label and a short random ID, stores the original in a mapping, and substitutes it in the text. Restore puts back only the labels you allow, email by default. I run it on the sample sentence. The output replaces the phone number and ID with placeholders, and the mapping holds the originals locally, never sent to the provider. Then I open the logging config. Prompts and outputs are stored redacted only, retention is thirty days, access is limited to two groups, and evaluation samples need privacy review with a monthly cap. Finally, I add a row to the record of processing for this feature.
```

Scene 14 — Recap

```text
To recap: map every data flow, including logs and evaluation stores. Apply classification, lawful basis, minimisation, purpose limitation, retention and rights. Check provider terms properly. And use redaction, permission-aware retrieval, tenant isolation, secret hygiene and log discipline. This isn't legal advice, so involve your privacy team early. Your next step is to draw the data flow for one AI feature, annotate every arrow, and fix the riskiest one. Next: security threats and vendor risk.
```

Scene 15 — Try this now (30 minutes)

```text
Try this now. Pick one AI feature and draw its data flow on a single page, including logs, analytics and evaluation stores. Annotate every arrow with what data moves, its sensitivity, where it's processed, how long it's kept and who can access it. Circle the riskiest arrow and write one change that reduces the risk, like redaction or shorter retention. Share it with your privacy lead.
```

</details>

### 10. Security threats and vendor risk for AI features

Lesson `security-and-vendor-risk` · module “Data, privacy and security architecture” · target 9 min · 15 scenes · 1204 words · planned 8:41 (words ≈ 8:43) · status: **to produce**

| Term | Say |
|---|---|
| OWASP | oh-wasp |
| SQL | sequel |
| CI | C I |
| MCP | M C P |

| # | Time | Sec | Narration | On screen | Visual |
|---|---|---|---|---|---|
| 1 | 0:00 | 34 | Your security team knows how to protect a web app. But an AI feature adds something new: a component that reads untrusted text and can be talked into doing things. A web page, an email or a customer message can carry instructions aimed at your AI. In this lesson you'll learn the seven AI-specific threats product teams must plan for, the controls that matter most, how to manage vendor and model risk, and how to prepare for AI incidents. | AI security and vendor risk<br>• Seven AI-specific threats<br>• Controls that matter<br>• Vendor risk and incidents | Layout: A familiar web app shield diagram; a new component labelled 'AI' appears with an open ear icon, and a note from outside slides toward it with hidden text. Timing (34s): 0-3s 'AI security and vendor risk' heading fades in; 3-28s elements build in narration order; 28-34s hold final frame. |
| 2 | 0:34 | 31 | Here's an analogy. Traditional security is like locking the doors and windows of a shop. AI adds a new kind of employee who reads every note left on the counter and tries to be helpful. If someone leaves a note saying, the owner says give me the till, a naive helper might try. AI security means limiting what the helper can reach, and never letting notes on the counter count as orders. | Analogy: the helpful new employee<br>• Reads every note on the counter<br>• Notes aren't orders<br>• Limit what they can reach | Layout: A shop counter with a friendly assistant figure; a customer slides a note 'the owner says give me the till'; a locked till and a 'notes are not orders' sign appear. Timing (31s): 0-3s 'Analogy: the helpful new employee' heading fades in; 3-25s elements build in narration order; 25-31s hold final frame. |
| 3 | 1:05 | 35 | First, prompt injection, direct or indirect: inputs or retrieved content that manipulate the model into ignoring instructions or misusing tools. Second, sensitive information disclosure: the model revealing other users' data, internal documents or system prompts through clever questioning. Third, excessive agency: AI features with more permissions or autonomy than needed, turning a manipulation into real damage. And fourth, insecure output handling: passing model output straight into web pages, SQL, shell commands or emails without validation, which enables classic injection attacks. | Threats 1–4<br>• Prompt injection<br>• Sensitive information disclosure<br>• Excessive agency<br>• Insecure output handling | Layout: four threat cards with simple visuals: a hidden instruction in a document, a leaking folder, an oversized key, and model output flowing unfiltered into a database icon. Timing (35s): 0-3s 'Threats 1–4' heading fades in; 3-29s elements build in narration order; 29-35s hold final frame. |
| 4 | 1:40 | 31 | Fifth, supply-chain risk: third-party models, plugins, MCP servers, datasets and libraries that are compromised or low quality. Sixth, data and memory poisoning: malicious content slipped into knowledge bases or memory that later shapes answers. And seventh, denial of wallet: abuse that drives up token usage and costs. Industry resources like OWASP's Top 10 lists for LLM and agentic applications catalogue these, and give your security, product and engineering teams a shared vocabulary. | Threats 5–7<br>• Supply chain<br>• Data and memory poisoning<br>• Denial of wallet<br>• Use OWASP's lists as shared vocabulary | Layout: three more cards: a package with a cracked seal, a knowledge base with a poisoned page, and a wallet draining with a token counter spinning. Timing (31s): 0-3s 'Threats 5–7' heading fades in; 3-25s elements build in narration order; 25-31s hold final frame. |
| 5 | 2:11 | 43 | Now the controls that matter most. Least privilege for every tool and data source. Human approval for consequential actions. Treat model output as untrusted input to other systems: encode it, validate it, parameterise queries. Permission-aware retrieval and tenant isolation. Rate limits and budgets per user and feature. Allow-listed integrations with security review and version pinning. Content provenance: who can add or edit documents the AI trusts? Red-teaming before launch and after major changes. And logging with anomaly detection on tool calls, data access and cost. Never render raw model output as HTML, or let it add arbitrary links and images. | Key controls<br>• Least privilege, approvals<br>• Output treated as untrusted<br>• Budgets, allow-lists, provenance<br>• Red-teaming, logging, anomaly alerts | Layout: A layered control stack builds up beside the AI component; a model output passes through an 'escape + validate + link allow-list' filter before rendering on a web page. Timing (43s): 0-3s 'Key controls' heading fades in; 3-37s elements build in narration order; 37-43s hold final frame. |
| 6 | 2:54 | 37 | Manage vendors and models as a risk too. For each provider, review security posture, data handling terms, how they notify you of model updates and deprecations, whether you can pin versions, availability and rate limits, and concentration risk: if this provider fails or changes terms, what breaks? Have a fallback. It can be as simple as a degraded mode, where the feature switches off gracefully and routes to humans, or as robust as a second provider behind your own abstraction layer, tested with your evaluation set. | Vendor and model risk<br>• Posture, terms, change notices, pinning<br>• Availability and concentration<br>• Fallbacks: degraded mode or second provider | Layout: A provider card with a status light turning red; traffic reroutes either to a 'human queue' or to a second provider box behind an abstraction layer. Timing (37s): 0-3s 'Vendor and model risk' heading fades in; 3-31s elements build in narration order; 31-37s hold final frame. |
| 7 | 3:31 | 30 | A simple example of insecure output handling. An AI writes product descriptions that are published straight onto your website. One day, a supplier's data contains a hidden instruction, and the AI includes a snippet of script in the description. Your site renders it, and visitors' browsers run it. The fix isn't a better prompt. It's treating model output as untrusted: escape it before rendering, and block unexpected links and code. | Simple example: unsafe publishing<br>• AI output published as raw HTML<br>• Hidden instruction sneaks in a script<br>• Fix: escape + block links and code | Layout: A product page renders a description containing a highlighted script tag; a warning shield then shows the same text escaped as harmless plain text. Timing (30s): 0-3s 'Simple example: unsafe publishing' heading fades in; 3-24s elements build in narration order; 24-30s hold final frame. |
| 8 | 4:01 | 36 | Here's a worked example. A B2B company's assistant drafts outreach using CRM data and web research. Threat modelling finds that research pages could carry injected instructions, so web content is treated as untrusted, there's no email-sending tool, only drafts, and links in drafts are allow-listed. CRM access was broader than needed, so it's cut to the fields actually used. Misuse could spike costs, so per-user daily budgets are added. And if the provider goes down, drafting falls back to templates with a notice. | Worked example: outreach assistant<br>• Web content untrusted; drafts only<br>• CRM fields reduced<br>• Per-user budgets<br>• Template fallback | Layout: before-and-after architecture diagram: the 'send email' tool is removed, CRM access shrinks to three fields, a budget meter appears, and a fallback arrow leads to a template library. Timing (36s): 0-3s 'Worked example: outreach assistant' heading fades in; 3-30s elements build in narration order; 30-36s hold final frame. |
| 9 | 4:37 | 29 | Illustrative numbers for the outreach assistant. Over a quarter it drafted about eight thousand emails. Link allow-listing blocked a few dozen drafts containing unexpected domains, several traced to injected text on scraped pages. Per-user budgets stopped one runaway script that would have used a month's budget in a day. And during a four-hour provider outage, template fallback kept the sales team working with a clear notice. | Business example (illustrative)<br>• ~8,000 drafts per quarter<br>• Dozens of bad links blocked<br>• One runaway script stopped<br>• Outage: templates kept work going | Layout: a quarter dashboard with three incident cards 'links blocked', 'budget stop', 'outage fallback', each with a small timeline marker. Timing (29s): 0-3s 'Business example (illustrative)' heading fades in; 3-23s elements build in narration order; 23-29s hold final frame. |
| 10 | 5:06 | 37 | Prepare for AI incidents: harmful or embarrassing outputs, data leaks, runaway costs and compromised integrations. Decide who's on call, how to disable features quickly with kill switches, how to preserve logs, and how to notify users and regulators where required. In the hands-on section, you'll build a prompt-injection regression suite: attack cases for emails, web pages, documents and user messages, with checks in code for forbidden tool calls, unapproved link domains and leaked secrets, run several times each and wired into your release pipeline. | Incident readiness + hands-on<br>• On-call, kill switches, logs, notifications<br>• Injection regression suite in CI<br>• Every real incident becomes a test | Layout: An incident runbook card with four steps; then a CI pipeline where a red 'injection test failed' stops a release. Timing (37s): 0-3s 'Incident readiness + hands-on' heading fades in; 3-31s elements build in narration order; 31-37s hold final frame. |
| 11 | 5:43 | 27 | Common mistakes. Treating the system prompt as a security control. Giving an AI feature access to everything because it's easier to set up. Rendering model output as raw HTML. No budgets, so one abusive user runs up a large bill. Red-teaming only once. And no fallback plan, so when the provider has an outage, the feature simply breaks in front of customers. | Common mistakes<br>• System prompt as security<br>• Access to everything<br>• Raw HTML output<br>• No budgets<br>• One-off red-teaming<br>• No fallback | Layout: A security checklist with six unchecked boxes turning red; a bill icon spikes; a provider status light turns red with a broken UI behind it. Timing (27s): 0-3s 'Common mistakes' heading fades in; 3-21s elements build in narration order; 21-27s hold final frame. |
| 12 | 6:10 | 29 | How will you know your controls are working? Your injection regression suite passes on every release. Tool permissions are reviewed and match what each feature needs. Cost anomalies trigger alerts before the monthly bill does. Your fallback mode has been tested, and you know how long it takes to switch. And your vendor register shows current terms, model change notices and an owner for every AI supplier. | How you'll know it's working<br>• Injection suite passes each release<br>• Permissions match needs<br>• Cost alerts fire early<br>• Fallback tested<br>• Vendor register current | Layout: A release pipeline with a green 'injection suite' stage; a cost chart with an alert marker before a spike; a fallback switch flipping in a timed drill. Timing (29s): 0-3s 'How you'll know it's working' heading fades in; 3-23s elements build in narration order; 23-29s hold final frame. |
| 13 | 6:39 | 65 | Watch me do it. I open the injection suite. First, the attacks: an email postscript telling the assistant to forward the thread, hidden web text asking for all CRM notes, a user asking for the system prompt and API keys, and a markdown image that would leak an email address in a URL. Next, the checks: fail if a forbidden tool like send email was called, fail if any link points outside our allowed domains, and fail if the output looks like it contains a secret. Then run suite sends each attack through our feature three times, because behaviour varies, and collects problems. I run it. One failure: the image attack produced a link to an outside domain. The fix isn't in the prompt. I add link allow-listing to the output handler, re-run, and all pass. Finally, I wire run suite into our release pipeline so any regression blocks deployment. | Watch me do it: injection suite<br>• Four realistic attacks<br>• Code checks: tools, links, secrets<br>• 3 runs each<br>• Fix in output handling; wire into CI | Layout: code left, results JSON right. Highlight ATTACKS, then check(); results show one failure 'unapproved link domain' in red, then an all-clear after the fix; a CI pipeline icon turns green. Timing (65s): 0-3s 'Watch me do it: injection suite' heading fades in; 3-59s elements build in narration order; 59-65s hold final frame. |
| 14 | 7:44 | 30 | To recap: AI adds prompt injection, disclosure, excessive agency, insecure output handling, supply-chain risk, poisoning and denial of wallet. Counter them with least privilege, approvals, untrusted-output handling, budgets, allow-lists, provenance, red-teaming and monitoring. Manage vendors for change, availability and concentration risk, and prepare incident response. Your next step is a thirty-minute threat modelling session on one AI feature: list the top three risks and the control you'll add for each. | Recap<br>• Seven threats, layered controls<br>• Vendor risk + fallbacks<br>• Next step: 30-minute threat model | Layout: recap card; 'Next module: Evaluation and monitoring' title card. Timing (30s): 0-3s 'Recap' heading fades in; 3-24s elements build in narration order; 24-30s hold final frame. |
| 15 | 8:14 | 27 | Try this now. Run a thirty-minute threat modelling session on one AI feature with an engineer and the feature owner. Go through the seven threats and, for each, write whether it applies, an example attack and the current control. Pick the top three risks and one control each. Then write four attack cases for your injection suite based on your feature's real inputs. | Try this now (30 minutes)<br>• Seven threats: applies? example? control?<br>• Top three risks + controls<br>• Four injection test cases | Layout: A threat table with seven rows filling in; three rows highlighted; four test case cards appear beneath. Timing (27s): 0-3s 'Try this now (30 minutes)' heading fades in; 3-21s elements build in narration order; 21-27s hold final frame. |

<details><summary>Narration per scene (paste into ElevenLabs)</summary>

Scene 1 — AI security and vendor risk

```text
Your security team knows how to protect a web app. But an AI feature adds something new: a component that reads untrusted text and can be talked into doing things. A web page, an email or a customer message can carry instructions aimed at your AI. In this lesson you'll learn the seven AI-specific threats product teams must plan for, the controls that matter most, how to manage vendor and model risk, and how to prepare for AI incidents.
```

Scene 2 — Analogy: the helpful new employee

```text
Here's an analogy. Traditional security is like locking the doors and windows of a shop. AI adds a new kind of employee who reads every note left on the counter and tries to be helpful. If someone leaves a note saying, the owner says give me the till, a naive helper might try. AI security means limiting what the helper can reach, and never letting notes on the counter count as orders.
```

Scene 3 — Threats 1–4

```text
First, prompt injection, direct or indirect: inputs or retrieved content that manipulate the model into ignoring instructions or misusing tools. Second, sensitive information disclosure: the model revealing other users' data, internal documents or system prompts through clever questioning. Third, excessive agency: AI features with more permissions or autonomy than needed, turning a manipulation into real damage. And fourth, insecure output handling: passing model output straight into web pages, SQL, shell commands or emails without validation, which enables classic injection attacks.
```

Scene 4 — Threats 5–7

```text
Fifth, supply-chain risk: third-party models, plugins, MCP servers, datasets and libraries that are compromised or low quality. Sixth, data and memory poisoning: malicious content slipped into knowledge bases or memory that later shapes answers. And seventh, denial of wallet: abuse that drives up token usage and costs. Industry resources like OWASP's Top 10 lists for LLM and agentic applications catalogue these, and give your security, product and engineering teams a shared vocabulary.
```

Scene 5 — Key controls

```text
Now the controls that matter most. Least privilege for every tool and data source. Human approval for consequential actions. Treat model output as untrusted input to other systems: encode it, validate it, parameterise queries. Permission-aware retrieval and tenant isolation. Rate limits and budgets per user and feature. Allow-listed integrations with security review and version pinning. Content provenance: who can add or edit documents the AI trusts? Red-teaming before launch and after major changes. And logging with anomaly detection on tool calls, data access and cost. Never render raw model output as HTML, or let it add arbitrary links and images.
```

Scene 6 — Vendor and model risk

```text
Manage vendors and models as a risk too. For each provider, review security posture, data handling terms, how they notify you of model updates and deprecations, whether you can pin versions, availability and rate limits, and concentration risk: if this provider fails or changes terms, what breaks? Have a fallback. It can be as simple as a degraded mode, where the feature switches off gracefully and routes to humans, or as robust as a second provider behind your own abstraction layer, tested with your evaluation set.
```

Scene 7 — Simple example: unsafe publishing

```text
A simple example of insecure output handling. An AI writes product descriptions that are published straight onto your website. One day, a supplier's data contains a hidden instruction, and the AI includes a snippet of script in the description. Your site renders it, and visitors' browsers run it. The fix isn't a better prompt. It's treating model output as untrusted: escape it before rendering, and block unexpected links and code.
```

Scene 8 — Worked example: outreach assistant

```text
Here's a worked example. A B2B company's assistant drafts outreach using CRM data and web research. Threat modelling finds that research pages could carry injected instructions, so web content is treated as untrusted, there's no email-sending tool, only drafts, and links in drafts are allow-listed. CRM access was broader than needed, so it's cut to the fields actually used. Misuse could spike costs, so per-user daily budgets are added. And if the provider goes down, drafting falls back to templates with a notice.
```

Scene 9 — Business example (illustrative)

```text
Illustrative numbers for the outreach assistant. Over a quarter it drafted about eight thousand emails. Link allow-listing blocked a few dozen drafts containing unexpected domains, several traced to injected text on scraped pages. Per-user budgets stopped one runaway script that would have used a month's budget in a day. And during a four-hour provider outage, template fallback kept the sales team working with a clear notice.
```

Scene 10 — Incident readiness + hands-on

```text
Prepare for AI incidents: harmful or embarrassing outputs, data leaks, runaway costs and compromised integrations. Decide who's on call, how to disable features quickly with kill switches, how to preserve logs, and how to notify users and regulators where required. In the hands-on section, you'll build a prompt-injection regression suite: attack cases for emails, web pages, documents and user messages, with checks in code for forbidden tool calls, unapproved link domains and leaked secrets, run several times each and wired into your release pipeline.
```

Scene 11 — Common mistakes

```text
Common mistakes. Treating the system prompt as a security control. Giving an AI feature access to everything because it's easier to set up. Rendering model output as raw HTML. No budgets, so one abusive user runs up a large bill. Red-teaming only once. And no fallback plan, so when the provider has an outage, the feature simply breaks in front of customers.
```

Scene 12 — How you'll know it's working

```text
How will you know your controls are working? Your injection regression suite passes on every release. Tool permissions are reviewed and match what each feature needs. Cost anomalies trigger alerts before the monthly bill does. Your fallback mode has been tested, and you know how long it takes to switch. And your vendor register shows current terms, model change notices and an owner for every AI supplier.
```

Scene 13 — Watch me do it: injection suite

```text
Watch me do it. I open the injection suite. First, the attacks: an email postscript telling the assistant to forward the thread, hidden web text asking for all CRM notes, a user asking for the system prompt and API keys, and a markdown image that would leak an email address in a URL. Next, the checks: fail if a forbidden tool like send email was called, fail if any link points outside our allowed domains, and fail if the output looks like it contains a secret. Then run suite sends each attack through our feature three times, because behaviour varies, and collects problems. I run it. One failure: the image attack produced a link to an outside domain. The fix isn't in the prompt. I add link allow-listing to the output handler, re-run, and all pass. Finally, I wire run suite into our release pipeline so any regression blocks deployment.
```

Scene 14 — Recap

```text
To recap: AI adds prompt injection, disclosure, excessive agency, insecure output handling, supply-chain risk, poisoning and denial of wallet. Counter them with least privilege, approvals, untrusted-output handling, budgets, allow-lists, provenance, red-teaming and monitoring. Manage vendors for change, availability and concentration risk, and prepare incident response. Your next step is a thirty-minute threat modelling session on one AI feature: list the top three risks and the control you'll add for each.
```

Scene 15 — Try this now (30 minutes)

```text
Try this now. Run a thirty-minute threat modelling session on one AI feature with an engineer and the feature owner. Go through the seven threats and, for each, write whether it applies, an example attack and the current control. Pick the top three risks and one control each. Then write four attack cases for your injection suite based on your feature's real inputs.
```

</details>

### 11. Evaluation-driven development

Lesson `evaluation-driven-development` · module “Evaluation-driven development and production monitoring” · target 8 min · 15 scenes · 1154 words · planned 8:21 (words ≈ 8:22) · status: **to produce**

| Term | Say |
|---|---|
| A/B | A B |
| JSON | jay-son |

| # | Time | Sec | Narration | On screen | Visual |
|---|---|---|---|---|---|
| 1 | 0:00 | 38 | In normal software, you write a test once and it passes or fails the same way forever. AI features aren't like that. The same input can produce different outputs. A prompt tweak can fix one case and break three others. A provider model update can shift behaviour overnight. The teams that ship reliable AI treat evaluation as the engine of development. In this lesson you'll learn the loop, the parts of an evaluation system, what product managers own, and how error analysis tells you exactly what to fix. | Evaluation-driven development<br>• Why AI needs a different loop<br>• Evaluation system components<br>• Error analysis and staged rollouts | Layout: two graphs: a flat, predictable line labelled 'traditional software' and a wobbly band labelled 'AI feature', with a small update icon causing a sudden shift. Timing (38s): 0-3s 'Evaluation-driven development' heading fades in; 3-32s elements build in narration order; 32-38s hold final frame. |
| 2 | 0:38 | 25 | Here's an analogy. Building AI without evaluations is like cooking for a restaurant without tasting. You might follow the recipe perfectly, but ingredients vary, ovens differ, and one day the supplier changes the flour. Tasting every batch is how chefs stay consistent. Your evaluation set is the tasting spoon, and you use it every time anything changes. | Analogy: the tasting spoon<br>• Ingredients vary; suppliers change<br>• Taste every batch<br>• Evals = the spoon, used on every change | Layout: A chef tasting from a spoon at each stage; a delivery van labelled 'new model version' arrives and the chef tastes again before serving. Timing (25s): 0-3s 'Analogy: the tasting spoon' heading fades in; 3-19s elements build in narration order; 19-25s hold final frame. |
| 3 | 1:03 | 33 | Here's the loop. Define success criteria from your AI brief. Build an evaluation set of real inputs with grading notes. Build the simplest version that could work. Run the evaluation, read the failures and categorise them. Change one thing: the prompt, retrieval, model, a tool or the UX. Re-run, and keep changes that improve results without regressions. Repeat until you meet launch criteria, and keep running after launch. It's test-driven development, adapted for probabilistic systems. | The loop<br>1 Criteria • 2 Eval set • 3 Simplest version<br>4 Run, read, categorise • 5 Change one thing<br>6 Re-run, keep wins • 7 Repeat after launch | Layout: A circular seven-step loop diagram with an arrow looping back; 'change one thing' is highlighted with a single-knob icon. Timing (33s): 0-3s 'The loop' heading fades in; 3-27s elements build in narration order; 27-33s hold final frame. |
| 4 | 1:36 | 33 | An evaluation system has five parts. Evaluation sets with representative, edge, failure and adversarial cases, tagged by category. Graders: code checks for objective criteria, rubric-based LLM judges calibrated against humans for subjective ones, and human review for samples. A harness that runs a version against the set and reports results by tag, compared with the previous version. Launch thresholds agreed in advance. And regression gates, so changes that drop key metrics below thresholds don't ship. | Five components<br>• Tagged evaluation sets<br>• Graders: code, calibrated judges, humans<br>• A harness with comparisons<br>• Launch thresholds • Regression gates | Layout: five building blocks stack into a machine: 'cases.jsonl', 'graders', 'harness', 'thresholds', 'gate'. A version number enters and a report card exits. Timing (33s): 0-3s 'Five components' heading fades in; 3-27s elements build in narration order; 27-33s hold final frame. |
| 5 | 2:09 | 28 | Evaluation isn't only an engineering job. Product managers and domain experts should own what good means for users, which scenarios matter most, including high-risk ones, the expert grades that LLM judges are calibrated against, and trade-off calls, like whether a small quality gain is worth extra latency. If the people who understand the customer don't own the rubric, you'll optimise the wrong thing very efficiently. | What product owns<br>• Success criteria and rubrics<br>• Case selection<br>• Grading calibration<br>• Trade-off decisions | Layout: A product manager figure (abstract) holding a rubric card; arrows connect it to 'judge calibration' and 'case list' boxes in the machine. Timing (28s): 0-3s 'What product owns' heading fades in; 3-22s elements build in narration order; 22-28s hold final frame. |
| 6 | 2:37 | 42 | Aggregate scores tell you how much. Reading failures tells you why. Take a sample of failures and categorise them: retrieved the wrong document, ignored a policy exception, wrong tone in Arabic replies, missing data in the source system. Fix the biggest category first. You'll often discover some failures aren't AI problems at all. They're data quality, unclear policy or confusing UX. Then confirm offline gains online: shadow mode, where the new version runs silently alongside, staged rollouts to a small share of traffic, and A/B tests with a primary metric, guardrails and duration decided before you start. | Error analysis → online checks<br>• Categorise failures; fix the biggest<br>• Some failures aren't AI problems<br>• Shadow mode, staged rollout, A/B tests | Layout: A bar chart of failure categories sorted by count, the top bar glowing. Then a traffic dial rolling from 5% to 50% to 100% with guardrail lights beside it. Timing (42s): 0-3s 'Error analysis → online checks' heading fades in; 3-36s elements build in narration order; 36-42s hold final frame. |
| 7 | 3:19 | 29 | A simple example. You change one line in a prompt to make replies friendlier. Your evaluation set of sixty cases shows tone scores rise, but three refund-policy answers now leave out the thirty-day window. Without the set, you'd have shipped a friendlier assistant that gets policy wrong. With it, you add an instruction about policy details, re-run, and ship a version that's friendlier and still accurate. | Simple example: a friendlier prompt<br>• Tone up<br>• 3 policy answers now incomplete<br>• Fix, re-run, then ship | Layout: A before/after comparison table with 60 cases: tone column rises green, a policy column shows three red cells; after a fix, all cells turn green. Timing (29s): 0-3s 'Simple example: a friendlier prompt' heading fades in; 3-23s elements build in narration order; 23-29s hold final frame. |
| 8 | 3:48 | 33 | Here's a worked example. A support-reply drafting feature meets its offline quality threshold and launches in pilot. Online, agents edit returns-related drafts far more than expected. Error analysis shows the drafts cite an outdated returns window, because a superseded document is still indexed. The fix is in retrieval, not the prompt, plus a regression case added to the evaluation set. Edit rates for returns fall the next week, and that error can never silently return. | Worked example: support drafts<br>• Offline pass, online edit spike<br>• Root cause: superseded document<br>• Fix retrieval + add regression case | Layout: An edit-rate line chart spikes on 'returns' then drops after a marker labelled 'retrieval fix + regression case'. Timing (33s): 0-3s 'Worked example: support drafts' heading fades in; 3-27s elements build in narration order; 27-33s hold final frame. |
| 9 | 4:21 | 27 | Illustrative numbers for the support drafts. Offline, the pilot passed eighty-eight percent of a hundred and fifty cases. Online, returns drafts were edited in about sixty percent of cases versus twenty-five percent elsewhere. After removing the superseded document and adding three regression cases, returns edits fell to about thirty percent in a week, and the regression cases have passed on every release since. | Business example (illustrative)<br>• Offline pass 88% (150 cases)<br>• Returns edits ~60% vs ~25%<br>• After fix: ~30%, regressions stay green | Layout: edit-rate bars by topic with 'returns' towering; after a 'fix' marker it drops; three regression case cards pinned beneath with green ticks. Timing (27s): 0-3s 'Business example (illustrative)' heading fades in; 3-21s elements build in narration order; 21-27s hold final frame. |
| 10 | 4:48 | 37 | The hands-on section gives you a minimal harness you can run without buying a platform: cases in a JSON lines file, cheap code graders for must-include and must-not-match rules, an LLM judge that returns pass or fail with failed criteria, results saved per version, and a release gate that fails the build if any tag drops more than a few points or safety falls below its floor. Calibrate the judge against fifty human-graded outputs first, and record the judge's model and prompt version with every result. | Hands-on in the lesson<br>• cases.jsonl + code graders + judge<br>• Per-version results by tag<br>• A regression gate for CI | Layout: terminal output of the gate: 'returns: 0.91 -> 0.84' in red and 'exit 1'; then a later run printing 'PASS: no regressions'. Timing (37s): 0-3s 'Hands-on in the lesson' heading fades in; 3-31s elements build in narration order; 31-37s hold final frame. |
| 11 | 5:25 | 25 | Common mistakes. Building the evaluation set after launch. Using only easy, typical cases. Trusting an LLM judge without checking it against human grades. Changing several things at once. Letting the evaluation set go stale while real usage evolves. And keeping evaluation in an engineer's laptop, so product and domain experts never see the results or influence the rubric. | Common mistakes<br>• Evals after launch<br>• Easy cases only<br>• Uncalibrated judges<br>• Many changes at once<br>• Stale eval sets<br>• Hidden from product | Layout: A laptop icon holding an eval spreadsheet with a 'private' lock; the lock breaks open and the sheet projects onto a shared team screen. Timing (25s): 0-3s 'Common mistakes' heading fades in; 3-19s elements build in narration order; 19-25s hold final frame. |
| 12 | 5:50 | 26 | How will you know evaluation-driven development is working? Every change has a before-and-after result on record. Regressions are caught by the gate, not by customers. The share of failures that surprise you in production shrinks over time, because each one becomes a new test case. And product conversations shift from I think it's better to here's what changed, by tag. | How you'll know it's working<br>• Before/after on every change<br>• Gate catches regressions<br>• Fewer production surprises<br>• Decisions by evidence | Layout: A change log where each entry has a small before/after score badge; a 'production surprises' line chart trends down over months. Timing (26s): 0-3s 'How you'll know it's working' heading fades in; 3-20s elements build in narration order; 20-26s hold final frame. |
| 13 | 6:16 | 66 | Watch me do it. I open the harness folder. Cases dot JSON L has one case per line with an ID, input, tags and expectations. In graders, must include checks required phrases, must not include checks forbidden patterns, and judge sends the rubric, input and output to a pinned judge model and parses pass or fail, treating unparseable output as a fail. In run dot py, evaluate runs my system on every case, applies code checks first, calls the judge only if a rubric exists, and saves results per version. By tag computes pass rates. Gate compares the new version with the baseline and fails if any tag drops more than three points or safety falls below one hundred percent. I run version eight against version seven. The output says returns, point nine one to point eight four, and exits with a failure, so the change doesn't ship until I fix it. | Watch me do it: harness + gate<br>• cases.jsonl<br>• Code graders → judge<br>• Results per version, by tag<br>• gate(): exit 1 on regression | Layout: folder tree left, code centre, terminal right. Highlight must_include, judge, then gate; the terminal prints 'returns: 0.91 -> 0.84' in red and 'exit 1'. Timing (66s): 0-3s 'Watch me do it: harness + gate' heading fades in; 3-60s elements build in narration order; 60-66s hold final frame. |
| 14 | 7:22 | 32 | To recap: AI features are probabilistic, so evaluation is the core development loop. Build tagged sets, calibrated graders, a harness, thresholds and gates. Product owns what good means. Error analysis finds the biggest failure categories, and staged rollouts confirm offline gains. Your next step is to categorise thirty failures from a feature or prototype, estimate the fix for each category, and start with the biggest. For more depth, see Evaluating and Monitoring LLM Applications. | Recap<br>• Evaluation is the engine<br>• Read failures, fix the biggest<br>• Next step: categorise 30 failures | Layout: recap card; 'Go deeper: Evaluating and Monitoring LLM Applications' course tile; 'Next: Monitoring in production' title card. Timing (32s): 0-3s 'Recap' heading fades in; 3-26s elements build in narration order; 26-32s hold final frame. |
| 15 | 7:54 | 27 | Try this now. Create a folder with a cases file and write fifteen cases for one AI feature: ten typical, three edge cases and two that should be refused or escalated. For each, write what must be included and what must not appear. Run your current version, record pass or fail, and categorise every failure. That's your baseline and your first improvement backlog. | Try this now (45 minutes)<br>• 15 cases: 10 typical, 3 edge, 2 refuse<br>• must_include / must_not_match<br>• Run, record, categorise failures | Layout: A cases.jsonl file with fifteen lines appearing; a results column filling with pass/fail; a small bar chart of failure categories. Timing (27s): 0-3s 'Try this now (45 minutes)' heading fades in; 3-21s elements build in narration order; 21-27s hold final frame. |

<details><summary>Narration per scene (paste into ElevenLabs)</summary>

Scene 1 — Evaluation-driven development

```text
In normal software, you write a test once and it passes or fails the same way forever. AI features aren't like that. The same input can produce different outputs. A prompt tweak can fix one case and break three others. A provider model update can shift behaviour overnight. The teams that ship reliable AI treat evaluation as the engine of development. In this lesson you'll learn the loop, the parts of an evaluation system, what product managers own, and how error analysis tells you exactly what to fix.
```

Scene 2 — Analogy: the tasting spoon

```text
Here's an analogy. Building AI without evaluations is like cooking for a restaurant without tasting. You might follow the recipe perfectly, but ingredients vary, ovens differ, and one day the supplier changes the flour. Tasting every batch is how chefs stay consistent. Your evaluation set is the tasting spoon, and you use it every time anything changes.
```

Scene 3 — The loop

```text
Here's the loop. Define success criteria from your AI brief. Build an evaluation set of real inputs with grading notes. Build the simplest version that could work. Run the evaluation, read the failures and categorise them. Change one thing: the prompt, retrieval, model, a tool or the UX. Re-run, and keep changes that improve results without regressions. Repeat until you meet launch criteria, and keep running after launch. It's test-driven development, adapted for probabilistic systems.
```

Scene 4 — Five components

```text
An evaluation system has five parts. Evaluation sets with representative, edge, failure and adversarial cases, tagged by category. Graders: code checks for objective criteria, rubric-based LLM judges calibrated against humans for subjective ones, and human review for samples. A harness that runs a version against the set and reports results by tag, compared with the previous version. Launch thresholds agreed in advance. And regression gates, so changes that drop key metrics below thresholds don't ship.
```

Scene 5 — What product owns

```text
Evaluation isn't only an engineering job. Product managers and domain experts should own what good means for users, which scenarios matter most, including high-risk ones, the expert grades that LLM judges are calibrated against, and trade-off calls, like whether a small quality gain is worth extra latency. If the people who understand the customer don't own the rubric, you'll optimise the wrong thing very efficiently.
```

Scene 6 — Error analysis → online checks

```text
Aggregate scores tell you how much. Reading failures tells you why. Take a sample of failures and categorise them: retrieved the wrong document, ignored a policy exception, wrong tone in Arabic replies, missing data in the source system. Fix the biggest category first. You'll often discover some failures aren't AI problems at all. They're data quality, unclear policy or confusing UX. Then confirm offline gains online: shadow mode, where the new version runs silently alongside, staged rollouts to a small share of traffic, and A/B tests with a primary metric, guardrails and duration decided before you start.
```

Scene 7 — Simple example: a friendlier prompt

```text
A simple example. You change one line in a prompt to make replies friendlier. Your evaluation set of sixty cases shows tone scores rise, but three refund-policy answers now leave out the thirty-day window. Without the set, you'd have shipped a friendlier assistant that gets policy wrong. With it, you add an instruction about policy details, re-run, and ship a version that's friendlier and still accurate.
```

Scene 8 — Worked example: support drafts

```text
Here's a worked example. A support-reply drafting feature meets its offline quality threshold and launches in pilot. Online, agents edit returns-related drafts far more than expected. Error analysis shows the drafts cite an outdated returns window, because a superseded document is still indexed. The fix is in retrieval, not the prompt, plus a regression case added to the evaluation set. Edit rates for returns fall the next week, and that error can never silently return.
```

Scene 9 — Business example (illustrative)

```text
Illustrative numbers for the support drafts. Offline, the pilot passed eighty-eight percent of a hundred and fifty cases. Online, returns drafts were edited in about sixty percent of cases versus twenty-five percent elsewhere. After removing the superseded document and adding three regression cases, returns edits fell to about thirty percent in a week, and the regression cases have passed on every release since.
```

Scene 10 — Hands-on in the lesson

```text
The hands-on section gives you a minimal harness you can run without buying a platform: cases in a JSON lines file, cheap code graders for must-include and must-not-match rules, an LLM judge that returns pass or fail with failed criteria, results saved per version, and a release gate that fails the build if any tag drops more than a few points or safety falls below its floor. Calibrate the judge against fifty human-graded outputs first, and record the judge's model and prompt version with every result.
```

Scene 11 — Common mistakes

```text
Common mistakes. Building the evaluation set after launch. Using only easy, typical cases. Trusting an LLM judge without checking it against human grades. Changing several things at once. Letting the evaluation set go stale while real usage evolves. And keeping evaluation in an engineer's laptop, so product and domain experts never see the results or influence the rubric.
```

Scene 12 — How you'll know it's working

```text
How will you know evaluation-driven development is working? Every change has a before-and-after result on record. Regressions are caught by the gate, not by customers. The share of failures that surprise you in production shrinks over time, because each one becomes a new test case. And product conversations shift from I think it's better to here's what changed, by tag.
```

Scene 13 — Watch me do it: harness + gate

```text
Watch me do it. I open the harness folder. Cases dot JSON L has one case per line with an ID, input, tags and expectations. In graders, must include checks required phrases, must not include checks forbidden patterns, and judge sends the rubric, input and output to a pinned judge model and parses pass or fail, treating unparseable output as a fail. In run dot py, evaluate runs my system on every case, applies code checks first, calls the judge only if a rubric exists, and saves results per version. By tag computes pass rates. Gate compares the new version with the baseline and fails if any tag drops more than three points or safety falls below one hundred percent. I run version eight against version seven. The output says returns, point nine one to point eight four, and exits with a failure, so the change doesn't ship until I fix it.
```

Scene 14 — Recap

```text
To recap: AI features are probabilistic, so evaluation is the core development loop. Build tagged sets, calibrated graders, a harness, thresholds and gates. Product owns what good means. Error analysis finds the biggest failure categories, and staged rollouts confirm offline gains. Your next step is to categorise thirty failures from a feature or prototype, estimate the fix for each category, and start with the biggest. For more depth, see Evaluating and Monitoring LLM Applications.
```

Scene 15 — Try this now (45 minutes)

```text
Try this now. Create a folder with a cases file and write fifteen cases for one AI feature: ten typical, three edge cases and two that should be refused or escalated. For each, write what must be included and what must not appear. Run your current version, record pass or fail, and categorise every failure. That's your baseline and your first improvement backlog.
```

</details>

### 12. Monitoring AI features in production

Lesson `monitoring-in-production` · module “Evaluation-driven development and production monitoring” · target 8 min · 15 scenes · 1184 words · planned 8:35 (words ≈ 8:35) · status: **to produce**

| Term | Say |
|---|---|
| OpenTelemetry | open telemetry |
| SQL | sequel |

| # | Time | Sec | Narration | On screen | Visual |
|---|---|---|---|---|---|
| 1 | 0:00 | 35 | Here's a story that happens more often than anyone admits. An AI summarisation feature has a stable thumbs-down rate, so everyone assumes it's fine. Meanwhile, after a provider model update, summaries got shorter and started dropping key figures. Users quietly fixed them by hand for weeks. Nobody noticed, because nobody was watching the right signals. In this lesson you'll learn what to monitor, why implicit signals are gold, how tracing speeds up diagnosis, and how to respond when something breaks. | Monitoring AI in production<br>• Six things to watch<br>• Implicit signals<br>• Tracing, alerts, incidents | Layout: A dashboard with a flat, reassuring thumbs-down line; beneath it, hidden lines for output length and edit rate diverge sharply after a 'model update' marker. Timing (35s): 0-3s 'Monitoring AI in production' heading fades in; 3-29s elements build in narration order; 29-35s hold final frame. |
| 2 | 0:35 | 27 | Here's an analogy. Launching an AI feature without monitoring is like launching a new restaurant and never reading the reviews, checking the bins or counting how much food comes back uneaten. Customers rarely complain directly. They just leave food on the plate and don't come back. In AI features, the uneaten food is the edited draft, the regenerated answer and the escalation. | Analogy: the uneaten food<br>• Customers rarely complain<br>• Edits, regenerations, escalations tell you | Layout: A restaurant table with plates returned half-eaten; each plate morphs into a UI signal icon: 'edited draft', 'regenerate', 'escalate'. Timing (27s): 0-3s 'Analogy: the uneaten food' heading fades in; 3-21s elements build in narration order; 21-27s hold final frame. |
| 3 | 1:02 | 33 | Launch is the beginning. AI features change even when your code doesn't: users bring new questions, content changes, providers update models and costs drift. So monitor six things. Operational health: latency, errors, timeouts, rate limits, fallbacks. Cost: tokens and spend per request, user, feature and successful outcome. Quality: automated checks on live outputs, sampled human review and calibrated judge scores. User signals. Safety and misuse. And drift in inputs and outputs, especially after model or prompt changes. | Six monitoring areas<br>• Operations • Cost • Quality<br>• User signals • Safety • Drift | Layout: six dashboard tiles light up one by one with icons: heartbeat, coin, star, hand, shield, wave. Timing (33s): 0-3s 'Six monitoring areas' heading fades in; 3-27s elements build in narration order; 27-33s hold final frame. |
| 4 | 1:35 | 33 | Explicit feedback is sparse. Most users never click thumbs up or down. Implicit signals scale. Edit distance between the AI draft and what was actually sent shows usefulness. Regenerate clicks suggest dissatisfaction. Escalations to humans show where automation fails. Follow-ups like that's not what I asked reveal misunderstandings. And acceptance rates of suggestions tell you whether the feature earns its place. Define these signals at design time, so the product logs them from day one. | Implicit signals<br>• Edit distance • Regenerations<br>• Escalations • 'That's not what I asked'<br>• Acceptance rate | Layout: A draft email and the sent version side by side with differences highlighted; counters for edits, regenerations and escalations tick upward. Timing (33s): 0-3s 'Implicit signals' heading fades in; 3-27s elements build in narration order; 27-33s hold final frame. |
| 5 | 2:08 | 41 | A practical dashboard shows volume, latency, cost per outcome, a sampled quality score, user signals, safety events, and version markers so you can see changes after every deployment. Alerts fire on error spikes, cost anomalies, quality drops beyond thresholds, safety events and provider failures. Keep thresholds meaningful, or alert fatigue will train everyone to ignore them. For retrieval, chains and agents, log traces: every step's inputs, outputs, timings and versions under one request ID. When a user reports a bad answer, you can see whether retrieval, the model, a tool or post-processing caused it. | Dashboards, alerts, traces<br>• Version markers on every chart<br>• Meaningful alert thresholds<br>• One request ID across every step | Layout: A trace waterfall view: request ID at the top, spans for retrieval, model call, tool call and post-check with timings; one span highlighted red. Timing (41s): 0-3s 'Dashboards, alerts, traces' heading fades in; 3-35s elements build in narration order; 35-41s hold final frame. |
| 6 | 2:49 | 31 | Monitoring needs data, but logs can quietly accumulate personal information. Apply the privacy lesson: minimise, mask, restrict access, set retention and document what you log. Tell users in your privacy notice how interactions are reviewed for quality. And assign ownership. Every AI feature needs a named dashboard owner, a weekly slot to review sampled outputs and trends, and a clear escalation path. A dashboard nobody looks at is just an expensive log. | Privacy and ownership<br>• Mask, restrict, retain, document<br>• Named owner + weekly review<br>• Clear escalation path | Layout: A calendar with a recurring 'AI quality review' slot; a log record with masked fields; an owner badge pinned to the dashboard. Timing (31s): 0-3s 'Privacy and ownership' heading fades in; 3-25s elements build in narration order; 25-31s hold final frame. |
| 7 | 3:20 | 32 | A simple example. Your AI writes product descriptions for an online shop. You log each description's length, whether a person edited it before publishing, and how much they changed. One week, average length drops by a third and edits jump. You check the version log: the provider updated the model on Tuesday. You adjust the prompt to specify length and must-include details, re-run your evaluation set, and redeploy. Found in days, not months. | Simple example: product descriptions<br>• Log length + edits<br>• Length drops, edits jump<br>• Linked to a model update, fixed in days | Layout: A line chart of average length dropping and an edit-rate line rising at a vertical marker labelled 'model update (Tue)'; after a 'prompt fix' marker, both return to normal. Timing (32s): 0-3s 'Simple example: product descriptions' heading fades in; 3-26s elements build in narration order; 26-32s hold final frame. |
| 8 | 3:52 | 40 | Back to our summarisation story, with good monitoring. Output length drops sharply after a provider update, and edit rates rise. Because the prompt version, model and output metrics are logged together, the team links the change to the update within a day, adjusts the prompt to require key figures, re-runs evaluations and redeploys. When something serious happens, follow an incident routine: assess severity, mitigate with a kill switch, rollback or fallback, preserve logs, communicate with affected users and regulators where required, then run a post-incident review that adds new evaluation cases. | Catching it early + incidents<br>• Length and edit-rate alerts<br>• Versions logged together<br>• Assess, mitigate, preserve, communicate, learn | Layout: timeline: 'model update' marker, alert fires, root cause found, prompt fix deployed, edit rate returns to normal. A five-step incident card appears beside it. Timing (40s): 0-3s 'Catching it early + incidents' heading fades in; 3-34s elements build in narration order; 34-40s hold final frame. |
| 9 | 4:32 | 33 | Illustrative numbers for the summarisation story. Before the update, average summaries were about a hundred and eighty words with a twenty percent edit rate. After, about a hundred and ten words and a forty-five percent edit rate. The alert fired on day two. Two days later, with a prompt fix and a passing evaluation run, length and edits were back to normal. Without monitoring, the first sign would have been a client complaint weeks later. | Business example (illustrative)<br>• Length ~180 → ~110 words<br>• Edit rate ~20% → ~45%<br>• Alert day 2; fixed day 4 | Layout: two line charts, length and edit rate, with a 'model update' marker, an 'alert' marker two days later, and a 'fix' marker returning both lines to normal. Timing (33s): 0-3s 'Business example (illustrative)' heading fades in; 3-27s elements build in narration order; 27-33s hold final frame. |
| 10 | 5:05 | 33 | The hands-on section shows one structured log event per AI call, tied together by a request ID and tagged with model and prompt versions, using attribute names that follow OpenTelemetry's generative AI conventions so your data stays portable. It logs token usage, including cached tokens, latency, status and output length, but no raw text. You'll also get a SQL query for a weekly dashboard showing latency, acceptance rate, edit rate and output length by prompt version. | Hands-on in the lesson<br>• One structured event per call<br>• Versions, tokens, latency, status<br>• Weekly dashboard query | Layout: code editor highlighting the log.info JSON block; then a chart built from the SQL query with version markers along the x-axis. Timing (33s): 0-3s 'Hands-on in the lesson' heading fades in; 3-27s elements build in narration order; 27-33s hold final frame. |
| 11 | 5:38 | 23 | Common mistakes. Monitoring only uptime and errors. Relying on thumbs up and down, which few people click. No version markers on charts, so you can't link changes to deployments. Alerts so noisy that everyone mutes them. Logging raw personal data because it's convenient. And a dashboard with no owner and no weekly review slot. | Common mistakes<br>• Uptime-only monitoring<br>• Relying on thumbs<br>• No version markers<br>• Noisy alerts<br>• Raw personal data in logs<br>• No owner | Layout: A dashboard with six problem badges appearing: a lonely uptime chart, an empty thumbs counter, charts without markers, a muted bell, a log with visible personal data, and an empty owner field. Timing (23s): 0-3s 'Common mistakes' heading fades in; 3-17s elements build in narration order; 17-23s hold final frame. |
| 12 | 6:01 | 27 | How will you know your monitoring is good enough? You detect quality drops before users complain. Every chart has version markers. Alerts fire rarely, and when they do, someone acts. A bad answer reported by a user can be traced to its cause in minutes using the request ID. And the weekly review actually happens, with notes on what was found and fixed. | How you'll know it's good enough<br>• Drops found before complaints<br>• Version markers everywhere<br>• Rare, actionable alerts<br>• Minutes to trace a bad answer<br>• Weekly review happens | Layout: A timeline where an internal alert marker appears well before a first 'user complaint' marker; a request ID search returns a full trace in seconds. Timing (27s): 0-3s 'How you'll know it's good enough' heading fades in; 3-21s elements build in narration order; 21-27s hold final frame. |
| 13 | 6:28 | 69 | Watch me do it. I open the logging wrapper. First, I create a request ID and start a timer. Inside the try block I call the model, keep the usage and output, and set the status to the stop reason if it's anything other than a normal end. On an API error, the status records the status code. In the finally block, whatever happened, I log one JSON event: feature, model, prompt version, input, output and cached tokens, latency, status, output length and user segment, and no raw text. Next, the user signals: when an agent sends, edits or rejects the draft, I log a second event with the same request ID. Then the SQL query joins the two, groups by prompt version for the last seven days, and returns calls, slow-tail latency, accept rate, edit rate and average length. I run it and see version seven's edit rate twice version six's, which is my cue to investigate. | Watch me do it: logging + dashboard<br>• request_id + timer<br>• One JSON event in finally<br>• Signals share the request_id<br>• SQL by prompt_version | Layout: Python left, SQL right, result table bottom. Highlight the finally block, then the JOIN; the result table shows version 7 edit rate highlighted beside version 6. Timing (69s): 0-3s 'Watch me do it: logging + dashboard' heading fades in; 3-63s elements build in narration order; 63-69s hold final frame. |
| 14 | 7:37 | 31 | To recap: monitor operations, cost, quality, user signals, safety and drift, not just uptime. Implicit signals scale better than thumbs. Use version markers, meaningful alerts and end-to-end traces, keep logs privacy-safe, give every dashboard an owner, and rehearse incident response. Your next step is to design a dashboard for one AI feature: two metrics in each of the six areas, and three alerts with thresholds. Next module: unit economics and UX. | Recap<br>• Six areas, implicit signals<br>• Traces, owners, incident routine<br>• Next step: design one dashboard | Layout: recap card; 'Next module: Unit economics and UX' title card. Timing (31s): 0-3s 'Recap' heading fades in; 3-25s elements build in narration order; 25-31s hold final frame. |
| 15 | 8:08 | 27 | Try this now. For one AI feature, write the structured log event you'd emit for every call: request ID, feature, model, prompt version, tokens, latency, status and output length. Add three user signals you'll log, like edited, regenerated or escalated. Then sketch a dashboard with two metrics per monitoring area and three alerts with thresholds. Give it an owner and a weekly slot. | Try this now (30 minutes)<br>• Define one log event per call<br>• Three user signals<br>• Dashboard sketch + 3 alerts + owner | Layout: A JSON event template fills in field by field; a dashboard wireframe with six tiles; three alert rules appear with thresholds and an owner avatar. Timing (27s): 0-3s 'Try this now (30 minutes)' heading fades in; 3-21s elements build in narration order; 21-27s hold final frame. |

<details><summary>Narration per scene (paste into ElevenLabs)</summary>

Scene 1 — Monitoring AI in production

```text
Here's a story that happens more often than anyone admits. An AI summarisation feature has a stable thumbs-down rate, so everyone assumes it's fine. Meanwhile, after a provider model update, summaries got shorter and started dropping key figures. Users quietly fixed them by hand for weeks. Nobody noticed, because nobody was watching the right signals. In this lesson you'll learn what to monitor, why implicit signals are gold, how tracing speeds up diagnosis, and how to respond when something breaks.
```

Scene 2 — Analogy: the uneaten food

```text
Here's an analogy. Launching an AI feature without monitoring is like launching a new restaurant and never reading the reviews, checking the bins or counting how much food comes back uneaten. Customers rarely complain directly. They just leave food on the plate and don't come back. In AI features, the uneaten food is the edited draft, the regenerated answer and the escalation.
```

Scene 3 — Six monitoring areas

```text
Launch is the beginning. AI features change even when your code doesn't: users bring new questions, content changes, providers update models and costs drift. So monitor six things. Operational health: latency, errors, timeouts, rate limits, fallbacks. Cost: tokens and spend per request, user, feature and successful outcome. Quality: automated checks on live outputs, sampled human review and calibrated judge scores. User signals. Safety and misuse. And drift in inputs and outputs, especially after model or prompt changes.
```

Scene 4 — Implicit signals

```text
Explicit feedback is sparse. Most users never click thumbs up or down. Implicit signals scale. Edit distance between the AI draft and what was actually sent shows usefulness. Regenerate clicks suggest dissatisfaction. Escalations to humans show where automation fails. Follow-ups like that's not what I asked reveal misunderstandings. And acceptance rates of suggestions tell you whether the feature earns its place. Define these signals at design time, so the product logs them from day one.
```

Scene 5 — Dashboards, alerts, traces

```text
A practical dashboard shows volume, latency, cost per outcome, a sampled quality score, user signals, safety events, and version markers so you can see changes after every deployment. Alerts fire on error spikes, cost anomalies, quality drops beyond thresholds, safety events and provider failures. Keep thresholds meaningful, or alert fatigue will train everyone to ignore them. For retrieval, chains and agents, log traces: every step's inputs, outputs, timings and versions under one request ID. When a user reports a bad answer, you can see whether retrieval, the model, a tool or post-processing caused it.
```

Scene 6 — Privacy and ownership

```text
Monitoring needs data, but logs can quietly accumulate personal information. Apply the privacy lesson: minimise, mask, restrict access, set retention and document what you log. Tell users in your privacy notice how interactions are reviewed for quality. And assign ownership. Every AI feature needs a named dashboard owner, a weekly slot to review sampled outputs and trends, and a clear escalation path. A dashboard nobody looks at is just an expensive log.
```

Scene 7 — Simple example: product descriptions

```text
A simple example. Your AI writes product descriptions for an online shop. You log each description's length, whether a person edited it before publishing, and how much they changed. One week, average length drops by a third and edits jump. You check the version log: the provider updated the model on Tuesday. You adjust the prompt to specify length and must-include details, re-run your evaluation set, and redeploy. Found in days, not months.
```

Scene 8 — Catching it early + incidents

```text
Back to our summarisation story, with good monitoring. Output length drops sharply after a provider update, and edit rates rise. Because the prompt version, model and output metrics are logged together, the team links the change to the update within a day, adjusts the prompt to require key figures, re-runs evaluations and redeploys. When something serious happens, follow an incident routine: assess severity, mitigate with a kill switch, rollback or fallback, preserve logs, communicate with affected users and regulators where required, then run a post-incident review that adds new evaluation cases.
```

Scene 9 — Business example (illustrative)

```text
Illustrative numbers for the summarisation story. Before the update, average summaries were about a hundred and eighty words with a twenty percent edit rate. After, about a hundred and ten words and a forty-five percent edit rate. The alert fired on day two. Two days later, with a prompt fix and a passing evaluation run, length and edits were back to normal. Without monitoring, the first sign would have been a client complaint weeks later.
```

Scene 10 — Hands-on in the lesson

```text
The hands-on section shows one structured log event per AI call, tied together by a request ID and tagged with model and prompt versions, using attribute names that follow OpenTelemetry's generative AI conventions so your data stays portable. It logs token usage, including cached tokens, latency, status and output length, but no raw text. You'll also get a SQL query for a weekly dashboard showing latency, acceptance rate, edit rate and output length by prompt version.
```

Scene 11 — Common mistakes

```text
Common mistakes. Monitoring only uptime and errors. Relying on thumbs up and down, which few people click. No version markers on charts, so you can't link changes to deployments. Alerts so noisy that everyone mutes them. Logging raw personal data because it's convenient. And a dashboard with no owner and no weekly review slot.
```

Scene 12 — How you'll know it's good enough

```text
How will you know your monitoring is good enough? You detect quality drops before users complain. Every chart has version markers. Alerts fire rarely, and when they do, someone acts. A bad answer reported by a user can be traced to its cause in minutes using the request ID. And the weekly review actually happens, with notes on what was found and fixed.
```

Scene 13 — Watch me do it: logging + dashboard

```text
Watch me do it. I open the logging wrapper. First, I create a request ID and start a timer. Inside the try block I call the model, keep the usage and output, and set the status to the stop reason if it's anything other than a normal end. On an API error, the status records the status code. In the finally block, whatever happened, I log one JSON event: feature, model, prompt version, input, output and cached tokens, latency, status, output length and user segment, and no raw text. Next, the user signals: when an agent sends, edits or rejects the draft, I log a second event with the same request ID. Then the SQL query joins the two, groups by prompt version for the last seven days, and returns calls, slow-tail latency, accept rate, edit rate and average length. I run it and see version seven's edit rate twice version six's, which is my cue to investigate.
```

Scene 14 — Recap

```text
To recap: monitor operations, cost, quality, user signals, safety and drift, not just uptime. Implicit signals scale better than thumbs. Use version markers, meaningful alerts and end-to-end traces, keep logs privacy-safe, give every dashboard an owner, and rehearse incident response. Your next step is to design a dashboard for one AI feature: two metrics in each of the six areas, and three alerts with thresholds. Next module: unit economics and UX.
```

Scene 15 — Try this now (30 minutes)

```text
Try this now. For one AI feature, write the structured log event you'd emit for every call: request ID, feature, model, prompt version, tokens, latency, status and output length. Add three user signals you'll log, like edited, regenerated or escalated. Then sketch a dashboard with two metrics per monitoring area and three alerts with thresholds. Give it an owner and a weekly slot.
```

</details>

### 13. Unit economics of AI features

Lesson `unit-economics` · module “Unit economics and UX patterns for AI features” · target 8 min · 15 scenes · 1182 words · planned 8:33 (words ≈ 8:34) · status: **to produce**

| # | Time | Sec | Narration | On screen | Visual |
|---|---|---|---|---|---|
| 1 | 0:00 | 33 | Traditional software features cost almost nothing each time someone clicks them. AI features are different. Every interaction spends tokens, calls models and tools, and sometimes needs a person to review the result. A feature your users love can quietly lose money at scale if nobody models the economics. In this lesson you'll learn the cost components, how to calculate cost per successful outcome, the levers that actually move it, and how to price AI features you sell. | Unit economics of AI features<br>• Variable cost per interaction<br>• Cost per successful outcome<br>• Levers and pricing | Layout: A classic software cost curve flat near zero; beside it an AI feature cost line rising with usage, crossing a revenue line at high volume. Timing (33s): 0-3s 'Unit economics of AI features' heading fades in; 3-27s elements build in narration order; 27-33s hold final frame. |
| 2 | 0:33 | 26 | Here's an analogy. Traditional software is like a toll-free bridge you've already built: more cars cost you almost nothing. AI features are more like a taxi service: every trip burns fuel, and some trips need a second driver to check the route. You can make taxis profitable, but only if you know the cost per trip and price accordingly. | Analogy: bridge vs taxi<br>• Software: near-zero cost per use<br>• AI: fuel per trip + sometimes a second driver | Layout: A bridge with cars crossing freely; a taxi fleet where each trip shows a fuel gauge dropping and occasionally a second passenger labelled 'reviewer'. Timing (26s): 0-3s 'Analogy: bridge vs taxi' heading fades in; 3-20s elements build in narration order; 20-26s hold final frame. |
| 3 | 0:59 | 32 | Here are the components. Model usage: input tokens, output tokens, reasoning tokens, and images or audio processed. Retrieval and storage: embeddings, vector databases and document processing. Tool and API calls to third-party services. Infrastructure: hosting, observability and evaluation runs. Human costs: review, escalations and quality sampling. And development and maintenance: engineering time, prompt and evaluation upkeep. Prices change often and vary by provider, so build a model you can update, rather than memorising numbers. | Cost components<br>• Model usage (incl. reasoning tokens)<br>• Retrieval, storage, tools<br>• Infrastructure, evals<br>• Human review, maintenance | Layout: A stacked cost bar per task with coloured segments for each component; the human review segment is surprisingly large. Timing (32s): 0-3s 'Cost components' heading fades in; 3-26s elements build in narration order; 26-32s hold final frame. |
| 4 | 1:31 | 38 | A simple model works. Cost per task equals model cost, plus retrieval, plus tools, plus human review. Model cost is calls per task times tokens times price. Review cost is review rate times minutes per review times loaded cost per minute. But the number that matters is cost per successful outcome: per resolved ticket, per qualified lead, per processed invoice. Divide by your success rate, because failures still cost money. A cheaper model that fails more often can cost more per outcome after retries, escalations and human time. | Cost per successful outcome<br>• Per-task cost ÷ success rate<br>• Failures, retries and escalations count<br>• Cheap per call ≠ cheap per outcome | Layout: A formula builds on screen; two model options compared: 'cheap model' with low per-call cost but more retries and escalations ends up with a taller bar per outcome. Timing (38s): 0-3s 'Cost per successful outcome' heading fades in; 3-32s elements build in narration order; 32-38s hold final frame. |
| 5 | 2:09 | 36 | Now the levers. Right-size models: small, fast ones for simple steps, larger ones only where they're needed. Route by difficulty. Use prompt caching for stable prefixes like instructions and documents, which providers bill at a fraction of the normal input price. Trim context: shorter prompts, fewer passages, concise tool results. Use batch processing for work that isn't urgent; many providers discount it. Reduce retries with structured outputs. And improve quality to reduce human review, which is often the biggest lever of all. | Levers<br>• Right-size and route<br>• Prompt caching • Trim context<br>• Batch for non-urgent work<br>• Fewer retries • Less review via quality | Layout: A control panel with sliders labelled with each lever; moving 'review rate' down shrinks the cost bar more than any other slider. Timing (36s): 0-3s 'Levers' heading fades in; 3-30s elements build in narration order; 30-36s hold final frame. |
| 6 | 2:45 | 31 | A simple example with illustrative numbers. An AI step costs about two cents per email in model usage. One in ten emails needs a person to spend three minutes fixing it, at about fifty cents a minute. That's fifteen cents of review spread across every email, seven times the model cost. Improve quality so only one in twenty needs fixing, and you save more than switching to a model half the price. | Simple example (illustrative)<br>• Model: ~2¢ per email<br>• Review: 1 in 10 × 3 min × 50¢ = 15¢<br>• Halving review beats halving model price | Layout: A per-email cost bar: a thin '2¢ model' segment and a thick '15¢ review' segment; two alternative bars show 'half-price model' vs 'half the review rate', the second clearly shorter. Timing (31s): 0-3s 'Simple example (illustrative)' heading fades in; 3-25s elements build in narration order; 25-31s hold final frame. |
| 7 | 3:16 | 35 | Here's a worked example. An operations team models invoice extraction. Model and processing cost per invoice is small. Human review of one in five invoices, a few minutes each, dominates the cost. By improving field accuracy, they reduce review to one in ten for trusted suppliers. Total cost per invoice falls far more from that change than it would have from switching to a cheaper model. So they invest in evaluation and quality, not in chasing the lowest token price. | Worked example: invoice extraction<br>• Review dominates cost<br>• Accuracy up → review 20% → 10%<br>• Bigger saving than a cheaper model | Layout: two stacked bars before and after; the review segment halves, while a ghost bar labelled 'cheaper model instead' shows a smaller saving. Timing (35s): 0-3s 'Worked example: invoice extraction' heading fades in; 3-29s elements build in narration order; 29-35s hold final frame. |
| 8 | 3:51 | 32 | More on the invoice team, illustrative. They process about four thousand invoices a month. Model cost is a few pence each; review at twenty percent, four minutes each, is the dominant cost. Better field accuracy cut review to ten percent for trusted suppliers, saving roughly fifty review hours a month. Switching to a model half the price would have saved far less, and their evaluation showed it would have raised the review rate. | Business example (illustrative)<br>• ~4,000 invoices/month<br>• Review 20% → 10%<br>• ~50 review hours saved<br>• Cheaper model would raise review | Layout: stacked cost bar per invoice; the review segment halves; beside it a ghost bar for 'half-price model' with a taller review segment and a red arrow. Timing (32s): 0-3s 'Business example (illustrative)' heading fades in; 3-26s elements build in narration order; 26-32s hold final frame. |
| 9 | 4:23 | 42 | If you sell a product with AI features, pricing must cover variable costs. Bundling into existing plans is simple, but watch heavy users. Usage-based credits are transparent but can deter use. Tiered plans put AI features in higher tiers with limits. Outcome-based pricing, per resolution or per result, aligns with value but shifts risk to you. Protect margins with fair-use limits, rate limits and monitoring of heavy users, and communicate limits clearly. And govern budgets: per-feature budgets and alerts, monthly cost per outcome alongside value delivered, and a review whenever providers change prices or launch new models. | Pricing and budgets<br>• Bundled • Usage-based • Tiered • Outcome-based<br>• Fair-use and rate limits<br>• Monthly cost-per-outcome review | Layout: four pricing cards side by side with pros and cons icons; a budget alert bell rings when a feature's spend crosses a line. Timing (42s): 0-3s 'Pricing and budgets' heading fades in; 3-36s elements build in narration order; 36-42s hold final frame. |
| 10 | 5:05 | 33 | The hands-on section gives you a small Python cost model with prices in a config you update from your provider's pricing page, and parameters for caching share, batch discounts, retries, review rate and success rate. It prints cost per successful outcome for three illustrative scenarios, so you can see which lever moves the result most. In many real features, that's the review rate. Then multiply by volume for low, expected and high adoption, and compare with value. | Hands-on in the lesson<br>• Config-driven cost model<br>• Caching, batch, retries, review, success rate<br>• Scenarios: baseline, cache+route, quality-first | Layout: terminal output listing three scenarios with cost per successful outcome; the 'quality-first' line highlighted. Timing (33s): 0-3s 'Hands-on in the lesson' heading fades in; 3-27s elements build in narration order; 27-33s hold final frame. |
| 11 | 5:38 | 24 | Common mistakes. Modelling only token costs. Using list prices without checking caching or batch discounts. Measuring cost per call instead of per successful outcome. Ignoring heavy users who drive most of the cost. Forgetting evaluation and monitoring in the business case. And never updating the model after launch, while prices, volumes and review rates all change. | Common mistakes<br>• Tokens only<br>• List prices only<br>• Per call, not per outcome<br>• Heavy users ignored<br>• No eval/monitoring costs<br>• Never updated | Layout: A spreadsheet cost model with six cells highlighted in red and small comments attached to each. Timing (24s): 0-3s 'Common mistakes' heading fades in; 3-18s elements build in narration order; 18-24s hold final frame. |
| 12 | 6:02 | 24 | How will you know your economics are healthy? Cost per successful outcome is tracked monthly and trending down or stable. It's comfortably below the value delivered per outcome. Heavy users are visible and within fair-use limits. And your pricing, if you sell the feature, covers variable costs at the high-adoption scenario, not just the expected one. | How you'll know it's healthy<br>• Cost per outcome tracked monthly<br>• Well below value per outcome<br>• Heavy users within limits<br>• Pricing survives high adoption | Layout: A chart with two lines: 'value per outcome' high and flat, 'cost per outcome' lower and gently declining; a gap between them shaded green. Timing (24s): 0-3s 'How you'll know it's healthy' heading fades in; 3-18s elements build in narration order; 18-24s hold final frame. |
| 13 | 6:26 | 67 | Watch me do it. I open unit economics dot py. First, prices live in a config dictionary per model tier, with a cache read multiplier, and a batch discount constant. I update these from the provider's pricing page. Next, model cost applies the cached share of input at the reduced rate, multiplies by calls, and applies the batch discount if used. Then cost per outcome adds model cost, retries, review cost from review rate times minutes times cost per minute, and other costs, and divides by the success rate, because failed tasks still cost money. I run three scenarios. Baseline is the most expensive per successful outcome. Cache plus route is cheaper per call but its lower success rate eats some of the saving. Quality first, with caching, batch and half the review rate, comes out lowest. I multiply by monthly volume for three adoption levels and paste the results into the business case. | Watch me do it: cost model<br>• Prices in config<br>• Caching + batch in model_cost()<br>• ÷ success rate<br>• Three scenarios × volume | Layout: code left, terminal right. Highlight PRICES, the cached_share line, then the division by success_rate; the terminal prints three scenario lines with 'quality-first' lowest in green. Timing (67s): 0-3s 'Watch me do it: cost model' heading fades in; 3-61s elements build in narration order; 61-67s hold final frame. |
| 14 | 7:33 | 31 | To recap: AI features have variable per-use costs. Model all the components, including human review and maintenance. Measure cost per successful outcome, pull the levers that matter, often quality before token price, and price AI features to cover variable costs with sensible limits. Your next step is to build a unit-economics model for one feature with low, expected and high adoption, including review costs. Next: UX patterns that build calibrated trust. | Recap<br>• Cost per successful outcome<br>• Quality is often the biggest lever<br>• Next step: three-scenario model | Layout: recap card; 'Next: UX patterns for AI features' title card. Timing (31s): 0-3s 'Recap' heading fades in; 3-25s elements build in narration order; 25-31s hold final frame. |
| 15 | 8:04 | 29 | Try this now. Take one AI feature and fill in the cost model from the lesson with your own numbers: tokens per task, price, caching share, review rate, minutes per review, loaded cost and success rate. Calculate cost per successful outcome. Then change one lever at a time, model tier, caching or review rate, and note which one moves the number most. That's where to focus. | Try this now (30 minutes)<br>• Fill the model with your numbers<br>• Cost per successful outcome<br>• Move one lever at a time | Layout: the Python cost model on screen; three slider controls labelled 'model tier', 'caching', 'review rate'; a result number updates as each slider moves, with the biggest change highlighted. Timing (29s): 0-3s 'Try this now (30 minutes)' heading fades in; 3-23s elements build in narration order; 23-29s hold final frame. |

<details><summary>Narration per scene (paste into ElevenLabs)</summary>

Scene 1 — Unit economics of AI features

```text
Traditional software features cost almost nothing each time someone clicks them. AI features are different. Every interaction spends tokens, calls models and tools, and sometimes needs a person to review the result. A feature your users love can quietly lose money at scale if nobody models the economics. In this lesson you'll learn the cost components, how to calculate cost per successful outcome, the levers that actually move it, and how to price AI features you sell.
```

Scene 2 — Analogy: bridge vs taxi

```text
Here's an analogy. Traditional software is like a toll-free bridge you've already built: more cars cost you almost nothing. AI features are more like a taxi service: every trip burns fuel, and some trips need a second driver to check the route. You can make taxis profitable, but only if you know the cost per trip and price accordingly.
```

Scene 3 — Cost components

```text
Here are the components. Model usage: input tokens, output tokens, reasoning tokens, and images or audio processed. Retrieval and storage: embeddings, vector databases and document processing. Tool and API calls to third-party services. Infrastructure: hosting, observability and evaluation runs. Human costs: review, escalations and quality sampling. And development and maintenance: engineering time, prompt and evaluation upkeep. Prices change often and vary by provider, so build a model you can update, rather than memorising numbers.
```

Scene 4 — Cost per successful outcome

```text
A simple model works. Cost per task equals model cost, plus retrieval, plus tools, plus human review. Model cost is calls per task times tokens times price. Review cost is review rate times minutes per review times loaded cost per minute. But the number that matters is cost per successful outcome: per resolved ticket, per qualified lead, per processed invoice. Divide by your success rate, because failures still cost money. A cheaper model that fails more often can cost more per outcome after retries, escalations and human time.
```

Scene 5 — Levers

```text
Now the levers. Right-size models: small, fast ones for simple steps, larger ones only where they're needed. Route by difficulty. Use prompt caching for stable prefixes like instructions and documents, which providers bill at a fraction of the normal input price. Trim context: shorter prompts, fewer passages, concise tool results. Use batch processing for work that isn't urgent; many providers discount it. Reduce retries with structured outputs. And improve quality to reduce human review, which is often the biggest lever of all.
```

Scene 6 — Simple example (illustrative)

```text
A simple example with illustrative numbers. An AI step costs about two cents per email in model usage. One in ten emails needs a person to spend three minutes fixing it, at about fifty cents a minute. That's fifteen cents of review spread across every email, seven times the model cost. Improve quality so only one in twenty needs fixing, and you save more than switching to a model half the price.
```

Scene 7 — Worked example: invoice extraction

```text
Here's a worked example. An operations team models invoice extraction. Model and processing cost per invoice is small. Human review of one in five invoices, a few minutes each, dominates the cost. By improving field accuracy, they reduce review to one in ten for trusted suppliers. Total cost per invoice falls far more from that change than it would have from switching to a cheaper model. So they invest in evaluation and quality, not in chasing the lowest token price.
```

Scene 8 — Business example (illustrative)

```text
More on the invoice team, illustrative. They process about four thousand invoices a month. Model cost is a few pence each; review at twenty percent, four minutes each, is the dominant cost. Better field accuracy cut review to ten percent for trusted suppliers, saving roughly fifty review hours a month. Switching to a model half the price would have saved far less, and their evaluation showed it would have raised the review rate.
```

Scene 9 — Pricing and budgets

```text
If you sell a product with AI features, pricing must cover variable costs. Bundling into existing plans is simple, but watch heavy users. Usage-based credits are transparent but can deter use. Tiered plans put AI features in higher tiers with limits. Outcome-based pricing, per resolution or per result, aligns with value but shifts risk to you. Protect margins with fair-use limits, rate limits and monitoring of heavy users, and communicate limits clearly. And govern budgets: per-feature budgets and alerts, monthly cost per outcome alongside value delivered, and a review whenever providers change prices or launch new models.
```

Scene 10 — Hands-on in the lesson

```text
The hands-on section gives you a small Python cost model with prices in a config you update from your provider's pricing page, and parameters for caching share, batch discounts, retries, review rate and success rate. It prints cost per successful outcome for three illustrative scenarios, so you can see which lever moves the result most. In many real features, that's the review rate. Then multiply by volume for low, expected and high adoption, and compare with value.
```

Scene 11 — Common mistakes

```text
Common mistakes. Modelling only token costs. Using list prices without checking caching or batch discounts. Measuring cost per call instead of per successful outcome. Ignoring heavy users who drive most of the cost. Forgetting evaluation and monitoring in the business case. And never updating the model after launch, while prices, volumes and review rates all change.
```

Scene 12 — How you'll know it's healthy

```text
How will you know your economics are healthy? Cost per successful outcome is tracked monthly and trending down or stable. It's comfortably below the value delivered per outcome. Heavy users are visible and within fair-use limits. And your pricing, if you sell the feature, covers variable costs at the high-adoption scenario, not just the expected one.
```

Scene 13 — Watch me do it: cost model

```text
Watch me do it. I open unit economics dot py. First, prices live in a config dictionary per model tier, with a cache read multiplier, and a batch discount constant. I update these from the provider's pricing page. Next, model cost applies the cached share of input at the reduced rate, multiplies by calls, and applies the batch discount if used. Then cost per outcome adds model cost, retries, review cost from review rate times minutes times cost per minute, and other costs, and divides by the success rate, because failed tasks still cost money. I run three scenarios. Baseline is the most expensive per successful outcome. Cache plus route is cheaper per call but its lower success rate eats some of the saving. Quality first, with caching, batch and half the review rate, comes out lowest. I multiply by monthly volume for three adoption levels and paste the results into the business case.
```

Scene 14 — Recap

```text
To recap: AI features have variable per-use costs. Model all the components, including human review and maintenance. Measure cost per successful outcome, pull the levers that matter, often quality before token price, and price AI features to cover variable costs with sensible limits. Your next step is to build a unit-economics model for one feature with low, expected and high adoption, including review costs. Next: UX patterns that build calibrated trust.
```

Scene 15 — Try this now (30 minutes)

```text
Try this now. Take one AI feature and fill in the cost model from the lesson with your own numbers: tokens per task, price, caching share, review rate, minutes per review, loaded cost and success rate. Calculate cost per successful outcome. Then change one lever at a time, model tier, caching or review rate, and note which one moves the number most. That's where to focus.
```

</details>

### 14. UX patterns for AI features

Lesson `ux-patterns-for-ai-features` · module “Unit economics and UX patterns for AI features” · target 9 min · 15 scenes · 1201 words · planned 8:42 (words ≈ 8:42) · status: **to produce**

| Term | Say |
|---|---|
| UX | U X |
| CRM | C R M |

| # | Time | Sec | Narration | On screen | Visual |
|---|---|---|---|---|---|
| 1 | 0:00 | 35 | Traditional software does what it says every time. AI features are sometimes wrong, often variable and hard to predict. That changes the design job. Good AI UX helps people get value when the AI is right, notice when it's wrong, and recover easily. The goal isn't blind trust or reflexive distrust. It's calibrated trust. In this lesson you'll learn ten core patterns, how to choose the right interface, the anti-patterns to avoid, and a practical kit for streaming, sources and microcopy. | UX for AI features<br>• Calibrated trust<br>• Ten core patterns<br>• Interfaces, anti-patterns, microcopy | Layout: A trust meter with three zones: 'blind trust' (red), 'calibrated' (green), 'distrust' (red). The needle settles in the green zone. Timing (35s): 0-3s 'UX for AI features' heading fades in; 3-29s elements build in narration order; 29-35s hold final frame. |
| 2 | 0:35 | 28 | Here's an analogy. Good AI UX is like working with a capable new assistant who shows their workings. They hand you a draft, point to where they found each fact, flag the bit they weren't sure about, and never send anything on your behalf without asking. You trust them more over time because they make checking easy, not because they claim to be perfect. | Analogy: an assistant who shows their workings<br>• Drafts, sources, flagged doubts<br>• Never sends without asking<br>• Trust earned by easy checking | Layout: An assistant figure hands over a document with sticky notes: 'source: policy p.4', 'not sure about this date', and a 'send?' tab awaiting approval. Timing (28s): 0-3s 'Analogy: an assistant who shows their workings' heading fades in; 3-22s elements build in narration order; 22-28s hold final frame. |
| 3 | 1:03 | 38 | The first five patterns. Draft, don't decide: show outputs as editable drafts, and let the user send, publish or approve. Show sources and evidence: citations, highlighted passages and links, so users can verify quickly. Communicate uncertainty and limits honestly, without fake precision like confidence percentages nobody can interpret. Make correction easy: inline editing, regenerate with guidance such as shorter or more formal, and one-click feedback. And preview before action: for sending, updating or booking, show exactly what will happen, require confirmation, and offer undo where you can. | Patterns 1–5<br>• Draft, don't decide<br>• Show sources<br>• Honest uncertainty<br>• Easy correction<br>• Preview before action | Layout: A UI mock-up of an AI draft panel: 'Suggested reply' label, cited source chips, an 'I may be wrong about the date' note, edit and regenerate buttons, and a 'Preview send' dialog. Timing (38s): 0-3s 'Patterns 1–5' heading fades in; 3-32s elements build in narration order; 32-38s hold final frame. |
| 4 | 1:41 | 40 | The next five. Progressive disclosure: the answer first, then reasoning, sources and details on demand. Set expectations at the entry point with example prompts and capability descriptions, which solves the blank-box problem. Fail gracefully: when the AI can't help, say so and offer a path, like search, a human or a form. Design for latency: stream output, show progress on multi-step tasks, and let people cancel. And be transparent: make clear what's AI-generated and when someone is talking to an AI rather than a person, which some regulations and platform policies require. | Patterns 6–10<br>• Progressive disclosure<br>• Set expectations<br>• Graceful failure<br>• Latency design<br>• AI transparency | Layout: A chat entry screen with example prompt chips; a 'not found' state with 'Search' and 'Ask a person' buttons; a progress list 'Searching... Drafting...' with a cancel link. Timing (40s): 0-3s 'Patterns 6–10' heading fades in; 3-34s elements build in narration order; 34-40s hold final frame. |
| 5 | 2:21 | 29 | Not every AI feature should be a chat box. Inline assistance, right inside the document, form or ticket, often wins because the context is already there. Structured outputs, like filled fields, tags and tables, fit workflows better than prose. Background automation with review queues suits high-volume operations. And chat fits open-ended exploration, where people don't know the exact path. Pick the interface that matches the job. | Choose the interface<br>• Inline assistance<br>• Structured outputs<br>• Background + review queue<br>• Chat for exploration | Layout: four small UI thumbnails: an inline suggestion in a text field, auto-filled CRM fields, a review queue list, and a chat window. Timing (29s): 0-3s 'Choose the interface' heading fades in; 3-23s elements build in narration order; 23-29s hold final frame. |
| 6 | 2:50 | 30 | A simple example. An AI feature suggests replies in a help desk. Version one shows a single reply with no context. Agents often ignore it. Version two labels it suggested reply, review before sending, shows the two help articles it used, highlights a date it wasn't sure about, and offers shorter and more formal buttons. The model is identical. Usage and trust both rise, because checking takes seconds. | Simple example: suggested replies<br>• v1: bare reply, ignored<br>• v2: label, sources, flagged date, quick edits<br>• Same model, more trust | Layout: two help-desk panels side by side: a bare text block vs a labelled suggestion with source chips, a highlighted uncertain date and 'Shorter' and 'More formal' buttons. Timing (30s): 0-3s 'Simple example: suggested replies' heading fades in; 3-24s elements build in narration order; 24-30s hold final frame. |
| 7 | 3:20 | 39 | Here's the power of UX. A sales team gets AI call summaries in their CRM. Version one is a long prose summary in a chat panel, and reps rarely read it. Version two fills structured fields automatically: pain points, budget signals, next steps and competitors mentioned, each linked to a timestamp in the recording, editable inline, with a confirm button that logs the activity. Adoption rises. Edit rates reveal budget signals are often wrong, so they're shown as suggested, with the supporting quote. The model didn't change. The UX did. | Worked example: CRM call summaries<br>• v1 prose in chat: ignored<br>• v2 structured fields + timestamps + confirm<br>• Weak field shown as 'suggested' with quote | Layout: before: a wall of text in a side panel with a low usage meter. After: tidy CRM fields with small play icons linking to timestamps, and a 'Suggested' badge on the budget field. Timing (39s): 0-3s 'Worked example: CRM call summaries' heading fades in; 3-33s elements build in narration order; 33-39s hold final frame. |
| 8 | 3:59 | 30 | Illustrative numbers for the CRM summaries. With the prose version, reps opened about one summary in five. With structured fields, about four in five calls ended with fields confirmed. Edit rates showed budget signals were changed in about half of cases, so the team labelled them suggested with the quote, and those edits dropped by half again. Managers also got cleaner pipeline data, because fields could be filtered. | Business example (illustrative)<br>• Prose: ~1 in 5 opened<br>• Fields: ~4 in 5 confirmed<br>• 'Suggested' label halved budget edits | Layout: two panels comparing prose vs structured fields with usage gauges; the budget field gains a 'Suggested' badge and its edit-rate bar halves. Timing (30s): 0-3s 'Business example (illustrative)' heading fades in; 3-24s elements build in narration order; 24-30s hold final frame. |
| 9 | 4:29 | 33 | Avoid the anti-patterns. Overclaiming, where copy implies the AI is always right. Hidden AI, where people can't tell what's generated. Dead ends: sorry, I can't help, with nowhere to go. Anthropomorphism that misleads people into over-trusting or oversharing. And confirmation fatigue from approving trivial steps, which trains people to stop reading the important ones. Build for accessibility too: streaming text, live updates and chat need care for screen-reader users, and support the languages your users actually speak. | Anti-patterns<br>• Overclaiming • Hidden AI • Dead ends<br>• Misleading personas • Confirmation fatigue<br>• Plus: accessibility and languages | Layout: five crossed-out UI snippets: 'Perfect reply!', an unlabeled AI paragraph, 'Sorry, I can't help.', a cartoonish persona, and a stack of approve dialogs. Timing (33s): 0-3s 'Anti-patterns' heading fades in; 3-27s elements build in narration order; 27-33s hold final frame. |
| 10 | 5:02 | 34 | In the hands-on section you'll stream a response with the Claude Python SDK so users see text immediately, and learn to send progress events for multi-step work. You'll get a sources contract, a JSON response shape with an answer, a status of answered, not found or needs human, sources with quotes and dates, limits and next actions, so every answer can show evidence consistently. And a microcopy kit for drafts, not-found states, uncertainty, action previews and AI disclosure. | Hands-on in the lesson<br>• Streaming with the SDK<br>• A sources response contract<br>• Microcopy kit | Layout: code editor showing the stream loop; then a JSON contract; then a table of microcopy lines appearing in a UI mock-up. Timing (34s): 0-3s 'Hands-on in the lesson' heading fades in; 3-28s elements build in narration order; 28-34s hold final frame. |
| 11 | 5:36 | 30 | How will you know your UX works? People use the feature for a high share of eligible tasks. Edit rates show they're engaged but not rewriting everything. In usability tests with deliberately wrong outputs, most people notice the error. Few users hit dead ends. And support tickets about confusing AI behaviour fall. Calibrated trust shows up as fast acceptance of good outputs and quick correction of bad ones. | How you'll know it works<br>• High use on eligible tasks<br>• Moderate, healthy edit rates<br>• Errors noticed in tests<br>• Few dead ends | Layout: A usability test scene: a participant looking at a planted wrong date, which gets circled; metric tiles for usage, edit rate and dead-end rate beside it. Timing (30s): 0-3s 'How you'll know it works' heading fades in; 3-24s elements build in narration order; 24-30s hold final frame. |
| 12 | 6:06 | 28 | One more principle worth remembering: design for the moment it goes wrong. For every AI feature, ask three questions. How will the user notice this is wrong? How do they fix it in one or two actions? And where do they go if the AI can't help at all? If you can't answer all three, the design isn't finished, however good the happy path looks. | Design for 'wrong'<br>• How will they notice?<br>• How do they fix it fast?<br>• Where do they go if AI can't help? | Layout: three large question cards appear one after another; each flips to show a small UI answer: a highlight, an edit button, a 'talk to a person' link. Timing (28s): 0-3s 'Design for 'wrong'' heading fades in; 3-22s elements build in narration order; 22-28s hold final frame. |
| 13 | 6:34 | 68 | Watch me do it. First, streaming. I open the stream call. Inside the with block, I iterate over text stream and forward each chunk, which in a web app goes to the browser as server-sent events. After the loop, get final message gives me the complete response for logging and checks. I run it and text appears word by word instead of after a pause. Next, the sources contract. The backend returns answer, status, sources with ID, title, quote, link and date, limits and actions. The front end shows a sources chip for each source, and when status is not found, it shows the honest not-found state with a search button and an ask-HR action, not a generic apology. Then the microcopy kit: suggested reply, review before sending, based on the last three emails and your returns policy, and the action preview naming the recipient. Finally, I test it with a colleague using a planted wrong date. | Watch me do it: stream + contract + copy<br>• text_stream loop<br>• JSON contract with status<br>• Not-found state UI<br>• Microcopy + planted-error test | Layout: code top-left, JSON contract bottom-left, UI mock right. Text streams into the UI word by word; the UI switches to a 'not found' state with two buttons when status changes. Timing (68s): 0-3s 'Watch me do it: stream + contract + copy' heading fades in; 3-62s elements build in narration order; 62-68s hold final frame. |
| 14 | 7:42 | 33 | To recap: design for calibrated trust, with value when right, visibility when wrong and easy recovery. Use the ten patterns, pick the interface that fits the job, and avoid overclaiming, hidden AI, dead ends and confirmation fatigue. Test with users on failure scenarios, not just happy paths. Your next step is to audit one AI feature against the ten patterns and sketch fixes for the two biggest gaps. Next: designing features where AI takes actions for users. | Recap<br>• Ten patterns, right interface<br>• Test failure scenarios<br>• Next step: audit one feature | Layout: recap card; 'Next: Designing agentic product features' title card. Timing (33s): 0-3s 'Recap' heading fades in; 3-27s elements build in narration order; 27-33s hold final frame. |
| 15 | 8:15 | 27 | Try this now. Pick one AI feature you use or build. Score it against the ten patterns, one to five each. Find the two lowest scores and sketch the fix for each, even on paper. Then show a colleague the feature with one deliberately wrong output and watch, without helping, whether they notice. What they do next tells you more than any survey. | Try this now (30 minutes)<br>• Score the 10 patterns<br>• Sketch fixes for the lowest two<br>• Planted-error test with a colleague | Layout: A ten-row scoring sheet with two low scores circled; two quick paper sketches; a colleague figure studying a screen with a planted error. Timing (27s): 0-3s 'Try this now (30 minutes)' heading fades in; 3-21s elements build in narration order; 21-27s hold final frame. |

<details><summary>Narration per scene (paste into ElevenLabs)</summary>

Scene 1 — UX for AI features

```text
Traditional software does what it says every time. AI features are sometimes wrong, often variable and hard to predict. That changes the design job. Good AI UX helps people get value when the AI is right, notice when it's wrong, and recover easily. The goal isn't blind trust or reflexive distrust. It's calibrated trust. In this lesson you'll learn ten core patterns, how to choose the right interface, the anti-patterns to avoid, and a practical kit for streaming, sources and microcopy.
```

Scene 2 — Analogy: an assistant who shows their workings

```text
Here's an analogy. Good AI UX is like working with a capable new assistant who shows their workings. They hand you a draft, point to where they found each fact, flag the bit they weren't sure about, and never send anything on your behalf without asking. You trust them more over time because they make checking easy, not because they claim to be perfect.
```

Scene 3 — Patterns 1–5

```text
The first five patterns. Draft, don't decide: show outputs as editable drafts, and let the user send, publish or approve. Show sources and evidence: citations, highlighted passages and links, so users can verify quickly. Communicate uncertainty and limits honestly, without fake precision like confidence percentages nobody can interpret. Make correction easy: inline editing, regenerate with guidance such as shorter or more formal, and one-click feedback. And preview before action: for sending, updating or booking, show exactly what will happen, require confirmation, and offer undo where you can.
```

Scene 4 — Patterns 6–10

```text
The next five. Progressive disclosure: the answer first, then reasoning, sources and details on demand. Set expectations at the entry point with example prompts and capability descriptions, which solves the blank-box problem. Fail gracefully: when the AI can't help, say so and offer a path, like search, a human or a form. Design for latency: stream output, show progress on multi-step tasks, and let people cancel. And be transparent: make clear what's AI-generated and when someone is talking to an AI rather than a person, which some regulations and platform policies require.
```

Scene 5 — Choose the interface

```text
Not every AI feature should be a chat box. Inline assistance, right inside the document, form or ticket, often wins because the context is already there. Structured outputs, like filled fields, tags and tables, fit workflows better than prose. Background automation with review queues suits high-volume operations. And chat fits open-ended exploration, where people don't know the exact path. Pick the interface that matches the job.
```

Scene 6 — Simple example: suggested replies

```text
A simple example. An AI feature suggests replies in a help desk. Version one shows a single reply with no context. Agents often ignore it. Version two labels it suggested reply, review before sending, shows the two help articles it used, highlights a date it wasn't sure about, and offers shorter and more formal buttons. The model is identical. Usage and trust both rise, because checking takes seconds.
```

Scene 7 — Worked example: CRM call summaries

```text
Here's the power of UX. A sales team gets AI call summaries in their CRM. Version one is a long prose summary in a chat panel, and reps rarely read it. Version two fills structured fields automatically: pain points, budget signals, next steps and competitors mentioned, each linked to a timestamp in the recording, editable inline, with a confirm button that logs the activity. Adoption rises. Edit rates reveal budget signals are often wrong, so they're shown as suggested, with the supporting quote. The model didn't change. The UX did.
```

Scene 8 — Business example (illustrative)

```text
Illustrative numbers for the CRM summaries. With the prose version, reps opened about one summary in five. With structured fields, about four in five calls ended with fields confirmed. Edit rates showed budget signals were changed in about half of cases, so the team labelled them suggested with the quote, and those edits dropped by half again. Managers also got cleaner pipeline data, because fields could be filtered.
```

Scene 9 — Anti-patterns

```text
Avoid the anti-patterns. Overclaiming, where copy implies the AI is always right. Hidden AI, where people can't tell what's generated. Dead ends: sorry, I can't help, with nowhere to go. Anthropomorphism that misleads people into over-trusting or oversharing. And confirmation fatigue from approving trivial steps, which trains people to stop reading the important ones. Build for accessibility too: streaming text, live updates and chat need care for screen-reader users, and support the languages your users actually speak.
```

Scene 10 — Hands-on in the lesson

```text
In the hands-on section you'll stream a response with the Claude Python SDK so users see text immediately, and learn to send progress events for multi-step work. You'll get a sources contract, a JSON response shape with an answer, a status of answered, not found or needs human, sources with quotes and dates, limits and next actions, so every answer can show evidence consistently. And a microcopy kit for drafts, not-found states, uncertainty, action previews and AI disclosure.
```

Scene 11 — How you'll know it works

```text
How will you know your UX works? People use the feature for a high share of eligible tasks. Edit rates show they're engaged but not rewriting everything. In usability tests with deliberately wrong outputs, most people notice the error. Few users hit dead ends. And support tickets about confusing AI behaviour fall. Calibrated trust shows up as fast acceptance of good outputs and quick correction of bad ones.
```

Scene 12 — Design for 'wrong'

```text
One more principle worth remembering: design for the moment it goes wrong. For every AI feature, ask three questions. How will the user notice this is wrong? How do they fix it in one or two actions? And where do they go if the AI can't help at all? If you can't answer all three, the design isn't finished, however good the happy path looks.
```

Scene 13 — Watch me do it: stream + contract + copy

```text
Watch me do it. First, streaming. I open the stream call. Inside the with block, I iterate over text stream and forward each chunk, which in a web app goes to the browser as server-sent events. After the loop, get final message gives me the complete response for logging and checks. I run it and text appears word by word instead of after a pause. Next, the sources contract. The backend returns answer, status, sources with ID, title, quote, link and date, limits and actions. The front end shows a sources chip for each source, and when status is not found, it shows the honest not-found state with a search button and an ask-HR action, not a generic apology. Then the microcopy kit: suggested reply, review before sending, based on the last three emails and your returns policy, and the action preview naming the recipient. Finally, I test it with a colleague using a planted wrong date.
```

Scene 14 — Recap

```text
To recap: design for calibrated trust, with value when right, visibility when wrong and easy recovery. Use the ten patterns, pick the interface that fits the job, and avoid overclaiming, hidden AI, dead ends and confirmation fatigue. Test with users on failure scenarios, not just happy paths. Your next step is to audit one AI feature against the ten patterns and sketch fixes for the two biggest gaps. Next: designing features where AI takes actions for users.
```

Scene 15 — Try this now (30 minutes)

```text
Try this now. Pick one AI feature you use or build. Score it against the ten patterns, one to five each. Find the two lowest scores and sketch the fix for each, even on paper. Then show a colleague the feature with one deliberately wrong output and watch, without helping, whether they notice. What they do next tells you more than any survey.
```

</details>

### 15. Designing agentic product features users can trust

Lesson `designing-agentic-product-features` · module “Unit economics and UX patterns for AI features” · target 9 min · 15 scenes · 1195 words · planned 8:40 (words ≈ 8:40) · status: **to produce**

| Term | Say |
|---|---|
| SME | S M E |
| MCP | M C P |

| # | Time | Sec | Narration | On screen | Visual |
|---|---|---|---|---|---|
| 1 | 0:00 | 38 | There's a big difference between an AI that suggests and an AI that does. A wrong suggestion costs you a few seconds. A wrong action, like an email to the wrong client, a duplicate payment or a deleted record, can cost money, relationships and trust. More and more products now include features where AI acts on the user's behalf. In this lesson you'll learn the delegation contract behind every agentic feature, the design patterns that make them trustworthy, and a plan schema you can enforce in code. | Agentic product features<br>• Suggest vs do<br>• The delegation contract<br>• Patterns users trust | Layout: two buttons side by side: 'Suggest' with a small lightbulb and 'Do it for me' with a robotic arm; the second glows with a caution outline. Timing (38s): 0-3s 'Agentic product features' heading fades in; 3-32s elements build in narration order; 32-38s hold final frame. |
| 2 | 0:38 | 30 | Here's an analogy. Giving an AI permission to act is like giving a house-sitter your keys. You tell them which rooms they can use, what they may spend on groceries, which deliveries to accept, and to call you before letting anyone in. You'd like a note of what happened each day. And you'd want to be able to take the keys back instantly. Agentic features need exactly that contract. | Analogy: the house-sitter<br>• Which rooms, what budget<br>• Call before big decisions<br>• Daily notes; keys back instantly | Layout: A set of house keys handed over with a checklist card: 'kitchen yes, study no', 'budget £50', 'call before letting anyone in', 'daily log'; a 'revoke' button glows on the keyring. Timing (30s): 0-3s 'Analogy: the house-sitter' heading fades in; 3-24s elements build in narration order; 24-30s hold final frame. |
| 3 | 1:08 | 32 | Every agentic feature is a delegation from a person to software, so make the contract explicit. Scope: what it may do, and what it may never do. Authority: whose permissions it uses, which should be the user's delegated, scoped permissions, never broader. Limits: amounts, volumes, recipients and time windows. Checkpoints: which actions need confirmation. Visibility: how the user sees what happened and why. And reversibility: how actions are undone, and which can't be. | The delegation contract<br>• Scope • Authority • Limits<br>• Checkpoints • Visibility • Reversibility | Layout: A contract document with six labelled clauses appearing one by one, signed by a user icon and an agent icon. Timing (32s): 0-3s 'The delegation contract' heading fades in; 3-26s elements build in narration order; 26-32s hold final frame. |
| 4 | 1:40 | 34 | First pattern: plan, then act. For multi-step tasks, show the plan before doing anything. I'll check three suppliers, request quotes from those under your budget, and draft a summary. Let the user edit it. Plans surface misunderstandings while they're still free to fix. Second: tiered confirmation. Reading and drafting need none. Reversible, low-impact actions get batch confirmation or an undo window. Irreversible or high-impact actions, like payments, external messages, deletions and commitments, need explicit confirmation showing the exact details. | Plan-then-act + tiered confirmation<br>• Editable plan preview<br>• None / undo window / explicit confirm<br>• By impact and reversibility | Layout: A plan card with three editable steps and an 'Edit' pencil; then a three-tier ladder of confirmation types with examples on each rung. Timing (34s): 0-3s 'Plan-then-act + tiered confirmation' heading fades in; 3-28s elements build in narration order; 28-34s hold final frame. |
| 5 | 2:14 | 42 | Third: autonomy settings that users control. Let people choose always ask, ask only for new recipients or amounts over a threshold, or act and notify, per action type. Default to conservative and let trust be earned. Fourth: a plain-language activity log. Ten forty-two, sent quote request to Al Noor Supplies, approved by you at ten forty-one, because it's under your budget. With links to the records. Fifth: undo and recovery, like delayed sends, soft deletes and version history. And sixth: interruptibility, so users can pause or stop any running task and see exactly where it stopped. | More patterns<br>• User-controlled autonomy settings<br>• Plain-language activity log<br>• Undo and recovery<br>• Pause and stop anytime | Layout: A settings panel with per-action toggles; an activity log timeline with timestamps and reasons; an 'Undo (9:58 left)' toast; a prominent 'Stop' button on a running task. Timing (42s): 0-3s 'More patterns' heading fades in; 3-36s elements build in narration order; 36-42s hold final frame. |
| 6 | 2:56 | 37 | And one more: graceful hand-back. When the agent is stuck or unsure, it should stop and ask, with context, rather than guess. Agents that operate websites and desktop apps like a person, called computer-use or browser agents, can automate work where no API exists. But they're slower, more error-prone and exposed to instructions hidden in web pages. Run them in isolated environments, restrict which sites and accounts they can reach, require confirmation for purchases, logins and submissions, and prefer APIs or MCP tools whenever they exist. | Hand-back + computer-use agents<br>• Stop and ask when unsure<br>• Isolate, restrict reach<br>• Confirm purchases, logins, submissions<br>• Prefer APIs/MCP | Layout: An agent cursor moving across a mock website inside a sandbox frame; a 'Confirm purchase?' dialog blocks the final click; an API plug icon offers a shortcut. Timing (37s): 0-3s 'Hand-back + computer-use agents' heading fades in; 3-31s elements build in narration order; 31-37s hold final frame. |
| 7 | 3:33 | 30 | A simple example. A calendar assistant can schedule meetings for a busy consultant. Scope: book internal meetings and hold slots for clients. Authority: the consultant's calendar only. Limits: nothing before nine or after six, no more than three new meetings a day. Checkpoints: any external invite needs a tap to approve. Visibility: a daily summary of what it booked and why. Reversibility: every booking can be undone in one click. | Simple example: a calendar assistant<br>• Internal meetings; client holds<br>• Hours and daily limits<br>• Tap to approve external invites<br>• Daily summary, one-click undo | Layout: A week calendar filling with meeting blocks; an external invite card pauses with an approve button; a daily summary card lists bookings with reasons and undo links. Timing (30s): 0-3s 'Simple example: a calendar assistant' heading fades in; 3-24s elements build in narration order; 24-30s hold final frame. |
| 8 | 4:03 | 46 | Here's a worked example. A B2B platform for small businesses in Saudi Arabia and the UAE adds a procurement assistant. It may search approved suppliers, request quotes and draft purchase orders, but never pay or accept terms. It acts with the buyer's own permissions. It contacts at most five suppliers per task, and purchase orders over the buyer's threshold need a manager's approval. It shows a plan preview, uses tiered confirmation, logs every step, offers a ten-minute undo on quote requests and a stop button. In the pilot, plan edits caught most misunderstandings, like the wrong delivery city or quantity units, before any supplier was contacted. | Worked example: SME procurement assistant<br>• Search, quote, draft PO; never pay<br>• Buyer's permissions; 5-supplier cap<br>• Plan edits caught most errors early | Layout: A plan preview where the user corrects 'Delivery: Jeddah' to 'Delivery: Riyadh' and 'units: boxes' to 'units: packs'; a counter labelled 'errors caught before contact' ticks up. Timing (46s): 0-3s 'Worked example: SME procurement assistant' heading fades in; 3-40s elements build in narration order; 40-46s hold final frame. |
| 9 | 4:49 | 30 | Illustrative numbers for the procurement pilot. Over a month, forty buyers ran about three hundred tasks. Roughly one plan in six was edited before execution, mostly delivery city and units, and those edits prevented every one of those errors from reaching a supplier. Undo was used about a dozen times. Two managers raised their approval thresholds after the first fortnight, a sign of trust earned rather than assumed. | Business example (illustrative)<br>• ~300 tasks, 40 buyers<br>• ~1 in 6 plans edited → errors prevented<br>• Undo used ~12 times<br>• Thresholds raised after 2 weeks | Layout: a donut chart with a one-sixth 'plan edited' slice; a counter for 'undo used: 12'; a settings slider for approval threshold moving up on day 14. Timing (30s): 0-3s 'Business example (illustrative)' heading fades in; 3-24s elements build in narration order; 24-30s hold final frame. |
| 10 | 5:19 | 33 | In the hands-on section you'll define a plan schema shared by the model, backend and interface: a goal, steps with action, target, details, reversibility, impact and reason, and a list of assumptions shown to the user to confirm. Code decides which steps need confirmation, based on action type, impact, reversibility and the user's own settings, and rejects plans that exceed limits, like too many quote requests. Every executed step is logged with the approval that authorised it. | Hands-on in the lesson<br>• Shared Plan/Step schema<br>• requires_confirmation() in code<br>• validate_plan() limits | Layout: code editor with the Plan and Step classes; a rendered UI card built from the same schema showing steps and assumptions with confirm buttons. Timing (33s): 0-3s 'Hands-on in the lesson' heading fades in; 3-27s elements build in narration order; 27-33s hold final frame. |
| 11 | 5:52 | 24 | Common mistakes. Using an admin or service account so the agent never lacks permission. Confirmation dialogs that say proceed? without showing the details. Asking for approval on everything, so people stop reading. No undo, even when it was technically easy. Logs written for engineers, not for users. And no way to stop a running task. | Common mistakes<br>• Admin accounts<br>• Detail-free confirmations<br>• Approvals on everything<br>• No undo<br>• Engineer-only logs<br>• No stop button | Layout: six UI anti-pattern thumbnails appear: an admin badge, a bare 'Proceed?' dialog, a pile of approvals, a missing undo, a cryptic log, and a running task with no stop control. Timing (24s): 0-3s 'Common mistakes' heading fades in; 3-18s elements build in narration order; 18-24s hold final frame. |
| 12 | 6:16 | 26 | How will you know users trust the feature appropriately? They loosen autonomy settings over time for routine actions, but keep approvals for high-impact ones. Plan edits catch mistakes early. Undo is used occasionally, not constantly. Support tickets about unexpected actions are rare. And when something goes wrong, the activity log lets the user understand what happened without contacting you. | How you'll know trust is calibrated<br>• Autonomy loosened for routine, not risky, actions<br>• Plan edits catch errors<br>• Occasional undo<br>• Rare 'unexpected action' tickets | Layout: A settings panel over time: routine action toggles move from 'always ask' to 'act and notify' while a payment toggle stays on 'always ask'; a small 'unexpected action tickets' chart stays low. Timing (26s): 0-3s 'How you'll know trust is calibrated' heading fades in; 3-20s elements build in narration order; 20-26s hold final frame. |
| 13 | 6:42 | 64 | Watch me do it. I open the plan contract. Step has an action from a fixed list, a target, details, whether it's reversible, an impact level and a reason. Plan has a goal, steps and assumptions, which the interface shows prominently. Next, requires confirmation: always for send message, always for high impact or irreversible steps, and otherwise it follows the user's own setting for that action, defaulting to always ask. Then validate plan rejects any plan with more than five quote requests. I generate a plan for twenty boxes of printer paper delivered to Riyadh. The assumptions list says delivery Jeddah. I correct it in the preview. Validation passes; the quote requests are reversible and low impact, and this buyer has set act and notify for them, so they run, while the purchase-order draft waits for confirmation. Every step is logged with the approval that authorised it. | Watch me do it: plan contract<br>• Step + Plan schemas<br>• requires_confirmation() rules<br>• validate_plan() limits<br>• Assumption corrected in preview | Layout: code left, plan preview UI right. Highlight the Step fields, then the confirmation function; the preview shows an assumption 'Jeddah' edited to 'Riyadh', and one step marked 'awaiting confirmation'. Timing (64s): 0-3s 'Watch me do it: plan contract' heading fades in; 3-58s elements build in narration order; 58-64s hold final frame. |
| 14 | 7:46 | 27 | To recap: agentic features are delegations. Make scope, authority, limits, checkpoints, visibility and reversibility explicit. Use plan-then-act, tiered confirmation, user-controlled autonomy, activity logs, undo, interruptibility and graceful hand-back. Treat computer-use agents with extra care. Your next step is to write the delegation contract for one agentic feature and sketch its plan preview and activity log. Next module: governance, adoption and scaling. | Recap<br>• Explicit delegation contract<br>• Patterns for trust<br>• Next step: contract + two screens | Layout: recap card; 'Next module: Governance, adoption and scaling' title card. Timing (27s): 0-3s 'Recap' heading fades in; 3-21s elements build in narration order; 21-27s hold final frame. |
| 15 | 8:13 | 27 | Try this now. Pick one feature where AI could act for users. Write its delegation contract in six lines: scope, authority, limits, checkpoints, visibility and reversibility. Then sketch two screens: the plan preview with editable steps and assumptions, and the activity log entry for one action. Show them to a potential user and ask what they'd want to change before trusting it. | Try this now (30 minutes)<br>• Six-line delegation contract<br>• Sketch plan preview + activity log<br>• Ask a user what they'd change | Layout: A six-line contract card; two wireframe screens drawn side by side; a speech bubble from a user figure with a suggested change. Timing (27s): 0-3s 'Try this now (30 minutes)' heading fades in; 3-21s elements build in narration order; 21-27s hold final frame. |

<details><summary>Narration per scene (paste into ElevenLabs)</summary>

Scene 1 — Agentic product features

```text
There's a big difference between an AI that suggests and an AI that does. A wrong suggestion costs you a few seconds. A wrong action, like an email to the wrong client, a duplicate payment or a deleted record, can cost money, relationships and trust. More and more products now include features where AI acts on the user's behalf. In this lesson you'll learn the delegation contract behind every agentic feature, the design patterns that make them trustworthy, and a plan schema you can enforce in code.
```

Scene 2 — Analogy: the house-sitter

```text
Here's an analogy. Giving an AI permission to act is like giving a house-sitter your keys. You tell them which rooms they can use, what they may spend on groceries, which deliveries to accept, and to call you before letting anyone in. You'd like a note of what happened each day. And you'd want to be able to take the keys back instantly. Agentic features need exactly that contract.
```

Scene 3 — The delegation contract

```text
Every agentic feature is a delegation from a person to software, so make the contract explicit. Scope: what it may do, and what it may never do. Authority: whose permissions it uses, which should be the user's delegated, scoped permissions, never broader. Limits: amounts, volumes, recipients and time windows. Checkpoints: which actions need confirmation. Visibility: how the user sees what happened and why. And reversibility: how actions are undone, and which can't be.
```

Scene 4 — Plan-then-act + tiered confirmation

```text
First pattern: plan, then act. For multi-step tasks, show the plan before doing anything. I'll check three suppliers, request quotes from those under your budget, and draft a summary. Let the user edit it. Plans surface misunderstandings while they're still free to fix. Second: tiered confirmation. Reading and drafting need none. Reversible, low-impact actions get batch confirmation or an undo window. Irreversible or high-impact actions, like payments, external messages, deletions and commitments, need explicit confirmation showing the exact details.
```

Scene 5 — More patterns

```text
Third: autonomy settings that users control. Let people choose always ask, ask only for new recipients or amounts over a threshold, or act and notify, per action type. Default to conservative and let trust be earned. Fourth: a plain-language activity log. Ten forty-two, sent quote request to Al Noor Supplies, approved by you at ten forty-one, because it's under your budget. With links to the records. Fifth: undo and recovery, like delayed sends, soft deletes and version history. And sixth: interruptibility, so users can pause or stop any running task and see exactly where it stopped.
```

Scene 6 — Hand-back + computer-use agents

```text
And one more: graceful hand-back. When the agent is stuck or unsure, it should stop and ask, with context, rather than guess. Agents that operate websites and desktop apps like a person, called computer-use or browser agents, can automate work where no API exists. But they're slower, more error-prone and exposed to instructions hidden in web pages. Run them in isolated environments, restrict which sites and accounts they can reach, require confirmation for purchases, logins and submissions, and prefer APIs or MCP tools whenever they exist.
```

Scene 7 — Simple example: a calendar assistant

```text
A simple example. A calendar assistant can schedule meetings for a busy consultant. Scope: book internal meetings and hold slots for clients. Authority: the consultant's calendar only. Limits: nothing before nine or after six, no more than three new meetings a day. Checkpoints: any external invite needs a tap to approve. Visibility: a daily summary of what it booked and why. Reversibility: every booking can be undone in one click.
```

Scene 8 — Worked example: SME procurement assistant

```text
Here's a worked example. A B2B platform for small businesses in Saudi Arabia and the UAE adds a procurement assistant. It may search approved suppliers, request quotes and draft purchase orders, but never pay or accept terms. It acts with the buyer's own permissions. It contacts at most five suppliers per task, and purchase orders over the buyer's threshold need a manager's approval. It shows a plan preview, uses tiered confirmation, logs every step, offers a ten-minute undo on quote requests and a stop button. In the pilot, plan edits caught most misunderstandings, like the wrong delivery city or quantity units, before any supplier was contacted.
```

Scene 9 — Business example (illustrative)

```text
Illustrative numbers for the procurement pilot. Over a month, forty buyers ran about three hundred tasks. Roughly one plan in six was edited before execution, mostly delivery city and units, and those edits prevented every one of those errors from reaching a supplier. Undo was used about a dozen times. Two managers raised their approval thresholds after the first fortnight, a sign of trust earned rather than assumed.
```

Scene 10 — Hands-on in the lesson

```text
In the hands-on section you'll define a plan schema shared by the model, backend and interface: a goal, steps with action, target, details, reversibility, impact and reason, and a list of assumptions shown to the user to confirm. Code decides which steps need confirmation, based on action type, impact, reversibility and the user's own settings, and rejects plans that exceed limits, like too many quote requests. Every executed step is logged with the approval that authorised it.
```

Scene 11 — Common mistakes

```text
Common mistakes. Using an admin or service account so the agent never lacks permission. Confirmation dialogs that say proceed? without showing the details. Asking for approval on everything, so people stop reading. No undo, even when it was technically easy. Logs written for engineers, not for users. And no way to stop a running task.
```

Scene 12 — How you'll know trust is calibrated

```text
How will you know users trust the feature appropriately? They loosen autonomy settings over time for routine actions, but keep approvals for high-impact ones. Plan edits catch mistakes early. Undo is used occasionally, not constantly. Support tickets about unexpected actions are rare. And when something goes wrong, the activity log lets the user understand what happened without contacting you.
```

Scene 13 — Watch me do it: plan contract

```text
Watch me do it. I open the plan contract. Step has an action from a fixed list, a target, details, whether it's reversible, an impact level and a reason. Plan has a goal, steps and assumptions, which the interface shows prominently. Next, requires confirmation: always for send message, always for high impact or irreversible steps, and otherwise it follows the user's own setting for that action, defaulting to always ask. Then validate plan rejects any plan with more than five quote requests. I generate a plan for twenty boxes of printer paper delivered to Riyadh. The assumptions list says delivery Jeddah. I correct it in the preview. Validation passes; the quote requests are reversible and low impact, and this buyer has set act and notify for them, so they run, while the purchase-order draft waits for confirmation. Every step is logged with the approval that authorised it.
```

Scene 14 — Recap

```text
To recap: agentic features are delegations. Make scope, authority, limits, checkpoints, visibility and reversibility explicit. Use plan-then-act, tiered confirmation, user-controlled autonomy, activity logs, undo, interruptibility and graceful hand-back. Treat computer-use agents with extra care. Your next step is to write the delegation contract for one agentic feature and sketch its plan preview and activity log. Next module: governance, adoption and scaling.
```

Scene 15 — Try this now (30 minutes)

```text
Try this now. Pick one feature where AI could act for users. Write its delegation contract in six lines: scope, authority, limits, checkpoints, visibility and reversibility. Then sketch two screens: the plan preview with editable steps and assumptions, and the activity log entry for one action. Show them to a potential user and ask what they'd want to change before trusting it.
```

</details>

### 16. Operational AI governance for product teams

Lesson `operational-governance` · module “Governance, adoption and scaling” · target 8 min · 15 scenes · 1144 words · planned 8:20 (words ≈ 8:17) · status: **to produce**

| Term | Say |
|---|---|
| NIST | nist |
| ISO/IEC 42001 | I S O I E C forty-two thousand and one |
| DPIA | D P I A |

| # | Time | Sec | Narration | On screen | Visual |
|---|---|---|---|---|---|
| 1 | 0:00 | 32 | Say AI governance in most companies and people picture a committee that says no slowly. Done well, it's the opposite. Clear rules on what's allowed, clear owners, and checks proportionate to risk are what let teams move fast with confidence. In this lesson you'll learn the building blocks of operational AI governance, how risk tiers keep low-risk tools moving while protecting high-risk ones, how regulation fits in, and how to automate the paperwork. | Operational AI governance<br>• Governance that enables<br>• Building blocks and risk tiers<br>• Regulation and automation | Layout: A traffic system: without signals, cars jam; with clear lanes, signals and a fast lane labelled 'low risk', traffic flows smoothly. Timing (32s): 0-3s 'Operational AI governance' heading fades in; 3-26s elements build in narration order; 26-32s hold final frame. |
| 2 | 0:32 | 28 | Here's an analogy. Good governance is like the rules of the road. Speed limits, lanes and traffic lights don't stop people driving; they let millions of people drive fast and safely at the same time. A motorway has different rules from a school street, because the risks differ. Risk tiers are your speed limits: fast lanes for low-risk tools, careful streets for high-risk ones. | Analogy: rules of the road<br>• Rules let everyone move fast safely<br>• Motorway vs school street<br>• Risk tiers = speed limits | Layout: A road map with a motorway (green, fast cars labelled 'Tier 1') and a school street (amber, slow cars labelled 'Tier 3'), each with appropriate signs. Timing (28s): 0-3s 'Analogy: rules of the road' heading fades in; 3-22s elements build in narration order; 22-28s hold final frame. |
| 3 | 1:00 | 37 | There are seven building blocks. An AI use policy: which tools staff may use with which data, what needs approval, and disclosure rules. Short and practical beats long and ignored. An inventory, or register, of every AI system: owner, purpose, users, data, providers and model versions, risk level, evaluation status and review dates. Risk-tiered review. Clear ownership: every feature has one accountable person, not the AI team in general. Standard artefacts with templates. Change management for prompts, models, tools and data sources. And incident management. | Seven building blocks<br>• Use policy • Register • Risk tiers<br>• Ownership • Standard artefacts<br>• Change and incident management | Layout: seven labelled blocks stack into a foundation under a building labelled 'AI features'. Timing (37s): 0-3s 'Seven building blocks' heading fades in; 3-31s elements build in narration order; 31-37s hold final frame. |
| 4 | 1:37 | 30 | Risk tiers make governance proportionate. Tier one, low: internal drafting aids with no personal data, which need a self-assessment checklist. Tier two, medium: customer-facing content with human review, or personal data processing, which needs product, privacy and security review. Tier three, high: decisions affecting people's rights or access, like credit, hiring or health; autonomous actions; or regulated domains. These need a full impact assessment, legal review and formal sign-off. | Risk tiers<br>• Tier 1: checklist<br>• Tier 2: product + privacy + security review<br>• Tier 3: impact assessment, legal, sign-off | Layout: three lanes with increasing numbers of checkpoints: one gate, three gates, five gates. Example feature icons travel down each lane at different speeds. Timing (30s): 0-3s 'Risk tiers' heading fades in; 3-24s elements build in narration order; 24-30s hold final frame. |
| 5 | 2:07 | 46 | Regulation is evolving and varies by jurisdiction. Product teams should know the EU AI Act, a risk-based framework whose obligations phase in over time, with high-risk deadlines postponed by the 2026 Digital Omnibus. Existing data protection laws already apply to AI processing. Sector rules in finance, health and employment matter, as do consumer protection and advertising rules, and national frameworks in places like the UAE, Saudi Arabia and Pakistan. Work with counsel on what applies, and design governance so new requirements can be absorbed. Voluntary frameworks like the NIST AI Risk Management Framework and ISO slash IEC forty-two thousand and one give you useful structure. | Regulation and frameworks<br>• EU AI Act (phased; 2026 changes)<br>• Data protection, sector, consumer rules<br>• NIST AI RMF, ISO/IEC 42001 | Layout: A world map with pins on the EU, UK, UAE, KSA, Pakistan and US, each opening a small card; below, two framework badges labelled 'NIST AI RMF' and 'ISO/IEC 42001'. Timing (46s): 0-3s 'Regulation and frameworks' heading fades in; 3-40s elements build in narration order; 40-46s hold final frame. |
| 6 | 2:53 | 27 | Governance runs across the whole lifecycle. At the idea stage, register the use case with an initial tier. When prototyping, use approved tools and data only. Before launch: evaluation report, tier-appropriate reviews and sign-offs. At launch: monitoring plan, incident runbook and user disclosures. In operation: periodic review, change control and audits. And at retirement: data deletion, communication and a register update. | Across the lifecycle<br>• Idea → Prototype → Pre-launch<br>• Launch → Operation → Retirement | Layout: A horizontal lifecycle arrow with six stages; a small governance icon attaches to each stage as it lights up. Timing (27s): 0-3s 'Across the lifecycle' heading fades in; 3-21s elements build in narration order; 21-27s hold final frame. |
| 7 | 3:20 | 30 | A simple example. A marketing team wants to use an AI tool to brainstorm social captions. No personal data, human-written final posts, internal use only. That's tier one: a self-assessment checklist and an entry in the register, done in a day. The same team later wants AI to reply to customer comments automatically. Customer-facing, public, sometimes complaints: that's at least tier two, with review, monitoring and an owner. | Simple example: two marketing uses<br>• Brainstorming captions → Tier 1, a day<br>• Auto-replying to comments → Tier 2+ | Layout: two request cards enter a sorting machine: 'caption brainstorm' exits into a green Tier 1 tray with a checklist; 'auto-reply to comments' exits into an amber Tier 2 tray with review and monitoring icons. Timing (30s): 0-3s 'Simple example: two marketing uses' heading fades in; 3-24s elements build in narration order; 24-30s hold final frame. |
| 8 | 3:50 | 40 | Here's proportionality in action. A financial services firm wants an assistant that helps advisers draft client suitability notes. That's tier three, a regulated advice context. So: an impact assessment, legal review of record-keeping and advice obligations, advisers remain responsible and review every note, evaluation focused on factual accuracy against client data, audit logging and quarterly review. It takes longer, but it launches with confidence and survives its first regulatory audit. Meanwhile, a tier one internal meeting-notes tool goes live in two weeks with a checklist. Both happen, because governance is proportionate. | Worked example: two speeds<br>• Tier 3 advice drafter: full review, launches confidently<br>• Tier 1 meeting notes: 2 weeks with a checklist | Layout: split screen: a slow, thorough lane with five checkpoints for the advice drafter; a fast lane with one checkpoint for the notes tool; both reach 'Live'. Timing (40s): 0-3s 'Worked example: two speeds' heading fades in; 3-34s elements build in narration order; 34-40s hold final frame. |
| 9 | 4:30 | 29 | Illustrative numbers for the two-speed firm. In one year it registered twenty-six AI uses: eighteen tier one, six tier two, two tier three. Tier one items went live in a median of four days, tier two in about three weeks, and tier three in about three months. The register made the first regulatory information request easy: they answered in an afternoon, with links to every artefact. | Business example (illustrative)<br>• 26 AI uses: 18 / 6 / 2 by tier<br>• Median go-live: 4 days / 3 weeks / 3 months<br>• Regulator request answered in an afternoon | Layout: three stacked lanes by tier with counts and median go-live times; a clock labelled 'regulator request: 1 afternoon' beside the register icon. Timing (29s): 0-3s 'Business example (illustrative)' heading fades in; 3-23s elements build in narration order; 23-29s hold final frame. |
| 10 | 4:59 | 32 | The hands-on section turns your register into something tools can check. Each entry is structured: owner, purpose, data categories, providers and regions, autonomy, risk tier, links to artefacts and review dates. A short Python script recalculates each entry's likely tier, flags missing artefacts for that tier, overdue reviews and missing owners, and prints a weekly report for your governance channel. Governance that's checked automatically gets followed. Governance that lives in a forgotten document doesn't. | Hands-on in the lesson<br>• Structured AI register entries<br>• Weekly automated checks<br>• Tier, artefacts, reviews, owners | Layout: A YAML register entry on screen; then a terminal listing 'AI-014 OK', 'AI-022 missing artefacts: [dpia]', 'AI-031 review overdue'. Timing (32s): 0-3s 'Hands-on in the lesson' heading fades in; 3-26s elements build in narration order; 26-32s hold final frame. |
| 11 | 5:31 | 24 | Common mistakes. A single review board for everything, which becomes a bottleneck. A long policy nobody reads. No register, so nobody knows what AI is in use. Governance that stops at launch. Treating vendor tools as outside governance because someone else built them. And documents that duplicate each other instead of templates that feed one register. | Common mistakes<br>• One board for everything<br>• Unread policies<br>• No register<br>• Stops at launch<br>• Vendor tools ignored<br>• Duplicated paperwork | Layout: A bottleneck funnel jammed with request cards; a thick unread policy binder; an empty register sheet; each tagged with a red label. Timing (24s): 0-3s 'Common mistakes' heading fades in; 3-18s elements build in narration order; 18-24s hold final frame. |
| 12 | 5:55 | 27 | How will you know your governance works? Low-risk tools go live quickly, high-risk ones get proper scrutiny, and people describe the process as helpful rather than obstructive. The register is complete and current. Reviews happen on schedule. And when a regulator, client or auditor asks what AI you use and how it's controlled, you can answer from the register in an afternoon. | How you'll know it works<br>• Fast lane for low risk<br>• Complete, current register<br>• Reviews on schedule<br>• Audit answers in an afternoon | Layout: A timeline showing Tier 1 approvals in days and Tier 3 in weeks; a register with all rows green; a clock showing 'audit response: 3 hours'. Timing (27s): 0-3s 'How you'll know it works' heading fades in; 3-21s elements build in narration order; 21-27s hold final frame. |
| 13 | 6:22 | 61 | Watch me do it. I open ai-register dot yaml. Each entry has an ID, name, owner, purpose, users, who's affected, data categories, providers with region and model, autonomy, risk tier, links to artefacts and review dates. Next, the check script. Required-by-tier lists the artefacts each tier needs: a brief for tier one, up to brief, data flow, evaluation report, impact assessment and runbook for tier three. Tier-for recalculates the likely tier: full autonomy or decisions about people means three; personal data or customers affected means at least two. Then the loop compares recorded and calculated tiers, lists missing artefacts, flags overdue reviews and missing owners. I run it on our register. AI fourteen is OK. AI twenty-two is missing its impact assessment. AI thirty-one's review is overdue. I post the output in the governance channel and assign the two fixes. | Watch me do it: register + checks<br>• Structured entries<br>• REQUIRED_BY_TIER<br>• tier_for() recalculation<br>• Weekly output to governance channel | Layout: YAML left, script centre, terminal right. Highlight REQUIRED_BY_TIER then tier_for; the terminal prints three lines, one OK, one missing dpia, one overdue, each with a coloured badge. Timing (61s): 0-3s 'Watch me do it: register + checks' heading fades in; 3-55s elements build in narration order; 55-61s hold final frame. |
| 14 | 7:23 | 30 | To recap: good governance enables speed. Build a use policy, a register, risk tiers, clear ownership, standard artefacts, and change and incident management. Keep up with regulation through counsel and design for change. Embed governance across the lifecycle and automate the checks. Your next step: draft a one-page risk-tier policy and classify three AI uses you know. Next, we'll look at AI regulation for product teams in more detail. | Recap<br>• Proportionate, automated governance<br>• Next step: risk-tier policy + three classifications | Layout: recap card; 'Next: AI regulation for product teams' title card. Timing (30s): 0-3s 'Recap' heading fades in; 3-24s elements build in narration order; 24-30s hold final frame. |
| 15 | 7:53 | 27 | Try this now. Write a one-page risk-tier policy with three tiers, examples and the review each needs. Then list three AI uses in your organisation and classify each. Create register entries for them with an owner, purpose, data categories, providers, autonomy, tier and next review date. If you have a few more minutes, run the register check script on those entries. | Try this now (40 minutes)<br>• One-page tier policy<br>• Classify three AI uses<br>• Register entries + run the check | Layout: A tier policy page; three use-case cards sliding into tier columns; three register rows appearing; a terminal printing 'OK' or issues for each. Timing (27s): 0-3s 'Try this now (40 minutes)' heading fades in; 3-21s elements build in narration order; 21-27s hold final frame. |

<details><summary>Narration per scene (paste into ElevenLabs)</summary>

Scene 1 — Operational AI governance

```text
Say AI governance in most companies and people picture a committee that says no slowly. Done well, it's the opposite. Clear rules on what's allowed, clear owners, and checks proportionate to risk are what let teams move fast with confidence. In this lesson you'll learn the building blocks of operational AI governance, how risk tiers keep low-risk tools moving while protecting high-risk ones, how regulation fits in, and how to automate the paperwork.
```

Scene 2 — Analogy: rules of the road

```text
Here's an analogy. Good governance is like the rules of the road. Speed limits, lanes and traffic lights don't stop people driving; they let millions of people drive fast and safely at the same time. A motorway has different rules from a school street, because the risks differ. Risk tiers are your speed limits: fast lanes for low-risk tools, careful streets for high-risk ones.
```

Scene 3 — Seven building blocks

```text
There are seven building blocks. An AI use policy: which tools staff may use with which data, what needs approval, and disclosure rules. Short and practical beats long and ignored. An inventory, or register, of every AI system: owner, purpose, users, data, providers and model versions, risk level, evaluation status and review dates. Risk-tiered review. Clear ownership: every feature has one accountable person, not the AI team in general. Standard artefacts with templates. Change management for prompts, models, tools and data sources. And incident management.
```

Scene 4 — Risk tiers

```text
Risk tiers make governance proportionate. Tier one, low: internal drafting aids with no personal data, which need a self-assessment checklist. Tier two, medium: customer-facing content with human review, or personal data processing, which needs product, privacy and security review. Tier three, high: decisions affecting people's rights or access, like credit, hiring or health; autonomous actions; or regulated domains. These need a full impact assessment, legal review and formal sign-off.
```

Scene 5 — Regulation and frameworks

```text
Regulation is evolving and varies by jurisdiction. Product teams should know the EU AI Act, a risk-based framework whose obligations phase in over time, with high-risk deadlines postponed by the 2026 Digital Omnibus. Existing data protection laws already apply to AI processing. Sector rules in finance, health and employment matter, as do consumer protection and advertising rules, and national frameworks in places like the UAE, Saudi Arabia and Pakistan. Work with counsel on what applies, and design governance so new requirements can be absorbed. Voluntary frameworks like the NIST AI Risk Management Framework and ISO slash IEC forty-two thousand and one give you useful structure.
```

Scene 6 — Across the lifecycle

```text
Governance runs across the whole lifecycle. At the idea stage, register the use case with an initial tier. When prototyping, use approved tools and data only. Before launch: evaluation report, tier-appropriate reviews and sign-offs. At launch: monitoring plan, incident runbook and user disclosures. In operation: periodic review, change control and audits. And at retirement: data deletion, communication and a register update.
```

Scene 7 — Simple example: two marketing uses

```text
A simple example. A marketing team wants to use an AI tool to brainstorm social captions. No personal data, human-written final posts, internal use only. That's tier one: a self-assessment checklist and an entry in the register, done in a day. The same team later wants AI to reply to customer comments automatically. Customer-facing, public, sometimes complaints: that's at least tier two, with review, monitoring and an owner.
```

Scene 8 — Worked example: two speeds

```text
Here's proportionality in action. A financial services firm wants an assistant that helps advisers draft client suitability notes. That's tier three, a regulated advice context. So: an impact assessment, legal review of record-keeping and advice obligations, advisers remain responsible and review every note, evaluation focused on factual accuracy against client data, audit logging and quarterly review. It takes longer, but it launches with confidence and survives its first regulatory audit. Meanwhile, a tier one internal meeting-notes tool goes live in two weeks with a checklist. Both happen, because governance is proportionate.
```

Scene 9 — Business example (illustrative)

```text
Illustrative numbers for the two-speed firm. In one year it registered twenty-six AI uses: eighteen tier one, six tier two, two tier three. Tier one items went live in a median of four days, tier two in about three weeks, and tier three in about three months. The register made the first regulatory information request easy: they answered in an afternoon, with links to every artefact.
```

Scene 10 — Hands-on in the lesson

```text
The hands-on section turns your register into something tools can check. Each entry is structured: owner, purpose, data categories, providers and regions, autonomy, risk tier, links to artefacts and review dates. A short Python script recalculates each entry's likely tier, flags missing artefacts for that tier, overdue reviews and missing owners, and prints a weekly report for your governance channel. Governance that's checked automatically gets followed. Governance that lives in a forgotten document doesn't.
```

Scene 11 — Common mistakes

```text
Common mistakes. A single review board for everything, which becomes a bottleneck. A long policy nobody reads. No register, so nobody knows what AI is in use. Governance that stops at launch. Treating vendor tools as outside governance because someone else built them. And documents that duplicate each other instead of templates that feed one register.
```

Scene 12 — How you'll know it works

```text
How will you know your governance works? Low-risk tools go live quickly, high-risk ones get proper scrutiny, and people describe the process as helpful rather than obstructive. The register is complete and current. Reviews happen on schedule. And when a regulator, client or auditor asks what AI you use and how it's controlled, you can answer from the register in an afternoon.
```

Scene 13 — Watch me do it: register + checks

```text
Watch me do it. I open ai-register dot yaml. Each entry has an ID, name, owner, purpose, users, who's affected, data categories, providers with region and model, autonomy, risk tier, links to artefacts and review dates. Next, the check script. Required-by-tier lists the artefacts each tier needs: a brief for tier one, up to brief, data flow, evaluation report, impact assessment and runbook for tier three. Tier-for recalculates the likely tier: full autonomy or decisions about people means three; personal data or customers affected means at least two. Then the loop compares recorded and calculated tiers, lists missing artefacts, flags overdue reviews and missing owners. I run it on our register. AI fourteen is OK. AI twenty-two is missing its impact assessment. AI thirty-one's review is overdue. I post the output in the governance channel and assign the two fixes.
```

Scene 14 — Recap

```text
To recap: good governance enables speed. Build a use policy, a register, risk tiers, clear ownership, standard artefacts, and change and incident management. Keep up with regulation through counsel and design for change. Embed governance across the lifecycle and automate the checks. Your next step: draft a one-page risk-tier policy and classify three AI uses you know. Next, we'll look at AI regulation for product teams in more detail.
```

Scene 15 — Try this now (40 minutes)

```text
Try this now. Write a one-page risk-tier policy with three tiers, examples and the review each needs. Then list three AI uses in your organisation and classify each. Create register entries for them with an owner, purpose, data categories, providers, autonomy, tier and next review date. If you have a few more minutes, run the register check script on those entries.
```

</details>

### 17. AI regulation for product teams (2026 update)

Lesson `ai-regulation-for-product-teams` · module “Governance, adoption and scaling” · target 9 min · 15 scenes · 1302 words · planned 9:26 (words ≈ 9:26) · status: **to produce**

| Term | Say |
|---|---|
| GPAI | G P A I |
| ICO | I C O |
| FCA | F C A |
| CMA | C M A |
| ASA | A S A |
| SDAIA | sud-eye-uh |
| DIFC | D I F C |
| ADGM | A D G M |
| PDPL | P D P L |
| ADMT | A D M T |

| # | Time | Sec | Narration | On screen | Visual |
|---|---|---|---|---|---|
| 1 | 0:00 | 37 | AI regulation used to be a set of principles on a slide. Now it's deadlines, obligations and fines, arriving at different speeds in different countries. Product teams don't need to become lawyers, but they do need to know enough to ask the right questions early and build the right evidence, because retrofitting disclosure, oversight and documentation after launch is painful. In this lesson you'll get the landscape as of September twenty twenty-six, and a screen you can run on any feature. Remember, this isn't legal advice. | AI regulation for product teams<br>• From principles to obligations<br>• The landscape, Sept 2026<br>• A screen for every feature | Layout: A slide titled 'AI principles' transforms into a calendar dotted with deadline markers and a world map with pins. Timing (37s): 0-3s 'AI regulation for product teams' heading fades in; 3-31s elements build in narration order; 31-37s hold final frame. |
| 2 | 0:37 | 30 | Here's an analogy. Regulation for AI products is a bit like building regulations. You don't need to be a structural engineer to build a house, but you do need to know that fire exits, wiring standards and inspections exist, and to plan for them from the first drawing. Adding a fire exit after the walls are up is expensive. So is adding AI disclosures, logs and oversight after launch. | Analogy: building regulations<br>• Know what's required<br>• Plan from the first drawing<br>• Retrofitting is expensive | Layout: An architectural drawing gains a 'fire exit' symbol early; a second scene shows workers knocking through a finished wall to add one, with a cost tag. Timing (30s): 0-3s 'Analogy: building regulations' heading fades in; 3-24s elements build in narration order; 24-30s hold final frame. |
| 3 | 1:07 | 51 | Start with the EU AI Act. It's risk-based, and it can reach you outside the EU when your AI system or its outputs are used there. Prohibited practices, such as manipulative techniques that cause harm and social scoring, have applied since February twenty twenty-five, and the twenty twenty-six amendments added a ban on systems generating non-consensual intimate imagery and child sexual abuse material. Obligations for general-purpose AI model providers have applied since August twenty twenty-five. And transparency obligations apply from August twenty twenty-six: telling people they're interacting with AI, marking synthetic content, and disclosing deepfakes. Only the machine-readable marking duty got a short transition, to December twenty twenty-six, and only for generative systems already on the market. | EU AI Act (1)<br>• Prohibitions: since Feb 2025<br>• GPAI duties: since Aug 2025<br>• Transparency (Art. 50): from Aug 2026 | Layout: An EU timeline bar with three filled milestones: Feb 2025 'Prohibitions', Aug 2025 'GPAI', Aug 2026 'Transparency', each with a small icon. Timing (51s): 0-3s 'EU AI Act (1)' heading fades in; 3-45s elements build in narration order; 45-51s hold final frame. |
| 4 | 1:58 | 37 | The high-risk regime is the big one for product teams working in hiring, education, credit, insurance or essential services. The Digital Omnibus on AI, which entered into force in late July twenty twenty-six, postponed those obligations. High-risk systems in Annex III areas now apply from the second of December twenty twenty-seven. High-risk AI embedded in regulated products, Annex I, from the second of August twenty twenty-eight. Postponed doesn't mean cancelled. Use the time to build logging, human oversight, data governance and documentation into your designs now. | EU AI Act (2)<br>• Annex III high-risk: 2 Dec 2027<br>• Annex I (products): 2 Aug 2028<br>• Deferred, not cancelled | Layout: the EU timeline extends to the right with two more milestones in outline: Dec 2027 'Annex III high-risk' and Aug 2028 'Annex I', with a 'build now' toolbox beside them. Timing (37s): 0-3s 'EU AI Act (2)' heading fades in; 3-31s elements build in narration order; 31-37s hold final frame. |
| 5 | 2:35 | 49 | The UK takes a different route. There's no single AI act. Existing regulators, like the ICO for data protection, the FCA for financial services, the CMA for competition and consumer law, and the ASA for advertising, apply existing law to AI under principles like safety, transparency, fairness, accountability and contestability. UK GDPR applies, and the Data Use and Access Act twenty twenty-five revised the rules on automated decision-making. In the United States, there's no comprehensive federal AI law. Agencies such as the FTC enforce existing rules on deceptive claims, and states are active. Colorado, for example, replaced its AI Act with a narrower automated decision-making transparency law due in January twenty twenty-seven. | UK and US<br>• UK: regulators + principles; UK GDPR; DUA Act 2025<br>• US: agencies + state laws<br>• e.g. Colorado's narrower ADMT law (Jan 2027) | Layout: split map: UK with regulator badges (ICO, FCA, CMA, ASA) orbiting; US map with several states highlighted and a Colorado callout. Timing (49s): 0-3s 'UK and US' heading fades in; 3-43s elements build in narration order; 43-49s hold final frame. |
| 6 | 3:24 | 40 | In the Gulf, the UAE has a federal Personal Data Protection Law, and free zones like DIFC and ADGM have their own data protection regimes, with DIFC including specific rules for autonomous systems. Saudi Arabia's Personal Data Protection Law has been enforced since September twenty twenty-four, alongside SDAIA's AI ethics principles and generative AI guidance. Pakistan's federal cabinet approved a National AI Policy in July twenty twenty-five, focused on skills, the ecosystem, sandboxes and responsible use, while comprehensive data protection legislation has been in draft for some time, so check its status. | Gulf and Pakistan<br>• UAE: PDPL + DIFC/ADGM regimes<br>• KSA: PDPL (enforced Sept 2024) + SDAIA principles<br>• Pakistan: National AI Policy 2025 | Layout: map zooming to the UAE, Saudi Arabia and Pakistan with document icons labelled with each framework. Timing (40s): 0-3s 'Gulf and Pakistan' heading fades in; 3-34s elements build in narration order; 34-40s hold final frame. |
| 7 | 4:04 | 31 | A simple example. A UK start-up launches an AI chat assistant for an online homeware shop with customers across Europe. From August twenty twenty-six, the EU transparency rules mean it must make clear to users that they're talking to an AI. The team adds a one-line disclosure at the start of every chat and a button to reach a person. Small change, done before launch, and it also happens to build trust. | Simple example: an EU-facing shop chatbot<br>• Transparency applies from Aug 2026<br>• One-line AI disclosure + human option<br>• Done before launch | Layout: A chat widget opening with 'You're chatting with our AI assistant' and a 'Talk to a person' button; a small EU flag icon and 'Aug 2026' tag beside it. Timing (31s): 0-3s 'Simple example: an EU-facing shop chatbot' heading fades in; 3-25s elements build in narration order; 25-31s hold final frame. |
| 8 | 4:35 | 39 | Here's the practical bit: turn regulation into product requirements. Transparency becomes AI disclosures, content labels and, where required, machine-readable marks. Human oversight becomes review, override and escalation paths, with no fully automated significant decisions without safeguards. Data protection becomes lawful basis, minimisation, retention, rights handling and impact assessments. Accuracy becomes evaluation evidence and monitoring. Documentation becomes your AI register, data flow maps and evaluation reports. Fairness becomes testing across user groups. And advertising law means no claims like one hundred percent accurate, and proper disclosure of sponsored and AI-generated ads. | Regulation → requirements<br>• Disclosure and labelling<br>• Oversight and escalation<br>• Data protection, evidence, records<br>• Fairness tests, honest ad claims | Layout: A two-column table animates: left 'regulatory theme', right 'product requirement', with arrows connecting each pair. Timing (39s): 0-3s 'Regulation → requirements' heading fades in; 3-33s elements build in narration order; 33-39s hold final frame. |
| 9 | 5:14 | 31 | A deeper business example, illustrative. A UK-based HR-tech start-up sells candidate screening to employers in the EU, UK and UAE. Its screen flags employment as a high-risk EU area from December twenty twenty-seven, UK GDPR automated-decision rules, and UAE data protection. Acting now, it builds recruiter override, decision logging and fairness testing across applicant groups into the roadmap, and rewrites marketing that claimed bias-free. Enterprise buyers' security questionnaires then pass without delays. | Business example (illustrative)<br>• HR-tech selling in EU, UK, UAE<br>• Flags: EU high-risk (Dec 2027), UK ADM rules<br>• Builds override, logs, fairness tests now | Layout: a map with three market pins; a roadmap strip below adds 'override', 'decision logs', 'fairness tests'; a marketing line 'bias-free' is struck through. Timing (31s): 0-3s 'Business example (illustrative)' heading fades in; 3-25s elements build in narration order; 25-31s hold final frame. |
| 10 | 5:45 | 37 | The hands-on section gives you a regulatory screen to run at the idea stage and before launch. It asks where the feature or its outputs are used, whether you're the provider, the deployer or both, and eight questions: direct interaction with people, generated media, high-risk domains, decisions with legal effects, personal and special-category data, biometrics or emotion recognition, advertising use, and sector regulators. It records the evidence you already have and the questions for counsel. You'll also get a short, honest AI disclosure template. | Hands-on in the lesson<br>• Regulatory applicability screen<br>• Evidence + questions for counsel<br>• Disclosure template | Layout: the screen template appears as a checklist document; a chat widget mock-up shows the disclosure text above the first message. Timing (37s): 0-3s 'Hands-on in the lesson' heading fades in; 3-31s elements build in narration order; 31-37s hold final frame. |
| 11 | 6:22 | 24 | Common mistakes. Assuming a law doesn't apply because your company isn't based in that region, even though your users are. Treating postponed deadlines as cancelled. Leaving legal review until the week before launch. Making marketing claims like one hundred percent accurate or unbiased. And writing a disclosure so long and legalistic that nobody reads it. | Common mistakes<br>• 'Not based there' ≠ not applicable<br>• Postponed ≠ cancelled<br>• Last-minute legal review<br>• Overclaiming in marketing<br>• Unreadable disclosures | Layout: A world map with a user pin in the EU and a company pin outside it, joined by a line labelled 'still applies'; a calendar with 'postponed' crossed and 'still coming' written. Timing (24s): 0-3s 'Common mistakes' heading fades in; 3-18s elements build in narration order; 18-24s hold final frame. |
| 12 | 6:46 | 29 | How will you know you're on top of it? Every AI feature has a completed regulatory screen on record, reviewed at launch and at major changes. Disclosures and human routes exist wherever users interact with AI. Your register links to the evidence regulators ask for: data flows, evaluations, oversight design. And your legal adviser receives specific questions, not a request to tell us if we're compliant. | How you'll know you're on top of it<br>• A screen per feature, kept current<br>• Disclosures + human routes live<br>• Evidence linked from the register<br>• Specific questions to counsel | Layout: A register row expanding to show linked documents: 'regulatory screen', 'data flow', 'eval report', 'oversight design', each with a green tick. Timing (29s): 0-3s 'How you'll know you're on top of it' heading fades in; 3-23s elements build in narration order; 23-29s hold final frame. |
| 13 | 7:15 | 65 | Watch me do it. I take the regulatory screen and fill it in for our shop's chat assistant. Markets: EU, UK and UAE. Role: deployer, since we use a third-party model inside our own product; I note that we may also count as provider of the overall system and flag that for counsel. Question one, direct interaction: yes, so disclosure design is required. Question two, generated media: no. Question three, high-risk domains: no. Question four, decisions with legal effects: no, it only answers questions and books returns. Question five, personal data: names, order numbers and addresses, processed in the EU region, so the privacy notice needs updating. Questions six and seven: no. Question eight: none. Evidence we have: register entry and data flow. Questions for counsel: our role, whether the returns flow needs extra consent, and the disclosure wording. Then I paste in the disclosure template and adapt it. | Watch me do it: regulatory screen<br>• Markets + role<br>• Eight questions answered<br>• Evidence we have<br>• Three questions for counsel | Layout: the screen document fills line by line; checkmarks and 'no' marks appear per question; a 'Questions for counsel' box collects three items; the disclosure template appears in a chat mock-up. Timing (65s): 0-3s 'Watch me do it: regulatory screen' heading fades in; 3-59s elements build in narration order; 59-65s hold final frame. |
| 14 | 8:20 | 37 | To recap: the EU AI Act phases in, with prohibitions and general-purpose model rules already applying, transparency from August twenty twenty-six, and high-risk deadlines pushed to late twenty twenty-seven and twenty twenty-eight. The UK and US regulate through existing regulators and a patchwork of laws, and the Gulf and Pakistan combine data protection with AI policies. Translate all of it into product requirements early. Your next step is to run the screen on one feature, list the requirements, and write three questions for your adviser. | Recap<br>• Know the milestones<br>• Build evidence into designs<br>• Next step: run the screen on one feature | Layout: recap card with the timeline and a checklist; 'Next: Adoption and scaling' title card. Timing (37s): 0-3s 'Recap' heading fades in; 3-31s elements build in narration order; 31-37s hold final frame. |
| 15 | 8:57 | 29 | Try this now. Run the regulatory screen from the lesson on one AI feature. Tick the markets, your role, and each of the eight questions. List the product requirements it implies, like a disclosure line, a human hand-off, a data protection impact assessment or a fairness test. Then write three specific questions for your legal or compliance adviser. Twenty minutes now can save months of rework later. | Try this now (20 minutes)<br>• Run the screen on one feature<br>• List implied product requirements<br>• Three specific questions for counsel | Layout: the screen checklist fills in; a requirements list builds beside it; three question cards stack under a 'for counsel' label. Timing (29s): 0-3s 'Try this now (20 minutes)' heading fades in; 3-23s elements build in narration order; 23-29s hold final frame. |

<details><summary>Narration per scene (paste into ElevenLabs)</summary>

Scene 1 — AI regulation for product teams

```text
AI regulation used to be a set of principles on a slide. Now it's deadlines, obligations and fines, arriving at different speeds in different countries. Product teams don't need to become lawyers, but they do need to know enough to ask the right questions early and build the right evidence, because retrofitting disclosure, oversight and documentation after launch is painful. In this lesson you'll get the landscape as of September twenty twenty-six, and a screen you can run on any feature. Remember, this isn't legal advice.
```

Scene 2 — Analogy: building regulations

```text
Here's an analogy. Regulation for AI products is a bit like building regulations. You don't need to be a structural engineer to build a house, but you do need to know that fire exits, wiring standards and inspections exist, and to plan for them from the first drawing. Adding a fire exit after the walls are up is expensive. So is adding AI disclosures, logs and oversight after launch.
```

Scene 3 — EU AI Act (1)

```text
Start with the EU AI Act. It's risk-based, and it can reach you outside the EU when your AI system or its outputs are used there. Prohibited practices, such as manipulative techniques that cause harm and social scoring, have applied since February twenty twenty-five, and the twenty twenty-six amendments added a ban on systems generating non-consensual intimate imagery and child sexual abuse material. Obligations for general-purpose AI model providers have applied since August twenty twenty-five. And transparency obligations apply from August twenty twenty-six: telling people they're interacting with AI, marking synthetic content, and disclosing deepfakes. Only the machine-readable marking duty got a short transition, to December twenty twenty-six, and only for generative systems already on the market.
```

Scene 4 — EU AI Act (2)

```text
The high-risk regime is the big one for product teams working in hiring, education, credit, insurance or essential services. The Digital Omnibus on AI, which entered into force in late July twenty twenty-six, postponed those obligations. High-risk systems in Annex III areas now apply from the second of December twenty twenty-seven. High-risk AI embedded in regulated products, Annex I, from the second of August twenty twenty-eight. Postponed doesn't mean cancelled. Use the time to build logging, human oversight, data governance and documentation into your designs now.
```

Scene 5 — UK and US

```text
The UK takes a different route. There's no single AI act. Existing regulators, like the ICO for data protection, the FCA for financial services, the CMA for competition and consumer law, and the ASA for advertising, apply existing law to AI under principles like safety, transparency, fairness, accountability and contestability. UK GDPR applies, and the Data Use and Access Act twenty twenty-five revised the rules on automated decision-making. In the United States, there's no comprehensive federal AI law. Agencies such as the FTC enforce existing rules on deceptive claims, and states are active. Colorado, for example, replaced its AI Act with a narrower automated decision-making transparency law due in January twenty twenty-seven.
```

Scene 6 — Gulf and Pakistan

```text
In the Gulf, the UAE has a federal Personal Data Protection Law, and free zones like DIFC and ADGM have their own data protection regimes, with DIFC including specific rules for autonomous systems. Saudi Arabia's Personal Data Protection Law has been enforced since September twenty twenty-four, alongside SDAIA's AI ethics principles and generative AI guidance. Pakistan's federal cabinet approved a National AI Policy in July twenty twenty-five, focused on skills, the ecosystem, sandboxes and responsible use, while comprehensive data protection legislation has been in draft for some time, so check its status.
```

Scene 7 — Simple example: an EU-facing shop chatbot

```text
A simple example. A UK start-up launches an AI chat assistant for an online homeware shop with customers across Europe. From August twenty twenty-six, the EU transparency rules mean it must make clear to users that they're talking to an AI. The team adds a one-line disclosure at the start of every chat and a button to reach a person. Small change, done before launch, and it also happens to build trust.
```

Scene 8 — Regulation → requirements

```text
Here's the practical bit: turn regulation into product requirements. Transparency becomes AI disclosures, content labels and, where required, machine-readable marks. Human oversight becomes review, override and escalation paths, with no fully automated significant decisions without safeguards. Data protection becomes lawful basis, minimisation, retention, rights handling and impact assessments. Accuracy becomes evaluation evidence and monitoring. Documentation becomes your AI register, data flow maps and evaluation reports. Fairness becomes testing across user groups. And advertising law means no claims like one hundred percent accurate, and proper disclosure of sponsored and AI-generated ads.
```

Scene 9 — Business example (illustrative)

```text
A deeper business example, illustrative. A UK-based HR-tech start-up sells candidate screening to employers in the EU, UK and UAE. Its screen flags employment as a high-risk EU area from December twenty twenty-seven, UK GDPR automated-decision rules, and UAE data protection. Acting now, it builds recruiter override, decision logging and fairness testing across applicant groups into the roadmap, and rewrites marketing that claimed bias-free. Enterprise buyers' security questionnaires then pass without delays.
```

Scene 10 — Hands-on in the lesson

```text
The hands-on section gives you a regulatory screen to run at the idea stage and before launch. It asks where the feature or its outputs are used, whether you're the provider, the deployer or both, and eight questions: direct interaction with people, generated media, high-risk domains, decisions with legal effects, personal and special-category data, biometrics or emotion recognition, advertising use, and sector regulators. It records the evidence you already have and the questions for counsel. You'll also get a short, honest AI disclosure template.
```

Scene 11 — Common mistakes

```text
Common mistakes. Assuming a law doesn't apply because your company isn't based in that region, even though your users are. Treating postponed deadlines as cancelled. Leaving legal review until the week before launch. Making marketing claims like one hundred percent accurate or unbiased. And writing a disclosure so long and legalistic that nobody reads it.
```

Scene 12 — How you'll know you're on top of it

```text
How will you know you're on top of it? Every AI feature has a completed regulatory screen on record, reviewed at launch and at major changes. Disclosures and human routes exist wherever users interact with AI. Your register links to the evidence regulators ask for: data flows, evaluations, oversight design. And your legal adviser receives specific questions, not a request to tell us if we're compliant.
```

Scene 13 — Watch me do it: regulatory screen

```text
Watch me do it. I take the regulatory screen and fill it in for our shop's chat assistant. Markets: EU, UK and UAE. Role: deployer, since we use a third-party model inside our own product; I note that we may also count as provider of the overall system and flag that for counsel. Question one, direct interaction: yes, so disclosure design is required. Question two, generated media: no. Question three, high-risk domains: no. Question four, decisions with legal effects: no, it only answers questions and books returns. Question five, personal data: names, order numbers and addresses, processed in the EU region, so the privacy notice needs updating. Questions six and seven: no. Question eight: none. Evidence we have: register entry and data flow. Questions for counsel: our role, whether the returns flow needs extra consent, and the disclosure wording. Then I paste in the disclosure template and adapt it.
```

Scene 14 — Recap

```text
To recap: the EU AI Act phases in, with prohibitions and general-purpose model rules already applying, transparency from August twenty twenty-six, and high-risk deadlines pushed to late twenty twenty-seven and twenty twenty-eight. The UK and US regulate through existing regulators and a patchwork of laws, and the Gulf and Pakistan combine data protection with AI policies. Translate all of it into product requirements early. Your next step is to run the screen on one feature, list the requirements, and write three questions for your adviser.
```

Scene 15 — Try this now (20 minutes)

```text
Try this now. Run the regulatory screen from the lesson on one AI feature. Tick the markets, your role, and each of the eight questions. List the product requirements it implies, like a disclosure line, a human hand-off, a data protection impact assessment or a fairness test. Then write three specific questions for your legal or compliance adviser. Twenty minutes now can save months of rework later.
```

</details>

### 18. Driving adoption and scaling from pilot to portfolio

Lesson `adoption-and-scaling` · module “Governance, adoption and scaling” · target 8 min · 15 scenes · 1185 words · planned 8:37 (words ≈ 8:35) · status: **to produce**

| # | Time | Sec | Narration | On screen | Visual |
|---|---|---|---|---|---|
| 1 | 0:00 | 35 | You've built an AI feature that works. It passes its evaluations, the economics add up and the UX is thoughtful. Three months later, only a handful of people use it. This is the most common way good AI features fail, and it has nothing to do with the model. The last mile is people. In this lesson you'll learn what drives adoption, a practical playbook, how to move from pilot to production to portfolio, and how to report value credibly. | Adoption and scaling<br>• Why working features go unused<br>• Adoption playbook<br>• Pilot → production → portfolio<br>• Credible value | Layout: A shiny new tool on a shelf gathering dust while people keep using an old spreadsheet; a small usage chart flatlines. Timing (35s): 0-3s 'Adoption and scaling' heading fades in; 3-29s elements build in narration order; 29-35s hold final frame. |
| 2 | 0:35 | 28 | Here's an analogy. Launching an AI feature without an adoption plan is like opening a gym, buying great equipment and never showing anyone how to use it. Most people will walk past the machines they don't understand. The gyms that thrive have induction sessions, trainers on the floor, and members who encourage each other. That's your training, your champions and your community of practice. | Analogy: a gym with no induction<br>• Great equipment, unused<br>• Inductions = training<br>• Trainers = champions | Layout: A gym with shiny machines and people walking past; then a trainer figure demonstrates a machine and a small group starts using it together. Timing (28s): 0-3s 'Analogy: a gym with no induction' heading fades in; 3-22s elements build in narration order; 22-28s hold final frame. |
| 3 | 1:03 | 33 | Six things drive adoption. Workflow fit: the feature appears where the work happens, with minimal extra steps. Clear value: people see time saved or quality improved in their own work. Trust: sources, reliability and honest limits. Skills: people know how to use it well, including how to review its output. Permission and incentives: managers endorse it, metrics don't punish learning time, and policies are clear. And champions: respected colleagues who share tips and real examples. | Adoption drivers<br>• Workflow fit • Clear value • Trust<br>• Skills • Permission and incentives<br>• Champions | Layout: six gears labelled with each driver, meshing together to turn a larger gear labelled 'Adoption'. Timing (33s): 0-3s 'Adoption drivers' heading fades in; 3-27s elements build in narration order; 27-33s hold final frame. |
| 4 | 1:36 | 38 | Here's the playbook. Pilot with motivated users who represent real work, not just enthusiasts. Train on real tasks, using participants' own work and a checklist for reviewing AI output. Share wins and failures, because concrete examples of time saved and mistakes caught set realistic expectations. Collect feedback continuously. Measure adoption and value: active users, usage per eligible task, edit rates, time saved and quality outcomes. And address fears directly. Be honest about how roles will change and how saved time will be used. Ignored anxiety quietly kills adoption. | Adoption playbook<br>• Representative pilot users<br>• Train on real tasks<br>• Share wins and failures<br>• Measure; address fears honestly | Layout: A workshop scene with abstract figures working on their own documents; a board with 'Wins' and 'Caught mistakes' columns filling with sticky notes. Timing (38s): 0-3s 'Adoption playbook' heading fades in; 3-32s elements build in narration order; 32-38s hold final frame. |
| 5 | 2:14 | 40 | Before moving from pilot to production, check the list. Success criteria met in evaluation and pilot metrics. Security, privacy and governance reviews complete for the risk tier. Monitoring, alerts and an incident runbook ready. A support model: who users contact and who owns fixes. Costs modelled at full scale. Training and documentation prepared. Then, as more features launch, build shared foundations: a model gateway for routing, logging, keys and budgets; retrieval infrastructure; an evaluation harness; monitoring and redaction services; and reusable assets like prompt libraries, evaluation sets and MCP servers for core systems. | Pilot → production → portfolio<br>• Exit checklist<br>• Shared platform: gateway, retrieval, evals, monitoring<br>• Reusable assets incl. MCP servers | Layout: A checklist with six ticks; then a platform layer diagram under several feature boxes, labelled Gateway, Retrieval, Eval harness, Monitoring, MCP servers. Timing (40s): 0-3s 'Pilot → production → portfolio' heading fades in; 3-34s elements build in narration order; 34-40s hold final frame. |
| 6 | 2:54 | 36 | Leadership will ask whether AI is worth it, so prepare credible evidence. Baselines measured before launch. Outcome metrics like time, cost, revenue and quality, not just usage. Controlled comparisons where possible, such as a pilot group against a similar group. Honest cost accounting, including review time and platform investment. And qualitative evidence from users and customers. Avoid inflated claims, like multiplying minutes saved per task by every task, when not all saved time becomes valuable work. Conservative, verified numbers build lasting credibility. | Credible value<br>• Baselines and outcomes<br>• Controlled comparisons<br>• Honest costs<br>• No inflated arithmetic | Layout: two bar charts: one labelled 'inflated' with an absurdly tall bar and a red cross; one labelled 'verified' with a modest bar, error bars and a green tick. Timing (36s): 0-3s 'Credible value' heading fades in; 3-30s elements build in narration order; 30-36s hold final frame. |
| 7 | 3:30 | 30 | A simple example. A ten-person law firm rolls out an AI drafting assistant. Instead of an email announcement, a respected senior associate runs two thirty-minute sessions using the team's own documents, shares a one-page checklist for reviewing AI drafts, and posts one good example and one caught mistake each week. Within a month, most eligible drafting tasks start with the assistant, and the partners trust the review checklist. | Simple example: a law firm rollout<br>• Senior associate as champion<br>• Training on real documents<br>• Weekly good example + caught mistake | Layout: A small office scene: two short training sessions on a calendar, a one-page checklist, and a weekly chat post with a green 'good example' and an amber 'caught mistake' card. Timing (30s): 0-3s 'Simple example: a law firm rollout' heading fades in; 3-24s elements build in narration order; 24-30s hold final frame. |
| 8 | 4:00 | 41 | Here's an example. A forty-person agency with offices in London, Dubai and Lahore pilots AI report drafting with one team. After six weeks, report preparation time drops meaningfully and client feedback is stable. Before scaling, they add monitoring, a review checklist and a short training session, then roll out team by team with a champion in each office. Three months later, they reuse the same gateway, evaluation harness and CRM integration for a proposal assistant, launching in half the time. And a quarterly portfolio review retires an image-tagging experiment that never found regular use. | Worked example: three-office agency<br>• Pilot, then team-by-team rollout<br>• Champions per office<br>• Reuse halves the next launch<br>• Retire what isn't used | Layout: A map with three office pins lighting up in sequence; a second feature launches faster on the same platform; a small experiment card fades out with 'retired'. Timing (41s): 0-3s 'Worked example: three-office agency' heading fades in; 3-35s elements build in narration order; 35-41s hold final frame. |
| 9 | 4:41 | 30 | Illustrative numbers for the three-office agency. In the pilot team, report preparation fell from about five hours to two per client. Usage per eligible task reached about eighty percent by week six. After rollout, the Lahore office adopted fastest because its champion ran weekly clinics. The proposal assistant, reusing the platform, launched in about five weeks instead of ten, and the retired image-tagging tool saved a small monthly licence fee. | Business example (illustrative)<br>• Reports ~5 h → ~2 h per client<br>• ~80% usage per eligible task<br>• Next launch: ~5 vs ~10 weeks | Layout: three office columns with adoption curves; Lahore's curve rises fastest with a 'weekly clinic' tag; below, two launch bars 10 weeks vs 5 weeks. Timing (30s): 0-3s 'Business example (illustrative)' heading fades in; 3-24s elements build in narration order; 24-30s hold final frame. |
| 10 | 5:11 | 33 | In the hands-on section you'll get an adoption query that measures active users, active share and, most honestly, usage per eligible task and average edit ratio by team. A four-question pulse survey, where the last question, confidence in checking the output, reveals training gaps rather than model gaps. And a pilot exit review that records each checklist item as met or not, and ends in a clear decision: scale, extend with specific fixes, or stop. | Hands-on in the lesson<br>• Adoption SQL by team<br>• Four-question pulse survey<br>• Pilot exit review | Layout: A dashboard table by team with active share and usage per eligible task; a short survey card; a decision stamp reading 'Scale'. Timing (33s): 0-3s 'Hands-on in the lesson' heading fades in; 3-27s elements build in narration order; 27-33s hold final frame. |
| 11 | 5:44 | 25 | Common mistakes. Launching to everyone at once. Training with generic demos instead of people's own work. Measuring logins instead of usage per eligible task. Staying silent about how roles will change. Scaling before the pilot exit checklist is met. And never retiring anything, so the portfolio fills with half-used tools that still cost money and carry risk. | Common mistakes<br>• Big-bang launches<br>• Generic demos<br>• Logins as the metric<br>• Silence about roles<br>• Scaling too early<br>• Never retiring | Layout: A rollout map lighting up everywhere at once and then flickering out; a login counter high while a 'usage per task' gauge sits low. Timing (25s): 0-3s 'Common mistakes' heading fades in; 3-19s elements build in narration order; 19-25s hold final frame. |
| 12 | 6:09 | 25 | How will you know adoption is real? Usage per eligible task rises and stays up after the launch buzz fades. Edit ratios settle at a healthy level. People can explain how they check AI output. Champions are answering questions you'd otherwise field. And the value report shows verified outcomes against a baseline, which leadership accepts without argument. | How you'll know adoption is real<br>• Usage per eligible task sustained<br>• Healthy edit ratios<br>• People can explain their checks<br>• Verified value vs baseline | Layout: A usage line that rises after launch, dips slightly and then stabilises high; a value report card with 'baseline' and 'now' columns and a 'verified' stamp. Timing (25s): 0-3s 'How you'll know adoption is real' heading fades in; 3-19s elements build in narration order; 19-25s hold final frame. |
| 13 | 6:34 | 66 | Watch me do it. I open the adoption query. It counts active users per team, divides by the number of eligible users, and then computes the honest metric: AI drafts used divided by eligible tasks, plus the average edit ratio. I run it for the last seven days. The London team shows eighty-two percent usage per task; Dubai shows forty-five. Next, the pulse survey results for Dubai: time saved is high for those who use it, but confidence in checking the output is only two out of five. That's a training gap, not a model problem. So Dubai's champion runs a thirty-minute session on the review checklist with real reports. Finally, the pilot exit review. I mark each checklist item met, not met with a plan, or not applicable. Monitoring, runbook and costs are met; Dubai training is not met, with a plan and date. Decision: scale, with Dubai training first. | Watch me do it: adoption data → action<br>• usage per eligible task by team<br>• Pulse: low review confidence<br>• Targeted training<br>• Exit review → scale with a condition | Layout: SQL left, results table right with London 82% and Dubai 45%; a pulse survey card shows confidence 2/5; a checklist with one amber 'plan + date' item and a 'Scale' stamp. Timing (66s): 0-3s 'Watch me do it: adoption data → action' heading fades in; 3-60s elements build in narration order; 60-66s hold final frame. |
| 14 | 7:40 | 33 | To recap: adoption depends on workflow fit, visible value, trust, skills, permission and champions. Pilot with representative users, train on real tasks, measure honestly, and address fears. Use an exit checklist, then scale with shared platform components and reusable assets, and retire what doesn't earn its place. Your next step is to write an adoption plan for one feature: pilot group, training, champions, metrics and the exit checklist. That completes the course's core path. Well done. | Recap<br>• People are the last mile<br>• Measure usage per eligible task<br>• Next step: write an adoption plan | Layout: recap card with the six gears; a course-complete banner with the badge outline. Timing (33s): 0-3s 'Recap' heading fades in; 3-27s elements build in narration order; 27-33s hold final frame. |
| 15 | 8:13 | 24 | Try this now. Write a one-page adoption plan for one AI feature. Name the pilot group and why they're representative, the training approach using real tasks, one champion per team, the four adoption metrics you'll track, and the pilot exit checklist. Then draft the four-question pulse survey and schedule it for two weeks after launch. | Try this now (30 minutes)<br>• Pilot group, training, champions<br>• Four adoption metrics<br>• Exit checklist + pulse survey scheduled | Layout: A one-page plan template filling in; a calendar entry 'Pulse survey — 2 weeks after launch' appears; four metric tiles line up at the bottom. Timing (24s): 0-3s 'Try this now (30 minutes)' heading fades in; 3-18s elements build in narration order; 18-24s hold final frame. |

<details><summary>Narration per scene (paste into ElevenLabs)</summary>

Scene 1 — Adoption and scaling

```text
You've built an AI feature that works. It passes its evaluations, the economics add up and the UX is thoughtful. Three months later, only a handful of people use it. This is the most common way good AI features fail, and it has nothing to do with the model. The last mile is people. In this lesson you'll learn what drives adoption, a practical playbook, how to move from pilot to production to portfolio, and how to report value credibly.
```

Scene 2 — Analogy: a gym with no induction

```text
Here's an analogy. Launching an AI feature without an adoption plan is like opening a gym, buying great equipment and never showing anyone how to use it. Most people will walk past the machines they don't understand. The gyms that thrive have induction sessions, trainers on the floor, and members who encourage each other. That's your training, your champions and your community of practice.
```

Scene 3 — Adoption drivers

```text
Six things drive adoption. Workflow fit: the feature appears where the work happens, with minimal extra steps. Clear value: people see time saved or quality improved in their own work. Trust: sources, reliability and honest limits. Skills: people know how to use it well, including how to review its output. Permission and incentives: managers endorse it, metrics don't punish learning time, and policies are clear. And champions: respected colleagues who share tips and real examples.
```

Scene 4 — Adoption playbook

```text
Here's the playbook. Pilot with motivated users who represent real work, not just enthusiasts. Train on real tasks, using participants' own work and a checklist for reviewing AI output. Share wins and failures, because concrete examples of time saved and mistakes caught set realistic expectations. Collect feedback continuously. Measure adoption and value: active users, usage per eligible task, edit rates, time saved and quality outcomes. And address fears directly. Be honest about how roles will change and how saved time will be used. Ignored anxiety quietly kills adoption.
```

Scene 5 — Pilot → production → portfolio

```text
Before moving from pilot to production, check the list. Success criteria met in evaluation and pilot metrics. Security, privacy and governance reviews complete for the risk tier. Monitoring, alerts and an incident runbook ready. A support model: who users contact and who owns fixes. Costs modelled at full scale. Training and documentation prepared. Then, as more features launch, build shared foundations: a model gateway for routing, logging, keys and budgets; retrieval infrastructure; an evaluation harness; monitoring and redaction services; and reusable assets like prompt libraries, evaluation sets and MCP servers for core systems.
```

Scene 6 — Credible value

```text
Leadership will ask whether AI is worth it, so prepare credible evidence. Baselines measured before launch. Outcome metrics like time, cost, revenue and quality, not just usage. Controlled comparisons where possible, such as a pilot group against a similar group. Honest cost accounting, including review time and platform investment. And qualitative evidence from users and customers. Avoid inflated claims, like multiplying minutes saved per task by every task, when not all saved time becomes valuable work. Conservative, verified numbers build lasting credibility.
```

Scene 7 — Simple example: a law firm rollout

```text
A simple example. A ten-person law firm rolls out an AI drafting assistant. Instead of an email announcement, a respected senior associate runs two thirty-minute sessions using the team's own documents, shares a one-page checklist for reviewing AI drafts, and posts one good example and one caught mistake each week. Within a month, most eligible drafting tasks start with the assistant, and the partners trust the review checklist.
```

Scene 8 — Worked example: three-office agency

```text
Here's an example. A forty-person agency with offices in London, Dubai and Lahore pilots AI report drafting with one team. After six weeks, report preparation time drops meaningfully and client feedback is stable. Before scaling, they add monitoring, a review checklist and a short training session, then roll out team by team with a champion in each office. Three months later, they reuse the same gateway, evaluation harness and CRM integration for a proposal assistant, launching in half the time. And a quarterly portfolio review retires an image-tagging experiment that never found regular use.
```

Scene 9 — Business example (illustrative)

```text
Illustrative numbers for the three-office agency. In the pilot team, report preparation fell from about five hours to two per client. Usage per eligible task reached about eighty percent by week six. After rollout, the Lahore office adopted fastest because its champion ran weekly clinics. The proposal assistant, reusing the platform, launched in about five weeks instead of ten, and the retired image-tagging tool saved a small monthly licence fee.
```

Scene 10 — Hands-on in the lesson

```text
In the hands-on section you'll get an adoption query that measures active users, active share and, most honestly, usage per eligible task and average edit ratio by team. A four-question pulse survey, where the last question, confidence in checking the output, reveals training gaps rather than model gaps. And a pilot exit review that records each checklist item as met or not, and ends in a clear decision: scale, extend with specific fixes, or stop.
```

Scene 11 — Common mistakes

```text
Common mistakes. Launching to everyone at once. Training with generic demos instead of people's own work. Measuring logins instead of usage per eligible task. Staying silent about how roles will change. Scaling before the pilot exit checklist is met. And never retiring anything, so the portfolio fills with half-used tools that still cost money and carry risk.
```

Scene 12 — How you'll know adoption is real

```text
How will you know adoption is real? Usage per eligible task rises and stays up after the launch buzz fades. Edit ratios settle at a healthy level. People can explain how they check AI output. Champions are answering questions you'd otherwise field. And the value report shows verified outcomes against a baseline, which leadership accepts without argument.
```

Scene 13 — Watch me do it: adoption data → action

```text
Watch me do it. I open the adoption query. It counts active users per team, divides by the number of eligible users, and then computes the honest metric: AI drafts used divided by eligible tasks, plus the average edit ratio. I run it for the last seven days. The London team shows eighty-two percent usage per task; Dubai shows forty-five. Next, the pulse survey results for Dubai: time saved is high for those who use it, but confidence in checking the output is only two out of five. That's a training gap, not a model problem. So Dubai's champion runs a thirty-minute session on the review checklist with real reports. Finally, the pilot exit review. I mark each checklist item met, not met with a plan, or not applicable. Monitoring, runbook and costs are met; Dubai training is not met, with a plan and date. Decision: scale, with Dubai training first.
```

Scene 14 — Recap

```text
To recap: adoption depends on workflow fit, visible value, trust, skills, permission and champions. Pilot with representative users, train on real tasks, measure honestly, and address fears. Use an exit checklist, then scale with shared platform components and reusable assets, and retire what doesn't earn its place. Your next step is to write an adoption plan for one feature: pilot group, training, champions, metrics and the exit checklist. That completes the course's core path. Well done.
```

Scene 15 — Try this now (30 minutes)

```text
Try this now. Write a one-page adoption plan for one AI feature. Name the pilot group and why they're representative, the training approach using real tasks, one champion per team, the four adoption metrics you'll track, and the pilot exit checklist. Then draft the four-question pulse survey and schedule it for two weeks after launch.
```

</details>
