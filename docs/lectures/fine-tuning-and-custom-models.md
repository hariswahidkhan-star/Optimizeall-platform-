# Fine-Tuning, Distillation and Custom Models — lecture production script

Course `fine-tuning-and-custom-models` · pack v1 · reviewed 2026-09 · level advanced · category ai

16 lectures · 18,667 narration words · planned runtime 2:15:18 (≈ 133 min at 140 wpm)

> Generated by `scripts/export-lecture-scripts.py` from the course pack — edit the pack, not this file. Production workflow: [README](README.md).

## Voice direction

Conversational expert, second person, ~140 wpm; precise and technical, calm confidence; spell out code identifiers when read aloud; audience level: advanced. Pause ~0.5 s between scenes; no music under narration (added in edit).

## Lectures

1. [When NOT to fine-tune: the customization ladder](#1-when-not-to-fine-tune-the-customization-ladder)
2. [The customization methods map: SFT, PEFT, preference, RFT and distillation](#2-the-customization-methods-map-sft-peft-preference-rft-and-distillation)
3. [Dataset design and curation: quality beats quantity](#3-dataset-design-and-curation-quality-beats-quantity)
4. [Synthetic data generation and distillation](#4-synthetic-data-generation-and-distillation)
5. [Training data safety, licensing and privacy](#5-training-data-safety-licensing-and-privacy)
6. [Supervised fine-tuning fundamentals](#6-supervised-fine-tuning-fundamentals)
7. [LoRA, QLoRA and parameter-efficient fine-tuning](#7-lora-qlora-and-parameter-efficient-fine-tuning)
8. [Open-source tooling: TRL, Unsloth, Axolotl and the training stack](#8-open-source-tooling-trl-unsloth-axolotl-and-the-training-stack)
9. [Preference optimization: RLHF and DPO for practitioners](#9-preference-optimization-rlhf-and-dpo-for-practitioners)
10. [Reinforcement fine-tuning with graders: RFT and GRPO](#10-reinforcement-fine-tuning-with-graders-rft-and-grpo)
11. [Evaluation before and after fine-tuning](#11-evaluation-before-and-after-fine-tuning)
12. [Hosted fine-tuning: which platforms, which models, which trade-offs](#12-hosted-fine-tuning-which-platforms-which-models-which-trade-offs)
13. [Fine-tuning embedding models for better retrieval](#13-fine-tuning-embedding-models-for-better-retrieval)
14. [Cost modeling: training, inference and break-even](#14-cost-modeling-training-inference-and-break-even)
15. [Deploying custom models and monitoring for drift](#15-deploying-custom-models-and-monitoring-for-drift)
16. [Capstone: fine-tune a small model for support-intent routing](#16-capstone-fine-tune-a-small-model-for-support-intent-routing)

### 1. When NOT to fine-tune: the customization ladder

Lesson `when-not-to-fine-tune` · module “Decide before you tune” · target 9 min · 15 scenes · 1197 words · planned 8:41 (words ≈ 8:40) · status: **to produce**

| Term | Say |
|---|---|
| RAG | rag |
| LoRA | LOR-ah |
| SFT | S F T |
| YAML | YAM-ul |

| # | Time | Sec | Narration | On screen | Visual |
|---|---|---|---|---|---|
| 1 | 0:00 | 30 | A company spent three months fine-tuning a model to answer questions about its products. It launched, and within two weeks half the answers were outdated because prices and policies had changed. The fix was a retrieval system that took a week. In this lesson you will learn when not to fine-tune, the customization ladder to climb first, and the five signals that tell you fine-tuning is genuinely worth it. | When NOT to fine-tune<br>• The customization ladder<br>• Five signals that justify it | A calendar flipping through three months, then a product card whose price tag keeps changing while an AI answer bubble shows an old price. |
| 2 | 0:30 | 36 | Here is an analogy. Prompting is like giving a talented new employee a clear brief. Retrieval is giving them access to the company handbook and files. Fine-tuning is sending them on a months-long training course that changes their habits. You would not send someone on a training course to learn this week's price list. You would give them the price list. But if you need them to write in the house style every single day without reminders, training starts to make sense. | Analogy: a new employee<br>• Prompt = a clear brief<br>• RAG = access to the handbook<br>• Fine-tuning = a course that changes habits | Three panels animate left to right: a brief on a clipboard, a bookshelf labeled handbook, and a classroom with a certificate. A price-list card tries to enter the classroom and is redirected to the bookshelf. |
| 3 | 1:06 | 32 | Here is the context for twenty twenty-six. Modern models follow instructions and formats far better than earlier generations, and prompt caching, long context and retrieval make prompting cheaper and more capable. When OpenAI began winding down its self-serve fine-tuning platform this year, it pointed to exactly that: newer models follow formats much better, and prompt-based approaches are often cheaper and faster. Fine-tuning is still powerful, but for specific problems, not as a default. | 2026 context<br>• Base models follow formats much better<br>• Caching, long context, RAG make prompting cheaper<br>• Fine-tuning: powerful, but specific | Timeline with older model icons struggling with a format checklist and a newer model icon ticking every box; a small note "prompting got cheaper" appears. |
| 4 | 1:38 | 29 | So what is fine-tuning good at? Changing behavior. Consistent format and style, like a brand voice, without long prompts. Narrow classification or extraction where a small cheap model must match a big one. Distillation, teaching a small model to imitate a large one on a single task. Some domain reasoning patterns with a clear grader. And latency and privacy, because a small tuned model can run locally. | Good at: behavior<br>• Format and style<br>• Narrow classification/extraction<br>• Distillation into small models<br>• Graded reasoning patterns<br>• Latency and privacy | Five icons appear on a clean grid: a style brush, a label tag, a large-to-small arrow, a grader checkmark, and a laptop with a lock. |
| 5 | 2:07 | 29 | And what is it bad at? Adding knowledge. Facts learned during fine-tuning are hard to update, easy to hallucinate around and impossible to cite. That is retrieval's job. It also cannot fix unclear requirements. If your experts disagree on the right answer, training will not settle it. And it is slow to iterate, because every change means new data, a new run and a new evaluation. | Bad at<br>• Adding or updating knowledge → use RAG<br>• Fixing unclear requirements<br>• Fast iteration | A brain icon with sticky notes of facts falling off, replaced by a search-and-document icon labeled retrieval. |
| 6 | 2:36 | 35 | Now the ladder. Rung one: a better prompt, with clear instructions, constraints and an output schema. Rung two: a handful of curated examples, which caching makes cheap. Rung three: structured output, tools and plain code for deterministic logic. Rung four: retrieval to ground answers in your documents. Rung five: model choice and routing, a stronger model or a cheaper one per task. Rung six: fine-tuning. Climb one rung at a time, and measure each one on the same evaluation set. | The customization ladder<br>1 Better prompt<br>2 Few-shot examples<br>3 Structure, tools, code<br>4 Retrieval<br>5 Model choice and routing<br>6 Fine-tuning | A six-rung ladder drawn vertically; a small figure climbs, and at each rung a score gauge ticks up slightly. |
| 7 | 3:11 | 33 | How do you know you have reached rung six for real? Look for five signals. One, your prompt is long and fragile and repeated at high volume. Two, a small model almost passes and you need its cost, speed or on-device deployment. Three, you have hundreds to thousands of high-quality examples. Four, you can measure success automatically or with a clear rubric. Five, the behavior is stable. If fewer than three are true, keep climbing the ladder. | Five signals<br>• Long, fragile, high-volume prompt<br>• Small model almost passes<br>• Hundreds to thousands of good examples<br>• Measurable<br>• Stable behavior | Five indicator lights in a row; three or more turning green unlocks a padlock labeled fine-tune. |
| 8 | 3:44 | 39 | A worked example, with illustrative numbers. A Dubai fashion retailer wants product descriptions in its brand voice, in English and Arabic. A style guide and six examples get seventy percent editor approval. A JSON schema removes formatting errors. A stronger model reaches eighty-four percent, but costs more. They write forty thousand descriptions a year, have two thousand approved examples, a clear rubric and a stable voice. Four of five signals. So they distil the stronger model's style into a small open model and keep the big one for new categories. | Worked example: Dubai retailer (illustrative)<br>• Prompt + examples: 70% approval<br>• Stronger model: 84%, higher cost<br>• 40k/year, 2k approved examples<br>• Decision: distil into a small model | Bar chart of approval rates by rung, then an arrow from a large model icon to a small model icon labeled distil, with a cost bar shrinking. |
| 9 | 4:23 | 33 | Your hands-on tool is a one-page worksheet. The task, your current best setup and its score, the target, the type of gap, whether it is knowledge, behavior, cost or latency, the five signals, the decision and an owner. If the gap is knowledge, the answer is retrieval, not fine-tuning. And remember the pitfalls: no baseline means no proof, every base-model upgrade may mean retraining, and hosted fine-tuning offerings change, as we will see in module five. | Hands-on: the worksheet<br>• Current best + score<br>• Target + gap type<br>• Five signals → decision<br>• Knowledge gap → RAG | YAML worksheet on screen filling in line by line, with the gap_type line highlighted and a small arrow pointing to "RAG, not fine-tuning" for knowledge gaps. |
| 10 | 4:56 | 33 | A simple example. A small bakery in Lahore wants its AI assistant to answer questions about today's menu and opening hours. Someone suggests fine-tuning. But the menu changes daily and the hours change during Ramadan. That is knowledge, and it changes often. The right answer is a short prompt plus a retrieval step that reads the current menu file. It took an afternoon, and when the menu changes, they edit one file. No training run needed. | Simple example: bakery menu<br>• Daily menu + seasonal hours = changing knowledge<br>• Prompt + retrieval from menu file<br>• Edit a file, not a model | A menu card updates its items with a flip animation each day; an arrow shows the assistant reading the latest card, while a greyed-out training icon is crossed out. |
| 11 | 5:29 | 38 | Let me give you a quick test you can run in a meeting. When someone proposes fine-tuning, ask three questions. What is the score of our best prompted setup on our evaluation set today? What specific gap remains: knowledge, behavior, cost or latency? And who will maintain the model when the base model changes next year? If nobody can answer the first question, the project is not ready. If the gap is knowledge, the answer is retrieval. And if nobody owns maintenance, the fine-tune will quietly rot. | The meeting test<br>• What does our best prompted setup score?<br>• Which gap: knowledge, behavior, cost, latency?<br>• Who maintains it next year? | Meeting room whiteboard with three questions written one by one; a sticky note labeled "owner?" is added beside the third. |
| 12 | 6:07 | 37 | A question from executives: are we falling behind if we do not fine-tune? No. Many of the strongest AI products in production run on well-designed prompts, retrieval and tools, with careful evaluation, and no fine-tuning at all. Fine-tuning is a tool for specific gaps, not a badge of sophistication. A second question: if we fine-tune, will we have to redo it when the base model changes? Usually yes. Plan for retraining, and keep your data and evaluation ready so it takes days, not months. | FAQ<br>• Behind if we don't fine-tune? No; many top products don't<br>• Base model changes → retrain? Usually; keep data + evals ready | Two Q&A cards; the first shows a product stack of prompt, retrieval, tools and evals with no fine-tuning layer and a green tick, the second shows a calendar with a retraining marker. |
| 13 | 6:44 | 27 | Try this now. Pick one AI task your team wants to improve and write down its current score on ten real examples, using your best prompt. Then write one sentence describing the remaining gap and label it: knowledge, behavior, cost or latency. If it says knowledge, stop and plan retrieval instead. If it says behavior, cost or latency, keep it for the worksheet. | Try this now<br>• Score your best prompt on 10 examples<br>• Name the gap: knowledge, behavior, cost, latency<br>• Knowledge → retrieval | A small score card with 10 checkboxes filling in, then a four-option label selector where one option lights up and a routing arrow appears. |
| 14 | 7:11 | 67 | Watch me do it. A product manager asks me to fine-tune a model for support replies. I open the worksheet. Current best setup: a strong API model with a good prompt. I run it on the fifty-item evaluation set with the rubric: mean score four point one. Target: four point three, at a lower cost per reply. Now the gap: I read the ten lowest-scoring replies. Six are wrong about this month's shipping promotion. That is knowledge, so I add retrieval over the promotions page and rerun: mean four point three. Target met on quality. Remaining gap: cost, since volume is one hundred thousand replies a month. I check the signals: long repeated prompt, yes; a small model almost passes, I test one, it scores three point nine; two thousand approved replies exist; rubric exists; tone is stable. Five of five. My written decision: retrieval now, and a distillation project for cost, starting next sprint. | Watch me do it<br>1 Score best setup on 50 items<br>2 Read the 10 worst<br>3 Knowledge gap → add retrieval<br>4 Check five signals<br>5 Write the decision | Worksheet YAML on screen; an evaluation table with scores; ten low-scoring replies expand, six tagged "promotion"; a retrieval icon added; score updates; five signal checkboxes tick; decision line typed. |
| 15 | 8:18 | 23 | Recap. Fine-tuning changes behavior, not knowledge. Climb the ladder: prompt, examples, structure, retrieval, model choice, and only then fine-tuning. Look for the five signals, and measure every rung on the same evaluation set. Your next step: fill in the worksheet for one real task and decide, with evidence, whether fine-tuning is justified. | Recap<br>• Behavior, not knowledge<br>• Ladder first<br>• Next: complete the worksheet | Recap card with the ladder icon, a check mark and a document icon. |

<details><summary>Narration per scene (paste into ElevenLabs)</summary>

Scene 1 — When NOT to fine-tune

```text
A company spent three months fine-tuning a model to answer questions about its products. It launched, and within two weeks half the answers were outdated because prices and policies had changed. The fix was a retrieval system that took a week. In this lesson you will learn when not to fine-tune, the customization ladder to climb first, and the five signals that tell you fine-tuning is genuinely worth it.
```

Scene 2 — Analogy: a new employee

```text
Here is an analogy. Prompting is like giving a talented new employee a clear brief. Retrieval is giving them access to the company handbook and files. Fine-tuning is sending them on a months-long training course that changes their habits. You would not send someone on a training course to learn this week's price list. You would give them the price list. But if you need them to write in the house style every single day without reminders, training starts to make sense.
```

Scene 3 — 2026 context

```text
Here is the context for twenty twenty-six. Modern models follow instructions and formats far better than earlier generations, and prompt caching, long context and retrieval make prompting cheaper and more capable. When OpenAI began winding down its self-serve fine-tuning platform this year, it pointed to exactly that: newer models follow formats much better, and prompt-based approaches are often cheaper and faster. Fine-tuning is still powerful, but for specific problems, not as a default.
```

Scene 4 — Good at: behavior

```text
So what is fine-tuning good at? Changing behavior. Consistent format and style, like a brand voice, without long prompts. Narrow classification or extraction where a small cheap model must match a big one. Distillation, teaching a small model to imitate a large one on a single task. Some domain reasoning patterns with a clear grader. And latency and privacy, because a small tuned model can run locally.
```

Scene 5 — Bad at

```text
And what is it bad at? Adding knowledge. Facts learned during fine-tuning are hard to update, easy to hallucinate around and impossible to cite. That is retrieval's job. It also cannot fix unclear requirements. If your experts disagree on the right answer, training will not settle it. And it is slow to iterate, because every change means new data, a new run and a new evaluation.
```

Scene 6 — The customization ladder

```text
Now the ladder. Rung one: a better prompt, with clear instructions, constraints and an output schema. Rung two: a handful of curated examples, which caching makes cheap. Rung three: structured output, tools and plain code for deterministic logic. Rung four: retrieval to ground answers in your documents. Rung five: model choice and routing, a stronger model or a cheaper one per task. Rung six: fine-tuning. Climb one rung at a time, and measure each one on the same evaluation set.
```

Scene 7 — Five signals

```text
How do you know you have reached rung six for real? Look for five signals. One, your prompt is long and fragile and repeated at high volume. Two, a small model almost passes and you need its cost, speed or on-device deployment. Three, you have hundreds to thousands of high-quality examples. Four, you can measure success automatically or with a clear rubric. Five, the behavior is stable. If fewer than three are true, keep climbing the ladder.
```

Scene 8 — Worked example: Dubai retailer (illustrative)

```text
A worked example, with illustrative numbers. A Dubai fashion retailer wants product descriptions in its brand voice, in English and Arabic. A style guide and six examples get seventy percent editor approval. A JSON schema removes formatting errors. A stronger model reaches eighty-four percent, but costs more. They write forty thousand descriptions a year, have two thousand approved examples, a clear rubric and a stable voice. Four of five signals. So they distil the stronger model's style into a small open model and keep the big one for new categories.
```

Scene 9 — Hands-on: the worksheet

```text
Your hands-on tool is a one-page worksheet. The task, your current best setup and its score, the target, the type of gap, whether it is knowledge, behavior, cost or latency, the five signals, the decision and an owner. If the gap is knowledge, the answer is retrieval, not fine-tuning. And remember the pitfalls: no baseline means no proof, every base-model upgrade may mean retraining, and hosted fine-tuning offerings change, as we will see in module five.
```

Scene 10 — Simple example: bakery menu

```text
A simple example. A small bakery in Lahore wants its AI assistant to answer questions about today's menu and opening hours. Someone suggests fine-tuning. But the menu changes daily and the hours change during Ramadan. That is knowledge, and it changes often. The right answer is a short prompt plus a retrieval step that reads the current menu file. It took an afternoon, and when the menu changes, they edit one file. No training run needed.
```

Scene 11 — The meeting test

```text
Let me give you a quick test you can run in a meeting. When someone proposes fine-tuning, ask three questions. What is the score of our best prompted setup on our evaluation set today? What specific gap remains: knowledge, behavior, cost or latency? And who will maintain the model when the base model changes next year? If nobody can answer the first question, the project is not ready. If the gap is knowledge, the answer is retrieval. And if nobody owns maintenance, the fine-tune will quietly rot.
```

Scene 12 — FAQ

```text
A question from executives: are we falling behind if we do not fine-tune? No. Many of the strongest AI products in production run on well-designed prompts, retrieval and tools, with careful evaluation, and no fine-tuning at all. Fine-tuning is a tool for specific gaps, not a badge of sophistication. A second question: if we fine-tune, will we have to redo it when the base model changes? Usually yes. Plan for retraining, and keep your data and evaluation ready so it takes days, not months.
```

Scene 13 — Try this now

```text
Try this now. Pick one AI task your team wants to improve and write down its current score on ten real examples, using your best prompt. Then write one sentence describing the remaining gap and label it: knowledge, behavior, cost or latency. If it says knowledge, stop and plan retrieval instead. If it says behavior, cost or latency, keep it for the worksheet.
```

Scene 14 — Watch me do it

```text
Watch me do it. A product manager asks me to fine-tune a model for support replies. I open the worksheet. Current best setup: a strong API model with a good prompt. I run it on the fifty-item evaluation set with the rubric: mean score four point one. Target: four point three, at a lower cost per reply. Now the gap: I read the ten lowest-scoring replies. Six are wrong about this month's shipping promotion. That is knowledge, so I add retrieval over the promotions page and rerun: mean four point three. Target met on quality. Remaining gap: cost, since volume is one hundred thousand replies a month. I check the signals: long repeated prompt, yes; a small model almost passes, I test one, it scores three point nine; two thousand approved replies exist; rubric exists; tone is stable. Five of five. My written decision: retrieval now, and a distillation project for cost, starting next sprint.
```

Scene 15 — Recap

```text
Recap. Fine-tuning changes behavior, not knowledge. Climb the ladder: prompt, examples, structure, retrieval, model choice, and only then fine-tuning. Look for the five signals, and measure every rung on the same evaluation set. Your next step: fill in the worksheet for one real task and decide, with evidence, whether fine-tuning is justified.
```

</details>

### 2. The customization methods map: SFT, PEFT, preference, RFT and distillation

Lesson `customization-methods-map` · module “Decide before you tune” · target 9 min · 15 scenes · 1201 words · planned 8:42 (words ≈ 8:42) · status: **to produce**

| Term | Say |
|---|---|
| SFT | S F T |
| PEFT | peft |
| LoRA | LOR-ah |
| QLoRA | Q LOR-ah |
| RLHF | R L H F |
| DPO | D P O |
| RFT | R F T |
| GRPO | G R P O |
| TRL | T R L |
| Unsloth | un-sloth |
| Axolotl | AX-oh-lot-ul |

| # | Time | Sec | Narration | On screen | Visual |
|---|---|---|---|---|---|
| 1 | 0:00 | 31 | Ask five engineers what fine-tuning means and you will get five answers. One means training on examples, one means LoRA, one means RLHF, one means distillation, and one means making the search better. They are all different techniques with different data, tools and results. In this lesson you will get one clear map of customization methods, a decision flow to pick the right one, and the exact data format each needs. | The methods map<br>• One vocabulary<br>• A decision flow<br>• Data formats per method | Five speech bubbles with different words overlapping, then resolving into a clean labeled map with seven regions. |
| 2 | 0:31 | 35 | Here is a way to remember the methods. SFT is teaching by showing: here is the task, here is how I would do it. Preference optimization is teaching by comparing: this answer is better than that one. Reinforcement fine-tuning is teaching by scoring: try many times, and I will mark each attempt. Distillation is apprenticeship: watch the master and copy. And embedding fine-tuning is training a librarian to find the right book. Five teaching styles for five kinds of learning. | Five teaching styles<br>• SFT = showing<br>• DPO = comparing<br>• RFT = scoring attempts<br>• Distillation = apprenticeship<br>• Embeddings = training a librarian | Five small classroom vignettes appear in a row, each with an icon: a demonstration, two cards with a winner, a scored test, a master and apprentice, and a librarian at a shelf. |
| 3 | 1:06 | 33 | Let's walk the map. Continued pre-training feeds large amounts of raw domain text, and it is rare and expensive for businesses. Supervised fine-tuning, SFT, gives input and ideal output pairs, and the model imitates the demonstrated behavior. Parameter-efficient fine-tuning, like LoRA and QLoRA, trains small adapter weights instead of the whole model. Important: that is a how, not a what. You can do SFT with LoRA, preference training with LoRA, and reinforcement training with LoRA. | Map, part 1<br>• Continued pre-training: raw text, rare<br>• SFT: input → ideal output<br>• PEFT (LoRA/QLoRA): train small adapters<br>• PEFT is how, not what | Map regions lighting up one by one; a small adapter plug icon attaches to three different objective regions to show PEFT is orthogonal. |
| 4 | 1:39 | 32 | Continuing. Preference optimization, which includes RLHF and DPO, uses pairs: a prompt, a preferred response and a rejected one, and nudges the model toward preferred answers. Great for tone and subjective quality. Reinforcement fine-tuning, with methods like GRPO, uses prompts plus a grader that scores outputs, perfect when correctness can be checked. Distillation trains a small student to imitate a strong teacher on your task. And embedding fine-tuning improves retrieval similarity, not generation. | Map, part 2<br>• Preference (RLHF, DPO): chosen vs rejected<br>• RFT (GRPO): prompts + grader<br>• Distillation: teacher → student<br>• Embedding fine-tuning: better retrieval | Four more map regions light up, each with a tiny illustration: two answer cards with thumbs up and down, a grader gauge, a big-to-small arrow, and a search magnifier. |
| 5 | 2:11 | 35 | Separate from the method is where you run it. Hosted fine-tuning APIs from cloud providers, model vendors and inference platforms mean no GPU operations and integrated serving, but you are limited to their models and methods, your data goes to them, and offerings change. OpenAI, for example, began winding down its self-serve fine-tuning platform in twenty twenty-six. Self-managed training on open weights, with tools like TRL, PEFT, Unsloth and Axolotl, gives full control and portable adapters, but you own the pipeline. | Hosted vs self-managed<br>• Hosted: easy, limited, data leaves, offerings change<br>• Self-managed: control, portability, you own the pipeline | Split screen: a cloud console with a limited dropdown of models on the left, a GPU workstation with a flexible toolbox on the right. |
| 6 | 2:46 | 35 | Here is the decision flow. Is the gap knowledge? Use retrieval and stop. Can you write ideal outputs? Use SFT with LoRA. Is quality subjective and easier to compare than to write? Collect preference pairs and run DPO after SFT. Is correctness checkable by code or a reliable grader? Use reinforcement fine-tuning, usually after SFT. Is the goal cheaper and faster at similar quality? Distil. Is retrieval missing the right passages? Fine-tune the embedding model, or try a reranker first. | Decision flow<br>• Knowledge → RAG<br>• Can write ideal outputs → SFT<br>• Easier to compare → DPO<br>• Checkable → RFT/GRPO<br>• Cheaper/faster → distil<br>• Retrieval misses → embeddings | Flowchart built step by step with diamond questions and method boxes, highlighted in sequence as narration proceeds. |
| 7 | 3:21 | 37 | One company, four methods. A Pakistani fintech app supports customers in English, Urdu and Roman Urdu. For routing eighteen intents, they use SFT with LoRA on a small model and six thousand labeled messages. For reply tone, agents find it easier to pick the better of two drafts, so they run DPO on top. For fee amounts in replies, a grader checks quotes against the fee schedule, so they use reinforcement fine-tuning. And because Roman Urdu searches missed English help articles, they fine-tune the embedding model. | One company, four methods<br>• Intents: SFT + LoRA<br>• Tone: DPO<br>• Fee accuracy: RFT with a grader<br>• Roman Urdu search: embedding fine-tuning | Four lanes of a support dashboard, each labeled with a gap and a method badge; mixed-script sample messages flow through. |
| 8 | 3:58 | 32 | Each method needs a different data shape, and the lesson shows all four. SFT uses chat messages ending in the ideal assistant reply. DPO uses a prompt, a chosen response and a rejected one. Reinforcement fine-tuning uses a prompt plus whatever fields your grader needs, like the expected fee. And embedding fine-tuning uses an anchor, the query, and a positive, the passage that should match. Getting the shape right on day one saves weeks. | Data shapes<br>• SFT: messages → ideal reply<br>• DPO: prompt, chosen, rejected<br>• RFT: prompt + grader fields<br>• Embeddings: anchor, positive | Four JSON lines appear stacked on screen, with each key highlighted in a color matching its method label. |
| 9 | 4:30 | 27 | Four pitfalls. Choosing a method by hype, like we need RLHF, instead of by gap type. Running DPO before the model can do the task at all, so do SFT first. Building reinforcement fine-tuning without a reliable grader, because the model will learn to exploit a weak one. And assuming a hosted provider supports every method for every model. Check the docs. | Pitfalls<br>• Method by hype<br>• DPO before SFT<br>• RFT with a weak grader<br>• Assuming hosted support | Four red warning cards, the third showing a gauge being gamed by a mischievous arrow. |
| 10 | 4:57 | 37 | A simple example. A recipe app wants three improvements. First, always output recipes in the same structured format: that is SFT with examples of the format. Second, users prefer friendlier, shorter tips, and it is easier to pick the better tip than to write the perfect one: that is preference data and DPO. Third, when a user searches for quick vegan dinner, results often miss the right recipes: that is an embedding problem. Three gaps, three different methods, and none of them is simply more fine-tuning. | Simple example: recipe app<br>• Consistent format → SFT<br>• Friendlier tips → DPO<br>• Search misses → embedding fine-tuning | Recipe app mock-up with three highlighted areas; each area gets a method badge that flies in and sticks to it. |
| 11 | 5:34 | 36 | A practical tip on sequencing. Most successful projects follow the same order: first SFT to teach the task and format, then, only if needed, preference optimization for subjective quality, or reinforcement fine-tuning for checkable correctness. Distillation is usually SFT with teacher outputs as the data. And embedding fine-tuning runs on its own track, because it improves retrieval rather than generation. Skipping SFT and jumping straight to DPO or GRPO usually gives weak results, because the model cannot yet do the basic task. | Sequencing<br>• SFT first<br>• Then DPO (subjective) or RFT (checkable)<br>• Distillation = SFT on teacher outputs<br>• Embeddings: separate track | Horizontal pipeline: SFT box first, branching to DPO and RFT boxes; a separate lane below labeled embeddings. |
| 12 | 6:10 | 35 | A common question: can we combine methods in one model? Yes, and it is normal. A typical sequence is SFT to teach the task, then DPO for tone, or GRPO for checkable correctness, all with LoRA adapters on the same base model. Separately, you might fine-tune an embedding model for retrieval. Each step needs its own evaluation, and each should earn its place: if DPO does not improve your blind win rate, drop it. More methods are not automatically better. | FAQ: combine methods?<br>• Yes: SFT → DPO or GRPO, with LoRA<br>• Embeddings on a separate track<br>• Each step must earn its place in evals | A layered pipeline diagram: base model, SFT layer, DPO layer, each with a small eval gate; a separate embedding model sits alongside. |
| 13 | 6:45 | 24 | Try this now. For one product you know, write three sentences that each start with the model currently fails when. Then next to each, write which method from the map fits, and what the training data would look like in one line. If you cannot describe the data, that is the real blocker to solve first. | Try this now<br>• 3 × "the model currently fails when…"<br>• Method per failure<br>• One-line data description<br>• Can't describe data? Solve that first | Three sentence strips appear; beside each, a method chip and a one-line JSON sample type in. |
| 14 | 7:09 | 66 | Watch me do it. I take a real product, a telecom self-service app, and list its three biggest AI complaints from support tickets. One: the assistant answers in long paragraphs when customers want short steps. Two: agents prefer a warmer tone than the model uses. Three: searches for eSIM activation do not find the right help article. For the first, I can write ideal short answers, so I mark SFT with LoRA and sketch one line of messages data. For the second, it is easier to compare than to write, so I mark DPO after SFT and sketch a chosen and rejected pair. For the third, retrieval is failing, so I check recall at fifty first: the right article is not even in the candidates, so a reranker will not help. I mark embedding fine-tuning with anchor and positive pairs. Three gaps, three methods, three data formats, written down in ten minutes. | Watch me do it<br>1 List 3 complaints<br>2 Short steps → SFT<br>3 Warmer tone → DPO<br>4 Missing articles → check recall@50 → embeddings<br>5 Sketch data for each | A table with three complaint rows; method badges drop into a second column; a third column shows a one-line JSON sample for each; a small recall@50 check appears for the third row. |
| 15 | 8:15 | 27 | Recap. SFT imitates demonstrations. LoRA is how you train cheaply, with any objective. DPO learns from preferences, reinforcement fine-tuning learns from a grader, distillation shrinks a teacher into a student, and embedding fine-tuning improves retrieval. Choose by gap type, then choose hosted or self-managed. Your next step: list three gaps in one product and map each to a method, data format and metric. | Recap<br>• Method by gap type<br>• Hosted vs self-managed is separate<br>• Next: map three gaps | Recap card with the seven-region map shrinking into an icon and a three-row table template. |

<details><summary>Narration per scene (paste into ElevenLabs)</summary>

Scene 1 — The methods map

```text
Ask five engineers what fine-tuning means and you will get five answers. One means training on examples, one means LoRA, one means RLHF, one means distillation, and one means making the search better. They are all different techniques with different data, tools and results. In this lesson you will get one clear map of customization methods, a decision flow to pick the right one, and the exact data format each needs.
```

Scene 2 — Five teaching styles

```text
Here is a way to remember the methods. SFT is teaching by showing: here is the task, here is how I would do it. Preference optimization is teaching by comparing: this answer is better than that one. Reinforcement fine-tuning is teaching by scoring: try many times, and I will mark each attempt. Distillation is apprenticeship: watch the master and copy. And embedding fine-tuning is training a librarian to find the right book. Five teaching styles for five kinds of learning.
```

Scene 3 — Map, part 1

```text
Let's walk the map. Continued pre-training feeds large amounts of raw domain text, and it is rare and expensive for businesses. Supervised fine-tuning, SFT, gives input and ideal output pairs, and the model imitates the demonstrated behavior. Parameter-efficient fine-tuning, like LoRA and QLoRA, trains small adapter weights instead of the whole model. Important: that is a how, not a what. You can do SFT with LoRA, preference training with LoRA, and reinforcement training with LoRA.
```

Scene 4 — Map, part 2

```text
Continuing. Preference optimization, which includes RLHF and DPO, uses pairs: a prompt, a preferred response and a rejected one, and nudges the model toward preferred answers. Great for tone and subjective quality. Reinforcement fine-tuning, with methods like GRPO, uses prompts plus a grader that scores outputs, perfect when correctness can be checked. Distillation trains a small student to imitate a strong teacher on your task. And embedding fine-tuning improves retrieval similarity, not generation.
```

Scene 5 — Hosted vs self-managed

```text
Separate from the method is where you run it. Hosted fine-tuning APIs from cloud providers, model vendors and inference platforms mean no GPU operations and integrated serving, but you are limited to their models and methods, your data goes to them, and offerings change. OpenAI, for example, began winding down its self-serve fine-tuning platform in twenty twenty-six. Self-managed training on open weights, with tools like TRL, PEFT, Unsloth and Axolotl, gives full control and portable adapters, but you own the pipeline.
```

Scene 6 — Decision flow

```text
Here is the decision flow. Is the gap knowledge? Use retrieval and stop. Can you write ideal outputs? Use SFT with LoRA. Is quality subjective and easier to compare than to write? Collect preference pairs and run DPO after SFT. Is correctness checkable by code or a reliable grader? Use reinforcement fine-tuning, usually after SFT. Is the goal cheaper and faster at similar quality? Distil. Is retrieval missing the right passages? Fine-tune the embedding model, or try a reranker first.
```

Scene 7 — One company, four methods

```text
One company, four methods. A Pakistani fintech app supports customers in English, Urdu and Roman Urdu. For routing eighteen intents, they use SFT with LoRA on a small model and six thousand labeled messages. For reply tone, agents find it easier to pick the better of two drafts, so they run DPO on top. For fee amounts in replies, a grader checks quotes against the fee schedule, so they use reinforcement fine-tuning. And because Roman Urdu searches missed English help articles, they fine-tune the embedding model.
```

Scene 8 — Data shapes

```text
Each method needs a different data shape, and the lesson shows all four. SFT uses chat messages ending in the ideal assistant reply. DPO uses a prompt, a chosen response and a rejected one. Reinforcement fine-tuning uses a prompt plus whatever fields your grader needs, like the expected fee. And embedding fine-tuning uses an anchor, the query, and a positive, the passage that should match. Getting the shape right on day one saves weeks.
```

Scene 9 — Pitfalls

```text
Four pitfalls. Choosing a method by hype, like we need RLHF, instead of by gap type. Running DPO before the model can do the task at all, so do SFT first. Building reinforcement fine-tuning without a reliable grader, because the model will learn to exploit a weak one. And assuming a hosted provider supports every method for every model. Check the docs.
```

Scene 10 — Simple example: recipe app

```text
A simple example. A recipe app wants three improvements. First, always output recipes in the same structured format: that is SFT with examples of the format. Second, users prefer friendlier, shorter tips, and it is easier to pick the better tip than to write the perfect one: that is preference data and DPO. Third, when a user searches for quick vegan dinner, results often miss the right recipes: that is an embedding problem. Three gaps, three different methods, and none of them is simply more fine-tuning.
```

Scene 11 — Sequencing

```text
A practical tip on sequencing. Most successful projects follow the same order: first SFT to teach the task and format, then, only if needed, preference optimization for subjective quality, or reinforcement fine-tuning for checkable correctness. Distillation is usually SFT with teacher outputs as the data. And embedding fine-tuning runs on its own track, because it improves retrieval rather than generation. Skipping SFT and jumping straight to DPO or GRPO usually gives weak results, because the model cannot yet do the basic task.
```

Scene 12 — FAQ: combine methods?

```text
A common question: can we combine methods in one model? Yes, and it is normal. A typical sequence is SFT to teach the task, then DPO for tone, or GRPO for checkable correctness, all with LoRA adapters on the same base model. Separately, you might fine-tune an embedding model for retrieval. Each step needs its own evaluation, and each should earn its place: if DPO does not improve your blind win rate, drop it. More methods are not automatically better.
```

Scene 13 — Try this now

```text
Try this now. For one product you know, write three sentences that each start with the model currently fails when. Then next to each, write which method from the map fits, and what the training data would look like in one line. If you cannot describe the data, that is the real blocker to solve first.
```

Scene 14 — Watch me do it

```text
Watch me do it. I take a real product, a telecom self-service app, and list its three biggest AI complaints from support tickets. One: the assistant answers in long paragraphs when customers want short steps. Two: agents prefer a warmer tone than the model uses. Three: searches for eSIM activation do not find the right help article. For the first, I can write ideal short answers, so I mark SFT with LoRA and sketch one line of messages data. For the second, it is easier to compare than to write, so I mark DPO after SFT and sketch a chosen and rejected pair. For the third, retrieval is failing, so I check recall at fifty first: the right article is not even in the candidates, so a reranker will not help. I mark embedding fine-tuning with anchor and positive pairs. Three gaps, three methods, three data formats, written down in ten minutes.
```

Scene 15 — Recap

```text
Recap. SFT imitates demonstrations. LoRA is how you train cheaply, with any objective. DPO learns from preferences, reinforcement fine-tuning learns from a grader, distillation shrinks a teacher into a student, and embedding fine-tuning improves retrieval. Choose by gap type, then choose hosted or self-managed. Your next step: list three gaps in one product and map each to a method, data format and metric.
```

</details>

### 3. Dataset design and curation: quality beats quantity

Lesson `dataset-design-and-curation` · module “Training data: design, synthesis and safety” · target 8 min · 15 scenes · 1176 words · planned 8:31 (words ≈ 8:31) · status: **to produce**

| Term | Say |
|---|---|
| kappa | KAP-uh |
| MinHash | min hash |
| JSONL | jason lines |

| # | Time | Sec | Narration | On screen | Visual |
|---|---|---|---|---|---|
| 1 | 0:00 | 31 | Here is a story I have seen many times. A team fine-tunes a model on twelve thousand examples, gets disappointing results, and spends two weeks tweaking learning rates. Nothing helps. Then someone reads a hundred training examples and finds duplicates, contradictory labels and a system prompt that does not match production. In this lesson you will learn to design and curate datasets so that the model learns what you actually mean. | Dataset design and curation<br>• The model becomes its data<br>• Guide, collect, clean, split<br>• Document it | A dashboard of learning-rate sliders being nudged endlessly, then a magnifying glass over a spreadsheet revealing duplicate rows and conflicting labels highlighted in red. |
| 2 | 0:31 | 34 | Think of training data as a recipe book you hand to a new cook. If half the recipes are duplicates, the cook thinks those dishes are twice as important. If two recipes for the same dish disagree on the salt, the cook learns to guess. And if the book only has summer dishes, the cook panics in winter. The cook can only be as good as the book. That is why dataset design matters more than any training setting. | Analogy: a recipe book<br>• Duplicates = false importance<br>• Contradictions = guessing<br>• Missing seasons = gaps<br>• Cook ≤ book | An open recipe book with pages flipping; duplicate pages glow, two conflicting pages flash red, and a missing "winter" tab appears as an empty slot. |
| 3 | 1:05 | 34 | Start with a labeling guide, before you collect anything. The task in one sentence and the exact output. Every label with a definition, two good examples and one tricky counter-example. Priority rules for ambiguous cases, like money issues outrank delivery issues. And what to do with out-of-scope, abusive or unexpected-language inputs. Then test the guide. Two people label the same hundred examples independently, and you measure agreement. If humans cannot apply the rule consistently, the model cannot learn it. | Step 1: the labeling guide<br>• Task + exact output<br>• Definitions and examples per label<br>• Priority rules for ambiguity<br>• Measure agreement between two annotators | A one-page guide document with sections filling in, then two annotator icons labeling the same list with a percentage agreement meter between them. |
| 4 | 1:39 | 34 | Step two is coverage, not volume. Think in dimensions. Every intent or output type. Every language and script your users write, for example English, Arabic, Urdu and Roman Urdu. Every channel, because WhatsApp messages look nothing like emails. A range of difficulty, including messages with two issues or sarcasm. Realistic lengths. And the edge cases where production fails. Build a coverage matrix, with classes as rows and languages or channels as columns, and fill empty cells on purpose. | Step 2: coverage<br>• Classes · languages · channels<br>• Difficulty · length · edge cases<br>• Coverage matrix: fill the gaps deliberately | Grid heat map with classes as rows and languages as columns; several cells are pale, then fill in as new examples are added. |
| 5 | 2:13 | 30 | How many examples do you need? It depends on the task and the base model, so treat any number as orientation to verify. Format and style changes often show up with a few hundred good examples. Classification across many classes and languages often needs thousands. Complex reasoning needs more, or a different method altogether. But a thousand clean, consistent, representative examples beat ten thousand messy ones almost every time. | How many?<br>• Style/format: often hundreds<br>• Many-class, multilingual classification: often thousands<br>• Complex reasoning: more, or RFT<br>• Quality beats quantity | Two stacks of cards: a tall messy stack and a shorter neat stack; a quality meter above favors the neat stack. |
| 6 | 2:43 | 30 | Step three, clean. Deduplicate exact and near duplicates, because duplicates overweight some patterns and leak across your splits. Review a sample of labels, starting with examples where a strong model disagrees with the label, since that is where errors cluster. Remove personal data you do not need. And normalize the format: the same system prompt and the same output formatting you will use in production, in every single example. | Step 3: clean<br>• Remove exact and near duplicates<br>• Review labels where a strong model disagrees<br>• Remove unneeded personal data<br>• Identical prompt and format as production | Cleaning animation: duplicate cards merge and fade, disputed labels get a reviewer flag, and a PII field is blurred out. |
| 7 | 3:13 | 31 | Step four, split without leakage. You need a training set, a validation set for tuning decisions, and a test set you touch once at the end. For conversations, never split randomly. Split by customer or by time, so all messages from one customer, or all tickets after a date, live in one split. Otherwise almost identical conversations sit in both training and test, and your scores look far better than reality. | Step 4: split<br>• Train · validation · test (touch once)<br>• Split by customer or time<br>• Random splits leak near-duplicates | Customer icons grouped into three coloured buckets, with a red dotted line showing what happens when one customer is split across buckets. |
| 8 | 3:44 | 31 | The lesson's script does the mechanical part. It validates every line of your JSON lines file, checks that the last message is the assistant's label and that the label is allowed, removes duplicate user messages, splits by customer, strips metadata before writing the training files, and prints a table of class counts per split. Read that table. Every class should appear in every split, and nothing important should be vanishingly rare. | Hands-on: prep_data.py<br>• Validate format and labels<br>• Deduplicate<br>• Split by customer<br>• Check class balance per split | Screen recording of the script running with a few skipped-line warnings, then a class-count table printed per split with one low count highlighted. |
| 9 | 4:15 | 37 | A worked example, with illustrative numbers. A Saudi telecom started with twelve thousand complaint messages. Forty percent were near duplicates from automated notifications, and two teams labeled billing differently. They wrote a priority rule, relabeled fifteen hundred disputed messages, removed duplicates and split by customer. The dataset shrank to six thousand eight hundred examples, and the fine-tuned model's test accuracy rose substantially. Less data, better model. They documented it all in a data card: sources, dates, languages, counts, guide version, agreement, cleaning and known gaps. | Worked example: Saudi telecom (illustrative)<br>• 12,000 → 6,800 examples<br>• Priority rule + relabeling<br>• Dedup + customer split<br>• Better accuracy · data card | A funnel shrinking a large pile to a smaller neat pile, with an accuracy bar rising beside it and a data card document appearing at the end. |
| 10 | 4:52 | 35 | A simple example. A florist's online shop wants to classify customer messages into order, delivery, complaint and other. They pull three hundred messages. Reading them, they find forty identical auto-replies, which they remove. They find two staff members labeled late delivery complaints differently, so they add a rule: anything about lateness is delivery. And they notice almost no messages from Valentine's week, their busiest period, so they pull more from last February. Three fixes, one afternoon, a much better dataset. | Simple example: florist shop<br>• Remove 40 duplicate auto-replies<br>• Rule: lateness → delivery<br>• Add Valentine's week messages | A message list scrolls; duplicate rows collapse, two conflicting labels merge under a new rule card, and a calendar highlights February with new rows sliding in. |
| 11 | 5:27 | 37 | One more habit that pays off: read your data. Before every training run, open fifty random examples and read them, start to finish. You will find things no script catches: a label that means something different to the night shift, a template reply pasted into hundreds of messages, a language you did not know you had. Keep a short log of what you found and what you changed. That log becomes part of your data card, and it is often the most useful page in it. | Read your data<br>• 50 random examples before every run<br>• Find what scripts miss<br>• Log findings in the data card | A reviewer scrolling through example cards with small sticky notes appearing on a few: "different meaning at night", "template reply", "unexpected language". |
| 12 | 6:04 | 33 | A question from teams with little data: can we start fine-tuning with only a few hundred examples? For narrow format or style tasks, sometimes yes, especially with LoRA on a capable base model. The key is that those few hundred are clean, consistent and representative. Start there, evaluate honestly, and let the error analysis tell you where more data is needed. Adding targeted examples for the specific failures usually helps more than doubling the dataset at random. | FAQ: only a few hundred examples?<br>• Narrow style/format: often enough to start<br>• Must be clean and representative<br>• Add targeted examples for failures | A small neat stack of example cards, then a targeted handful added exactly where an error-analysis chart shows the biggest bar. |
| 13 | 6:37 | 21 | Try this now. Export fifty real examples for one task, and read every one of them. Count duplicates, count examples you are unsure how to label, and note which languages and channels appear. Those three numbers will tell you more about your fine-tuning readiness than any model benchmark. | Try this now<br>• Read 50 real examples<br>• Count duplicates<br>• Count unsure labels<br>• Note languages and channels | Three counters animate upward as a list of fifty examples scrolls past: duplicates, unsure, and a small pie chart for languages. |
| 14 | 6:58 | 67 | Watch me do it. I export eight hundred support messages with labels from two teams. First I read fifty. I spot auto-generated delivery notifications, so I run a dedup on normalized text: one hundred and ten duplicates removed. Then I sort by label and read twenty billing examples: team A labels late refunds as billing, team B as refund. I write a priority rule, refunds outrank billing, and relabel the sixty affected messages. Next the coverage matrix: rows are intents, columns are English, Arabic and Roman Urdu. Roman Urdu account-access has only four examples, so I flag it for collection. Then I run the prep script: it validates every line, splits by customer, and prints class counts per split. One rare class is missing from the test split entirely, so I adjust the split seed. Finally I fill in the data card: source, dates, counts, the new priority rule and the known gap. | Watch me do it<br>1 Read 50<br>2 Dedup (−110)<br>3 Priority rule + relabel 60<br>4 Coverage matrix gap<br>5 Prep script + split check<br>6 Data card | A message list with duplicates collapsing and a counter showing 110 removed; two label columns reconcile under a rule card; a heat-map matrix with one pale cell; terminal class-count table; data card filling in. |
| 15 | 8:05 | 26 | Recap. The model becomes its data. Write and test a labeling guide. Cover the real variety of your traffic. Clean duplicates, fix labels and match production format. Split by customer or time. And document a data card. Your next step: write a one-page guide for one task, have two people label fifty examples, measure agreement, and run the prep script. | Recap<br>• Guide → coverage → clean → split → document<br>• Next: guide + 50 double-labeled examples | Recap card showing the five steps as a horizontal pipeline with ticks. |

<details><summary>Narration per scene (paste into ElevenLabs)</summary>

Scene 1 — Dataset design and curation

```text
Here is a story I have seen many times. A team fine-tunes a model on twelve thousand examples, gets disappointing results, and spends two weeks tweaking learning rates. Nothing helps. Then someone reads a hundred training examples and finds duplicates, contradictory labels and a system prompt that does not match production. In this lesson you will learn to design and curate datasets so that the model learns what you actually mean.
```

Scene 2 — Analogy: a recipe book

```text
Think of training data as a recipe book you hand to a new cook. If half the recipes are duplicates, the cook thinks those dishes are twice as important. If two recipes for the same dish disagree on the salt, the cook learns to guess. And if the book only has summer dishes, the cook panics in winter. The cook can only be as good as the book. That is why dataset design matters more than any training setting.
```

Scene 3 — Step 1: the labeling guide

```text
Start with a labeling guide, before you collect anything. The task in one sentence and the exact output. Every label with a definition, two good examples and one tricky counter-example. Priority rules for ambiguous cases, like money issues outrank delivery issues. And what to do with out-of-scope, abusive or unexpected-language inputs. Then test the guide. Two people label the same hundred examples independently, and you measure agreement. If humans cannot apply the rule consistently, the model cannot learn it.
```

Scene 4 — Step 2: coverage

```text
Step two is coverage, not volume. Think in dimensions. Every intent or output type. Every language and script your users write, for example English, Arabic, Urdu and Roman Urdu. Every channel, because WhatsApp messages look nothing like emails. A range of difficulty, including messages with two issues or sarcasm. Realistic lengths. And the edge cases where production fails. Build a coverage matrix, with classes as rows and languages or channels as columns, and fill empty cells on purpose.
```

Scene 5 — How many?

```text
How many examples do you need? It depends on the task and the base model, so treat any number as orientation to verify. Format and style changes often show up with a few hundred good examples. Classification across many classes and languages often needs thousands. Complex reasoning needs more, or a different method altogether. But a thousand clean, consistent, representative examples beat ten thousand messy ones almost every time.
```

Scene 6 — Step 3: clean

```text
Step three, clean. Deduplicate exact and near duplicates, because duplicates overweight some patterns and leak across your splits. Review a sample of labels, starting with examples where a strong model disagrees with the label, since that is where errors cluster. Remove personal data you do not need. And normalize the format: the same system prompt and the same output formatting you will use in production, in every single example.
```

Scene 7 — Step 4: split

```text
Step four, split without leakage. You need a training set, a validation set for tuning decisions, and a test set you touch once at the end. For conversations, never split randomly. Split by customer or by time, so all messages from one customer, or all tickets after a date, live in one split. Otherwise almost identical conversations sit in both training and test, and your scores look far better than reality.
```

Scene 8 — Hands-on: prep_data.py

```text
The lesson's script does the mechanical part. It validates every line of your JSON lines file, checks that the last message is the assistant's label and that the label is allowed, removes duplicate user messages, splits by customer, strips metadata before writing the training files, and prints a table of class counts per split. Read that table. Every class should appear in every split, and nothing important should be vanishingly rare.
```

Scene 9 — Worked example: Saudi telecom (illustrative)

```text
A worked example, with illustrative numbers. A Saudi telecom started with twelve thousand complaint messages. Forty percent were near duplicates from automated notifications, and two teams labeled billing differently. They wrote a priority rule, relabeled fifteen hundred disputed messages, removed duplicates and split by customer. The dataset shrank to six thousand eight hundred examples, and the fine-tuned model's test accuracy rose substantially. Less data, better model. They documented it all in a data card: sources, dates, languages, counts, guide version, agreement, cleaning and known gaps.
```

Scene 10 — Simple example: florist shop

```text
A simple example. A florist's online shop wants to classify customer messages into order, delivery, complaint and other. They pull three hundred messages. Reading them, they find forty identical auto-replies, which they remove. They find two staff members labeled late delivery complaints differently, so they add a rule: anything about lateness is delivery. And they notice almost no messages from Valentine's week, their busiest period, so they pull more from last February. Three fixes, one afternoon, a much better dataset.
```

Scene 11 — Read your data

```text
One more habit that pays off: read your data. Before every training run, open fifty random examples and read them, start to finish. You will find things no script catches: a label that means something different to the night shift, a template reply pasted into hundreds of messages, a language you did not know you had. Keep a short log of what you found and what you changed. That log becomes part of your data card, and it is often the most useful page in it.
```

Scene 12 — FAQ: only a few hundred examples?

```text
A question from teams with little data: can we start fine-tuning with only a few hundred examples? For narrow format or style tasks, sometimes yes, especially with LoRA on a capable base model. The key is that those few hundred are clean, consistent and representative. Start there, evaluate honestly, and let the error analysis tell you where more data is needed. Adding targeted examples for the specific failures usually helps more than doubling the dataset at random.
```

Scene 13 — Try this now

```text
Try this now. Export fifty real examples for one task, and read every one of them. Count duplicates, count examples you are unsure how to label, and note which languages and channels appear. Those three numbers will tell you more about your fine-tuning readiness than any model benchmark.
```

Scene 14 — Watch me do it

```text
Watch me do it. I export eight hundred support messages with labels from two teams. First I read fifty. I spot auto-generated delivery notifications, so I run a dedup on normalized text: one hundred and ten duplicates removed. Then I sort by label and read twenty billing examples: team A labels late refunds as billing, team B as refund. I write a priority rule, refunds outrank billing, and relabel the sixty affected messages. Next the coverage matrix: rows are intents, columns are English, Arabic and Roman Urdu. Roman Urdu account-access has only four examples, so I flag it for collection. Then I run the prep script: it validates every line, splits by customer, and prints class counts per split. One rare class is missing from the test split entirely, so I adjust the split seed. Finally I fill in the data card: source, dates, counts, the new priority rule and the known gap.
```

Scene 15 — Recap

```text
Recap. The model becomes its data. Write and test a labeling guide. Cover the real variety of your traffic. Clean duplicates, fix labels and match production format. Split by customer or time. And document a data card. Your next step: write a one-page guide for one task, have two people label fifty examples, measure agreement, and run the prep script.
```

</details>

### 4. Synthetic data generation and distillation

Lesson `synthetic-data-and-distillation` · module “Training data: design, synthesis and safety” · target 9 min · 15 scenes · 1196 words · planned 8:41 (words ≈ 8:40) · status: **to produce**

| Term | Say |
|---|---|
| DPO | D P O |
| vLLM | V L L M |
| gpt-oss | G P T O S S |
| Qwen | chwen |

| # | Time | Sec | Narration | On screen | Visual |
|---|---|---|---|---|---|
| 1 | 0:00 | 34 | Imagine you need two thousand labeled customer messages in Roman Urdu for a rare complaint type, and you have forty. You could wait six months for real data. Or you could ask a strong model to help. In this lesson you will learn how to use synthetic data and distillation properly: the four ways to use a teacher model, how to make synthetic data diverse, how to filter it, and the license checks that keep you out of trouble. | Synthetic data and distillation<br>• Four uses of a teacher<br>• Diversity and filtering<br>• License and terms checks | A small pile of 40 message cards next to an empty space for 2,000; a teacher model icon begins producing cards that pass through a filter. |
| 2 | 0:34 | 33 | Here is an analogy for distillation. A master chef runs a famous restaurant, but hiring the master for every branch is too expensive. So the master cooks a thousand plates of the signature dish while an apprentice watches, tastes and practices. The apprentice will never be the master, but for that one dish, they can get very close, and they cost a fraction as much. The key word is one dish. Distillation shines on narrow tasks. | Analogy: master and apprentice<br>• Teacher = master chef<br>• Student = apprentice<br>• One signature dish = narrow task | Kitchen scene: a large chef figure plates identical dishes while a smaller figure copies; a price tag on each shows a big difference, and a label "one dish" highlights. |
| 3 | 1:07 | 31 | First, the idea of distillation. A strong teacher model produces outputs on your task, and a smaller student model is trained to imitate them. Done well, the student keeps much of the teacher's quality on that one task, at a fraction of the cost and latency. Many public open-weight models were themselves trained partly this way. But the student also inherits the teacher's mistakes and biases, so filtering is not optional. | Distillation<br>• Teacher outputs → student training<br>• Much of the quality, far less cost<br>• Mistakes are inherited too | Large teacher icon passing answer cards down to a smaller student icon; one card has a red error mark that the student copies. |
| 4 | 1:38 | 32 | There are four ways to use a teacher. One, label real inputs you already have, with human spot-checks. This is usually the safest and most valuable. Two, write ideal responses to real prompts in your required style. Three, generate new inputs to fill coverage gaps, like rare intents or languages. Four, generate preference pairs: several responses per prompt, a judge picks the better one, and you get chosen and rejected pairs for DPO. | Four uses<br>1 Label real inputs (safest)<br>2 Write ideal responses<br>3 Generate inputs for gaps<br>4 Create preference pairs | Four numbered panels, each with a simple illustration: a label on a real message, a reply draft, a new message card, and two reply cards with a winner ribbon. |
| 5 | 2:10 | 33 | If you simply ask for five hundred customer complaints, you get five hundred cousins of the same complaint. The fix is to control attributes. Combine intent, language, channel, persona, difficulty and length, and generate a few examples for each combination. For example, a refund request, in Roman Urdu, on WhatsApp, angry and vague. Seed prompts with one or two real, anonymized examples to anchor the style. Diversity comes from the grid, not from the temperature. | Diversity by design<br>• intent × language × channel × difficulty<br>• A few examples per combination<br>• Seed with anonymized real examples | A multi-dimensional grid with sliders for intent, language, channel and difficulty; each combination spawns a distinct message card in a different style. |
| 6 | 2:43 | 29 | Filtering is the real work. Validators check schema, allowed labels, length and language. Deduplication removes the repetition synthetic data loves. Consistency checks have the teacher, or a second model, re-label each message, and disagreements go to humans instead of training. Decontamination removes anything too similar to your evaluation set. And humans review a sample of every batch. Track the rejection rate; if it jumps, something upstream changed. | Filter hard<br>• Validators<br>• Deduplication<br>• Re-label consistency → humans<br>• Decontaminate vs eval set<br>• Human sampling + rejection rate | Cards flowing through five sequential filter gates; rejected cards drop into labeled bins, and a rejection-rate gauge sits at the end. |
| 7 | 3:12 | 41 | Now the license and terms check, and please do not skip it. Some API providers' terms restrict using outputs to develop models that compete with them. Read the current terms of any API you use as a teacher and write down your conclusion. Open-weight teachers under Apache or MIT, like gpt-oss, many Qwen releases or DeepSeek's MIT releases, are simpler. Llama licenses attach conditions to training other models on Llama outputs, such as naming requirements, so check the exact version. And your real inputs must be usable for training under your privacy notices and contracts. | License and terms<br>• API terms on training with outputs<br>• Apache/MIT teachers are simpler<br>• Llama: conditions on outputs<br>• Rights to real inputs | Document stack with three highlighted clauses; a checklist beside it gets ticked and signed with a date. |
| 8 | 3:53 | 33 | The hands-on script uses an open-weight teacher behind any OpenAI-compatible endpoint. It loops over combinations of intent, language, channel and difficulty, generates one message each with fictional names and numbers, deduplicates, and has the teacher re-label each message at temperature zero with your priority rule. Agreements go into the training file with metadata marking them synthetic. Disagreements go to a review file for humans. Then you decontaminate, sample for review and mix with real data. | Hands-on: synth.py<br>• Attribute grid → generate<br>• Dedup → re-label at temperature 0<br>• Agree → train file · disagree → review<br>• Mark synthetic in metadata | Screen recording of synth.py running with a live counter of kept versus reviewed examples, then two output files appearing in a folder. |
| 9 | 4:26 | 38 | A worked example, with illustrative numbers. A UK subscription box company used a big API model with a long style guide for customer replies. They generated teacher replies for three thousand real, redacted emails, had agents approve or edit six hundred, filtered out replies failing a policy checker, and trained a small open model with LoRA. On real held-out emails graded by agents, the student came within a few points of the teacher's approval rate at a much lower cost, with the teacher kept as a fallback. | Worked example: UK subscription box (illustrative)<br>• 3,000 real emails → teacher replies<br>• 600 agent-reviewed + policy filter<br>• LoRA student within a few points<br>• Teacher as fallback | Before-and-after cost per reply bar chart and an approval-rate comparison with overlapping confidence whiskers. |
| 10 | 5:04 | 37 | A simple example. A language school has two hundred real student questions about course schedules in English, but almost none in Arabic. They ask an open-weight teacher model to write Arabic versions of the questions in different styles, formal, casual and with typos, using made-up names. They generate six hundred, filter duplicates and anything off-topic, have a bilingual teacher review fifty, and keep four hundred. The Arabic gap in their coverage matrix is filled, and they test only on real Arabic questions collected later. | Simple example: language school<br>• Gap: Arabic questions<br>• Generate 600 in varied styles<br>• Filter + review 50 → keep 400<br>• Test on real Arabic only | Coverage matrix with an empty Arabic cell; generated cards pour in, pass a filter, a reviewer icon checks a sample, and the cell fills to a healthy level. |
| 11 | 5:41 | 35 | How much synthetic data is too much? There is no universal number, but watch two signals. First, per-class share: if a class is mostly synthetic, its test performance on real data is the only thing to trust. Second, style drift: synthetic messages tend to be cleaner, more polite and more grammatical than real ones. If your model starts struggling with typos, slang or voice-note transcripts, you have too much clean synthetic data. Keep real examples dominant wherever you have them. | Too much synthetic?<br>• Watch synthetic share per class<br>• Watch style drift: too clean, too polite<br>• Keep real data dominant | Stacked bars per class showing real and synthetic proportions; one bar mostly synthetic is flagged; a message card with typos contrasts with a polished synthetic one. |
| 12 | 6:16 | 30 | A question I hear: if the teacher model makes mistakes, will the student copy them? Yes, faithfully. That is why filtering matters so much, and why the student should be evaluated against real data, not against the teacher. Sometimes a well-filtered student even outperforms its teacher on the narrow task, because the filters removed the teacher's worst outputs. But you only know that from an honest evaluation on real examples. | FAQ: will the student copy teacher mistakes?<br>• Yes, without filtering<br>• Evaluate on real data, not vs teacher<br>• Filtered students can beat teachers on narrow tasks | Teacher outputs pass through a filter that removes red-flagged cards; the student trains on the rest, and its score bar is compared on real data. |
| 13 | 6:46 | 22 | Try this now. Find the emptiest cell in your coverage matrix, the class and language combination with the fewest real examples. Write a generation prompt for it that specifies intent, language, channel and difficulty, and includes one anonymized real example. Generate twenty, and read all twenty before you generate any more. | Try this now<br>• Find the emptiest cell<br>• Write a controlled generation prompt<br>• Generate 20, read all 20 | The coverage matrix appears; one cell pulses. A prompt card types beside it, and twenty small cards appear in a grid with a reviewer eye icon. |
| 14 | 7:08 | 67 | Watch me do it. My coverage matrix shows almost no Roman Urdu messages about card blocking. I start the teacher model on our vLLM server and run the synthetic script only for that cell, across two channels and three difficulty styles, generating sixty messages with fictional names. The script deduplicates and re-labels each one with the teacher. Fifty-one agree with the intended label and go into the synthetic file; nine go to the review file. I read the nine: four are genuinely ambiguous, card blocked and fraud together, so I add them as edge cases with a human label. Five are off-topic and I drop them. Then I read twenty of the kept messages: two sound too polished for WhatsApp, so I add a style note to the generation prompt, typos and short sentences, and regenerate that batch. Finally I decontaminate against the test set and record the synthetic share for that class. | Watch me do it<br>1 Target one empty cell<br>2 Generate 60<br>3 51 kept · 9 to review<br>4 Read and fix style<br>5 Decontaminate + record share | Coverage matrix with one pulsing cell; terminal generation counter; kept vs review files; a reviewer view of nine messages sorted into edge case and drop; a style note added to the prompt; a synthetic-share badge on the class. |
| 15 | 8:15 | 26 | Recap. Use teachers to label, write, fill gaps and create preference pairs. Design for diversity with attribute grids. Filter hard, and evaluate the student only on real held-out data. And always check terms and licenses. Your next step: run the pipeline for one under-covered cell of your coverage matrix, review fifty outputs, and report your rejection rate and the reasons. | Recap<br>• Four uses, diversity, filtering<br>• Real data for evaluation<br>• Next: one coverage gap, 50 reviewed outputs | Recap card with a teacher-student icon, a filter icon and a document icon. |

<details><summary>Narration per scene (paste into ElevenLabs)</summary>

Scene 1 — Synthetic data and distillation

```text
Imagine you need two thousand labeled customer messages in Roman Urdu for a rare complaint type, and you have forty. You could wait six months for real data. Or you could ask a strong model to help. In this lesson you will learn how to use synthetic data and distillation properly: the four ways to use a teacher model, how to make synthetic data diverse, how to filter it, and the license checks that keep you out of trouble.
```

Scene 2 — Analogy: master and apprentice

```text
Here is an analogy for distillation. A master chef runs a famous restaurant, but hiring the master for every branch is too expensive. So the master cooks a thousand plates of the signature dish while an apprentice watches, tastes and practices. The apprentice will never be the master, but for that one dish, they can get very close, and they cost a fraction as much. The key word is one dish. Distillation shines on narrow tasks.
```

Scene 3 — Distillation

```text
First, the idea of distillation. A strong teacher model produces outputs on your task, and a smaller student model is trained to imitate them. Done well, the student keeps much of the teacher's quality on that one task, at a fraction of the cost and latency. Many public open-weight models were themselves trained partly this way. But the student also inherits the teacher's mistakes and biases, so filtering is not optional.
```

Scene 4 — Four uses

```text
There are four ways to use a teacher. One, label real inputs you already have, with human spot-checks. This is usually the safest and most valuable. Two, write ideal responses to real prompts in your required style. Three, generate new inputs to fill coverage gaps, like rare intents or languages. Four, generate preference pairs: several responses per prompt, a judge picks the better one, and you get chosen and rejected pairs for DPO.
```

Scene 5 — Diversity by design

```text
If you simply ask for five hundred customer complaints, you get five hundred cousins of the same complaint. The fix is to control attributes. Combine intent, language, channel, persona, difficulty and length, and generate a few examples for each combination. For example, a refund request, in Roman Urdu, on WhatsApp, angry and vague. Seed prompts with one or two real, anonymized examples to anchor the style. Diversity comes from the grid, not from the temperature.
```

Scene 6 — Filter hard

```text
Filtering is the real work. Validators check schema, allowed labels, length and language. Deduplication removes the repetition synthetic data loves. Consistency checks have the teacher, or a second model, re-label each message, and disagreements go to humans instead of training. Decontamination removes anything too similar to your evaluation set. And humans review a sample of every batch. Track the rejection rate; if it jumps, something upstream changed.
```

Scene 7 — License and terms

```text
Now the license and terms check, and please do not skip it. Some API providers' terms restrict using outputs to develop models that compete with them. Read the current terms of any API you use as a teacher and write down your conclusion. Open-weight teachers under Apache or MIT, like gpt-oss, many Qwen releases or DeepSeek's MIT releases, are simpler. Llama licenses attach conditions to training other models on Llama outputs, such as naming requirements, so check the exact version. And your real inputs must be usable for training under your privacy notices and contracts.
```

Scene 8 — Hands-on: synth.py

```text
The hands-on script uses an open-weight teacher behind any OpenAI-compatible endpoint. It loops over combinations of intent, language, channel and difficulty, generates one message each with fictional names and numbers, deduplicates, and has the teacher re-label each message at temperature zero with your priority rule. Agreements go into the training file with metadata marking them synthetic. Disagreements go to a review file for humans. Then you decontaminate, sample for review and mix with real data.
```

Scene 9 — Worked example: UK subscription box (illustrative)

```text
A worked example, with illustrative numbers. A UK subscription box company used a big API model with a long style guide for customer replies. They generated teacher replies for three thousand real, redacted emails, had agents approve or edit six hundred, filtered out replies failing a policy checker, and trained a small open model with LoRA. On real held-out emails graded by agents, the student came within a few points of the teacher's approval rate at a much lower cost, with the teacher kept as a fallback.
```

Scene 10 — Simple example: language school

```text
A simple example. A language school has two hundred real student questions about course schedules in English, but almost none in Arabic. They ask an open-weight teacher model to write Arabic versions of the questions in different styles, formal, casual and with typos, using made-up names. They generate six hundred, filter duplicates and anything off-topic, have a bilingual teacher review fifty, and keep four hundred. The Arabic gap in their coverage matrix is filled, and they test only on real Arabic questions collected later.
```

Scene 11 — Too much synthetic?

```text
How much synthetic data is too much? There is no universal number, but watch two signals. First, per-class share: if a class is mostly synthetic, its test performance on real data is the only thing to trust. Second, style drift: synthetic messages tend to be cleaner, more polite and more grammatical than real ones. If your model starts struggling with typos, slang or voice-note transcripts, you have too much clean synthetic data. Keep real examples dominant wherever you have them.
```

Scene 12 — FAQ: will the student copy teacher mistakes?

```text
A question I hear: if the teacher model makes mistakes, will the student copy them? Yes, faithfully. That is why filtering matters so much, and why the student should be evaluated against real data, not against the teacher. Sometimes a well-filtered student even outperforms its teacher on the narrow task, because the filters removed the teacher's worst outputs. But you only know that from an honest evaluation on real examples.
```

Scene 13 — Try this now

```text
Try this now. Find the emptiest cell in your coverage matrix, the class and language combination with the fewest real examples. Write a generation prompt for it that specifies intent, language, channel and difficulty, and includes one anonymized real example. Generate twenty, and read all twenty before you generate any more.
```

Scene 14 — Watch me do it

```text
Watch me do it. My coverage matrix shows almost no Roman Urdu messages about card blocking. I start the teacher model on our vLLM server and run the synthetic script only for that cell, across two channels and three difficulty styles, generating sixty messages with fictional names. The script deduplicates and re-labels each one with the teacher. Fifty-one agree with the intended label and go into the synthetic file; nine go to the review file. I read the nine: four are genuinely ambiguous, card blocked and fraud together, so I add them as edge cases with a human label. Five are off-topic and I drop them. Then I read twenty of the kept messages: two sound too polished for WhatsApp, so I add a style note to the generation prompt, typos and short sentences, and regenerate that batch. Finally I decontaminate against the test set and record the synthetic share for that class.
```

Scene 15 — Recap

```text
Recap. Use teachers to label, write, fill gaps and create preference pairs. Design for diversity with attribute grids. Filter hard, and evaluate the student only on real held-out data. And always check terms and licenses. Your next step: run the pipeline for one under-covered cell of your coverage matrix, review fifty outputs, and report your rejection rate and the reasons.
```

</details>

### 5. Training data safety, licensing and privacy

Lesson `data-safety-licensing-and-privacy` · module “Training data: design, synthesis and safety” · target 8 min · 15 scenes · 1179 words · planned 8:32 (words ≈ 8:33) · status: **to produce**

| Term | Say |
|---|---|
| PII | P I I |
| Presidio | preh-SID-ee-oh |
| CNIC | C N I C |
| ICO | I C O |
| GPAI | G P A I |
| spaCy | spay-see |

| # | Time | Sec | Narration | On screen | Visual |
|---|---|---|---|---|---|
| 1 | 0:00 | 33 | Here is an uncomfortable fact. If a training example contains a customer's phone number, the fine-tuned model might one day repeat it to someone else. And deleting that customer from your database does not delete them from the model. In this lesson you will learn to treat training data as both an asset and a liability: rights, minimization, PII redaction, copyright and licenses, regulation touchpoints, and safety testing after tuning. This is practical guidance, not legal advice. | Data safety, licensing, privacy<br>• Memorization is real<br>• Rights and minimization<br>• Redaction, licenses, safety tests | A model icon with a speech bubble containing a blurred phone number; a delete key is pressed on a database row but the bubble remains. |
| 2 | 0:33 | 36 | Here is an analogy. Training a model on data is like mixing ingredients into a cake. Before baking, you can still pick out a raisin you did not want. After baking, the raisin is inside, and getting it out means baking a new cake. Personal data in training sets is that raisin. So you check the ingredients before you bake: do we have the right to use them, do we need them at all, and have we removed what should not be there? | Analogy: baking a cake<br>• Before baking: remove what you don't want<br>• After baking: remake the cake<br>• Check ingredients first | Mixing bowl with ingredients; a tweezer removes one raisin labeled PII before the bowl goes into an oven. Then a finished cake with a highlighted raisin inside and a "bake again" label. |
| 3 | 1:09 | 36 | Ask four questions of every dataset. Rights: are we allowed to use this for training, under privacy law, contracts, platform terms, copyright and dataset licenses? Minimization: does the task need personal data at all? An intent classifier does not need names or account numbers. Security: where is data stored, who can access it, and where does training run? And retention and deletion: how long do we keep raw data, cleaned data and models, and what happens when someone asks to be deleted? | Four questions<br>• Rights<br>• Minimization<br>• Security<br>• Retention and deletion | Four quadrant diagram with an icon in each: a scale of justice, a funnel, a padlock and a clock. |
| 4 | 1:45 | 37 | Privacy frameworks shape the answers. Under GDPR and UK GDPR, training can be a new purpose compared with why you collected the data, so you need a lawful basis, transparency and respect for data subject rights. Regulators, like the UK ICO, have published guidance on AI and personal data. Saudi Arabia's and the UAE's data protection laws, plus DIFC and ADGM regimes, affect processing and where training can happen. Sector rules often go further. For significant personal data or high-impact uses, do an impact assessment first. | Privacy frameworks<br>• GDPR / UK GDPR: purpose, basis, rights<br>• KSA and UAE PDPL; DIFC, ADGM<br>• Sector rules<br>• Impact assessment first | Map highlighting UK, EU, KSA and UAE with regime labels; then a clipboard titled impact assessment. |
| 5 | 2:22 | 35 | Now redaction. Detect personal data with a tool like Microsoft Presidio, and add custom recognizers for local identifiers, like Pakistani CNIC numbers or Emirates ID numbers. Replace rather than delete: swap real values for consistent fake ones so the model still learns the structure of a message. Then check quality on a sample, because recognizers miss things, especially in Arabic, Urdu and mixed scripts. Keep a mapping back to real values only if you truly need it, locked away separately. | Redaction<br>• Presidio + local ID recognizers<br>• Replace with consistent fakes<br>• Measure misses on a sample<br>• Avoid keeping mappings | Text sample with highlighted entities being replaced by placeholder tokens; a small miss counter increments when a mixed-script name slips through. |
| 6 | 2:57 | 28 | Copyright and licenses next. Customer content, scraped text, bought datasets and open datasets all have terms. CC BY needs attribution, share-alike adds conditions, and non-commercial licenses forbid commercial use. Copyright law on AI training differs by country and is still evolving, so prefer data you created, licensed for training, or that your contracts permit. And remember the base model's license carries into your fine-tuned version. | Copyright and licenses<br>• CC BY, BY-SA, NC terms<br>• Law still evolving by jurisdiction<br>• Prefer owned or explicitly licensed data<br>• Base model license carries through | Stack of dataset folders each with a small license tag; one NC tag turns red next to a commercial product icon. |
| 7 | 3:25 | 31 | Regulation touchpoints. Under the EU AI Act, providers of general-purpose AI models have documentation and copyright duties. European Commission guidance says only substantial modifications, judged with a compute-based indicator, turn someone who modifies a model into a provider of a general-purpose model. Typical small LoRA fine-tunes are far below that, but check the current guidance. Separately, your use case may be high-risk or carry transparency duties regardless of how you trained. | Regulation touchpoints<br>• EU AI Act GPAI provider duties<br>• Substantial modification test (compute-based)<br>• Small LoRA fine-tunes usually far below<br>• Use case obligations apply anyway | A dial labeled "modification compute" with a small LoRA marker near zero and a threshold marker far along; then a separate panel labeled "your use case". |
| 8 | 3:56 | 28 | Fine-tuning can also weaken a model's safety behavior, even with harmless data. So add safety evaluations to your before-and-after tests: refusals of harmful requests, resistance to jailbreak attempts, and no leakage of training data. A simple memorization test prompts the model with the start of real training records and checks that it does not complete sensitive details. If safety drops, mix safety examples into training. | Safety after tuning<br>• Refusals and jailbreak resistance<br>• Memorization test<br>• Add safety examples if needed | Before-and-after gauges for refusal rate and leakage; a prompt starting with a real record prefix is shown, and the model output is checked by a scanner. |
| 9 | 4:24 | 29 | A worked example. A clinic network in Pakistan wants a model that drafts follow-up reminders from appointment notes. They minimize first: the model needs the appointment type and timing, not diagnoses. They redact names and CNICs with custom recognizers, train on their own servers, run an impact assessment, add a memorization test, and update patient notices where required. The model is useful, and the risk is proportionate. | Worked example: clinic network<br>• Minimize: type + timing only<br>• Redact names, CNICs<br>• Train on own servers<br>• Impact assessment + memorization test | Clinic building icon; a note card is trimmed down to two fields, passes a redaction step, and enters an on-premise training box. |
| 10 | 4:53 | 34 | A simple example. An estate agency wants a model that classifies inquiry emails. The emails contain names, phone numbers and addresses. For classification, none of that is needed. So they replace names with Customer, phone numbers with a fixed fake number, and addresses with City, before training. They check fifty redacted emails by hand, find three missed phone numbers in an unusual format, add a pattern for it, and re-run. The model learns the intent, not the people. | Simple example: estate agency<br>• Replace names, phones, addresses<br>• Hand-check 50 → 3 misses<br>• Add a pattern, re-run<br>• Model learns intent, not people | An email with highlighted entities being replaced by placeholders; a checklist shows 47 clean and 3 flagged, then a new pattern card is added and all 50 turn clean. |
| 11 | 5:27 | 31 | Document it so others can trust it. Your data card should record the lawful basis or contractual permission you rely on, what personal data was removed and how, the measured redaction miss rate, the licenses of any external datasets, the base model license, and who approved the use. When a regulator, customer or auditor asks how the model was trained, you want to hand over a document, not start an investigation. | Document for trust<br>• Lawful basis or permission<br>• Redaction method + miss rate<br>• Dataset and base-model licenses<br>• Approver | Data card document with sections highlighted in sequence, then handed across a desk to an auditor icon. |
| 12 | 5:58 | 35 | A practical question: can we use customer support transcripts to train a model? Possibly, but check three things first. Does your privacy notice, contract or other legal basis cover this use? Can you remove or replace personal data without losing what the model needs to learn? And do any customer contracts restrict how their data may be used? If all three check out, document the decision in the data card, and consider giving enterprise customers a way to opt out. | FAQ: train on support transcripts?<br>• Legal basis/notice/contract covers it?<br>• Can PII be removed without losing value?<br>• Customer contract restrictions?<br>• Document + consider opt-out | Three checkpoint gates labeled basis, redaction and contracts; transcript cards pass through each and land in a dataset folder with a data card attached. |
| 13 | 6:33 | 23 | Try this now. Take ten real training examples and highlight every piece of personal data by hand. Then run an automatic detector on the same ten and compare. Count what the detector missed. That miss count, even on ten examples, tells you whether your redaction is ready or needs custom patterns first. | Try this now<br>• Hand-highlight PII in 10 examples<br>• Run a detector on the same 10<br>• Count the misses | Two columns of the same ten examples: hand-highlighted on the left, detector-highlighted on the right; mismatches blink and a miss counter increments. |
| 14 | 6:56 | 67 | Watch me do it. I take one hundred training examples and run the redaction script with the default recognizers plus our CNIC and Emirates ID patterns. Then I review every redacted example by hand, with the original beside it. I find six misses: four phone numbers written with spaces, one name in Urdu script, one account number inside a sentence. I add a phone pattern that accepts spaces and an account-number pattern, rerun, and review again: one miss left, the Urdu name. I note it as a known limitation and add a human review step for Urdu-script messages. Then the rights check: I open our privacy notice and the relevant customer contract clause, and write in the data card the basis we rely on and who approved it. Finally I add a memorization test to the evaluation plan: ten prompts starting with the beginning of real records, checking the model does not complete sensitive details. | Watch me do it<br>1 Redact 100 examples<br>2 Hand review: 6 misses<br>3 Add patterns → 1 miss<br>4 Rights check → data card<br>5 Add memorization test | Two-column review screen with original and redacted text; miss counter drops from 6 to 1; a patterns file gains two lines; the data card fields for basis and approver fill in; a memorization test list appears. |
| 15 | 8:03 | 29 | Recap. Models can memorize, so treat training data as a liability. Ask about rights, minimization, security and deletion. Redact and replace personal data, and measure what you miss. Respect copyright and licenses. Know your regulation touchpoints. And re-test safety after tuning. Your next step: run the redaction script on a hundred of your examples, review misses by hand, and record the miss rate in your data card. | Recap<br>• Four questions<br>• Redact, measure, document<br>• Next: 100-example redaction audit | Recap card with a shield, a document and a checklist icon. |

<details><summary>Narration per scene (paste into ElevenLabs)</summary>

Scene 1 — Data safety, licensing, privacy

```text
Here is an uncomfortable fact. If a training example contains a customer's phone number, the fine-tuned model might one day repeat it to someone else. And deleting that customer from your database does not delete them from the model. In this lesson you will learn to treat training data as both an asset and a liability: rights, minimization, PII redaction, copyright and licenses, regulation touchpoints, and safety testing after tuning. This is practical guidance, not legal advice.
```

Scene 2 — Analogy: baking a cake

```text
Here is an analogy. Training a model on data is like mixing ingredients into a cake. Before baking, you can still pick out a raisin you did not want. After baking, the raisin is inside, and getting it out means baking a new cake. Personal data in training sets is that raisin. So you check the ingredients before you bake: do we have the right to use them, do we need them at all, and have we removed what should not be there?
```

Scene 3 — Four questions

```text
Ask four questions of every dataset. Rights: are we allowed to use this for training, under privacy law, contracts, platform terms, copyright and dataset licenses? Minimization: does the task need personal data at all? An intent classifier does not need names or account numbers. Security: where is data stored, who can access it, and where does training run? And retention and deletion: how long do we keep raw data, cleaned data and models, and what happens when someone asks to be deleted?
```

Scene 4 — Privacy frameworks

```text
Privacy frameworks shape the answers. Under GDPR and UK GDPR, training can be a new purpose compared with why you collected the data, so you need a lawful basis, transparency and respect for data subject rights. Regulators, like the UK ICO, have published guidance on AI and personal data. Saudi Arabia's and the UAE's data protection laws, plus DIFC and ADGM regimes, affect processing and where training can happen. Sector rules often go further. For significant personal data or high-impact uses, do an impact assessment first.
```

Scene 5 — Redaction

```text
Now redaction. Detect personal data with a tool like Microsoft Presidio, and add custom recognizers for local identifiers, like Pakistani CNIC numbers or Emirates ID numbers. Replace rather than delete: swap real values for consistent fake ones so the model still learns the structure of a message. Then check quality on a sample, because recognizers miss things, especially in Arabic, Urdu and mixed scripts. Keep a mapping back to real values only if you truly need it, locked away separately.
```

Scene 6 — Copyright and licenses

```text
Copyright and licenses next. Customer content, scraped text, bought datasets and open datasets all have terms. CC BY needs attribution, share-alike adds conditions, and non-commercial licenses forbid commercial use. Copyright law on AI training differs by country and is still evolving, so prefer data you created, licensed for training, or that your contracts permit. And remember the base model's license carries into your fine-tuned version.
```

Scene 7 — Regulation touchpoints

```text
Regulation touchpoints. Under the EU AI Act, providers of general-purpose AI models have documentation and copyright duties. European Commission guidance says only substantial modifications, judged with a compute-based indicator, turn someone who modifies a model into a provider of a general-purpose model. Typical small LoRA fine-tunes are far below that, but check the current guidance. Separately, your use case may be high-risk or carry transparency duties regardless of how you trained.
```

Scene 8 — Safety after tuning

```text
Fine-tuning can also weaken a model's safety behavior, even with harmless data. So add safety evaluations to your before-and-after tests: refusals of harmful requests, resistance to jailbreak attempts, and no leakage of training data. A simple memorization test prompts the model with the start of real training records and checks that it does not complete sensitive details. If safety drops, mix safety examples into training.
```

Scene 9 — Worked example: clinic network

```text
A worked example. A clinic network in Pakistan wants a model that drafts follow-up reminders from appointment notes. They minimize first: the model needs the appointment type and timing, not diagnoses. They redact names and CNICs with custom recognizers, train on their own servers, run an impact assessment, add a memorization test, and update patient notices where required. The model is useful, and the risk is proportionate.
```

Scene 10 — Simple example: estate agency

```text
A simple example. An estate agency wants a model that classifies inquiry emails. The emails contain names, phone numbers and addresses. For classification, none of that is needed. So they replace names with Customer, phone numbers with a fixed fake number, and addresses with City, before training. They check fifty redacted emails by hand, find three missed phone numbers in an unusual format, add a pattern for it, and re-run. The model learns the intent, not the people.
```

Scene 11 — Document for trust

```text
Document it so others can trust it. Your data card should record the lawful basis or contractual permission you rely on, what personal data was removed and how, the measured redaction miss rate, the licenses of any external datasets, the base model license, and who approved the use. When a regulator, customer or auditor asks how the model was trained, you want to hand over a document, not start an investigation.
```

Scene 12 — FAQ: train on support transcripts?

```text
A practical question: can we use customer support transcripts to train a model? Possibly, but check three things first. Does your privacy notice, contract or other legal basis cover this use? Can you remove or replace personal data without losing what the model needs to learn? And do any customer contracts restrict how their data may be used? If all three check out, document the decision in the data card, and consider giving enterprise customers a way to opt out.
```

Scene 13 — Try this now

```text
Try this now. Take ten real training examples and highlight every piece of personal data by hand. Then run an automatic detector on the same ten and compare. Count what the detector missed. That miss count, even on ten examples, tells you whether your redaction is ready or needs custom patterns first.
```

Scene 14 — Watch me do it

```text
Watch me do it. I take one hundred training examples and run the redaction script with the default recognizers plus our CNIC and Emirates ID patterns. Then I review every redacted example by hand, with the original beside it. I find six misses: four phone numbers written with spaces, one name in Urdu script, one account number inside a sentence. I add a phone pattern that accepts spaces and an account-number pattern, rerun, and review again: one miss left, the Urdu name. I note it as a known limitation and add a human review step for Urdu-script messages. Then the rights check: I open our privacy notice and the relevant customer contract clause, and write in the data card the basis we rely on and who approved it. Finally I add a memorization test to the evaluation plan: ten prompts starting with the beginning of real records, checking the model does not complete sensitive details.
```

Scene 15 — Recap

```text
Recap. Models can memorize, so treat training data as a liability. Ask about rights, minimization, security and deletion. Redact and replace personal data, and measure what you miss. Respect copyright and licenses. Know your regulation touchpoints. And re-test safety after tuning. Your next step: run the redaction script on a hundred of your examples, review misses by hand, and record the miss rate in your data card.
```

</details>

### 6. Supervised fine-tuning fundamentals

Lesson `sft-fundamentals` · module “Supervised fine-tuning and PEFT” · target 8 min · 15 scenes · 1178 words · planned 8:33 (words ≈ 8:32) · status: **to produce**

| Term | Say |
|---|---|
| SFT | S F T |
| TRL | T R L |
| LoRA | LOR-ah |
| bf16 | B F sixteen |
| epoch | EE-pock |

| # | Time | Sec | Narration | On screen | Visual |
|---|---|---|---|---|---|
| 1 | 0:00 | 29 | Supervised fine-tuning is the workhorse of model customization. It is also where most first attempts quietly go wrong, not because the idea is hard, but because of three small details. In this lesson you will see what actually happens during SFT, the data formats that make it easy, the hyperparameters that matter, how to read training curves, and a complete working script with TRL and LoRA. | SFT fundamentals<br>• What happens in training<br>• Formats and hyperparameters<br>• Curves and a working script | An abstract neural network whose connection strengths gently shift as example cards flow into it. |
| 2 | 0:29 | 37 | An analogy for SFT. Imagine teaching someone to write customer replies by giving them a folder of excellent past replies, each paired with the customer's message. They read message, reply, message, reply, thousands of times, and slowly their instincts shift. That is SFT. Loss masking means you only grade them on the replies they write, not on copying out the customer's message. And the chat template is the letterhead: if you train on one letterhead and then hand them a different one at work, they hesitate. | Analogy: learning from a folder of great replies<br>• Message → reply pairs<br>• Graded only on replies (loss masking)<br>• Same letterhead (template) at work | A folder opens to paired message and reply cards; only reply cards get a red-pen grade mark. Then two letterheads appear, one matching and one mismatched with a question mark. |
| 3 | 1:06 | 27 | A language model predicts the next token. SFT simply continues that training on your examples. For each example the model sees the prompt and the ideal response, and its weights are nudged so the ideal response becomes a little more likely. Repeat over thousands of examples, for one to a few passes called epochs, and the model's default behavior shifts toward your demonstrations. | What SFT does<br>• Next-token training on your examples<br>• Make ideal responses more likely<br>• 1–3 epochs | Token-by-token animation of a response being predicted, with probability bars for the correct next token growing taller with each pass. |
| 4 | 1:33 | 29 | Three details make or break it. First, loss masking. You want the model to learn to produce answers, not to reproduce your prompts, so compute the loss on the response tokens only. Second, the chat template. Instruction-tuned models expect conversations wrapped in special tokens, and training and serving must use the same template. Third, consistency. The system prompt, formatting and style in training must match production exactly. | Three make-or-break details<br>• Loss on responses only<br>• Same chat template in training and serving<br>• Same system prompt and format as production | A conversation transcript where the assistant turn is highlighted as "learned" and the prompt is greyed out; then two template frames snapping together labeled train and serve. |
| 5 | 2:02 | 32 | TRL, Hugging Face's training library, understands conversational data. The simplest reliable shape for classification and extraction is prompt and completion: the prompt holds the system and user messages, the completion holds the assistant's answer. With that shape, TRL computes loss on the completion only by default. For multi-turn message data there is an assistant-only loss option, but it depends on the model's chat template marking assistant turns, so check the docs for your model. | Data formats<br>• messages: conversational LM<br>• prompt + completion: completion-only loss by default<br>• Assistant-only loss depends on the template | Two JSON lines side by side with prompt and completion keys highlighted; a loss mask bar under the second shows only the completion shaded. |
| 6 | 2:34 | 35 | The hyperparameters that matter most. Learning rate: for LoRA, something around one to two times ten to the minus four is a common starting point, and full fine-tuning uses much lower rates. Epochs: one to three. Effective batch size, which is batch per device times gradient accumulation times GPUs, often sixteen to sixty-four. Maximum length, enough to cover your real examples. And a scheduler with a short warmup. Start from defaults, change one thing at a time, and compare on validation. | Hyperparameters (orientation)<br>• LR: LoRA ~1e-4 to 2e-4; full FT far lower<br>• Epochs: 1–3<br>• Effective batch: 16–64<br>• Max length · scheduler<br>• One change at a time | Control panel with five labeled dials, each turning to a starting mark; a note reads "verify on your task". |
| 7 | 3:09 | 32 | Now read the curves. Training loss should fall. Validation loss should fall and then flatten. If validation loss starts rising while training loss keeps falling, you are overfitting: the model is memorizing. Use an earlier checkpoint, fewer epochs, a lower learning rate or more data. If both stay high, suspect unclear labels or too low a learning rate. And remember that loss is a proxy. Always run your task evaluation on the checkpoints. | Reading curves<br>• Train ↓, validation ↓ then flat: good<br>• Validation ↑ while train ↓: overfitting<br>• Both high: data or LR problem<br>• Loss is a proxy: run task evals | Line chart with two curves; the validation curve dips then rises, and a vertical marker highlights the best checkpoint. |
| 8 | 3:41 | 37 | The script in the lesson is complete. It loads prompt-completion JSON lines for training and validation, sets up SFTConfig with two epochs, a learning rate of two times ten to the minus four, cosine decay, a maximum length, evaluation and saving every hundred steps. It adds a LoRA configuration on all linear layers, trains, and saves the adapter. A second snippet loads the base model plus adapter and classifies a Roman Urdu message. Argument names evolve, so check the docs for your TRL version. | Hands-on: train_sft.py<br>• SFTConfig: epochs, LR, schedule, eval, save<br>• LoraConfig: r=16, alpha=32, all-linear<br>• Save adapter → load with PeftModel → test | Screen recording of the script in an editor, then a terminal showing training progress with loss values, and a final inference output label. |
| 9 | 4:18 | 31 | A worked example with illustrative numbers. A Karachi marketplace fine-tunes a one point seven billion model on fifty-four hundred examples across fourteen intents in Urdu, Roman Urdu and English. Validation loss rises after the first epoch, and validation accuracy peaks there too. They keep that checkpoint, add six hundred examples for two intents the model confused, and retrain. Test accuracy improves, and responses come back far faster than their previous API calls. | Worked example: Karachi marketplace (illustrative)<br>• 1.7B model, 5,400 examples, 14 intents<br>• Best checkpoint: epoch 1<br>• +600 examples for confused intents<br>• Better accuracy, much lower latency | Confusion matrix with two bright off-diagonal cells that fade after extra data is added; latency comparison bar beside it. |
| 10 | 4:49 | 37 | A simple example. A gym chain wants a tiny model to label member messages as booking, billing, complaint or other. They prepare eight hundred prompt and completion examples, train a LoRA adapter for two epochs on a small instruct model, and watch the curves. Validation loss flattens after the first epoch. They compare checkpoints on the validation set: epoch one scores slightly better. They keep epoch one, run the test set once, and move on. The whole job takes less than an hour on one GPU. | Simple example: gym chain<br>• 800 prompt/completion examples<br>• LoRA, 2 epochs, small model<br>• Best checkpoint: epoch 1<br>• < 1 hour on one GPU | A training progress bar runs; two loss curves draw, with the validation curve flattening; two checkpoint cards compare and epoch 1 gets a star. |
| 11 | 5:26 | 30 | A few habits make SFT experiments far more productive. Start with a tiny run on a hundred examples to confirm the pipeline works end to end, including evaluation, before spending hours of GPU time. Keep a fixed validation set and never change it mid-project. Change one hyperparameter at a time. And save the exact config and data hash with every run, so when something works, you know precisely why. | Productive habits<br>• Tiny smoke-test run first<br>• Fixed validation set<br>• One change at a time<br>• Save config + data hash per run | A small test tube icon labeled smoke test lighting green before a large training job starts; a run log table with config hash columns. |
| 12 | 5:56 | 37 | A frequent question: how do I know whether to fine-tune a smaller model or just use a bigger one with a good prompt? Run both on your golden set. If the small fine-tuned model matches the big prompted one on your task, you have won on cost and speed. If it falls short, check whether more or better data closes the gap. And remember maintenance: the small model is now yours to retrain and monitor, while the big model's prompt is simpler to update. | FAQ: small fine-tune vs big prompted model?<br>• Run both on the golden set<br>• Match → win on cost and speed<br>• Short → better data?<br>• Factor in maintenance | Two model cards compared on a golden-set scoreboard; cost and latency bars favor the small model, and a wrench icon labeled maintenance appears beside it. |
| 13 | 6:33 | 27 | Try this now. Convert twenty of your labeled examples into the prompt and completion format shown in the lesson, with the exact system prompt you will use in production. Load them with the datasets library and print one example after the tokenizer's chat template is applied. Seeing the special tokens around your text is the moment chat templates stop being abstract. | Try this now<br>• Convert 20 examples to prompt/completion<br>• Use the production system prompt<br>• Print one after applying the chat template | A JSON line transforms into a templated string with special tokens highlighted in a different color, animating token by token. |
| 14 | 7:00 | 67 | Watch me do it. I convert two thousand labeled messages into prompt and completion lines with our exact production system prompt. I load them with the datasets library and print one example after the chat template is applied, to see the special tokens. Then I do a smoke run: one hundred examples, twenty steps, to confirm training, saving and evaluation all work. It does. Now the real run: two epochs, learning rate two times ten to the minus four, LoRA on all linear layers, evaluation every hundred steps. I watch the curves: training loss falls steadily; validation loss flattens after about one and a half epochs. I evaluate three saved checkpoints on the validation set with macro F1: the middle one wins. I load it with the base model, test five tricky Roman Urdu messages by hand, and only then run the test set, once. Result recorded with the config and data hash. | Watch me do it<br>1 Convert to prompt/completion<br>2 Inspect the template<br>3 Smoke run<br>4 Full run + curves<br>5 Pick checkpoint by F1<br>6 Test once | Terminal showing the templated example with special tokens colored; a smoke-run progress bar; loss curves drawing live; a checkpoint comparison table with one starred; a final test-set result card with a config hash. |
| 15 | 8:07 | 26 | Recap. SFT continues next-token training on your demonstrations. Mask loss to responses, keep templates and prompts identical to production, and use prompt-completion format for short-answer tasks. Tune one thing at a time, watch for overfitting, and pick checkpoints by task metrics. Your next step: train a LoRA adapter on your dataset, plot the curves, and compare accuracy across checkpoints. | Recap<br>• Responses-only loss, consistent templates<br>• Watch validation, pick by task metrics<br>• Next: train, plot, compare checkpoints | Recap card with a loss-curve icon, a template icon and a checkpoint icon. |

<details><summary>Narration per scene (paste into ElevenLabs)</summary>

Scene 1 — SFT fundamentals

```text
Supervised fine-tuning is the workhorse of model customization. It is also where most first attempts quietly go wrong, not because the idea is hard, but because of three small details. In this lesson you will see what actually happens during SFT, the data formats that make it easy, the hyperparameters that matter, how to read training curves, and a complete working script with TRL and LoRA.
```

Scene 2 — Analogy: learning from a folder of great replies

```text
An analogy for SFT. Imagine teaching someone to write customer replies by giving them a folder of excellent past replies, each paired with the customer's message. They read message, reply, message, reply, thousands of times, and slowly their instincts shift. That is SFT. Loss masking means you only grade them on the replies they write, not on copying out the customer's message. And the chat template is the letterhead: if you train on one letterhead and then hand them a different one at work, they hesitate.
```

Scene 3 — What SFT does

```text
A language model predicts the next token. SFT simply continues that training on your examples. For each example the model sees the prompt and the ideal response, and its weights are nudged so the ideal response becomes a little more likely. Repeat over thousands of examples, for one to a few passes called epochs, and the model's default behavior shifts toward your demonstrations.
```

Scene 4 — Three make-or-break details

```text
Three details make or break it. First, loss masking. You want the model to learn to produce answers, not to reproduce your prompts, so compute the loss on the response tokens only. Second, the chat template. Instruction-tuned models expect conversations wrapped in special tokens, and training and serving must use the same template. Third, consistency. The system prompt, formatting and style in training must match production exactly.
```

Scene 5 — Data formats

```text
TRL, Hugging Face's training library, understands conversational data. The simplest reliable shape for classification and extraction is prompt and completion: the prompt holds the system and user messages, the completion holds the assistant's answer. With that shape, TRL computes loss on the completion only by default. For multi-turn message data there is an assistant-only loss option, but it depends on the model's chat template marking assistant turns, so check the docs for your model.
```

Scene 6 — Hyperparameters (orientation)

```text
The hyperparameters that matter most. Learning rate: for LoRA, something around one to two times ten to the minus four is a common starting point, and full fine-tuning uses much lower rates. Epochs: one to three. Effective batch size, which is batch per device times gradient accumulation times GPUs, often sixteen to sixty-four. Maximum length, enough to cover your real examples. And a scheduler with a short warmup. Start from defaults, change one thing at a time, and compare on validation.
```

Scene 7 — Reading curves

```text
Now read the curves. Training loss should fall. Validation loss should fall and then flatten. If validation loss starts rising while training loss keeps falling, you are overfitting: the model is memorizing. Use an earlier checkpoint, fewer epochs, a lower learning rate or more data. If both stay high, suspect unclear labels or too low a learning rate. And remember that loss is a proxy. Always run your task evaluation on the checkpoints.
```

Scene 8 — Hands-on: train_sft.py

```text
The script in the lesson is complete. It loads prompt-completion JSON lines for training and validation, sets up SFTConfig with two epochs, a learning rate of two times ten to the minus four, cosine decay, a maximum length, evaluation and saving every hundred steps. It adds a LoRA configuration on all linear layers, trains, and saves the adapter. A second snippet loads the base model plus adapter and classifies a Roman Urdu message. Argument names evolve, so check the docs for your TRL version.
```

Scene 9 — Worked example: Karachi marketplace (illustrative)

```text
A worked example with illustrative numbers. A Karachi marketplace fine-tunes a one point seven billion model on fifty-four hundred examples across fourteen intents in Urdu, Roman Urdu and English. Validation loss rises after the first epoch, and validation accuracy peaks there too. They keep that checkpoint, add six hundred examples for two intents the model confused, and retrain. Test accuracy improves, and responses come back far faster than their previous API calls.
```

Scene 10 — Simple example: gym chain

```text
A simple example. A gym chain wants a tiny model to label member messages as booking, billing, complaint or other. They prepare eight hundred prompt and completion examples, train a LoRA adapter for two epochs on a small instruct model, and watch the curves. Validation loss flattens after the first epoch. They compare checkpoints on the validation set: epoch one scores slightly better. They keep epoch one, run the test set once, and move on. The whole job takes less than an hour on one GPU.
```

Scene 11 — Productive habits

```text
A few habits make SFT experiments far more productive. Start with a tiny run on a hundred examples to confirm the pipeline works end to end, including evaluation, before spending hours of GPU time. Keep a fixed validation set and never change it mid-project. Change one hyperparameter at a time. And save the exact config and data hash with every run, so when something works, you know precisely why.
```

Scene 12 — FAQ: small fine-tune vs big prompted model?

```text
A frequent question: how do I know whether to fine-tune a smaller model or just use a bigger one with a good prompt? Run both on your golden set. If the small fine-tuned model matches the big prompted one on your task, you have won on cost and speed. If it falls short, check whether more or better data closes the gap. And remember maintenance: the small model is now yours to retrain and monitor, while the big model's prompt is simpler to update.
```

Scene 13 — Try this now

```text
Try this now. Convert twenty of your labeled examples into the prompt and completion format shown in the lesson, with the exact system prompt you will use in production. Load them with the datasets library and print one example after the tokenizer's chat template is applied. Seeing the special tokens around your text is the moment chat templates stop being abstract.
```

Scene 14 — Watch me do it

```text
Watch me do it. I convert two thousand labeled messages into prompt and completion lines with our exact production system prompt. I load them with the datasets library and print one example after the chat template is applied, to see the special tokens. Then I do a smoke run: one hundred examples, twenty steps, to confirm training, saving and evaluation all work. It does. Now the real run: two epochs, learning rate two times ten to the minus four, LoRA on all linear layers, evaluation every hundred steps. I watch the curves: training loss falls steadily; validation loss flattens after about one and a half epochs. I evaluate three saved checkpoints on the validation set with macro F1: the middle one wins. I load it with the base model, test five tricky Roman Urdu messages by hand, and only then run the test set, once. Result recorded with the config and data hash.
```

Scene 15 — Recap

```text
Recap. SFT continues next-token training on your demonstrations. Mask loss to responses, keep templates and prompts identical to production, and use prompt-completion format for short-answer tasks. Tune one thing at a time, watch for overfitting, and pick checkpoints by task metrics. Your next step: train a LoRA adapter on your dataset, plot the curves, and compare accuracy across checkpoints.
```

</details>

### 7. LoRA, QLoRA and parameter-efficient fine-tuning

Lesson `lora-qlora-and-peft` · module “Supervised fine-tuning and PEFT” · target 9 min · 15 scenes · 1202 words · planned 8:43 (words ≈ 8:43) · status: **to produce**

| Term | Say |
|---|---|
| LoRA | LOR-ah |
| QLoRA | Q LOR-ah |
| PEFT | peft |
| NF4 | N F four |
| DoRA | DOR-ah |
| rsLoRA | R S LOR-ah |
| bitsandbytes | bits and bytes |

| # | Time | Sec | Narration | On screen | Visual |
|---|---|---|---|---|---|
| 1 | 0:00 | 36 | A few years ago, fine-tuning a seven billion parameter model meant a rack of expensive GPUs and a full copy of the model for every task. Today you can do it overnight on one consumer graphics card and store the result in a file smaller than a movie. That shift is thanks to LoRA and QLoRA. In this lesson you will learn how they work, how to choose settings, and how to take an adapter all the way to a served model. | LoRA, QLoRA and PEFT<br>• How they work<br>• Choosing settings<br>• From adapter to served model | A room full of server racks shrinks into a single desktop GPU, and a large model file shrinks into a small adapter file beside the base model. |
| 2 | 0:36 | 37 | Here is the analogy for LoRA. Imagine a huge, expensive textbook. Instead of reprinting it with your changes, you add a slim set of sticky notes that adjust key pages. Anyone with the original book plus your sticky notes gets your version. You can keep different sets of notes for different courses, and swap them in seconds. QLoRA goes one step further: you keep a compressed pocket edition of the textbook on your desk while writing the notes, so it takes far less space. | Analogy: sticky notes on a textbook<br>• Base model = the textbook (unchanged)<br>• LoRA adapter = sticky notes<br>• Swap note sets per task<br>• QLoRA = pocket edition while writing notes | A thick book on a desk; colored sticky-note sets attach to pages, then swap for another color set. A smaller pocket-sized copy replaces the big book while notes are written. |
| 3 | 1:13 | 22 | Full fine-tuning updates every weight, which needs memory for gradients and optimizer states several times the model's size, and produces a full new model per task. Parameter-efficient fine-tuning freezes the base model and trains a small number of new parameters instead. LoRA is the most widely used way to do that. | Why PEFT<br>• Full FT: every weight, huge memory, full copies<br>• PEFT: freeze base, train a little<br>• LoRA: the standard | Side-by-side: a model block fully lit in red labeled full fine-tuning, and a frozen blue block with a small glowing attachment labeled LoRA. |
| 4 | 1:35 | 39 | Here is the idea. For each big weight matrix, LoRA learns a low-rank update made of two thin matrices, A and B, multiplied together and added to the frozen weights, scaled by alpha over r. The rank, r, controls capacity, usually eight to sixty-four. The numbers make it concrete. A four thousand by four thousand matrix has about sixteen point eight million weights. LoRA with rank sixteen trains about a hundred and thirty-one thousand. Under one percent. Across a whole model, adapters are tens to a few hundred megabytes. | How LoRA works<br>• W' = W + (alpha/r) × B × A<br>• Rank r: 8–64 typical<br>• 4096×4096: 16.8M weights vs 131k trained at r=16 | Matrix diagram: a large square W, plus a tall thin B times a short wide A forming a same-sized overlay; parameter counts appear under each. |
| 5 | 2:14 | 32 | Which layers get adapters? Early practice targeted only the attention projections. Current practice commonly targets all linear layers, attention and the feed-forward blocks, and that tends to work better. Research published in twenty twenty-five, for example the "LoRA Without Regret" study, reported that LoRA on all layers can match full fine-tuning on many post-training datasets, with a learning rate around ten times higher than full fine-tuning, while noting limits on very large datasets. | Target modules<br>• Old: attention only<br>• Now: all linear layers<br>• 2025 research: often matches full FT, ~10× higher LR | Transformer block diagram with attention and MLP sub-blocks; adapters light up first only on attention, then on all linear layers. |
| 6 | 2:46 | 33 | QLoRA takes it further. It loads the frozen base model in four-bit precision, using a data type called NF4 with double quantization of the scales, and trains sixteen-bit LoRA adapters on top. The original paper fine-tuned a sixty-five billion parameter model on a single forty-eight gigabyte GPU. In practice, it lets you tune seven to fourteen billion parameter models on one sixteen to twenty-four gigabyte card, with slightly slower training and a small quality trade-off. | QLoRA<br>• Frozen base in 4-bit NF4 + double quantization<br>• 16-bit adapters on top<br>• 7–14B on one 16–24 GB GPU<br>• Slower, small quality trade-off | Memory bar comparison: 16-bit base filling a card completely, then a 4-bit base using a small slice with adapters and activations stacked on top, fitting under the line. |
| 7 | 3:19 | 33 | The QLoRA script in the lesson loads the base with a bits and bytes four-bit configuration, adds a LoRA config on all linear layers, turns on gradient checkpointing to save activation memory, and trains with TRL's SFT trainer. For deployment, you load the base in sixteen-bit, attach the adapter, merge it with merge and unload, save, and then quantize the merged model for your serving engine. And then you re-evaluate, because quantization can shift behavior. | Hands-on<br>• 4-bit load + LoRA all-linear + grad checkpointing<br>• Train with SFTTrainer<br>• Merge into 16-bit base → quantize → re-evaluate | Code editor walkthrough highlighting BitsAndBytesConfig, LoraConfig and merge_and_unload lines, then a pipeline arrow to GGUF and AWQ icons with a checkmark labeled re-evaluate. |
| 8 | 3:52 | 29 | How to choose settings. For a style or format change with a few thousand examples, start with rank eight to sixteen on all linear layers. For classification across many classes and languages, sixteen to thirty-two. For large datasets or harder behaviors, thirty-two to sixty-four, and compare against full fine-tuning if you can. Refinements like DoRA and rank-stabilized LoRA exist, but try them only after a solid baseline. | Starting points<br>• Style/format: r 8–16<br>• Multi-class, multilingual: r 16–32<br>• Large or hard: r 32–64<br>• DoRA, rsLoRA after a baseline | Three-row table appearing with rank values, and two small "later" tags beside the variant names. |
| 9 | 4:21 | 33 | A worked example, with illustrative results. A London agency manages twelve creators with distinct voices. They train one QLoRA adapter per creator on four to nine hundred approved captions each, on one twenty-four gigabyte GPU, overnight. A single base model serves all twelve adapters. In blind ratings, most adapters beat the prompted base model on sounding like the creator. Two creators with fewer than three hundred examples show little gain, so the agency collects more before retraining. | Worked example: London creator agency (illustrative)<br>• 12 creators → 12 adapters<br>• 400–900 captions each, one 24 GB GPU<br>• One base serves all<br>• Too little data → little gain | Twelve small adapter cards with different colors plugging into one base model icon; two cards are dimmer with a "needs data" label. |
| 10 | 4:54 | 33 | A simple example. A marketing agency wants one small model to write product descriptions for three clients with very different tones: a luxury perfume brand, a budget electronics store and a children's toy shop. Instead of three full models, they train three LoRA adapters, each on about five hundred approved descriptions, on one twenty-four gigabyte GPU. At serving time, one base model loads, and the right adapter is picked per client. Three voices, one GPU, small files. | Simple example: 3 client voices<br>• 3 LoRA adapters × ~500 examples<br>• One 24 GB GPU<br>• One base model serves all three | One base model icon in the center with three colored adapter plugs labeled perfume, electronics and toys; a request arrives and the matching plug lights up. |
| 11 | 5:27 | 33 | What about memory for full fine-tuning versus LoRA, in practical terms? Full fine-tuning with a standard optimizer typically needs memory for the weights, the gradients and two optimizer states per parameter, several times the model size. LoRA keeps the base frozen and only needs gradients and optimizer states for the tiny adapter. QLoRA then shrinks the frozen base itself. That is the whole reason a job that once needed a multi-GPU server now fits on one card. | Where the memory goes<br>• Full FT: weights + gradients + optimizer states<br>• LoRA: states only for the adapter<br>• QLoRA: plus a 4-bit frozen base | Three stacked memory bars: a tall full fine-tuning bar with four segments, a shorter LoRA bar, and a short QLoRA bar fitting under a single-GPU line. |
| 12 | 6:00 | 37 | A question I get: does LoRA make the model forget what it already knew? Much less than full fine-tuning, because the base weights stay frozen. But the adapter can still shift behavior in ways that hurt other tasks, especially with a high learning rate or many epochs. That is why your regression suite matters: after training, check general instructions, other languages and safety behavior, not only the target task. If you see forgetting, reduce epochs or learning rate, or mix in some general examples. | FAQ: does LoRA cause forgetting?<br>• Less than full fine-tuning (base frozen)<br>• Still possible: high LR, many epochs<br>• Run regression suite<br>• Fix: fewer epochs, lower LR, mix general data | A frozen base model block with an adapter attached; a regression dashboard checks general, language and safety tiles, one turning amber then green after a fix. |
| 13 | 6:37 | 30 | Try this now. Take a model you might fine-tune and look up two numbers in its config: hidden size and number of layers. Estimate the LoRA parameters for one projection matrix at rank sixteen: sixteen times the two dimensions added together. Then multiply by the number of target matrices per layer and the number of layers. Compare that with the model's total parameters. Seeing the percentage makes LoRA's efficiency real. | Try this now<br>• Find hidden size + layers<br>• r × (d + k) per matrix<br>• × matrices per layer × layers<br>• Compare with total parameters | Calculator-style animation: numbers from a config snippet feed a formula, producing a small parameter count beside a huge total, with a percentage badge. |
| 14 | 7:07 | 65 | Watch me do it. I have a twenty-four gigabyte GPU and want to adapt an eight billion model to a client's brand voice. I load the base in four-bit with the bits and bytes config and check memory: about six gigabytes. I add a LoRA config, rank sixteen, alpha thirty-two, all linear layers, and print the trainable parameters: well under one percent of the model. I train on six hundred approved captions with gradient checkpointing; memory peaks around seventeen gigabytes. Training takes about forty minutes. Then deployment: I load the base in sixteen-bit on a bigger machine, attach the adapter, merge and save. I convert the merged model to GGUF, quantize to Q five K M, and load it in Ollama. Last and most important, I rerun the voice evaluation on the quantized model: the score is within noise of the unquantized adapter. Only now is it ready to serve. | Watch me do it<br>1 4-bit base (~6 GB)<br>2 LoRA r=16, all-linear<br>3 Train (~17 GB peak)<br>4 Merge into 16-bit<br>5 Quantize + load<br>6 Re-evaluate | GPU memory gauge rising in steps as base, adapter and training load; a trainable-parameters printout; training progress; merge and GGUF conversion commands; a before/after evaluation score comparison. |
| 15 | 8:12 | 31 | Recap. LoRA trains small adapters while the base stays frozen, and all linear layers is the usual default. QLoRA puts the base in four-bit so you can train on one consumer GPU. Keep adapters per task, or merge into a sixteen-bit base for simple deployment, then quantize and re-evaluate. Your next step: train a QLoRA adapter for a style task, merge and quantize it, and compare scores before and after quantization. | Recap<br>• Adapters, all-linear, QLoRA<br>• Merge into 16-bit, quantize, re-test<br>• Next: train + merge + compare | Recap card with an adapter plug icon, a 4-bit chip and a checkmark. |

<details><summary>Narration per scene (paste into ElevenLabs)</summary>

Scene 1 — LoRA, QLoRA and PEFT

```text
A few years ago, fine-tuning a seven billion parameter model meant a rack of expensive GPUs and a full copy of the model for every task. Today you can do it overnight on one consumer graphics card and store the result in a file smaller than a movie. That shift is thanks to LoRA and QLoRA. In this lesson you will learn how they work, how to choose settings, and how to take an adapter all the way to a served model.
```

Scene 2 — Analogy: sticky notes on a textbook

```text
Here is the analogy for LoRA. Imagine a huge, expensive textbook. Instead of reprinting it with your changes, you add a slim set of sticky notes that adjust key pages. Anyone with the original book plus your sticky notes gets your version. You can keep different sets of notes for different courses, and swap them in seconds. QLoRA goes one step further: you keep a compressed pocket edition of the textbook on your desk while writing the notes, so it takes far less space.
```

Scene 3 — Why PEFT

```text
Full fine-tuning updates every weight, which needs memory for gradients and optimizer states several times the model's size, and produces a full new model per task. Parameter-efficient fine-tuning freezes the base model and trains a small number of new parameters instead. LoRA is the most widely used way to do that.
```

Scene 4 — How LoRA works

```text
Here is the idea. For each big weight matrix, LoRA learns a low-rank update made of two thin matrices, A and B, multiplied together and added to the frozen weights, scaled by alpha over r. The rank, r, controls capacity, usually eight to sixty-four. The numbers make it concrete. A four thousand by four thousand matrix has about sixteen point eight million weights. LoRA with rank sixteen trains about a hundred and thirty-one thousand. Under one percent. Across a whole model, adapters are tens to a few hundred megabytes.
```

Scene 5 — Target modules

```text
Which layers get adapters? Early practice targeted only the attention projections. Current practice commonly targets all linear layers, attention and the feed-forward blocks, and that tends to work better. Research published in twenty twenty-five, for example the "LoRA Without Regret" study, reported that LoRA on all layers can match full fine-tuning on many post-training datasets, with a learning rate around ten times higher than full fine-tuning, while noting limits on very large datasets.
```

Scene 6 — QLoRA

```text
QLoRA takes it further. It loads the frozen base model in four-bit precision, using a data type called NF4 with double quantization of the scales, and trains sixteen-bit LoRA adapters on top. The original paper fine-tuned a sixty-five billion parameter model on a single forty-eight gigabyte GPU. In practice, it lets you tune seven to fourteen billion parameter models on one sixteen to twenty-four gigabyte card, with slightly slower training and a small quality trade-off.
```

Scene 7 — Hands-on

```text
The QLoRA script in the lesson loads the base with a bits and bytes four-bit configuration, adds a LoRA config on all linear layers, turns on gradient checkpointing to save activation memory, and trains with TRL's SFT trainer. For deployment, you load the base in sixteen-bit, attach the adapter, merge it with merge and unload, save, and then quantize the merged model for your serving engine. And then you re-evaluate, because quantization can shift behavior.
```

Scene 8 — Starting points

```text
How to choose settings. For a style or format change with a few thousand examples, start with rank eight to sixteen on all linear layers. For classification across many classes and languages, sixteen to thirty-two. For large datasets or harder behaviors, thirty-two to sixty-four, and compare against full fine-tuning if you can. Refinements like DoRA and rank-stabilized LoRA exist, but try them only after a solid baseline.
```

Scene 9 — Worked example: London creator agency (illustrative)

```text
A worked example, with illustrative results. A London agency manages twelve creators with distinct voices. They train one QLoRA adapter per creator on four to nine hundred approved captions each, on one twenty-four gigabyte GPU, overnight. A single base model serves all twelve adapters. In blind ratings, most adapters beat the prompted base model on sounding like the creator. Two creators with fewer than three hundred examples show little gain, so the agency collects more before retraining.
```

Scene 10 — Simple example: 3 client voices

```text
A simple example. A marketing agency wants one small model to write product descriptions for three clients with very different tones: a luxury perfume brand, a budget electronics store and a children's toy shop. Instead of three full models, they train three LoRA adapters, each on about five hundred approved descriptions, on one twenty-four gigabyte GPU. At serving time, one base model loads, and the right adapter is picked per client. Three voices, one GPU, small files.
```

Scene 11 — Where the memory goes

```text
What about memory for full fine-tuning versus LoRA, in practical terms? Full fine-tuning with a standard optimizer typically needs memory for the weights, the gradients and two optimizer states per parameter, several times the model size. LoRA keeps the base frozen and only needs gradients and optimizer states for the tiny adapter. QLoRA then shrinks the frozen base itself. That is the whole reason a job that once needed a multi-GPU server now fits on one card.
```

Scene 12 — FAQ: does LoRA cause forgetting?

```text
A question I get: does LoRA make the model forget what it already knew? Much less than full fine-tuning, because the base weights stay frozen. But the adapter can still shift behavior in ways that hurt other tasks, especially with a high learning rate or many epochs. That is why your regression suite matters: after training, check general instructions, other languages and safety behavior, not only the target task. If you see forgetting, reduce epochs or learning rate, or mix in some general examples.
```

Scene 13 — Try this now

```text
Try this now. Take a model you might fine-tune and look up two numbers in its config: hidden size and number of layers. Estimate the LoRA parameters for one projection matrix at rank sixteen: sixteen times the two dimensions added together. Then multiply by the number of target matrices per layer and the number of layers. Compare that with the model's total parameters. Seeing the percentage makes LoRA's efficiency real.
```

Scene 14 — Watch me do it

```text
Watch me do it. I have a twenty-four gigabyte GPU and want to adapt an eight billion model to a client's brand voice. I load the base in four-bit with the bits and bytes config and check memory: about six gigabytes. I add a LoRA config, rank sixteen, alpha thirty-two, all linear layers, and print the trainable parameters: well under one percent of the model. I train on six hundred approved captions with gradient checkpointing; memory peaks around seventeen gigabytes. Training takes about forty minutes. Then deployment: I load the base in sixteen-bit on a bigger machine, attach the adapter, merge and save. I convert the merged model to GGUF, quantize to Q five K M, and load it in Ollama. Last and most important, I rerun the voice evaluation on the quantized model: the score is within noise of the unquantized adapter. Only now is it ready to serve.
```

Scene 15 — Recap

```text
Recap. LoRA trains small adapters while the base stays frozen, and all linear layers is the usual default. QLoRA puts the base in four-bit so you can train on one consumer GPU. Keep adapters per task, or merge into a sixteen-bit base for simple deployment, then quantize and re-evaluate. Your next step: train a QLoRA adapter for a style task, merge and quantize it, and compare scores before and after quantization.
```

</details>

### 8. Open-source tooling: TRL, Unsloth, Axolotl and the training stack

Lesson `open-source-training-tooling` · module “Supervised fine-tuning and PEFT” · target 8 min · 15 scenes · 1119 words · planned 8:07 (words ≈ 8:07) · status: **to produce**

| Term | Say |
|---|---|
| TRL | T R L |
| Unsloth | un-sloth |
| Axolotl | AX-oh-lot-ul |
| LLaMA-Factory | llama factory |
| FSDP | F S D P |
| DeepSpeed | deep speed |
| MLflow | M L flow |
| SageMaker | sage maker |

| # | Time | Sec | Narration | On screen | Visual |
|---|---|---|---|---|---|
| 1 | 0:00 | 27 | Your first fine-tune usually lives in a notebook on one person's machine. It works, everyone celebrates, and three weeks later nobody can reproduce it. In this lesson you will map the open-source training stack, see when to use TRL, Unsloth or Axolotl, where to get GPUs, and how to make training reproducible so the second model is easier than the first. | The training stack<br>• TRL, Unsloth, Axolotl<br>• GPUs and environments<br>• Reproducibility | A single notebook window on a laptop, then a question mark appears three weeks later on the calendar; the scene transitions to a tidy pipeline diagram. |
| 2 | 0:27 | 32 | Think of the training stack like a professional kitchen. Transformers is the pantry with every ingredient. PEFT is a technique, like sous vide, that gets great results with less energy. TRL is the set of recipes: SFT, DPO, GRPO. Unsloth is a faster oven. Axolotl is the kitchen's written recipe cards that any chef can follow exactly. And experiment tracking is the kitchen logbook: what was cooked, when, how, and how it tasted. | Analogy: a professional kitchen<br>• Transformers = pantry<br>• PEFT = technique<br>• TRL = recipes<br>• Unsloth = faster oven<br>• Axolotl = recipe cards · tracking = logbook | A kitchen layout animates in labeled stations: pantry, technique station, recipe binder, oven, card rack and a logbook on the pass. |
| 3 | 0:59 | 31 | Here is the stack, layer by layer. Transformers loads models and templates. Datasets handles your data. PEFT provides LoRA and QLoRA adapters. TRL provides the trainers, SFT, DPO, GRPO, reward modeling and distillation. Unsloth speeds up training and cuts memory with custom kernels. Axolotl and LLaMA-Factory turn runs into YAML configuration. Accelerate, DeepSpeed and FSDP scale to multiple GPUs. And tracking tools like Weights and Biases or MLflow record every run. | The stack<br>• Transformers · Datasets · PEFT<br>• TRL trainers<br>• Unsloth (speed) · Axolotl/LLaMA-Factory (config)<br>• Accelerate/DeepSpeed/FSDP · tracking | Layered stack diagram building from bottom to top with plain-text labels (no logos), each layer lighting up as named. |
| 4 | 1:30 | 33 | You do not need all of it. On a single GPU, TRL plus PEFT gives maximum transparency, and Unsloth gives maximum speed when memory is tight. For teams running many experiments or multi-GPU jobs, a configuration tool like Axolotl, checked into Git, keeps everyone honest. TRL itself follows one pattern across trainers: a config object, a trainer that takes a model, datasets and an optional adapter config. It moves fast, so pin versions and read release notes. | Choosing<br>• One GPU, transparency: TRL + PEFT<br>• One GPU, tight memory: Unsloth<br>• Teams, many runs: Axolotl in Git<br>• Pin versions | Three paths on a decision map, each ending at a tool label, with a small padlock labeled "pin versions" on all three. |
| 5 | 2:03 | 29 | Unsloth in practice. You load a model with its fast language model helper, in four-bit if you like, then add LoRA adapters with get peft model, choosing rank, alpha and target modules, and gradient checkpointing. You hand that model to TRL's SFT trainer as usual. Unsloth also offers helpers to export merged models and GGUF files. Check its model list and installation guide for your GPU. | Unsloth<br>• FastLanguageModel.from_pretrained (4-bit)<br>• get_peft_model: r, alpha, targets<br>• Train with TRL · export helpers | Code editor showing the Unsloth snippet with the two helper calls highlighted, then a speed gauge and a memory gauge beside a training progress bar. |
| 6 | 2:32 | 29 | Axolotl expresses a run as YAML. Base model, four-bit loading, QLoRA adapter, rank and alpha, all linear targets, the dataset path with a chat template type, validation split, sequence length, packing, batch sizes, epochs, learning rate, scheduler and output directory. One command, axolotl train, runs it. The magic is not the command, it is that the whole experiment is a reviewable file in a pull request. | Axolotl<br>• Whole run as YAML<br>• axolotl train config.yml<br>• Reviewable in pull requests | YAML file scrolling in an editor, followed by a pull-request view with a reviewer comment on the learning_rate line. |
| 7 | 3:01 | 30 | Where do the GPUs come from? Your own workstation for frequent small jobs. Hourly cloud GPU instances, in a region that fits your data rules. Notebook platforms for experiments. Or managed training on platforms like Vertex AI, SageMaker or Azure Machine Learning when you want scheduling and governance. One firm rule: regulated customer data never goes into a free public notebook. Use approved infrastructure with encryption and access control. | GPU sources<br>• Own workstation<br>• Cloud GPUs in the right region<br>• Notebooks for experiments<br>• Managed training platforms<br>• Regulated data: approved infra only | Four tiles with simple icons: desktop tower, cloud with region pin, notebook, and a managed-console window; a red no-entry sign on a data file heading to a public notebook. |
| 8 | 3:31 | 30 | Now reproducibility, the habit that pays for itself. Pin library versions together, because Transformers, TRL, PEFT and bits and bytes must match. Record the base model's exact revision. Fix random seeds. Hash your training files so you know which data made which model. Track every run, with curves, metrics, hardware, duration and cost. And store each adapter with a model card describing base model, data, metrics, intended use and limitations. | Reproducibility checklist<br>• Pinned versions + base revision<br>• Seeds + dataset hash<br>• Tracked runs<br>• Model card per adapter | Checklist with five items ticking; a run-tracking dashboard shows two runs with matching curves. |
| 9 | 4:01 | 33 | A worked example. A Riyadh fintech's first fine-tune was a notebook on one laptop. Good results, but no one could reproduce them after a library update. For the second model, they moved to Axolotl YAML in Git, pinned versions in a container image, trained on cloud GPUs in an in-kingdom region, logged runs to MLflow, and added an evaluation gate that blocks promotion if accuracy drops. The second model was larger, yet took less engineering time. | Worked example: Riyadh fintech<br>• Notebook → YAML in Git<br>• Pinned container<br>• In-kingdom cloud GPUs<br>• MLflow + evaluation gate | Before-and-after diagram: a lone laptop on the left, a pipeline of Git, container, cloud GPU, tracking and eval gate on the right. |
| 10 | 4:34 | 30 | A simple example. A two-person startup has one twelve gigabyte GPU. They try TRL and PEFT with QLoRA on a seven billion model and run out of memory at their sequence length. They switch the same data to Unsloth's loader with four-bit and gradient checkpointing, and the job fits and runs faster. Same data, same method, different tool. Knowing the stack lets you pick the tool that fits your constraints. | Simple example: 12 GB GPU<br>• TRL + PEFT QLoRA: out of memory<br>• Unsloth 4-bit + checkpointing: fits, faster<br>• Same data and method | Memory bar for a 12 GB GPU: first attempt overflows red, second attempt with a different tool label fits under the line in green. |
| 11 | 5:04 | 34 | Experiment tracking deserves its own moment. Every run should record its config, library versions, data hash, hardware, duration, cost, training curves and evaluation metrics, in one place the whole team can see. Tools like Weights and Biases or MLflow do this with a few lines of setup. It turns the question which run was the good one from an argument into a lookup, and it is the first thing an auditor or a new team member will ask for. | Track every run<br>• Config, versions, data hash<br>• Hardware, duration, cost<br>• Curves and eval metrics<br>• One shared place | Run-tracking dashboard with a table of runs, sortable metric columns and one row starred as the chosen model. |
| 12 | 5:38 | 35 | A common question: should we build our own training pipeline or use a managed service? If you fine-tune rarely and want minimal operations, managed training on a cloud platform or an inference provider that supports adapters can be simpler. If you fine-tune often, need specific methods, or must keep data in particular environments, owning a small reproducible pipeline with open-source tools pays off. Many teams do both: prototype on a managed service, then move steady workloads to their own pipeline. | FAQ: own pipeline or managed?<br>• Rare jobs, minimal ops → managed<br>• Frequent jobs, specific methods, data constraints → own pipeline<br>• Often both | Two paths on a decision map: a managed cloud console and a self-hosted pipeline, joined by an arrow labeled "prototype, then move". |
| 13 | 6:13 | 20 | Try this now. Create a requirements file that pins exact versions of transformers, TRL, PEFT, datasets and accelerate, and commit it next to your training script. Then write the base model's revision hash into your config. Those two small steps make your next run reproducible. | Try this now<br>• Pin transformers, trl, peft, datasets, accelerate<br>• Commit requirements + script<br>• Record base model revision | Editor view of a requirements file with pinned versions typing in, then a git commit animation with a hash label. |
| 14 | 6:33 | 66 | Watch me do it. I turn a working notebook into a reproducible project. First I create a requirements file pinning transformers, TRL, PEFT, datasets, accelerate and bits and bytes to the exact versions that worked. Then I write the training settings as an Axolotl YAML: base model with its revision hash, QLoRA, rank, dataset path with the chat template type, sequence length, batch sizes, epochs and learning rate. I compute the SHA of the training file and write it into the config comments. I commit everything to Git. Next I run axolotl train on a rented cloud GPU in our approved region, with runs logged to MLflow. The run finishes; I note the metrics. Then a colleague clones the repository on a different machine and runs the same command. Her validation macro F1 matches mine to the second decimal place. That is the moment the model stops belonging to one person's laptop. | Watch me do it<br>1 Pin versions<br>2 Axolotl YAML with revision hash<br>3 Hash the data<br>4 Commit + run in approved region<br>5 Colleague reproduces the run | Editor showing requirements.txt and a YAML config; a terminal computes a SHA; a git commit animation; an MLflow-style runs table where two runs from different users show matching metrics. |
| 15 | 7:39 | 28 | Recap. Use TRL and PEFT for transparency, Unsloth for speed on one GPU, and Axolotl or LLaMA-Factory for reproducible configuration. Get GPUs from environments that match your data rules. Pin, hash, track and document every run. Your next step: turn your SFT script into a pinned project or an Axolotl YAML in Git, run it twice, and confirm the metrics match within normal variance. | Recap<br>• Right tool for the job<br>• Approved environments<br>• Next: reproduce a run twice | Recap card with two matching loss curves overlaid. |

<details><summary>Narration per scene (paste into ElevenLabs)</summary>

Scene 1 — The training stack

```text
Your first fine-tune usually lives in a notebook on one person's machine. It works, everyone celebrates, and three weeks later nobody can reproduce it. In this lesson you will map the open-source training stack, see when to use TRL, Unsloth or Axolotl, where to get GPUs, and how to make training reproducible so the second model is easier than the first.
```

Scene 2 — Analogy: a professional kitchen

```text
Think of the training stack like a professional kitchen. Transformers is the pantry with every ingredient. PEFT is a technique, like sous vide, that gets great results with less energy. TRL is the set of recipes: SFT, DPO, GRPO. Unsloth is a faster oven. Axolotl is the kitchen's written recipe cards that any chef can follow exactly. And experiment tracking is the kitchen logbook: what was cooked, when, how, and how it tasted.
```

Scene 3 — The stack

```text
Here is the stack, layer by layer. Transformers loads models and templates. Datasets handles your data. PEFT provides LoRA and QLoRA adapters. TRL provides the trainers, SFT, DPO, GRPO, reward modeling and distillation. Unsloth speeds up training and cuts memory with custom kernels. Axolotl and LLaMA-Factory turn runs into YAML configuration. Accelerate, DeepSpeed and FSDP scale to multiple GPUs. And tracking tools like Weights and Biases or MLflow record every run.
```

Scene 4 — Choosing

```text
You do not need all of it. On a single GPU, TRL plus PEFT gives maximum transparency, and Unsloth gives maximum speed when memory is tight. For teams running many experiments or multi-GPU jobs, a configuration tool like Axolotl, checked into Git, keeps everyone honest. TRL itself follows one pattern across trainers: a config object, a trainer that takes a model, datasets and an optional adapter config. It moves fast, so pin versions and read release notes.
```

Scene 5 — Unsloth

```text
Unsloth in practice. You load a model with its fast language model helper, in four-bit if you like, then add LoRA adapters with get peft model, choosing rank, alpha and target modules, and gradient checkpointing. You hand that model to TRL's SFT trainer as usual. Unsloth also offers helpers to export merged models and GGUF files. Check its model list and installation guide for your GPU.
```

Scene 6 — Axolotl

```text
Axolotl expresses a run as YAML. Base model, four-bit loading, QLoRA adapter, rank and alpha, all linear targets, the dataset path with a chat template type, validation split, sequence length, packing, batch sizes, epochs, learning rate, scheduler and output directory. One command, axolotl train, runs it. The magic is not the command, it is that the whole experiment is a reviewable file in a pull request.
```

Scene 7 — GPU sources

```text
Where do the GPUs come from? Your own workstation for frequent small jobs. Hourly cloud GPU instances, in a region that fits your data rules. Notebook platforms for experiments. Or managed training on platforms like Vertex AI, SageMaker or Azure Machine Learning when you want scheduling and governance. One firm rule: regulated customer data never goes into a free public notebook. Use approved infrastructure with encryption and access control.
```

Scene 8 — Reproducibility checklist

```text
Now reproducibility, the habit that pays for itself. Pin library versions together, because Transformers, TRL, PEFT and bits and bytes must match. Record the base model's exact revision. Fix random seeds. Hash your training files so you know which data made which model. Track every run, with curves, metrics, hardware, duration and cost. And store each adapter with a model card describing base model, data, metrics, intended use and limitations.
```

Scene 9 — Worked example: Riyadh fintech

```text
A worked example. A Riyadh fintech's first fine-tune was a notebook on one laptop. Good results, but no one could reproduce them after a library update. For the second model, they moved to Axolotl YAML in Git, pinned versions in a container image, trained on cloud GPUs in an in-kingdom region, logged runs to MLflow, and added an evaluation gate that blocks promotion if accuracy drops. The second model was larger, yet took less engineering time.
```

Scene 10 — Simple example: 12 GB GPU

```text
A simple example. A two-person startup has one twelve gigabyte GPU. They try TRL and PEFT with QLoRA on a seven billion model and run out of memory at their sequence length. They switch the same data to Unsloth's loader with four-bit and gradient checkpointing, and the job fits and runs faster. Same data, same method, different tool. Knowing the stack lets you pick the tool that fits your constraints.
```

Scene 11 — Track every run

```text
Experiment tracking deserves its own moment. Every run should record its config, library versions, data hash, hardware, duration, cost, training curves and evaluation metrics, in one place the whole team can see. Tools like Weights and Biases or MLflow do this with a few lines of setup. It turns the question which run was the good one from an argument into a lookup, and it is the first thing an auditor or a new team member will ask for.
```

Scene 12 — FAQ: own pipeline or managed?

```text
A common question: should we build our own training pipeline or use a managed service? If you fine-tune rarely and want minimal operations, managed training on a cloud platform or an inference provider that supports adapters can be simpler. If you fine-tune often, need specific methods, or must keep data in particular environments, owning a small reproducible pipeline with open-source tools pays off. Many teams do both: prototype on a managed service, then move steady workloads to their own pipeline.
```

Scene 13 — Try this now

```text
Try this now. Create a requirements file that pins exact versions of transformers, TRL, PEFT, datasets and accelerate, and commit it next to your training script. Then write the base model's revision hash into your config. Those two small steps make your next run reproducible.
```

Scene 14 — Watch me do it

```text
Watch me do it. I turn a working notebook into a reproducible project. First I create a requirements file pinning transformers, TRL, PEFT, datasets, accelerate and bits and bytes to the exact versions that worked. Then I write the training settings as an Axolotl YAML: base model with its revision hash, QLoRA, rank, dataset path with the chat template type, sequence length, batch sizes, epochs and learning rate. I compute the SHA of the training file and write it into the config comments. I commit everything to Git. Next I run axolotl train on a rented cloud GPU in our approved region, with runs logged to MLflow. The run finishes; I note the metrics. Then a colleague clones the repository on a different machine and runs the same command. Her validation macro F1 matches mine to the second decimal place. That is the moment the model stops belonging to one person's laptop.
```

Scene 15 — Recap

```text
Recap. Use TRL and PEFT for transparency, Unsloth for speed on one GPU, and Axolotl or LLaMA-Factory for reproducible configuration. Get GPUs from environments that match your data rules. Pin, hash, track and document every run. Your next step: turn your SFT script into a pinned project or an Axolotl YAML in Git, run it twice, and confirm the metrics match within normal variance.
```

</details>

### 9. Preference optimization: RLHF and DPO for practitioners

Lesson `rlhf-and-dpo-explained` · module “Preference and reinforcement fine-tuning” · target 9 min · 15 scenes · 1218 words · planned 8:50 (words ≈ 8:50) · status: **to produce**

| Term | Say |
|---|---|
| RLHF | R L H F |
| DPO | D P O |
| PPO | P P O |
| KL | K L |
| IPO | I P O |
| KTO | K T O |
| ORPO | OR-poh |
| SimPO | SIM-poh |
| RLAIF | R L A I F |
| InstructGPT | instruct G P T |

| # | Time | Sec | Narration | On screen | Visual |
|---|---|---|---|---|---|
| 1 | 0:00 | 30 | Try this. Write the perfect reply to an angry customer. Hard, right? Now I show you two replies and ask which is better. Easy. That gap, between writing and choosing, is why preference optimization exists. In this lesson you will understand RLHF at a practitioner level, see why DPO became the default, meet the variants, learn where preference data comes from, and run a DPO job with TRL. | Preference optimization<br>• Choosing is easier than writing<br>• RLHF and DPO<br>• Data sources and a working job | Two reply cards side by side with a cursor hovering, then a green tick landing on one; a blank page with a blinking cursor fades in the background. |
| 2 | 0:30 | 37 | Here is the analogy. Supervised fine-tuning is like a cooking class where the teacher shows you the perfect dish. Preference optimization is like a tasting panel: they taste two versions and say this one is better. You never see a perfect recipe, but after hundreds of tastings, your cooking drifts toward what the panel prefers. Beta is how far you are allowed to drift from your original style in one go. Too far, too fast, and you may forget how to cook the basics. | Analogy: tasting panel<br>• SFT = cooking class with a perfect dish<br>• DPO = panel picks the better of two<br>• Beta = how far you may drift | Two plates on a table with a panel of three abstract judges raising a card for one; a slider labeled beta limits how far a style meter can move. |
| 3 | 1:07 | 35 | Classic RLHF has three stages. First, supervised fine-tuning. Second, train a reward model on human comparisons, so it predicts which answer people prefer. Third, use reinforcement learning, often an algorithm called PPO, to push the model toward higher reward, with a penalty that keeps it close to the original. It works, and frontier labs still rely heavily on reinforcement learning. But it is complex: two models, unstable training, and reward hacking, where the model exploits flaws in the reward model. | Classic RLHF<br>1 SFT<br>2 Reward model from comparisons<br>3 RL (e.g. PPO) + KL penalty<br>• Powerful, complex, reward hacking risk | Three-stage pipeline diagram with arrows; a small mischievous arrow at stage three sneaks around a gauge labeled reward. |
| 4 | 1:42 | 37 | DPO, direct preference optimization, published in twenty twenty-three, showed you can skip the reward model and the reinforcement loop. You keep a frozen reference model, usually your SFT model. For each pair, training makes the chosen response more likely relative to the reference, and the rejected response less likely. A parameter called beta controls how far the model may drift. Higher beta stays closer, lower beta allows bigger changes and bigger risks. It trains much like SFT, which is why practitioners adopted it so quickly. | DPO<br>• No reward model, no RL loop<br>• Frozen reference (SFT model)<br>• Push chosen up, rejected down<br>• beta: how far to drift | Two probability bars for chosen and rejected answers moving apart relative to a dashed reference line; a beta dial controls the maximum stretch. |
| 5 | 2:19 | 32 | You will hear other names. IPO regularizes DPO when it overfits. KTO learns from unpaired thumbs up and thumbs down, when you have ratings but not pairs. ORPO folds SFT and preferences into one stage without a reference model. SimPO is reference-free and normalizes for length. And online or iterative DPO generates fresh pairs from the current model and repeats. Start with DPO, and reach for a variant when you have a specific reason. | Variants<br>• IPO: less overfitting<br>• KTO: unpaired ratings<br>• ORPO: one stage, no reference<br>• SimPO: length-normalized<br>• Online DPO: iterate | Family tree with DPO at the root and five labeled branches, each with a one-word reason tag. |
| 6 | 2:51 | 36 | Where does preference data come from? The richest source is often free: agent edits. The model's draft is the rejected answer, the agent's edited version is the chosen one. You can also run A/B choices, collect thumbs up and down, or use a strong judge model against a rubric, which is cheaper but must be calibrated against humans. Two quality rules. Pairs must differ on what you care about, and chosen answers must not simply be longer, or your model learns to ramble. | Preference data sources<br>• Agent edits (draft vs edited)<br>• A/B choices<br>• Thumbs up/down<br>• AI judge (calibrate!)<br>• Watch length bias | An agent editing a draft on screen: the original turns into a "rejected" card and the edit into a "chosen" card, then both drop into a dataset bin. |
| 7 | 3:27 | 33 | The hands-on starts from your merged SFT model. Your data file has a prompt, a chosen reply and a rejected reply per line. The DPO config sets beta to point one, a learning rate lower than SFT, one epoch, a modest batch with gradient accumulation, and evaluation steps. With a LoRA config, TRL can use the base weights with the adapter switched off as the reference model, which saves memory. Train, save, and then evaluate properly. | Hands-on: train_dpo.py<br>• Start from the SFT model<br>• prompt · chosen · rejected<br>• beta 0.1, low LR, 1 epoch<br>• LoRA: implicit reference | Editor showing a preference JSON line, then the DPOConfig block with beta and learning_rate highlighted, then a terminal with training logs. |
| 8 | 4:00 | 31 | How do you know it worked? The logs show reward accuracy, how often chosen beats rejected, and margins. Both should rise on validation. But the real test is a blind win rate. Take held-out prompts, generate answers from the SFT model and the DPO model, shuffle them, and have reviewers pick the better one without knowing which is which. Also re-run your accuracy and safety regression tests, and watch response length. | Measuring success<br>• Reward accuracy and margins (logs)<br>• Blind win rate on held-out prompts<br>• Accuracy + safety regressions<br>• Length drift | Reviewer interface mock-up with two anonymized answers A and B, a pick button, and a tally chart building up wins for one side. |
| 9 | 4:31 | 33 | A worked example, with illustrative results. A UAE bank's SFT model was accurate but curt. In four weeks, agent edits produced thirty-two hundred pairs in English and Arabic. The team removed pairs where the only change was length, balanced topics, and ran DPO with LoRA. In a blind review of two hundred held-out conversations, reviewers clearly preferred the DPO model, with no drop on the factual checklist. Length rose slightly, so they kept monitoring it. | Worked example: UAE bank (illustrative)<br>• 3,200 pairs from agent edits<br>• Length-only edits removed<br>• DPO + LoRA<br>• Clear blind preference, accuracy stable | Bilingual chat bubbles transforming from curt to warm; a win-rate bar chart and an unchanged accuracy bar beside it. |
| 10 | 5:04 | 34 | A simple example. A hotel's booking assistant gives correct but stiff answers. Over two weeks, the front-desk team is shown two reply drafts for real guest questions and taps the better one. They collect five hundred pairs. After removing pairs where the only difference was length, they run a small DPO job on the SFT model. In a blind test on fifty new questions, staff prefer the new replies most of the time, and booking accuracy stays the same. | Simple example: hotel assistant (illustrative)<br>• 500 tap-the-better pairs<br>• Remove length-only pairs<br>• Small DPO job<br>• Preferred in blind test, accuracy stable | Tablet mock-up showing two reply cards with a tap on one; a counter climbs to 500, then a before-and-after win-rate bar appears. |
| 11 | 5:38 | 32 | A practical workflow that works well. Ship the SFT model with a light review loop, where agents edit drafts before sending. Log the original draft and the edited version automatically. Every few weeks, filter those pairs, remove length-only edits, balance topics, and run a DPO update. Evaluate with a blind win rate and your regression suite, and release through champion and challenger. Your product improves continuously from normal work, without a separate labeling project. | Continuous preference loop<br>• Agents edit drafts<br>• Log draft vs edit automatically<br>• Periodic DPO update<br>• Blind win rate → release | Circular loop diagram: draft, agent edit, logged pair, DPO update, evaluation, release, back to draft. |
| 12 | 6:10 | 37 | A question people ask: how many preference pairs do we need? It depends on how different your target behavior is from the current model. For a focused tone adjustment, a few hundred high-quality pairs can move the needle. For broader changes, thousands. As always, quality beats quantity: pairs where the chosen answer is clearly better on the dimension you care about teach far more than pairs where the difference is subtle or just length. Start small, measure the blind win rate, and scale if it helps. | FAQ: how many pairs?<br>• Focused tone: a few hundred can help<br>• Broad changes: thousands<br>• Clear, relevant differences beat volume | A small stack of clearly contrasting pairs next to a larger stack of subtle pairs; a win-rate meter rises more for the small clear stack. |
| 13 | 6:47 | 22 | Try this now. Take ten outputs from your current model and, for each, write a better version yourself or ask a colleague to. You now have ten preference pairs. Look at them together: is the better version longer every time? If so, you have found length bias before it reaches training. | Try this now<br>• 10 outputs → write better versions<br>• 10 preference pairs<br>• Check: is "better" always longer? | Ten pairs of cards appear in a grid with a length bar under each card; a warning icon appears if the chosen bars are always longer. |
| 14 | 7:09 | 70 | Watch me do it. Our logs hold four weeks of agent edits: eighteen hundred pairs of original draft and edited reply. I load them and compute the length difference for each pair. Four hundred pairs differ only by added sentences with the same meaning; I drop them to avoid teaching length. I balance topics so refunds do not dominate, and split off two hundred pairs for validation. I run the DPO script on our merged SFT model with beta zero point one and one epoch. In the logs, reward accuracy on validation climbs from about fifty percent to the high seventies, and margins widen. Now the real test: I generate replies from the SFT and DPO models for sixty held-out prompts, shuffle them, and ask three agents to pick the better one blind. The DPO model wins most comparisons. I rerun the accuracy checklist and the safety suite: no regressions. Average length rose a little; I set an alert on it. | Watch me do it<br>1 1,800 edit pairs<br>2 Drop 400 length-only pairs<br>3 Balance + validation split<br>4 DPO run: reward accuracy up<br>5 Blind test + regressions | Data table with a length-difference column and 400 rows fading out; a topic balance chart; training logs with reward accuracy climbing; a blind review interface; a regression dashboard all green with a small length alert. |
| 15 | 8:19 | 31 | Recap. Preference optimization learns from comparisons. RLHF uses a reward model and reinforcement learning, while DPO optimizes preferences directly with a reference model and a beta setting. Agent edits are gold, but watch length bias. And judge success by blind win rate plus regression tests. Your next step: collect two hundred pairs from your workflow, filter them, run a small DPO job, and measure the win rate on fifty held-out prompts. | Recap<br>• Compare, don't write<br>• DPO as the default<br>• Next: 200 pairs, 50-prompt blind test | Recap card with two reply cards and a trophy icon. |

<details><summary>Narration per scene (paste into ElevenLabs)</summary>

Scene 1 — Preference optimization

```text
Try this. Write the perfect reply to an angry customer. Hard, right? Now I show you two replies and ask which is better. Easy. That gap, between writing and choosing, is why preference optimization exists. In this lesson you will understand RLHF at a practitioner level, see why DPO became the default, meet the variants, learn where preference data comes from, and run a DPO job with TRL.
```

Scene 2 — Analogy: tasting panel

```text
Here is the analogy. Supervised fine-tuning is like a cooking class where the teacher shows you the perfect dish. Preference optimization is like a tasting panel: they taste two versions and say this one is better. You never see a perfect recipe, but after hundreds of tastings, your cooking drifts toward what the panel prefers. Beta is how far you are allowed to drift from your original style in one go. Too far, too fast, and you may forget how to cook the basics.
```

Scene 3 — Classic RLHF

```text
Classic RLHF has three stages. First, supervised fine-tuning. Second, train a reward model on human comparisons, so it predicts which answer people prefer. Third, use reinforcement learning, often an algorithm called PPO, to push the model toward higher reward, with a penalty that keeps it close to the original. It works, and frontier labs still rely heavily on reinforcement learning. But it is complex: two models, unstable training, and reward hacking, where the model exploits flaws in the reward model.
```

Scene 4 — DPO

```text
DPO, direct preference optimization, published in twenty twenty-three, showed you can skip the reward model and the reinforcement loop. You keep a frozen reference model, usually your SFT model. For each pair, training makes the chosen response more likely relative to the reference, and the rejected response less likely. A parameter called beta controls how far the model may drift. Higher beta stays closer, lower beta allows bigger changes and bigger risks. It trains much like SFT, which is why practitioners adopted it so quickly.
```

Scene 5 — Variants

```text
You will hear other names. IPO regularizes DPO when it overfits. KTO learns from unpaired thumbs up and thumbs down, when you have ratings but not pairs. ORPO folds SFT and preferences into one stage without a reference model. SimPO is reference-free and normalizes for length. And online or iterative DPO generates fresh pairs from the current model and repeats. Start with DPO, and reach for a variant when you have a specific reason.
```

Scene 6 — Preference data sources

```text
Where does preference data come from? The richest source is often free: agent edits. The model's draft is the rejected answer, the agent's edited version is the chosen one. You can also run A/B choices, collect thumbs up and down, or use a strong judge model against a rubric, which is cheaper but must be calibrated against humans. Two quality rules. Pairs must differ on what you care about, and chosen answers must not simply be longer, or your model learns to ramble.
```

Scene 7 — Hands-on: train_dpo.py

```text
The hands-on starts from your merged SFT model. Your data file has a prompt, a chosen reply and a rejected reply per line. The DPO config sets beta to point one, a learning rate lower than SFT, one epoch, a modest batch with gradient accumulation, and evaluation steps. With a LoRA config, TRL can use the base weights with the adapter switched off as the reference model, which saves memory. Train, save, and then evaluate properly.
```

Scene 8 — Measuring success

```text
How do you know it worked? The logs show reward accuracy, how often chosen beats rejected, and margins. Both should rise on validation. But the real test is a blind win rate. Take held-out prompts, generate answers from the SFT model and the DPO model, shuffle them, and have reviewers pick the better one without knowing which is which. Also re-run your accuracy and safety regression tests, and watch response length.
```

Scene 9 — Worked example: UAE bank (illustrative)

```text
A worked example, with illustrative results. A UAE bank's SFT model was accurate but curt. In four weeks, agent edits produced thirty-two hundred pairs in English and Arabic. The team removed pairs where the only change was length, balanced topics, and ran DPO with LoRA. In a blind review of two hundred held-out conversations, reviewers clearly preferred the DPO model, with no drop on the factual checklist. Length rose slightly, so they kept monitoring it.
```

Scene 10 — Simple example: hotel assistant (illustrative)

```text
A simple example. A hotel's booking assistant gives correct but stiff answers. Over two weeks, the front-desk team is shown two reply drafts for real guest questions and taps the better one. They collect five hundred pairs. After removing pairs where the only difference was length, they run a small DPO job on the SFT model. In a blind test on fifty new questions, staff prefer the new replies most of the time, and booking accuracy stays the same.
```

Scene 11 — Continuous preference loop

```text
A practical workflow that works well. Ship the SFT model with a light review loop, where agents edit drafts before sending. Log the original draft and the edited version automatically. Every few weeks, filter those pairs, remove length-only edits, balance topics, and run a DPO update. Evaluate with a blind win rate and your regression suite, and release through champion and challenger. Your product improves continuously from normal work, without a separate labeling project.
```

Scene 12 — FAQ: how many pairs?

```text
A question people ask: how many preference pairs do we need? It depends on how different your target behavior is from the current model. For a focused tone adjustment, a few hundred high-quality pairs can move the needle. For broader changes, thousands. As always, quality beats quantity: pairs where the chosen answer is clearly better on the dimension you care about teach far more than pairs where the difference is subtle or just length. Start small, measure the blind win rate, and scale if it helps.
```

Scene 13 — Try this now

```text
Try this now. Take ten outputs from your current model and, for each, write a better version yourself or ask a colleague to. You now have ten preference pairs. Look at them together: is the better version longer every time? If so, you have found length bias before it reaches training.
```

Scene 14 — Watch me do it

```text
Watch me do it. Our logs hold four weeks of agent edits: eighteen hundred pairs of original draft and edited reply. I load them and compute the length difference for each pair. Four hundred pairs differ only by added sentences with the same meaning; I drop them to avoid teaching length. I balance topics so refunds do not dominate, and split off two hundred pairs for validation. I run the DPO script on our merged SFT model with beta zero point one and one epoch. In the logs, reward accuracy on validation climbs from about fifty percent to the high seventies, and margins widen. Now the real test: I generate replies from the SFT and DPO models for sixty held-out prompts, shuffle them, and ask three agents to pick the better one blind. The DPO model wins most comparisons. I rerun the accuracy checklist and the safety suite: no regressions. Average length rose a little; I set an alert on it.
```

Scene 15 — Recap

```text
Recap. Preference optimization learns from comparisons. RLHF uses a reward model and reinforcement learning, while DPO optimizes preferences directly with a reference model and a beta setting. Agent edits are gold, but watch length bias. And judge success by blind win rate plus regression tests. Your next step: collect two hundred pairs from your workflow, filter them, run a small DPO job, and measure the win rate on fifty held-out prompts.
```

</details>

### 10. Reinforcement fine-tuning with graders: RFT and GRPO

Lesson `rft-and-grpo` · module “Preference and reinforcement fine-tuning” · target 8 min · 14 scenes · 1169 words · planned 8:30 (words ≈ 8:28) · status: **to produce**

| Term | Say |
|---|---|
| RFT | R F T |
| GRPO | G R P O |
| PPO | P P O |
| DeepSeekMath | deep seek math |
| Nova | NOH-vah |
| Lambda | LAM-dah |
| COD | C O D |

| # | Time | Sec | Narration | On screen | Visual |
|---|---|---|---|---|---|
| 1 | 0:00 | 37 | Some questions have a right answer you can check. Is the fee correct? Does the code pass its tests? Did the model extract the right weight from the booking? When you can check, you can train differently. Instead of writing ideal answers or comparing pairs, you write a grader, and let the model learn to score higher. That is reinforcement fine-tuning. In this lesson you will learn GRPO, how to design graders, how to run it with TRL, and how to catch reward hacking. | Reinforcement fine-tuning<br>• Checkable answers<br>• GRPO and graders<br>• Catching reward hacking | A stream of answers passing a checkpoint gate with a green or red light; green answers glow brighter over time. |
| 2 | 0:37 | 37 | Here is an analogy for GRPO. Imagine a class of students who each attempt the same maths question eight times. A strict marker scores every attempt. The teacher does not compare a student with the whole school, only with their own eight attempts: which were better than their own average? The student learns to do more of what worked. The marker is the grader, and if the marker only checks whether the right number appears anywhere, clever students will write every number they can think of. | Analogy: eight attempts, one marker<br>• Group of attempts per question<br>• Compare with own average<br>• Marker = grader<br>• Lenient marker → gaming | A worksheet with eight attempt rows; a marker icon stamps scores; an average line appears and rows above it glow. A sneaky attempt listing many numbers gets a red cross after the marker is tightened. |
| 3 | 1:14 | 38 | Here is GRPO, group relative policy optimization, introduced in DeepSeek's maths research and used in training DeepSeek R1. For each prompt, the model samples a group of answers, say eight. Your graders score each one. Each answer is compared with its own group's average. Better than average gets reinforced, worse gets discouraged. Then the model updates, optionally with a penalty to stay near a reference. Because the comparison is within the group, you do not need a separate critic model, which makes it simpler and lighter than PPO. | GRPO in four steps<br>1 Sample a group (e.g. 8)<br>2 Score with graders<br>3 Compare to group average<br>4 Update (optional KL)<br>• No critic model | Eight answer cards fanning out from one prompt, each receiving a score; an average line appears and cards above it glow green, below it dim. |
| 4 | 1:52 | 35 | The grader is the whole game. A good grader is correct: it rewards what you actually want, so test it on known good and bad outputs first. It is hard to game: if you reward containing the right number, the model may list every number it can think of, so parse strictly. It is dense enough to learn from, so partial credit per field often beats all or nothing. And it is composable: correctness plus format plus length, with weights. | Good graders are<br>• Correct (tested first)<br>• Hard to game (strict parsing)<br>• Dense (partial credit)<br>• Composable (weighted rewards) | Four quadrant cards; the "hard to game" card shows a sneaky answer stuffed with numbers being rejected by a strict parser. |
| 5 | 2:27 | 39 | Examples. For fee quotes: the parsed amount must equal the fee schedule's calculation, with exactly one amount and a currency. For extraction: per-field match against gold, valid JSON and no extra fields. For SQL: the query runs on a sandbox database and returns the expected rows, and only read-only statements are allowed. For policy answers: it cites the right clause, stays under a length limit and makes no forbidden promises. A model can act as a judge for fuzzier tasks, but it is easier to game, so prefer code. | Grader examples<br>• Fees: exact amount + one amount<br>• Extraction: per-field + valid JSON<br>• SQL: sandbox result + read-only<br>• Policy: correct clause + limits | Table appearing row by row with task, correctness grader and guard-rail grader columns. |
| 6 | 3:06 | 36 | The TRL script has two reward functions. The first parses amounts with a strict pattern and gives one point only if exactly one amount is quoted and it matches the expected fee from the dataset. The second gives a small bonus for quoting one amount in a short answer. Extra dataset columns, like expected fee, arrive in your reward functions as keyword arguments. The config samples eight generations per prompt, caps completion length, and uses LoRA on top of your SFT model. | Hands-on: train_grpo.py<br>• fee_correct: strict parse, exact match<br>• one_amount_and_short: guard-rail bonus<br>• Dataset columns → reward kwargs<br>• 8 generations, LoRA, start from SFT | Code editor walkthrough of the two reward functions, then the GRPOConfig block with num_generations highlighted, then a reward curve rising in a terminal chart. |
| 7 | 3:42 | 31 | Can you do this without running GPUs? Sometimes. Amazon Bedrock offers reinforcement fine-tuning for Amazon Nova models, starting with Nova 2 Lite according to AWS documentation, with graders as Lambda functions or a model judge. Microsoft's Foundry has offered it for selected reasoning models with gated access. OpenAI offered it too, but began winding down its self-serve fine-tuning platform in twenty twenty-six. These offerings change quickly, so always check current documentation. | Hosted RFT (verify current docs)<br>• Amazon Bedrock: Nova (Nova 2 Lite first)<br>• Microsoft Foundry: selected models, gated<br>• OpenAI: platform winding down | Three cloud cards with plain-text labels; the third card carries an amber "winding down" tag. |
| 8 | 4:13 | 28 | Now expect reward hacking, because it will happen. Rewards climb, but real quality does not, and outputs get weird. Countermeasures: read samples every few hundred steps, not just the curves. Keep an independent evaluation, different from the training grader, on held-out prompts. Add guard-rail rewards, strict parsing and length caps. And always start from a decent SFT model, so exploration begins from sensible behavior. | Reward hacking countermeasures<br>• Read samples regularly<br>• Independent held-out evaluation<br>• Guard-rails, strict parsing, length caps<br>• Start from a good SFT model | Two charts side by side: training reward rising steadily, independent evaluation flat, with a warning icon between them; then a sample viewer panel. |
| 9 | 4:41 | 35 | A worked example, with illustrative results. A Karachi logistics firm extracts consignee, city, weight and cash-on-delivery amount from messy WhatsApp bookings. SFT struggled with Urdu numerals and mixed units. A per-field grader with partial credit and unit normalization, plus GRPO on four thousand real messages, improved held-out accuracy, especially on weights and amounts. But early on the model learned to leave uncertain fields empty, since an empty field was never wrong. They fixed the grader to penalize missing required fields. | Worked example: Karachi logistics (illustrative)<br>• Per-field grader, partial credit<br>• GRPO on 4,000 real messages<br>• Gains on weight and COD amount<br>• Hack found: empty fields → penalized | WhatsApp-style message bubbles with mixed numerals flowing into a structured form; one empty field flashes red and a grader rule updates. |
| 10 | 5:16 | 37 | A simple example. A tutoring app wants its small model to solve percentage problems like what is fifteen percent of two hundred forty. The grader is easy: compute the right answer in code and check the model's final number. They collect five hundred problems, run GRPO for an hour on top of an SFT model, and the pass rate on a separate test set rises. They also add a small reward for showing one clear final answer, which stops the model hedging between two numbers. | Simple example: percentage problems<br>• Grader: compute answer in code<br>• 500 problems, GRPO on SFT model<br>• Pass rate rises on separate test<br>• Bonus for one clear final answer | A math problem card; eight model answers appear, a code checker ticks the correct ones, and a pass-rate gauge rises over training steps. |
| 11 | 5:53 | 34 | How big does an RFT run need to be? Smaller than you might think for narrow tasks. A few hundred to a few thousand prompts can move a checkable metric, because each prompt produces a whole group of scored samples. The expensive part is generation: sampling eight answers per prompt costs far more than a single SFT pass. That is why fast generation, for example with vLLM integration, and short maximum completion lengths matter so much for cost. | Run size and cost<br>• Hundreds to thousands of prompts can help<br>• Each prompt → a group of samples<br>• Generation dominates cost<br>• Fast generation + short completions | Cost breakdown pie chart where the generation slice dominates; a single prompt fans out into eight sample cards. |
| 12 | 6:27 | 26 | Try this now. Write your grader before you write any training code. Collect ten outputs you know are correct and ten you know are wrong, including one sneaky wrong answer that looks right. Run the grader on all twenty. If it scores the sneaky one as correct, fix the grader now, because training would find that loophole within hours. | Try this now<br>• Grader first<br>• 10 correct + 10 wrong (incl. 1 sneaky)<br>• Fix any loophole before training | Twenty output cards pass through a grader gate; one sneaky card slips through green, then the gate is updated and it turns red. |
| 13 | 6:53 | 69 | Watch me do it. Before any training, I write the fee grader and test it. Ten correct answers score one; ten wrong answers score zero; then I write a sneaky answer that lists three amounts including the right one. The grader gives it one. Loophole. I change it to require exactly one parsed amount, rerun, and the sneaky answer now scores zero. Next I start GRPO on our SFT model with eight generations per prompt and five hundred fee questions. Every hundred steps I stop to read twenty sampled answers, not just the reward curve. Around step three hundred, answers get shorter and one starts hedging with approximately; the length bonus is fine, but I add a small penalty for hedging words. At the end, I run the independent evaluation, a separate held-out set scored by a different grader, and pass rate is clearly above the SFT baseline. Samples look normal. Only then do I save the adapter. | Watch me do it<br>1 Test grader (catch a loophole)<br>2 Fix: exactly one amount<br>3 GRPO, 8 generations<br>4 Read samples every 100 steps<br>5 Independent evaluation | Grader test grid with a sneaky card passing then failing after a code edit; training reward curve; periodic sample viewer panels; a hedging word highlighted; final comparison bar vs SFT baseline. |
| 14 | 8:02 | 28 | Recap. Use reinforcement fine-tuning when correctness is checkable. GRPO reinforces answers that beat their group's average, without a critic model. Graders must be correct, hard to game, dense and composable. Expect reward hacking and keep an independent evaluation. Your next step: write a grader for one checkable task, test it on twenty good and bad outputs, and list two ways a model could game it. | Recap<br>• Checkable → RFT<br>• Graders are the whole game<br>• Next: build and attack your own grader | Recap card with a gauge icon and a magnifying glass over a sample. |

<details><summary>Narration per scene (paste into ElevenLabs)</summary>

Scene 1 — Reinforcement fine-tuning

```text
Some questions have a right answer you can check. Is the fee correct? Does the code pass its tests? Did the model extract the right weight from the booking? When you can check, you can train differently. Instead of writing ideal answers or comparing pairs, you write a grader, and let the model learn to score higher. That is reinforcement fine-tuning. In this lesson you will learn GRPO, how to design graders, how to run it with TRL, and how to catch reward hacking.
```

Scene 2 — Analogy: eight attempts, one marker

```text
Here is an analogy for GRPO. Imagine a class of students who each attempt the same maths question eight times. A strict marker scores every attempt. The teacher does not compare a student with the whole school, only with their own eight attempts: which were better than their own average? The student learns to do more of what worked. The marker is the grader, and if the marker only checks whether the right number appears anywhere, clever students will write every number they can think of.
```

Scene 3 — GRPO in four steps

```text
Here is GRPO, group relative policy optimization, introduced in DeepSeek's maths research and used in training DeepSeek R1. For each prompt, the model samples a group of answers, say eight. Your graders score each one. Each answer is compared with its own group's average. Better than average gets reinforced, worse gets discouraged. Then the model updates, optionally with a penalty to stay near a reference. Because the comparison is within the group, you do not need a separate critic model, which makes it simpler and lighter than PPO.
```

Scene 4 — Good graders are

```text
The grader is the whole game. A good grader is correct: it rewards what you actually want, so test it on known good and bad outputs first. It is hard to game: if you reward containing the right number, the model may list every number it can think of, so parse strictly. It is dense enough to learn from, so partial credit per field often beats all or nothing. And it is composable: correctness plus format plus length, with weights.
```

Scene 5 — Grader examples

```text
Examples. For fee quotes: the parsed amount must equal the fee schedule's calculation, with exactly one amount and a currency. For extraction: per-field match against gold, valid JSON and no extra fields. For SQL: the query runs on a sandbox database and returns the expected rows, and only read-only statements are allowed. For policy answers: it cites the right clause, stays under a length limit and makes no forbidden promises. A model can act as a judge for fuzzier tasks, but it is easier to game, so prefer code.
```

Scene 6 — Hands-on: train_grpo.py

```text
The TRL script has two reward functions. The first parses amounts with a strict pattern and gives one point only if exactly one amount is quoted and it matches the expected fee from the dataset. The second gives a small bonus for quoting one amount in a short answer. Extra dataset columns, like expected fee, arrive in your reward functions as keyword arguments. The config samples eight generations per prompt, caps completion length, and uses LoRA on top of your SFT model.
```

Scene 7 — Hosted RFT (verify current docs)

```text
Can you do this without running GPUs? Sometimes. Amazon Bedrock offers reinforcement fine-tuning for Amazon Nova models, starting with Nova 2 Lite according to AWS documentation, with graders as Lambda functions or a model judge. Microsoft's Foundry has offered it for selected reasoning models with gated access. OpenAI offered it too, but began winding down its self-serve fine-tuning platform in twenty twenty-six. These offerings change quickly, so always check current documentation.
```

Scene 8 — Reward hacking countermeasures

```text
Now expect reward hacking, because it will happen. Rewards climb, but real quality does not, and outputs get weird. Countermeasures: read samples every few hundred steps, not just the curves. Keep an independent evaluation, different from the training grader, on held-out prompts. Add guard-rail rewards, strict parsing and length caps. And always start from a decent SFT model, so exploration begins from sensible behavior.
```

Scene 9 — Worked example: Karachi logistics (illustrative)

```text
A worked example, with illustrative results. A Karachi logistics firm extracts consignee, city, weight and cash-on-delivery amount from messy WhatsApp bookings. SFT struggled with Urdu numerals and mixed units. A per-field grader with partial credit and unit normalization, plus GRPO on four thousand real messages, improved held-out accuracy, especially on weights and amounts. But early on the model learned to leave uncertain fields empty, since an empty field was never wrong. They fixed the grader to penalize missing required fields.
```

Scene 10 — Simple example: percentage problems

```text
A simple example. A tutoring app wants its small model to solve percentage problems like what is fifteen percent of two hundred forty. The grader is easy: compute the right answer in code and check the model's final number. They collect five hundred problems, run GRPO for an hour on top of an SFT model, and the pass rate on a separate test set rises. They also add a small reward for showing one clear final answer, which stops the model hedging between two numbers.
```

Scene 11 — Run size and cost

```text
How big does an RFT run need to be? Smaller than you might think for narrow tasks. A few hundred to a few thousand prompts can move a checkable metric, because each prompt produces a whole group of scored samples. The expensive part is generation: sampling eight answers per prompt costs far more than a single SFT pass. That is why fast generation, for example with vLLM integration, and short maximum completion lengths matter so much for cost.
```

Scene 12 — Try this now

```text
Try this now. Write your grader before you write any training code. Collect ten outputs you know are correct and ten you know are wrong, including one sneaky wrong answer that looks right. Run the grader on all twenty. If it scores the sneaky one as correct, fix the grader now, because training would find that loophole within hours.
```

Scene 13 — Watch me do it

```text
Watch me do it. Before any training, I write the fee grader and test it. Ten correct answers score one; ten wrong answers score zero; then I write a sneaky answer that lists three amounts including the right one. The grader gives it one. Loophole. I change it to require exactly one parsed amount, rerun, and the sneaky answer now scores zero. Next I start GRPO on our SFT model with eight generations per prompt and five hundred fee questions. Every hundred steps I stop to read twenty sampled answers, not just the reward curve. Around step three hundred, answers get shorter and one starts hedging with approximately; the length bonus is fine, but I add a small penalty for hedging words. At the end, I run the independent evaluation, a separate held-out set scored by a different grader, and pass rate is clearly above the SFT baseline. Samples look normal. Only then do I save the adapter.
```

Scene 14 — Recap

```text
Recap. Use reinforcement fine-tuning when correctness is checkable. GRPO reinforces answers that beat their group's average, without a critic model. Graders must be correct, hard to game, dense and composable. Expect reward hacking and keep an independent evaluation. Your next step: write a grader for one checkable task, test it on twenty good and bad outputs, and list two ways a model could game it.
```

</details>

### 11. Evaluation before and after fine-tuning

Lesson `evaluation-before-and-after` · module “Evaluation, hosted platforms, embeddings and cost” · target 8 min · 15 scenes · 1153 words · planned 8:20 (words ≈ 8:21) · status: **to produce**

| Term | Say |
|---|---|
| macro-F1 | macro F one |
| MRR | M R R |
| nDCG | N D C G |
| p95 | P ninety-five |

| # | Time | Sec | Narration | On screen | Visual |
|---|---|---|---|---|---|
| 1 | 0:00 | 31 | Every fine-tuning project ends with the same question: is it better? And most teams answer it badly, by comparing against the wrong thing, looking only at averages, or deciding what better means after seeing the results. In this lesson you will build an evaluation plan that gives an honest answer: the right baseline, the four parts of a plan, test sets and slices, a comparison harness, and online testing before full rollout. | Evaluation before and after<br>• The right baseline<br>• A four-part plan<br>• Slices, harness, online tests | A scale with "fine-tuned" on one side and a question mark on the other, which resolves into a labeled baseline weight. |
| 2 | 0:31 | 33 | Here is an analogy. Evaluating a fine-tune is like testing a new recipe in a restaurant. You do not compare it with a raw ingredient, you compare it with the dish you already serve. You taste it across the whole menu, not just one plate, so you know it does not ruin the side dishes. And you decide in advance what better means, so nobody moves the goalposts when the chef is attached to their creation. | Analogy: a new recipe<br>• Compare with today's dish (best baseline)<br>• Taste across the menu (slices, regressions)<br>• Define better in advance | Two plates side by side labeled "current dish" and "new recipe"; a menu board with several items gets tick marks, and a sealed envelope labeled "decision rule" sits on the table. |
| 3 | 1:04 | 25 | First, the baseline. Not the raw base model. Compare against the best setup you reached without fine-tuning: your best prompt, examples, structure, retrieval and model choice. That is what you would ship otherwise. If the fine-tune cannot beat it, at acceptable cost and risk, it should not ship. This single choice changes the conclusion of many projects. | The right baseline<br>• Not the raw base model<br>• Best non-fine-tuned setup<br>• Must beat what you would otherwise ship | Two comparison bars: fine-tuned vs raw base shows a big gap, then the raw base bar is replaced by a taller "best prompted setup" bar and the gap shrinks. |
| 4 | 1:29 | 31 | A plan has four parts. Target metrics, what the fine-tune should improve. A regression suite, what must not get worse: general instructions, other languages, safety, formatting. Operational metrics: latency, throughput, cost per thousand requests. And a decision rule written in advance, for example, ship if accuracy improves by at least three points with a confidence interval above zero, no safety regression beyond one point, and p ninety-five latency under eight hundred milliseconds. | Four-part plan<br>• Target metrics<br>• Regression suite<br>• Operational metrics<br>• Decision rule, written first | Four stacked cards; the fourth card shows a short written rule with three conditions and a signature line. |
| 5 | 2:00 | 30 | Test sets. A held-out test set from the production distribution, split by customer or time, never used for decisions along the way. Slices: results by language, channel, class and difficulty, because an average can hide a collapse on Roman Urdu or a rare critical class. A challenge set of hand-written hard and adversarial cases. And a fresh sample of recent traffic, to check the model generalizes beyond its training period. | Test sets<br>• Held-out, split by entity/time<br>• Slices by language, class, channel<br>• Challenge set<br>• Fresh traffic sample | A dataset block splitting into four labeled pieces; a slice view shows several bars where one slice is noticeably lower than the rest. |
| 6 | 2:30 | 25 | Match metrics to the task. Classification: accuracy, and macro F1, which treats rare classes fairly, plus a confusion matrix. Extraction: per-field exact and normalized match, and JSON validity. Generation: rubric scores from trained reviewers or a calibrated judge, and blind pairwise win rate. Retrieval: recall at k and ranking metrics. Checkable reasoning: pass rate under an independent grader. | Metrics by task<br>• Classification: macro-F1, confusion matrix<br>• Extraction: per-field match, validity<br>• Generation: rubric, blind win rate<br>• Retrieval: recall@k, MRR, nDCG | Five small metric icons in a grid, each labeled with the task type beneath. |
| 7 | 2:55 | 33 | The harness in the lesson serves your adapter beside the base model with vLLM, so one server answers as the base model or as base plus adapter by model name. The Python script runs your baseline prompt and your fine-tuned model over the same test set, through the same client, and prints macro F1 and a per-class report for each. Add the paired bootstrap from the benchmarking lesson for confidence intervals, and repeat it per language slice. | Hands-on<br>• vLLM --enable-lora: base + adapter<br>• Same client, same test set<br>• Macro-F1 + per-class report<br>• Bootstrap intervals per slice | Terminal showing vllm serve with --lora-modules, then a Python output with two classification reports side by side. |
| 8 | 3:28 | 33 | For generative tasks, train reviewers with the rubric and anchor examples, and measure their agreement. Use blind, randomized pairwise comparisons between baseline and fine-tune. If you use an AI judge, calibrate it against at least fifty human judgments and swap the answer order. Then go online. Shadow mode runs the fine-tune on live traffic without showing outputs. A canary or A/B test routes a small share of real users. Only then, full rollout with monitoring. | Beyond offline<br>• Trained reviewers, blind pairs<br>• Calibrated judges<br>• Shadow → canary/A-B → rollout | Three-stage rollout diagram: a ghosted model processing live traffic, then a small slice of users routed, then full traffic with a monitoring dashboard. |
| 9 | 4:01 | 45 | A worked example, with illustrative numbers. A Dubai airline triages baggage claims. The baseline, a strong API model with a detailed prompt, scores macro F1 of point eight six. The fine-tuned three billion model scores point eight nine overall, but Arabic claims drop to point eight one against the baseline's point eight seven. Their rule said no slice may regress by more than two points, so they did not ship. They added nine hundred Arabic examples, retrained, reached point nine zero overall and point eight eight on Arabic, passed a week in shadow mode, and shipped with the API model as a fallback. | Worked example: Dubai airline (illustrative)<br>• Baseline 0.86 · fine-tune 0.89<br>• Arabic slice 0.81 vs 0.87 → blocked<br>• +900 Arabic examples → 0.90 / 0.88<br>• Shadow week → ship with fallback | Slice bar chart with the Arabic bar flagged red, then a second version where it turns green; a shield icon for the fallback. |
| 10 | 4:46 | 33 | A simple example. A travel agency's baseline is a strong API model with a good prompt, scoring ninety correct out of a hundred on intent classification. Their fine-tuned small model scores ninety-two. Before celebrating, they check slices: English ninety-four, Arabic eighty-eight, compared with ninety-one and eighty-nine for the baseline. Arabic dropped one point, within their two-point rule. Latency is five times lower. The rule says ship, so they ship, and keep watching the Arabic slice. | Simple example: travel agency (illustrative)<br>• Baseline 90 · fine-tune 92<br>• Arabic −1 (within 2-point rule)<br>• 5× lower latency<br>• Rule says ship | Bar chart with overall and per-language bars for baseline and fine-tune; a rule card checks each condition with a tick, ending with a "ship" stamp. |
| 11 | 5:19 | 32 | Make evaluation automatic. Put the test set, the baseline configuration, the scoring code and the decision rule in version control, and run them in your pipeline every time a model is trained. The pipeline produces the report and marks the model ship or no-ship according to the rule. Humans still review the report and the worst failures, but nobody has to remember how to run the evaluation, and nobody can quietly skip it. | Automate it<br>• Test set, baseline, scorer, rule in Git<br>• Pipeline runs on every training job<br>• Auto report + ship/no-ship flag<br>• Humans review failures | CI pipeline with stages train, evaluate, report; the evaluate stage outputs a report card with a green or red flag. |
| 12 | 5:51 | 33 | A question from teams under deadline pressure: can we skip the online test if offline results are great? You can shorten it, but do not skip it. Offline tests use the data you collected; live traffic brings new phrasing, new topics and new user behavior. Even a few days in shadow mode often reveals issues, like a new product name, that no offline set contained. A short shadow period is cheap insurance compared with a public failure. | FAQ: skip online testing?<br>• Shorten, don't skip<br>• Live traffic brings new phrasing and topics<br>• Shadow mode = cheap insurance | A calendar showing a short shadow-mode period; live messages flow into a ghosted model and one new product name gets flagged. |
| 13 | 6:24 | 24 | Try this now. Write your decision rule in three lines before your next training run. Line one: the target metric and minimum improvement. Line two: the maximum regression allowed on any slice or safety test. Line three: the latency or cost limit. Share it with a colleague and ask them to hold you to it. | Try this now<br>• Line 1: target + minimum gain<br>• Line 2: max slice/safety regression<br>• Line 3: latency or cost limit<br>• Share it | Three-line rule card types itself, then is sealed in an envelope and passed to a colleague icon. |
| 14 | 6:48 | 67 | Watch me do it. I write the decision rule first, in a text file: ship if macro F1 improves by at least three points with the interval above zero, no language slice drops by more than two points, and p ninety-five latency is under eight hundred milliseconds. I commit it. Then I start vLLM with the base model and our adapter registered under the name intents. I run the comparison script: baseline prompt on the base model versus the adapter, same test set. Overall macro F1 rises five points. I run the bootstrap: interval comfortably above zero. Now slices: English up six, Urdu up four, Arabic down one. Within the rule. Latency: p ninety-five at four hundred milliseconds. I also run the safety suite and a small general-instruction test: no regressions. I paste all numbers into the report and check each line of the rule. All conditions met: ship to shadow mode for one week. | Watch me do it<br>1 Write + commit the rule<br>2 Serve base + adapter<br>3 Compare overall + bootstrap<br>4 Check slices, latency, safety<br>5 Apply the rule | A text file with the rule typed and committed; vLLM launch with --lora-modules; a comparison output; slice bars with a two-point tolerance band; a checklist ticking each condition; a "shadow mode" stamp. |
| 15 | 7:55 | 25 | Recap. Beat your best non-fine-tuned setup, not the raw base model. Plan targets, regressions, operations and a written decision rule. Report by slice with confidence intervals. Use blind comparisons for generation, then shadow and canary before rollout. Your next step: write your decision rule, evaluate baseline against fine-tune by slice, and make a clear ship or no-ship call. | Recap<br>• Right baseline, written rule<br>• Slices + intervals<br>• Next: ship or no-ship, with evidence | Recap card with a gavel icon over a small results table. |

<details><summary>Narration per scene (paste into ElevenLabs)</summary>

Scene 1 — Evaluation before and after

```text
Every fine-tuning project ends with the same question: is it better? And most teams answer it badly, by comparing against the wrong thing, looking only at averages, or deciding what better means after seeing the results. In this lesson you will build an evaluation plan that gives an honest answer: the right baseline, the four parts of a plan, test sets and slices, a comparison harness, and online testing before full rollout.
```

Scene 2 — Analogy: a new recipe

```text
Here is an analogy. Evaluating a fine-tune is like testing a new recipe in a restaurant. You do not compare it with a raw ingredient, you compare it with the dish you already serve. You taste it across the whole menu, not just one plate, so you know it does not ruin the side dishes. And you decide in advance what better means, so nobody moves the goalposts when the chef is attached to their creation.
```

Scene 3 — The right baseline

```text
First, the baseline. Not the raw base model. Compare against the best setup you reached without fine-tuning: your best prompt, examples, structure, retrieval and model choice. That is what you would ship otherwise. If the fine-tune cannot beat it, at acceptable cost and risk, it should not ship. This single choice changes the conclusion of many projects.
```

Scene 4 — Four-part plan

```text
A plan has four parts. Target metrics, what the fine-tune should improve. A regression suite, what must not get worse: general instructions, other languages, safety, formatting. Operational metrics: latency, throughput, cost per thousand requests. And a decision rule written in advance, for example, ship if accuracy improves by at least three points with a confidence interval above zero, no safety regression beyond one point, and p ninety-five latency under eight hundred milliseconds.
```

Scene 5 — Test sets

```text
Test sets. A held-out test set from the production distribution, split by customer or time, never used for decisions along the way. Slices: results by language, channel, class and difficulty, because an average can hide a collapse on Roman Urdu or a rare critical class. A challenge set of hand-written hard and adversarial cases. And a fresh sample of recent traffic, to check the model generalizes beyond its training period.
```

Scene 6 — Metrics by task

```text
Match metrics to the task. Classification: accuracy, and macro F1, which treats rare classes fairly, plus a confusion matrix. Extraction: per-field exact and normalized match, and JSON validity. Generation: rubric scores from trained reviewers or a calibrated judge, and blind pairwise win rate. Retrieval: recall at k and ranking metrics. Checkable reasoning: pass rate under an independent grader.
```

Scene 7 — Hands-on

```text
The harness in the lesson serves your adapter beside the base model with vLLM, so one server answers as the base model or as base plus adapter by model name. The Python script runs your baseline prompt and your fine-tuned model over the same test set, through the same client, and prints macro F1 and a per-class report for each. Add the paired bootstrap from the benchmarking lesson for confidence intervals, and repeat it per language slice.
```

Scene 8 — Beyond offline

```text
For generative tasks, train reviewers with the rubric and anchor examples, and measure their agreement. Use blind, randomized pairwise comparisons between baseline and fine-tune. If you use an AI judge, calibrate it against at least fifty human judgments and swap the answer order. Then go online. Shadow mode runs the fine-tune on live traffic without showing outputs. A canary or A/B test routes a small share of real users. Only then, full rollout with monitoring.
```

Scene 9 — Worked example: Dubai airline (illustrative)

```text
A worked example, with illustrative numbers. A Dubai airline triages baggage claims. The baseline, a strong API model with a detailed prompt, scores macro F1 of point eight six. The fine-tuned three billion model scores point eight nine overall, but Arabic claims drop to point eight one against the baseline's point eight seven. Their rule said no slice may regress by more than two points, so they did not ship. They added nine hundred Arabic examples, retrained, reached point nine zero overall and point eight eight on Arabic, passed a week in shadow mode, and shipped with the API model as a fallback.
```

Scene 10 — Simple example: travel agency (illustrative)

```text
A simple example. A travel agency's baseline is a strong API model with a good prompt, scoring ninety correct out of a hundred on intent classification. Their fine-tuned small model scores ninety-two. Before celebrating, they check slices: English ninety-four, Arabic eighty-eight, compared with ninety-one and eighty-nine for the baseline. Arabic dropped one point, within their two-point rule. Latency is five times lower. The rule says ship, so they ship, and keep watching the Arabic slice.
```

Scene 11 — Automate it

```text
Make evaluation automatic. Put the test set, the baseline configuration, the scoring code and the decision rule in version control, and run them in your pipeline every time a model is trained. The pipeline produces the report and marks the model ship or no-ship according to the rule. Humans still review the report and the worst failures, but nobody has to remember how to run the evaluation, and nobody can quietly skip it.
```

Scene 12 — FAQ: skip online testing?

```text
A question from teams under deadline pressure: can we skip the online test if offline results are great? You can shorten it, but do not skip it. Offline tests use the data you collected; live traffic brings new phrasing, new topics and new user behavior. Even a few days in shadow mode often reveals issues, like a new product name, that no offline set contained. A short shadow period is cheap insurance compared with a public failure.
```

Scene 13 — Try this now

```text
Try this now. Write your decision rule in three lines before your next training run. Line one: the target metric and minimum improvement. Line two: the maximum regression allowed on any slice or safety test. Line three: the latency or cost limit. Share it with a colleague and ask them to hold you to it.
```

Scene 14 — Watch me do it

```text
Watch me do it. I write the decision rule first, in a text file: ship if macro F1 improves by at least three points with the interval above zero, no language slice drops by more than two points, and p ninety-five latency is under eight hundred milliseconds. I commit it. Then I start vLLM with the base model and our adapter registered under the name intents. I run the comparison script: baseline prompt on the base model versus the adapter, same test set. Overall macro F1 rises five points. I run the bootstrap: interval comfortably above zero. Now slices: English up six, Urdu up four, Arabic down one. Within the rule. Latency: p ninety-five at four hundred milliseconds. I also run the safety suite and a small general-instruction test: no regressions. I paste all numbers into the report and check each line of the rule. All conditions met: ship to shadow mode for one week.
```

Scene 15 — Recap

```text
Recap. Beat your best non-fine-tuned setup, not the raw base model. Plan targets, regressions, operations and a written decision rule. Report by slice with confidence intervals. Use blind comparisons for generation, then shadow and canary before rollout. Your next step: write your decision rule, evaluate baseline against fine-tune by slice, and make a clear ship or no-ship call.
```

</details>

### 12. Hosted fine-tuning: which platforms, which models, which trade-offs

Lesson `hosted-fine-tuning-apis` · module “Evaluation, hosted platforms, embeddings and cost” · target 8 min · 15 scenes · 1125 words · planned 8:11 (words ≈ 8:09) · status: **to produce**

| Term | Say |
|---|---|
| Vertex AI | vertex A I |
| Gemini | JEM-in-eye |
| Bedrock | bed rock |
| Nova | NOH-vah |
| boto3 | boto three |
| S3 | S three |
| Foundry | foundry |

| # | Time | Sec | Narration | On screen | Visual |
|---|---|---|---|---|---|
| 1 | 0:00 | 33 | In twenty twenty-six, a lot of teams woke up to an email saying the fine-tuning platform they built on was being wound down. Their fine-tuned models would keep running for a while, but new training would end. The teams that shrugged were the ones who had kept their data and evaluations portable. In this lesson you will map the hosted fine-tuning landscape, the formats and APIs, and the checklist that keeps you able to move. | Hosted fine-tuning<br>• The 2026 landscape<br>• Formats and APIs<br>• Staying portable | An email notification icon with a subject line reading "Changes to fine-tuning"; one team looks worried, another calmly opens a folder labeled portable data. |
| 2 | 0:33 | 32 | Here is an analogy. Hosted fine-tuning is like tailoring a suit at a department store. It is convenient, they have the machines, and the result fits well. But the suit stays in their store, and if the store closes the tailoring counter, you cannot take the pattern elsewhere. Self-managed fine-tuning on open weights is owning the pattern. Either way, keep your measurements, which are your data and your evaluation, in your own notebook. | Analogy: tailoring a suit<br>• Hosted = store tailoring, suit stays there<br>• Open weights = you own the pattern<br>• Your measurements = data + evals, always yours | A department store counter with a suit on display; a "counter closed" sign flips on. Next to it, a person holds a paper pattern and a notebook labeled measurements. |
| 3 | 1:05 | 33 | Here is the snapshot, as of September twenty twenty-six, and please verify it. Google's Vertex AI offers supervised fine-tuning for Gemini two point five Pro, Flash and Flash-Lite, plus preference tuning, and tuning for selected open models. Amazon Bedrock customizes Amazon Nova models, including supervised fine-tuning, distillation and reinforcement fine-tuning starting with Nova two Lite. Microsoft Foundry fine-tunes the GPT four point one and GPT four o families, with other methods and models on varying access. | Snapshot (verify)<br>• Vertex AI: Gemini 2.5 Pro/Flash/Flash-Lite SFT, preference tuning<br>• Bedrock: Nova SFT, distillation, RFT<br>• Foundry: GPT-4.1 and GPT-4o families | Three plain-text provider cards (no logos) with bullet lists appearing; a "verify current docs" stamp in the corner. |
| 4 | 1:38 | 29 | And the changes. OpenAI began winding down its self-serve fine-tuning platform this year. It closed to new organizations, is phasing out new jobs for existing customers, and keeps existing fine-tuned models available for inference until their base models are deprecated. Mistral's docs now place its self-serve fine-tuning API under legacy resources. Meanwhile, several inference platforms offer LoRA fine-tuning on open-weight models, often letting you download the adapter. | What changed<br>• OpenAI: self-serve fine-tuning winding down<br>• Mistral: fine-tuning API under legacy docs<br>• Open-model platforms: LoRA, often exportable | Timeline with two amber markers for the changes, and a green marker for open-model platforms with a download arrow. |
| 5 | 2:07 | 29 | That makes portability the key strategic question. A fine-tune of a closed model lives only with that provider. You keep your data and evaluation, not the weights. A fine-tune of an open model, on your GPUs or a platform that exports adapters, gives you portable weights. Either way, keep your datasets, labeling guides, graders and evaluation harness provider-neutral, and moving becomes days of work, not months. | Portability<br>• Closed-model fine-tune: data and evals only<br>• Open-model fine-tune: portable weights<br>• Keep all assets provider-neutral | Two suitcases: one small labeled "data + evals" that travels anywhere, one larger with weights that only an open-model path lets you carry. |
| 6 | 2:36 | 30 | Formats differ. OpenAI-style chat format, also used by Azure, is a list of messages with roles system, user and assistant. Gemini on Vertex uses contents with parts, and the assistant role is called model, plus a separate system instruction. TRL uses prompt and completion. The lesson shows the same example in all three. Keep one canonical format and write small converters, so switching is a script, not a project. | Formats<br>• OpenAI/Azure: messages (system, user, assistant)<br>• Vertex Gemini: contents/parts, role "model"<br>• TRL: prompt + completion<br>• One canonical format + converters | Three JSON lines stacked with matching fields connected by coloured lines, and a converter gear between them. |
| 7 | 3:06 | 34 | The lesson includes two illustrative job launches. On Vertex AI, the Google Gen AI SDK's tunings dot tune call takes a base model, a training dataset in Cloud Storage and a config with a display name and validation data, and you poll until it finishes. On Bedrock, boto3's create model customization job takes a job name, role, base model, customization type, S3 locations and hyperparameters, whose names differ per base model. Check each SDK's current docs before running. | Launching jobs<br>• Vertex: client.tunings.tune(base_model, datasets, config)<br>• Bedrock: create_model_customization_job(...)<br>• Hyperparameters vary by model | Two code panels side by side with key parameters highlighted; small cloud-storage and S3 bucket icons feed into each. |
| 8 | 3:40 | 25 | Before choosing, run a six-point checklist. Which base models can you tune, and in which regions? Which methods? How is training data handled: retained, used for anything else, encrypted, region-bound? What does inference cost for custom models, and is dedicated capacity required? What happens when the base model is deprecated? And can you export weights or adapters? | Six-point checklist<br>• Models + regions<br>• Methods<br>• Data handling<br>• Inference price/capacity<br>• Deprecation lifecycle<br>• Export options | Checklist with six items and a small scorecard for two providers filling in beside it. |
| 9 | 4:05 | 31 | A worked example. A Saudi e-commerce firm needs an Arabic intent classifier with data staying in the kingdom. They check which providers offer tuning and inference in an in-kingdom region, find their preferred closed model is not tunable there, and choose LoRA on an open-weight model using in-kingdom cloud GPUs. Their adapters are portable, and their canonical data and harness mean they can compare a hosted option later if availability changes. | Worked example: Riyadh e-commerce<br>• Requirement: in-kingdom data<br>• Preferred closed model not tunable there<br>• LoRA on open model, in-kingdom GPUs<br>• Portable assets for later comparison | Map of Saudi Arabia with a region pin; a closed-model card greyed out with "not available here", an open-model card with an adapter icon highlighted. |
| 10 | 4:36 | 38 | A simple example. A marketing team already runs everything on Google Cloud and needs a model that writes product titles in a strict format. Vertex AI supports supervised tuning for the Gemini two point five models they already use. They convert their canonical data to the Gemini format with a small script, launch a job, and compare the tuned model with their best prompt. The tuned model wins on format accuracy. They keep the canonical data and converter in Git, so they could re-run the same experiment elsewhere. | Simple example: team on Google Cloud<br>• Vertex AI supervised tuning<br>• Convert canonical data → Gemini format<br>• Compare with best prompt<br>• Keep data + converter in Git | Pipeline animation: canonical JSONL → converter gear → Gemini-format file → tuning job icon → results card, with a Git branch icon storing the first two items. |
| 11 | 5:14 | 33 | One more consideration: lifecycle. Hosted base models are retired on a schedule, and a fine-tune of a retired base model stops working when the base does. When you pick a hosted path, note the base model's announced retirement policy, set a reminder well before any date, and budget for retraining on the successor, including re-running your full evaluation. With portable data and an automated evaluation, that migration becomes a routine task rather than an emergency. | Lifecycle planning<br>• Base models retire on a schedule<br>• Fine-tunes retire with them<br>• Calendar reminders + retraining budget<br>• Portable data makes it routine | Timeline with a base model retirement marker; a fine-tune bar ending at the same point, and a new bar for the successor starting earlier with an overlap period. |
| 12 | 5:47 | 28 | A question worth asking every time: does a hosted fine-tune cost more to run than the base model? It can. Some platforms price inference on custom models differently, and some require dedicated capacity to serve them. Put those numbers into your cost model before training, not after. A fine-tune that saves prompt tokens but requires expensive dedicated hosting may not save money at your volume. | FAQ: is custom-model inference pricier?<br>• It can be<br>• Some require dedicated capacity<br>• Model costs before training | Two cost bars: base model with long prompts vs custom model with short prompts plus a dedicated-capacity block; a question mark over which is cheaper at your volume. |
| 13 | 6:15 | 20 | Try this now. Write a twenty-line converter from your canonical JSON lines to one other provider's format, for example from messages to Gemini's contents and parts. Run it on ten examples and validate them. Once you have one converter working, switching providers stops being scary. | Try this now<br>• 20-line converter to one other format<br>• Run on 10 examples<br>• Validate the output | Split code view: canonical JSON on the left, converted JSON on the right, with matching fields linked by animated lines. |
| 14 | 6:35 | 71 | Watch me do it. Our canonical data is prompt and completion JSON lines. I write a short converter to the Gemini format: the system message becomes the system instruction, user turns become contents with role user and a text part, and the assistant completion becomes role model. I convert ten examples and validate them against the format described in the docs. Then I upload the full files to a storage bucket in the approved region and launch the tuning job with the Google Gen AI SDK, passing the base model, training dataset and a validation dataset. While it runs, I fill in the six-point checklist: models and regions, methods, data handling from the terms page, inference pricing for tuned models, the deprecation policy for the base, and export options, which for this closed model are none. When the job finishes, I evaluate the tuned model with the same harness and the same test set as our open-model adapter, so the comparison is fair. | Watch me do it<br>1 Write a converter<br>2 Validate 10 examples<br>3 Upload + launch job<br>4 Fill the 6-point checklist<br>5 Evaluate with the same harness | Code editor with a converter function mapping fields with colored arrows; a validation output; a cloud storage upload bar; the tuning-job code; a checklist with six rows filling in; a comparison table of two models on one test set. |
| 15 | 7:46 | 25 | Recap. Hosted fine-tuning is convenient and fast-moving. Know the current landscape, and verify it. Closed-model fine-tunes are not portable, so keep data, graders and evaluations provider-neutral. Check regions, data handling, inference pricing and lifecycle. Your next step: complete the checklist for two hosted options and one self-managed option, and write converters from your canonical format to each. | Recap<br>• Verify, then commit<br>• Portability by design<br>• Next: checklist + converters | Recap card with a suitcase icon and a checklist. |

<details><summary>Narration per scene (paste into ElevenLabs)</summary>

Scene 1 — Hosted fine-tuning

```text
In twenty twenty-six, a lot of teams woke up to an email saying the fine-tuning platform they built on was being wound down. Their fine-tuned models would keep running for a while, but new training would end. The teams that shrugged were the ones who had kept their data and evaluations portable. In this lesson you will map the hosted fine-tuning landscape, the formats and APIs, and the checklist that keeps you able to move.
```

Scene 2 — Analogy: tailoring a suit

```text
Here is an analogy. Hosted fine-tuning is like tailoring a suit at a department store. It is convenient, they have the machines, and the result fits well. But the suit stays in their store, and if the store closes the tailoring counter, you cannot take the pattern elsewhere. Self-managed fine-tuning on open weights is owning the pattern. Either way, keep your measurements, which are your data and your evaluation, in your own notebook.
```

Scene 3 — Snapshot (verify)

```text
Here is the snapshot, as of September twenty twenty-six, and please verify it. Google's Vertex AI offers supervised fine-tuning for Gemini two point five Pro, Flash and Flash-Lite, plus preference tuning, and tuning for selected open models. Amazon Bedrock customizes Amazon Nova models, including supervised fine-tuning, distillation and reinforcement fine-tuning starting with Nova two Lite. Microsoft Foundry fine-tunes the GPT four point one and GPT four o families, with other methods and models on varying access.
```

Scene 4 — What changed

```text
And the changes. OpenAI began winding down its self-serve fine-tuning platform this year. It closed to new organizations, is phasing out new jobs for existing customers, and keeps existing fine-tuned models available for inference until their base models are deprecated. Mistral's docs now place its self-serve fine-tuning API under legacy resources. Meanwhile, several inference platforms offer LoRA fine-tuning on open-weight models, often letting you download the adapter.
```

Scene 5 — Portability

```text
That makes portability the key strategic question. A fine-tune of a closed model lives only with that provider. You keep your data and evaluation, not the weights. A fine-tune of an open model, on your GPUs or a platform that exports adapters, gives you portable weights. Either way, keep your datasets, labeling guides, graders and evaluation harness provider-neutral, and moving becomes days of work, not months.
```

Scene 6 — Formats

```text
Formats differ. OpenAI-style chat format, also used by Azure, is a list of messages with roles system, user and assistant. Gemini on Vertex uses contents with parts, and the assistant role is called model, plus a separate system instruction. TRL uses prompt and completion. The lesson shows the same example in all three. Keep one canonical format and write small converters, so switching is a script, not a project.
```

Scene 7 — Launching jobs

```text
The lesson includes two illustrative job launches. On Vertex AI, the Google Gen AI SDK's tunings dot tune call takes a base model, a training dataset in Cloud Storage and a config with a display name and validation data, and you poll until it finishes. On Bedrock, boto3's create model customization job takes a job name, role, base model, customization type, S3 locations and hyperparameters, whose names differ per base model. Check each SDK's current docs before running.
```

Scene 8 — Six-point checklist

```text
Before choosing, run a six-point checklist. Which base models can you tune, and in which regions? Which methods? How is training data handled: retained, used for anything else, encrypted, region-bound? What does inference cost for custom models, and is dedicated capacity required? What happens when the base model is deprecated? And can you export weights or adapters?
```

Scene 9 — Worked example: Riyadh e-commerce

```text
A worked example. A Saudi e-commerce firm needs an Arabic intent classifier with data staying in the kingdom. They check which providers offer tuning and inference in an in-kingdom region, find their preferred closed model is not tunable there, and choose LoRA on an open-weight model using in-kingdom cloud GPUs. Their adapters are portable, and their canonical data and harness mean they can compare a hosted option later if availability changes.
```

Scene 10 — Simple example: team on Google Cloud

```text
A simple example. A marketing team already runs everything on Google Cloud and needs a model that writes product titles in a strict format. Vertex AI supports supervised tuning for the Gemini two point five models they already use. They convert their canonical data to the Gemini format with a small script, launch a job, and compare the tuned model with their best prompt. The tuned model wins on format accuracy. They keep the canonical data and converter in Git, so they could re-run the same experiment elsewhere.
```

Scene 11 — Lifecycle planning

```text
One more consideration: lifecycle. Hosted base models are retired on a schedule, and a fine-tune of a retired base model stops working when the base does. When you pick a hosted path, note the base model's announced retirement policy, set a reminder well before any date, and budget for retraining on the successor, including re-running your full evaluation. With portable data and an automated evaluation, that migration becomes a routine task rather than an emergency.
```

Scene 12 — FAQ: is custom-model inference pricier?

```text
A question worth asking every time: does a hosted fine-tune cost more to run than the base model? It can. Some platforms price inference on custom models differently, and some require dedicated capacity to serve them. Put those numbers into your cost model before training, not after. A fine-tune that saves prompt tokens but requires expensive dedicated hosting may not save money at your volume.
```

Scene 13 — Try this now

```text
Try this now. Write a twenty-line converter from your canonical JSON lines to one other provider's format, for example from messages to Gemini's contents and parts. Run it on ten examples and validate them. Once you have one converter working, switching providers stops being scary.
```

Scene 14 — Watch me do it

```text
Watch me do it. Our canonical data is prompt and completion JSON lines. I write a short converter to the Gemini format: the system message becomes the system instruction, user turns become contents with role user and a text part, and the assistant completion becomes role model. I convert ten examples and validate them against the format described in the docs. Then I upload the full files to a storage bucket in the approved region and launch the tuning job with the Google Gen AI SDK, passing the base model, training dataset and a validation dataset. While it runs, I fill in the six-point checklist: models and regions, methods, data handling from the terms page, inference pricing for tuned models, the deprecation policy for the base, and export options, which for this closed model are none. When the job finishes, I evaluate the tuned model with the same harness and the same test set as our open-model adapter, so the comparison is fair.
```

Scene 15 — Recap

```text
Recap. Hosted fine-tuning is convenient and fast-moving. Know the current landscape, and verify it. Closed-model fine-tunes are not portable, so keep data, graders and evaluations provider-neutral. Check regions, data handling, inference pricing and lifecycle. Your next step: complete the checklist for two hosted options and one self-managed option, and write converters from your canonical format to each.
```

</details>

### 13. Fine-tuning embedding models for better retrieval

Lesson `embeddings-fine-tuning` · module “Evaluation, hosted platforms, embeddings and cost” · target 8 min · 15 scenes · 1152 words · planned 8:20 (words ≈ 8:21) · status: **to produce**

| Term | Say |
|---|---|
| MNRL | M N R L |
| MRR | M R R |
| nDCG | N D C G |
| Recall@k | recall at K |
| TEI | T E I |
| MiniLM | mini L M |

| # | Time | Sec | Narration | On screen | Visual |
|---|---|---|---|---|---|
| 1 | 0:00 | 31 | Here is a secret about RAG systems. When answers are wrong, the language model often gets the blame, but the real culprit is retrieval. The right passage never reached the model. And one of the cheapest, highest-return fixes is fine-tuning the embedding model on your own queries. In this lesson you will learn when to do it, how contrastive training works, a complete Sentence Transformers script, and the operational steps people forget. | Embedding fine-tuning<br>• Retrieval is often the bottleneck<br>• How contrastive training works<br>• Script + operations | A RAG pipeline diagram where the generator is initially blamed with a red outline, then the spotlight moves to the retrieval step showing a missing document. |
| 2 | 0:31 | 34 | Here is an analogy. An embedding model is a librarian who shelves books by meaning. A general librarian is great with popular topics but gets confused by your company's jargon, maybe shelving refund reversal next to reverse osmosis. Fine-tuning is spending a week with that librarian, showing them real questions and the right books. Hard negatives are the tricky cases: books that look relevant but are not. After the week, they shelve and find your books far better. | Analogy: training a librarian<br>• General librarian confused by jargon<br>• Show real questions + right books<br>• Hard negatives = look-alike wrong books | Library shelf animation: a book labeled "refund reversal" is misplaced next to "reverse osmosis", then moved to the correct shelf after training pairs appear on cards. |
| 3 | 1:05 | 31 | Generic embedding models learn from broad web text. They may not understand your product names, jargon, abbreviations, or code-mixed queries like Roman Urdu. Embedding models are small and train quickly, often in minutes to an hour on one GPU, so fine-tuning them is cheap. But try the cheaper steps first: hybrid keyword and vector search, better chunking, metadata filters, and a reranker that rescores the top results. Fine-tune when retrieval still misses. | Why and when<br>• Generic models miss jargon, code-mixing<br>• Small, fast, cheap to train<br>• First: hybrid, chunking, filters, reranker<br>• Then: fine-tune | A ladder of four cheaper steps with ticks, and a fifth step labeled fine-tune embeddings highlighted at the top. |
| 4 | 1:36 | 32 | How does it learn? You provide pairs: an anchor, which is a query, and a positive, the passage that answers it. The usual loss, multiple negatives ranking loss, pulls each query toward its own passage and pushes it away from every other passage in the batch. So bigger batches mean more negatives. And hard negatives, passages that look relevant but are not, teach the model the fine distinctions that matter in your domain. | Contrastive training<br>• (anchor, positive) pairs<br>• MNRL: other in-batch positives = negatives<br>• Bigger batches, hard negatives | Vector space animation: a query point moves closer to its matching passage point while other passage points drift away; a look-alike point is labeled hard negative. |
| 5 | 2:08 | 29 | Where do pairs come from? Search logs, where a query led to a document the user clicked and stayed on. Support tickets, where an agent linked a help article to a customer's question. And synthetic questions, where a model writes realistic questions for each passage, which you then filter. Real pairs from your own users are the most valuable, because they capture how people actually ask. | Pair sources<br>• Search logs (click and stay)<br>• Support tickets (agent-linked article)<br>• Synthetic questions (filtered) | Three streams feeding into a pairs dataset: a search bar, a support chat with a linked article, and a model icon generating questions. |
| 6 | 2:37 | 32 | The script in the lesson uses Sentence Transformers. It loads a multilingual base model, reads anchor and positive pairs, holds out ten percent, and optionally mines hard negatives with the current model. It builds an information retrieval evaluator, measures before training, trains one epoch with multiple negatives ranking loss and a no-duplicates batch sampler, then measures again and saves. Recent versions reorganized some import paths, so check the docs if an import fails. | Hands-on: train_embed.py<br>• Multilingual base model<br>• Hard negatives (optional)<br>• IR evaluator before/after<br>• MNRL + no-duplicates sampler | Editor walkthrough of the script, then terminal output showing a before metrics block and an after metrics block with Recall@5 highlighted. |
| 7 | 3:09 | 32 | Now the operational steps people forget. Re-index everything. Vectors from the old and new model are not comparable, so re-embed the whole corpus, ideally into a new index, then switch. Version the index with the model name and revision. Keep any query and document prefixes consistent between training and serving. And note that embedding models are small enough for CPU at moderate traffic, or you can serve them from inference servers that support embeddings. | Operations<br>• Re-embed the whole corpus<br>• New, versioned index, then switch<br>• Consistent prefixes<br>• CPU or embedding server | Two index boxes labeled v1 and v2; documents stream into v2, a switch flips traffic from v1 to v2, and v1 fades. |
| 8 | 3:41 | 25 | Evaluate properly. The quick evaluator in the script uses only the test pairs, which is a start. A realistic evaluation searches the full corpus, all your passages, with real held-out queries, and reports recall at k and mean reciprocal rank, compared with the base model and with a reranker-only baseline. Also check that other languages did not regress. | Evaluate properly<br>• Full corpus, real queries<br>• Recall@k, MRR<br>• vs base model and reranker-only<br>• Check other languages | Bar chart comparing Recall@5 for base, reranker-only and fine-tuned, split by language. |
| 9 | 4:06 | 42 | A worked example, with illustrative numbers. A Pakistani fintech's help center is written in English, but many searches are in Roman Urdu, like bill payment nahi ho rahi. A generic multilingual model found the right article in the top five for about half of those. They built eight thousand pairs from agent-linked articles, added four thousand filtered synthetic questions, mined hard negatives, and trained for under an hour on one GPU. Recall at five on real Roman Urdu queries rose substantially, English held steady, and they switched to a new versioned index after a shadow comparison. | Worked example: Roman Urdu search (illustrative)<br>• ~50% top-5 hit rate before<br>• 8,000 real + 4,000 synthetic pairs<br>• < 1 hour on one GPU<br>• Big Roman Urdu gain, English stable | Search box with a Roman Urdu query returning the wrong article, then the right article at the top after fine-tuning; a small before/after metric. |
| 10 | 4:48 | 31 | A simple example. An electronics store's search struggles when customers type phone charger that does not overheat, because product pages talk about thermal protection. They collect three hundred pairs from search logs where customers clicked and bought, fine-tune a small multilingual embedding model for twenty minutes, and re-index their two thousand product pages. Recall at five on real held-out searches improves noticeably, and they switch after a week of shadow testing. | Simple example: electronics search<br>• "doesn't overheat" vs "thermal protection"<br>• 300 click-and-buy pairs<br>• 20-minute fine-tune + re-index<br>• Recall@5 improves | Search bar with a query; before: irrelevant results; after: the right charger at the top, with a small recall gauge moving up. |
| 11 | 5:19 | 34 | Should you fine-tune the embedding model or add a reranker? Often both, in that order of cheapness. A reranker, a cross-encoder that reads the query and each candidate together, can fix ordering problems quickly without re-indexing. But a reranker can only reorder what retrieval already found. If the right passage never appears in the top fifty, only a better embedding model, or hybrid keyword search, will bring it in. Measure recall at fifty to tell which problem you have. | Reranker or fine-tune?<br>• Reranker: fixes ordering, no re-index<br>• Can only reorder what was found<br>• Right passage missing from top 50 → better embeddings or hybrid<br>• Check recall@50 | Two-stage retrieval diagram: a wide funnel retrieving 50 candidates, then a reranker reordering them; a missing document outside the funnel is highlighted. |
| 12 | 5:53 | 33 | A question from RAG teams: should we fine-tune the embedding model, the generator, or both? Start with whichever step is failing. If the right passages are not being retrieved, the generator cannot fix that, so improve retrieval first. If the right passages are retrieved but answers are still poor, work on the generator's prompt, format or, eventually, fine-tuning. Your evaluation should separately measure retrieval hit rate and answer quality so you know which one to fix. | FAQ: tune embeddings, generator or both?<br>• Fix the failing step first<br>• Missing passages → retrieval<br>• Good passages, bad answers → generator<br>• Measure both separately | RAG pipeline with two gauges, retrieval hit rate and answer quality; the lower gauge pulses to show where to focus. |
| 13 | 6:26 | 25 | Try this now. Export fifty real queries from your search or support logs, and for each, write the document that should have been found. Run them through your current retrieval and count how often the right document is in the top five. That single number is your baseline, and it tells you whether embedding fine-tuning is worth it. | Try this now<br>• 50 real queries + correct documents<br>• Run current retrieval<br>• Count top-5 hits = baseline | A table of 50 queries with a hit/miss column filling in; a percentage appears at the bottom labeled baseline recall@5. |
| 14 | 6:51 | 64 | Watch me do it. I export three months of help-center searches where users clicked an article and did not come back to search again. After cleaning, I have two thousand four hundred query and article pairs. I split off ten percent, and for evaluation I use the full corpus of all articles, not just the held-out positives. Baseline recall at five: fifty-eight percent. I mine one hard negative per pair with the current model, and train one epoch with multiple negatives ranking loss and the no-duplicates sampler, batch size sixty-four. It takes twenty-five minutes. Recall at five on the same evaluation rises to seventy-three percent, and English-only queries do not regress. Then operations: I embed every article with the new model into a new index named with the model version, run both indexes in shadow for a week, compare results on live queries, and switch. All numbers illustrative. | Watch me do it (illustrative)<br>1 2,400 click pairs<br>2 Full-corpus baseline: 58%<br>3 Hard negatives + 1 epoch<br>4 Recall@5: 73%<br>5 New versioned index, shadow, switch | Search-log table filtering to click pairs; an evaluator output showing a baseline; training progress; an after-metrics panel; two index boxes v1 and v2 running in parallel before a traffic switch. |
| 15 | 7:55 | 25 | Recap. Retrieval caps RAG quality. Try cheaper fixes first, then fine-tune embeddings with anchor and positive pairs, in-batch negatives and hard negatives. Evaluate on real queries against the full corpus, and always re-index into a versioned index. Your next step: build five hundred pairs from your logs, fine-tune, and report recall at five and MRR before and after. | Recap<br>• Pairs, negatives, evaluation, re-index<br>• Next: 500 pairs, before/after metrics | Recap card with a magnifying glass over a vector cluster. |

<details><summary>Narration per scene (paste into ElevenLabs)</summary>

Scene 1 — Embedding fine-tuning

```text
Here is a secret about RAG systems. When answers are wrong, the language model often gets the blame, but the real culprit is retrieval. The right passage never reached the model. And one of the cheapest, highest-return fixes is fine-tuning the embedding model on your own queries. In this lesson you will learn when to do it, how contrastive training works, a complete Sentence Transformers script, and the operational steps people forget.
```

Scene 2 — Analogy: training a librarian

```text
Here is an analogy. An embedding model is a librarian who shelves books by meaning. A general librarian is great with popular topics but gets confused by your company's jargon, maybe shelving refund reversal next to reverse osmosis. Fine-tuning is spending a week with that librarian, showing them real questions and the right books. Hard negatives are the tricky cases: books that look relevant but are not. After the week, they shelve and find your books far better.
```

Scene 3 — Why and when

```text
Generic embedding models learn from broad web text. They may not understand your product names, jargon, abbreviations, or code-mixed queries like Roman Urdu. Embedding models are small and train quickly, often in minutes to an hour on one GPU, so fine-tuning them is cheap. But try the cheaper steps first: hybrid keyword and vector search, better chunking, metadata filters, and a reranker that rescores the top results. Fine-tune when retrieval still misses.
```

Scene 4 — Contrastive training

```text
How does it learn? You provide pairs: an anchor, which is a query, and a positive, the passage that answers it. The usual loss, multiple negatives ranking loss, pulls each query toward its own passage and pushes it away from every other passage in the batch. So bigger batches mean more negatives. And hard negatives, passages that look relevant but are not, teach the model the fine distinctions that matter in your domain.
```

Scene 5 — Pair sources

```text
Where do pairs come from? Search logs, where a query led to a document the user clicked and stayed on. Support tickets, where an agent linked a help article to a customer's question. And synthetic questions, where a model writes realistic questions for each passage, which you then filter. Real pairs from your own users are the most valuable, because they capture how people actually ask.
```

Scene 6 — Hands-on: train_embed.py

```text
The script in the lesson uses Sentence Transformers. It loads a multilingual base model, reads anchor and positive pairs, holds out ten percent, and optionally mines hard negatives with the current model. It builds an information retrieval evaluator, measures before training, trains one epoch with multiple negatives ranking loss and a no-duplicates batch sampler, then measures again and saves. Recent versions reorganized some import paths, so check the docs if an import fails.
```

Scene 7 — Operations

```text
Now the operational steps people forget. Re-index everything. Vectors from the old and new model are not comparable, so re-embed the whole corpus, ideally into a new index, then switch. Version the index with the model name and revision. Keep any query and document prefixes consistent between training and serving. And note that embedding models are small enough for CPU at moderate traffic, or you can serve them from inference servers that support embeddings.
```

Scene 8 — Evaluate properly

```text
Evaluate properly. The quick evaluator in the script uses only the test pairs, which is a start. A realistic evaluation searches the full corpus, all your passages, with real held-out queries, and reports recall at k and mean reciprocal rank, compared with the base model and with a reranker-only baseline. Also check that other languages did not regress.
```

Scene 9 — Worked example: Roman Urdu search (illustrative)

```text
A worked example, with illustrative numbers. A Pakistani fintech's help center is written in English, but many searches are in Roman Urdu, like bill payment nahi ho rahi. A generic multilingual model found the right article in the top five for about half of those. They built eight thousand pairs from agent-linked articles, added four thousand filtered synthetic questions, mined hard negatives, and trained for under an hour on one GPU. Recall at five on real Roman Urdu queries rose substantially, English held steady, and they switched to a new versioned index after a shadow comparison.
```

Scene 10 — Simple example: electronics search

```text
A simple example. An electronics store's search struggles when customers type phone charger that does not overheat, because product pages talk about thermal protection. They collect three hundred pairs from search logs where customers clicked and bought, fine-tune a small multilingual embedding model for twenty minutes, and re-index their two thousand product pages. Recall at five on real held-out searches improves noticeably, and they switch after a week of shadow testing.
```

Scene 11 — Reranker or fine-tune?

```text
Should you fine-tune the embedding model or add a reranker? Often both, in that order of cheapness. A reranker, a cross-encoder that reads the query and each candidate together, can fix ordering problems quickly without re-indexing. But a reranker can only reorder what retrieval already found. If the right passage never appears in the top fifty, only a better embedding model, or hybrid keyword search, will bring it in. Measure recall at fifty to tell which problem you have.
```

Scene 12 — FAQ: tune embeddings, generator or both?

```text
A question from RAG teams: should we fine-tune the embedding model, the generator, or both? Start with whichever step is failing. If the right passages are not being retrieved, the generator cannot fix that, so improve retrieval first. If the right passages are retrieved but answers are still poor, work on the generator's prompt, format or, eventually, fine-tuning. Your evaluation should separately measure retrieval hit rate and answer quality so you know which one to fix.
```

Scene 13 — Try this now

```text
Try this now. Export fifty real queries from your search or support logs, and for each, write the document that should have been found. Run them through your current retrieval and count how often the right document is in the top five. That single number is your baseline, and it tells you whether embedding fine-tuning is worth it.
```

Scene 14 — Watch me do it (illustrative)

```text
Watch me do it. I export three months of help-center searches where users clicked an article and did not come back to search again. After cleaning, I have two thousand four hundred query and article pairs. I split off ten percent, and for evaluation I use the full corpus of all articles, not just the held-out positives. Baseline recall at five: fifty-eight percent. I mine one hard negative per pair with the current model, and train one epoch with multiple negatives ranking loss and the no-duplicates sampler, batch size sixty-four. It takes twenty-five minutes. Recall at five on the same evaluation rises to seventy-three percent, and English-only queries do not regress. Then operations: I embed every article with the new model into a new index named with the model version, run both indexes in shadow for a week, compare results on live queries, and switch. All numbers illustrative.
```

Scene 15 — Recap

```text
Recap. Retrieval caps RAG quality. Try cheaper fixes first, then fine-tune embeddings with anchor and positive pairs, in-batch negatives and hard negatives. Evaluate on real queries against the full corpus, and always re-index into a versioned index. Your next step: build five hundred pairs from your logs, fine-tune, and report recall at five and MRR before and after.
```

</details>

### 14. Cost modeling: training, inference and break-even

Lesson `cost-modeling-for-custom-models` · module “Evaluation, hosted platforms, embeddings and cost” · target 8 min · 15 scenes · 1120 words · planned 8:06 (words ≈ 8:07) · status: **to produce**

| Term | Say |
|---|---|
| GPU | G P U |

| # | Time | Sec | Narration | On screen | Visual |
|---|---|---|---|---|---|
| 1 | 0:00 | 31 | Sooner or later, someone from finance asks two questions: what does this cost, and when does it pay back? If your answer is it depends, the project stalls. In this lesson you will build a cost model for custom models that you can defend: one-off and recurring costs, two ways to estimate training, inference economics, break-even, and stress tests. Every number I use is illustrative; you will plug in your own. | Cost modeling<br>• One-off vs recurring<br>• Training and inference<br>• Break-even and stress tests | A finance dashboard with two empty fields labeled cost and payback that fill in as the scene progresses. |
| 2 | 0:31 | 33 | Here is an analogy. Deciding whether to fine-tune for cost is like deciding whether to buy a delivery van instead of paying a courier per parcel. The van has an upfront price, insurance and maintenance. The courier charges per parcel. With a few parcels a week, the courier wins. With hundreds a day, the van pays for itself. Break-even is simply the week when the van's total cost drops below the courier bills you would have paid. | Analogy: van vs courier<br>• Fine-tune = buy a van (upfront + upkeep)<br>• API = courier per parcel<br>• Volume decides<br>• Break-even = lines cross | Two cumulative cost lines over weeks: a flat-start van line with an upfront jump and a steadily rising courier line; they cross, and a marker labels break-even. |
| 3 | 1:04 | 25 | Start with the components. One-off costs: data labeling and review, synthetic generation, training compute, evaluation hours and judge tokens, and engineering time. Recurring costs: inference, whether per token or GPU hosting; monitoring and drift checks; and retraining when data drifts or the base model changes. Hosted custom models may be priced differently from base models, so check. | Components<br>• One-off: data, compute, evaluation, engineering<br>• Recurring: inference, monitoring, retraining<br>• Custom-model pricing may differ | Two columns of cost blocks stacking up: one-off on the left, recurring on the right, with a small calendar icon on the recurring side. |
| 4 | 1:29 | 33 | Training compute, two ways. Hosted: training tokens equal examples times average tokens per example times epochs, multiplied by the provider's training price. Six thousand examples of three hundred and fifty tokens for two epochs is four point two million tokens. Self-managed: GPU hours equal training tokens divided by measured tokens per second times thirty-six hundred, times the hourly rate, plus overhead for experiments. Measure throughput with a short trial run, and budget for several runs, not one. | Training compute<br>• Hosted: examples × tokens × epochs × price<br>• Self: tokens ÷ (tokens/s × 3600) × rate<br>• Measure throughput · budget 3–5 runs | Two formula cards with an example calculation filling in; a stopwatch icon next to "measure tokens per second". |
| 5 | 2:02 | 29 | Now inference economics. The usual motivation is replacing an expensive prompted model with a cheaper fine-tuned one. Cost per request is input tokens times input price plus output tokens times output price. Here is a hidden bonus: a fine-tuned model often needs much shorter prompts, because the instructions and examples are baked into the weights. So you save on input tokens as well as the per-token price. | Inference economics<br>• cost/request = in × price_in + out × price_out<br>• Fine-tunes need shorter prompts<br>• Save on tokens and price | Two request bars: a long prompt bar with instructions and examples, and a short prompt bar after fine-tuning; a cost label shrinks accordingly. |
| 6 | 2:31 | 35 | Break-even is simple. Monthly saving equals saving per request times monthly requests, minus any new fixed and recurring costs. Break-even months equal one-off costs divided by that monthly saving. In the lesson's illustrative example, a marketplace classifying three million messages a month moves from about three thousand dollars a month on an API to a nine hundred dollar GPU server plus four hundred for monitoring and retraining. With about eleven thousand eight hundred in one-off costs, break-even is roughly seven months. | Break-even (illustrative)<br>• Saving: $3,000 − $900 − $400 = $1,700/month<br>• One-off: $11,800<br>• Break-even ≈ 7 months | Line chart of cumulative cost for API vs fine-tuned path crossing at around month 7, labeled illustrative. |
| 7 | 3:06 | 40 | Now stress-test, because assumptions are where business cases die. If volume halves, the monthly saving falls to about two hundred dollars and break-even stretches to roughly five years, probably not worth it. If the API price halves instead, you get the same result. If volume doubles and one server still copes, break-even drops to under three months. Also remember discounts: prompt caching and batch pricing can make the current setup cheaper than list prices suggest. The calculator in the lesson loops over volumes so you can see the whole picture in seconds. | Stress tests<br>• Volume halves → ~5 years<br>• API price halves → same<br>• Volume doubles → < 3 months<br>• Include caching and batch discounts | Three scenario lines on the same chart diverging: optimistic, expected, pessimistic, with the pessimistic line never crossing within 24 months. |
| 8 | 3:46 | 27 | Cost is not the only value. A fine-tune can raise accuracy and reduce human escalations, cut latency and lift conversion in live chat, run on devices to enable new products, or keep data in-country to open a market. Put numbers on these where you can, for example, each correctly auto-routed ticket saves three minutes of agent time, times the number of tickets. | Value beyond cost<br>• Fewer escalations<br>• Lower latency<br>• On-device products<br>• Residency and market access | Four value icons orbiting a central model icon, each with a small plus sign and a placeholder number. |
| 9 | 4:13 | 23 | Four pitfalls. Using prices from memory instead of current price sheets. Ignoring engineering and retraining costs. Forgetting discounts on the current API. And not stress-testing volume and price. A good model is one page, cites a source for every price, shows sensitivity to volume and price, and still makes sense under pessimistic assumptions. | Pitfalls<br>• Prices from memory<br>• Missing engineering/retraining<br>• Forgotten discounts<br>• No stress test | Four warning cards with a single-page cost model document in the center marked with a green tick. |
| 10 | 4:36 | 37 | A simple example with round, illustrative numbers. A startup spends one thousand dollars a month on API calls for a classification task. A fine-tuned small model would cost two hundred a month to run, plus one hundred for monitoring. One-off costs, data, training and evaluation, come to three thousand. Monthly saving: seven hundred. Break-even: a little over four months. If volume halves, the saving falls to two hundred, and break-even jumps to fifteen months. That second number is the one to show your finance team. | Simple example (illustrative)<br>• API $1,000/mo → FT $200 + $100<br>• One-off $3,000<br>• Break-even ≈ 4.3 months<br>• Half volume → ≈ 15 months | Calculator panel filling in values; two results appear, one green (4.3 months) and one amber (15 months) beneath a "stress test" label. |
| 11 | 5:13 | 33 | Present the model to decision-makers on one page. At the top, the recommendation and the break-even under expected assumptions. Below, a small table of three scenarios: pessimistic, expected and optimistic, each with volume, prices and break-even months. Then the value beyond cost, like fewer escalations or faster replies, with your estimate and how you measured it. And finally the sources for every price and the date you checked them. Decision-makers trust numbers they can trace. | One-page presentation<br>• Recommendation + expected break-even<br>• Three scenarios table<br>• Value beyond cost<br>• Price sources + date checked | A single slide mock-up with a headline recommendation, a three-row scenario table, a value box and a footnote listing sources with dates. |
| 12 | 5:46 | 30 | A question finance teams ask: what about the cost of people? Include it. Engineering time to build the pipeline, reviewer time for labeling and evaluation, and ongoing time for monitoring and retraining are often larger than the compute bill, especially in the first year. A model that looks cheap on GPU hours can look very different once people's time is included. Honest models include both, and the recommendation still holds. | FAQ: what about people costs?<br>• Include engineering, labeling, evaluation, monitoring<br>• Often bigger than compute in year one | Stacked cost bar where a small compute segment is dwarfed by people segments labeled engineering, labeling and monitoring. |
| 13 | 6:16 | 21 | Try this now. Open your provider's current price page and your usage dashboard, and calculate your real cost per request for one AI task, including input and output tokens. Write the date and the source next to the number. Every cost model starts with one honest, sourced number. | Try this now<br>• Current price page + usage dashboard<br>• Real cost per request for one task<br>• Note date and source | A price sheet and a usage chart side by side; values are pulled into a formula card that outputs cost per request with a small date stamp. |
| 14 | 6:37 | 68 | Watch me do it. I open our usage dashboard for the classification feature: one point eight million requests last month, average nine hundred forty input tokens and six output tokens. I open the provider's current price page and note the date. Cost per request: input tokens times the input price plus output tokens times the output price, and I multiply by volume for the monthly bill. For the fine-tuned option, I run a short training trial on a rented GPU and measure tokens per second, which gives GPU hours for the full run; I multiply by four for experiments. I add labeling hours, evaluation hours and engineering days at our loaded rates, and the monthly server and monitoring costs. I put everything into the calculator and run three volume scenarios. Expected case: break-even in about eight months. Pessimistic, half the volume: never within two years. I write both on the summary slide, with sources and dates. | Watch me do it<br>1 Real volume + tokens<br>2 Current prices, dated<br>3 Measure training throughput<br>4 Add people costs<br>5 Three scenarios → summary slide | Usage dashboard with highlighted averages; a price page snippet with a date stamp; a throughput measurement in a terminal; a spreadsheet filling one-off and recurring rows; three break-even lines, one never crossing. |
| 15 | 7:45 | 21 | Recap. Separate one-off and recurring costs. Estimate training by tokens or measured GPU hours. Remember shorter prompts after fine-tuning. Compute break-even, stress-test it, and count value beyond cost. Your next step: fill in the calculator with your real volumes and current prices, and run pessimistic, expected and optimistic scenarios. | Recap<br>• Costs, break-even, stress tests, value<br>• Next: three scenarios with real prices | Recap card with a calculator and a three-line chart icon. |

<details><summary>Narration per scene (paste into ElevenLabs)</summary>

Scene 1 — Cost modeling

```text
Sooner or later, someone from finance asks two questions: what does this cost, and when does it pay back? If your answer is it depends, the project stalls. In this lesson you will build a cost model for custom models that you can defend: one-off and recurring costs, two ways to estimate training, inference economics, break-even, and stress tests. Every number I use is illustrative; you will plug in your own.
```

Scene 2 — Analogy: van vs courier

```text
Here is an analogy. Deciding whether to fine-tune for cost is like deciding whether to buy a delivery van instead of paying a courier per parcel. The van has an upfront price, insurance and maintenance. The courier charges per parcel. With a few parcels a week, the courier wins. With hundreds a day, the van pays for itself. Break-even is simply the week when the van's total cost drops below the courier bills you would have paid.
```

Scene 3 — Components

```text
Start with the components. One-off costs: data labeling and review, synthetic generation, training compute, evaluation hours and judge tokens, and engineering time. Recurring costs: inference, whether per token or GPU hosting; monitoring and drift checks; and retraining when data drifts or the base model changes. Hosted custom models may be priced differently from base models, so check.
```

Scene 4 — Training compute

```text
Training compute, two ways. Hosted: training tokens equal examples times average tokens per example times epochs, multiplied by the provider's training price. Six thousand examples of three hundred and fifty tokens for two epochs is four point two million tokens. Self-managed: GPU hours equal training tokens divided by measured tokens per second times thirty-six hundred, times the hourly rate, plus overhead for experiments. Measure throughput with a short trial run, and budget for several runs, not one.
```

Scene 5 — Inference economics

```text
Now inference economics. The usual motivation is replacing an expensive prompted model with a cheaper fine-tuned one. Cost per request is input tokens times input price plus output tokens times output price. Here is a hidden bonus: a fine-tuned model often needs much shorter prompts, because the instructions and examples are baked into the weights. So you save on input tokens as well as the per-token price.
```

Scene 6 — Break-even (illustrative)

```text
Break-even is simple. Monthly saving equals saving per request times monthly requests, minus any new fixed and recurring costs. Break-even months equal one-off costs divided by that monthly saving. In the lesson's illustrative example, a marketplace classifying three million messages a month moves from about three thousand dollars a month on an API to a nine hundred dollar GPU server plus four hundred for monitoring and retraining. With about eleven thousand eight hundred in one-off costs, break-even is roughly seven months.
```

Scene 7 — Stress tests

```text
Now stress-test, because assumptions are where business cases die. If volume halves, the monthly saving falls to about two hundred dollars and break-even stretches to roughly five years, probably not worth it. If the API price halves instead, you get the same result. If volume doubles and one server still copes, break-even drops to under three months. Also remember discounts: prompt caching and batch pricing can make the current setup cheaper than list prices suggest. The calculator in the lesson loops over volumes so you can see the whole picture in seconds.
```

Scene 8 — Value beyond cost

```text
Cost is not the only value. A fine-tune can raise accuracy and reduce human escalations, cut latency and lift conversion in live chat, run on devices to enable new products, or keep data in-country to open a market. Put numbers on these where you can, for example, each correctly auto-routed ticket saves three minutes of agent time, times the number of tickets.
```

Scene 9 — Pitfalls

```text
Four pitfalls. Using prices from memory instead of current price sheets. Ignoring engineering and retraining costs. Forgetting discounts on the current API. And not stress-testing volume and price. A good model is one page, cites a source for every price, shows sensitivity to volume and price, and still makes sense under pessimistic assumptions.
```

Scene 10 — Simple example (illustrative)

```text
A simple example with round, illustrative numbers. A startup spends one thousand dollars a month on API calls for a classification task. A fine-tuned small model would cost two hundred a month to run, plus one hundred for monitoring. One-off costs, data, training and evaluation, come to three thousand. Monthly saving: seven hundred. Break-even: a little over four months. If volume halves, the saving falls to two hundred, and break-even jumps to fifteen months. That second number is the one to show your finance team.
```

Scene 11 — One-page presentation

```text
Present the model to decision-makers on one page. At the top, the recommendation and the break-even under expected assumptions. Below, a small table of three scenarios: pessimistic, expected and optimistic, each with volume, prices and break-even months. Then the value beyond cost, like fewer escalations or faster replies, with your estimate and how you measured it. And finally the sources for every price and the date you checked them. Decision-makers trust numbers they can trace.
```

Scene 12 — FAQ: what about people costs?

```text
A question finance teams ask: what about the cost of people? Include it. Engineering time to build the pipeline, reviewer time for labeling and evaluation, and ongoing time for monitoring and retraining are often larger than the compute bill, especially in the first year. A model that looks cheap on GPU hours can look very different once people's time is included. Honest models include both, and the recommendation still holds.
```

Scene 13 — Try this now

```text
Try this now. Open your provider's current price page and your usage dashboard, and calculate your real cost per request for one AI task, including input and output tokens. Write the date and the source next to the number. Every cost model starts with one honest, sourced number.
```

Scene 14 — Watch me do it

```text
Watch me do it. I open our usage dashboard for the classification feature: one point eight million requests last month, average nine hundred forty input tokens and six output tokens. I open the provider's current price page and note the date. Cost per request: input tokens times the input price plus output tokens times the output price, and I multiply by volume for the monthly bill. For the fine-tuned option, I run a short training trial on a rented GPU and measure tokens per second, which gives GPU hours for the full run; I multiply by four for experiments. I add labeling hours, evaluation hours and engineering days at our loaded rates, and the monthly server and monitoring costs. I put everything into the calculator and run three volume scenarios. Expected case: break-even in about eight months. Pessimistic, half the volume: never within two years. I write both on the summary slide, with sources and dates.
```

Scene 15 — Recap

```text
Recap. Separate one-off and recurring costs. Estimate training by tokens or measured GPU hours. Remember shorter prompts after fine-tuning. Compute break-even, stress-test it, and count value beyond cost. Your next step: fill in the calculator with your real volumes and current prices, and run pessimistic, expected and optimistic scenarios.
```

</details>

### 15. Deploying custom models and monitoring for drift

Lesson `deployment-monitoring-and-drift` · module “Deploy, monitor and capstone” · target 8 min · 15 scenes · 1147 words · planned 8:18 (words ≈ 8:19) · status: **to produce**

| Term | Say |
|---|---|
| LoRA | LOR-ah |
| PSI | P S I |
| MLflow | M L flow |
| Eid | eed |

| # | Time | Sec | Narration | On screen | Visual |
|---|---|---|---|---|---|
| 1 | 0:00 | 27 | Launch day is exciting. Month four is where custom models quietly fail. Customers start writing differently, a new product launches, a festive season changes everything, and nobody notices until complaints pile up. In this lesson you will learn how to deploy custom models, version and release them safely, monitor for drift and quality, and decide when to retrain, before your users tell you. | Deploy and monitor<br>• Deployment options<br>• Versioning and release<br>• Drift, quality, retraining | A launch confetti moment, then a calendar flipping to month four with a slowly sagging quality line nobody is watching. |
| 2 | 0:27 | 30 | Here is an analogy. A fine-tuned model in production is like a new employee who was trained on last year's customers. At first they are excellent. But customers change, products change, and seasons change. A good manager checks in every week, reviews a sample of their work, notices new kinds of requests, and arranges refresher training when needed. Monitoring is that weekly check-in, and retraining is the refresher course. | Analogy: a new employee<br>• Trained on last year's customers<br>• Weekly check-ins = monitoring<br>• Refresher course = retraining | Calendar with weekly check-in markers; a performance line dips when a new product icon appears, then recovers after a "refresher" marker. |
| 3 | 0:57 | 36 | Four ways to deploy. Multi-LoRA serving puts one base model on a GPU and loads many small adapters, perfect for many tasks or clients. Merged and quantized models fold the adapter into the base and compress it, simplest for a single task or edge devices. Ollama can load a base model plus an adapter from a Modelfile for local use. And hosted endpoints serve closed-model fine-tunes. Whatever you pick, serving must use the same chat template, system prompt and preprocessing as training. | Deployment options<br>• Multi-LoRA on a shared base<br>• Merged + quantized<br>• Ollama FROM + ADAPTER<br>• Hosted custom endpoint<br>• Same template and prompt as training | Four deployment tiles; the multi-LoRA tile shows several coloured adapters plugging into one base model on a GPU. |
| 4 | 1:33 | 33 | Version everything. A model version combines the base model revision, the adapter hash and the data version. Store models in a registry, with a model card: base and license, data card, evaluation report, intended use, limitations and owner. Release using champion and challenger: the current model keeps serving while the new one runs in shadow or on a small share of traffic. And always keep one-click rollback, so the previous version is a configuration change away. | Versioning and release<br>• Version = base rev + adapter hash + data version<br>• Registry + model card<br>• Champion/challenger<br>• One-click rollback | Registry list with version tags; a champion badge on one row and a challenger badge on another, with a rollback arrow. |
| 5 | 2:06 | 31 | What do you monitor? Four things. Input drift: language and channel mix, message length, new product names, and shifts in the distribution of embeddings. Output drift: the label distribution, format validity, length and refusal rates. Quality: a weekly human review of a random sample, implicit feedback like agent re-labels and edits, and scheduled re-runs of your test set plus a fresh labeled sample. And operations: latency, errors, cost and GPU utilization. | Monitor four things<br>• Input drift<br>• Output drift<br>• Quality (sampled review, feedback, re-tests)<br>• Operations | Four-panel monitoring dashboard mock-up with small charts, one panel showing a spike highlighted in amber. |
| 6 | 2:37 | 35 | A neat trick for input drift: embed a daily sample of messages and compare it with your training data. You can compare the centers of the two clouds, or train a quick classifier to tell training from today. If that classifier succeeds easily, your inputs have drifted. For categories like language or label, the population stability index is a simple number to track, and values above about point two are a common signal to investigate. Calibrate thresholds on your own history. | Detecting drift<br>• Embedding clouds: train vs today<br>• "Can a classifier tell them apart?"<br>• PSI per category (> 0.2: investigate)<br>• Calibrate thresholds | Two point clouds in 2D, one for training and one for today, drifting apart; a PSI gauge beside them moves into amber. |
| 7 | 3:12 | 29 | The lesson's drift report is a short Python script. It loads metadata for training examples and this week's production traffic: language, channel, length and label. It computes the population stability index for language, channel and predicted label, the share of the other label, and median lengths, then prints alerts for anything above threshold. Run it weekly and put the output where the team will see it. | Hands-on: drift_report.py<br>• PSI for language, channel, label<br>• Share of "other"<br>• Median length<br>• Weekly alerts | Terminal output showing a JSON report with one PSI value above 0.2 highlighted and an ALERTS line listing it. |
| 8 | 3:41 | 33 | When should you retrain? Decide the triggers in advance. Quality on the weekly sample falls below the bar. Drift alerts persist for two weeks. A new intent, product or language appears with real volume. The base model is scheduled for deprecation, or a better base passes your evaluation. Or simply a scheduled cadence, such as quarterly. Retraining reuses the pipeline, adds new labeled data especially from failures, and goes through the same evaluation rule and champion-challenger release. | Retraining triggers<br>• Quality below bar<br>• Persistent drift<br>• New intent/product/language<br>• Base deprecation or better base<br>• Scheduled cadence | A trigger panel with five switches; flipping any one starts a circular pipeline: data, train, evaluate, shadow, release. |
| 9 | 4:14 | 33 | A worked example. A Jeddah retailer's intent classifier ran well for months. In the first week of Ramadan, the share of other jumped, and traffic shifted to late-night WhatsApp messages about delivery slots and Eid gift wrapping. The drift report flagged label and channel shifts, and the weekly review confirmed a new intent. They labeled seven hundred examples, retrained, passed shadow mode in three days and released. Now they refresh data before Ramadan and major sales seasons. | Worked example: Jeddah retailer<br>• Ramadan: "other" spikes, channel shifts<br>• Drift report + weekly review confirm<br>• 700 new examples → retrain → shadow 3 days<br>• Pre-season refresh now scheduled | Crescent moon icon over a timeline; a spike in the "other" label chart, then a quick retrain loop and a stable line afterwards. |
| 10 | 4:47 | 32 | A simple example. A telecom's classifier routes messages into ten intents. One Monday, the share of other doubles. The weekly review shows many messages about a new eSIM offer that did not exist when the model was trained. They label two hundred eSIM messages, add a new intent, retrain, run the evaluation rule, shadow test for two days and release. Without the drift report, those customers would have bounced between teams for weeks. | Simple example: new eSIM offer<br>• "other" share doubles<br>• Review: new eSIM intent<br>• Label 200 → retrain → evaluate → shadow → release | Label distribution bar chart with "other" spiking; a zoom reveals message cards mentioning eSIM; a pipeline loop animates to a new intent bar appearing. |
| 11 | 5:19 | 28 | Who owns all of this? Name a model owner for every production model: someone who reads the weekly report, approves retraining, signs off releases and decides on rollbacks. Pair them with an on-call engineer for operational incidents. Write both names on the model card. Monitoring that nobody reads is theater; a named owner with a weekly thirty-minute review is what actually keeps quality up. | Ownership<br>• Named model owner<br>• On-call engineer for incidents<br>• Names on the model card<br>• 30-minute weekly review | Model card with two name fields filled in, and a recurring calendar event labeled weekly model review. |
| 12 | 5:47 | 35 | A question from operations teams: how often should we retrain? There is no fixed answer, which is why triggers matter more than calendars. Some models run well for a year; others need updates every few weeks during seasonal peaks. Start with a quarterly review as a safety net, and let your drift reports and weekly quality samples tell you when to act sooner. Track how often triggers fire, and adjust your data collection so retraining is quick when it is needed. | FAQ: how often to retrain?<br>• Triggers over calendars<br>• Quarterly review as a safety net<br>• Drift + weekly samples decide sooner | Calendar with a quarterly marker and several trigger lightning icons at irregular points; each trigger links to a retrain loop. |
| 13 | 6:22 | 25 | Try this now. For one model you run or plan, write down the three numbers you would look at every Monday morning, for example the share of the other label, the weekly sample accuracy, and p ninety-five latency. Then write the threshold for each that would make you act. That is your monitoring plan in three lines. | Try this now<br>• 3 Monday-morning numbers<br>• A threshold for each<br>• = your monitoring plan | Three dashboard tiles appear with a number and a dashed threshold line each; one tile crosses its threshold and turns amber. |
| 14 | 6:47 | 65 | Watch me do it. It is Monday morning. I run the drift report on last week's traffic. Language PSI is low. Channel PSI is point one five, fine. Label PSI is point two eight, above threshold, and the share of other has doubled. I open fifty messages labeled other and read them: thirty mention a new instalment payment option launched last Tuesday. That is a new intent. I check the weekly quality sample: accuracy on known intents is stable, so the model itself is fine; the world changed. I label two hundred instalment messages, add the intent, retrain with the same config, and run the evaluation rule: all conditions pass. I deploy the new adapter as challenger in shadow for three days, compare with the champion, then promote it. The old adapter stays in the registry for rollback. I update the model card and add instalments to the monitoring labels. | Watch me do it<br>1 Monday drift report<br>2 Label PSI 0.28 → read "other"<br>3 New intent found<br>4 Label, retrain, evaluate<br>5 Shadow → promote, keep rollback | Terminal drift report with one PSI value highlighted; a message browser grouping 30 similar messages; a retrain pipeline animation; champion and challenger boxes in shadow; a registry list showing both versions. |
| 15 | 7:52 | 26 | Recap. Choose a deployment option that matches your tasks and runtime. Version base, adapter and data together, keep model cards, release champion versus challenger, and keep rollback one click away. Monitor input drift, output drift, quality and operations, and write retraining triggers in advance. Your next step: build a weekly drift report, define three triggers, and document your rollback procedure. | Recap<br>• Deploy, version, monitor, retrain<br>• Next: drift report + triggers + rollback doc | Recap card with a cycle diagram of deploy, monitor and retrain. |

<details><summary>Narration per scene (paste into ElevenLabs)</summary>

Scene 1 — Deploy and monitor

```text
Launch day is exciting. Month four is where custom models quietly fail. Customers start writing differently, a new product launches, a festive season changes everything, and nobody notices until complaints pile up. In this lesson you will learn how to deploy custom models, version and release them safely, monitor for drift and quality, and decide when to retrain, before your users tell you.
```

Scene 2 — Analogy: a new employee

```text
Here is an analogy. A fine-tuned model in production is like a new employee who was trained on last year's customers. At first they are excellent. But customers change, products change, and seasons change. A good manager checks in every week, reviews a sample of their work, notices new kinds of requests, and arranges refresher training when needed. Monitoring is that weekly check-in, and retraining is the refresher course.
```

Scene 3 — Deployment options

```text
Four ways to deploy. Multi-LoRA serving puts one base model on a GPU and loads many small adapters, perfect for many tasks or clients. Merged and quantized models fold the adapter into the base and compress it, simplest for a single task or edge devices. Ollama can load a base model plus an adapter from a Modelfile for local use. And hosted endpoints serve closed-model fine-tunes. Whatever you pick, serving must use the same chat template, system prompt and preprocessing as training.
```

Scene 4 — Versioning and release

```text
Version everything. A model version combines the base model revision, the adapter hash and the data version. Store models in a registry, with a model card: base and license, data card, evaluation report, intended use, limitations and owner. Release using champion and challenger: the current model keeps serving while the new one runs in shadow or on a small share of traffic. And always keep one-click rollback, so the previous version is a configuration change away.
```

Scene 5 — Monitor four things

```text
What do you monitor? Four things. Input drift: language and channel mix, message length, new product names, and shifts in the distribution of embeddings. Output drift: the label distribution, format validity, length and refusal rates. Quality: a weekly human review of a random sample, implicit feedback like agent re-labels and edits, and scheduled re-runs of your test set plus a fresh labeled sample. And operations: latency, errors, cost and GPU utilization.
```

Scene 6 — Detecting drift

```text
A neat trick for input drift: embed a daily sample of messages and compare it with your training data. You can compare the centers of the two clouds, or train a quick classifier to tell training from today. If that classifier succeeds easily, your inputs have drifted. For categories like language or label, the population stability index is a simple number to track, and values above about point two are a common signal to investigate. Calibrate thresholds on your own history.
```

Scene 7 — Hands-on: drift_report.py

```text
The lesson's drift report is a short Python script. It loads metadata for training examples and this week's production traffic: language, channel, length and label. It computes the population stability index for language, channel and predicted label, the share of the other label, and median lengths, then prints alerts for anything above threshold. Run it weekly and put the output where the team will see it.
```

Scene 8 — Retraining triggers

```text
When should you retrain? Decide the triggers in advance. Quality on the weekly sample falls below the bar. Drift alerts persist for two weeks. A new intent, product or language appears with real volume. The base model is scheduled for deprecation, or a better base passes your evaluation. Or simply a scheduled cadence, such as quarterly. Retraining reuses the pipeline, adds new labeled data especially from failures, and goes through the same evaluation rule and champion-challenger release.
```

Scene 9 — Worked example: Jeddah retailer

```text
A worked example. A Jeddah retailer's intent classifier ran well for months. In the first week of Ramadan, the share of other jumped, and traffic shifted to late-night WhatsApp messages about delivery slots and Eid gift wrapping. The drift report flagged label and channel shifts, and the weekly review confirmed a new intent. They labeled seven hundred examples, retrained, passed shadow mode in three days and released. Now they refresh data before Ramadan and major sales seasons.
```

Scene 10 — Simple example: new eSIM offer

```text
A simple example. A telecom's classifier routes messages into ten intents. One Monday, the share of other doubles. The weekly review shows many messages about a new eSIM offer that did not exist when the model was trained. They label two hundred eSIM messages, add a new intent, retrain, run the evaluation rule, shadow test for two days and release. Without the drift report, those customers would have bounced between teams for weeks.
```

Scene 11 — Ownership

```text
Who owns all of this? Name a model owner for every production model: someone who reads the weekly report, approves retraining, signs off releases and decides on rollbacks. Pair them with an on-call engineer for operational incidents. Write both names on the model card. Monitoring that nobody reads is theater; a named owner with a weekly thirty-minute review is what actually keeps quality up.
```

Scene 12 — FAQ: how often to retrain?

```text
A question from operations teams: how often should we retrain? There is no fixed answer, which is why triggers matter more than calendars. Some models run well for a year; others need updates every few weeks during seasonal peaks. Start with a quarterly review as a safety net, and let your drift reports and weekly quality samples tell you when to act sooner. Track how often triggers fire, and adjust your data collection so retraining is quick when it is needed.
```

Scene 13 — Try this now

```text
Try this now. For one model you run or plan, write down the three numbers you would look at every Monday morning, for example the share of the other label, the weekly sample accuracy, and p ninety-five latency. Then write the threshold for each that would make you act. That is your monitoring plan in three lines.
```

Scene 14 — Watch me do it

```text
Watch me do it. It is Monday morning. I run the drift report on last week's traffic. Language PSI is low. Channel PSI is point one five, fine. Label PSI is point two eight, above threshold, and the share of other has doubled. I open fifty messages labeled other and read them: thirty mention a new instalment payment option launched last Tuesday. That is a new intent. I check the weekly quality sample: accuracy on known intents is stable, so the model itself is fine; the world changed. I label two hundred instalment messages, add the intent, retrain with the same config, and run the evaluation rule: all conditions pass. I deploy the new adapter as challenger in shadow for three days, compare with the champion, then promote it. The old adapter stays in the registry for rollback. I update the model card and add instalments to the monitoring labels.
```

Scene 15 — Recap

```text
Recap. Choose a deployment option that matches your tasks and runtime. Version base, adapter and data together, keep model cards, release champion versus challenger, and keep rollback one click away. Monitor input drift, output drift, quality and operations, and write retraining triggers in advance. Your next step: build a weekly drift report, define three triggers, and document your rollback procedure.
```

</details>

### 16. Capstone: fine-tune a small model for support-intent routing

Lesson `capstone-support-intent-router` · module “Deploy, monitor and capstone” · target 8 min · 15 scenes · 1135 words · planned 8:13 (words ≈ 8:13) · status: **to produce**

| Term | Say |
|---|---|
| QLoRA | Q LOR-ah |
| macro-F1 | macro F one |
| GRPO | G R P O |
| vLLM | V L L M |

| # | Time | Sec | Narration | On screen | Visual |
|---|---|---|---|---|---|
| 1 | 0:00 | 30 | This is where everything comes together. You are going to fine-tune a small open-weight model to route customer support messages to the right intent, fast and cheaply enough to run on every message, and prove with evidence that it beats your best non-fine-tuned setup. Uncertain messages will escalate to a stronger model or a human. If brand voice matters more to you, the same steps build a brand-voice classifier instead. | Capstone: support-intent router<br>• Small fine-tuned model<br>• Proven against your best baseline<br>• Escalation when unsure | Stream of incoming messages in several scripts flowing into a small model icon that sorts them into labeled lanes; a few uncertain ones divert to a larger model and a human icon. |
| 2 | 0:30 | 33 | Here is how to picture the router. It is a hospital reception desk. A trained receptionist handles most visitors quickly and correctly: that is your small fine-tuned model. When the receptionist is unsure, or the case is unusual, they call a senior nurse: that is the stronger model or a human. The receptionist does not need to know everything. They need to be fast, accurate on common cases, and honest about when to ask for help. | Analogy: a reception desk<br>• Receptionist = small fine-tuned model<br>• Senior nurse = stronger model or human<br>• Fast, accurate, honest about uncertainty | Reception desk illustration: most visitor icons are directed quickly to doors; an uncertain one triggers a call button and a senior figure appears. |
| 3 | 1:03 | 34 | Step one, frame it. Write the problem statement, the list of intents with definitions, and your ladder baseline: your best prompt and model, scored on a draft test set. Fill in the fine-tuning worksheet. Step two, data. A labeling guide with priority rules, two-annotator agreement on a hundred messages, two to six thousand examples covering intents, languages and channels, PII redaction, careful synthetic top-ups, decontamination, and a split by customer or time, all documented in a data card. | Steps 1–2<br>• Frame: intents, baseline, worksheet<br>• Data: guide, agreement, coverage<br>• Redact, top up, decontaminate, split<br>• Data card | Checklist sliding in with two grouped sections; a coverage matrix heat map fills in. |
| 4 | 1:37 | 28 | Step three, train. Use LoRA or QLoRA supervised fine-tuning on a one to four billion parameter instruct model, with prompt and completion data. Track every run and choose the checkpoint by validation macro F1. Step four is optional: if one class is systematically confused and you can check it, try GRPO with a per-label grader, and keep it only if your independent test improves. | Steps 3–4<br>• LoRA/QLoRA SFT, 1–4B instruct model<br>• Track runs, pick by validation macro-F1<br>• Optional GRPO for a checkable confusion | Training dashboard with a macro-F1 line per checkpoint and a star on the best one; an optional side branch labeled GRPO. |
| 5 | 2:05 | 30 | Step five, evaluate. Write your decision rule first. Compare baseline and fine-tune on the test set by slice, with bootstrap confidence intervals. Measure latency and cost per thousand messages. And run a small challenge set: ambiguous messages, messages with two intents, prompt injection attempts and out-of-scope requests. Step six, deploy and monitor: serve with multi-LoRA or a merged, quantized model, add confidence routing, a drift report, retraining triggers and rollback. | Steps 5–6<br>• Decision rule → slices + intervals<br>• Latency, cost, challenge set<br>• Deploy + routing + drift + triggers + rollback | Evaluation report page building up, followed by a deployment diagram with a monitoring panel attached. |
| 6 | 2:35 | 35 | Here is how the router works. The small fine-tuned model classifies each message twice, once at temperature zero and once with a little randomness, and a JSON schema forces a valid label. If both agree and the label is not other, the small model's answer stands. Otherwise the message goes to a stronger model, or to a human queue if no stronger route is allowed. If data must stay inside your boundary, make sure the stronger route is internal too. | Confidence routing<br>• Two samples + schema-forced label<br>• Agree and not "other" → small model<br>• Else → stronger model or human<br>• Keep sensitive routes internal | Flowchart: message → two small-model votes → agreement check → either a green "done" or an escalation arrow to a larger model and a human icon. |
| 7 | 3:10 | 24 | Measure the router, not just the model. What share of traffic does the small model handle alone, and how accurate is it on that share? What share escalates, and what does that cost? Tune the rule until the escalated share and overall accuracy meet your targets. This is where the business case is won or lost. | Measure the router<br>• Share handled alone + accuracy on it<br>• Escalation share + cost<br>• Tune to targets | Donut chart splitting traffic into small-model, large-model and human slices, with accuracy labels on each. |
| 8 | 3:34 | 29 | Seven deliverables. The problem statement and baseline worksheet. The data card. The training record: config, versions, base model revision, data hash, curves and chosen checkpoint. The evaluation report with decision rule, slices, intervals, challenge results, routing share, latency, cost and the ship decision. The cost model with three scenarios. The deployment and monitoring plan. And a model card with intended use, limitations, languages, owner and base-model license. | Seven deliverables<br>1 Problem + baseline<br>2 Data card<br>3 Training record<br>4 Evaluation report<br>5 Cost model<br>6 Deploy + monitor plan<br>7 Model card | Seven document icons arranging into a neat folder labeled capstone. |
| 9 | 4:03 | 37 | What does strong look like? An illustrative example. A Lahore e-commerce enabler routes WhatsApp and email messages for forty merchants. Baseline: macro F1 of point eight four. Fine-tuned three billion model: point eight eight, with Roman Urdu at point eight six after adding seven hundred examples to thin cells. With agreement routing, the small model handles eighty-two percent of traffic alone at ninety-three percent accuracy, and cost per thousand messages falls sharply. The report also names two failure patterns and the plan to fix them. | Strong submission (illustrative)<br>• Baseline 0.84 → fine-tune 0.88<br>• Roman Urdu 0.86 after targeted data<br>• 82% handled by small model at 93%<br>• Named failure patterns + plan | Results slide mock-up with three headline numbers and a short "known failures" box. |
| 10 | 4:40 | 37 | A simple dry run before the full project. Take four intents and a hundred labeled messages. Use your best prompt with a strong model as the baseline and record its accuracy. Fine-tune a tiny model on seventy messages, test on the other thirty, and implement the two-sample agreement rule. Count how many of the thirty the small model handled alone and how many it got right. That mini version takes a day and exposes most of the practical problems you will face at full scale. | Dry run in a day<br>• 4 intents, 100 messages<br>• Baseline with best prompt<br>• Tiny fine-tune on 70, test on 30<br>• Count handled-alone + accuracy | Mini pipeline diagram with small numbers: 100 messages split 70/30, a baseline card, a tiny model card, and a result donut chart for the 30 test messages. |
| 11 | 5:17 | 32 | A few tips for a smooth capstone. Start with the evaluation, not the training: build the test set and the baseline score in week one. Keep the first model small and the first dataset modest, so you can iterate quickly. Save every run with its config and data hash. And write the report as you go, not at the end. Most strong submissions spend more time on data and evaluation than on training itself. | Capstone tips<br>• Evaluation first, in week one<br>• Small model, modest data, fast iterations<br>• Save every run<br>• Write the report as you go | Four-week plan timeline: week one evaluation, week two data, week three training iterations, week four report and deployment plan. |
| 12 | 5:49 | 32 | A capstone question: what if the fine-tuned model does not beat the baseline? Then your report says so, clearly, and that is a successful capstone. You will have learned where the gap is, maybe data coverage, maybe label quality, maybe the task needs a stronger model. Write down what you would try next and what evidence would change the decision. An honest no-ship decision, backed by evaluation, is exactly the skill this course teaches. | FAQ: what if we don't beat the baseline?<br>• Report it clearly: still a success<br>• Diagnose the gap<br>• Next steps + what would change the decision | An evaluation report with a "no-ship" stamp, followed by a short next-steps list and a green tick labeled "honest decision". |
| 13 | 6:21 | 23 | Try this now. Write your intent list with a one-line definition for each, plus the priority rule for the most common ambiguity. Share it with two people who handle these messages today and ask them to label the same twenty messages. Their agreement score is the first number in your capstone report. | Try this now<br>• Intent list + one-line definitions<br>• Priority rule for the top ambiguity<br>• 2 people label the same 20 messages | Intent list document types in; two labeler avatars label the same 20 message cards and an agreement percentage appears between them. |
| 14 | 6:44 | 66 | Watch me do it, in miniature. I take six intents and six hundred labeled messages. Baseline first: our best prompt on a strong model scores macro F1 of point eight three on a held-out set of one hundred and twenty. Then I fine-tune a small instruct model with LoRA on four hundred and eighty messages; it scores point eight six. I serve it with vLLM and run the router script on the held-out set. Two samples agree and the label is not other for ninety-eight messages; the small model is right on ninety-four of those. The other twenty-two escalate to the strong model, which gets nineteen right. Blended accuracy is higher than either alone, and the cost per thousand messages drops sharply because most never touch the strong model. I write these numbers, share handled, accuracy on that share and blended cost, straight into the evaluation report. Illustrative numbers, real workflow. | Watch me do it (illustrative)<br>1 Baseline 0.83<br>2 LoRA model 0.86<br>3 Router: 98 handled, 94 correct<br>4 22 escalated, 19 correct<br>5 Report share, accuracy, cost | Mini pipeline animation from dataset split to baseline card, training card and router; a donut chart for handled vs escalated with accuracy labels; a cost-per-1,000 bar comparison; the report table filling in. |
| 15 | 7:50 | 23 | Congratulations. You can now decide when fine-tuning is worth it, build and protect datasets, train with SFT, LoRA and QLoRA, apply DPO and GRPO, evaluate honestly, choose hosted or self-managed paths, fine-tune embeddings, model costs, and run custom models in production. Your next step is the capstone itself. Then take the final exam. | Course recap<br>• Decide · data · train · prefer · reinforce<br>• Evaluate · host · embed · cost · operate<br>• Next: build the capstone, then the exam | Journey map retracing the six modules as milestones ending at a flag labeled capstone. |

<details><summary>Narration per scene (paste into ElevenLabs)</summary>

Scene 1 — Capstone: support-intent router

```text
This is where everything comes together. You are going to fine-tune a small open-weight model to route customer support messages to the right intent, fast and cheaply enough to run on every message, and prove with evidence that it beats your best non-fine-tuned setup. Uncertain messages will escalate to a stronger model or a human. If brand voice matters more to you, the same steps build a brand-voice classifier instead.
```

Scene 2 — Analogy: a reception desk

```text
Here is how to picture the router. It is a hospital reception desk. A trained receptionist handles most visitors quickly and correctly: that is your small fine-tuned model. When the receptionist is unsure, or the case is unusual, they call a senior nurse: that is the stronger model or a human. The receptionist does not need to know everything. They need to be fast, accurate on common cases, and honest about when to ask for help.
```

Scene 3 — Steps 1–2

```text
Step one, frame it. Write the problem statement, the list of intents with definitions, and your ladder baseline: your best prompt and model, scored on a draft test set. Fill in the fine-tuning worksheet. Step two, data. A labeling guide with priority rules, two-annotator agreement on a hundred messages, two to six thousand examples covering intents, languages and channels, PII redaction, careful synthetic top-ups, decontamination, and a split by customer or time, all documented in a data card.
```

Scene 4 — Steps 3–4

```text
Step three, train. Use LoRA or QLoRA supervised fine-tuning on a one to four billion parameter instruct model, with prompt and completion data. Track every run and choose the checkpoint by validation macro F1. Step four is optional: if one class is systematically confused and you can check it, try GRPO with a per-label grader, and keep it only if your independent test improves.
```

Scene 5 — Steps 5–6

```text
Step five, evaluate. Write your decision rule first. Compare baseline and fine-tune on the test set by slice, with bootstrap confidence intervals. Measure latency and cost per thousand messages. And run a small challenge set: ambiguous messages, messages with two intents, prompt injection attempts and out-of-scope requests. Step six, deploy and monitor: serve with multi-LoRA or a merged, quantized model, add confidence routing, a drift report, retraining triggers and rollback.
```

Scene 6 — Confidence routing

```text
Here is how the router works. The small fine-tuned model classifies each message twice, once at temperature zero and once with a little randomness, and a JSON schema forces a valid label. If both agree and the label is not other, the small model's answer stands. Otherwise the message goes to a stronger model, or to a human queue if no stronger route is allowed. If data must stay inside your boundary, make sure the stronger route is internal too.
```

Scene 7 — Measure the router

```text
Measure the router, not just the model. What share of traffic does the small model handle alone, and how accurate is it on that share? What share escalates, and what does that cost? Tune the rule until the escalated share and overall accuracy meet your targets. This is where the business case is won or lost.
```

Scene 8 — Seven deliverables

```text
Seven deliverables. The problem statement and baseline worksheet. The data card. The training record: config, versions, base model revision, data hash, curves and chosen checkpoint. The evaluation report with decision rule, slices, intervals, challenge results, routing share, latency, cost and the ship decision. The cost model with three scenarios. The deployment and monitoring plan. And a model card with intended use, limitations, languages, owner and base-model license.
```

Scene 9 — Strong submission (illustrative)

```text
What does strong look like? An illustrative example. A Lahore e-commerce enabler routes WhatsApp and email messages for forty merchants. Baseline: macro F1 of point eight four. Fine-tuned three billion model: point eight eight, with Roman Urdu at point eight six after adding seven hundred examples to thin cells. With agreement routing, the small model handles eighty-two percent of traffic alone at ninety-three percent accuracy, and cost per thousand messages falls sharply. The report also names two failure patterns and the plan to fix them.
```

Scene 10 — Dry run in a day

```text
A simple dry run before the full project. Take four intents and a hundred labeled messages. Use your best prompt with a strong model as the baseline and record its accuracy. Fine-tune a tiny model on seventy messages, test on the other thirty, and implement the two-sample agreement rule. Count how many of the thirty the small model handled alone and how many it got right. That mini version takes a day and exposes most of the practical problems you will face at full scale.
```

Scene 11 — Capstone tips

```text
A few tips for a smooth capstone. Start with the evaluation, not the training: build the test set and the baseline score in week one. Keep the first model small and the first dataset modest, so you can iterate quickly. Save every run with its config and data hash. And write the report as you go, not at the end. Most strong submissions spend more time on data and evaluation than on training itself.
```

Scene 12 — FAQ: what if we don't beat the baseline?

```text
A capstone question: what if the fine-tuned model does not beat the baseline? Then your report says so, clearly, and that is a successful capstone. You will have learned where the gap is, maybe data coverage, maybe label quality, maybe the task needs a stronger model. Write down what you would try next and what evidence would change the decision. An honest no-ship decision, backed by evaluation, is exactly the skill this course teaches.
```

Scene 13 — Try this now

```text
Try this now. Write your intent list with a one-line definition for each, plus the priority rule for the most common ambiguity. Share it with two people who handle these messages today and ask them to label the same twenty messages. Their agreement score is the first number in your capstone report.
```

Scene 14 — Watch me do it (illustrative)

```text
Watch me do it, in miniature. I take six intents and six hundred labeled messages. Baseline first: our best prompt on a strong model scores macro F1 of point eight three on a held-out set of one hundred and twenty. Then I fine-tune a small instruct model with LoRA on four hundred and eighty messages; it scores point eight six. I serve it with vLLM and run the router script on the held-out set. Two samples agree and the label is not other for ninety-eight messages; the small model is right on ninety-four of those. The other twenty-two escalate to the strong model, which gets nineteen right. Blended accuracy is higher than either alone, and the cost per thousand messages drops sharply because most never touch the strong model. I write these numbers, share handled, accuracy on that share and blended cost, straight into the evaluation report. Illustrative numbers, real workflow.
```

Scene 15 — Course recap

```text
Congratulations. You can now decide when fine-tuning is worth it, build and protect datasets, train with SFT, LoRA and QLoRA, apply DPO and GRPO, evaluate honestly, choose hosted or self-managed paths, fine-tune embeddings, model costs, and run custom models in production. Your next step is the capstone itself. Then take the final exam.
```

</details>
