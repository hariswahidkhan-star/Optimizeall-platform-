# Technical SEO Mastery — lecture production script

Course `technical-seo-mastery` · pack v2 · reviewed 2026-09 · level advanced · category seo

18 lectures · 17,370 narration words · planned runtime 2:05:45 (≈ 124 min at 140 wpm)

> Generated by `scripts/export-lecture-scripts.py` from the course pack — edit the pack, not this file. Production workflow: [README](README.md).

## Voice direction

Conversational expert, second person, ~140 wpm; clear and methodical, explain jargon the first time; audience level: advanced. Pause ~0.5 s between scenes; no music under narration (added in edit).

## Lectures

1. [The crawl, render and index pipeline](#1-the-crawl-render-and-index-pipeline)
2. [JavaScript SEO: SPAs, SSR and prerendering](#2-javascript-seo-spas-ssr-and-prerendering)
3. [Crawl budget and log-file analysis](#3-crawl-budget-and-log-file-analysis)
4. [robots.txt, meta robots and X-Robots-Tag](#4-robots-txt-meta-robots-and-x-robots-tag)
5. [Beyond Google: Bing Webmaster Tools, IndexNow and AI-era crawling](#5-beyond-google-bing-webmaster-tools-indexnow-and-ai-era-crawling)
6. [Status codes, redirects and soft 404s](#6-status-codes-redirects-and-soft-404s)
7. [Canonicalisation and duplicate content](#7-canonicalisation-and-duplicate-content)
8. [Pagination and faceted navigation](#8-pagination-and-faceted-navigation)
9. [Site architecture and internal linking](#9-site-architecture-and-internal-linking)
10. [XML sitemaps done properly](#10-xml-sitemaps-done-properly)
11. [hreflang and international SEO](#11-hreflang-and-international-seo)
12. [Structured data, JSON-LD and rich results](#12-structured-data-json-ld-and-rich-results)
13. [Core Web Vitals: diagnosis and fixes](#13-core-web-vitals-diagnosis-and-fixes)
14. [INP deep dive: long tasks, attribution and real-user monitoring](#14-inp-deep-dive-long-tasks-attribution-and-real-user-monitoring)
15. [Mobile-first indexing, HTTPS and security headers](#15-mobile-first-indexing-https-and-security-headers)
16. [Site migrations: a zero-drama checklist](#16-site-migrations-a-zero-drama-checklist)
17. [Auditing workflow, tools and Search Console monitoring](#17-auditing-workflow-tools-and-search-console-monitoring)
18. [Search Console API and BigQuery: automating performance and indexing data](#18-search-console-api-and-bigquery-automating-performance-and-indexing-data)

### 1. The crawl, render and index pipeline

Lesson `the-crawl-render-index-pipeline` · module “How search engines crawl, render and index” · target 7 min · 12 scenes · 1019 words · planned 7:23 (words ≈ 7:23) · status: **to produce**

| Term | Say |
|---|---|
| WRS | W R S |
| IndexNow | index now |
| Chromium | chromium |
| Headingley | heading-lee |

| # | Time | Sec | Narration | On screen | Visual |
|---|---|---|---|---|---|
| 1 | 0:00 | 34 | When a page doesn't rank, most people ask, what keywords are we missing? An expert asks a different question first. Which stage did this URL fail at? Every technical SEO problem is a failure somewhere in a pipeline: discovery, crawling, rendering, indexing and serving. In this lecture you'll learn that pipeline properly, using Google's documented behaviour, and you'll leave with a diagnostic routine, and a small Python script, that tells you exactly where a URL is getting stuck. | The crawl, render and index pipeline<br>• Five stages every URL passes through<br>• Where URLs get stuck<br>• A diagnostic routine you can run today | Five connected stage boxes animate left to right: Discovery, Crawling, Rendering, Indexing, Serving. A single URL token travels along and stops at 'Rendering' with a red marker. Title appears. |
| 2 | 0:34 | 37 | Think of it like a parcel being delivered. First, the courier has to know the address exists. That's discovery. Then they have to be allowed through the gate and pick it up. That's crawling. Then they unpack it and assemble it. That's rendering. Then someone decides whether it's worth putting on the shelf. That's indexing. And finally, a customer has to choose it from the shelf. That's serving, or ranking. If the parcel never left the depot, arguing about the shelf display is pointless. | The parcel analogy<br>• Discovery: the address exists<br>• Crawl: collected<br>• Render: assembled<br>• Index: shelved<br>• Serve: chosen | A parcel icon moves through five illustrated stations: address book, gate, workbench, shelf, shopper. Each station labels itself with the pipeline stage. |
| 3 | 1:11 | 30 | Stage one, discovery. Search engines learn about URLs from links on pages they've already crawled, which is still the main route, from XML sitemaps, from redirect and canonical targets, and, for engines that support it, from IndexNow. Bing, Yandex, Seznam, Naver and Yep use IndexNow; Google doesn't. A page with no internal links and no sitemap entry is an orphan, and orphans are discovered slowly or not at all. | Stage 1: Discovery<br>• Links (still the main route)<br>• Sitemaps, redirects, canonicals<br>• IndexNow for Bing and others, not Google | Web of linked page nodes; a new node appears with links attached and lights up; an isolated orphan node stays grey. A small IndexNow arrow points to a Bing-style engine icon only. |
| 4 | 1:41 | 40 | Stage two, crawling. Googlebot checks robots.txt, requests the URL, and records the status code, headers and raw HTML. Three facts matter. robots.txt controls crawling, not indexing, so a blocked URL can still be indexed from links, without its content. Crawl rate adapts to your server: slow responses, five hundreds and four-twenty-nines make Googlebot back off. And Google documents file size limits. Googlebot fetches the first two megabytes of an HTML file and up to sixty-four megabytes of a PDF. Anything after the cut-off is ignored, so keep critical content and links early. | Stage 2: Crawling<br>• robots.txt controls crawling, not indexing<br>• Slow or erroring servers slow the crawl<br>• Googlebot: first 2 MB of HTML, 64 MB of PDF | Googlebot icon passes a robots.txt gate, fetches a page, and a size bar fills up to a '2 MB' marker; content beyond the marker greys out. |
| 5 | 2:21 | 40 | Stage three, rendering. Google's Web Rendering Service runs an up-to-date version of Chromium to execute JavaScript and build the rendered page. It doesn't click, scroll like a user or fill in forms, and it doesn't keep cookies or local storage between loads. It won't run scripts blocked by robots.txt. And in December twenty twenty-five Google clarified two things: pages with a non-two-hundred status may not be rendered at all, and a noindex in the initial HTML may cause rendering to be skipped. So never rely on JavaScript to remove a noindex. | Stage 3: Rendering<br>• Evergreen Chromium; no clicks, no state<br>• Blocked scripts = broken render<br>• Non-200 or initial noindex may skip rendering | A browser frame builds the page from HTML plus JS; a blocked script file shows a padlock and the page stays half-empty. Two caution chips appear: 'non-200' and 'noindex in raw HTML'. |
| 6 | 3:01 | 37 | Stage four, indexing. Google analyses the content, clusters duplicates, chooses a canonical for each cluster, and decides whether to store the page at all. Indexing isn't guaranteed. Learn the Page indexing statuses. Discovered, currently not indexed, means known but not yet crawled. Crawled, currently not indexed, usually means quality or duplication concerns. Duplicate, Google chose a different canonical than the user, means your signals disagree with Google's choice. And soft four-oh-four means a page that returns two hundred but looks empty or like an error. | Stage 4: Indexing<br>• Duplicates clustered; a canonical chosen<br>• Not guaranteed<br>• Know the Page indexing statuses | Several similar page cards merge into a cluster; one is crowned 'canonical'. A status legend appears with the four statuses and short meanings. |
| 7 | 3:38 | 42 | Stage five, serving. Only indexed canonical URLs can rank. Then ranking systems weigh relevance, quality, usefulness and many other signals. Technical SEO's job is to make sure the right URLs reach this stage with their full content. It doesn't guarantee rankings on its own. Now, the diagnostic routine. When a URL underperforms, walk the pipeline in order: is it discoverable, is crawling allowed, does it return a clean two hundred, does the rendered HTML contain the content and links, are the index directives right, what does URL Inspection say, and only then, look at content and competition. | Stage 5 + the routine<br>• Only indexed canonicals can rank<br>• Walk the stages in order<br>• Content and competition come last | A seven-step checklist slides in beside the pipeline; each step links to its stage with a thin line. |
| 8 | 4:20 | 40 | Hands-on. The lesson text includes a Python script using the Search Console URL Inspection API. You give it a list of URLs from a property you own, and it records the verdict, coverage state, robots state, last crawl time, which crawler was used, your declared canonical, and Google's chosen canonical. Keep an eye on the quotas; at the time of writing they're documented as two thousand calls per day per property. Then sort the output. Patterns jump out: a template where Google's canonical differs from yours, or a batch of pages never crawled. | Hands-on: URL Inspection API<br>• Verdict, coverage, robots, last crawl<br>• Declared vs Google-selected canonical<br>• Mind the quotas; sort for patterns | Code walkthrough: credentials line, the inspect() call, and the CSV writer highlight. Output table sorts by 'coverage', revealing a cluster of mismatched canonicals. |
| 9 | 5:00 | 32 | Worked example one, simple. A plumber's website in Leeds has a new page for boiler repairs in Headingley that doesn't show up anywhere. Walk the pipeline. Discovery: it isn't linked from anywhere and isn't in the sitemap. That's it. The page is an orphan. Add it to the services hub and the sitemap, and within days URL Inspection shows it crawled and indexed. No keyword research needed. The failure was at stage one. | Example 1: the orphaned Leeds page<br>• Not linked, not in sitemap<br>• Failed at Discovery<br>• Link it + sitemap → indexed | Pipeline diagram: the token stops before 'Discovery'. A link arrow from a hub page attaches; the token then travels through all stages to 'Indexed'. |
| 10 | 5:32 | 41 | Worked example two, with illustrative details. A Riyadh news publisher says new Arabic articles don't appear in Google. Walk fifty recent URLs. They're in the news sitemap. robots.txt allows them. They return two hundred quickly. But URL Inspection shows the rendered HTML has no article body. The body is injected by a script loaded from a subdomain, and that subdomain's robots.txt blocks it. Rendering is the failed stage. The fix is allowing the script host or server-rendering the body. Within days, verdicts change to indexed, and the script host goes into the robots.txt regression tests. | Example 2: Riyadh publisher (illustrative)<br>• Discovery, crawl, status: all fine<br>• Rendered HTML missing the body<br>• Blocked script host → allow or server-render | Pipeline with green ticks on Discovery and Crawling, red on Rendering. Zoom into a rendered-HTML panel with an empty article container and a padlocked script URL; padlock opens and text fills in. |
| 11 | 6:13 | 39 | Common mistakes. Blaming the algorithm for pages that were never indexed. Using robots.txt to remove pages from the index, which stops Google seeing the noindex. Assuming what you see in your browser is what Googlebot sees, when cookie banners, geo-redirects and A-B tests can change the response. And treating crawled, currently not indexed as a technical bug when it's usually a quality or duplication signal. How do you measure success? Track the share of priority URLs indexed with Google's canonical matching yours, and the days from publishing to first crawl. | Mistakes and measures<br>• 'The algorithm' vs never indexed<br>• robots.txt ≠ removal<br>• Browser ≠ Googlebot; 'crawled not indexed' ≈ quality | Four mistake cards with corrections; two KPI tiles: '% indexed with matching canonical' and 'days to first crawl'. |
| 12 | 6:52 | 31 | Recap. Discovery, crawling, rendering, indexing, serving. Find the failed stage before you touch content. Remember the two-megabyte HTML limit, that robots.txt doesn't control indexing, and that non-two-hundred pages or an initial noindex may skip rendering. Try this now. Take five URLs that underperform, run them through the seven-step routine or the inspection script, and write down the stage each one fails at. You'll be surprised how often it isn't ranking at all. | Recap and try this now<br>• Find the failed stage first<br>• 5 URLs through the routine<br>• Label each with its stage | Five URL tokens run through the pipeline; each stops at a different stage with a label. End card: 'Next: JavaScript SEO'. |

<details><summary>Narration per scene (paste into ElevenLabs)</summary>

Scene 1 — The crawl, render and index pipeline

```text
When a page doesn't rank, most people ask, what keywords are we missing? An expert asks a different question first. Which stage did this URL fail at? Every technical SEO problem is a failure somewhere in a pipeline: discovery, crawling, rendering, indexing and serving. In this lecture you'll learn that pipeline properly, using Google's documented behaviour, and you'll leave with a diagnostic routine, and a small Python script, that tells you exactly where a URL is getting stuck.
```

Scene 2 — The parcel analogy

```text
Think of it like a parcel being delivered. First, the courier has to know the address exists. That's discovery. Then they have to be allowed through the gate and pick it up. That's crawling. Then they unpack it and assemble it. That's rendering. Then someone decides whether it's worth putting on the shelf. That's indexing. And finally, a customer has to choose it from the shelf. That's serving, or ranking. If the parcel never left the depot, arguing about the shelf display is pointless.
```

Scene 3 — Stage 1: Discovery

```text
Stage one, discovery. Search engines learn about URLs from links on pages they've already crawled, which is still the main route, from XML sitemaps, from redirect and canonical targets, and, for engines that support it, from IndexNow. Bing, Yandex, Seznam, Naver and Yep use IndexNow; Google doesn't. A page with no internal links and no sitemap entry is an orphan, and orphans are discovered slowly or not at all.
```

Scene 4 — Stage 2: Crawling

```text
Stage two, crawling. Googlebot checks robots.txt, requests the URL, and records the status code, headers and raw HTML. Three facts matter. robots.txt controls crawling, not indexing, so a blocked URL can still be indexed from links, without its content. Crawl rate adapts to your server: slow responses, five hundreds and four-twenty-nines make Googlebot back off. And Google documents file size limits. Googlebot fetches the first two megabytes of an HTML file and up to sixty-four megabytes of a PDF. Anything after the cut-off is ignored, so keep critical content and links early.
```

Scene 5 — Stage 3: Rendering

```text
Stage three, rendering. Google's Web Rendering Service runs an up-to-date version of Chromium to execute JavaScript and build the rendered page. It doesn't click, scroll like a user or fill in forms, and it doesn't keep cookies or local storage between loads. It won't run scripts blocked by robots.txt. And in December twenty twenty-five Google clarified two things: pages with a non-two-hundred status may not be rendered at all, and a noindex in the initial HTML may cause rendering to be skipped. So never rely on JavaScript to remove a noindex.
```

Scene 6 — Stage 4: Indexing

```text
Stage four, indexing. Google analyses the content, clusters duplicates, chooses a canonical for each cluster, and decides whether to store the page at all. Indexing isn't guaranteed. Learn the Page indexing statuses. Discovered, currently not indexed, means known but not yet crawled. Crawled, currently not indexed, usually means quality or duplication concerns. Duplicate, Google chose a different canonical than the user, means your signals disagree with Google's choice. And soft four-oh-four means a page that returns two hundred but looks empty or like an error.
```

Scene 7 — Stage 5 + the routine

```text
Stage five, serving. Only indexed canonical URLs can rank. Then ranking systems weigh relevance, quality, usefulness and many other signals. Technical SEO's job is to make sure the right URLs reach this stage with their full content. It doesn't guarantee rankings on its own. Now, the diagnostic routine. When a URL underperforms, walk the pipeline in order: is it discoverable, is crawling allowed, does it return a clean two hundred, does the rendered HTML contain the content and links, are the index directives right, what does URL Inspection say, and only then, look at content and competition.
```

Scene 8 — Hands-on: URL Inspection API

```text
Hands-on. The lesson text includes a Python script using the Search Console URL Inspection API. You give it a list of URLs from a property you own, and it records the verdict, coverage state, robots state, last crawl time, which crawler was used, your declared canonical, and Google's chosen canonical. Keep an eye on the quotas; at the time of writing they're documented as two thousand calls per day per property. Then sort the output. Patterns jump out: a template where Google's canonical differs from yours, or a batch of pages never crawled.
```

Scene 9 — Example 1: the orphaned Leeds page

```text
Worked example one, simple. A plumber's website in Leeds has a new page for boiler repairs in Headingley that doesn't show up anywhere. Walk the pipeline. Discovery: it isn't linked from anywhere and isn't in the sitemap. That's it. The page is an orphan. Add it to the services hub and the sitemap, and within days URL Inspection shows it crawled and indexed. No keyword research needed. The failure was at stage one.
```

Scene 10 — Example 2: Riyadh publisher (illustrative)

```text
Worked example two, with illustrative details. A Riyadh news publisher says new Arabic articles don't appear in Google. Walk fifty recent URLs. They're in the news sitemap. robots.txt allows them. They return two hundred quickly. But URL Inspection shows the rendered HTML has no article body. The body is injected by a script loaded from a subdomain, and that subdomain's robots.txt blocks it. Rendering is the failed stage. The fix is allowing the script host or server-rendering the body. Within days, verdicts change to indexed, and the script host goes into the robots.txt regression tests.
```

Scene 11 — Mistakes and measures

```text
Common mistakes. Blaming the algorithm for pages that were never indexed. Using robots.txt to remove pages from the index, which stops Google seeing the noindex. Assuming what you see in your browser is what Googlebot sees, when cookie banners, geo-redirects and A-B tests can change the response. And treating crawled, currently not indexed as a technical bug when it's usually a quality or duplication signal. How do you measure success? Track the share of priority URLs indexed with Google's canonical matching yours, and the days from publishing to first crawl.
```

Scene 12 — Recap and try this now

```text
Recap. Discovery, crawling, rendering, indexing, serving. Find the failed stage before you touch content. Remember the two-megabyte HTML limit, that robots.txt doesn't control indexing, and that non-two-hundred pages or an initial noindex may skip rendering. Try this now. Take five URLs that underperform, run them through the seven-step routine or the inspection script, and write down the stage each one fails at. You'll be surprised how often it isn't ranking at all.
```

</details>

### 2. JavaScript SEO: SPAs, SSR and prerendering

Lesson `javascript-seo-spas-ssr-prerendering` · module “How search engines crawl, render and index” · target 7 min · 11 scenes · 936 words · planned 6:46 (words ≈ 6:47) · status: **to produce**

| Term | Say |
|---|---|
| SPA | S P A |
| SSR | S S R |
| SSG | S S G |
| Next.js | next J S |
| Playwright | play-write |

| # | Time | Sec | Narration | On screen | Visual |
|---|---|---|---|---|---|
| 1 | 0:00 | 32 | Here's a question that saves hours of debugging. What does the very first HTML response of your page contain? Not what you see in the browser after everything loads. The first response. In this lecture you'll learn why that question matters so much, how rendering strategies compare, the non-negotiables for JavaScript sites, what Google clarified in late twenty twenty-five, and how to diff raw and rendered HTML yourself with a short Python script. | JavaScript SEO<br>• Raw HTML vs rendered HTML<br>• Rendering strategies compared<br>• Non-negotiables + a diff script | Split screen: left 'First response' shows an almost empty div and a script tag; right 'After JavaScript' shows a full product grid. A question mark hovers between them. |
| 2 | 0:32 | 40 | On a classic server-rendered site, the first response already contains the text, the links and the meta tags. On a client-side single-page app, it can be an empty div and a script tag. Everything search engines need only exists after JavaScript runs. Google can render JavaScript, but rendering is an extra step with extra ways to fail. And many other crawlers, including social preview bots and several AI crawlers, don't run JavaScript at all. Think of it as sending flat-pack furniture. Google will assemble it. Many others will just look at the box. | Why it matters<br>• SPA first response can be nearly empty<br>• Rendering = extra step, extra failure modes<br>• Many crawlers never render | Flat-pack furniture box labelled 'raw HTML'; a Googlebot character assembles it into a bookshelf; other bot characters just glance at the box and walk away. |
| 3 | 1:12 | 38 | Let's compare strategies. Client-side rendering builds the page in the browser, so it carries the highest SEO risk. Server-side rendering returns full HTML per request and then hydrates, which is low risk if hydration doesn't change the content. Static site generation builds HTML at deploy time, the lowest risk, great for content that changes infrequently. Incremental regeneration rebuilds static pages on a schedule or trigger. And dynamic rendering, serving prerendered HTML to bots and the app to users, is something Google calls a workaround, not a long-term solution. | Rendering strategies<br>• CSR: highest risk<br>• SSR / SSG / ISR: low risk<br>• Dynamic rendering: a workaround | Five-row comparison table animates with a risk meter per row, from red for CSR to green for SSG; dynamic rendering gets an amber 'workaround' tag. |
| 4 | 1:50 | 37 | Now the non-negotiables. One: real links. Google discovers links from anchor elements with an href. A span with an onclick handler isn't a link. Two: real URLs, not hash fragments. Use paths like slash products slash red-shoes; Google generally ignores everything after the hash. Three: correct status codes. An app that shows product not found but returns two hundred creates soft four-oh-fours; route unknown paths to a real four-oh-four from the server. Four: don't block the JavaScript, CSS or API endpoints your pages need to render. | Non-negotiables 1–4<br>• Real <a href> links<br>• Real paths, not #fragments<br>• Real 404s<br>• Don't block JS, CSS or APIs | Code snippets appear: a crawlable anchor with a green tick, a span onclick with a red cross, a hash URL crossed out, and a robots.txt line blocking /api/ turning red. |
| 5 | 2:27 | 43 | Five: put meta tags in the initial HTML where you can. Google usually honours titles, canonicals and robots meta set by JavaScript after rendering, but conflicting values between raw and rendered HTML make results unpredictable. And here's what Google made explicit in December twenty twenty-five. When it sees noindex, it may skip rendering, so a noindex in the raw HTML that JavaScript later removes may never be seen as removed. And pages with non-two-hundred status codes may not be rendered at all. Six: lazy-load safely, with native loading equals lazy or intersection observers, never scroll events for primary content. | Non-negotiables 5–6<br>• Meta tags in the initial HTML<br>• Noindex may skip rendering; non-200 may not render<br>• Lazy-load without scroll events | Raw vs rendered head sections side by side; a noindex tag in the raw column has a warning icon 'rendering may be skipped'. Then a lazy-loading image grid loads as it enters the viewport. |
| 6 | 3:10 | 27 | Hydration mismatches deserve a moment. With server-side rendering, the server sends HTML, and client JavaScript then hydrates it. If the client renders something different, say a different price, or a different canonical because of a locale cookie, Google may index whichever version it saw. The only way to catch this is comparing raw HTML with rendered HTML. Which brings us to the hands-on. | Hydration mismatches<br>• Server HTML ≠ client render<br>• Different price, different canonical<br>• Catch it by diffing raw vs rendered | Server HTML card shows 'price: 149'; after a hydration animation the card shows 'price: 129' and canonical changes; a diff highlighter marks both lines. |
| 7 | 3:37 | 38 | The lesson text has a Python script that fetches a URL twice. Once with requests, which gives you the raw HTML. And once with Playwright's headless Chromium, which gives you the rendered HTML. It summarises both: title, canonical, robots meta, number of unique links and word count, and flags any differences. A different canonical or robots value is high severity. Big differences in links or words mean the page depends on rendering. One caveat: Playwright isn't Google's renderer, so confirm important findings with URL Inspection's rendered HTML. | Hands-on: raw vs rendered diff<br>• requests = raw; Playwright = rendered<br>• Compare title, canonical, robots, links, words<br>• Confirm with URL Inspection | Code walkthrough: summarise() function highlights; then the Playwright block; then the diff printout with two rows flagged '<-- differs'. |
| 8 | 4:15 | 40 | Worked example one, with illustrative details. A Dubai e-commerce brand rebuilt its catalogue as a React single-page app. Category pages returned only the shell, and products loaded from an API path that was disallowed in robots.txt to save crawl budget. URL Inspection showed an empty grid, so Google saw thin category pages with no product links. The fix: allow the API path, move category and product templates to server-side rendering, output product links as real anchors, and return four-oh-four for discontinued products. Then watch the Page indexing report over the following weeks. | Example 1: Dubai React catalogue (illustrative)<br>• Shell-only categories; API blocked<br>• Google saw empty grids<br>• Allow API, SSR, real links, real 404s | Before: URL Inspection screenshot mock with an empty grid. After: full grid with product anchors. A robots.txt '/api/' line flips from Disallow to allowed. |
| 9 | 4:55 | 44 | Worked example two, also illustrative. A Lahore SaaS company says its Next.js marketing site is server-rendered, so JavaScript SEO isn't a concern. The diff shows the raw HTML of the pricing page contains only a loading skeleton, because the pricing component fetches plans on the client after hydration. The fix is fetching plan data at build or request time. And a second finding: the locale switcher changes the canonical on the client based on a cookie. The fix is rendering the canonical on the server from the URL path only. A framework that can do SSR doesn't mean every component does. | Example 2: 'our framework does SSR' (illustrative)<br>• Pricing fetched client-side → skeleton in raw HTML<br>• Canonical changed by a cookie on the client<br>• Fetch server-side; canonical from URL path | Raw HTML panel shows a grey skeleton; after the fix it shows plan names and prices. A cookie icon attempting to change a canonical tag gets blocked. |
| 10 | 5:39 | 34 | Common mistakes. Testing only in a browser where you're logged in or have cookies. Relying on dynamic rendering indefinitely and letting the bot version drift from the user version, which can look like cloaking. Infinite scroll with no paginated URLs. And forgetting that social, many AI and some search crawlers don't run JavaScript at all. How do you measure success? The share of key templates where title, canonical, robots and main content match between raw and rendered HTML. | Common mistakes<br>• Testing while logged in<br>• Drifting dynamic rendering<br>• Infinite scroll without URLs; forgetting non-rendering crawlers | Four mistake cards; a KPI tile 'templates with raw = rendered essentials' fills to 100%. |
| 11 | 6:13 | 33 | Recap. For anything you want indexed, send meaningful HTML in the first response. Use real links, real URLs and real status codes. Don't block resources. Keep noindex and canonicals consistent from the start, because noindex can stop rendering and non-two-hundred pages may not render. Try this now. Run the diff script on one URL from each of your main templates, and list every row marked differs. Each one is either a fix or a documented decision. | Recap and try this now<br>• Meaningful HTML first<br>• Diff one URL per template<br>• Every 'differs' → fix or document | Template list with one URL each runs through the diff; flagged rows collect into a fix list. End card: 'Next module: crawl control'. |

<details><summary>Narration per scene (paste into ElevenLabs)</summary>

Scene 1 — JavaScript SEO

```text
Here's a question that saves hours of debugging. What does the very first HTML response of your page contain? Not what you see in the browser after everything loads. The first response. In this lecture you'll learn why that question matters so much, how rendering strategies compare, the non-negotiables for JavaScript sites, what Google clarified in late twenty twenty-five, and how to diff raw and rendered HTML yourself with a short Python script.
```

Scene 2 — Why it matters

```text
On a classic server-rendered site, the first response already contains the text, the links and the meta tags. On a client-side single-page app, it can be an empty div and a script tag. Everything search engines need only exists after JavaScript runs. Google can render JavaScript, but rendering is an extra step with extra ways to fail. And many other crawlers, including social preview bots and several AI crawlers, don't run JavaScript at all. Think of it as sending flat-pack furniture. Google will assemble it. Many others will just look at the box.
```

Scene 3 — Rendering strategies

```text
Let's compare strategies. Client-side rendering builds the page in the browser, so it carries the highest SEO risk. Server-side rendering returns full HTML per request and then hydrates, which is low risk if hydration doesn't change the content. Static site generation builds HTML at deploy time, the lowest risk, great for content that changes infrequently. Incremental regeneration rebuilds static pages on a schedule or trigger. And dynamic rendering, serving prerendered HTML to bots and the app to users, is something Google calls a workaround, not a long-term solution.
```

Scene 4 — Non-negotiables 1–4

```text
Now the non-negotiables. One: real links. Google discovers links from anchor elements with an href. A span with an onclick handler isn't a link. Two: real URLs, not hash fragments. Use paths like slash products slash red-shoes; Google generally ignores everything after the hash. Three: correct status codes. An app that shows product not found but returns two hundred creates soft four-oh-fours; route unknown paths to a real four-oh-four from the server. Four: don't block the JavaScript, CSS or API endpoints your pages need to render.
```

Scene 5 — Non-negotiables 5–6

```text
Five: put meta tags in the initial HTML where you can. Google usually honours titles, canonicals and robots meta set by JavaScript after rendering, but conflicting values between raw and rendered HTML make results unpredictable. And here's what Google made explicit in December twenty twenty-five. When it sees noindex, it may skip rendering, so a noindex in the raw HTML that JavaScript later removes may never be seen as removed. And pages with non-two-hundred status codes may not be rendered at all. Six: lazy-load safely, with native loading equals lazy or intersection observers, never scroll events for primary content.
```

Scene 6 — Hydration mismatches

```text
Hydration mismatches deserve a moment. With server-side rendering, the server sends HTML, and client JavaScript then hydrates it. If the client renders something different, say a different price, or a different canonical because of a locale cookie, Google may index whichever version it saw. The only way to catch this is comparing raw HTML with rendered HTML. Which brings us to the hands-on.
```

Scene 7 — Hands-on: raw vs rendered diff

```text
The lesson text has a Python script that fetches a URL twice. Once with requests, which gives you the raw HTML. And once with Playwright's headless Chromium, which gives you the rendered HTML. It summarises both: title, canonical, robots meta, number of unique links and word count, and flags any differences. A different canonical or robots value is high severity. Big differences in links or words mean the page depends on rendering. One caveat: Playwright isn't Google's renderer, so confirm important findings with URL Inspection's rendered HTML.
```

Scene 8 — Example 1: Dubai React catalogue (illustrative)

```text
Worked example one, with illustrative details. A Dubai e-commerce brand rebuilt its catalogue as a React single-page app. Category pages returned only the shell, and products loaded from an API path that was disallowed in robots.txt to save crawl budget. URL Inspection showed an empty grid, so Google saw thin category pages with no product links. The fix: allow the API path, move category and product templates to server-side rendering, output product links as real anchors, and return four-oh-four for discontinued products. Then watch the Page indexing report over the following weeks.
```

Scene 9 — Example 2: 'our framework does SSR' (illustrative)

```text
Worked example two, also illustrative. A Lahore SaaS company says its Next.js marketing site is server-rendered, so JavaScript SEO isn't a concern. The diff shows the raw HTML of the pricing page contains only a loading skeleton, because the pricing component fetches plans on the client after hydration. The fix is fetching plan data at build or request time. And a second finding: the locale switcher changes the canonical on the client based on a cookie. The fix is rendering the canonical on the server from the URL path only. A framework that can do SSR doesn't mean every component does.
```

Scene 10 — Common mistakes

```text
Common mistakes. Testing only in a browser where you're logged in or have cookies. Relying on dynamic rendering indefinitely and letting the bot version drift from the user version, which can look like cloaking. Infinite scroll with no paginated URLs. And forgetting that social, many AI and some search crawlers don't run JavaScript at all. How do you measure success? The share of key templates where title, canonical, robots and main content match between raw and rendered HTML.
```

Scene 11 — Recap and try this now

```text
Recap. For anything you want indexed, send meaningful HTML in the first response. Use real links, real URLs and real status codes. Don't block resources. Keep noindex and canonicals consistent from the start, because noindex can stop rendering and non-two-hundred pages may not render. Try this now. Run the diff script on one URL from each of your main templates, and list every row marked differs. Each one is either a fix or a documented decision.
```

</details>

### 3. Crawl budget and log-file analysis

Lesson `crawl-budget-and-log-file-analysis` · module “Crawl budget, robots.txt and indexing directives” · target 7 min · 11 scenes · 916 words · planned 6:37 (words ≈ 6:38) · status: **to produce**

| Term | Say |
|---|---|
| CDN | C D N |
| DNS | D N S |
| pandas | pandas |
| awk | awk |

| # | Time | Sec | Narration | On screen | Visual |
|---|---|---|---|---|---|
| 1 | 0:00 | 32 | Crawl budget is one of the most misunderstood ideas in SEO. Small sites worry about it when they shouldn't, and huge sites ignore it when it's quietly costing them. In this lecture you'll learn what crawl budget really is, who should care, what wastes it, and how to use server logs, the only ground truth, to see what Googlebot actually does on your site, with a Python analysis you can reuse on any audit. | Crawl budget and log-file analysis<br>• What crawl budget really is<br>• Who should care<br>• Logs: the ground truth | A Googlebot icon with a fuel gauge; fuel drains as it wanders into maze-like parameter URLs. Title appears. |
| 2 | 0:32 | 39 | Google describes crawl budget as the set of URLs Googlebot can and wants to crawl. Two parts. Crawl capacity is how much Googlebot can fetch without overloading your server; it rises with fast responses and falls with slow ones, five hundreds and four-twenty-nines. Crawl demand is how much Google wants to crawl, driven by popularity, staleness and the size of the URL inventory it knows about. Think of a delivery driver. Capacity is how many stops the van and roads allow. Demand is how many parcels are worth delivering today. | What crawl budget is<br>• Capacity: what your server allows<br>• Demand: what Google wants to crawl<br>• Budget = can AND wants | Delivery van analogy: a road-width gauge labelled 'capacity' and a parcel stack labelled 'demand'; the van's route length equals the smaller of the two. |
| 3 | 1:11 | 31 | Who should care? Google's own guidance says crawl budget is mainly a concern for very large sites, with hundreds of thousands to millions of URLs, sites whose content changes very often, and sites with many URLs stuck in discovered, currently not indexed. A two-hundred-page brochure site doesn't have a crawl budget problem. It has a quality or linking problem. So before you optimise crawl budget, check whether you're in the club. | Who should care?<br>• Very large sites<br>• Rapidly changing content<br>• Many 'Discovered – not indexed' URLs | Three site-size icons: small brochure (crossed out), large marketplace (tick), news site (tick). A Page indexing status chip 'Discovered – currently not indexed' pulses. |
| 4 | 1:42 | 34 | What wastes crawl budget? Faceted and parameter URLs are the big one: colour, size, sort and price combinations can generate millions of near-duplicates. Infinite spaces, like calendars with endless next month links, internal search results and session IDs. Redirect chains and soft four-oh-fours, where every hop or empty page costs a fetch. Duplicate host and protocol variants all returning two hundred. And slow server responses, which directly reduce capacity. Most large-site crawl problems are one of these five. | The big wasters<br>• Facets and parameters<br>• Infinite spaces<br>• Chains, soft 404s, duplicates, slow servers | A URL tree explodes into thousands of parameter branches; separate icons for a never-ending calendar, a session ID URL and a redirect chain. |
| 5 | 2:16 | 40 | Now logs. Crawlers and Search Console tell you what could be crawled. Server logs tell you what was crawled. A combined log line gives you the IP, timestamp, method, path with query string, status, bytes and user agent. If a CDN serves cached pages, get the CDN's logs, because origin logs miss cached hits. And verify before you trust. Anyone can fake a Googlebot user agent. Do a reverse DNS lookup to a Google domain, then a forward lookup that returns the same IP, or match against Google's published crawler IP ranges. | Logs: the ground truth<br>• IP, time, URL, status, UA<br>• CDN logs for cached sites<br>• Verify: reverse + forward DNS, or IP ranges | A log line breaks into labelled fields. Then the IP goes through reverse DNS to 'crawl-66-249-66-1.googlebot.com' and forward DNS back to the same IP with a green tick. |
| 6 | 2:56 | 32 | Here's the procedure. Collect at least two to four weeks of logs. Filter to verified search engine bots. Classify each URL by template, like product, category, blog, facet, search or asset. Join with your crawl data and Search Console. Then answer five questions. What share of bot hits go to non-indexable URLs? Are important pages crawled regularly? Are there status code problems? Are orphan URLs being crawled? And is crawl activity changing after releases? | The procedure<br>• 2–4 weeks; verified bots only<br>• Classify by template; join crawl + GSC<br>• Answer five questions | Flowchart: logs → verify → classify → join → five question cards fan out. |
| 7 | 3:28 | 35 | The lesson text has a pandas script that does exactly this. A regex parses each line. A small list of patterns assigns a template. A cached function verifies Googlebot with reverse and forward DNS. Then it prints the share of crawl by template, a table of status codes by template, and, after joining with your crawler export, the number of indexable URLs that got zero verified Googlebot hits in the period. That last number is often the headline of an audit. | Hands-on: pandas log analysis<br>• Parse → verify → classify<br>• Share of crawl + status by template<br>• Indexable URLs with zero hits | Code walkthrough: TEMPLATES list, is_google() function, value_counts() and crosstab() outputs, then the join producing a big number '3,412 indexable URLs never crawled' labelled illustrative. |
| 8 | 4:03 | 33 | Worked example one, simple. A mid-sized UK retailer with fifteen thousand products runs the script. Around a third of verified Googlebot hits go to sort parameters, linked from every category page. Status codes are healthy. Fix: stop linking sort parameters as crawlable links, and disallow the sort parameter in robots.txt once you've confirmed none of those URLs should be indexed. A month later, the logs show the share on sort URLs falling and product crawl frequency rising. | Example 1: UK retailer<br>• ~1/3 of crawl on sort parameters (illustrative)<br>• Remove crawlable sort links; disallow<br>• Measure the shift in logs | Pie chart of crawl by template with a large 'sort' slice; after the fix, the slice shrinks and 'product' grows. |
| 9 | 4:36 | 43 | Worked example two, with illustrative details. A Dubai classifieds site with around two million live ads finds new listings take a week or more to be crawled. The report shows most verified Googlebot requests going to sort and page combinations on search results, and to expired ads returning two hundred with this ad has expired. Actions: disallow sort parameters, return four-ten for ads expired more than thirty days, a fresh listings sitemap with accurate lastmod, and caching that halves response time. Success is measured in logs: share of crawl on live ads, and hours from posting to first Googlebot hit. | Example 2: Dubai classifieds (illustrative)<br>• Crawl stuck on sort/page + expired ads<br>• Disallow, 410 old ads, fresh sitemap, caching<br>• KPI: hours from posting to first crawl | Timeline: 'ad posted' to 'first Googlebot hit' shrinks from 7 days to under a day (illustrative). Supporting icons for 410, sitemap and cache. |
| 10 | 5:19 | 40 | How do you improve crawl efficiency generally? Remove internal links to parameter URLs you don't want crawled, and block truly useless patterns in robots.txt. Return four-oh-four or four-ten for gone content instead of redirecting everything to the home page. Flatten redirect chains. Keep sitemaps to canonical, indexable, two-hundred URLs. Improve response times and watch the Crawl stats report. Support three-oh-four not modified where your stack allows. Common mistakes: using noindex to save crawl budget, since noindexed pages are still crawled; using crawl-delay for Google, which ignores it; and analysing unverified Googlebot traffic. | Improve efficiency; avoid mistakes<br>• Remove links, block useless patterns, 404/410<br>• Flatten chains; clean sitemaps; speed; 304s<br>• Noindex ≠ crawl saving; Google ignores crawl-delay | Checklist ticks down the left; three mistake chips on the right with red crosses. |
| 11 | 5:59 | 38 | Recap. Crawl budget is capacity plus demand, and it matters mainly for large or fast-changing sites. The biggest wasters are facets, infinite spaces, chains, soft four-oh-fours, duplicates and slow servers. Logs are the ground truth, but only after verification. Try this now. Export two weeks of logs, run the pandas script, and write down two numbers: the share of verified Googlebot hits on non-indexable templates, and the count of indexable URLs with zero hits. Those two numbers tell you whether you have a crawl problem at all. | Recap and try this now<br>• Run the script on 2 weeks of logs<br>• Number 1: % crawl on non-indexable templates<br>• Number 2: indexable URLs with zero hits | Two big KPI tiles fill in with illustrative values. End card: 'Next: robots.txt, meta robots and X-Robots-Tag'. |

<details><summary>Narration per scene (paste into ElevenLabs)</summary>

Scene 1 — Crawl budget and log-file analysis

```text
Crawl budget is one of the most misunderstood ideas in SEO. Small sites worry about it when they shouldn't, and huge sites ignore it when it's quietly costing them. In this lecture you'll learn what crawl budget really is, who should care, what wastes it, and how to use server logs, the only ground truth, to see what Googlebot actually does on your site, with a Python analysis you can reuse on any audit.
```

Scene 2 — What crawl budget is

```text
Google describes crawl budget as the set of URLs Googlebot can and wants to crawl. Two parts. Crawl capacity is how much Googlebot can fetch without overloading your server; it rises with fast responses and falls with slow ones, five hundreds and four-twenty-nines. Crawl demand is how much Google wants to crawl, driven by popularity, staleness and the size of the URL inventory it knows about. Think of a delivery driver. Capacity is how many stops the van and roads allow. Demand is how many parcels are worth delivering today.
```

Scene 3 — Who should care?

```text
Who should care? Google's own guidance says crawl budget is mainly a concern for very large sites, with hundreds of thousands to millions of URLs, sites whose content changes very often, and sites with many URLs stuck in discovered, currently not indexed. A two-hundred-page brochure site doesn't have a crawl budget problem. It has a quality or linking problem. So before you optimise crawl budget, check whether you're in the club.
```

Scene 4 — The big wasters

```text
What wastes crawl budget? Faceted and parameter URLs are the big one: colour, size, sort and price combinations can generate millions of near-duplicates. Infinite spaces, like calendars with endless next month links, internal search results and session IDs. Redirect chains and soft four-oh-fours, where every hop or empty page costs a fetch. Duplicate host and protocol variants all returning two hundred. And slow server responses, which directly reduce capacity. Most large-site crawl problems are one of these five.
```

Scene 5 — Logs: the ground truth

```text
Now logs. Crawlers and Search Console tell you what could be crawled. Server logs tell you what was crawled. A combined log line gives you the IP, timestamp, method, path with query string, status, bytes and user agent. If a CDN serves cached pages, get the CDN's logs, because origin logs miss cached hits. And verify before you trust. Anyone can fake a Googlebot user agent. Do a reverse DNS lookup to a Google domain, then a forward lookup that returns the same IP, or match against Google's published crawler IP ranges.
```

Scene 6 — The procedure

```text
Here's the procedure. Collect at least two to four weeks of logs. Filter to verified search engine bots. Classify each URL by template, like product, category, blog, facet, search or asset. Join with your crawl data and Search Console. Then answer five questions. What share of bot hits go to non-indexable URLs? Are important pages crawled regularly? Are there status code problems? Are orphan URLs being crawled? And is crawl activity changing after releases?
```

Scene 7 — Hands-on: pandas log analysis

```text
The lesson text has a pandas script that does exactly this. A regex parses each line. A small list of patterns assigns a template. A cached function verifies Googlebot with reverse and forward DNS. Then it prints the share of crawl by template, a table of status codes by template, and, after joining with your crawler export, the number of indexable URLs that got zero verified Googlebot hits in the period. That last number is often the headline of an audit.
```

Scene 8 — Example 1: UK retailer

```text
Worked example one, simple. A mid-sized UK retailer with fifteen thousand products runs the script. Around a third of verified Googlebot hits go to sort parameters, linked from every category page. Status codes are healthy. Fix: stop linking sort parameters as crawlable links, and disallow the sort parameter in robots.txt once you've confirmed none of those URLs should be indexed. A month later, the logs show the share on sort URLs falling and product crawl frequency rising.
```

Scene 9 — Example 2: Dubai classifieds (illustrative)

```text
Worked example two, with illustrative details. A Dubai classifieds site with around two million live ads finds new listings take a week or more to be crawled. The report shows most verified Googlebot requests going to sort and page combinations on search results, and to expired ads returning two hundred with this ad has expired. Actions: disallow sort parameters, return four-ten for ads expired more than thirty days, a fresh listings sitemap with accurate lastmod, and caching that halves response time. Success is measured in logs: share of crawl on live ads, and hours from posting to first Googlebot hit.
```

Scene 10 — Improve efficiency; avoid mistakes

```text
How do you improve crawl efficiency generally? Remove internal links to parameter URLs you don't want crawled, and block truly useless patterns in robots.txt. Return four-oh-four or four-ten for gone content instead of redirecting everything to the home page. Flatten redirect chains. Keep sitemaps to canonical, indexable, two-hundred URLs. Improve response times and watch the Crawl stats report. Support three-oh-four not modified where your stack allows. Common mistakes: using noindex to save crawl budget, since noindexed pages are still crawled; using crawl-delay for Google, which ignores it; and analysing unverified Googlebot traffic.
```

Scene 11 — Recap and try this now

```text
Recap. Crawl budget is capacity plus demand, and it matters mainly for large or fast-changing sites. The biggest wasters are facets, infinite spaces, chains, soft four-oh-fours, duplicates and slow servers. Logs are the ground truth, but only after verification. Try this now. Export two weeks of logs, run the pandas script, and write down two numbers: the share of verified Googlebot hits on non-indexable templates, and the count of indexable URLs with zero hits. Those two numbers tell you whether you have a crawl problem at all.
```

</details>

### 4. robots.txt, meta robots and X-Robots-Tag

Lesson `robots-txt-meta-robots-x-robots-tag` · module “Crawl budget, robots.txt and indexing directives” · target 7 min · 12 scenes · 957 words · planned 6:56 (words ≈ 6:56) · status: **to produce**

| Term | Say |
|---|---|
| RFC 9309 | R F C ninety-three oh nine |
| KiB | kibibytes |
| X-Robots-Tag | X robots tag |
| pytest | pie-test |
| Nginx | engine X |

| # | Time | Sec | Narration | On screen | Visual |
|---|---|---|---|---|---|
| 1 | 0:00 | 29 | robots.txt, meta robots and the X-Robots-Tag header look simple. They're also behind some of the most expensive mistakes in SEO, including the one-line production Disallow that removes a whole site from crawling. In this lecture you'll learn which tool does which job, the rules that trip up experts, the classic blocked-plus-noindex trap, and how to put robots.txt into automated tests so a bad deploy can't slip through. | robots.txt, meta robots, X-Robots-Tag<br>• The right tool for each job<br>• Rules that trip up experts<br>• Automated regression tests | A robots.txt file with a single highlighted line 'Disallow: /' and a traffic graph beside it plunging. Title appears. |
| 2 | 0:29 | 38 | Start with the job, not the tool. To stop crawling of a path, use robots.txt Disallow, but it doesn't guarantee de-indexing. To keep a crawlable page out of the index, use a meta robots noindex, and don't block it in robots.txt. For non-HTML files like PDFs, use the X-Robots-Tag HTTP header. To consolidate duplicates, use canonicals or redirects. For an urgent temporary removal, use Search Console's Removals tool, which lasts about six months. And to protect private content, use authentication, because robots.txt is public and advertises your paths. | Right tool, right job<br>• Stop crawling: robots.txt<br>• Stop indexing: noindex (not blocked)<br>• Files: X-Robots-Tag; privacy: authentication | Six job cards on the left connect with lines to six tool cards on the right; the 'privacy' card connects to a padlock, not to robots.txt. |
| 3 | 1:07 | 37 | Now robots.txt fundamentals. It lives at the root of each host and protocol, so www and a shop subdomain each need their own. It follows RFC ninety-three-oh-nine. Four rules matter most. Grouping: a crawler obeys only the most specific user-agent group that matches it, so a Googlebot group means Googlebot ignores the star group entirely. Precedence: for Google, the longest matching rule wins, and if Allow and Disallow tie, Allow wins. Wildcards: star matches any sequence and dollar anchors the end. And paths are case-sensitive. | robots.txt fundamentals<br>• One file per host and protocol<br>• Most specific group wins<br>• Longest rule wins; * and $; case-sensitive | Two host cards each with its own robots.txt. Then a robots.txt editor highlights a Googlebot group and greys out the * group for Googlebot; a rule-length comparison bar shows the longer rule winning. |
| 4 | 1:44 | 31 | A few more facts people forget. Google processes only the first five hundred kibibytes of robots.txt. Google doesn't support noindex, crawl-delay or nofollow lines in robots.txt, although Bing does support crawl-delay. And error handling matters. If robots.txt returns a four-hundred-level error, Google treats it as no restrictions. If it returns five-hundred-level errors for an extended period, Google may stop crawling the site. So a broken robots.txt server can quietly halt crawling. | Facts people forget<br>• First 500 KiB only<br>• No noindex / crawl-delay for Google<br>• 4xx = no rules; prolonged 5xx = crawling may stop | Three fact cards; the last shows a robots.txt request returning '503' and a Googlebot icon pausing with a stop sign. |
| 5 | 2:15 | 40 | Meta robots and X-Robots-Tag carry the index directives. Common values: noindex, nofollow, none, nosnippet, max-snippet, max-image-preview, max-video-preview, noimageindex and unavailable after. There's also the data-nosnippet attribute to exclude a section from snippets. Snippet controls also apply to how Google may show your content in AI features, but they're blunt, because you lose normal snippets too. Since twenty twenty-six, Google also has a Search Console setting that excludes a site from AI Overviews and AI Mode without touching snippets. For PDFs, the lesson text has Apache and Nginx examples for an X-Robots-Tag noindex header. | Index directives<br>• noindex, nofollow, nosnippet, max-snippet…<br>• data-nosnippet for page sections<br>• X-Robots-Tag for non-HTML files | HTML head with meta robots lines, then an HTTP response panel with 'X-Robots-Tag: noindex'. Nginx config snippet slides in for .pdf files. |
| 6 | 2:55 | 32 | Now the classic trap. You Disallow a path in robots.txt and add noindex to the pages. Google can't crawl them, so it never sees the noindex, and it may keep the URLs indexed from links. The correct sequence is: allow crawling, serve noindex, wait until the URLs drop out, confirmed in URL Inspection or the Page indexing report, and only then consider a Disallow if you also want to stop crawling. Order matters. | The blocked + noindex trap<br>• Blocked pages can't show their noindex<br>• Allow → noindex → confirm drop-out<br>• Only then Disallow (optional) | Sequence diagram: a padlocked page with a hidden 'noindex' label that Googlebot can't read; then the correct four-step sequence animates with ticks. |
| 7 | 3:27 | 31 | Staging sites deserve their own warning. They get indexed constantly because someone links to them or a sitemap leaks. The robust approach is HTTP authentication or IP allow-listing, with a global X-Robots-Tag noindex header as a second layer. Don't rely on Disallow slash alone. And make sure deploy scripts can't copy staging robots rules to production, because a production Disallow slash is one of the most damaging one-line mistakes in SEO. | Staging sites<br>• Authentication or IP allow-listing<br>• X-Robots-Tag noindex as a second layer<br>• Never ship staging robots.txt | Staging server behind a login gate with a noindex header badge; a deploy pipeline arrow tries to carry 'Disallow: /' to production and is stopped by a red CI check. |
| 8 | 3:58 | 40 | Robots.txt now carries AI crawler decisions too. Training bots like GPTBot and ClaudeBot, control tokens like Google-Extended and Applebot-Extended, and AI search bots like OAI-SearchBot and PerplexityBot. Technically, treat each AI group like any other: it ignores the star group, so repeat shared disallows, and include it in your tests. The policy side is covered in the AI Search Optimization course. Now the hands-on. The lesson text includes a pytest file that fetches robots.txt, fails the build if a sitewide Disallow appears, and asserts which agents can and can't fetch key paths. | AI groups + hands-on tests<br>• Treat AI groups like any group<br>• pytest: fail on sitewide Disallow<br>• Assert must-allow and must-block paths | robots.txt with AI user-agent groups colour-coded. Then a CI pipeline runs 'pytest test_robots.py' with green passes and one red failure 'Googlebot blocked from /static/app.js'. |
| 9 | 4:38 | 25 | Worked example one, simple. A Birmingham law firm's developer blocks slash wp-content to hide uploads. That also blocks the theme's CSS and JavaScript, and URL Inspection shows an unstyled, broken render. The fix is removing that rule and, if some files must stay private, putting them behind authentication instead. Five minutes, and the page renders properly again. | Example 1: blocked CSS and JS<br>• Disallow /wp-content/ breaks rendering<br>• Remove the rule<br>• Protect private files with authentication | URL Inspection mock screenshot of an unstyled page; the robots rule is deleted and the screenshot re-renders styled. |
| 10 | 5:03 | 41 | Let's make testing concrete with Search Console. The robots.txt report shows which robots.txt files Google found for the top hosts in your property, when each was last fetched, and any fetch errors or parsing warnings. After any robots.txt change, check that Google has fetched the new version before you expect crawl behaviour to change. For individual URLs, URL Inspection tells you whether crawling is allowed. Combine those with your own automated tests and you have three layers: your CI test before deploy, Google's fetched version after deploy, and log evidence of how Googlebot actually behaves. | Three layers of robots testing<br>• CI test before deploy<br>• Search Console robots.txt report after deploy<br>• Logs for real behaviour | Three stacked layers light up in order: a CI pipeline, a Search Console report card showing 'Last fetched' and a log chart. |
| 11 | 5:44 | 33 | Worked example two, with illustrative details. A Karachi e-learning site added noindex to eight thousand thin tag pages and, in the same release, disallowed the tag folder in robots.txt to be thorough. Three months later, the pages were still indexed. Googlebot couldn't fetch them, so it never saw the noindex. The fix followed the correct sequence: remove the Disallow, keep noindex, confirm the URLs dropping out in Page indexing, and only then consider re-adding the Disallow. | Example 2: noindex that never worked (illustrative)<br>• Noindex + Disallow in one release<br>• Pages stayed indexed for months<br>• Remove Disallow → confirm drop-out → then decide | Line chart 'indexed tag pages' stays flat for three months, then drops after the Disallow is removed (annotation). |
| 12 | 6:17 | 39 | Common mistakes: blocking CSS and JavaScript to save budget, which breaks rendering. Assuming robots.txt hides confidential URLs, when it advertises them. Leaving noindex on templates after launch. And forgetting that a specific Googlebot group overrides the star group. Recap: use each tool for its job, respect grouping and precedence, never combine a block with a noindex you need Google to see, and protect staging properly. Try this now. Add the pytest file to your project, fill in five must-allow and three must-block paths, and run it against production today. | Mistakes, recap, try this now<br>• Right tool, right job; grouping; sequencing<br>• Protect staging<br>• Add the pytest file and run it today | Mistake chips fade; recap bullets tick; a terminal shows 'pytest: 8 passed'. End card: 'Next: Bing Webmaster Tools and IndexNow'. |

<details><summary>Narration per scene (paste into ElevenLabs)</summary>

Scene 1 — robots.txt, meta robots, X-Robots-Tag

```text
robots.txt, meta robots and the X-Robots-Tag header look simple. They're also behind some of the most expensive mistakes in SEO, including the one-line production Disallow that removes a whole site from crawling. In this lecture you'll learn which tool does which job, the rules that trip up experts, the classic blocked-plus-noindex trap, and how to put robots.txt into automated tests so a bad deploy can't slip through.
```

Scene 2 — Right tool, right job

```text
Start with the job, not the tool. To stop crawling of a path, use robots.txt Disallow, but it doesn't guarantee de-indexing. To keep a crawlable page out of the index, use a meta robots noindex, and don't block it in robots.txt. For non-HTML files like PDFs, use the X-Robots-Tag HTTP header. To consolidate duplicates, use canonicals or redirects. For an urgent temporary removal, use Search Console's Removals tool, which lasts about six months. And to protect private content, use authentication, because robots.txt is public and advertises your paths.
```

Scene 3 — robots.txt fundamentals

```text
Now robots.txt fundamentals. It lives at the root of each host and protocol, so www and a shop subdomain each need their own. It follows RFC ninety-three-oh-nine. Four rules matter most. Grouping: a crawler obeys only the most specific user-agent group that matches it, so a Googlebot group means Googlebot ignores the star group entirely. Precedence: for Google, the longest matching rule wins, and if Allow and Disallow tie, Allow wins. Wildcards: star matches any sequence and dollar anchors the end. And paths are case-sensitive.
```

Scene 4 — Facts people forget

```text
A few more facts people forget. Google processes only the first five hundred kibibytes of robots.txt. Google doesn't support noindex, crawl-delay or nofollow lines in robots.txt, although Bing does support crawl-delay. And error handling matters. If robots.txt returns a four-hundred-level error, Google treats it as no restrictions. If it returns five-hundred-level errors for an extended period, Google may stop crawling the site. So a broken robots.txt server can quietly halt crawling.
```

Scene 5 — Index directives

```text
Meta robots and X-Robots-Tag carry the index directives. Common values: noindex, nofollow, none, nosnippet, max-snippet, max-image-preview, max-video-preview, noimageindex and unavailable after. There's also the data-nosnippet attribute to exclude a section from snippets. Snippet controls also apply to how Google may show your content in AI features, but they're blunt, because you lose normal snippets too. Since twenty twenty-six, Google also has a Search Console setting that excludes a site from AI Overviews and AI Mode without touching snippets. For PDFs, the lesson text has Apache and Nginx examples for an X-Robots-Tag noindex header.
```

Scene 6 — The blocked + noindex trap

```text
Now the classic trap. You Disallow a path in robots.txt and add noindex to the pages. Google can't crawl them, so it never sees the noindex, and it may keep the URLs indexed from links. The correct sequence is: allow crawling, serve noindex, wait until the URLs drop out, confirmed in URL Inspection or the Page indexing report, and only then consider a Disallow if you also want to stop crawling. Order matters.
```

Scene 7 — Staging sites

```text
Staging sites deserve their own warning. They get indexed constantly because someone links to them or a sitemap leaks. The robust approach is HTTP authentication or IP allow-listing, with a global X-Robots-Tag noindex header as a second layer. Don't rely on Disallow slash alone. And make sure deploy scripts can't copy staging robots rules to production, because a production Disallow slash is one of the most damaging one-line mistakes in SEO.
```

Scene 8 — AI groups + hands-on tests

```text
Robots.txt now carries AI crawler decisions too. Training bots like GPTBot and ClaudeBot, control tokens like Google-Extended and Applebot-Extended, and AI search bots like OAI-SearchBot and PerplexityBot. Technically, treat each AI group like any other: it ignores the star group, so repeat shared disallows, and include it in your tests. The policy side is covered in the AI Search Optimization course. Now the hands-on. The lesson text includes a pytest file that fetches robots.txt, fails the build if a sitewide Disallow appears, and asserts which agents can and can't fetch key paths.
```

Scene 9 — Example 1: blocked CSS and JS

```text
Worked example one, simple. A Birmingham law firm's developer blocks slash wp-content to hide uploads. That also blocks the theme's CSS and JavaScript, and URL Inspection shows an unstyled, broken render. The fix is removing that rule and, if some files must stay private, putting them behind authentication instead. Five minutes, and the page renders properly again.
```

Scene 10 — Three layers of robots testing

```text
Let's make testing concrete with Search Console. The robots.txt report shows which robots.txt files Google found for the top hosts in your property, when each was last fetched, and any fetch errors or parsing warnings. After any robots.txt change, check that Google has fetched the new version before you expect crawl behaviour to change. For individual URLs, URL Inspection tells you whether crawling is allowed. Combine those with your own automated tests and you have three layers: your CI test before deploy, Google's fetched version after deploy, and log evidence of how Googlebot actually behaves.
```

Scene 11 — Example 2: noindex that never worked (illustrative)

```text
Worked example two, with illustrative details. A Karachi e-learning site added noindex to eight thousand thin tag pages and, in the same release, disallowed the tag folder in robots.txt to be thorough. Three months later, the pages were still indexed. Googlebot couldn't fetch them, so it never saw the noindex. The fix followed the correct sequence: remove the Disallow, keep noindex, confirm the URLs dropping out in Page indexing, and only then consider re-adding the Disallow.
```

Scene 12 — Mistakes, recap, try this now

```text
Common mistakes: blocking CSS and JavaScript to save budget, which breaks rendering. Assuming robots.txt hides confidential URLs, when it advertises them. Leaving noindex on templates after launch. And forgetting that a specific Googlebot group overrides the star group. Recap: use each tool for its job, respect grouping and precedence, never combine a block with a noindex you need Google to see, and protect staging properly. Try this now. Add the pytest file to your project, fill in five must-allow and three must-block paths, and run it against production today.
```

</details>

### 5. Beyond Google: Bing Webmaster Tools, IndexNow and AI-era crawling

Lesson `bing-webmaster-tools-and-indexnow` · module “Crawl budget, robots.txt and indexing directives” · target 6 min · 11 scenes · 898 words · planned 6:31 (words ≈ 6:30) · status: **to produce**

| Term | Say |
|---|---|
| IndexNow | index now |
| Seznam | seh-znam |
| Naver | nay-ver |
| Yandex | yan-dex |
| JSON | jason |

| # | Time | Sec | Narration | On screen | Visual |
|---|---|---|---|---|---|
| 1 | 0:00 | 28 | Most technical SEOs live inside Google Search Console. That's understandable. But in twenty twenty-six, ignoring Bing is a mistake, because Bing's index powers Microsoft Copilot's web grounding, sits behind other search products, and now reports AI citations directly. In this lecture you'll set up Bing Webmaster Tools properly, implement IndexNow to push changes instantly to participating engines, and handle the technical side of AI-era crawling. | Beyond Google<br>• Bing Webmaster Tools, set up properly<br>• IndexNow: push, don't wait<br>• AI-era crawling, technically | A single Google-style console tab expands into a row of tabs: Bing Webmaster Tools, IndexNow, CDN bot analytics. Title appears. |
| 2 | 0:28 | 39 | Why Bing? Its share of classic search varies by market, but its reach goes further than bing.com. It grounds Copilot's answers, and it has fed other assistants over the years. And since February twenty twenty-six, Bing Webmaster Tools has an AI Performance report showing how often your pages are cited in Copilot and Bing AI answers, which pages, and the grounding queries used to find them. That's data Google doesn't give you. So if you care about AI visibility, Bing is suddenly one of your best sources of evidence. | Why Bing matters<br>• Grounds Copilot's answers<br>• Feeds other products and partners<br>• AI Performance: citations + grounding queries | Bing index icon at the centre with arrows to 'Copilot', 'Partners', 'bing.com'. A mock AI Performance panel slides in with a citations line chart. |
| 3 | 1:07 | 34 | Setup takes about fifteen minutes. The fastest route is importing your sites from Search Console. Then submit your sitemap index and check the Sitemaps report for errors. Use Site Explorer and URL Inspection to see index status and crawl details. Review crawl settings: Bing lets you shape crawl rate by time of day, and unlike Google, Bingbot supports crawl-delay in robots.txt. Then review the SEO reports, backlinks and AI Performance monthly, and add team members with sensible roles. | Setup in 15 minutes<br>• Import from Search Console<br>• Sitemaps, Site Explorer, URL Inspection<br>• Crawl settings; monthly reports | Screen-recording style mock: an 'Import from Google Search Console' button, then a sitemap submission field, then a crawl-rate time-of-day slider. |
| 4 | 1:41 | 34 | Now IndexNow. Think of the difference between waiting for the postman to check whether you have letters, and ringing the sorting office to say, I've just posted three. IndexNow is an open protocol that lets you notify participating search engines immediately when URLs are added, updated or deleted. Participants include Bing, Yandex, Seznam, Naver and Yep, and a submission to one is shared with the others. One important caveat: Google doesn't use IndexNow. It relies on crawling and sitemaps. | IndexNow<br>• Push notifications for changed URLs<br>• Bing, Yandex, Seznam, Naver, Yep (shared)<br>• Not Google | Postman analogy: a waiting postman vs a phone call to a sorting office. Then a single submission arrow fans out to five engine icons; a Google-style icon sits aside with 'not participating'. |
| 5 | 2:15 | 42 | Here's how it works. Generate a key, eight to one hundred and twenty-eight characters of letters, numbers and dashes. Host it as a text file at your site root, containing only the key, so engines can confirm you own the site. Then, when URLs change, POST a JSON payload with your host, key, key location and a URL list, up to ten thousand URLs per request, to an IndexNow endpoint. The lesson text has a short Python example that reads the key from an environment variable and prints the response. Two hundred or two-oh-two means accepted. | How IndexNow works<br>• Key file at your site root<br>• POST host, key, keyLocation, urlList<br>• 200/202 = accepted | Code walkthrough: the key file URL, then the payload dictionary with each field highlighted, then requests.post and a '202 Accepted' response badge. |
| 6 | 2:57 | 37 | Operational rules matter more than the code. Submit only URLs that actually changed: new pages, meaningful updates, and deletions or redirects. Don't resubmit your whole site every day. Trigger it from your CMS or deploy pipeline, and check first whether your CMS or CDN already has built-in IndexNow support. Log every submission and its response, and alert on repeated four-hundred-level errors, which usually mean the key file, host or URL list is wrong. And keep your sitemaps accurate anyway. IndexNow complements sitemaps; it doesn't replace them. | Operational rules<br>• Only changed URLs<br>• Trigger from CMS/deploy; check built-in support<br>• Log responses; keep sitemaps accurate | A CMS 'Publish' button triggers a small IndexNow event in a log table; a row with '403' turns red and triggers an alert bell. |
| 7 | 3:34 | 38 | Next, AI-era crawling from a technical point of view. AI crawlers add load and new failure modes. Some are bursty, so monitor request rates by user agent and use CDN rate limits for abusive patterns rather than blanket blocks. Verify identities with published IP ranges or reverse DNS where providers document them. Keep server-rendered HTML lean, because many AI crawlers don't run JavaScript. And align your CDN bot settings with your written policy, because block AI bots toggles can silently block search-oriented AI crawlers you actually want. | AI-era crawling, technically<br>• Monitor rates by UA; rate-limit abuse<br>• Verify identities<br>• Lean SSR HTML; CDN settings = policy | Stacked area chart of requests by bot type with a spike from one AI crawler; a rate-limit line caps it. A CDN toggle panel shows 'Block AI bots' being reviewed against a policy document. |
| 8 | 4:12 | 28 | Worked example one, simple. A Manchester dental practice's site has never been verified in Bing. Importing from Search Console takes two minutes. Bing's Sitemaps report immediately flags that the sitemap lists old HTTP URLs from before the site moved to HTTPS. Fixing the sitemap helps both Bing and Google. A good reminder that a second tool often spots problems the first one didn't highlight. | Example 1: Manchester dental practice<br>• Imported into Bing in 2 minutes<br>• Sitemap lists old http:// URLs<br>• Fix helps Bing and Google | Import dialog completes; Sitemaps report shows a warning row 'http:// URLs'; the sitemap updates to https and the warning clears. |
| 9 | 4:40 | 39 | Worked example two, with illustrative details. A Lahore Urdu news portal publishes around one hundred and fifty stories a day. Google finds them quickly through the news sitemap, but Bing is slower and Copilot rarely cites the portal. The team imports the property into Bing Webmaster Tools, fixes sitemap errors Bing reports, including relative URLs in one sitemap, and adds IndexNow calls to the CMS publish and update hooks. Logs then show Bingbot fetching new stories much sooner, and the AI Performance report starts listing citations for explainer articles. | Example 2: Lahore news portal (illustrative)<br>• Slow Bing discovery; few Copilot citations<br>• Fix sitemaps; IndexNow on publish/update<br>• Faster Bingbot fetches; first citations | Timeline comparing 'publish → first Bingbot hit' before and after (illustrative), and an AI Performance tile ticking up from zero. |
| 10 | 5:19 | 37 | How do you measure success? For Bing: indexed pages versus submitted, time from publishing to the first Bingbot hit in your logs, and the AI Performance citations trend. For IndexNow: the share of submissions accepted, and errors per week. For AI crawling: request rates and status codes by bot, with no unintended blocks. Common mistakes: ignoring Bing because nobody uses it, pinging IndexNow with every URL every day, expecting IndexNow to speed up Google, and letting CDN defaults decide which AI crawlers are allowed. | Measures and mistakes<br>• Bing indexing, time-to-crawl, AI citations<br>• IndexNow acceptance and errors<br>• Don't ignore Bing; don't spam IndexNow | KPI tiles on the left; mistake chips on the right being struck through. |
| 11 | 5:56 | 35 | Recap. Bing's index powers Copilot and more, and its Webmaster Tools now shows AI citations and grounding queries. IndexNow pushes changed URLs to Bing and other participants instantly, but not to Google. AI crawlers need monitoring, verification and deliberate CDN settings. Try this now. Import your site into Bing Webmaster Tools, check the Sitemaps and AI Performance reports, and find out whether your CMS already supports IndexNow. If it does, switch it on and watch the submission log for a week. | Recap and try this now<br>• Import into Bing Webmaster Tools<br>• Check Sitemaps + AI Performance<br>• Enable IndexNow; watch the log | Three-step checklist ticks; end card: 'Next module: status codes, redirects and duplicates'. |

<details><summary>Narration per scene (paste into ElevenLabs)</summary>

Scene 1 — Beyond Google

```text
Most technical SEOs live inside Google Search Console. That's understandable. But in twenty twenty-six, ignoring Bing is a mistake, because Bing's index powers Microsoft Copilot's web grounding, sits behind other search products, and now reports AI citations directly. In this lecture you'll set up Bing Webmaster Tools properly, implement IndexNow to push changes instantly to participating engines, and handle the technical side of AI-era crawling.
```

Scene 2 — Why Bing matters

```text
Why Bing? Its share of classic search varies by market, but its reach goes further than bing.com. It grounds Copilot's answers, and it has fed other assistants over the years. And since February twenty twenty-six, Bing Webmaster Tools has an AI Performance report showing how often your pages are cited in Copilot and Bing AI answers, which pages, and the grounding queries used to find them. That's data Google doesn't give you. So if you care about AI visibility, Bing is suddenly one of your best sources of evidence.
```

Scene 3 — Setup in 15 minutes

```text
Setup takes about fifteen minutes. The fastest route is importing your sites from Search Console. Then submit your sitemap index and check the Sitemaps report for errors. Use Site Explorer and URL Inspection to see index status and crawl details. Review crawl settings: Bing lets you shape crawl rate by time of day, and unlike Google, Bingbot supports crawl-delay in robots.txt. Then review the SEO reports, backlinks and AI Performance monthly, and add team members with sensible roles.
```

Scene 4 — IndexNow

```text
Now IndexNow. Think of the difference between waiting for the postman to check whether you have letters, and ringing the sorting office to say, I've just posted three. IndexNow is an open protocol that lets you notify participating search engines immediately when URLs are added, updated or deleted. Participants include Bing, Yandex, Seznam, Naver and Yep, and a submission to one is shared with the others. One important caveat: Google doesn't use IndexNow. It relies on crawling and sitemaps.
```

Scene 5 — How IndexNow works

```text
Here's how it works. Generate a key, eight to one hundred and twenty-eight characters of letters, numbers and dashes. Host it as a text file at your site root, containing only the key, so engines can confirm you own the site. Then, when URLs change, POST a JSON payload with your host, key, key location and a URL list, up to ten thousand URLs per request, to an IndexNow endpoint. The lesson text has a short Python example that reads the key from an environment variable and prints the response. Two hundred or two-oh-two means accepted.
```

Scene 6 — Operational rules

```text
Operational rules matter more than the code. Submit only URLs that actually changed: new pages, meaningful updates, and deletions or redirects. Don't resubmit your whole site every day. Trigger it from your CMS or deploy pipeline, and check first whether your CMS or CDN already has built-in IndexNow support. Log every submission and its response, and alert on repeated four-hundred-level errors, which usually mean the key file, host or URL list is wrong. And keep your sitemaps accurate anyway. IndexNow complements sitemaps; it doesn't replace them.
```

Scene 7 — AI-era crawling, technically

```text
Next, AI-era crawling from a technical point of view. AI crawlers add load and new failure modes. Some are bursty, so monitor request rates by user agent and use CDN rate limits for abusive patterns rather than blanket blocks. Verify identities with published IP ranges or reverse DNS where providers document them. Keep server-rendered HTML lean, because many AI crawlers don't run JavaScript. And align your CDN bot settings with your written policy, because block AI bots toggles can silently block search-oriented AI crawlers you actually want.
```

Scene 8 — Example 1: Manchester dental practice

```text
Worked example one, simple. A Manchester dental practice's site has never been verified in Bing. Importing from Search Console takes two minutes. Bing's Sitemaps report immediately flags that the sitemap lists old HTTP URLs from before the site moved to HTTPS. Fixing the sitemap helps both Bing and Google. A good reminder that a second tool often spots problems the first one didn't highlight.
```

Scene 9 — Example 2: Lahore news portal (illustrative)

```text
Worked example two, with illustrative details. A Lahore Urdu news portal publishes around one hundred and fifty stories a day. Google finds them quickly through the news sitemap, but Bing is slower and Copilot rarely cites the portal. The team imports the property into Bing Webmaster Tools, fixes sitemap errors Bing reports, including relative URLs in one sitemap, and adds IndexNow calls to the CMS publish and update hooks. Logs then show Bingbot fetching new stories much sooner, and the AI Performance report starts listing citations for explainer articles.
```

Scene 10 — Measures and mistakes

```text
How do you measure success? For Bing: indexed pages versus submitted, time from publishing to the first Bingbot hit in your logs, and the AI Performance citations trend. For IndexNow: the share of submissions accepted, and errors per week. For AI crawling: request rates and status codes by bot, with no unintended blocks. Common mistakes: ignoring Bing because nobody uses it, pinging IndexNow with every URL every day, expecting IndexNow to speed up Google, and letting CDN defaults decide which AI crawlers are allowed.
```

Scene 11 — Recap and try this now

```text
Recap. Bing's index powers Copilot and more, and its Webmaster Tools now shows AI citations and grounding queries. IndexNow pushes changed URLs to Bing and other participants instantly, but not to Google. AI crawlers need monitoring, verification and deliberate CDN settings. Try this now. Import your site into Bing Webmaster Tools, check the Sitemaps and AI Performance reports, and find out whether your CMS already supports IndexNow. If it does, switch it on and watch the submission log for a week.
```

</details>

### 6. Status codes, redirects and soft 404s

Lesson `status-codes-redirects-soft-404s` · module “Status codes, redirects, canonicals and duplicate URLs” · target 7 min · 12 scenes · 944 words · planned 6:51 (words ≈ 6:50) · status: **to produce**

| Term | Say |
|---|---|
| HEAD | head request |
| Retry-After | retry after |
| SPA | S P A |

| # | Time | Sec | Narration | On screen | Visual |
|---|---|---|---|---|---|
| 1 | 0:00 | 37 | Before a search engine reads a single word on your page, it reads a three-digit number. The HTTP status code. Get it right and you tell Google exactly what to do with the URL and its signals. Get it wrong and you create soft four-oh-fours, wasted crawl and lost link equity. In this lecture you'll learn what each status code tells Google, how to choose the right redirect, how to find chains and loops with a short script, and how to fix soft four-oh-fours at scale. | Status codes, redirects and soft 404s<br>• What each code tells Google<br>• Choosing the right redirect<br>• Chains, loops and soft 404s | A three-digit number '200' flips like a departure board to '301', '404', '503'. Title appears. |
| 2 | 0:37 | 34 | Here's the quick map. Two hundred means OK, eligible for indexing. Three-oh-one and three-oh-eight are permanent moves and strong signals that the target should be canonical. Three-oh-two and three-oh-seven are temporary, weaker canonical signals, though if they persist Google may treat them as permanent. Three-oh-four means not modified. Four-oh-four and four-ten mean gone; the URL drops out over time, and there's no penalty for having them. Four-twenty-nine and five hundreds slow crawling, and prolonged errors can drop URLs. | The status map<br>• 200 OK; 301/308 permanent; 302/307 temporary<br>• 304 not modified; 404/410 gone<br>• 429/5xx slow crawling | Departure-board style table animates row by row, each code with a small icon: tick, arrow, clock arrow, cache, bin, traffic cone. |
| 3 | 1:11 | 23 | Here's the key insight people miss. Four-oh-fours are normal and healthy for content that no longer exists. You don't need to redirect every dead URL. The problem is four-oh-fours on URLs that should exist, or dead URLs that have valuable backlinks or traffic. Those deserve a redirect to the most relevant equivalent. | 404s are normal<br>• Healthy for genuinely gone content<br>• Problem: 404s that should exist<br>• Or dead URLs with links or traffic | A garden of URLs; some wilted '404' plants are left alone, but one with backlink vines attached gets transplanted via a '301' arrow. |
| 4 | 1:34 | 36 | Choosing the redirect. For a permanent move, like a URL change, HTTPS, a domain move or merged content, use three-oh-one or three-oh-eight. For something genuinely temporary, like a short promotion or a test where the original should stay canonical, use three-oh-two or three-oh-seven. For planned downtime, use five-oh-three with a Retry-After header, not a redirect to a maintenance page that returns two hundred. And avoid meta refresh and JavaScript redirects when a server redirect is possible, because they're slower and weaker signals. | Choosing the redirect<br>• Permanent: 301/308<br>• Temporary: 302/307<br>• Downtime: 503 + Retry-After<br>• Avoid meta refresh / JS redirects | Decision tree: 'Is the move permanent?' → 301/308; 'Temporary?' → 302/307; 'Planned downtime?' → 503 with a clock icon. |
| 5 | 2:10 | 36 | Chains and loops. A chain is A to B to C. Google documents following up to ten hops, but every hop costs crawl resources, adds latency for users, and adds risk. Best practice: redirect every legacy URL directly to its final destination in one hop. Update internal links, canonicals, hreflang and sitemaps to point at final URLs so users and bots rarely hit redirects at all. And when you add a new redirect, rewrite older rules that pointed at the now-redirected URL. | Chains and loops<br>• One hop to the final URL<br>• Update links, canonicals, hreflang, sitemaps<br>• Rewrite old rules when adding new ones | A → B → C chain collapses into a single A → C arrow; internal link lines re-point directly to C. |
| 6 | 2:46 | 34 | Hands-on. The lesson text has a Python script that traces redirects hop by hop for a list of URLs. It uses HEAD requests, falls back to GET if the server rejects HEAD, follows Location headers, detects loops, and prints the final URL, its status, the number of hops, the redirect types and a problem flag: chain, loop, temporary or error. Run it on legacy URLs, on internal links that point at redirects, and on your top backlink targets. | Hands-on: the redirect tracer<br>• Hop-by-hop HEAD/GET<br>• Detects chains, loops, 302s, errors<br>• Run on legacy, internal and backlink URLs | Code walkthrough of the trace() function; output CSV rows appear with problem flags highlighted in amber and red. |
| 7 | 3:20 | 41 | Now soft four-oh-fours. A soft four-oh-four is a URL that returns two hundred but looks like an error or an empty page. No products found. This item is unavailable. A thin template with no main content. Or a redirect to something irrelevant. Search Console reports them in Page indexing. Fixes depend on the cause. Deleted product: return four-oh-four or four-ten, or redirect to a close replacement. Empty filter results: four-oh-four. Out of stock but returning: keep two hundred with useful content like alternatives and a restock date. Single-page app error views: a real server four-oh-four. | Soft 404s<br>• 200 status, error-like content<br>• Deleted → 404/410 or relevant 301<br>• Out of stock → useful 200; SPA → real 404 | A page labelled '200 OK' displays 'Sorry, not found'; a detector stamps 'soft 404'. Then a fix table slides in with four rows. |
| 8 | 4:01 | 36 | Worked example one, with illustrative details. A UK retailer's audit finds twelve thousand discontinued product URLs returning two hundred with sorry, this product is no longer available. Some have backlinks. The plan: pull backlink data and Search Console clicks per URL; redirect those with links or traffic to the closest current product or the parent category, only where genuinely relevant; return four-ten for the rest; remove them from sitemaps and internal links; and watch the soft four-oh-four count trend down in Search Console. | Example 1: 12,000 discontinued products (illustrative)<br>• Links/traffic → relevant 301<br>• The rest → 410<br>• Clean sitemaps and links; monitor soft 404s | A large grid of product tiles splits into two groups: tiles with link icons get green 301 arrows to relevant products; the rest get '410' tags. A line chart of soft 404s declines. |
| 9 | 4:37 | 40 | A quick word on redirect mapping principles, because this is where most redirect work goes wrong. Map each old URL to the most relevant equivalent, not the home page. Mass-redirecting unrelated URLs to the home page is typically treated as a soft four-oh-four, so you gain nothing. If there's genuinely no equivalent, a four-oh-four or four-ten is honest and perfectly acceptable. And keep redirects in place for the long term. Google's migration guidance suggests at least a year, and many practitioners keep them indefinitely, because external links and bookmarks persist for years. | Redirect mapping principles<br>• Most relevant equivalent, not the home page<br>• No equivalent → honest 404/410<br>• Keep redirects at least a year | Mapping table: old URLs on the left, arrows to closely matching new URLs; one arrow aimed at the home page turns red and is replaced by a '410' tag. |
| 10 | 5:17 | 35 | Worked example two, also illustrative. A Doha electronics retailer takes its store offline for a four-hour upgrade and redirects every URL with a three-oh-two to a maintenance page that returns two hundred. Afterwards, Search Console shows spikes in soft four-oh-fours, and some product results temporarily show the maintenance page. Next time, the right pattern is a five-oh-three with Retry-After on the original URLs, a short window, and never a two hundred we'll be back page in place of real content. | Example 2: maintenance done wrong (illustrative)<br>• 302 → '/maintenance' (200)<br>• Soft 404 spike; wrong pages in results<br>• Next time: 503 + Retry-After | Before: every URL arrow points to a single maintenance page with a '200'. After: each URL shows its own '503 Retry-After: 14400' badge. |
| 11 | 5:52 | 33 | Common mistakes. Using three-oh-twos for permanent moves because it's the platform default, so check your CMS or plugin. Redirecting everything to the home page, which Google typically treats as a soft four-oh-four. Custom error pages that return two hundred. And letting redirect rules pile up for years without flattening chains. How do you measure success? Soft four-oh-fours trending down, zero chains on priority URLs, and legacy URLs with backlinks resolving in one hop to relevant two-hundred pages. | Mistakes and measures<br>• 302 defaults; home-page redirects<br>• Error pages returning 200; piled-up rules<br>• KPIs: soft 404s down, zero chains | Mistake chips fade; three KPI tiles fill in with illustrative targets. |
| 12 | 6:25 | 26 | Recap. Status codes are instructions. Use permanent redirects for permanent moves, five-oh-three for downtime, and honest four-oh-fours for gone content. Flatten chains to one hop and fix soft four-oh-fours by cause. Try this now. Export your top one hundred backlink target URLs, run them through the redirect tracer, and ticket every chain, loop, temporary redirect and error you find. | Recap and try this now<br>• Status codes are instructions<br>• One hop; honest 404s; 503 for downtime<br>• Trace your top 100 backlink targets | Tracer output collapses into a small ticket list. End card: 'Next: canonicalisation and duplicate content'. |

<details><summary>Narration per scene (paste into ElevenLabs)</summary>

Scene 1 — Status codes, redirects and soft 404s

```text
Before a search engine reads a single word on your page, it reads a three-digit number. The HTTP status code. Get it right and you tell Google exactly what to do with the URL and its signals. Get it wrong and you create soft four-oh-fours, wasted crawl and lost link equity. In this lecture you'll learn what each status code tells Google, how to choose the right redirect, how to find chains and loops with a short script, and how to fix soft four-oh-fours at scale.
```

Scene 2 — The status map

```text
Here's the quick map. Two hundred means OK, eligible for indexing. Three-oh-one and three-oh-eight are permanent moves and strong signals that the target should be canonical. Three-oh-two and three-oh-seven are temporary, weaker canonical signals, though if they persist Google may treat them as permanent. Three-oh-four means not modified. Four-oh-four and four-ten mean gone; the URL drops out over time, and there's no penalty for having them. Four-twenty-nine and five hundreds slow crawling, and prolonged errors can drop URLs.
```

Scene 3 — 404s are normal

```text
Here's the key insight people miss. Four-oh-fours are normal and healthy for content that no longer exists. You don't need to redirect every dead URL. The problem is four-oh-fours on URLs that should exist, or dead URLs that have valuable backlinks or traffic. Those deserve a redirect to the most relevant equivalent.
```

Scene 4 — Choosing the redirect

```text
Choosing the redirect. For a permanent move, like a URL change, HTTPS, a domain move or merged content, use three-oh-one or three-oh-eight. For something genuinely temporary, like a short promotion or a test where the original should stay canonical, use three-oh-two or three-oh-seven. For planned downtime, use five-oh-three with a Retry-After header, not a redirect to a maintenance page that returns two hundred. And avoid meta refresh and JavaScript redirects when a server redirect is possible, because they're slower and weaker signals.
```

Scene 5 — Chains and loops

```text
Chains and loops. A chain is A to B to C. Google documents following up to ten hops, but every hop costs crawl resources, adds latency for users, and adds risk. Best practice: redirect every legacy URL directly to its final destination in one hop. Update internal links, canonicals, hreflang and sitemaps to point at final URLs so users and bots rarely hit redirects at all. And when you add a new redirect, rewrite older rules that pointed at the now-redirected URL.
```

Scene 6 — Hands-on: the redirect tracer

```text
Hands-on. The lesson text has a Python script that traces redirects hop by hop for a list of URLs. It uses HEAD requests, falls back to GET if the server rejects HEAD, follows Location headers, detects loops, and prints the final URL, its status, the number of hops, the redirect types and a problem flag: chain, loop, temporary or error. Run it on legacy URLs, on internal links that point at redirects, and on your top backlink targets.
```

Scene 7 — Soft 404s

```text
Now soft four-oh-fours. A soft four-oh-four is a URL that returns two hundred but looks like an error or an empty page. No products found. This item is unavailable. A thin template with no main content. Or a redirect to something irrelevant. Search Console reports them in Page indexing. Fixes depend on the cause. Deleted product: return four-oh-four or four-ten, or redirect to a close replacement. Empty filter results: four-oh-four. Out of stock but returning: keep two hundred with useful content like alternatives and a restock date. Single-page app error views: a real server four-oh-four.
```

Scene 8 — Example 1: 12,000 discontinued products (illustrative)

```text
Worked example one, with illustrative details. A UK retailer's audit finds twelve thousand discontinued product URLs returning two hundred with sorry, this product is no longer available. Some have backlinks. The plan: pull backlink data and Search Console clicks per URL; redirect those with links or traffic to the closest current product or the parent category, only where genuinely relevant; return four-ten for the rest; remove them from sitemaps and internal links; and watch the soft four-oh-four count trend down in Search Console.
```

Scene 9 — Redirect mapping principles

```text
A quick word on redirect mapping principles, because this is where most redirect work goes wrong. Map each old URL to the most relevant equivalent, not the home page. Mass-redirecting unrelated URLs to the home page is typically treated as a soft four-oh-four, so you gain nothing. If there's genuinely no equivalent, a four-oh-four or four-ten is honest and perfectly acceptable. And keep redirects in place for the long term. Google's migration guidance suggests at least a year, and many practitioners keep them indefinitely, because external links and bookmarks persist for years.
```

Scene 10 — Example 2: maintenance done wrong (illustrative)

```text
Worked example two, also illustrative. A Doha electronics retailer takes its store offline for a four-hour upgrade and redirects every URL with a three-oh-two to a maintenance page that returns two hundred. Afterwards, Search Console shows spikes in soft four-oh-fours, and some product results temporarily show the maintenance page. Next time, the right pattern is a five-oh-three with Retry-After on the original URLs, a short window, and never a two hundred we'll be back page in place of real content.
```

Scene 11 — Mistakes and measures

```text
Common mistakes. Using three-oh-twos for permanent moves because it's the platform default, so check your CMS or plugin. Redirecting everything to the home page, which Google typically treats as a soft four-oh-four. Custom error pages that return two hundred. And letting redirect rules pile up for years without flattening chains. How do you measure success? Soft four-oh-fours trending down, zero chains on priority URLs, and legacy URLs with backlinks resolving in one hop to relevant two-hundred pages.
```

Scene 12 — Recap and try this now

```text
Recap. Status codes are instructions. Use permanent redirects for permanent moves, five-oh-three for downtime, and honest four-oh-fours for gone content. Flatten chains to one hop and fix soft four-oh-fours by cause. Try this now. Export your top one hundred backlink target URLs, run them through the redirect tracer, and ticket every chain, loop, temporary redirect and error you find.
```

</details>

### 7. Canonicalisation and duplicate content

Lesson `canonicalisation-and-duplicate-content` · module “Status codes, redirects, canonicals and duplicate URLs” · target 7 min · 13 scenes · 1041 words · planned 7:32 (words ≈ 7:33) · status: **to produce**

| Term | Say |
|---|---|
| rel canonical | rel canonical |
| pandas | pandas |
| Umrah | oom-rah |

| # | Time | Sec | Narration | On screen | Visual |
|---|---|---|---|---|---|
| 1 | 0:00 | 34 | Here's a myth to retire on day one. Duplicate content is not, by itself, a penalty. Google has been consistent about that. But duplicates still cost you. Signals get split, Google may show the wrong URL, and crawl resources are wasted. In this lecture you'll learn where duplicates come from, which canonical signals Google weighs, how to implement rel canonical correctly, how to audit canonicals at scale with pandas, and when canonical tags simply can't fix the problem. | Canonicalisation and duplicate content<br>• Not a penalty, but a real cost<br>• Canonical signals and rules<br>• Auditing at scale | Several near-identical page cards drift apart with link-signal lines split between them; they then snap together into one card with a crown labelled 'canonical'. |
| 2 | 0:34 | 34 | The real costs of duplicates are practical. Links and other signals get split across URL variants. Google may choose a different canonical than you want, so the wrong URL shows in results. And crawl resources get wasted fetching the same content again and again. Canonicalisation is simply telling search engines which URL in a cluster of duplicates is the representative one. Deliberately deceptive or scraped duplication is a different matter, but ordinary site duplication is a housekeeping job. | Why duplicates cost you<br>• Split signals<br>• Wrong URL in results<br>• Wasted crawl | Three cost icons appear one by one: a split arrow, a wrong URL in a results list, and a fuel gauge draining. |
| 3 | 1:08 | 30 | Where do duplicates come from? Protocol and host variants, like http and https, www and non-www. Trailing slashes and uppercase letters. Parameters for tracking, sorting and sessions. Index files like index dot html. Faceted paths reachable in different orders. Print or alternate views. Products that live in several categories. Syndicated articles. And near-duplicates, like city pages where only the town name changes. Most sites have at least five of these. | Where duplicates come from<br>• Host/protocol, slash/case, parameters<br>• Index files, facets, alternate views<br>• Multi-category products, syndication, near-duplicates | A single product page spawns nine URL variants in a fan, each labelled with its source type. |
| 4 | 1:38 | 35 | Google combines canonical signals, and none is an absolute directive. From strongest to weakest: redirects, where the source effectively disappears; rel canonical in the HTML head or HTTP header, a strong hint; internal linking consistency, meaning which variant you link to; sitemap inclusion, a weak hint; and preferences such as HTTPS over HTTP and cleaner URLs. When signals conflict, say the canonical says A but internal links and the sitemap say B, Google may pick B. Consistency is the whole game. | Canonical signals, strongest first<br>• Redirects<br>• rel=canonical<br>• Internal links, sitemaps, HTTPS/clean URLs | Signal-strength bars from tall to short; then a tug-of-war between 'canonical → A' and 'links + sitemap → B', with B winning. |
| 5 | 2:13 | 43 | Implementing rel canonical correctly. Use absolute URLs with the right protocol and host. Put it in the head, because a canonical in the body is ignored, and watch out for invalid elements that close the head early. Use one canonical per page. Self-reference on canonical pages. Make sure the target is indexable: two hundred, not noindexed, not blocked, not redirected. Don't canonicalise paginated pages to page one; they're not duplicates. Don't combine noindex with a canonical pointing elsewhere. And for syndication, ask partners for a cross-domain canonical plus a clear link back, knowing Google may not always honour it. | rel=canonical rules<br>• Absolute, in the head, one per page, self-referencing<br>• Target must be indexable<br>• Not for pagination; not with noindex | HTML head panel with a canonical link highlighted; a stray div injected above it closes the head early and pushes the canonical into the body with a red warning. |
| 6 | 2:56 | 27 | How do you check what Google chose? URL Inspection shows the user-declared canonical and the Google-selected canonical. In the Page indexing report, look at duplicate, Google chose different canonical than user, and duplicate without user-selected canonical. Sample URLs from each and ask one question: which signal is pulling Google the other way? It's usually internal links, sitemaps or redirects disagreeing with your tag. | What did Google choose?<br>• URL Inspection: declared vs selected<br>• Page indexing duplicate buckets<br>• Find the conflicting signal | URL Inspection mock with two fields, 'User-declared canonical' and 'Google-selected canonical', showing different URLs; a magnifier scans internal links and finds the culprit. |
| 7 | 3:23 | 35 | Now the hands-on. Export your crawl with address, status, indexability and canonical. The pandas script in the lesson text flags six problems: missing canonicals on two-hundred pages, relative canonicals, canonicals on the wrong host, canonicals pointing at non-two-hundred URLs, canonicals pointing at non-indexable URLs, and canonical chains, where A points to B and B points to C. It prints counts and writes the flagged rows to a CSV for tickets. For JavaScript sites, run it on both raw and rendered canonicals. | Hands-on: canonical audit script<br>• Six issue flags<br>• Counts + CSV for tickets<br>• Raw and rendered for JS sites | Code walkthrough of the flag columns; a summary table of six counts appears, then a CSV icon 'canonical_issues.csv'. |
| 8 | 3:58 | 31 | Worked example one, simple. A Sharjah furniture store's CMS sets every page's canonical to the home page after a theme update. Within weeks, product pages start dropping out of the index. The audit script flags thousands of canonical-to-home rows immediately. The fix is a template change to self-referencing canonicals, followed by monitoring Page indexing as products return. One bad default, huge impact, which is why canonical checks belong in release tests. | Example 1: every canonical → home page<br>• Theme update default<br>• Products fall out of the index<br>• Self-referencing template + release test | Many product cards with arrows all pointing at the home page; the template switch flips each arrow back to itself; a small indexed-pages chart recovers. |
| 9 | 4:29 | 42 | Worked example two, with illustrative details. An Islamabad travel agency has a hundred and forty Umrah packages from city pages that differ only by the city name. Google picks a handful as canonical and ignores the rest. Canonical tags can't fix this, because the pages are near-duplicates meant to rank separately. The agency keeps pages only for cities it genuinely departs from, with real schedules, local contacts and prices in rupees, consolidates the rest into one page with a city selector, and redirects the retired URLs. That also avoids doorway-style patterns that Google's spam policies target. | Example 2: near-duplicate city pages (illustrative)<br>• 140 pages, only the city changes<br>• Canonicals can't fix intent to rank separately<br>• Keep real departure cities; consolidate the rest | A grid of 140 identical cards with only the city label differing; most fade into one consolidated page with a city dropdown; a few remain with rich local details. |
| 10 | 5:11 | 43 | Let's walk through a canonical audit procedure you can reuse on any site. First, crawl with canonical extraction on, both raw and rendered if it's a JavaScript site. Second, flag missing canonicals, canonicals to non-two-hundred or noindexed URLs, chains, relative or wrong-host canonicals, and raw-versus-rendered mismatches. Third, compare canonical targets with internal link targets: are you linking to the non-canonical variant? Fourth, compare with the sitemap, where every URL should be self-canonical. And fifth, enforce host and protocol with sitewide redirects, and strip tracking parameters from internal links. Five steps, and most canonical problems surface in an afternoon. | A reusable canonical audit<br>• Crawl raw + rendered<br>• Flag the six problem types<br>• Compare with links and sitemaps; enforce host/protocol | Five-step numbered procedure animates; each step shows a small icon: crawler, flag, link, sitemap, redirect. |
| 11 | 5:54 | 39 | One more concept worth separating clearly: consolidation versus differentiation. If two URLs show the same thing, consolidate them with redirects or canonicals. If two URLs are meant to rank for different searches, they must be genuinely different, with unique, useful content. Canonical tags are a consolidation tool. They can't make near-identical pages rank separately, and trying to scale hundreds of barely different pages can drift into doorway territory under Google's spam policies. So for every duplicate cluster, ask: should these be one page, or should they be truly different pages? | Consolidate or differentiate?<br>• Same content → redirect/canonical<br>• Different intent → genuinely different pages<br>• Near-identical at scale → doorway risk | A fork in the road: left path 'Consolidate' merges cards into one; right path 'Differentiate' adds unique content blocks to each card. A warning sign 'doorway risk' on a third, shortcut path. |
| 12 | 6:33 | 30 | Common mistakes. Every page canonicalised to the home page. Canonicals pointing to URLs that redirect. Parameter variants linked internally, like sort orders in navigation, while the canonical says otherwise. And relying on canonical tags to fix a problem a redirect should solve, such as http versus https. Measure success by the share of sampled URLs where Google's selected canonical matches yours, and by shrinking duplicate buckets in Page indexing. | Mistakes and measures<br>• Canonical to home page; to redirects<br>• Linking non-canonical variants<br>• KPI: declared = selected canonical | Mistake chips; a KPI dial 'declared = selected' moves towards 100%. |
| 13 | 7:03 | 29 | Recap. Duplicates aren't a penalty, but they split signals and confuse selection. Redirects are the strongest signal, then rel canonical, then links and sitemaps. Keep every signal consistent, and don't expect canonicals to fix near-duplicate pages meant to rank separately. Try this now. Run the canonical audit script on your latest crawl, then use URL Inspection on five flagged URLs to see whether Google agrees with you. | Recap and try this now<br>• Consistency is the whole game<br>• Run the audit script<br>• Inspect 5 flagged URLs | Five URL Inspection cards flip over showing matched and mismatched canonicals. End card: 'Next: pagination and faceted navigation'. |

<details><summary>Narration per scene (paste into ElevenLabs)</summary>

Scene 1 — Canonicalisation and duplicate content

```text
Here's a myth to retire on day one. Duplicate content is not, by itself, a penalty. Google has been consistent about that. But duplicates still cost you. Signals get split, Google may show the wrong URL, and crawl resources are wasted. In this lecture you'll learn where duplicates come from, which canonical signals Google weighs, how to implement rel canonical correctly, how to audit canonicals at scale with pandas, and when canonical tags simply can't fix the problem.
```

Scene 2 — Why duplicates cost you

```text
The real costs of duplicates are practical. Links and other signals get split across URL variants. Google may choose a different canonical than you want, so the wrong URL shows in results. And crawl resources get wasted fetching the same content again and again. Canonicalisation is simply telling search engines which URL in a cluster of duplicates is the representative one. Deliberately deceptive or scraped duplication is a different matter, but ordinary site duplication is a housekeeping job.
```

Scene 3 — Where duplicates come from

```text
Where do duplicates come from? Protocol and host variants, like http and https, www and non-www. Trailing slashes and uppercase letters. Parameters for tracking, sorting and sessions. Index files like index dot html. Faceted paths reachable in different orders. Print or alternate views. Products that live in several categories. Syndicated articles. And near-duplicates, like city pages where only the town name changes. Most sites have at least five of these.
```

Scene 4 — Canonical signals, strongest first

```text
Google combines canonical signals, and none is an absolute directive. From strongest to weakest: redirects, where the source effectively disappears; rel canonical in the HTML head or HTTP header, a strong hint; internal linking consistency, meaning which variant you link to; sitemap inclusion, a weak hint; and preferences such as HTTPS over HTTP and cleaner URLs. When signals conflict, say the canonical says A but internal links and the sitemap say B, Google may pick B. Consistency is the whole game.
```

Scene 5 — rel=canonical rules

```text
Implementing rel canonical correctly. Use absolute URLs with the right protocol and host. Put it in the head, because a canonical in the body is ignored, and watch out for invalid elements that close the head early. Use one canonical per page. Self-reference on canonical pages. Make sure the target is indexable: two hundred, not noindexed, not blocked, not redirected. Don't canonicalise paginated pages to page one; they're not duplicates. Don't combine noindex with a canonical pointing elsewhere. And for syndication, ask partners for a cross-domain canonical plus a clear link back, knowing Google may not always honour it.
```

Scene 6 — What did Google choose?

```text
How do you check what Google chose? URL Inspection shows the user-declared canonical and the Google-selected canonical. In the Page indexing report, look at duplicate, Google chose different canonical than user, and duplicate without user-selected canonical. Sample URLs from each and ask one question: which signal is pulling Google the other way? It's usually internal links, sitemaps or redirects disagreeing with your tag.
```

Scene 7 — Hands-on: canonical audit script

```text
Now the hands-on. Export your crawl with address, status, indexability and canonical. The pandas script in the lesson text flags six problems: missing canonicals on two-hundred pages, relative canonicals, canonicals on the wrong host, canonicals pointing at non-two-hundred URLs, canonicals pointing at non-indexable URLs, and canonical chains, where A points to B and B points to C. It prints counts and writes the flagged rows to a CSV for tickets. For JavaScript sites, run it on both raw and rendered canonicals.
```

Scene 8 — Example 1: every canonical → home page

```text
Worked example one, simple. A Sharjah furniture store's CMS sets every page's canonical to the home page after a theme update. Within weeks, product pages start dropping out of the index. The audit script flags thousands of canonical-to-home rows immediately. The fix is a template change to self-referencing canonicals, followed by monitoring Page indexing as products return. One bad default, huge impact, which is why canonical checks belong in release tests.
```

Scene 9 — Example 2: near-duplicate city pages (illustrative)

```text
Worked example two, with illustrative details. An Islamabad travel agency has a hundred and forty Umrah packages from city pages that differ only by the city name. Google picks a handful as canonical and ignores the rest. Canonical tags can't fix this, because the pages are near-duplicates meant to rank separately. The agency keeps pages only for cities it genuinely departs from, with real schedules, local contacts and prices in rupees, consolidates the rest into one page with a city selector, and redirects the retired URLs. That also avoids doorway-style patterns that Google's spam policies target.
```

Scene 10 — A reusable canonical audit

```text
Let's walk through a canonical audit procedure you can reuse on any site. First, crawl with canonical extraction on, both raw and rendered if it's a JavaScript site. Second, flag missing canonicals, canonicals to non-two-hundred or noindexed URLs, chains, relative or wrong-host canonicals, and raw-versus-rendered mismatches. Third, compare canonical targets with internal link targets: are you linking to the non-canonical variant? Fourth, compare with the sitemap, where every URL should be self-canonical. And fifth, enforce host and protocol with sitewide redirects, and strip tracking parameters from internal links. Five steps, and most canonical problems surface in an afternoon.
```

Scene 11 — Consolidate or differentiate?

```text
One more concept worth separating clearly: consolidation versus differentiation. If two URLs show the same thing, consolidate them with redirects or canonicals. If two URLs are meant to rank for different searches, they must be genuinely different, with unique, useful content. Canonical tags are a consolidation tool. They can't make near-identical pages rank separately, and trying to scale hundreds of barely different pages can drift into doorway territory under Google's spam policies. So for every duplicate cluster, ask: should these be one page, or should they be truly different pages?
```

Scene 12 — Mistakes and measures

```text
Common mistakes. Every page canonicalised to the home page. Canonicals pointing to URLs that redirect. Parameter variants linked internally, like sort orders in navigation, while the canonical says otherwise. And relying on canonical tags to fix a problem a redirect should solve, such as http versus https. Measure success by the share of sampled URLs where Google's selected canonical matches yours, and by shrinking duplicate buckets in Page indexing.
```

Scene 13 — Recap and try this now

```text
Recap. Duplicates aren't a penalty, but they split signals and confuse selection. Redirects are the strongest signal, then rel canonical, then links and sitemaps. Keep every signal consistent, and don't expect canonicals to fix near-duplicate pages meant to rank separately. Try this now. Run the canonical audit script on your latest crawl, then use URL Inspection on five flagged URLs to see whether Google agrees with you.
```

</details>

### 8. Pagination and faceted navigation

Lesson `pagination-and-faceted-navigation` · module “Status codes, redirects, canonicals and duplicate URLs” · target 8 min · 13 scenes · 1055 words · planned 7:38 (words ≈ 7:39) · status: **to produce**

| Term | Say |
|---|---|
| rel next | rel next |
| History API | history A P I |

| # | Time | Sec | Narration | On screen | Visual |
|---|---|---|---|---|---|
| 1 | 0:00 | 32 | On large sites, two everyday features cause more crawl and indexing problems than almost anything else: pagination and faceted navigation. Users love filters for colour, size, brand and price. Crawlers can drown in them. In this lecture you'll learn how Google recommends handling pagination today, how to decide which facets deserve to be indexed, the implementation options and their trade-offs, and a small script that shows you which parameters are eating your crawl. | Pagination and faceted navigation<br>• Pagination done right<br>• Decide, then implement facets<br>• Measure which parameters eat the crawl | A category page with filter chips; each chip click spawns more URLs until the screen fills with a maze. Title appears. |
| 2 | 0:32 | 39 | Pagination first. Google stopped using rel next and rel prev as an indexing signal back in twenty nineteen. They're harmless, but they don't do what people think. Today's best practice is simple. Give each page a unique URL. Link pages sequentially with real anchor links, ideally with numbered links so deep pages are fewer clicks away. Self-canonicalise each page, rather than pointing everything at page one. Keep page one at the clean URL. And don't noindex paginated pages by default, because that can reduce crawling of the links on them. | Pagination today<br>• Unique URL per page<br>• Real, numbered anchor links<br>• Self-canonical; clean page 1; don't noindex by default | Paginated navigation bar builds with numbered anchors; each page card shows its own canonical tag pointing to itself. |
| 3 | 1:11 | 31 | What about infinite scroll and load more buttons? Google doesn't scroll or click. So if products beyond the first batch only appear after a click, Google may never see them, and they lose their internal links. Back infinite scroll with real paginated URLs that are also linked as anchors, and update the URL with the History API as people scroll. Users keep the smooth experience. Crawlers get a path to every item. | Infinite scroll and 'Load more'<br>• Google doesn't click or scroll<br>• Back it with real paginated URLs<br>• History API for users | A 'Load more' button with products hidden beneath; then hidden anchor links '?page=2', '?page=3' appear underneath, visible to a crawler icon. |
| 4 | 1:42 | 38 | Now facets. Google published dedicated faceted navigation guidance in December twenty twenty-four, and the first step is a business decision per facet. Brand, key colour or type, like Nike trainers, often has real search demand, so it can be indexable, on a clean static URL, with a unique title, heading and intro, and included in sitemaps. Size, price range and rating usually have low demand. Sort order, view mode and items per page have none. And combinations of three or more facets rarely do. Decide first, implement second. | Facets: decide first<br>• Demand-backed facets → indexable pages<br>• Size, price, rating → usually not<br>• Sort, view, per-page, 3+ combos → block | Decision table animates with facet types as rows and treatment chips appearing in the final column. |
| 5 | 2:20 | 39 | For facets you don't want in search, you have five options, with different strengths. robots.txt disallow for parameter patterns is the most effective way to stop crawling of huge facet spaces. URL fragments, the part after the hash, aren't crawled as separate URLs. rel canonical to the unfiltered category reduces indexing but Google still crawls the variants. Noindex keeps them out of the index but they're still crawled. And nofollow on filter links is only a hint. For crawl efficiency on big sites, robots.txt and fragments do the heavy lifting. | Implementation options<br>• robots.txt disallow: stops crawling<br>• #fragments: not crawled<br>• canonical / noindex: still crawled; nofollow: a hint | Five option cards with two meters each, 'stops crawling' and 'stops indexing'; robots.txt and fragments score high on crawling. |
| 6 | 2:59 | 37 | For facets you do want indexed: use a clean, consistent URL or parameter order, never both brand then colour and colour then brand. Return four-oh-four for combinations with no results rather than an empty two-hundred page. Generate unique titles and headings, ideally with a short intro. And include them in sitemaps and link to them from relevant categories. And one sequencing warning: don't block in robots.txt URLs that are already indexed and noindexed, because Google can no longer see the noindex. Noindex first, wait, then block. | Indexable facets + sequencing<br>• One parameter order; 404 for zero results<br>• Unique titles, intros; sitemaps + links<br>• Noindex → wait → then block | Two URLs with parameters in different orders merge into one normalised URL. Then a three-step sequence animates: noindex, wait (hourglass), disallow. |
| 7 | 3:36 | 30 | Hands-on. Before you decide treatments, measure. The lesson text has a short Python script that reads URLs from your verified Googlebot logs or your crawl, groups them by parameter combination, and counts how many fall into low-value parameters, multi-facet combinations, single facets and clean URLs. The top fifteen combinations usually reveal the culprits within seconds. Then use the robots.txt patterns in the lesson, and test them before shipping. | Hands-on: facet classifier<br>• Group URLs by parameter keys<br>• Count low-value, multi-facet, single, clean<br>• Top 15 combos reveal the culprits | Code walkthrough: parse_qsl and the Counter; output shows 'sort&colour&size' at the top with a big count (illustrative). |
| 8 | 4:06 | 37 | Worked example one, with illustrative details. A Karachi-based marketplace has three thousand categories and fourteen filter types. Logs show most Googlebot hits going to parameter URLs, while new products take weeks to be crawled. The team checks keyword demand per facet: brand and colour for key categories have demand. They create static indexable URLs for those, a few thousand pages with unique intros. Everything else moves to parameters disallowed in robots.txt with consistent ordering. Zero-result combinations return four-oh-four. Then they re-measure in logs after several weeks. | Example 1: Karachi marketplace (illustrative)<br>• Demand check per facet<br>• Static pages for brand/colour<br>• Disallow the rest; 404 zero results; re-measure | Before/after crawl-share pie charts: 'parameters' slice shrinks; 'products' and 'approved facets' slices grow. |
| 9 | 4:43 | 37 | Worked example two, also illustrative. A UK DIY retailer replaced numbered pagination with a JavaScript load more button. Products beyond the first thirty-six in each category lost their internal links, and logs showed many long-tail products uncrawled for weeks. The fix kept the button for users, but backed it with real paginated URLs rendered as anchor links, each self-canonical, with the URL updated through the History API as users load more. Within a few weeks, the missing products started appearing in the logs again. | Example 2: UK DIY 'Load more' (illustrative)<br>• Products beyond 36 lost links<br>• Add real paginated anchors, self-canonical<br>• Long-tail products crawled again | Category grid with 36 visible products and a 'Load more' button; hidden page anchors appear; log chart shows long-tail product hits rising. |
| 10 | 5:20 | 32 | A note on view-all pages. If you have a view-all version of a category that loads quickly, it's fine to let paginated pages point their canonical at the view-all page, because it genuinely contains everything. But if view-all is slow, heavy, or would push the HTML past sensible sizes, don't do it. Remember Googlebot only fetches the first two megabytes of HTML. For most large catalogues, well-linked, self-canonical paginated pages are the safer default. | View-all pages<br>• Fast view-all → paginated pages may canonicalise to it<br>• Slow or huge → don't<br>• Default: self-canonical pagination | A 'View all' page loads quickly (green) vs a heavy one with a size bar passing a 2 MB marker (red). |
| 11 | 5:52 | 43 | Internal search result pages deserve a special mention, because they combine every facet problem at once. Every query a user types can become a new URL. If those URLs are linked, for example from a popular searches widget, crawlers will follow them, and you'll get thin, near-infinite pages in the index. Google's guidelines have long advised against letting auto-generated search result pages be indexed. The usual treatment is to disallow the search path in robots.txt, remove links to search result URLs, and, if you want to capture popular queries, build proper category or guide pages for them instead. | Internal search pages<br>• Every query = a new URL<br>• Disallow the path; remove links<br>• Build real pages for popular queries | A search box spawns endless URL cards; a robots.txt rule blocks the stream; three curated category pages appear for popular queries. |
| 12 | 6:35 | 33 | Common mistakes. Blocking URLs in robots.txt that are already indexed and noindexed. Canonicalising every page of a category to page one. Letting sort parameters be linked from every listing. And letting internal search result pages be crawled and indexed, which Google's guidelines have long advised against. Measure success in the logs: the share of Googlebot hits on low-value parameters going down, the crawl frequency of products going up, and the indexed count of your approved facet pages. | Mistakes and measures<br>• Wrong sequencing; canonical to page 1<br>• Linked sort parameters; indexed internal search<br>• KPIs from logs and Page indexing | Mistake chips fade; three KPI tiles animate with arrows up and down. |
| 13 | 7:08 | 30 | Recap. Paginate with unique, linked, self-canonical URLs. Back infinite scroll with real pages. For facets, decide by demand, then implement: indexable clean pages for the valuable few, and robots.txt or fragments for the rest. Sequence noindex before blocking. Try this now. Run the facet classifier on a week of verified Googlebot URLs and write down the three parameter combinations taking the most crawl. That's your facet decision meeting agenda. | Recap and try this now<br>• Paginate properly; back infinite scroll<br>• Decide facets by demand<br>• Find your top 3 crawl-eating combos | Three parameter combos appear on an agenda card titled 'Facet decisions'. End card: 'Next module: architecture and sitemaps'. |

<details><summary>Narration per scene (paste into ElevenLabs)</summary>

Scene 1 — Pagination and faceted navigation

```text
On large sites, two everyday features cause more crawl and indexing problems than almost anything else: pagination and faceted navigation. Users love filters for colour, size, brand and price. Crawlers can drown in them. In this lecture you'll learn how Google recommends handling pagination today, how to decide which facets deserve to be indexed, the implementation options and their trade-offs, and a small script that shows you which parameters are eating your crawl.
```

Scene 2 — Pagination today

```text
Pagination first. Google stopped using rel next and rel prev as an indexing signal back in twenty nineteen. They're harmless, but they don't do what people think. Today's best practice is simple. Give each page a unique URL. Link pages sequentially with real anchor links, ideally with numbered links so deep pages are fewer clicks away. Self-canonicalise each page, rather than pointing everything at page one. Keep page one at the clean URL. And don't noindex paginated pages by default, because that can reduce crawling of the links on them.
```

Scene 3 — Infinite scroll and 'Load more'

```text
What about infinite scroll and load more buttons? Google doesn't scroll or click. So if products beyond the first batch only appear after a click, Google may never see them, and they lose their internal links. Back infinite scroll with real paginated URLs that are also linked as anchors, and update the URL with the History API as people scroll. Users keep the smooth experience. Crawlers get a path to every item.
```

Scene 4 — Facets: decide first

```text
Now facets. Google published dedicated faceted navigation guidance in December twenty twenty-four, and the first step is a business decision per facet. Brand, key colour or type, like Nike trainers, often has real search demand, so it can be indexable, on a clean static URL, with a unique title, heading and intro, and included in sitemaps. Size, price range and rating usually have low demand. Sort order, view mode and items per page have none. And combinations of three or more facets rarely do. Decide first, implement second.
```

Scene 5 — Implementation options

```text
For facets you don't want in search, you have five options, with different strengths. robots.txt disallow for parameter patterns is the most effective way to stop crawling of huge facet spaces. URL fragments, the part after the hash, aren't crawled as separate URLs. rel canonical to the unfiltered category reduces indexing but Google still crawls the variants. Noindex keeps them out of the index but they're still crawled. And nofollow on filter links is only a hint. For crawl efficiency on big sites, robots.txt and fragments do the heavy lifting.
```

Scene 6 — Indexable facets + sequencing

```text
For facets you do want indexed: use a clean, consistent URL or parameter order, never both brand then colour and colour then brand. Return four-oh-four for combinations with no results rather than an empty two-hundred page. Generate unique titles and headings, ideally with a short intro. And include them in sitemaps and link to them from relevant categories. And one sequencing warning: don't block in robots.txt URLs that are already indexed and noindexed, because Google can no longer see the noindex. Noindex first, wait, then block.
```

Scene 7 — Hands-on: facet classifier

```text
Hands-on. Before you decide treatments, measure. The lesson text has a short Python script that reads URLs from your verified Googlebot logs or your crawl, groups them by parameter combination, and counts how many fall into low-value parameters, multi-facet combinations, single facets and clean URLs. The top fifteen combinations usually reveal the culprits within seconds. Then use the robots.txt patterns in the lesson, and test them before shipping.
```

Scene 8 — Example 1: Karachi marketplace (illustrative)

```text
Worked example one, with illustrative details. A Karachi-based marketplace has three thousand categories and fourteen filter types. Logs show most Googlebot hits going to parameter URLs, while new products take weeks to be crawled. The team checks keyword demand per facet: brand and colour for key categories have demand. They create static indexable URLs for those, a few thousand pages with unique intros. Everything else moves to parameters disallowed in robots.txt with consistent ordering. Zero-result combinations return four-oh-four. Then they re-measure in logs after several weeks.
```

Scene 9 — Example 2: UK DIY 'Load more' (illustrative)

```text
Worked example two, also illustrative. A UK DIY retailer replaced numbered pagination with a JavaScript load more button. Products beyond the first thirty-six in each category lost their internal links, and logs showed many long-tail products uncrawled for weeks. The fix kept the button for users, but backed it with real paginated URLs rendered as anchor links, each self-canonical, with the URL updated through the History API as users load more. Within a few weeks, the missing products started appearing in the logs again.
```

Scene 10 — View-all pages

```text
A note on view-all pages. If you have a view-all version of a category that loads quickly, it's fine to let paginated pages point their canonical at the view-all page, because it genuinely contains everything. But if view-all is slow, heavy, or would push the HTML past sensible sizes, don't do it. Remember Googlebot only fetches the first two megabytes of HTML. For most large catalogues, well-linked, self-canonical paginated pages are the safer default.
```

Scene 11 — Internal search pages

```text
Internal search result pages deserve a special mention, because they combine every facet problem at once. Every query a user types can become a new URL. If those URLs are linked, for example from a popular searches widget, crawlers will follow them, and you'll get thin, near-infinite pages in the index. Google's guidelines have long advised against letting auto-generated search result pages be indexed. The usual treatment is to disallow the search path in robots.txt, remove links to search result URLs, and, if you want to capture popular queries, build proper category or guide pages for them instead.
```

Scene 12 — Mistakes and measures

```text
Common mistakes. Blocking URLs in robots.txt that are already indexed and noindexed. Canonicalising every page of a category to page one. Letting sort parameters be linked from every listing. And letting internal search result pages be crawled and indexed, which Google's guidelines have long advised against. Measure success in the logs: the share of Googlebot hits on low-value parameters going down, the crawl frequency of products going up, and the indexed count of your approved facet pages.
```

Scene 13 — Recap and try this now

```text
Recap. Paginate with unique, linked, self-canonical URLs. Back infinite scroll with real pages. For facets, decide by demand, then implement: indexable clean pages for the valuable few, and robots.txt or fragments for the rest. Sequence noindex before blocking. Try this now. Run the facet classifier on a week of verified Googlebot URLs and write down the three parameter combinations taking the most crawl. That's your facet decision meeting agenda.
```

</details>

### 9. Site architecture and internal linking

Lesson `site-architecture-and-internal-linking` · module “Site architecture, internal linking and XML sitemaps” · target 7 min · 13 scenes · 998 words · planned 7:14 (words ≈ 7:14) · status: **to produce**

| Term | Say |
|---|---|
| networkx | network X |
| PageRank | page rank |
| BreadcrumbList | breadcrumb list |

| # | Time | Sec | Narration | On screen | Visual |
|---|---|---|---|---|---|
| 1 | 0:00 | 35 | Internal links are the one ranking-related signal you control completely. No outreach, no budget, just decisions about which pages link to which. And yet most sites leave their internal linking to chance: a mega-menu here, a random related posts widget there. In this lecture you'll learn how architecture tells search engines what matters, the patterns that leak or pool link value, how to find orphans, and how to compute internal PageRank from a crawl to find your best linking opportunities. | Site architecture and internal linking<br>• How structure signals importance<br>• Leaks, pools and orphans<br>• Internal PageRank from a crawl | A site tree grows from the home page; link lines pulse with 'value' flowing down branches; one isolated leaf page stays grey. |
| 2 | 0:35 | 33 | Internal links do three jobs at once. They let crawlers discover URLs. They pass signals, because Google still uses links, including internal ones, to understand importance and relationships. And they give context through anchor text. Think of a city's road network. Main roads lead to the important districts, local roads connect neighbourhoods, and signposts tell you where you're going. A good architecture puts valuable pages close to the centre, groups related content, and uses clear signposts. | Three jobs of internal links<br>• Discovery<br>• Signals of importance<br>• Context via anchors | City map analogy: main roads (navigation) lead to districts (hubs), local streets (contextual links) connect houses (pages), signposts (anchor text) label routes. |
| 3 | 1:08 | 37 | Six principles. Keep priority pages at a shallow click depth; depth isn't a ranking factor itself, but deep pages tend to be crawled less and receive less internal value. Group content into hubs and spokes: a hub covers a topic broadly and links to detailed spokes, which link back and to close siblings. Use descriptive anchors, not click here. Give every indexable page at least one crawlable link from an indexable page. Link to canonical, final URLs. And use breadcrumbs with BreadcrumbList markup on hierarchical sites. | Six principles<br>• Shallow depth for priority pages<br>• Hubs and spokes; descriptive anchors<br>• No orphans; canonical targets; breadcrumbs | Hub-and-spoke diagram builds: a 'Local SEO' hub with spokes; anchor labels appear on each line; a breadcrumb trail slides across the top. |
| 4 | 1:45 | 33 | Where does link value leak or pool? Mega-menus linking to hundreds of pages dilute emphasis and bloat HTML. Links inside JavaScript widgets without an href aren't discovered. Sitewide footer links to low-value pages waste emphasis. Pages reachable only through internal search are effectively orphaned. Blog posts that fall off page one of the blog index sink deeper every week. And nofollow on internal links doesn't save value; it just wastes that link's signal. Remove internal nofollow. | Leaks and pools<br>• Bloated mega-menus; JS links without href<br>• Footer clutter; search-only pages<br>• Sinking blog posts; internal nofollow | Pipe diagram with leaks labelled by pattern; water (link value) drips away at each leak, pooling in a 'tag archives' bucket. |
| 5 | 2:18 | 33 | How do you measure your architecture? Use a crawler to export, for each indexable URL, its crawl depth, unique inlinks and linking pages, a link score such as Screaming Frog's Link Score, and the anchor texts used. Then join with Search Console clicks and impressions and with business value. The classic finding: high-value pages with few inlinks at high depth, while low-value pages like tags, archives and filters soak up most of the internal links. | Measure it<br>• Depth, inlinks, link score, anchors<br>• Join with GSC and revenue<br>• Classic finding: value vs links mismatch | Scatter plot: x = internal link score, y = revenue; high-revenue low-link dots glow red in the top-left quadrant. |
| 6 | 2:51 | 27 | Orphans deserve their own routine. Crawl from the home page. Separately, collect URLs from XML sitemaps, Search Console exports, analytics landing pages and server logs. Anything in the second list but not in the first is an orphan. For each one, decide: link it, redirect it, or remove it. Orphans with traffic or backlinks are usually the most valuable to rescue. | Finding orphans<br>• Crawl from home<br>• Compare with sitemaps, GSC, analytics, logs<br>• Link, redirect or remove | Two circles of a Venn diagram: 'Crawled via links' and 'Known from other sources'; the non-overlapping part of the second circle is labelled 'orphans'. |
| 7 | 3:18 | 34 | Now the hands-on. The lesson text has a script using networkx. Export all internal links from your crawler, keep followed hyperlinks, and build a directed graph. Compute PageRank on it, which gives each page a relative internal importance score. Join that with crawl depth, inlinks and Search Console impressions. Then list pages with lots of impressions but weak internal PageRank. That's your prioritised list of linking opportunities, where contextual links from relevant, strong pages are most likely to help. | Hands-on: internal PageRank<br>• Build a graph from inlinks<br>• networkx PageRank<br>• High impressions + low PR = opportunities | Code walkthrough: from_pandas_edgelist, nx.pagerank, then the merge and filter. A graph visual shows node sizes scaled by PageRank. |
| 8 | 3:52 | 40 | Worked example one, with illustrative details. An agency has four hundred blog posts. Its top-converting guide, on Google Business Profile optimisation, sits at depth seven, linked only from paginated blog archives. The fix: create a local SEO hub linked from the main navigation, link the hub to the twenty-five most relevant posts with descriptive anchors and have each link back, add contextual links from the three service pages that mention Business Profiles, and merge weak overlapping posts with redirects. After recrawling, the guide sits at depth two with dozens of relevant links. | Example 1: agency blog (illustrative)<br>• Top guide at depth 7<br>• New hub in navigation; contextual links<br>• Now depth 2 with relevant inlinks | Depth ladder: the guide climbs from rung 7 to rung 2 as hub and contextual links attach. |
| 9 | 4:32 | 36 | Worked example two, also illustrative. A Jeddah fashion retailer links every product from a new arrivals carousel on the home page, until the item rotates out after two weeks and loses almost all its internal links. Evergreen best-sellers end up six or more clicks deep. Fixes: permanent category and best-sellers modules with real links, related products linked by attributes like fabric and occasion, breadcrumbs with markup, and a quarterly check of depth and internal PageRank for the top two hundred revenue products. | Example 2: Jeddah 'new arrivals' trap (illustrative)<br>• Rotating carousel = temporary links<br>• Best-sellers sink to depth 6+<br>• Permanent modules, attribute links, breadcrumbs | Carousel rotates products off the home page; their link lines snap. Then permanent 'Best-sellers' and 'Shop by fabric' modules attach stable links. |
| 10 | 5:08 | 35 | Let's look at typical structures. For a service business, it's usually home, then a services hub, then individual services, with a separate guides hub and case studies. For e-commerce, it's home, then department, category, subcategory and product, supported by cross-links like related products and shop by brand, rendered as real links. For publishers, it's topic hubs plus date archives. The pattern is the same: a clear hierarchy for discovery, plus contextual cross-links that follow how users actually think about the topic. | Typical structures<br>• Services: hubs → services, guides, cases<br>• E-commerce: department → category → product<br>• Publishers: topic hubs + archives | Three small tree diagrams side by side for service, e-commerce and publisher sites, with dotted cross-links between branches. |
| 11 | 5:43 | 36 | Here's an internal linking audit checklist to finish the lesson. Does the home page link to top categories and hubs? Are there any internal links to redirects, errors or non-canonical URLs? Is any important page deeper than sensible for the site's size? Does every indexable page have links from other indexable pages? Is anchor text descriptive and varied? Are breadcrumbs present and marked up? And do pagination and facet links behave as designed? Seven questions, and you'll catch nearly every architecture issue. | The internal linking checklist<br>• Home → hubs; no links to 3xx/4xx/non-canonical<br>• Sensible depth; no orphans; good anchors<br>• Breadcrumbs; pagination and facets as designed | Seven checkboxes tick one by one beside a site tree that highlights the relevant parts. |
| 12 | 6:19 | 28 | Common mistakes. Adding internal nofollow to sculpt PageRank, which doesn't redistribute value. Relying on XML sitemaps instead of links to surface important pages. Automated related links that connect pages randomly rather than by topic. And treating footers as a dumping ground. Measure success with crawl depth and internal PageRank of your priority pages, the orphan count, and crawl frequency of those pages in your logs. | Mistakes and measures<br>• Internal nofollow; sitemap-only discovery<br>• Random 'related' widgets; footer dumping<br>• KPIs: depth, internal PR, orphans, crawl frequency | Mistake chips fade; KPI tiles appear. |
| 13 | 6:47 | 27 | Recap. Internal links drive discovery, signal importance and provide context. Keep priority pages shallow, build hubs, use descriptive anchors, eliminate orphans, and measure with depth, link scores and your own internal PageRank. Try this now. Run the networkx script on your latest crawl, and pick the top five high-impression, low-PageRank pages. For each, add three contextual links from relevant, strong pages this week. | Recap and try this now<br>• Run the internal PageRank script<br>• Pick 5 high-impression, low-PR pages<br>• Add 3 contextual links each | Five page cards each gain three new incoming link lines. End card: 'Next: XML sitemaps done properly'. |

<details><summary>Narration per scene (paste into ElevenLabs)</summary>

Scene 1 — Site architecture and internal linking

```text
Internal links are the one ranking-related signal you control completely. No outreach, no budget, just decisions about which pages link to which. And yet most sites leave their internal linking to chance: a mega-menu here, a random related posts widget there. In this lecture you'll learn how architecture tells search engines what matters, the patterns that leak or pool link value, how to find orphans, and how to compute internal PageRank from a crawl to find your best linking opportunities.
```

Scene 2 — Three jobs of internal links

```text
Internal links do three jobs at once. They let crawlers discover URLs. They pass signals, because Google still uses links, including internal ones, to understand importance and relationships. And they give context through anchor text. Think of a city's road network. Main roads lead to the important districts, local roads connect neighbourhoods, and signposts tell you where you're going. A good architecture puts valuable pages close to the centre, groups related content, and uses clear signposts.
```

Scene 3 — Six principles

```text
Six principles. Keep priority pages at a shallow click depth; depth isn't a ranking factor itself, but deep pages tend to be crawled less and receive less internal value. Group content into hubs and spokes: a hub covers a topic broadly and links to detailed spokes, which link back and to close siblings. Use descriptive anchors, not click here. Give every indexable page at least one crawlable link from an indexable page. Link to canonical, final URLs. And use breadcrumbs with BreadcrumbList markup on hierarchical sites.
```

Scene 4 — Leaks and pools

```text
Where does link value leak or pool? Mega-menus linking to hundreds of pages dilute emphasis and bloat HTML. Links inside JavaScript widgets without an href aren't discovered. Sitewide footer links to low-value pages waste emphasis. Pages reachable only through internal search are effectively orphaned. Blog posts that fall off page one of the blog index sink deeper every week. And nofollow on internal links doesn't save value; it just wastes that link's signal. Remove internal nofollow.
```

Scene 5 — Measure it

```text
How do you measure your architecture? Use a crawler to export, for each indexable URL, its crawl depth, unique inlinks and linking pages, a link score such as Screaming Frog's Link Score, and the anchor texts used. Then join with Search Console clicks and impressions and with business value. The classic finding: high-value pages with few inlinks at high depth, while low-value pages like tags, archives and filters soak up most of the internal links.
```

Scene 6 — Finding orphans

```text
Orphans deserve their own routine. Crawl from the home page. Separately, collect URLs from XML sitemaps, Search Console exports, analytics landing pages and server logs. Anything in the second list but not in the first is an orphan. For each one, decide: link it, redirect it, or remove it. Orphans with traffic or backlinks are usually the most valuable to rescue.
```

Scene 7 — Hands-on: internal PageRank

```text
Now the hands-on. The lesson text has a script using networkx. Export all internal links from your crawler, keep followed hyperlinks, and build a directed graph. Compute PageRank on it, which gives each page a relative internal importance score. Join that with crawl depth, inlinks and Search Console impressions. Then list pages with lots of impressions but weak internal PageRank. That's your prioritised list of linking opportunities, where contextual links from relevant, strong pages are most likely to help.
```

Scene 8 — Example 1: agency blog (illustrative)

```text
Worked example one, with illustrative details. An agency has four hundred blog posts. Its top-converting guide, on Google Business Profile optimisation, sits at depth seven, linked only from paginated blog archives. The fix: create a local SEO hub linked from the main navigation, link the hub to the twenty-five most relevant posts with descriptive anchors and have each link back, add contextual links from the three service pages that mention Business Profiles, and merge weak overlapping posts with redirects. After recrawling, the guide sits at depth two with dozens of relevant links.
```

Scene 9 — Example 2: Jeddah 'new arrivals' trap (illustrative)

```text
Worked example two, also illustrative. A Jeddah fashion retailer links every product from a new arrivals carousel on the home page, until the item rotates out after two weeks and loses almost all its internal links. Evergreen best-sellers end up six or more clicks deep. Fixes: permanent category and best-sellers modules with real links, related products linked by attributes like fabric and occasion, breadcrumbs with markup, and a quarterly check of depth and internal PageRank for the top two hundred revenue products.
```

Scene 10 — Typical structures

```text
Let's look at typical structures. For a service business, it's usually home, then a services hub, then individual services, with a separate guides hub and case studies. For e-commerce, it's home, then department, category, subcategory and product, supported by cross-links like related products and shop by brand, rendered as real links. For publishers, it's topic hubs plus date archives. The pattern is the same: a clear hierarchy for discovery, plus contextual cross-links that follow how users actually think about the topic.
```

Scene 11 — The internal linking checklist

```text
Here's an internal linking audit checklist to finish the lesson. Does the home page link to top categories and hubs? Are there any internal links to redirects, errors or non-canonical URLs? Is any important page deeper than sensible for the site's size? Does every indexable page have links from other indexable pages? Is anchor text descriptive and varied? Are breadcrumbs present and marked up? And do pagination and facet links behave as designed? Seven questions, and you'll catch nearly every architecture issue.
```

Scene 12 — Mistakes and measures

```text
Common mistakes. Adding internal nofollow to sculpt PageRank, which doesn't redistribute value. Relying on XML sitemaps instead of links to surface important pages. Automated related links that connect pages randomly rather than by topic. And treating footers as a dumping ground. Measure success with crawl depth and internal PageRank of your priority pages, the orphan count, and crawl frequency of those pages in your logs.
```

Scene 13 — Recap and try this now

```text
Recap. Internal links drive discovery, signal importance and provide context. Keep priority pages shallow, build hubs, use descriptive anchors, eliminate orphans, and measure with depth, link scores and your own internal PageRank. Try this now. Run the networkx script on your latest crawl, and pick the top five high-impression, low-PageRank pages. For each, add three contextual links from relevant, strong pages this week.
```

</details>

### 10. XML sitemaps done properly

Lesson `xml-sitemaps-done-properly` · module “Site architecture, internal linking and XML sitemaps” · target 7 min · 13 scenes · 999 words · planned 7:13 (words ≈ 7:14) · status: **to produce**

| Term | Say |
|---|---|
| lastmod | last mod |
| gzip | G zip |
| UTF-8 | U T F eight |
| changefreq | change freak |

| # | Time | Sec | Narration | On screen | Visual |
|---|---|---|---|---|---|
| 1 | 0:00 | 31 | XML sitemaps look like a solved problem. A plugin generates one, you submit it, done. But on real audits, sitemaps are full of redirects, noindexed pages, non-canonical URLs and fake lastmod dates, and that teaches search engines to trust them less. In this lecture you'll learn what sitemaps are for, the hard limits, how Google uses lastmod, specialised sitemaps, segmentation for diagnostics, and a Python checker that finds dirty URLs automatically. | XML sitemaps done properly<br>• What they are and aren't<br>• Limits, lastmod, special types<br>• Segmentation and a hygiene checker | A sitemap XML file scrolls; some URL rows glow red with labels '301', 'noindex', 'non-canonical'. Title appears. |
| 2 | 0:31 | 35 | A sitemap is a list of URLs you want search engines to know about, with optional metadata. It helps discovery, especially for large sites, new sites with few links, and pages that change often. It doesn't guarantee crawling or indexing, and it doesn't replace internal linking. Think of it as a list you hand to a delivery driver. Useful, especially in a big new estate. But if the roads don't connect to the houses, the list alone won't get everything delivered. | What sitemaps are<br>• A discovery aid<br>• Not a guarantee<br>• Not a replacement for links | Delivery driver holding a list in a new housing estate; some houses on the list have no road connection and stay grey. |
| 3 | 1:06 | 30 | The hard limits. Up to fifty thousand URLs or fifty megabytes uncompressed per file, and gzip is allowed. Use a sitemap index to list multiple sitemaps. Use absolute, canonical URLs that return two hundred and are indexable. Encode in UTF-8 and escape special characters, so an ampersand becomes amp. And a sitemap can only include URLs from its own host and path, unless cross-submission is verified in Search Console. | Hard limits and rules<br>• 50,000 URLs or 50 MB per file<br>• Sitemap index for many files<br>• Absolute, canonical, 200, indexable; UTF-8 | A stack of sitemap files under a sitemap index; a counter shows '50,000' and a size gauge shows '50 MB' limits. |
| 4 | 1:36 | 29 | Now lastmod, priority and changefreq. Google uses lastmod only if it's consistently and verifiably accurate, meaning it reflects the last significant change to the main content, not the time the sitemap was generated. Set every URL to today, and you teach Google to ignore your lastmod. Priority and changefreq? Google ignores them, so you can omit them. One field matters, and only if you tell the truth. | lastmod, priority, changefreq<br>• lastmod: used only if accurate<br>• 'Everything changed today' = ignored<br>• priority and changefreq: ignored by Google | Three field labels; 'priority' and 'changefreq' fade out; 'lastmod' shows two versions: honest dates (green) vs all identical 'today' (red). |
| 5 | 2:05 | 33 | Specialised sitemaps. Image sitemaps help discovery of images loaded in ways crawlers might miss; Google now only needs image loc, having deprecated the caption, title, geo-location and licence tags. Video sitemaps describe video landing pages with thumbnail, title, description and a content or player location. News sitemaps, for eligible publishers, list only articles from the last two days, up to a thousand URLs. And hreflang can live in sitemaps too, which we'll cover in the international lesson. | Specialised sitemaps<br>• Image: image:loc is what matters<br>• Video: thumbnail, title, description, location<br>• News: last 2 days, ≤ 1,000 URLs | Three small XML snippets slide in: image, video and news namespaces highlighted. |
| 6 | 2:38 | 39 | Submission and discovery. Reference your sitemap index in robots.txt. Submit it in Search Console and Bing Webmaster Tools. Google's old sitemap ping endpoint was retired in twenty twenty-three, so rely on robots.txt, Search Console and accurate lastmod. For Bing and several other engines, IndexNow gives you instant change notifications. And segment your sitemaps by template: products, categories, guides, locations. Then you can filter the Page indexing report by sitemap and see, for example, that products are mostly crawled, currently not indexed, while guides are fine. One giant file hides that. | Submit and segment<br>• robots.txt + Search Console + Bing<br>• Ping retired; IndexNow for Bing & co.<br>• Segment by template for diagnostics | A sitemap index splits into segment files; a Page indexing chart filters by each segment showing different indexed ratios. |
| 7 | 3:17 | 35 | Hands-on. The lesson text includes a Python checker. It follows the sitemap index recursively, handles gzip, and for each URL fetches the page without following redirects. It flags anything that isn't a two hundred, anything with noindex in the header or meta robots, and anything whose canonical points elsewhere. For big sites, sample a couple of thousand URLs per segment and run it from an allow-listed IP. A crawler's list mode can do the same job with a user interface. | Hands-on: sitemap hygiene checker<br>• Recursive index + gzip<br>• Flags non-200, noindex, non-self canonical<br>• Sample large segments | Code walkthrough of urls_from() recursion and the problem checks; output '12,000 URLs checked, 431 problems' labelled illustrative. |
| 8 | 3:52 | 32 | Worked example one, simple. A Leeds recruitment agency's sitemap plugin includes every expired job with a two-hundred page saying this role has closed, plus tag archives and attachment pages. The checker flags hundreds of problems. The fixes: exclude tags and attachments in the plugin settings, remove closed jobs from the sitemap, and give closed jobs a four-oh-four or a useful page linking to similar live roles. The sitemap shrinks to URLs worth crawling. | Example 1: Leeds recruitment agency<br>• Closed jobs, tags, attachments in sitemap<br>• Exclude in plugin; remove closed jobs<br>• A lean sitemap of worthwhile URLs | Sitemap list shrinks as rows labelled 'closed job', 'tag', 'attachment' drop out. |
| 9 | 4:24 | 33 | Worked example two, with illustrative details. A Lahore property portal generated sitemaps nightly, with every URL's lastmod set to the generation time. So the field carried no signal. The team changed the generator to use each listing's real last significant change, meaning price, status or description, not view counters. They split sitemaps into active listings, projects and guides, and added a small changed in the last forty-eight hours sitemap. Logs then showed updated listings recrawled sooner. | Example 2: Lahore property portal (illustrative)<br>• lastmod = generation time → ignored<br>• Real change timestamps; segments<br>• 48-hour 'recent changes' sitemap | Timestamp column flips from identical values to varied real dates; a small 'recent' sitemap icon appears; a logs chart shows faster recrawl of updated listings. |
| 10 | 4:57 | 36 | For very large sites, here's a technique worth knowing: a freshness sitemap. Alongside your full, segmented sitemaps, keep a small sitemap that contains only URLs created or substantially updated in the last few days. Because its lastmod values are genuine, it gives crawlers a compact, trustworthy list of what changed. That's valuable for marketplaces, classifieds and publishers, where new inventory must be discovered quickly. Rotate URLs out once they're older than your chosen window, and keep the full sitemaps as the complete record. | Large sites: the freshness sitemap<br>• Only URLs changed in the last few days<br>• Genuine lastmod values<br>• Rotate out; keep full sitemaps too | A small 'recent changes' sitemap card sits beside a big stack; new URL cards enter it and older ones slide out after a timer. |
| 11 | 5:33 | 39 | Let me give you the sitemap hygiene audit as a routine. Download all sitemaps by following the index. Crawl the URL list: every URL should return two hundred, be self-canonical, indexable and not blocked by robots.txt. Compare with your site crawl to find indexable canonical pages missing from sitemaps, and sitemap URLs missing from the crawl, which are orphans. Check lastmod accuracy against CMS update dates on a sample. Check Search Console for couldn't fetch or parsing errors. And confirm generation is automatic, because hand-built sitemaps drift out of date. | The hygiene routine<br>• Download → crawl list → compare with site crawl<br>• Check lastmod on a sample<br>• Check GSC errors; automate generation | Six-step routine animates as a vertical checklist with small tool icons beside each step. |
| 12 | 6:12 | 34 | Common mistakes. Including redirected, four-oh-four, noindexed or non-canonical URLs, which weakens trust in the file. Letting a plugin generate sitemaps for tag archives, attachments or internal search. Using lastmod as a freshness hack. And forgetting to update sitemaps after a migration: the new sitemap should list new URLs, and temporarily keeping a list of old URLs can help Google discover the redirects. Measure success with the share of sitemap URLs that are clean, and the indexed-to-submitted ratio per segment. | Mistakes and measures<br>• Dirty URLs; plugin bloat<br>• Fake lastmod; stale post-migration sitemaps<br>• KPIs: % clean, indexed/submitted by segment | Mistake chips; KPI tiles by segment with small ratio bars. |
| 13 | 6:46 | 27 | Recap. Sitemaps aid discovery but don't guarantee indexing. Keep them clean: canonical, indexable, two-hundred URLs only. Make lastmod truthful or leave it out. Segment by template for diagnostics, and use IndexNow for engines that support it. Try this now. Run the hygiene checker on one sitemap segment, and fix the source of every problem it finds, rather than editing the file by hand. | Recap and try this now<br>• Clean, truthful, segmented<br>• Run the checker on one segment<br>• Fix the generator, not the file | Checker output shrinks to zero problems after a generator setting changes. End card: 'Next module: international SEO and structured data'. |

<details><summary>Narration per scene (paste into ElevenLabs)</summary>

Scene 1 — XML sitemaps done properly

```text
XML sitemaps look like a solved problem. A plugin generates one, you submit it, done. But on real audits, sitemaps are full of redirects, noindexed pages, non-canonical URLs and fake lastmod dates, and that teaches search engines to trust them less. In this lecture you'll learn what sitemaps are for, the hard limits, how Google uses lastmod, specialised sitemaps, segmentation for diagnostics, and a Python checker that finds dirty URLs automatically.
```

Scene 2 — What sitemaps are

```text
A sitemap is a list of URLs you want search engines to know about, with optional metadata. It helps discovery, especially for large sites, new sites with few links, and pages that change often. It doesn't guarantee crawling or indexing, and it doesn't replace internal linking. Think of it as a list you hand to a delivery driver. Useful, especially in a big new estate. But if the roads don't connect to the houses, the list alone won't get everything delivered.
```

Scene 3 — Hard limits and rules

```text
The hard limits. Up to fifty thousand URLs or fifty megabytes uncompressed per file, and gzip is allowed. Use a sitemap index to list multiple sitemaps. Use absolute, canonical URLs that return two hundred and are indexable. Encode in UTF-8 and escape special characters, so an ampersand becomes amp. And a sitemap can only include URLs from its own host and path, unless cross-submission is verified in Search Console.
```

Scene 4 — lastmod, priority, changefreq

```text
Now lastmod, priority and changefreq. Google uses lastmod only if it's consistently and verifiably accurate, meaning it reflects the last significant change to the main content, not the time the sitemap was generated. Set every URL to today, and you teach Google to ignore your lastmod. Priority and changefreq? Google ignores them, so you can omit them. One field matters, and only if you tell the truth.
```

Scene 5 — Specialised sitemaps

```text
Specialised sitemaps. Image sitemaps help discovery of images loaded in ways crawlers might miss; Google now only needs image loc, having deprecated the caption, title, geo-location and licence tags. Video sitemaps describe video landing pages with thumbnail, title, description and a content or player location. News sitemaps, for eligible publishers, list only articles from the last two days, up to a thousand URLs. And hreflang can live in sitemaps too, which we'll cover in the international lesson.
```

Scene 6 — Submit and segment

```text
Submission and discovery. Reference your sitemap index in robots.txt. Submit it in Search Console and Bing Webmaster Tools. Google's old sitemap ping endpoint was retired in twenty twenty-three, so rely on robots.txt, Search Console and accurate lastmod. For Bing and several other engines, IndexNow gives you instant change notifications. And segment your sitemaps by template: products, categories, guides, locations. Then you can filter the Page indexing report by sitemap and see, for example, that products are mostly crawled, currently not indexed, while guides are fine. One giant file hides that.
```

Scene 7 — Hands-on: sitemap hygiene checker

```text
Hands-on. The lesson text includes a Python checker. It follows the sitemap index recursively, handles gzip, and for each URL fetches the page without following redirects. It flags anything that isn't a two hundred, anything with noindex in the header or meta robots, and anything whose canonical points elsewhere. For big sites, sample a couple of thousand URLs per segment and run it from an allow-listed IP. A crawler's list mode can do the same job with a user interface.
```

Scene 8 — Example 1: Leeds recruitment agency

```text
Worked example one, simple. A Leeds recruitment agency's sitemap plugin includes every expired job with a two-hundred page saying this role has closed, plus tag archives and attachment pages. The checker flags hundreds of problems. The fixes: exclude tags and attachments in the plugin settings, remove closed jobs from the sitemap, and give closed jobs a four-oh-four or a useful page linking to similar live roles. The sitemap shrinks to URLs worth crawling.
```

Scene 9 — Example 2: Lahore property portal (illustrative)

```text
Worked example two, with illustrative details. A Lahore property portal generated sitemaps nightly, with every URL's lastmod set to the generation time. So the field carried no signal. The team changed the generator to use each listing's real last significant change, meaning price, status or description, not view counters. They split sitemaps into active listings, projects and guides, and added a small changed in the last forty-eight hours sitemap. Logs then showed updated listings recrawled sooner.
```

Scene 10 — Large sites: the freshness sitemap

```text
For very large sites, here's a technique worth knowing: a freshness sitemap. Alongside your full, segmented sitemaps, keep a small sitemap that contains only URLs created or substantially updated in the last few days. Because its lastmod values are genuine, it gives crawlers a compact, trustworthy list of what changed. That's valuable for marketplaces, classifieds and publishers, where new inventory must be discovered quickly. Rotate URLs out once they're older than your chosen window, and keep the full sitemaps as the complete record.
```

Scene 11 — The hygiene routine

```text
Let me give you the sitemap hygiene audit as a routine. Download all sitemaps by following the index. Crawl the URL list: every URL should return two hundred, be self-canonical, indexable and not blocked by robots.txt. Compare with your site crawl to find indexable canonical pages missing from sitemaps, and sitemap URLs missing from the crawl, which are orphans. Check lastmod accuracy against CMS update dates on a sample. Check Search Console for couldn't fetch or parsing errors. And confirm generation is automatic, because hand-built sitemaps drift out of date.
```

Scene 12 — Mistakes and measures

```text
Common mistakes. Including redirected, four-oh-four, noindexed or non-canonical URLs, which weakens trust in the file. Letting a plugin generate sitemaps for tag archives, attachments or internal search. Using lastmod as a freshness hack. And forgetting to update sitemaps after a migration: the new sitemap should list new URLs, and temporarily keeping a list of old URLs can help Google discover the redirects. Measure success with the share of sitemap URLs that are clean, and the indexed-to-submitted ratio per segment.
```

Scene 13 — Recap and try this now

```text
Recap. Sitemaps aid discovery but don't guarantee indexing. Keep them clean: canonical, indexable, two-hundred URLs only. Make lastmod truthful or leave it out. Segment by template for diagnostics, and use IndexNow for engines that support it. Try this now. Run the hygiene checker on one sitemap segment, and fix the source of every problem it finds, rather than editing the file by hand.
```

</details>

### 11. hreflang and international SEO

Lesson `hreflang-and-international-seo` · module “International SEO and structured data” · target 7 min · 12 scenes · 944 words · planned 6:49 (words ≈ 6:50) · status: **to produce**

| Term | Say |
|---|---|
| hreflang | H ref lang |
| x-default | X default |
| ccTLD | C C T L D |
| en-gb | E N dash G B |
| rtl | right to left |

| # | Time | Sec | Narration | On screen | Visual |
|---|---|---|---|---|---|
| 1 | 0:00 | 32 | If you serve customers in more than one country or language, hreflang is probably broken somewhere on your site. That isn't cynicism. It's what audits find again and again, because the rules are strict and CMSs get them subtly wrong. In this lecture you'll learn how to choose a site structure, the hreflang syntax, the rules that break most implementations, and a script that checks reciprocity, codes and targets across thousands of pages. | hreflang and international SEO<br>• Choose a structure<br>• Syntax and the rules that break<br>• A reciprocity checker | A world map with pins for UK, UAE, KSA and Pakistan; each pin links to a language version of the same page; one link is broken and flashes red. |
| 2 | 0:32 | 38 | International SEO has two separate decisions. Structure: where each version lives. And annotation: how you tell search engines which version is for whom. For structure, you can use country-code domains like dot a e or dot p k, which give clear country signals but mean separate domains to maintain. Subdirectories like slash en-g b or slash ar-s a consolidate authority and are simpler to run. Subdomains work somewhat like separate sites. And parameters are not recommended. For most businesses expanding from one site, subdirectories are the pragmatic default. | Two decisions<br>• Structure: ccTLD, subdirectory, subdomain<br>• Annotation: hreflang<br>• Subdirectories = pragmatic default | Structure comparison table animates with pros and cons; the subdirectory row gets a 'default' badge. |
| 3 | 1:10 | 41 | A few fundamentals before any code. Translate genuinely. A regional version where only the currency changes may be treated as a duplicate. Mark the language in HTML, for Arabic with lang equals a r and dir equals r t l. Never auto-redirect by IP without an alternative, because Googlebot crawls mostly from US IP addresses and may never see your local versions; use a dismissible banner suggesting the local version instead. And let users switch versions with crawlable links. Search Console's old country targeting report was retired in twenty twenty-two, so these signals matter more. | Fundamentals<br>• Genuine translation<br>• lang and dir attributes<br>• No forced IP redirects; crawlable switchers | An Arabic page flips to right-to-left layout with 'lang=ar dir=rtl' tags; a dismissible banner 'View the UAE site?' appears instead of a forced redirect. |
| 4 | 1:51 | 40 | The syntax. hreflang tells Google that URLs are equivalents in different languages or regions. The value is an ISO six-three-nine-one language code, optionally followed by an ISO three-one-six-six region code. So en dash g b for English in the United Kingdom, a r dash a e for Arabic in the UAE, u r dash p k for Urdu in Pakistan. Add x-default for the fallback, often a language selector or a global page. And here's the rule that matters most: every version in the set must include the same complete list, including itself. | Syntax<br>• language[-REGION]: en-gb, ar-ae, ur-pk<br>• x-default for the fallback<br>• Every version lists the full set, incl. itself | Code panel of seven link rel=alternate lines builds; each line's code is highlighted; a 'self' tag attaches to the current page's own line. |
| 5 | 2:31 | 40 | Now the rules that break most implementations. Return links: if page A lists B, B must list A, or the annotation may be ignored. Self-reference: each page includes itself. Canonical alignment: hreflang must point to canonical URLs that return two hundred, and each language version should self-canonicalise; never canonicalise the Arabic page to the English one. Valid codes: e n dash u k is invalid, because the UK's code is g b, and a region alone is invalid. And consistency: use one method, HTML tags, HTTP headers or sitemaps, not a mix. | Rules that break<br>• Return links; self-reference<br>• Canonical alignment (200, self-canonical)<br>• Valid codes (gb not uk); one method | Two pages with arrows; a missing return arrow flashes red. Then an 'en-uk' tag gets a red cross and morphs into 'en-gb'. |
| 6 | 3:11 | 28 | For large sites, hreflang in XML sitemaps keeps HTML lighter and is easier to generate centrally. Each URL block lists alternate links for every version, including itself, using the xhtml namespace. Repeat a block for each version with the full set. It's the same rules, just in a different place. And don't put hreflang in both HTML and sitemaps unless you're absolutely sure they agree. | hreflang in sitemaps<br>• xhtml:link alternates per URL<br>• Full set in every block<br>• Don't mix methods | XML snippet with xhtml:link lines animates; a duplicate block for the Arabic URL mirrors the English one. |
| 7 | 3:39 | 33 | Hands-on. The lesson text has a pandas script that reads your crawler's hreflang export: source, language value, target, plus status and canonical for each page. It flags invalid codes, the dash u k mistake, missing return links, targets that aren't two hundred, and targets that aren't canonical. It also counts pages missing a self-referencing annotation, and writes everything flagged to a CSV. On a site with thousands of pages and four markets, this takes minutes, not days. | Hands-on: reciprocity checker<br>• Invalid codes; '-uk' mistake<br>• Missing return links; non-200 or non-canonical targets<br>• Missing self-reference | Code walkthrough of the issue columns; a matrix of page pairs shows missing return links as empty red cells. |
| 8 | 4:12 | 34 | Worked example one, with illustrative details. A SaaS company runs slash en, slash en-g b and slash a r. UAE users see the UK page with prices in pounds. The audit shows the Arabic pages canonicalise to the English pages, a CMS default, so Google drops them from the hreflang cluster. Fixes: self-canonicals, new en-a e pages with prices in dirhams where the business genuinely serves the UAE, a complete reciprocal set, and x-default pointing to a selector page. | Example 1: UAE users see UK prices (illustrative)<br>• Arabic pages canonicalised to English<br>• Self-canonicals + en-ae pages in AED<br>• Complete set; x-default to selector | A UAE search result shows a GBP price tag; after fixes, it shows an AED price tag and the Arabic page appears for Arabic queries. |
| 9 | 4:46 | 39 | Worked example two, also illustrative. A Riyadh B2B software company runs en-s a and a r-s a plus a global English site. Arabic pages rank poorly, and English pages show to Arabic searchers. The checker finds Arabic pages listing English but not themselves, and an x-default pointing at a URL that redirects. Fixes: complete self-referencing sets, x-default to the final global URL, correct lang and dir attributes, and genuinely localised Arabic copy for key pages. Then they filter Search Console Performance by country and watch which version earns impressions. | Example 2: Arabic and English in KSA (illustrative)<br>• Missing self-reference; x-default redirects<br>• Fix sets, x-default, lang/dir<br>• Localise key pages; monitor by country | Performance chart filtered to Saudi Arabia shows impressions shifting from the English URL to the Arabic URL after the fix date. |
| 10 | 5:25 | 32 | How do you verify? Crawl all language versions with hreflang extraction and run the checker. Check coverage: does every version of a page exist? Missing translations should be left out of the set, not pointed at another language. In Search Console, filter Performance by country and see which URL version gets impressions in each market. And search from the target market, using a VPN or a localisation tool, for branded and key queries. | Verify<br>• Crawl + checker<br>• Coverage: omit missing translations<br>• GSC by country; search from the market | Four verification icons tick in sequence: crawler, checker CSV, GSC country filter, a location pin with a search bar. |
| 11 | 5:57 | 28 | Common mistakes. Using hreflang to target countries you don't actually serve with distinct content. Relying on hreflang to fix poor translations. And forgetting that hreflang is a hint; strong local relevance signals like currency, addresses, local phone numbers and local links still matter. Measure success with the number of hreflang errors going to zero, and the right URL version earning impressions in each country. | Mistakes and measures<br>• Targeting markets you don't serve<br>• hreflang can't fix bad translation<br>• KPIs: zero errors; right URL per country | Mistake chips; KPI tiles 'hreflang errors: 0' and 'correct URL share by country'. |
| 12 | 6:25 | 24 | Recap. Choose a structure, usually subdirectories. Implement complete, reciprocal, self-referencing sets that point to canonical two-hundred URLs with valid codes, using one method. Verify with a checker and with Search Console by country. Try this now. Export hreflang from your crawler, run the checker, and fix the single most common issue it reports across your templates. | Recap and try this now<br>• Complete, reciprocal, canonical, valid<br>• Run the checker<br>• Fix the most common issue first | Issue counts in a bar chart; the tallest bar shrinks to zero. End card: 'Next: structured data and rich results'. |

<details><summary>Narration per scene (paste into ElevenLabs)</summary>

Scene 1 — hreflang and international SEO

```text
If you serve customers in more than one country or language, hreflang is probably broken somewhere on your site. That isn't cynicism. It's what audits find again and again, because the rules are strict and CMSs get them subtly wrong. In this lecture you'll learn how to choose a site structure, the hreflang syntax, the rules that break most implementations, and a script that checks reciprocity, codes and targets across thousands of pages.
```

Scene 2 — Two decisions

```text
International SEO has two separate decisions. Structure: where each version lives. And annotation: how you tell search engines which version is for whom. For structure, you can use country-code domains like dot a e or dot p k, which give clear country signals but mean separate domains to maintain. Subdirectories like slash en-g b or slash ar-s a consolidate authority and are simpler to run. Subdomains work somewhat like separate sites. And parameters are not recommended. For most businesses expanding from one site, subdirectories are the pragmatic default.
```

Scene 3 — Fundamentals

```text
A few fundamentals before any code. Translate genuinely. A regional version where only the currency changes may be treated as a duplicate. Mark the language in HTML, for Arabic with lang equals a r and dir equals r t l. Never auto-redirect by IP without an alternative, because Googlebot crawls mostly from US IP addresses and may never see your local versions; use a dismissible banner suggesting the local version instead. And let users switch versions with crawlable links. Search Console's old country targeting report was retired in twenty twenty-two, so these signals matter more.
```

Scene 4 — Syntax

```text
The syntax. hreflang tells Google that URLs are equivalents in different languages or regions. The value is an ISO six-three-nine-one language code, optionally followed by an ISO three-one-six-six region code. So en dash g b for English in the United Kingdom, a r dash a e for Arabic in the UAE, u r dash p k for Urdu in Pakistan. Add x-default for the fallback, often a language selector or a global page. And here's the rule that matters most: every version in the set must include the same complete list, including itself.
```

Scene 5 — Rules that break

```text
Now the rules that break most implementations. Return links: if page A lists B, B must list A, or the annotation may be ignored. Self-reference: each page includes itself. Canonical alignment: hreflang must point to canonical URLs that return two hundred, and each language version should self-canonicalise; never canonicalise the Arabic page to the English one. Valid codes: e n dash u k is invalid, because the UK's code is g b, and a region alone is invalid. And consistency: use one method, HTML tags, HTTP headers or sitemaps, not a mix.
```

Scene 6 — hreflang in sitemaps

```text
For large sites, hreflang in XML sitemaps keeps HTML lighter and is easier to generate centrally. Each URL block lists alternate links for every version, including itself, using the xhtml namespace. Repeat a block for each version with the full set. It's the same rules, just in a different place. And don't put hreflang in both HTML and sitemaps unless you're absolutely sure they agree.
```

Scene 7 — Hands-on: reciprocity checker

```text
Hands-on. The lesson text has a pandas script that reads your crawler's hreflang export: source, language value, target, plus status and canonical for each page. It flags invalid codes, the dash u k mistake, missing return links, targets that aren't two hundred, and targets that aren't canonical. It also counts pages missing a self-referencing annotation, and writes everything flagged to a CSV. On a site with thousands of pages and four markets, this takes minutes, not days.
```

Scene 8 — Example 1: UAE users see UK prices (illustrative)

```text
Worked example one, with illustrative details. A SaaS company runs slash en, slash en-g b and slash a r. UAE users see the UK page with prices in pounds. The audit shows the Arabic pages canonicalise to the English pages, a CMS default, so Google drops them from the hreflang cluster. Fixes: self-canonicals, new en-a e pages with prices in dirhams where the business genuinely serves the UAE, a complete reciprocal set, and x-default pointing to a selector page.
```

Scene 9 — Example 2: Arabic and English in KSA (illustrative)

```text
Worked example two, also illustrative. A Riyadh B2B software company runs en-s a and a r-s a plus a global English site. Arabic pages rank poorly, and English pages show to Arabic searchers. The checker finds Arabic pages listing English but not themselves, and an x-default pointing at a URL that redirects. Fixes: complete self-referencing sets, x-default to the final global URL, correct lang and dir attributes, and genuinely localised Arabic copy for key pages. Then they filter Search Console Performance by country and watch which version earns impressions.
```

Scene 10 — Verify

```text
How do you verify? Crawl all language versions with hreflang extraction and run the checker. Check coverage: does every version of a page exist? Missing translations should be left out of the set, not pointed at another language. In Search Console, filter Performance by country and see which URL version gets impressions in each market. And search from the target market, using a VPN or a localisation tool, for branded and key queries.
```

Scene 11 — Mistakes and measures

```text
Common mistakes. Using hreflang to target countries you don't actually serve with distinct content. Relying on hreflang to fix poor translations. And forgetting that hreflang is a hint; strong local relevance signals like currency, addresses, local phone numbers and local links still matter. Measure success with the number of hreflang errors going to zero, and the right URL version earning impressions in each country.
```

Scene 12 — Recap and try this now

```text
Recap. Choose a structure, usually subdirectories. Implement complete, reciprocal, self-referencing sets that point to canonical two-hundred URLs with valid codes, using one method. Verify with a checker and with Search Console by country. Try this now. Export hreflang from your crawler, run the checker, and fix the single most common issue it reports across your templates.
```

</details>

### 12. Structured data, JSON-LD and rich results

Lesson `structured-data-and-rich-results` · module “International SEO and structured data” · target 7 min · 12 scenes · 962 words · planned 6:58 (words ≈ 6:58) · status: **to produce**

| Term | Say |
|---|---|
| JSON-LD | jason L D |
| schema.org | schema dot org |
| ISO 8601 | I S O eighty-six oh one |
| @id | at I D |

| # | Time | Sec | Narration | On screen | Visual |
|---|---|---|---|---|---|
| 1 | 0:00 | 32 | Structured data is one of the most over-sold and under-maintained parts of SEO. Over-sold, because people promise ranking boosts it doesn't give. Under-maintained, because markup goes stale, prices drift, and rich result types get retired while proposals still promise them. In this lecture you'll learn what structured data really does, which rich results still exist in twenty twenty-six, how to build clean JSON-LD from your CMS data, and how to validate it at scale. | Structured data and rich results<br>• What it really does<br>• What still exists in 2026<br>• Generate, validate, maintain | A JSON-LD code block glows beside a search result with star ratings and price; a 'retired' stamp lands on an FAQ accordion result. Title appears. |
| 2 | 0:32 | 38 | Structured data is machine-readable information about a page, usually using the schema dot org vocabulary. Google uses it to understand content and to make pages eligible for rich results, like review stars, product price and availability, breadcrumbs, event details and video features. Two truths to remember. It makes you eligible, not guaranteed; Google decides whether to show rich results. And it's not a direct ranking boost. Its value is understanding and presentation. Google recommends JSON-LD because it sits separately from your visible HTML and is easy to maintain. | What structured data does<br>• Understanding + rich result eligibility<br>• Eligible ≠ guaranteed<br>• Not a ranking boost; JSON-LD recommended | Schema properties float out of a product page and attach to a search result card as price, availability and rating. |
| 3 | 1:10 | 42 | Which rich results still exist? Google has been pruning. HowTo rich results were removed in twenty twenty-three. From June twenty twenty-five, seven little-used types were phased out: book actions, course info, claim review, estimated salary, learning video, special announcement and vehicle listing. And FAQ rich results stopped appearing on the seventh of May, twenty twenty-six, with the FAQ report and Rich Results Test support removed afterwards. Still alive: product and merchant listings, review snippets within policy, articles, recipes, video, events, breadcrumbs, organisation details, local business and job postings, among others. Always check the Search Gallery first. | Rich results in 2026<br>• Gone: HowTo (2023), 7 types (2025), FAQ (May 2026)<br>• Alive: Product, Review, Article, Recipe, Video, Event…<br>• Check the Search Gallery first | A shelf of rich result icons; three groups fall off with year labels; the remaining icons light up. |
| 4 | 1:52 | 33 | A solid starter set by site type. Every site: Organization with logo, same-as and contact details, WebSite, and BreadcrumbList. E-commerce: Product with Offer, genuine ratings and reviews, and merchant return and shipping details. Publishers: Article or NewsArticle with author, dates and images. Local businesses: a LocalBusiness subtype with address, geo coordinates and opening hours. Events: Event with dates, location and offers. Plus VideoObject and JobPosting where relevant. Use stable at-id values so entities connect across pages. | Starter set by site type<br>• Everyone: Organization, WebSite, BreadcrumbList<br>• E-commerce, publishers, local, events<br>• Stable @id to connect entities | Matrix of site types vs schema types with ticks appearing; an @id connector links Article.publisher to Organization. |
| 5 | 2:25 | 34 | Policies that trip people up. Mark up only what's visible and true on the page; hidden or misleading markup violates Google's guidelines and can lead to a manual action removing rich result eligibility. Self-serving reviews: review rich results aren't shown for reviews a LocalBusiness or Organization publishes about itself on its own site. And don't put Product markup for many products on a category page as if each were the main item. Markup describes the page's main content, honestly. | Policies that trip people up<br>• Only visible, true content<br>• No self-serving review stars<br>• One main item per page | A review-stars rich result for a business's own homepage gets a red 'not eligible' stamp; a hidden-markup icon triggers a 'manual action' notice. |
| 6 | 2:59 | 33 | What about AI search? Google's twenty twenty-six guide says no special schema is needed for AI Overviews or AI Mode. But structured data still matters for rich results and for consistent understanding of entities, products and prices, which reduces wrong facts in AI answers. And product data increasingly flows through feeds like Merchant Center as well as page markup, so keep feed and markup identical. The honest pitch is accuracy and eligibility, never guaranteed AI citations. | Structured data and AI search<br>• No special schema needed for Google's AI features<br>• Consistency reduces wrong facts<br>• Keep feeds and markup identical | An AI answer card quoting a price; three sources (page, JSON-LD, feed) show the same value with green ticks. |
| 7 | 3:32 | 35 | Hands-on. The lesson text has a Python function that builds an Article and BreadcrumbList graph from CMS fields: headline, published and modified dates with timezones, author with a profile URL, publisher by at-id, image, and breadcrumb items with positions. You render the output server-side in a script tag of type application ld plus json. The key idea is that the markup is generated from the same fields as the visible byline, dates and breadcrumbs, so they can never drift apart. | Hands-on: generate JSON-LD from CMS data<br>• Article + BreadcrumbList @graph<br>• ISO 8601 dates with timezones<br>• Same fields as the visible page | Code walkthrough of article_jsonld(): the headline, dates, author and breadcrumb comprehension highlight; the printed JSON appears on the right. |
| 8 | 4:07 | 35 | Validation workflow. The Rich Results Test shows which Google rich results a rendered page is eligible for, with errors and warnings. The Schema Markup Validator checks general schema dot org validity, including types Google doesn't use. Search Console's enhancement reports monitor valid items, errors and warnings per type sitewide. Your crawler's structured data extraction checks every template at scale. And spot-check results after deployment. Errors block eligibility; warnings mean optional properties are missing, so fix those that are true and available. | Validation workflow<br>• Rich Results Test; Schema Markup Validator<br>• Search Console enhancement reports<br>• Crawl extraction; SERP spot-checks | Five tool cards in a pipeline; an error card blocks the pipe (red); a warning card lets it pass with an amber note. |
| 9 | 4:42 | 30 | Worked example one, simple. A Karachi bakery's theme and SEO plugin both output Organization markup, with different phone numbers and logos. Both validate, but machines now see conflicting facts. The fix is disabling one source, keeping a single Organization entity with a stable at-id, and matching the phone number to the Business Profile. Duplicate conflicting blocks from multiple plugins are one of the most common findings on real audits. | Example 1: duplicate Organization blocks<br>• Theme + plugin, conflicting facts<br>• Keep one entity with stable @id<br>• Match the Business Profile | Two JSON cards overlap with conflicting phone numbers; one fades out; the remaining card links to a Business Profile card. |
| 10 | 5:12 | 37 | Worked example two, with illustrative details. A Dubai events company marks up every event, but the Rich Results Test shows errors. Start dates have no timezone. Online events have no location. And past events are still marked as scheduled. The fixes: ISO dates with plus four hours, VirtualLocation with a URL for online events and the right attendance mode, event status updated when events are cancelled, rescheduled or postponed, and past events removed from listings. The Search Console Events report then shows valid items rising. | Example 2: Dubai events (illustrative)<br>• Missing timezones; no location for online events<br>• Stale eventStatus<br>• Fix → valid items rise | Event JSON snippet with three red-underlined fields; each gets corrected ('+04:00', VirtualLocation, EventRescheduled); a report chart shows valid items rising. |
| 11 | 5:49 | 32 | Implementation tips and common mistakes. Keep dates in ISO eight-six-oh-one with a timezone. If you inject JSON-LD with JavaScript or a tag manager, confirm it appears in the rendered HTML in URL Inspection; server-side output is more robust. Avoid duplicate blocks from multiple plugins. Common mistakes: marking up reviews that aren't on the page, product markup on category pages, expecting rankings to rise, and letting structured data prices go stale after a sale ends. | Tips and mistakes<br>• ISO 8601 with timezone; server-side output<br>• No duplicate plugin blocks<br>• Stale prices; invisible reviews; ranking expectations | Tip checklist on the left; mistake chips on the right with red crosses. |
| 12 | 6:21 | 37 | Recap. Structured data gives understanding and eligibility, not rankings. Check the Search Gallery before promising any rich result, especially after the twenty twenty-five and twenty twenty-six retirements. Generate markup from the same data as the page, validate with the Rich Results Test and Search Console, and keep it in sync with feeds. Try this now. Run one URL from each of your templates through the Rich Results Test, and list every error and every rich result type you're marking up that no longer exists. | Recap and try this now<br>• Eligibility, not rankings<br>• Generate from page data; validate; sync feeds<br>• Test one URL per template | A list of templates each passes through the Rich Results Test; results summarise into 'errors' and 'retired types'. End card: 'Next module: Core Web Vitals'. |

<details><summary>Narration per scene (paste into ElevenLabs)</summary>

Scene 1 — Structured data and rich results

```text
Structured data is one of the most over-sold and under-maintained parts of SEO. Over-sold, because people promise ranking boosts it doesn't give. Under-maintained, because markup goes stale, prices drift, and rich result types get retired while proposals still promise them. In this lecture you'll learn what structured data really does, which rich results still exist in twenty twenty-six, how to build clean JSON-LD from your CMS data, and how to validate it at scale.
```

Scene 2 — What structured data does

```text
Structured data is machine-readable information about a page, usually using the schema dot org vocabulary. Google uses it to understand content and to make pages eligible for rich results, like review stars, product price and availability, breadcrumbs, event details and video features. Two truths to remember. It makes you eligible, not guaranteed; Google decides whether to show rich results. And it's not a direct ranking boost. Its value is understanding and presentation. Google recommends JSON-LD because it sits separately from your visible HTML and is easy to maintain.
```

Scene 3 — Rich results in 2026

```text
Which rich results still exist? Google has been pruning. HowTo rich results were removed in twenty twenty-three. From June twenty twenty-five, seven little-used types were phased out: book actions, course info, claim review, estimated salary, learning video, special announcement and vehicle listing. And FAQ rich results stopped appearing on the seventh of May, twenty twenty-six, with the FAQ report and Rich Results Test support removed afterwards. Still alive: product and merchant listings, review snippets within policy, articles, recipes, video, events, breadcrumbs, organisation details, local business and job postings, among others. Always check the Search Gallery first.
```

Scene 4 — Starter set by site type

```text
A solid starter set by site type. Every site: Organization with logo, same-as and contact details, WebSite, and BreadcrumbList. E-commerce: Product with Offer, genuine ratings and reviews, and merchant return and shipping details. Publishers: Article or NewsArticle with author, dates and images. Local businesses: a LocalBusiness subtype with address, geo coordinates and opening hours. Events: Event with dates, location and offers. Plus VideoObject and JobPosting where relevant. Use stable at-id values so entities connect across pages.
```

Scene 5 — Policies that trip people up

```text
Policies that trip people up. Mark up only what's visible and true on the page; hidden or misleading markup violates Google's guidelines and can lead to a manual action removing rich result eligibility. Self-serving reviews: review rich results aren't shown for reviews a LocalBusiness or Organization publishes about itself on its own site. And don't put Product markup for many products on a category page as if each were the main item. Markup describes the page's main content, honestly.
```

Scene 6 — Structured data and AI search

```text
What about AI search? Google's twenty twenty-six guide says no special schema is needed for AI Overviews or AI Mode. But structured data still matters for rich results and for consistent understanding of entities, products and prices, which reduces wrong facts in AI answers. And product data increasingly flows through feeds like Merchant Center as well as page markup, so keep feed and markup identical. The honest pitch is accuracy and eligibility, never guaranteed AI citations.
```

Scene 7 — Hands-on: generate JSON-LD from CMS data

```text
Hands-on. The lesson text has a Python function that builds an Article and BreadcrumbList graph from CMS fields: headline, published and modified dates with timezones, author with a profile URL, publisher by at-id, image, and breadcrumb items with positions. You render the output server-side in a script tag of type application ld plus json. The key idea is that the markup is generated from the same fields as the visible byline, dates and breadcrumbs, so they can never drift apart.
```

Scene 8 — Validation workflow

```text
Validation workflow. The Rich Results Test shows which Google rich results a rendered page is eligible for, with errors and warnings. The Schema Markup Validator checks general schema dot org validity, including types Google doesn't use. Search Console's enhancement reports monitor valid items, errors and warnings per type sitewide. Your crawler's structured data extraction checks every template at scale. And spot-check results after deployment. Errors block eligibility; warnings mean optional properties are missing, so fix those that are true and available.
```

Scene 9 — Example 1: duplicate Organization blocks

```text
Worked example one, simple. A Karachi bakery's theme and SEO plugin both output Organization markup, with different phone numbers and logos. Both validate, but machines now see conflicting facts. The fix is disabling one source, keeping a single Organization entity with a stable at-id, and matching the phone number to the Business Profile. Duplicate conflicting blocks from multiple plugins are one of the most common findings on real audits.
```

Scene 10 — Example 2: Dubai events (illustrative)

```text
Worked example two, with illustrative details. A Dubai events company marks up every event, but the Rich Results Test shows errors. Start dates have no timezone. Online events have no location. And past events are still marked as scheduled. The fixes: ISO dates with plus four hours, VirtualLocation with a URL for online events and the right attendance mode, event status updated when events are cancelled, rescheduled or postponed, and past events removed from listings. The Search Console Events report then shows valid items rising.
```

Scene 11 — Tips and mistakes

```text
Implementation tips and common mistakes. Keep dates in ISO eight-six-oh-one with a timezone. If you inject JSON-LD with JavaScript or a tag manager, confirm it appears in the rendered HTML in URL Inspection; server-side output is more robust. Avoid duplicate blocks from multiple plugins. Common mistakes: marking up reviews that aren't on the page, product markup on category pages, expecting rankings to rise, and letting structured data prices go stale after a sale ends.
```

Scene 12 — Recap and try this now

```text
Recap. Structured data gives understanding and eligibility, not rankings. Check the Search Gallery before promising any rich result, especially after the twenty twenty-five and twenty twenty-six retirements. Generate markup from the same data as the page, validate with the Rich Results Test and Search Console, and keep it in sync with feeds. Try this now. Run one URL from each of your templates through the Rich Results Test, and list every error and every rich result type you're marking up that no longer exists.
```

</details>

### 13. Core Web Vitals: diagnosis and fixes

Lesson `core-web-vitals-diagnosis-and-fixes` · module “Core Web Vitals, mobile-first indexing and security” · target 7 min · 12 scenes · 948 words · planned 6:53 (words ≈ 6:52) · status: **to produce**

| Term | Say |
|---|---|
| LCP | L C P |
| INP | I N P |
| CLS | C L S |
| CrUX | crux |
| bfcache | B F cache |
| AVIF | A V I F |

| # | Time | Sec | Narration | On screen | Visual |
|---|---|---|---|---|---|
| 1 | 0:00 | 32 | Core Web Vitals are where SEO, engineering and conversion rate meet. They measure what real users experience: how fast the main content appears, how quickly the page responds when you tap, and whether things jump around. In this lecture you'll learn the three metrics and their thresholds, why field data beats lab data, the proven fixes for LCP, INP and CLS, a prioritisation workflow, and how to pull real-user data with the CrUX API. | Core Web Vitals: diagnosis and fixes<br>• LCP, INP, CLS and thresholds<br>• Field vs lab data<br>• Fixes, prioritisation, CrUX API | Three gauges labelled LCP, INP and CLS swing into view, each with green, amber and red zones. Title appears. |
| 2 | 0:32 | 42 | The three metrics. Largest Contentful Paint measures loading: when the main content appears. Good is two point five seconds or less; poor is over four seconds. Interaction to Next Paint measures responsiveness across all interactions: good is two hundred milliseconds or less; poor is over five hundred. Cumulative Layout Shift measures visual stability: good is zero point one or less; poor is over zero point two five. Google evaluates them at the seventy-fifth percentile of page loads, split by mobile and desktop. INP replaced First Input Delay in March twenty twenty-four, and those thresholds haven't changed. | Metrics and thresholds (p75)<br>• LCP ≤ 2.5 s good; > 4 s poor<br>• INP ≤ 200 ms good; > 500 ms poor<br>• CLS ≤ 0.1 good; > 0.25 poor | Threshold table animates row by row; a '75th percentile' marker slides along a distribution curve. |
| 3 | 1:14 | 29 | Keep perspective. Google says Core Web Vitals are used by its ranking systems, but good scores don't guarantee top rankings, and relevance and helpfulness still dominate. So treat Core Web Vitals as a user experience and conversion investment with a modest search component, especially where competing pages are otherwise similar. And be wary of blog posts claiming new thresholds; check web dot dev for the current definitions. | Keep perspective<br>• A ranking signal, not a trump card<br>• UX and conversion investment first<br>• Verify thresholds on web.dev | Balance scale: 'relevance & helpfulness' outweighs 'CWV', but CWV tips a close tie between two similar pages. |
| 4 | 1:43 | 33 | Field versus lab. Field data comes from real users: the Chrome UX Report, shown in PageSpeed Insights and Search Console's Core Web Vitals report, and your own real-user monitoring with the web-vitals JavaScript library. That's what counts. Lab data is simulated: Lighthouse, the lab section of PageSpeed Insights, Chrome DevTools. Use it to debug, not to judge success. Lab tools can't measure INP directly, because it needs real interactions. Total Blocking Time is only a rough proxy. | Field vs lab<br>• Field (CrUX, RUM): what counts<br>• Lab (Lighthouse, DevTools): for debugging<br>• INP needs real interactions | Two panels: 'Field' with many user icons feeding a distribution; 'Lab' with a single simulated device in a test tube. |
| 5 | 2:16 | 38 | Fixing LCP. Break it into four parts and fix the biggest one. Time to first byte: slow servers and uncached HTML, fixed with full-page caching, edge caching and backend work. Resource load delay: the browser discovers the LCP image late, for example as a CSS background or a JavaScript-inserted image. Resource load duration: the image is too heavy. And element render delay: render-blocking CSS or JavaScript, or client-side rendering. Make the hero image discoverable with a preload and fetchpriority high, set its dimensions, and never lazy-load it. | Fixing LCP<br>• TTFB → caching, CDN<br>• Load delay → discoverable hero, preload<br>• Load duration → AVIF/WebP, srcset<br>• Render delay → critical CSS, less JS | Stacked bar of LCP split into four coloured segments; the largest segment shrinks as its fix label appears. Code snippet: preload + fetchpriority=high. |
| 6 | 2:54 | 33 | Fixing INP, briefly, because the next lesson goes deep. Poor INP almost always means long tasks on the main thread. Reduce and split JavaScript, and remove unused third-party scripts like heavy chat widgets or overloaded tag managers. Break up long tasks and yield to the main thread, using scheduler dot yield where available with a fallback. Give immediate visual feedback before heavy work. Avoid huge DOMs and forced synchronous layouts. And move heavy computation to web workers. | Fixing INP (overview)<br>• Long main-thread tasks are the usual cause<br>• Cut third-party JS; yield<br>• Instant feedback; smaller DOM; workers | Main-thread timeline with a long red task blocking a tap; the task splits into small chunks with gaps, and the tap is handled quickly. |
| 7 | 3:27 | 30 | Fixing CLS. Always set width and height, or a CSS aspect ratio, on images, videos and iframes. Reserve space for ads, embeds and cookie banners. Don't insert content above existing content unless the user asked for it. Reduce font-swap shifts with font-display optional or size-adjusted fallback fonts. Animate with transforms, not properties that trigger layout. And check back-forward cache eligibility, because pages restored from the bfcache avoid many shifts entirely. | Fixing CLS<br>• Dimensions on media; reserve ad/banner space<br>• No surprise insertions; font fallbacks<br>• transform animations; bfcache | A page where an ad pushes text down (red shift region), then the same page with a reserved ad box and no shift (green). |
| 8 | 3:57 | 36 | Prioritisation workflow. Open Search Console's Core Web Vitals report and list the poor and needs-improvement URL groups on mobile. Map groups to templates and weigh them by traffic and revenue. For each template, pull field data and run a lab trace to find the dominant sub-part. Write tickets with the specific fix and the expected metric movement. And after release, wait for field data to update, because CrUX uses a rolling twenty-eight-day window, and use Validate fix in Search Console to track. | Prioritisation workflow<br>• GSC groups → templates → traffic/revenue<br>• Field + lab trace per template<br>• Ticket → release → wait 28 days → validate | Five-step flow with a calendar showing a 28-day rolling window sliding along. |
| 9 | 4:33 | 37 | Hands-on. The lesson text has a short Python function for the CrUX API. You post a URL or origin, a form factor like phone, and the three metrics, and you get back the seventy-fifth percentile for each. LCP and INP come back in milliseconds, CLS as a score. A four-oh-four means there isn't enough field data for that URL, so fall back to the origin, the Search Console group, or your own real-user monitoring. For trend charts, the CrUX History API returns weekly data. | Hands-on: CrUX API<br>• POST url/origin + formFactor + metrics<br>• p75 per metric<br>• 404 = insufficient data → origin fallback | Code walkthrough of p75(); a results table shows two URLs with p75 values, one falling back to origin. |
| 10 | 5:10 | 27 | Worked example one, simple. An Abu Dhabi restaurant site has poor LCP on mobile. The trace shows the hero photo is a four-megabyte JPEG, lazy-loaded by the theme. The fixes: serve a compressed AVIF or WebP at sensible sizes with srcset, remove lazy-loading from the hero, and add fetchpriority high. Field LCP moves into good once the twenty-eight-day window rolls over. | Example 1: Abu Dhabi restaurant<br>• 4 MB lazy-loaded hero JPEG<br>• Compress, srcset, no lazy, fetchpriority=high<br>• LCP good after the window rolls | A heavy image file icon shrinks from '4 MB' to a small AVIF; an LCP gauge moves from red to green over a 28-day timeline. |
| 11 | 5:37 | 39 | Worked example two, with illustrative details. A Lahore news site fails CLS on mobile article pages. A DevTools trace with layout shift regions shows two culprits: ad slots that collapse and expand as ads load, and a web font swap that reflows headlines. Fixes: fixed-height ad containers with a sensible fallback when no ad fills, font-display optional with a size-adjusted fallback font, and moving the cookie banner to a bottom overlay. Field CLS moves into good after the window rolls over, confirmed via the CrUX API and Validate fix. | Example 2: Lahore news site (illustrative)<br>• Collapsing ad slots; font swap reflow<br>• Reserved ad space; font fallback; overlay banner<br>• CLS good, confirmed in field data | Article page with blue layout-shift highlights around ads and headline; after fixes, no highlights; CLS gauge turns green. |
| 12 | 6:16 | 37 | Recap. Know the thresholds: two point five seconds, two hundred milliseconds, zero point one, at the seventy-fifth percentile. Trust field data, debug with lab tools, fix the dominant sub-part per template, and wait for the twenty-eight-day window. Common mistakes: chasing a Lighthouse score of one hundred while field data is fine, lazy-loading the hero, and ignoring third-party scripts. Try this now. Run the CrUX script for your top five templates on phone, and write one ticket for the worst metric on the highest-revenue template. | Recap and try this now<br>• p75 thresholds; field over lab<br>• Fix per template; wait 28 days<br>• CrUX for 5 templates → 1 ticket | Five template rows with three coloured metric chips each; the worst chip on the top row turns into a ticket card. End card: 'Next: INP and real-user monitoring deep dive'. |

<details><summary>Narration per scene (paste into ElevenLabs)</summary>

Scene 1 — Core Web Vitals: diagnosis and fixes

```text
Core Web Vitals are where SEO, engineering and conversion rate meet. They measure what real users experience: how fast the main content appears, how quickly the page responds when you tap, and whether things jump around. In this lecture you'll learn the three metrics and their thresholds, why field data beats lab data, the proven fixes for LCP, INP and CLS, a prioritisation workflow, and how to pull real-user data with the CrUX API.
```

Scene 2 — Metrics and thresholds (p75)

```text
The three metrics. Largest Contentful Paint measures loading: when the main content appears. Good is two point five seconds or less; poor is over four seconds. Interaction to Next Paint measures responsiveness across all interactions: good is two hundred milliseconds or less; poor is over five hundred. Cumulative Layout Shift measures visual stability: good is zero point one or less; poor is over zero point two five. Google evaluates them at the seventy-fifth percentile of page loads, split by mobile and desktop. INP replaced First Input Delay in March twenty twenty-four, and those thresholds haven't changed.
```

Scene 3 — Keep perspective

```text
Keep perspective. Google says Core Web Vitals are used by its ranking systems, but good scores don't guarantee top rankings, and relevance and helpfulness still dominate. So treat Core Web Vitals as a user experience and conversion investment with a modest search component, especially where competing pages are otherwise similar. And be wary of blog posts claiming new thresholds; check web dot dev for the current definitions.
```

Scene 4 — Field vs lab

```text
Field versus lab. Field data comes from real users: the Chrome UX Report, shown in PageSpeed Insights and Search Console's Core Web Vitals report, and your own real-user monitoring with the web-vitals JavaScript library. That's what counts. Lab data is simulated: Lighthouse, the lab section of PageSpeed Insights, Chrome DevTools. Use it to debug, not to judge success. Lab tools can't measure INP directly, because it needs real interactions. Total Blocking Time is only a rough proxy.
```

Scene 5 — Fixing LCP

```text
Fixing LCP. Break it into four parts and fix the biggest one. Time to first byte: slow servers and uncached HTML, fixed with full-page caching, edge caching and backend work. Resource load delay: the browser discovers the LCP image late, for example as a CSS background or a JavaScript-inserted image. Resource load duration: the image is too heavy. And element render delay: render-blocking CSS or JavaScript, or client-side rendering. Make the hero image discoverable with a preload and fetchpriority high, set its dimensions, and never lazy-load it.
```

Scene 6 — Fixing INP (overview)

```text
Fixing INP, briefly, because the next lesson goes deep. Poor INP almost always means long tasks on the main thread. Reduce and split JavaScript, and remove unused third-party scripts like heavy chat widgets or overloaded tag managers. Break up long tasks and yield to the main thread, using scheduler dot yield where available with a fallback. Give immediate visual feedback before heavy work. Avoid huge DOMs and forced synchronous layouts. And move heavy computation to web workers.
```

Scene 7 — Fixing CLS

```text
Fixing CLS. Always set width and height, or a CSS aspect ratio, on images, videos and iframes. Reserve space for ads, embeds and cookie banners. Don't insert content above existing content unless the user asked for it. Reduce font-swap shifts with font-display optional or size-adjusted fallback fonts. Animate with transforms, not properties that trigger layout. And check back-forward cache eligibility, because pages restored from the bfcache avoid many shifts entirely.
```

Scene 8 — Prioritisation workflow

```text
Prioritisation workflow. Open Search Console's Core Web Vitals report and list the poor and needs-improvement URL groups on mobile. Map groups to templates and weigh them by traffic and revenue. For each template, pull field data and run a lab trace to find the dominant sub-part. Write tickets with the specific fix and the expected metric movement. And after release, wait for field data to update, because CrUX uses a rolling twenty-eight-day window, and use Validate fix in Search Console to track.
```

Scene 9 — Hands-on: CrUX API

```text
Hands-on. The lesson text has a short Python function for the CrUX API. You post a URL or origin, a form factor like phone, and the three metrics, and you get back the seventy-fifth percentile for each. LCP and INP come back in milliseconds, CLS as a score. A four-oh-four means there isn't enough field data for that URL, so fall back to the origin, the Search Console group, or your own real-user monitoring. For trend charts, the CrUX History API returns weekly data.
```

Scene 10 — Example 1: Abu Dhabi restaurant

```text
Worked example one, simple. An Abu Dhabi restaurant site has poor LCP on mobile. The trace shows the hero photo is a four-megabyte JPEG, lazy-loaded by the theme. The fixes: serve a compressed AVIF or WebP at sensible sizes with srcset, remove lazy-loading from the hero, and add fetchpriority high. Field LCP moves into good once the twenty-eight-day window rolls over.
```

Scene 11 — Example 2: Lahore news site (illustrative)

```text
Worked example two, with illustrative details. A Lahore news site fails CLS on mobile article pages. A DevTools trace with layout shift regions shows two culprits: ad slots that collapse and expand as ads load, and a web font swap that reflows headlines. Fixes: fixed-height ad containers with a sensible fallback when no ad fills, font-display optional with a size-adjusted fallback font, and moving the cookie banner to a bottom overlay. Field CLS moves into good after the window rolls over, confirmed via the CrUX API and Validate fix.
```

Scene 12 — Recap and try this now

```text
Recap. Know the thresholds: two point five seconds, two hundred milliseconds, zero point one, at the seventy-fifth percentile. Trust field data, debug with lab tools, fix the dominant sub-part per template, and wait for the twenty-eight-day window. Common mistakes: chasing a Lighthouse score of one hundred while field data is fine, lazy-loading the hero, and ignoring third-party scripts. Try this now. Run the CrUX script for your top five templates on phone, and write one ticket for the worst metric on the highest-revenue template.
```

</details>

### 14. INP deep dive: long tasks, attribution and real-user monitoring

Lesson `inp-and-real-user-monitoring` · module “Core Web Vitals, mobile-first indexing and security” · target 7 min · 12 scenes · 991 words · planned 7:10 (words ≈ 7:11) · status: **to produce**

| Term | Say |
|---|---|
| INP | I N P |
| LoAF | loaf |
| RUM | rum |
| DOM | dom |
| sendBeacon | send beacon |

| # | Time | Sec | Narration | On screen | Visual |
|---|---|---|---|---|---|
| 1 | 0:00 | 37 | Interaction to Next Paint is the Core Web Vital that sites fail most often on mobile, and it's the hardest to debug. Why? Because it depends on real interactions, on real devices, with real third-party scripts. Your laptop in the office won't reproduce a customer tapping a filter on a mid-range Android phone. In this lecture you'll learn the anatomy of an interaction, the tools that show you what's slow, and how to set up real-user monitoring that names the exact element and script responsible. | INP deep dive<br>• Anatomy of an interaction<br>• The right tools<br>• RUM that names the culprit | A finger taps a filter chip on a phone; a stopwatch runs while a spinner stalls; a magnifying glass reveals a tangled script icon. Title appears. |
| 2 | 0:37 | 34 | First, what does INP actually measure? It watches interactions during a page visit: clicks, taps and key presses. It reports a value close to the worst one, ignoring some extreme outliers on pages with many interactions. Good is two hundred milliseconds or less at the seventy-fifth percentile. Poor is over five hundred. Think of it like a restaurant. INP isn't the average wait. It's closer to, how long did the slowest table wait for someone to acknowledge them? | What INP measures<br>• All interactions during a visit<br>• Close to the worst one<br>• ≤ 200 ms good; > 500 ms poor (p75) | A restaurant floor plan with tables; each table shows a wait time; the longest wait is highlighted as 'INP'. |
| 3 | 1:11 | 31 | Every interaction has three phases. Input delay: the time until your event handlers can even start, because the main thread is busy with something else, like hydration, third-party scripts or timers. Processing duration: your handlers running. And presentation delay: the browser working out styles, layout and paint for the next frame. Here's the key idea. You can't fix INP until you know which phase dominates, because each phase has completely different fixes. | Three phases<br>• Input delay: main thread busy<br>• Processing: your handlers<br>• Presentation: style, layout, paint | A horizontal bar split into three coloured segments labelled Input delay, Processing, Presentation, with a tap icon at the start and a paint icon at the end. |
| 4 | 1:42 | 44 | Now tools. The Chrome DevTools Performance panel lets you record an interaction and see the long tasks around it, and its live metrics show INP for interactions you perform. The Long Animation Frames API, often called LoAF, exposes frames that took more than fifty milliseconds, with script attribution: which script, which function, which source URL. That's far more useful than the older Long Tasks API. The web-vitals library's attribution build reports INP from real users, with the target element, the phase breakdown and those LoAF entries. And CrUX and Search Console tell you that you have a problem, but not why. | The toolkit<br>• DevTools Performance panel<br>• Long Animation Frames (LoAF) API<br>• web-vitals attribution build; CrUX tells 'that', not 'why' | Four tool cards: a DevTools flame chart, a LoAF entry listing a script URL, a web-vitals JSON payload, and a CrUX gauge with a 'why?' bubble. |
| 5 | 2:26 | 37 | Hands-on. The lesson text has a small script using the web-vitals attribution build. When INP is reported, it sends a beacon with the value and rating, the interaction target as a CSS selector, whether it was a pointer or keyboard interaction, the three phase durations, the source URLs of scripts from the long animation frames, and the page path. In production, pin and self-host the library rather than loading it from a public CDN, and check the README for field names when you upgrade. | Hands-on: RUM for INP<br>• onINP from the attribution build<br>• Target, type, three phases, script URLs<br>• sendBeacon to your endpoint | Code walkthrough: onINP callback fields highlight one by one; the JSON body forms and flies to a '/rum' endpoint icon. |
| 6 | 3:03 | 34 | Then aggregate. On your server or in your analytics warehouse, group by page template and by target, and rank by the seventy-fifth percentile of the value. Now you have a list like: on category pages, taps on the filter chip button are the slowest interaction, mostly processing time, and the scripts involved are our main bundle plus a heatmap tool. That's a ticket a developer can act on. Compare that with, INP is poor, please improve. Night and day. | Aggregate and rank<br>• Group by template and target<br>• Rank by p75<br>• Turns 'INP is poor' into a precise ticket | A table aggregates: template, target, p75 INP, dominant phase, top scripts; the top row highlights and converts into a ticket card. |
| 7 | 3:37 | 37 | Fixes by phase. For input delay, reduce what's running when people interact: defer or remove non-essential third-party scripts, load chat widgets only when someone clicks them, split hydration with islands or partial hydration, and avoid long timers and polling. For processing, make handlers cheap: give instant visual feedback, then yield to the main thread before heavy work, using scheduler dot yield with a setTimeout fallback. For presentation delay, keep rendering cheap: smaller DOMs, virtualised long lists, no layout thrashing, and content-visibility auto for off-screen sections. | Fixes by phase<br>• Input delay: fewer scripts, split hydration<br>• Processing: feedback first, then yield<br>• Presentation: smaller DOM, virtualise, no thrashing | Three columns, one per phase, each filling with fix chips; a code snippet shows showSpinner() then scheduler.yield(). |
| 8 | 4:14 | 31 | Worked example one, simple. A Birmingham salon's booking page has a date picker that freezes for a moment on older phones. DevTools shows a single long task when the calendar opens, as the script builds a whole year of dates at once. The fix is building only the visible month, and giving instant feedback when the button is tapped. Processing duration drops, and the page feels responsive. Small change, big difference. | Example 1: salon booking calendar<br>• Builds a whole year on open<br>• Build the visible month; instant feedback<br>• Processing time drops | Flame chart with one long 'buildCalendar' task shrinking into a short task; a phone mock shows the calendar opening smoothly. |
| 9 | 4:45 | 42 | Worked example two, with illustrative details. A Dubai marketplace has poor mobile INP on category pages. RUM shows the worst interactions are taps on filter chips, dominated by processing time, and LoAF attribution points at the site's own bundle plus a heatmap script loaded through the tag manager. Fixes: the filter handler updates the chip immediately and yields before recomputing the grid; the grid is virtualised; and the heatmap script is sampled to a small share of sessions. Real-user p75 INP for filter taps drops into the good range over the following weeks, later confirmed in CrUX. | Example 2: Dubai marketplace filters (illustrative)<br>• Filter chip taps: processing-heavy<br>• Own bundle + heatmap script<br>• Yield, virtualise, sample the heatmap | RUM chart for 'button.filter-chip' p75 INP falling from red into green over several weeks (illustrative), with annotations for each fix. |
| 10 | 5:27 | 31 | How do you measure success? Real-user p75 INP by template and by top interaction target. The share of interactions rated good. And the CrUX or Search Console status for the URL group after the twenty-eight-day window. Common mistakes: optimising Total Blocking Time in Lighthouse and assuming INP is fixed; collecting INP without attribution, so nobody knows which element is slow; and blaming the framework before checking third-party scripts in the LoAF data. | Measures and mistakes<br>• RUM p75 by template and target; % good<br>• CrUX after 28 days<br>• TBT ≠ INP; no attribution; blaming the framework | KPI tiles on the left; three mistake chips on the right. |
| 11 | 5:58 | 41 | Where should you send the RUM data? For a small site, sending INP as a GA4 event with custom parameters for the target and phases works, and you can explore it in GA4 or export it to BigQuery. For larger sites, a dedicated endpoint that writes to your data warehouse, or a commercial RUM product, gives you more control and volume. Either way, sample if traffic is huge, respect consent requirements for analytics in your markets, and never send personal data in the payload. A CSS selector and a script URL are all you need. | Where RUM data goes<br>• Small sites: GA4 event → BigQuery<br>• Larger: own endpoint or RUM product<br>• Sample; respect consent; no personal data | Two pipelines: a beacon flowing into a GA4 event then a BigQuery table; another into a warehouse icon. A consent toggle gate sits before both. |
| 12 | 6:39 | 31 | Recap. INP captures near-worst interaction latency, split into input delay, processing and presentation. Use DevTools and LoAF to debug, and the web-vitals attribution build for real-user data that names the element and script. Fix the dominant phase. Try this now. Add the attribution snippet to one template for a week, collecting to a simple endpoint or as a GA4 event. Then rank the top five slowest targets and pick one to fix. | Recap and try this now<br>• Know the three phases<br>• Add attribution RUM to one template<br>• Rank top 5 slow targets; fix one | A week-long collection bar fills; a ranked list of five targets appears; the first becomes a ticket. End card: 'Next: mobile-first indexing, HTTPS and security'. |

<details><summary>Narration per scene (paste into ElevenLabs)</summary>

Scene 1 — INP deep dive

```text
Interaction to Next Paint is the Core Web Vital that sites fail most often on mobile, and it's the hardest to debug. Why? Because it depends on real interactions, on real devices, with real third-party scripts. Your laptop in the office won't reproduce a customer tapping a filter on a mid-range Android phone. In this lecture you'll learn the anatomy of an interaction, the tools that show you what's slow, and how to set up real-user monitoring that names the exact element and script responsible.
```

Scene 2 — What INP measures

```text
First, what does INP actually measure? It watches interactions during a page visit: clicks, taps and key presses. It reports a value close to the worst one, ignoring some extreme outliers on pages with many interactions. Good is two hundred milliseconds or less at the seventy-fifth percentile. Poor is over five hundred. Think of it like a restaurant. INP isn't the average wait. It's closer to, how long did the slowest table wait for someone to acknowledge them?
```

Scene 3 — Three phases

```text
Every interaction has three phases. Input delay: the time until your event handlers can even start, because the main thread is busy with something else, like hydration, third-party scripts or timers. Processing duration: your handlers running. And presentation delay: the browser working out styles, layout and paint for the next frame. Here's the key idea. You can't fix INP until you know which phase dominates, because each phase has completely different fixes.
```

Scene 4 — The toolkit

```text
Now tools. The Chrome DevTools Performance panel lets you record an interaction and see the long tasks around it, and its live metrics show INP for interactions you perform. The Long Animation Frames API, often called LoAF, exposes frames that took more than fifty milliseconds, with script attribution: which script, which function, which source URL. That's far more useful than the older Long Tasks API. The web-vitals library's attribution build reports INP from real users, with the target element, the phase breakdown and those LoAF entries. And CrUX and Search Console tell you that you have a problem, but not why.
```

Scene 5 — Hands-on: RUM for INP

```text
Hands-on. The lesson text has a small script using the web-vitals attribution build. When INP is reported, it sends a beacon with the value and rating, the interaction target as a CSS selector, whether it was a pointer or keyboard interaction, the three phase durations, the source URLs of scripts from the long animation frames, and the page path. In production, pin and self-host the library rather than loading it from a public CDN, and check the README for field names when you upgrade.
```

Scene 6 — Aggregate and rank

```text
Then aggregate. On your server or in your analytics warehouse, group by page template and by target, and rank by the seventy-fifth percentile of the value. Now you have a list like: on category pages, taps on the filter chip button are the slowest interaction, mostly processing time, and the scripts involved are our main bundle plus a heatmap tool. That's a ticket a developer can act on. Compare that with, INP is poor, please improve. Night and day.
```

Scene 7 — Fixes by phase

```text
Fixes by phase. For input delay, reduce what's running when people interact: defer or remove non-essential third-party scripts, load chat widgets only when someone clicks them, split hydration with islands or partial hydration, and avoid long timers and polling. For processing, make handlers cheap: give instant visual feedback, then yield to the main thread before heavy work, using scheduler dot yield with a setTimeout fallback. For presentation delay, keep rendering cheap: smaller DOMs, virtualised long lists, no layout thrashing, and content-visibility auto for off-screen sections.
```

Scene 8 — Example 1: salon booking calendar

```text
Worked example one, simple. A Birmingham salon's booking page has a date picker that freezes for a moment on older phones. DevTools shows a single long task when the calendar opens, as the script builds a whole year of dates at once. The fix is building only the visible month, and giving instant feedback when the button is tapped. Processing duration drops, and the page feels responsive. Small change, big difference.
```

Scene 9 — Example 2: Dubai marketplace filters (illustrative)

```text
Worked example two, with illustrative details. A Dubai marketplace has poor mobile INP on category pages. RUM shows the worst interactions are taps on filter chips, dominated by processing time, and LoAF attribution points at the site's own bundle plus a heatmap script loaded through the tag manager. Fixes: the filter handler updates the chip immediately and yields before recomputing the grid; the grid is virtualised; and the heatmap script is sampled to a small share of sessions. Real-user p75 INP for filter taps drops into the good range over the following weeks, later confirmed in CrUX.
```

Scene 10 — Measures and mistakes

```text
How do you measure success? Real-user p75 INP by template and by top interaction target. The share of interactions rated good. And the CrUX or Search Console status for the URL group after the twenty-eight-day window. Common mistakes: optimising Total Blocking Time in Lighthouse and assuming INP is fixed; collecting INP without attribution, so nobody knows which element is slow; and blaming the framework before checking third-party scripts in the LoAF data.
```

Scene 11 — Where RUM data goes

```text
Where should you send the RUM data? For a small site, sending INP as a GA4 event with custom parameters for the target and phases works, and you can explore it in GA4 or export it to BigQuery. For larger sites, a dedicated endpoint that writes to your data warehouse, or a commercial RUM product, gives you more control and volume. Either way, sample if traffic is huge, respect consent requirements for analytics in your markets, and never send personal data in the payload. A CSS selector and a script URL are all you need.
```

Scene 12 — Recap and try this now

```text
Recap. INP captures near-worst interaction latency, split into input delay, processing and presentation. Use DevTools and LoAF to debug, and the web-vitals attribution build for real-user data that names the element and script. Fix the dominant phase. Try this now. Add the attribution snippet to one template for a week, collecting to a simple endpoint or as a GA4 event. Then rank the top five slowest targets and pick one to fix.
```

</details>

### 15. Mobile-first indexing, HTTPS and security headers

Lesson `mobile-first-https-and-security-headers` · module “Core Web Vitals, mobile-first indexing and security” · target 7 min · 13 scenes · 954 words · planned 6:55 (words ≈ 6:55) · status: **to produce**

| Term | Say |
|---|---|
| HSTS | H S T S |
| CSP | C S P |
| TLS | T L S |
| MFA | M F A |
| nosniff | no sniff |

| # | Time | Sec | Narration | On screen | Visual |
|---|---|---|---|---|---|
| 1 | 0:00 | 31 | Google predominantly indexes the mobile version of your site. So if your mobile pages show less content, fewer links or different tags than desktop, that's what Google sees. In this lecture you'll learn how mobile-first indexing works in practice, the parity checklist, what to do about interstitials, the HTTPS essentials, a sensible baseline of security headers, and how to monitor for hacked content, because a compromised site suffers badly in search. | Mobile-first, HTTPS and security<br>• Mobile parity<br>• HTTPS essentials<br>• Security headers and hack monitoring | Desktop and mobile versions of a page side by side; the mobile one is missing a block of links, highlighted in red. Title appears. |
| 2 | 0:31 | 30 | Mobile-first indexing means Google uses the mobile version of content, crawled by the smartphone Googlebot, for indexing and ranking. For responsive sites, where the same HTML serves every device, this is mostly a non-issue. Problems appear when the mobile experience differs. Think of it like a shop window. If your street-facing window shows only half your stock, passers-by assume that's all you sell, even if the back room is full. | Mobile-first indexing<br>• Smartphone Googlebot, mobile content<br>• Responsive = mostly fine<br>• Differences = risk | A shop-window illustration: the window (mobile) shows half the products; the back room (desktop) holds the rest, greyed out to passers-by. |
| 3 | 1:01 | 35 | The parity checklist. Primary content should be the same. Hiding content in tabs or accordions is fine if it's in the HTML; removing it from mobile removes it from Google. Metadata should match: titles, descriptions and robots meta, and a mobile template that accidentally outputs noindex affects your whole index. Structured data should match. Internal links should match, because a slimmed mobile menu that drops category links reduces discovery. And images and lazy-loaded content should load without swipes or clicks. | Parity checklist<br>• Same content (accordions OK)<br>• Same metadata and structured data<br>• Same internal links; lazy-load without interaction | Checklist with five rows; each row shows a mobile and desktop icon with an equals sign turning green. |
| 4 | 1:36 | 31 | Legacy separate mobile sites, like m dot example dot com, need rel alternate from desktop to mobile and rel canonical from mobile to desktop, plus equivalent content. Honestly, migrating to responsive design removes a whole class of errors, and it's what most sites should do. To check parity, use URL Inspection, which uses the smartphone crawler, and crawl with a mobile user agent, comparing content, links and directives with a desktop crawl. | Separate mobile sites<br>• rel=alternate / rel=canonical pairs<br>• Equivalent content<br>• Responsive removes a class of errors | Two domains linked with bidirectional annotations; then they merge into one responsive site icon. |
| 5 | 2:07 | 29 | Intrusive interstitials. Full-screen pop-ups that block content right after someone arrives from search make a poor experience. Legally required interstitials, like cookie consent or age verification, are acceptable, but design them to be small and dismissible, and keep the underlying content in the HTML so it can still be crawled. A consent banner that covers the whole screen and pushes content around can also hurt CLS. | Interstitials<br>• Avoid full-screen blockers after search clicks<br>• Legal ones OK: small, dismissible<br>• Keep content in the HTML | A phone screen fully covered by a pop-up (red cross) vs a slim bottom consent banner with content visible (green tick). |
| 6 | 2:36 | 29 | HTTPS essentials. It's a lightweight ranking signal and a baseline of trust. Have a valid certificate covering all hosts. Redirect HTTP to HTTPS sitewide in a single hop. Make canonicals, hreflang, sitemaps and internal links all use HTTPS. Eliminate mixed content, meaning HTTP images, scripts or iframes on HTTPS pages. And use a modern TLS configuration, at least TLS one point two, preferably one point three. | HTTPS essentials<br>• Valid cert for all hosts; one-hop redirect<br>• HTTPS everywhere in signals<br>• No mixed content; TLS 1.2+ | A padlock icon; five checklist items tick; a mixed-content warning on an image flips to secure. |
| 7 | 3:05 | 32 | Security headers aren't ranking factors in their own right, but a hacked site suffers severely, with spam injections, malware warnings and manual actions. A sensible baseline: Strict-Transport-Security to force HTTPS, starting with a short max-age before increasing, and adding preload only when you're certain. A Content Security Policy, deployed first in report-only mode, because tag managers and widgets often need allow-listing. X-Content-Type-Options nosniff. A Referrer-Policy like strict-origin-when-cross-origin. And frame-ancestors in CSP to prevent clickjacking. | Security header baseline<br>• HSTS (short max-age first)<br>• CSP (report-only first); frame-ancestors<br>• nosniff; Referrer-Policy | HTTP response header panel builds line by line; a 'Report-Only' badge sits on the CSP line; an HSTS max-age dial turns up gradually. |
| 8 | 3:37 | 29 | Hands-on. The lesson text includes a Python parity and header check. It fetches a URL with a mobile and a desktop user agent, and compares status, number of links, word count, JSON-LD blocks, robots meta and canonical. For the mobile response it also prints the key security headers or flags them as missing. It checks raw HTML only, so for JavaScript sites compare rendered output too. | Hands-on: parity + header check<br>• Mobile vs desktop: links, words, JSON-LD, robots, canonical<br>• Security headers present?<br>• Raw HTML; render JS sites separately | Code walkthrough; output table shows mobile links 42 vs desktop 118 flagged, and 'Content-Security-Policy MISSING'. |
| 9 | 4:06 | 30 | Worked example one, simple. A Leeds charity moved images to a new CDN domain, but some templates still reference the old HTTP image URLs. Browsers block or warn on mixed content, and some images disappear on mobile. A crawl for HTTP resources on HTTPS pages finds them in minutes. The fix is updating templates to the HTTPS CDN URLs and adding an upgrade-insecure-requests directive as a safety net. | Example 1: mixed content after a CDN change<br>• Old http:// image URLs in templates<br>• Crawl for insecure resources<br>• Update templates; upgrade-insecure-requests | Page with broken image placeholders and a 'Not secure' warning; after fix, images load and the padlock returns. |
| 10 | 4:36 | 33 | Worked example two, with illustrative details. A Karachi electronics retailer launched a cleaner mobile menu showing only six top categories, while desktop linked to forty subcategories. Because Google indexes the mobile version, subcategory pages lost most of their internal links, and their crawl frequency fell in the logs. The fix kept the clean look, but rendered all the subcategory links inside an accessible, collapsible menu present in the HTML. Parity checks were added to the release checklist. | Example 2: slimmed mobile menu (illustrative)<br>• 6 links on mobile vs 40 on desktop<br>• Subcategories lose links and crawl<br>• Collapsible full menu in the HTML | Mobile menu expands from six items to a collapsible tree of forty; a log chart of subcategory crawl frequency recovers. |
| 11 | 5:09 | 36 | Hacked-site monitoring and common mistakes. Watch Search Console's Security issues and Manual actions reports with email alerts on. Occasionally search your site for spam terms, and look for unfamiliar URLs or queries in languages you don't publish in. Keep your CMS and plugins patched and enforce multi-factor authentication. Common mistakes: a mobile template with fewer links or missing structured data, mixed content after a domain change, setting HSTS with include subdomains before a legacy subdomain supports HTTPS, and believing security headers lift rankings. | Monitoring and mistakes<br>• Security issues + Manual actions alerts<br>• Spam-term checks; patching; MFA<br>• Parity gaps; mixed content; premature HSTS | Alert bell icons on two Search Console report cards; mistake chips below. |
| 12 | 5:45 | 44 | Let's talk about the HSTS trap in a bit more detail, because it catches experienced teams. Strict-Transport-Security tells browsers to use HTTPS for your domain for a period of time. Add include subdomains, and every subdomain must support HTTPS too. If you have an old subdomain, maybe a legacy blog or an internal tool, that only works on HTTP, returning visitors suddenly can't reach it at all. And if you've submitted your domain to the browser preload list, undoing that is slow. So roll out in stages: short max-age, confirm every subdomain, then increase, and add preload only when you're certain. | The HSTS trap<br>• includeSubDomains needs HTTPS everywhere<br>• Legacy HTTP subdomains become unreachable<br>• Stage it: short max-age → confirm → increase → preload | Domain tree with subdomains; one legacy subdomain lacks a padlock and turns red when includeSubDomains switches on. A staged rollout slider moves step by step. |
| 13 | 6:29 | 26 | Recap. Google indexes your mobile version, so make it equal in content, metadata, structured data and links. Keep interstitials small, HTTPS clean, and security headers sensible, and monitor for hacks. Try this now. Run the parity script on one URL from each main template, and fix any template where mobile has noticeably fewer links, words or JSON-LD blocks than desktop. | Recap and try this now<br>• Mobile = the indexed version<br>• HTTPS + headers + monitoring<br>• Parity check on each template | Template list with mobile/desktop bars; one mismatched template gets a ticket. End card: 'Next module: migrations, auditing and monitoring'. |

<details><summary>Narration per scene (paste into ElevenLabs)</summary>

Scene 1 — Mobile-first, HTTPS and security

```text
Google predominantly indexes the mobile version of your site. So if your mobile pages show less content, fewer links or different tags than desktop, that's what Google sees. In this lecture you'll learn how mobile-first indexing works in practice, the parity checklist, what to do about interstitials, the HTTPS essentials, a sensible baseline of security headers, and how to monitor for hacked content, because a compromised site suffers badly in search.
```

Scene 2 — Mobile-first indexing

```text
Mobile-first indexing means Google uses the mobile version of content, crawled by the smartphone Googlebot, for indexing and ranking. For responsive sites, where the same HTML serves every device, this is mostly a non-issue. Problems appear when the mobile experience differs. Think of it like a shop window. If your street-facing window shows only half your stock, passers-by assume that's all you sell, even if the back room is full.
```

Scene 3 — Parity checklist

```text
The parity checklist. Primary content should be the same. Hiding content in tabs or accordions is fine if it's in the HTML; removing it from mobile removes it from Google. Metadata should match: titles, descriptions and robots meta, and a mobile template that accidentally outputs noindex affects your whole index. Structured data should match. Internal links should match, because a slimmed mobile menu that drops category links reduces discovery. And images and lazy-loaded content should load without swipes or clicks.
```

Scene 4 — Separate mobile sites

```text
Legacy separate mobile sites, like m dot example dot com, need rel alternate from desktop to mobile and rel canonical from mobile to desktop, plus equivalent content. Honestly, migrating to responsive design removes a whole class of errors, and it's what most sites should do. To check parity, use URL Inspection, which uses the smartphone crawler, and crawl with a mobile user agent, comparing content, links and directives with a desktop crawl.
```

Scene 5 — Interstitials

```text
Intrusive interstitials. Full-screen pop-ups that block content right after someone arrives from search make a poor experience. Legally required interstitials, like cookie consent or age verification, are acceptable, but design them to be small and dismissible, and keep the underlying content in the HTML so it can still be crawled. A consent banner that covers the whole screen and pushes content around can also hurt CLS.
```

Scene 6 — HTTPS essentials

```text
HTTPS essentials. It's a lightweight ranking signal and a baseline of trust. Have a valid certificate covering all hosts. Redirect HTTP to HTTPS sitewide in a single hop. Make canonicals, hreflang, sitemaps and internal links all use HTTPS. Eliminate mixed content, meaning HTTP images, scripts or iframes on HTTPS pages. And use a modern TLS configuration, at least TLS one point two, preferably one point three.
```

Scene 7 — Security header baseline

```text
Security headers aren't ranking factors in their own right, but a hacked site suffers severely, with spam injections, malware warnings and manual actions. A sensible baseline: Strict-Transport-Security to force HTTPS, starting with a short max-age before increasing, and adding preload only when you're certain. A Content Security Policy, deployed first in report-only mode, because tag managers and widgets often need allow-listing. X-Content-Type-Options nosniff. A Referrer-Policy like strict-origin-when-cross-origin. And frame-ancestors in CSP to prevent clickjacking.
```

Scene 8 — Hands-on: parity + header check

```text
Hands-on. The lesson text includes a Python parity and header check. It fetches a URL with a mobile and a desktop user agent, and compares status, number of links, word count, JSON-LD blocks, robots meta and canonical. For the mobile response it also prints the key security headers or flags them as missing. It checks raw HTML only, so for JavaScript sites compare rendered output too.
```

Scene 9 — Example 1: mixed content after a CDN change

```text
Worked example one, simple. A Leeds charity moved images to a new CDN domain, but some templates still reference the old HTTP image URLs. Browsers block or warn on mixed content, and some images disappear on mobile. A crawl for HTTP resources on HTTPS pages finds them in minutes. The fix is updating templates to the HTTPS CDN URLs and adding an upgrade-insecure-requests directive as a safety net.
```

Scene 10 — Example 2: slimmed mobile menu (illustrative)

```text
Worked example two, with illustrative details. A Karachi electronics retailer launched a cleaner mobile menu showing only six top categories, while desktop linked to forty subcategories. Because Google indexes the mobile version, subcategory pages lost most of their internal links, and their crawl frequency fell in the logs. The fix kept the clean look, but rendered all the subcategory links inside an accessible, collapsible menu present in the HTML. Parity checks were added to the release checklist.
```

Scene 11 — Monitoring and mistakes

```text
Hacked-site monitoring and common mistakes. Watch Search Console's Security issues and Manual actions reports with email alerts on. Occasionally search your site for spam terms, and look for unfamiliar URLs or queries in languages you don't publish in. Keep your CMS and plugins patched and enforce multi-factor authentication. Common mistakes: a mobile template with fewer links or missing structured data, mixed content after a domain change, setting HSTS with include subdomains before a legacy subdomain supports HTTPS, and believing security headers lift rankings.
```

Scene 12 — The HSTS trap

```text
Let's talk about the HSTS trap in a bit more detail, because it catches experienced teams. Strict-Transport-Security tells browsers to use HTTPS for your domain for a period of time. Add include subdomains, and every subdomain must support HTTPS too. If you have an old subdomain, maybe a legacy blog or an internal tool, that only works on HTTP, returning visitors suddenly can't reach it at all. And if you've submitted your domain to the browser preload list, undoing that is slow. So roll out in stages: short max-age, confirm every subdomain, then increase, and add preload only when you're certain.
```

Scene 13 — Recap and try this now

```text
Recap. Google indexes your mobile version, so make it equal in content, metadata, structured data and links. Keep interstitials small, HTTPS clean, and security headers sensible, and monitor for hacks. Try this now. Run the parity script on one URL from each main template, and fix any template where mobile has noticeably fewer links, words or JSON-LD blocks than desktop.
```

</details>

### 16. Site migrations: a zero-drama checklist

Lesson `site-migrations-checklist` · module “Migrations, auditing workflow and monitoring” · target 7 min · 12 scenes · 958 words · planned 6:56 (words ≈ 6:57) · status: **to produce**

| Term | Say |
|---|---|
| SKU | S K U |
| PHP | P H P |
| Diwali | di-wah-lee |

| # | Time | Sec | Narration | On screen | Visual |
|---|---|---|---|---|---|
| 1 | 0:00 | 30 | A site migration is the single riskiest thing most websites ever do in search. Done well, visibility dips briefly and recovers. Done badly, years of rankings can disappear in a week. In this lecture you'll learn what counts as a migration, a four-phase plan from benchmarking to post-launch monitoring, how to build and test a redirect map with a script, and how to set rollback criteria before you need them. | Site migrations<br>• What counts as a migration<br>• Four phases, zero drama<br>• Redirect maps you can prove | A moving truck carrying page icons from an old building to a new one; a few pages fall off the back labelled '404'. Title appears. |
| 2 | 0:30 | 36 | What counts as a migration? Any change that alters URLs, hosts, templates or rendering at scale. A domain change. HTTP to HTTPS. A URL restructure. A platform or CMS change, like WordPress to Shopify or a move to a headless front end. Merging sites. And major redesigns. Doing several at once multiplies risk, so if you can, separate a domain move from a replatform. And set expectations in writing: even perfect migrations can fluctuate while Google recrawls, and larger sites take longer. | What counts<br>• Domain, protocol, URLs, platform, merges, redesigns<br>• Don't stack changes if you can avoid it<br>• Expect temporary fluctuation | Six migration type cards; three of them stack on top of each other and a risk meter jumps to red; they separate and the meter drops. |
| 3 | 1:06 | 30 | Phase one: planning, weeks before. Define scope and owners: who signs off redirects, who deploys, who monitors. Benchmark everything: a full crawl, Search Console exports by page and query, indexing counts, analytics landing pages and conversions, rank tracking, top-linked URLs and a log sample. Then inventory old URLs from every source: crawl, sitemaps, Search Console, analytics, backlinks and logs. Crawls alone miss old URLs that still receive links. | Phase 1: plan<br>• Owners and sign-off<br>• Benchmark everything<br>• Inventory URLs from every source | Planning board with sticky notes: owners, benchmark sources, URL sources; a funnel merges URL lists into one inventory. |
| 4 | 1:36 | 35 | Build the redirect map. Old URL to the most relevant new URL, in one hop, with a three-oh-one or three-oh-eight. Prioritise by traffic, links and revenue. Pages with no equivalent get a deliberate four-oh-four or four-ten. Plan content and template parity: titles, meta descriptions, headings, body content, internal links, structured data, hreflang, canonicals and alt text, carried over or consciously improved. And choose timing away from peak seasons, like Ramadan and Eid campaigns for Gulf retailers, Black Friday, Diwali or back-to-school. | The redirect map + parity + timing<br>• One hop, most relevant, 301/308<br>• Deliberate 404/410 for no equivalent<br>• Template parity; avoid peak seasons | CSV redirect map scrolls with columns old_url, new_url, type, priority; a calendar greys out peak weeks. |
| 5 | 2:11 | 37 | Phase two: staging and pre-launch QA. Protect staging with authentication, not just robots.txt. Crawl staging and compare against the benchmark: missing pages, title changes, lost internal links, structured data errors, canonicals pointing at staging hosts. Test the redirect map against staging with a list crawl or the script in the lesson text. Check that the production robots.txt and meta robots won't ship a Disallow slash or a staging noindex. Check rendering and Core Web Vitals of new templates. And prepare the new XML sitemaps. | Phase 2: staging QA<br>• Authenticated staging<br>• Crawl diff vs benchmark<br>• Test redirects; check production robots; CWV | Staging environment behind a lock; a diff view between old and new crawls highlights missing pages and changed titles. |
| 6 | 2:48 | 38 | Hands-on. The script in the lesson text reads your redirect map and requests each old URL. For normal rows it passes only if there's exactly one redirect hop, a three-oh-one or three-oh-eight, ending at a two-hundred page with the expected path. For rows marked four-ten, it passes only if the old URL returns four-ten directly. It prints pass and fail counts and writes failures to a CSV. Run it on staging before launch, on production within the first hour, and daily for two weeks on the high-priority tier. | Hands-on: redirect map validator<br>• One hop, 301/308, 200, expected path<br>• 410 rows return 410<br>• Staging → launch hour → daily | Code walkthrough of the ok condition; output 'OK 5,412 / FAIL 88' (illustrative); failures CSV icon. |
| 7 | 3:26 | 36 | Phase three: launch day. Deploy redirects together with the new site. Immediately verify robots.txt, and a sample of meta robots and canonicals, on production. Crawl the old URL list and confirm one-hop redirects to two-hundred pages. For domain moves, verify both properties in Search Console and use the Change of Address tool. Submit new sitemaps; keeping an old-URL sitemap available briefly can help Google discover the redirects. And update external profiles you control: Business Profile, social profiles, directories, ad destinations and email templates. | Phase 3: launch day<br>• Redirects ship with the site<br>• Verify robots, meta robots, canonicals<br>• Change of Address; sitemaps; external profiles | Launch checklist with a clock showing the first hour; each item ticks; a Change of Address form mock completes. |
| 8 | 4:02 | 36 | Phase four: post-launch monitoring, for four to twelve weeks. Daily for the first two weeks: four-oh-fours and five hundreds on old and new URLs, and Googlebot crawling of new URLs in logs and Crawl stats. Weekly: indexing of new versus old URLs, traffic and conversions by page group against the benchmark, rankings for priority queries, and structured data and Core Web Vitals reports. Keep redirects for at least a year, as Google advises, and ideally indefinitely. And keep the old domain registered. | Phase 4: monitor 4–12 weeks<br>• Daily: errors, crawl of new URLs<br>• Weekly: indexing, traffic, rankings, CWV<br>• Redirects ≥ 1 year; keep old domain | Monitoring dashboard with daily and weekly tiles; a timeline shows old URLs fading and new URLs rising in the index. |
| 9 | 4:38 | 39 | Worked example one, with illustrative details. A Lahore retailer moves from a custom PHP store to a hosted e-commerce platform that forces its own product and collection URL patterns. The team exports every legacy URL, including those found only in backlinks and logs, maps products by SKU and categories by name, uses the platform's redirect manager for one-hop redirects, and recreates custom title templates. After launch, logs reveal thousands of hits to old image URLs used in Google Images, so they add image redirects too. That's a commonly forgotten item. | Example 1: Lahore replatform (illustrative)<br>• SKU-based product mapping<br>• One-hop redirects via platform manager<br>• Don't forget image URLs | Mapping table joins old and new by SKU; after launch, a log panel shows old image URL hits; image redirects are added. |
| 10 | 5:17 | 37 | Worked example two, also illustrative. A UAE beauty brand merges its separate Arabic domain into an ar-a e folder on its main site. The plan separates the domain move from any redesign, maps every Arabic URL one-to-one, keeps hreflang reciprocal between English and Arabic from day one, uses Change of Address, and updates the Business Profile and social links. Monitoring by page group shows Arabic pages recrawled and indexed at the new URLs over the following weeks, and the old domain's redirects stay in place indefinitely. | Example 2: merging an Arabic domain (illustrative)<br>• Domain move without redesign<br>• One-to-one map; hreflang from day one<br>• Change of Address; redirects kept | Two domains merge; Arabic page cards slide into an '/ar-ae/' folder with hreflang links to '/en-ae/' twins. |
| 11 | 5:54 | 31 | Rollback criteria and common mistakes. Agree in advance what triggers a rollback or an emergency fix: for example, robots.txt blocking the site, redirects failing for a large share of legacy URLs, or checkout broken. Without predefined criteria, teams argue while visibility drops. Common mistakes: redirecting everything to the home page, changing URLs, content and design at once without benchmarks, removing redirects after a few months, and forgetting image, PDF and hreflang URLs. | Rollback criteria and mistakes<br>• Pre-agreed triggers<br>• No home-page dumping; no stacked changes<br>• Keep redirects; include images, PDFs, hreflang | A red 'rollback' button behind glass with three trigger conditions listed; mistake chips below. |
| 12 | 6:25 | 31 | Recap. Plan, benchmark and inventory. Build a one-hop redirect map and prove it with a script on staging and production. Launch with verification, Change of Address and sitemaps. Monitor daily, then weekly, and keep redirects for at least a year. Try this now. Take your last migration's redirect map, or your current URL list, and run the validator on the top two hundred URLs by traffic. Every fail is a live problem. | Recap and try this now<br>• Plan → QA → launch → monitor<br>• Prove redirects with the validator<br>• Run it on your top 200 URLs | The four phases light up in sequence; validator output shows failures turning into tickets. End card: 'Next: auditing workflow and Search Console monitoring'. |

<details><summary>Narration per scene (paste into ElevenLabs)</summary>

Scene 1 — Site migrations

```text
A site migration is the single riskiest thing most websites ever do in search. Done well, visibility dips briefly and recovers. Done badly, years of rankings can disappear in a week. In this lecture you'll learn what counts as a migration, a four-phase plan from benchmarking to post-launch monitoring, how to build and test a redirect map with a script, and how to set rollback criteria before you need them.
```

Scene 2 — What counts

```text
What counts as a migration? Any change that alters URLs, hosts, templates or rendering at scale. A domain change. HTTP to HTTPS. A URL restructure. A platform or CMS change, like WordPress to Shopify or a move to a headless front end. Merging sites. And major redesigns. Doing several at once multiplies risk, so if you can, separate a domain move from a replatform. And set expectations in writing: even perfect migrations can fluctuate while Google recrawls, and larger sites take longer.
```

Scene 3 — Phase 1: plan

```text
Phase one: planning, weeks before. Define scope and owners: who signs off redirects, who deploys, who monitors. Benchmark everything: a full crawl, Search Console exports by page and query, indexing counts, analytics landing pages and conversions, rank tracking, top-linked URLs and a log sample. Then inventory old URLs from every source: crawl, sitemaps, Search Console, analytics, backlinks and logs. Crawls alone miss old URLs that still receive links.
```

Scene 4 — The redirect map + parity + timing

```text
Build the redirect map. Old URL to the most relevant new URL, in one hop, with a three-oh-one or three-oh-eight. Prioritise by traffic, links and revenue. Pages with no equivalent get a deliberate four-oh-four or four-ten. Plan content and template parity: titles, meta descriptions, headings, body content, internal links, structured data, hreflang, canonicals and alt text, carried over or consciously improved. And choose timing away from peak seasons, like Ramadan and Eid campaigns for Gulf retailers, Black Friday, Diwali or back-to-school.
```

Scene 5 — Phase 2: staging QA

```text
Phase two: staging and pre-launch QA. Protect staging with authentication, not just robots.txt. Crawl staging and compare against the benchmark: missing pages, title changes, lost internal links, structured data errors, canonicals pointing at staging hosts. Test the redirect map against staging with a list crawl or the script in the lesson text. Check that the production robots.txt and meta robots won't ship a Disallow slash or a staging noindex. Check rendering and Core Web Vitals of new templates. And prepare the new XML sitemaps.
```

Scene 6 — Hands-on: redirect map validator

```text
Hands-on. The script in the lesson text reads your redirect map and requests each old URL. For normal rows it passes only if there's exactly one redirect hop, a three-oh-one or three-oh-eight, ending at a two-hundred page with the expected path. For rows marked four-ten, it passes only if the old URL returns four-ten directly. It prints pass and fail counts and writes failures to a CSV. Run it on staging before launch, on production within the first hour, and daily for two weeks on the high-priority tier.
```

Scene 7 — Phase 3: launch day

```text
Phase three: launch day. Deploy redirects together with the new site. Immediately verify robots.txt, and a sample of meta robots and canonicals, on production. Crawl the old URL list and confirm one-hop redirects to two-hundred pages. For domain moves, verify both properties in Search Console and use the Change of Address tool. Submit new sitemaps; keeping an old-URL sitemap available briefly can help Google discover the redirects. And update external profiles you control: Business Profile, social profiles, directories, ad destinations and email templates.
```

Scene 8 — Phase 4: monitor 4–12 weeks

```text
Phase four: post-launch monitoring, for four to twelve weeks. Daily for the first two weeks: four-oh-fours and five hundreds on old and new URLs, and Googlebot crawling of new URLs in logs and Crawl stats. Weekly: indexing of new versus old URLs, traffic and conversions by page group against the benchmark, rankings for priority queries, and structured data and Core Web Vitals reports. Keep redirects for at least a year, as Google advises, and ideally indefinitely. And keep the old domain registered.
```

Scene 9 — Example 1: Lahore replatform (illustrative)

```text
Worked example one, with illustrative details. A Lahore retailer moves from a custom PHP store to a hosted e-commerce platform that forces its own product and collection URL patterns. The team exports every legacy URL, including those found only in backlinks and logs, maps products by SKU and categories by name, uses the platform's redirect manager for one-hop redirects, and recreates custom title templates. After launch, logs reveal thousands of hits to old image URLs used in Google Images, so they add image redirects too. That's a commonly forgotten item.
```

Scene 10 — Example 2: merging an Arabic domain (illustrative)

```text
Worked example two, also illustrative. A UAE beauty brand merges its separate Arabic domain into an ar-a e folder on its main site. The plan separates the domain move from any redesign, maps every Arabic URL one-to-one, keeps hreflang reciprocal between English and Arabic from day one, uses Change of Address, and updates the Business Profile and social links. Monitoring by page group shows Arabic pages recrawled and indexed at the new URLs over the following weeks, and the old domain's redirects stay in place indefinitely.
```

Scene 11 — Rollback criteria and mistakes

```text
Rollback criteria and common mistakes. Agree in advance what triggers a rollback or an emergency fix: for example, robots.txt blocking the site, redirects failing for a large share of legacy URLs, or checkout broken. Without predefined criteria, teams argue while visibility drops. Common mistakes: redirecting everything to the home page, changing URLs, content and design at once without benchmarks, removing redirects after a few months, and forgetting image, PDF and hreflang URLs.
```

Scene 12 — Recap and try this now

```text
Recap. Plan, benchmark and inventory. Build a one-hop redirect map and prove it with a script on staging and production. Launch with verification, Change of Address and sitemaps. Monitor daily, then weekly, and keep redirects for at least a year. Try this now. Take your last migration's redirect map, or your current URL list, and run the validator on the top two hundred URLs by traffic. Every fail is a live problem.
```

</details>

### 17. Auditing workflow, tools and Search Console monitoring

Lesson `auditing-workflow-and-search-console-monitoring` · module “Migrations, auditing workflow and monitoring” · target 6 min · 12 scenes · 890 words · planned 6:26 (words ≈ 6:27) · status: **to produce**

| Term | Say |
|---|---|
| Lumar | loo-mar |
| JetOctopus | jet octopus |
| Oncrawl | on-crawl |
| BigQuery | big query |
| cron | cron |

| # | Time | Sec | Narration | On screen | Visual |
|---|---|---|---|---|---|
| 1 | 0:00 | 33 | Most technical SEO disasters aren't discovered by audits. They're discovered by traffic graphs, weeks too late. A noindex left on a template. A robots.txt copied from staging. A canonical that suddenly points at the home page. In this lecture you'll learn a repeatable audit workflow, the Search Console reports that matter most in twenty twenty-six, how to read the data without fooling yourself, and how to build a lightweight monitoring layer that catches problems in days. | Auditing workflow and monitoring<br>• A repeatable audit sequence<br>• Search Console reports that matter<br>• Monitoring that catches problems in days | A traffic graph drops sharply with a 'found 3 weeks later' annotation; it rewinds and an alert bell rings on day one instead. Title appears. |
| 2 | 0:33 | 34 | Senior technical SEOs work from a consistent sequence, so nothing important is skipped and findings are comparable over time. Context: business goals, priority templates, recent releases, markets and stack. Access: a Search Console Domain property, Bing Webmaster Tools, analytics, logs and staging. Crawl: with JavaScript rendering where relevant, a mobile user agent, and sitemap and Search Console integrations. Indexation review by sitemap segment. Template review. Logs. Prioritise by impact, effort and confidence. Report and ticket. Then verify and monitor. | The audit sequence<br>• Context → access → crawl<br>• Indexation → templates → logs<br>• Prioritise → ticket → verify and monitor | Nine numbered steps form a loop; the last step 'monitor' connects back to the first. |
| 3 | 1:07 | 33 | The tool stack, vendor-neutral. Crawling: Screaming Frog, Sitebulb, Lumar, JetOctopus or Oncrawl. Google data: Search Console's interface, API and bulk export to BigQuery. Bing data: Bing Webmaster Tools, which now includes AI citation reporting. Performance: PageSpeed Insights, CrUX, Lighthouse, DevTools and web-vitals RUM. Structured data: the Rich Results Test and Schema Markup Validator. Logs: log analysers, BigQuery or your CDN's analytics. The tool matters less than the configuration, so record your crawl settings in every audit. | The tool stack<br>• Crawlers, Search Console, Bing<br>• Performance and structured data tools<br>• Logs; record your crawl config | Tool category tiles arrange into a grid; a 'config.json' document pins to the crawler tile. |
| 4 | 1:40 | 33 | Search Console reports that matter most. Page indexing, filtered by sitemap for segment views, with Validate fix after you resolve an issue. URL Inspection, for index status, Google-selected canonical, last crawl and rendered HTML. Performance, for clicks, impressions, CTR and position by query, page, country, device and appearance. Crawl stats, hidden under Settings, for response codes, response times and host status. Plus Sitemaps, Core Web Vitals, HTTPS, Enhancements, Manual actions, Security issues, Removals and the robots.txt report. | Core Search Console reports<br>• Page indexing; URL Inspection<br>• Performance; Crawl stats (Settings)<br>• Sitemaps, CWV, enhancements, manual actions, robots.txt | Mock Search Console sidebar; each report name highlights as it's mentioned with a one-line purpose label. |
| 5 | 2:13 | 38 | And what's new in twenty twenty-five and twenty twenty-six? A branded queries filter in Performance, which splits branded and non-branded queries using Google's own classification, rolled out to eligible properties. Query groups, for organising queries into custom categories. Custom annotations, so you can mark releases and incidents directly on charts. And the generative AI performance report, showing impressions in AI Overviews, AI Mode and AI Overviews in Discover, without clicks or queries. Use regex filters too, for example to exclude brand terms in a way you control. | New in Search Console<br>• Branded queries filter<br>• Query groups; custom annotations<br>• Generative AI impressions report | Performance chart with an annotation flag 'Release 4.2'; a filter chip 'Branded / Non-branded' toggles the chart; a new report tile 'Generative AI' appears. |
| 6 | 2:51 | 30 | Reading Search Console data correctly. Performance data omits anonymised queries, so query totals won't sum to page totals. Average position is an impression-weighted average, and mixing many queries hides detail, so analyse query and page pairs. Report timelines lag by a few days, and indexing reports update less often than performance data. And compare like with like: same weekday mix, and seasonality such as Ramadan, Eid, Christmas or summer holidays. | Reading the data<br>• Anonymised queries omitted<br>• Average position is weighted; use query-page pairs<br>• Lags; like-for-like periods | Two stacked bars 'page total' vs 'sum of queries' with a gap labelled 'anonymised'; a calendar highlights matching periods across two years. |
| 7 | 3:21 | 37 | Now monitoring. Make sure the right people are verified users and receive Search Console emails. Schedule weekly crawls of key templates and compare with the previous run for new noindex, canonical, status or title changes. Monitor robots.txt and a set of critical URLs for changes. Build dashboards by page group, with release annotations. Alert on spikes of five hundreds to Googlebot, or drops in Googlebot hits, in your logs. And add an SEO check to every release that touches templates, routing or robots directives. | The monitoring layer<br>• Search Console emails to the right people<br>• Weekly crawls + change monitoring<br>• Dashboards, log alerts, release checks | Layered diagram: emails, scheduled crawl, change monitor, dashboard and log alerts stacked as a shield over a website icon. |
| 8 | 3:58 | 34 | Hands-on. The lesson text includes a small Python change monitor. It fetches a list of critical URLs, including robots.txt, records the status, X-Robots-Tag header, title, canonical and meta robots, and hashes robots.txt. It saves the state, and on the next run compares and prints every change. Run it weekly, or daily, from cron or your CI system, and send the output to email or chat. It's deliberately simple, and that's the point: it will catch the expensive mistakes. | Hands-on: change monitor<br>• Status, X-Robots-Tag, title, canonical, robots<br>• robots.txt hash<br>• Diff against last run → alert | Code walkthrough of the state comparison loop; an alert line appears: 'product template robots: '' → 'noindex, nofollow''. |
| 9 | 4:32 | 29 | Worked example one, simple. A Liverpool e-commerce store only verified a URL-prefix property for https www. Its old blog lives on a subdomain, and nobody noticed that subdomain had thousands of spam pages injected after a plugin was compromised. A Domain property would have shown it. The fix: verify the Domain property through DNS, review Security issues, clean the subdomain, close the vulnerability, and request a review. | Example 1: the forgotten subdomain<br>• URL-prefix property hid a subdomain<br>• Injected spam pages unnoticed<br>• Domain property; clean; request review | Property selector switching from URL-prefix to Domain; a hidden subdomain appears with a spam warning; cleanup steps tick. |
| 10 | 5:01 | 31 | Worked example two, with illustrative details. A Manchester agency runs the change monitor for a client. On a Tuesday, it reports the product template's meta robots changing from empty to noindex, nofollow. A staging flag was left on in a CMS release. The fix ships the same afternoon, before Google recrawls most products. Without monitoring, the first sign would have been a traffic drop two weeks later, and weeks more to recover. | Example 2: the Tuesday noindex (illustrative)<br>• Monitor flags noindex on product template<br>• Staging flag in a CMS release<br>• Fixed same day, before recrawl | Alert message pops up in a chat window; a timeline shows 'detected Tuesday 10:00, fixed 15:00' and a flat traffic line instead of a drop. |
| 11 | 5:32 | 27 | Common mistakes. Running a crawler with default settings on a JavaScript site and reporting no content. Delivering a two-hundred-row spreadsheet with no priorities. Auditing once a year instead of monitoring continuously. And only verifying a URL-prefix property, missing subdomains and protocol variants. Measure success with time to detect, which should be days, and the number of issues caught before they affected traffic. | Mistakes and measures<br>• Default crawls on JS sites; unprioritised dumps<br>• Annual audits only; URL-prefix only<br>• KPIs: time to detect; issues caught early | Mistake chips; a KPI tile 'time to detect: 1 day' glows green. |
| 12 | 5:59 | 27 | Recap. Work from a repeatable sequence. Master the core Search Console reports, including the new branded filter, annotations and AI report. Read the data carefully. And build a monitoring layer so problems are cheap to fix. Try this now. Set up the change monitor for ten critical URLs, including robots.txt, schedule it weekly, and send its output somewhere your team actually reads. | Recap and try this now<br>• Sequence, reports, careful reading<br>• Monitor 10 critical URLs weekly<br>• Alerts where the team reads them | A cron schedule icon and a chat channel icon connect to the monitor script. End card: 'Next: Search Console API and BigQuery automation'. |

<details><summary>Narration per scene (paste into ElevenLabs)</summary>

Scene 1 — Auditing workflow and monitoring

```text
Most technical SEO disasters aren't discovered by audits. They're discovered by traffic graphs, weeks too late. A noindex left on a template. A robots.txt copied from staging. A canonical that suddenly points at the home page. In this lecture you'll learn a repeatable audit workflow, the Search Console reports that matter most in twenty twenty-six, how to read the data without fooling yourself, and how to build a lightweight monitoring layer that catches problems in days.
```

Scene 2 — The audit sequence

```text
Senior technical SEOs work from a consistent sequence, so nothing important is skipped and findings are comparable over time. Context: business goals, priority templates, recent releases, markets and stack. Access: a Search Console Domain property, Bing Webmaster Tools, analytics, logs and staging. Crawl: with JavaScript rendering where relevant, a mobile user agent, and sitemap and Search Console integrations. Indexation review by sitemap segment. Template review. Logs. Prioritise by impact, effort and confidence. Report and ticket. Then verify and monitor.
```

Scene 3 — The tool stack

```text
The tool stack, vendor-neutral. Crawling: Screaming Frog, Sitebulb, Lumar, JetOctopus or Oncrawl. Google data: Search Console's interface, API and bulk export to BigQuery. Bing data: Bing Webmaster Tools, which now includes AI citation reporting. Performance: PageSpeed Insights, CrUX, Lighthouse, DevTools and web-vitals RUM. Structured data: the Rich Results Test and Schema Markup Validator. Logs: log analysers, BigQuery or your CDN's analytics. The tool matters less than the configuration, so record your crawl settings in every audit.
```

Scene 4 — Core Search Console reports

```text
Search Console reports that matter most. Page indexing, filtered by sitemap for segment views, with Validate fix after you resolve an issue. URL Inspection, for index status, Google-selected canonical, last crawl and rendered HTML. Performance, for clicks, impressions, CTR and position by query, page, country, device and appearance. Crawl stats, hidden under Settings, for response codes, response times and host status. Plus Sitemaps, Core Web Vitals, HTTPS, Enhancements, Manual actions, Security issues, Removals and the robots.txt report.
```

Scene 5 — New in Search Console

```text
And what's new in twenty twenty-five and twenty twenty-six? A branded queries filter in Performance, which splits branded and non-branded queries using Google's own classification, rolled out to eligible properties. Query groups, for organising queries into custom categories. Custom annotations, so you can mark releases and incidents directly on charts. And the generative AI performance report, showing impressions in AI Overviews, AI Mode and AI Overviews in Discover, without clicks or queries. Use regex filters too, for example to exclude brand terms in a way you control.
```

Scene 6 — Reading the data

```text
Reading Search Console data correctly. Performance data omits anonymised queries, so query totals won't sum to page totals. Average position is an impression-weighted average, and mixing many queries hides detail, so analyse query and page pairs. Report timelines lag by a few days, and indexing reports update less often than performance data. And compare like with like: same weekday mix, and seasonality such as Ramadan, Eid, Christmas or summer holidays.
```

Scene 7 — The monitoring layer

```text
Now monitoring. Make sure the right people are verified users and receive Search Console emails. Schedule weekly crawls of key templates and compare with the previous run for new noindex, canonical, status or title changes. Monitor robots.txt and a set of critical URLs for changes. Build dashboards by page group, with release annotations. Alert on spikes of five hundreds to Googlebot, or drops in Googlebot hits, in your logs. And add an SEO check to every release that touches templates, routing or robots directives.
```

Scene 8 — Hands-on: change monitor

```text
Hands-on. The lesson text includes a small Python change monitor. It fetches a list of critical URLs, including robots.txt, records the status, X-Robots-Tag header, title, canonical and meta robots, and hashes robots.txt. It saves the state, and on the next run compares and prints every change. Run it weekly, or daily, from cron or your CI system, and send the output to email or chat. It's deliberately simple, and that's the point: it will catch the expensive mistakes.
```

Scene 9 — Example 1: the forgotten subdomain

```text
Worked example one, simple. A Liverpool e-commerce store only verified a URL-prefix property for https www. Its old blog lives on a subdomain, and nobody noticed that subdomain had thousands of spam pages injected after a plugin was compromised. A Domain property would have shown it. The fix: verify the Domain property through DNS, review Security issues, clean the subdomain, close the vulnerability, and request a review.
```

Scene 10 — Example 2: the Tuesday noindex (illustrative)

```text
Worked example two, with illustrative details. A Manchester agency runs the change monitor for a client. On a Tuesday, it reports the product template's meta robots changing from empty to noindex, nofollow. A staging flag was left on in a CMS release. The fix ships the same afternoon, before Google recrawls most products. Without monitoring, the first sign would have been a traffic drop two weeks later, and weeks more to recover.
```

Scene 11 — Mistakes and measures

```text
Common mistakes. Running a crawler with default settings on a JavaScript site and reporting no content. Delivering a two-hundred-row spreadsheet with no priorities. Auditing once a year instead of monitoring continuously. And only verifying a URL-prefix property, missing subdomains and protocol variants. Measure success with time to detect, which should be days, and the number of issues caught before they affected traffic.
```

Scene 12 — Recap and try this now

```text
Recap. Work from a repeatable sequence. Master the core Search Console reports, including the new branded filter, annotations and AI report. Read the data carefully. And build a monitoring layer so problems are cheap to fix. Try this now. Set up the change monitor for ten critical URLs, including robots.txt, schedule it weekly, and send its output somewhere your team actually reads.
```

</details>

### 18. Search Console API and BigQuery: automating performance and indexing data

Lesson `search-console-api-and-bigquery-automation` · module “Migrations, auditing workflow and monitoring” · target 7 min · 13 scenes · 960 words · planned 6:57 (words ≈ 6:57) · status: **to produce**

| Term | Say |
|---|---|
| BigQuery | big query |
| SQL | sequel |
| OAuth | oh auth |
| Looker Studio | looker studio |
| CTR | C T R |

| # | Time | Sec | Narration | On screen | Visual |
|---|---|---|---|---|---|
| 1 | 0:00 | 33 | Search Console's interface is brilliant for exploring. It's terrible for scale. It shows a limited number of rows, you can't join it with revenue or crawl data, and it won't tell you on a Monday morning that a page group dropped thirty percent last week. In this lecture you'll automate Search Console with its API and the BigQuery bulk export, split brand from non-brand properly, and set up an alert that catches drops within days. | Search Console API + BigQuery<br>• Beyond the UI's limits<br>• Brand vs non-brand, page groups<br>• Alerts on real drops | A Search Console table with a '1,000 rows' ceiling; data pours over the ceiling into a BigQuery warehouse icon. Title appears. |
| 2 | 0:33 | 30 | First, access. Create a Google Cloud project, enable the Search Console API, and create a service account. Then add that service account's email address as a user on your Search Console property. Restricted access is enough for reading. Keep the key file path in an environment variable and never commit the key to a repository. The scope you need is webmasters read-only. For personal scripts, OAuth user credentials work too. | Access<br>• Cloud project + API enabled<br>• Service account added as a GSC user<br>• Key path in env var; read-only scope | Four-step setup animation: project, API toggle, service account email, 'Add user' dialog in Search Console. |
| 3 | 1:03 | 37 | Hands-on one: the Search Analytics API. The function in the lesson text calls search analytics query with a date range, dimensions like query and page, the web search type, a row limit of twenty-five thousand, and a start row. It keeps paging until a batch comes back smaller than the limit. Then it builds a pandas data frame. From there, the script extracts a page group from the URL, flags branded queries with a regex, and sums clicks and impressions by group and brand. | Hands-on 1: Search Analytics API<br>• Page through rows with startRow<br>• query × page dimensions<br>• Page groups + brand flag | Code walkthrough: the while loop with startRow increments; a data frame preview; a grouped summary table appears. |
| 4 | 1:40 | 31 | A few details that trip people up. The API omits anonymised queries, so query-level totals won't match page totals. Recent days are incomplete; use data state final for stable reporting, or all if you need the freshest partial data. Respect quotas and back off when you get four-twenty-nine errors. And never average the average position column across rows. Weight it by impressions, or better, use the BigQuery export, which gives you sums. | Gotchas<br>• Anonymised queries omitted<br>• dataState: final vs all<br>• Quotas + backoff; weight position by impressions | Three warning cards; a small formula shows weighted position = Σ(position × impressions) / Σ impressions. |
| 5 | 2:11 | 28 | Hands-on two: the bulk data export. In Search Console settings, open Bulk data export and link a Google Cloud project. Google then exports daily tables into BigQuery, including a site-level impressions table and a URL-level impressions table. There's one crucial detail: exports start from the day you enable them. There's no backfill. So switch it on early, even if you won't use it for months. | Hands-on 2: bulk export to BigQuery<br>• Settings → Bulk data export<br>• Daily site- and URL-level tables<br>• No backfill: enable it now | Settings panel toggles 'Bulk data export'; daily table icons start stacking in a BigQuery dataset from that date onward; an empty area before it is labelled 'no backfill'. |
| 6 | 2:39 | 34 | The SQL in the lesson text computes non-brand clicks, impressions, CTR and average position by page group for the last twenty-eight complete days. It extracts the page group with a regular expression, filters to web search, excludes anonymised queries and branded terms, and calculates average position as the sum of position divided by impressions, plus one, because the export stores zero-based position sums. Connect the dataset to Looker Studio or another BI tool for dashboards with release annotations. | The SQL<br>• Page group via REGEXP_EXTRACT<br>• Exclude anonymised + branded queries<br>• avg_position = Σ sum_position / Σ impressions + 1 | SQL walkthrough with each clause highlighting; a result table and then a Looker Studio-style chart with annotation flags. |
| 7 | 3:13 | 34 | Hands-on three: an alert. Schedule a query that compares the last seven complete days with the previous seven, by page group. Alert when a group falls by more than a threshold you choose, say thirty percent, and only when volume is large enough to matter. Pair it with the change monitor from the previous lesson, because most sudden drops coincide with a template, robots or redirect change. The alert tells you where; the change monitor usually tells you why. | Hands-on 3: the drop alert<br>• Last 7 vs previous 7 days by group<br>• Threshold + minimum volume<br>• Pair with the change monitor | Two bars per page group compare weeks; one group's bar falls past a dashed threshold line and an alert bell rings, linking to a change-monitor entry. |
| 8 | 3:47 | 27 | Worked example one, simple. A Leeds accountancy firm's marketing manager spends two hours every month copying Search Console data into a spreadsheet. A thirty-line script using the API now pulls the same data, groups it by service page, and emails a table every Monday. It saves time, but more importantly, the numbers are consistent every month, which the manual process never was. | Example 1: the Monday email<br>• 2 hours of monthly copying → a script<br>• Grouped by service page<br>• Consistent numbers every week | Stopwatch showing '2 hours' crossed out; an email card 'Weekly SEO summary' arrives with a small table. |
| 9 | 4:14 | 31 | Worked example two, with illustrative details. A UK recipe publisher reports organic up ten percent in the interface. The BigQuery split tells a different story. Branded clicks are up, thanks to a TV mention, while non-brand clicks are flat, and the quick-answer page group is down. The team uses the data to prioritise deeper content for the declining group, and reports honestly to leadership, with the TV appearance annotated on the chart. | Example 2: brand vs non-brand truth (illustrative)<br>• Headline: organic +10%<br>• Reality: brand up (TV), non-brand flat<br>• Quick-answer group down → prioritise | Headline number '+10%' splits into two lines, 'brand' rising with a TV annotation and 'non-brand' flat; one page group line dips. |
| 10 | 4:45 | 30 | How do you measure success? Reports refresh automatically, and nobody copies data out of the interface. Brand versus non-brand and page-group trends are available in minutes. And alerts fire within days of material drops, with each one traced to a cause. Common mistakes: summing or averaging average position without weighting, forgetting anonymised queries when reconciling totals, enabling bulk export too late, and committing service account keys to a repository. | Measures and mistakes<br>• Automatic refresh; minutes to insight<br>• Alerts traced to causes<br>• Weighted position; anonymised queries; enable export early; protect keys | KPI tiles on the left; mistake chips on the right. |
| 11 | 5:15 | 37 | What about indexing data? The URL Inspection API has daily and per-minute quotas per property, so you can't inspect a large site exhaustively. Treat it as a sampling instrument. Each month, inspect a random sample of fifty to a hundred URLs per page group, store the verdict, coverage state and Google-selected canonical in BigQuery next to your performance data, and chart the share of each group that's indexed with a matching canonical. For whole-site coverage, use the Page indexing report filtered by sitemap segment. | Indexing data at scale<br>• URL Inspection = sampling, not sweeping<br>• 50–100 URLs per group monthly<br>• Store next to performance data | A page group grid with a random sample of cells lighting up; inspected results flow into a BigQuery table beside performance rows; a chart of '% indexed with matching canonical' per group. |
| 12 | 5:52 | 32 | And a word on governance. Once Search Console data lives in BigQuery, it becomes company data. Decide who can access the dataset, how long you keep it, and which dashboards are the official source of truth, so marketing, product and leadership stop arguing over different numbers. Document the brand regex and page group rules in one place, and version them, because changing a regex silently changes every trend. Small discipline, big payoff in trust. | Governance<br>• Dataset access and retention<br>• One official dashboard<br>• Versioned brand regex and page-group rules | A dataset icon with a lock and a retention calendar; a single 'official dashboard' badge; a small changelog for the regex rules. |
| 13 | 6:24 | 33 | Recap. Use the API for flexible pulls with pagination, and the bulk export for complete daily data in BigQuery. Split brand and non-brand, group by page type, weight position properly, and alert on real drops. Try this now. Turn on bulk data export today, even if you won't use it yet. Then run the API script for the last twenty-eight days, and look at non-brand clicks by page group. That's the view your leadership actually needs. | Recap and try this now<br>• Enable bulk export today<br>• Run the API script for 28 days<br>• Share non-brand clicks by page group | A toggle switches on; a grouped bar chart of non-brand clicks by page group appears. End card: course completion badge 'Certified Technical SEO Specialist'. |

<details><summary>Narration per scene (paste into ElevenLabs)</summary>

Scene 1 — Search Console API + BigQuery

```text
Search Console's interface is brilliant for exploring. It's terrible for scale. It shows a limited number of rows, you can't join it with revenue or crawl data, and it won't tell you on a Monday morning that a page group dropped thirty percent last week. In this lecture you'll automate Search Console with its API and the BigQuery bulk export, split brand from non-brand properly, and set up an alert that catches drops within days.
```

Scene 2 — Access

```text
First, access. Create a Google Cloud project, enable the Search Console API, and create a service account. Then add that service account's email address as a user on your Search Console property. Restricted access is enough for reading. Keep the key file path in an environment variable and never commit the key to a repository. The scope you need is webmasters read-only. For personal scripts, OAuth user credentials work too.
```

Scene 3 — Hands-on 1: Search Analytics API

```text
Hands-on one: the Search Analytics API. The function in the lesson text calls search analytics query with a date range, dimensions like query and page, the web search type, a row limit of twenty-five thousand, and a start row. It keeps paging until a batch comes back smaller than the limit. Then it builds a pandas data frame. From there, the script extracts a page group from the URL, flags branded queries with a regex, and sums clicks and impressions by group and brand.
```

Scene 4 — Gotchas

```text
A few details that trip people up. The API omits anonymised queries, so query-level totals won't match page totals. Recent days are incomplete; use data state final for stable reporting, or all if you need the freshest partial data. Respect quotas and back off when you get four-twenty-nine errors. And never average the average position column across rows. Weight it by impressions, or better, use the BigQuery export, which gives you sums.
```

Scene 5 — Hands-on 2: bulk export to BigQuery

```text
Hands-on two: the bulk data export. In Search Console settings, open Bulk data export and link a Google Cloud project. Google then exports daily tables into BigQuery, including a site-level impressions table and a URL-level impressions table. There's one crucial detail: exports start from the day you enable them. There's no backfill. So switch it on early, even if you won't use it for months.
```

Scene 6 — The SQL

```text
The SQL in the lesson text computes non-brand clicks, impressions, CTR and average position by page group for the last twenty-eight complete days. It extracts the page group with a regular expression, filters to web search, excludes anonymised queries and branded terms, and calculates average position as the sum of position divided by impressions, plus one, because the export stores zero-based position sums. Connect the dataset to Looker Studio or another BI tool for dashboards with release annotations.
```

Scene 7 — Hands-on 3: the drop alert

```text
Hands-on three: an alert. Schedule a query that compares the last seven complete days with the previous seven, by page group. Alert when a group falls by more than a threshold you choose, say thirty percent, and only when volume is large enough to matter. Pair it with the change monitor from the previous lesson, because most sudden drops coincide with a template, robots or redirect change. The alert tells you where; the change monitor usually tells you why.
```

Scene 8 — Example 1: the Monday email

```text
Worked example one, simple. A Leeds accountancy firm's marketing manager spends two hours every month copying Search Console data into a spreadsheet. A thirty-line script using the API now pulls the same data, groups it by service page, and emails a table every Monday. It saves time, but more importantly, the numbers are consistent every month, which the manual process never was.
```

Scene 9 — Example 2: brand vs non-brand truth (illustrative)

```text
Worked example two, with illustrative details. A UK recipe publisher reports organic up ten percent in the interface. The BigQuery split tells a different story. Branded clicks are up, thanks to a TV mention, while non-brand clicks are flat, and the quick-answer page group is down. The team uses the data to prioritise deeper content for the declining group, and reports honestly to leadership, with the TV appearance annotated on the chart.
```

Scene 10 — Measures and mistakes

```text
How do you measure success? Reports refresh automatically, and nobody copies data out of the interface. Brand versus non-brand and page-group trends are available in minutes. And alerts fire within days of material drops, with each one traced to a cause. Common mistakes: summing or averaging average position without weighting, forgetting anonymised queries when reconciling totals, enabling bulk export too late, and committing service account keys to a repository.
```

Scene 11 — Indexing data at scale

```text
What about indexing data? The URL Inspection API has daily and per-minute quotas per property, so you can't inspect a large site exhaustively. Treat it as a sampling instrument. Each month, inspect a random sample of fifty to a hundred URLs per page group, store the verdict, coverage state and Google-selected canonical in BigQuery next to your performance data, and chart the share of each group that's indexed with a matching canonical. For whole-site coverage, use the Page indexing report filtered by sitemap segment.
```

Scene 12 — Governance

```text
And a word on governance. Once Search Console data lives in BigQuery, it becomes company data. Decide who can access the dataset, how long you keep it, and which dashboards are the official source of truth, so marketing, product and leadership stop arguing over different numbers. Document the brand regex and page group rules in one place, and version them, because changing a regex silently changes every trend. Small discipline, big payoff in trust.
```

Scene 13 — Recap and try this now

```text
Recap. Use the API for flexible pulls with pagination, and the bulk export for complete daily data in BigQuery. Split brand and non-brand, group by page type, weight position properly, and alert on real drops. Try this now. Turn on bulk data export today, even if you won't use it yet. Then run the API script for the last twenty-eight days, and look at non-brand clicks by page group. That's the view your leadership actually needs.
```

</details>
