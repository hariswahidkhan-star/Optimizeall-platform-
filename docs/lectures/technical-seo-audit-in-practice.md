# Technical SEO Audit Workshop — lecture production script

Course `technical-seo-audit-in-practice` · pack v2 · reviewed 2026-09 · level intermediate · category seo

14 lectures · 13,019 narration words · planned runtime 1:34:22 (≈ 93 min at 140 wpm)

> Generated by `scripts/export-lecture-scripts.py` from the course pack — edit the pack, not this file. Production workflow: [README](README.md).

## Voice direction

Conversational expert, second person, ~140 wpm; clear and methodical, explain jargon the first time; audience level: intermediate. Pause ~0.5 s between scenes; no music under narration (added in edit).

## Lectures

1. [Meet the client and scope the audit](#1-meet-the-client-and-scope-the-audit)
2. [Configuring the crawl like Googlebot](#2-configuring-the-crawl-like-googlebot)
3. [Reading the crawl: indexability, status and templates](#3-reading-the-crawl-indexability-status-and-templates)
4. [Search Console, logs and performance evidence](#4-search-console-logs-and-performance-evidence)
5. [Log-file analysis lab: Cloudflare logs to crawl insights in Python](#5-log-file-analysis-lab-cloudflare-logs-to-crawl-insights-in-python)
6. [Rendering and structured data checks: raw vs rendered, markup vs visible](#6-rendering-and-structured-data-checks-raw-vs-rendered-markup-vs-visible)
7. [From findings to root causes](#7-from-findings-to-root-causes)
8. [Prioritising with impact × effort × confidence](#8-prioritising-with-impact-effort-confidence)
9. [Anatomy of a great SEO ticket](#9-anatomy-of-a-great-seo-ticket)
10. [Working with development teams: sprints, QA and release risk](#10-working-with-development-teams-sprints-qa-and-release-risk)
11. [Verifying fixes: recrawl, Search Console and logs](#11-verifying-fixes-recrawl-search-console-and-logs)
12. [Reporting to stakeholders](#12-reporting-to-stakeholders)
13. [Auditing AI search readiness: access, accuracy and Bing](#13-auditing-ai-search-readiness-access-accuracy-and-bing)
14. [The 2026 technical audit checklist and deliverables pack](#14-the-2026-technical-audit-checklist-and-deliverables-pack)

### 1. Meet the client and scope the audit

Lesson `meet-the-client-and-scope-the-audit` · module “Scoping the audit and setting up” · target 7 min · 12 scenes · 924 words · planned 6:43 (words ≈ 6:42) · status: **to produce**

| Term | Say |
|---|---|
| Kiran | kee-ran |
| GA4 | G A four |
| WAF | waff |
| Logpush | log push |

| # | Time | Sec | Narration | On screen | Visual |
|---|---|---|---|---|---|
| 1 | 0:00 | 35 | Welcome to the Technical SEO Audit Workshop. For the next fifteen lessons, you'll audit one business from first call to final report. Our client is Kiran Home, a fictional Lahore-based homeware brand selling to Pakistan, the UAE and the UK, whose organic traffic fell after a storefront relaunch. In this first lecture you'll learn why scoping decides whether an audit gets implemented, the questions to ask, the access to request, and how to write hypotheses before you crawl a single URL. | Meet the client, scope the audit<br>• Kiran Home: a fictional Lahore homeware brand<br>• Scoping decides implementation<br>• Questions, access, hypotheses | Branded title slide for 'Kiran Home (fictional)': brass lantern and cushion illustrations, a map linking Lahore, Dubai and London, and a traffic line dipping after a 'relaunch' marker. |
| 2 | 0:35 | 40 | Meet the client. Kiran Home sells handmade cushions, block-print bedding, brassware and rugs from a workshop in Lahore. It runs a headless storefront, React with server-side rendering, on a hosted commerce back end. About four thousand products, a hundred and eighty categories and three hundred blog posts, across four market folders: English for Pakistan, English and Arabic for the UAE, and English for the UK. The team is a founder, a marketing manager, two developers and a freelance designer. Three months ago they relaunched. Organic sessions fell. Everything here is illustrative. | The client (illustrative)<br>• Headless React + SSR storefront<br>• ~4,000 products, 4 market folders<br>• Relaunch 3 months ago → organic decline | Fact card animates in rows: platform, size, markets, team, recent change, tooling. A small org chart with five role icons. |
| 3 | 1:15 | 30 | Here's the key idea: audits fail more often from poor scoping than from missed issues. A three-hundred-row spreadsheet of every warning a crawler produces isn't an audit. It's a data dump. Scoping tells you which questions matter, which templates carry revenue, and who can actually act on your findings. Think of a doctor. A good one asks where it hurts and what changed before ordering every test in the hospital. | Why scoping comes first<br>• Data dumps don't get implemented<br>• Find the questions that matter<br>• Like a doctor: history before tests | A giant spreadsheet with 300 rows falls over; a doctor's clipboard with three focused questions replaces it. |
| 4 | 1:45 | 40 | The scoping call has four groups of questions. Business: which products, categories and markets drive revenue and margin, what does success look like in six months, and what are the seasonal peaks? For Kiran Home, that's Eid gifting, wedding season and Black Friday in the UK and UAE. History: what changed and when, any manual actions, outages or previous audits, and what happened to their recommendations? Team and process: who implements, how work is tracked, sprint length, and what marketing can change without developers. And constraints: budget, time and platform limits. | The scoping call<br>• Business: revenue, success, seasons<br>• History: changes, incidents, past audits<br>• Team/process and constraints | Four quadrant cards fill with question lists; Kiran Home's seasonal peaks appear on a small calendar strip. |
| 5 | 2:25 | 41 | Next, the access checklist. Request everything in one message so you're not blocked mid-audit. A Search Console Domain property, verified by DNS, because Kiran Home only has a URL-prefix property. Switch on bulk data export to BigQuery now, because it has no backfill. Bing Webmaster Tools, including its AI Performance report. GA4 with a year of pre-relaunch data. Four or more weeks of Cloudflare logs. Staging access. The launch redirect map. And, new for twenty twenty-six, read access to CDN bot and firewall rules, plus the status of the Search Console AI opt-out control. | Access checklist<br>• GSC Domain property + bulk export ON<br>• Bing (incl. AI Performance), GA4, logs<br>• Staging, redirect map, CDN bot rules, AI control status | Checklist animates with checkboxes ticking; a 'no backfill' warning tag flashes beside bulk export. |
| 6 | 3:06 | 36 | Then write the scope statement and get it signed off. Objective: identify the technical causes of the post-relaunch decline and produce a prioritised, developer-ready fix plan. In scope: all four market folders, key templates, indexing, crawling, rendering, redirects, hreflang, structured data and Core Web Vitals. Out of scope: keyword research, content rewrites and link building. Deliverables: a findings workbook, a roadmap, up to fifteen tickets, a stakeholder report and a walkthrough. Timeline and owners, named. This one page prevents weeks of scope creep. | The scope statement<br>• Objective, in/out of scope<br>• Deliverables, timeline, owners<br>• Signed off before crawling | A one-page scope document assembles section by section; a signature line gets signed at the end. |
| 7 | 3:42 | 37 | Now the most underrated step: hypotheses. Before crawling, write down what you suspect. For Kiran Home: redirect gaps, meaning some old URLs weren't redirected or went to irrelevant pages. Rendering or indexability regressions, like changed canonicals or noindex tags. Broken international signals, meaning hreflang or market routing changed. Mobile performance regressions. And a newer one: bot access changed, because relaunches often come with new CDN bot rules that challenge Bingbot or AI search bots. Hypotheses focus the audit and stop you drowning in data. | Hypotheses before data<br>• H1 redirects · H2 rendering/indexability<br>• H3 international · H4 performance<br>• H5 bot access / CDN rules | Five hypothesis cards pin to a board, each with an empty 'verdict' box to be filled later in the course. |
| 8 | 4:19 | 27 | Keep an open mind, though. The cause could be non-technical. A broad core update might coincide with the relaunch, or the comparison period might include Ramadan one year and not the other. So check confirmed Google update dates and seasonality against the traffic drop before blaming the relaunch. We'll test every hypothesis with evidence, and we'll record an alternative explanation too. | Alternative explanations<br>• Core updates<br>• Seasonality (e.g. Ramadan timing)<br>• Test, don't assume | Traffic chart with overlaid markers for 'relaunch', 'core update' and 'Ramadan'; a question mark hovers over which one explains the dip. |
| 9 | 4:46 | 40 | Worked example one: that's Kiran Home, which we'll follow all course. Worked example two, much smaller, and illustrative. A Leeds physiotherapy clinic with a sixty-page WordPress site asks for an SEO audit after a redesign. Good scoping keeps it proportionate. Objective: restore enquiries from local service pages. In scope: indexability, redirects from the old site, local landing pages, Business Profile consistency and mobile performance. Out of scope: content strategy. Deliverable: a ten-item prioritised list and five tickets for the freelance developer, in one week. Same discipline, a fraction of the size. | Example 2: a small clinic (illustrative)<br>• 60-page WordPress site<br>• Proportionate scope, one week<br>• 10 priorities + 5 tickets | Scale comparison: a large Kiran Home scope document next to a small clinic scope card; both share the same section headings. |
| 10 | 5:26 | 25 | The hands-on for this lesson is a scoping pack you can send today. The lesson text has an email template listing exactly what access you need, and a forty-five-minute kickoff agenda: goals and revenue templates, what changed and when, team and release process, constraints, hypotheses, and timeline and deliverables. Adapt it once and reuse it on every client. | Hands-on: the scoping pack<br>• Access request email<br>• 45-minute kickoff agenda<br>• Reuse on every client | Email template appears with numbered access items; a kickoff agenda card with six time-boxed segments. |
| 11 | 5:51 | 21 | Common mistakes. Starting to crawl before understanding what makes money. Accepting a URL-prefix property and missing half the picture. Forgetting to switch on bulk export on day one. And promising, we'll recover your traffic by a certain date. Promise a diagnosis and a plan, not a ranking outcome. | Common mistakes<br>• Crawling before understanding revenue<br>• URL-prefix only; no bulk export<br>• Promising recovery dates | Mistake chips fall and are replaced by a single card: 'Promise a diagnosis and a plan'. |
| 12 | 6:12 | 31 | Recap. Scope first: business, history, team and constraints. Request all access in one go, including Domain property, bulk export, Bing, logs and CDN rules. Write a signed scope statement and five hypotheses. Try this now. Take the scoping email template and adapt it for a real or practice client, and write three hypotheses for their site before you open a crawler. In the next lesson, we configure the crawl for Kiran Home. | Recap and try this now<br>• Adapt the scoping pack<br>• Write 3 hypotheses before crawling<br>• Next: configuring the crawl | Scoping pack and hypothesis board side by side with ticks. End card: 'Next: configuring the crawl like Googlebot'. |

<details><summary>Narration per scene (paste into ElevenLabs)</summary>

Scene 1 — Meet the client, scope the audit

```text
Welcome to the Technical SEO Audit Workshop. For the next fifteen lessons, you'll audit one business from first call to final report. Our client is Kiran Home, a fictional Lahore-based homeware brand selling to Pakistan, the UAE and the UK, whose organic traffic fell after a storefront relaunch. In this first lecture you'll learn why scoping decides whether an audit gets implemented, the questions to ask, the access to request, and how to write hypotheses before you crawl a single URL.
```

Scene 2 — The client (illustrative)

```text
Meet the client. Kiran Home sells handmade cushions, block-print bedding, brassware and rugs from a workshop in Lahore. It runs a headless storefront, React with server-side rendering, on a hosted commerce back end. About four thousand products, a hundred and eighty categories and three hundred blog posts, across four market folders: English for Pakistan, English and Arabic for the UAE, and English for the UK. The team is a founder, a marketing manager, two developers and a freelance designer. Three months ago they relaunched. Organic sessions fell. Everything here is illustrative.
```

Scene 3 — Why scoping comes first

```text
Here's the key idea: audits fail more often from poor scoping than from missed issues. A three-hundred-row spreadsheet of every warning a crawler produces isn't an audit. It's a data dump. Scoping tells you which questions matter, which templates carry revenue, and who can actually act on your findings. Think of a doctor. A good one asks where it hurts and what changed before ordering every test in the hospital.
```

Scene 4 — The scoping call

```text
The scoping call has four groups of questions. Business: which products, categories and markets drive revenue and margin, what does success look like in six months, and what are the seasonal peaks? For Kiran Home, that's Eid gifting, wedding season and Black Friday in the UK and UAE. History: what changed and when, any manual actions, outages or previous audits, and what happened to their recommendations? Team and process: who implements, how work is tracked, sprint length, and what marketing can change without developers. And constraints: budget, time and platform limits.
```

Scene 5 — Access checklist

```text
Next, the access checklist. Request everything in one message so you're not blocked mid-audit. A Search Console Domain property, verified by DNS, because Kiran Home only has a URL-prefix property. Switch on bulk data export to BigQuery now, because it has no backfill. Bing Webmaster Tools, including its AI Performance report. GA4 with a year of pre-relaunch data. Four or more weeks of Cloudflare logs. Staging access. The launch redirect map. And, new for twenty twenty-six, read access to CDN bot and firewall rules, plus the status of the Search Console AI opt-out control.
```

Scene 6 — The scope statement

```text
Then write the scope statement and get it signed off. Objective: identify the technical causes of the post-relaunch decline and produce a prioritised, developer-ready fix plan. In scope: all four market folders, key templates, indexing, crawling, rendering, redirects, hreflang, structured data and Core Web Vitals. Out of scope: keyword research, content rewrites and link building. Deliverables: a findings workbook, a roadmap, up to fifteen tickets, a stakeholder report and a walkthrough. Timeline and owners, named. This one page prevents weeks of scope creep.
```

Scene 7 — Hypotheses before data

```text
Now the most underrated step: hypotheses. Before crawling, write down what you suspect. For Kiran Home: redirect gaps, meaning some old URLs weren't redirected or went to irrelevant pages. Rendering or indexability regressions, like changed canonicals or noindex tags. Broken international signals, meaning hreflang or market routing changed. Mobile performance regressions. And a newer one: bot access changed, because relaunches often come with new CDN bot rules that challenge Bingbot or AI search bots. Hypotheses focus the audit and stop you drowning in data.
```

Scene 8 — Alternative explanations

```text
Keep an open mind, though. The cause could be non-technical. A broad core update might coincide with the relaunch, or the comparison period might include Ramadan one year and not the other. So check confirmed Google update dates and seasonality against the traffic drop before blaming the relaunch. We'll test every hypothesis with evidence, and we'll record an alternative explanation too.
```

Scene 9 — Example 2: a small clinic (illustrative)

```text
Worked example one: that's Kiran Home, which we'll follow all course. Worked example two, much smaller, and illustrative. A Leeds physiotherapy clinic with a sixty-page WordPress site asks for an SEO audit after a redesign. Good scoping keeps it proportionate. Objective: restore enquiries from local service pages. In scope: indexability, redirects from the old site, local landing pages, Business Profile consistency and mobile performance. Out of scope: content strategy. Deliverable: a ten-item prioritised list and five tickets for the freelance developer, in one week. Same discipline, a fraction of the size.
```

Scene 10 — Hands-on: the scoping pack

```text
The hands-on for this lesson is a scoping pack you can send today. The lesson text has an email template listing exactly what access you need, and a forty-five-minute kickoff agenda: goals and revenue templates, what changed and when, team and release process, constraints, hypotheses, and timeline and deliverables. Adapt it once and reuse it on every client.
```

Scene 11 — Common mistakes

```text
Common mistakes. Starting to crawl before understanding what makes money. Accepting a URL-prefix property and missing half the picture. Forgetting to switch on bulk export on day one. And promising, we'll recover your traffic by a certain date. Promise a diagnosis and a plan, not a ranking outcome.
```

Scene 12 — Recap and try this now

```text
Recap. Scope first: business, history, team and constraints. Request all access in one go, including Domain property, bulk export, Bing, logs and CDN rules. Write a signed scope statement and five hypotheses. Try this now. Take the scoping email template and adapt it for a real or practice client, and write three hypotheses for their site before you open a crawler. In the next lesson, we configure the crawl for Kiran Home.
```

</details>

### 2. Configuring the crawl like Googlebot

Lesson `configuring-the-crawl` · module “Scoping the audit and setting up” · target 7 min · 12 scenes · 920 words · planned 6:42 (words ≈ 6:40) · status: **to produce**

| Term | Say |
|---|---|
| XPath | X path |
| CLI | C L I |
| curl | curl |
| Sitebulb | site bulb |

| # | Time | Sec | Narration | On screen | Visual |
|---|---|---|---|---|---|
| 1 | 0:00 | 37 | Before you look at a single finding, look at your crawler settings. They decide what you'll find. The same tool gives completely different results depending on configuration. A text-only crawl of a JavaScript storefront may report no content. A crawl blocked by bot protection reports a site full of four-oh-threes. In this lecture you'll configure a crawl for Kiran Home that approximates how Googlebot experiences the site, run it in a repeatable way, and record every setting so you can re-crawl identically after fixes. | Configuring the crawl<br>• Settings decide findings<br>• Approximate Googlebot<br>• Repeatable and recorded | Two crawl result dashboards side by side from the same site: one full of '403' errors, one healthy. A settings gear spins between them. Title appears. |
| 2 | 0:37 | 40 | Here's the configuration sheet for Kiran Home. Start at the canonical host. Seed the crawl with all XML sitemaps and the list of old URLs from the launch redirect map, so you find orphans and test redirects. Use a Googlebot smartphone user agent, because of mobile-first indexing. Turn JavaScript rendering on, because it's a headless React storefront. Respect robots.txt, and optionally run a second crawl ignoring it to see what's hidden. No depth limit, but cap the URL count and review patterns. Modest speed, agreed with developers. Crawl all parameters initially. | The configuration sheet<br>• Seeds: home + sitemaps + old URLs<br>• Googlebot Smartphone; JS rendering on<br>• Respect robots; cap URLs; modest speed | Settings table animates row by row with the reason column appearing after each choice. |
| 3 | 1:17 | 33 | Extraction and integrations. Extract canonical, meta robots, X-Robots-Tag, hreflang, JSON-LD, H1, word count and the price element. Connect the Search Console API and GA4, so traffic and index data join the crawl by URL. Then two safeguards. First, get your crawler allow-listed with the CDN or bot protection for the audit window, otherwise you'll crawl a wall of challenge pages. Second, crawl staging separately with authentication, and check that it's noindexed, not the other way round. | Extraction + safeguards<br>• Canonical, robots, hreflang, JSON-LD, price<br>• GSC + GA4 integrations<br>• Allow-list the crawler; staging separately | Extraction fields drop into a crawler config panel; a CDN rule 'Allow audit crawler IP' is added; a separate staging crawl runs behind a lock. |
| 4 | 1:50 | 31 | Custom extraction checks what matters to this business. For Kiran Home, we want to know whether product prices and add to basket buttons exist in the rendered HTML, and whether the JSON-LD price matches the visible price. So we extract the visible price with a CSS selector, all JSON-LD blocks with XPath, the add to basket button, and breadcrumb links. Default reports catch generic issues. Custom extraction answers the client's questions. | Custom extraction<br>• Visible price (CSS)<br>• JSON-LD blocks (XPath)<br>• Add-to-basket button; breadcrumbs | Product page wireframe; four elements highlight with their selectors; extracted values populate a table. |
| 5 | 2:21 | 28 | Segment before you crawl. Configure segments by URL pattern: product, category, blog, facet and search, using regular expressions like the ones in the lesson text. Here's why that matters. Without segments, you report three thousand two hundred duplicate titles. With segments, you report that all three thousand one hundred duplicate titles are facet URLs, which shouldn't be crawlable anyway. Same data, completely different conclusion. | Segment first<br>• product, category, blog, facet, search<br>• Regex by URL pattern<br>• Turns counts into causes | A single '3,200 duplicate titles' number splits into segment bars; the facet bar dominates with a label 'root cause'. |
| 6 | 2:49 | 31 | Two crawls are better than one. The rendered crawl, JavaScript on, respecting robots.txt, with a Googlebot smartphone agent, is your primary dataset. The raw crawl, JavaScript off, shows what depends on rendering: links, canonicals, content and structured data. Add a list crawl of old URLs from the pre-relaunch site to test redirects, and a sitemap-only crawl to test sitemap hygiene. Four views of the same site, each answering a different question. | Four crawls<br>• Rendered (primary)<br>• Raw (what depends on JS)<br>• Old-URL list; sitemap-only | Four crawl icons in a grid, each with a question label: 'What does Google see?', 'What needs JS?', 'Do redirects work?', 'Are sitemaps clean?'. |
| 7 | 3:20 | 35 | Hands-on. Crawlers can run headless from the command line, which makes crawls repeatable and schedulable. The lesson text shows a Screaming Frog example with a saved configuration file, an output folder by date, selected exports and timestamped output. Check the help for your version's exact flags, and other crawlers offer equivalent scheduled or cloud crawls. Store the config file in version control with the audit. There's also a quick curl loop to check key URLs aren't being blocked before you start. | Hands-on: headless, versioned crawls<br>• CLI crawl with saved config<br>• Dated outputs, version control<br>• curl pre-check for blocking | Terminal shows the headless crawl command; a folder tree fills with dated exports; a curl loop prints '200' lines. |
| 8 | 3:55 | 40 | Watch the first minutes of the crawl and stop to fix the config if you see these signs. All URLs returning four-oh-three or a challenge page means bot protection. The crawl exploding into parameter URLs within minutes means a facet trap, so cap it, note it as a finding, and exclude it from the main crawl. Rendered pages with no content means rendering timeouts, so increase the timeout or check blocked resources. And redirects to a login or geo page means IP-based geo-redirects, which is itself a finding for an international site. | Watch the first minutes<br>• 403s → bot protection<br>• Parameter explosion → facet trap<br>• Empty renders → timeouts; geo-redirects → finding | Live crawl counter with four warning lights; each lights up with its diagnosis label. |
| 9 | 4:35 | 29 | Worked example one, simple, and illustrative. On the Kiran Home kick-off crawl, the first two hundred URLs all return five-oh-three. The CDN's bot fight mode is challenging the crawler. The developers add a temporary allow rule for the audit crawler's IP. But note the second-order question for the audit: does the same rule set challenge Bingbot or AI search bots? That becomes evidence for hypothesis five. | Example 1: Kiran Home kick-off (illustrative)<br>• First 200 URLs → 503 challenge<br>• Temporary allow rule for the crawler<br>• Question for H5: are real bots challenged too? | Crawl log with a wall of '503'; a CDN rule is added; rows turn '200'. A sticky note 'Check Bingbot / AI bots → H5' attaches. |
| 10 | 5:04 | 30 | Worked example two, also illustrative. A Doha fashion retailer's crawl finds only seven hundred URLs on a site with six thousand products. The rendered crawl used a two-second timeout, and the category grid loads after three. Increasing the timeout and checking resources reveals the full catalogue, and a genuine finding: the category API is slow, which also hurts real users and Google's rendering. Configuration problems often hide real problems. | Example 2: the too-short timeout (illustrative)<br>• 700 of 6,000 products found<br>• 2 s timeout vs 3 s grid load<br>• Fix config → real finding: slow API | Crawl count jumps from 700 to 6,000 after a timeout slider moves from 2 s to 8 s; a 'slow API' finding card appears. |
| 11 | 5:34 | 35 | Recording the configuration. Save the crawler's configuration file, and note the crawler version, date, settings, seeds and exclusions in your findings workbook. When you re-crawl for verification later in this workshop, you'll use the identical configuration, so any difference comes from fixes and not from settings. Common mistakes: crawling with a desktop user agent, leaving rendering off on a JavaScript site, crawling from a blocked IP and reporting site returns four-oh-three as a finding, and not seeding with old URLs. | Record it; avoid mistakes<br>• Version, date, settings, seeds, exclusions<br>• Identical config for re-crawls<br>• Desktop UA; no rendering; blocked IP; no old-URL seeds | Config file icon with a version tag 'v1.2' saved into a workbook tab; mistake chips below. |
| 12 | 6:09 | 33 | Recap. Configure the crawl to approximate Googlebot: smartphone agent, rendering on, robots respected, seeded with sitemaps and old URLs, segmented, and allow-listed. Run rendered and raw crawls, plus list crawls. Record everything and re-use it. Try this now. Write a configuration sheet for a site you know, with a reason for every setting, and run a quick curl check on three key URLs with a Googlebot smartphone user agent to see if anything blocks you. | Recap and try this now<br>• Config sheet with reasons<br>• curl-check 3 URLs<br>• Next: reading the crawl | Config sheet template and a terminal side by side. End card: 'Next module: running the crawl and gathering evidence'. |

<details><summary>Narration per scene (paste into ElevenLabs)</summary>

Scene 1 — Configuring the crawl

```text
Before you look at a single finding, look at your crawler settings. They decide what you'll find. The same tool gives completely different results depending on configuration. A text-only crawl of a JavaScript storefront may report no content. A crawl blocked by bot protection reports a site full of four-oh-threes. In this lecture you'll configure a crawl for Kiran Home that approximates how Googlebot experiences the site, run it in a repeatable way, and record every setting so you can re-crawl identically after fixes.
```

Scene 2 — The configuration sheet

```text
Here's the configuration sheet for Kiran Home. Start at the canonical host. Seed the crawl with all XML sitemaps and the list of old URLs from the launch redirect map, so you find orphans and test redirects. Use a Googlebot smartphone user agent, because of mobile-first indexing. Turn JavaScript rendering on, because it's a headless React storefront. Respect robots.txt, and optionally run a second crawl ignoring it to see what's hidden. No depth limit, but cap the URL count and review patterns. Modest speed, agreed with developers. Crawl all parameters initially.
```

Scene 3 — Extraction + safeguards

```text
Extraction and integrations. Extract canonical, meta robots, X-Robots-Tag, hreflang, JSON-LD, H1, word count and the price element. Connect the Search Console API and GA4, so traffic and index data join the crawl by URL. Then two safeguards. First, get your crawler allow-listed with the CDN or bot protection for the audit window, otherwise you'll crawl a wall of challenge pages. Second, crawl staging separately with authentication, and check that it's noindexed, not the other way round.
```

Scene 4 — Custom extraction

```text
Custom extraction checks what matters to this business. For Kiran Home, we want to know whether product prices and add to basket buttons exist in the rendered HTML, and whether the JSON-LD price matches the visible price. So we extract the visible price with a CSS selector, all JSON-LD blocks with XPath, the add to basket button, and breadcrumb links. Default reports catch generic issues. Custom extraction answers the client's questions.
```

Scene 5 — Segment first

```text
Segment before you crawl. Configure segments by URL pattern: product, category, blog, facet and search, using regular expressions like the ones in the lesson text. Here's why that matters. Without segments, you report three thousand two hundred duplicate titles. With segments, you report that all three thousand one hundred duplicate titles are facet URLs, which shouldn't be crawlable anyway. Same data, completely different conclusion.
```

Scene 6 — Four crawls

```text
Two crawls are better than one. The rendered crawl, JavaScript on, respecting robots.txt, with a Googlebot smartphone agent, is your primary dataset. The raw crawl, JavaScript off, shows what depends on rendering: links, canonicals, content and structured data. Add a list crawl of old URLs from the pre-relaunch site to test redirects, and a sitemap-only crawl to test sitemap hygiene. Four views of the same site, each answering a different question.
```

Scene 7 — Hands-on: headless, versioned crawls

```text
Hands-on. Crawlers can run headless from the command line, which makes crawls repeatable and schedulable. The lesson text shows a Screaming Frog example with a saved configuration file, an output folder by date, selected exports and timestamped output. Check the help for your version's exact flags, and other crawlers offer equivalent scheduled or cloud crawls. Store the config file in version control with the audit. There's also a quick curl loop to check key URLs aren't being blocked before you start.
```

Scene 8 — Watch the first minutes

```text
Watch the first minutes of the crawl and stop to fix the config if you see these signs. All URLs returning four-oh-three or a challenge page means bot protection. The crawl exploding into parameter URLs within minutes means a facet trap, so cap it, note it as a finding, and exclude it from the main crawl. Rendered pages with no content means rendering timeouts, so increase the timeout or check blocked resources. And redirects to a login or geo page means IP-based geo-redirects, which is itself a finding for an international site.
```

Scene 9 — Example 1: Kiran Home kick-off (illustrative)

```text
Worked example one, simple, and illustrative. On the Kiran Home kick-off crawl, the first two hundred URLs all return five-oh-three. The CDN's bot fight mode is challenging the crawler. The developers add a temporary allow rule for the audit crawler's IP. But note the second-order question for the audit: does the same rule set challenge Bingbot or AI search bots? That becomes evidence for hypothesis five.
```

Scene 10 — Example 2: the too-short timeout (illustrative)

```text
Worked example two, also illustrative. A Doha fashion retailer's crawl finds only seven hundred URLs on a site with six thousand products. The rendered crawl used a two-second timeout, and the category grid loads after three. Increasing the timeout and checking resources reveals the full catalogue, and a genuine finding: the category API is slow, which also hurts real users and Google's rendering. Configuration problems often hide real problems.
```

Scene 11 — Record it; avoid mistakes

```text
Recording the configuration. Save the crawler's configuration file, and note the crawler version, date, settings, seeds and exclusions in your findings workbook. When you re-crawl for verification later in this workshop, you'll use the identical configuration, so any difference comes from fixes and not from settings. Common mistakes: crawling with a desktop user agent, leaving rendering off on a JavaScript site, crawling from a blocked IP and reporting site returns four-oh-three as a finding, and not seeding with old URLs.
```

Scene 12 — Recap and try this now

```text
Recap. Configure the crawl to approximate Googlebot: smartphone agent, rendering on, robots respected, seeded with sitemaps and old URLs, segmented, and allow-listed. Run rendered and raw crawls, plus list crawls. Record everything and re-use it. Try this now. Write a configuration sheet for a site you know, with a reason for every setting, and run a quick curl check on three key URLs with a Googlebot smartphone user agent to see if anything blocks you.
```

</details>

### 3. Reading the crawl: indexability, status and templates

Lesson `reading-the-crawl-findings` · module “Running the crawl and gathering evidence” · target 7 min · 13 scenes · 945 words · planned 6:50 (words ≈ 6:51) · status: **to produce**

| Term | Say |
|---|---|
| F-03 | F three |
| crosstab | cross-tab |
| hreflang | H ref lang |

| # | Time | Sec | Narration | On screen | Visual |
|---|---|---|---|---|---|
| 1 | 0:00 | 32 | The Kiran Home crawl has finished. Around sixty-one thousand URLs, all illustrative, on a site that only wants about four and a half thousand pages indexed. Your job isn't to list every warning. It's to explain that gap with a handful of patterns, backed by real examples. In this lecture you'll pivot the crawl by segment, investigate each pattern with samples, review redirects and templates, and record findings so they survive a sceptical stakeholder. | Reading the crawl<br>• From thousands of rows to a few patterns<br>• Samples, not assumptions<br>• Findings that survive scrutiny | A counter shows '61,000 URLs crawled' beside '4,500 wanted'; the gap between them is highlighted. Title appears. |
| 2 | 0:32 | 31 | Step one: the indexability overview by segment. Pivot the crawl by segment and indexability. For Kiran Home, the illustrative picture is striking. Around sixteen thousand product URLs, but only about four thousand indexable, because the rest canonicalise to another market. About forty-two thousand facet URLs, all indexable, nothing preventing it. Nineteen hundred internal search pages, crawlable and indexable. And blog posts mostly canonicalised to the UK versions. Three patterns jump out immediately. | Step 1: segment × indexability<br>• Products canonicalised across markets<br>• Facets and search crawlable + indexable<br>• Blog canonicalised to /en-gb/ | Pivot table animates; cells for facets and cross-market canonicals glow as three pattern labels appear. |
| 3 | 1:03 | 34 | Step two: never report a pattern without opening real examples. Open five products in each market. On the UAE brass lantern page, the canonical points to the UK version, while hreflang lists both UAE and UK. So every non-UK product tells Google it's a duplicate of the UK page, and hreflang can't work, because hreflang only connects canonical pages. That fits hypothesis three, broken international signals, and it explains why UAE users might see UK prices in results. | Step 2: open real examples<br>• UAE page → canonical to UK page<br>• hreflang can't work across non-canonicals<br>• Supports H3; explains UK prices in UAE | HTML head snippet for the /en-ae/ lantern page; the canonical line highlights red pointing to /en-gb/; hreflang lines grey out. |
| 4 | 1:37 | 38 | Step three: status codes and redirects. The list crawl of fifty-six hundred pre-relaunch URLs, illustrative, shows about thirty-one hundred single-hop redirects to relevant pages, which is good. Nine hundred chains of two or more hops. Seven hundred redirects to the home page, mostly old blog category and tag URLs. Eight hundred and fifty four-oh-fours, mostly old product URLs with a different slug pattern. And fifty old URLs still live. The four-oh-fours need checking against backlinks and Search Console clicks; those with links or traffic are high priority. | Step 3: redirect review<br>• Chains, home-page redirects, 404s<br>• 404s with links/traffic = priority<br>• Evidence for H1 | Horizontal bar chart of redirect outcomes builds; the 404 bar splits into 'has links/traffic' and 'no value'. |
| 5 | 2:15 | 33 | Step four: template-level checks. For each key template, open a representative URL and review the rendered HTML: title and H1, canonical, robots directives, main content, internal links as real anchors, and valid JSON-LD matching visible data. For Kiran Home, category pages render only the first twenty-four products, and load more is a button with no paginated URLs. So products beyond the first twenty-four have few internal links. That's a discovery issue, and evidence for hypothesis two. | Step 4: template checks<br>• Title/H1, canonical, robots, content, links, JSON-LD<br>• Categories: 24 products + button, no page URLs<br>• Discovery issue → H2 | Category page wireframe shows 24 product tiles and a 'Load more' button; products 25+ appear greyed out with no incoming links. |
| 6 | 2:48 | 23 | Step five: content signals. Duplicate titles are mostly on facets, which is a symptom, not a separate issue. About four hundred products have fewer than thirty words of description, which is genuinely thin. And market pages that differ only by currency are near-duplicates, acceptable for regional pages if hreflang is correct, but worth noting. | Step 5: content signals<br>• Duplicate titles = facet symptom<br>• ~400 thin products (illustrative)<br>• Currency-only market variants: OK with hreflang | Three content signal cards; the duplicate-titles card gets an arrow pointing to the facet root cause. |
| 7 | 3:11 | 27 | Hands-on. The lesson text has a pandas script that assigns each crawled URL a segment and a market, then builds the segment-by-indexability pivot. It then takes canonicalised product URLs and cross-tabulates their market against the market their canonical points to. For Kiran Home, that crosstab makes the pattern unmistakable in a single table: Pakistan and UAE products all pointing to the UK. | Hands-on: pivot and crosstab<br>• Segment + market columns<br>• Segment × indexability pivot<br>• Market × canonical market crosstab | Code walkthrough; a crosstab matrix fills, with the /en-gb/ column lighting up for all non-UK rows. |
| 8 | 3:38 | 33 | Recording findings properly. Each finding gets an ID, a title, the evidence with counts and example URLs, the source, the affected templates and markets, the hypothesis it links to, and a severity to be scored later. For Kiran Home, finding F three: non-UK product pages canonicalise to UK versions. Evidence: sixteen thousand two hundred product URLs crawled, twelve thousand three hundred canonicalised to UK equivalents, five examples, and URL Inspection confirming Google chose the UK URL. | Record findings<br>• ID, title, evidence, examples, source<br>• Affected templates/markets; hypothesis link<br>• Severity scored later | A finding card 'F-03' fills in field by field in a workbook tab. |
| 9 | 4:11 | 38 | Worked example one is Kiran Home, which you've just seen. Worked example two, a different and illustrative client. A Riyadh electronics store's crawler flags twelve hundred low content pages. Segmenting shows eleven hundred are colour variant URLs that already canonicalise correctly to the parent product. Working as designed. Only a hundred are genuinely thin accessory pages. The finding reported is the hundred, with examples, and the eleven hundred go into a notable non-issues list, so the client doesn't panic the next time a tool shows that warning. | Example 2: the thin-content red herring (illustrative)<br>• 1,200 'low content' warnings<br>• 1,100 variants canonicalised correctly<br>• Report the 100; list the rest as non-issues | A big warning count splits: a large grey slice 'working as designed' and a small red slice 'real issue'. |
| 10 | 4:49 | 17 | Common mistakes. Reporting the crawler's issue list verbatim. Treating symptoms, like duplicate titles on facets, as separate root issues. And stating a finding without URL-level examples and the evidence source. Every one of these makes your audit easier to dismiss. | Common mistakes<br>• Verbatim tool output<br>• Symptoms as separate issues<br>• No examples or sources | Mistake chips; a stamp 'Easy to dismiss' on a generic issue list. |
| 11 | 5:06 | 42 | Let's pause on a skill that separates senior auditors from juniors: distinguishing a symptom from a cause while you're still reading the crawl. A useful habit is to ask, if this pattern disappeared tomorrow, what would have caused it to disappear? For duplicate titles on facets, the answer is, facets stopped being crawlable. So duplicate titles aren't the finding; facet crawlability is. For thin products, the answer is, someone wrote better descriptions. So thin content is its own finding, owned by marketing. Asking that question as you read keeps your workbook short and your tickets meaningful. | Symptom or cause?<br>• 'What would make this pattern disappear?'<br>• Duplicate titles → facet crawlability<br>• Thin products → a real content finding | Two pattern cards; each gets a 'what would make it disappear?' bubble, pointing to either a root-cause card or a standalone finding card. |
| 12 | 5:48 | 34 | One more practical tip: keep a notable non-issues list from the very start. Every crawler produces warnings that are harmless in context: variant URLs that canonicalise as designed, multiple H1s on a clear page, missing meta descriptions on paginated pages, low word counts on contact pages. Listing them explicitly in your report does two things. It shows the client you looked and made a judgement. And it stops them panicking when a tool flags the same thing next month. | Keep a non-issues list<br>• Designed canonicals, multiple H1s, etc.<br>• Shows judgement<br>• Prevents future panic | A two-column board: 'Findings' and 'Notable non-issues'; warning cards get sorted into the right column. |
| 13 | 6:22 | 28 | Recap. Pivot by segment and indexability, open real examples, review redirects and templates, separate symptoms from causes, and record each finding with evidence. Try this now. Take any crawl export, add a segment column with a few regex patterns, and build the pivot. Write down the three biggest patterns and open five example URLs for each before you call any of them a finding. | Recap and try this now<br>• Segment + pivot<br>• 3 patterns × 5 examples<br>• Then write findings | Pivot table and five example URL cards per pattern. End card: 'Next: Search Console, logs and performance evidence'. |

<details><summary>Narration per scene (paste into ElevenLabs)</summary>

Scene 1 — Reading the crawl

```text
The Kiran Home crawl has finished. Around sixty-one thousand URLs, all illustrative, on a site that only wants about four and a half thousand pages indexed. Your job isn't to list every warning. It's to explain that gap with a handful of patterns, backed by real examples. In this lecture you'll pivot the crawl by segment, investigate each pattern with samples, review redirects and templates, and record findings so they survive a sceptical stakeholder.
```

Scene 2 — Step 1: segment × indexability

```text
Step one: the indexability overview by segment. Pivot the crawl by segment and indexability. For Kiran Home, the illustrative picture is striking. Around sixteen thousand product URLs, but only about four thousand indexable, because the rest canonicalise to another market. About forty-two thousand facet URLs, all indexable, nothing preventing it. Nineteen hundred internal search pages, crawlable and indexable. And blog posts mostly canonicalised to the UK versions. Three patterns jump out immediately.
```

Scene 3 — Step 2: open real examples

```text
Step two: never report a pattern without opening real examples. Open five products in each market. On the UAE brass lantern page, the canonical points to the UK version, while hreflang lists both UAE and UK. So every non-UK product tells Google it's a duplicate of the UK page, and hreflang can't work, because hreflang only connects canonical pages. That fits hypothesis three, broken international signals, and it explains why UAE users might see UK prices in results.
```

Scene 4 — Step 3: redirect review

```text
Step three: status codes and redirects. The list crawl of fifty-six hundred pre-relaunch URLs, illustrative, shows about thirty-one hundred single-hop redirects to relevant pages, which is good. Nine hundred chains of two or more hops. Seven hundred redirects to the home page, mostly old blog category and tag URLs. Eight hundred and fifty four-oh-fours, mostly old product URLs with a different slug pattern. And fifty old URLs still live. The four-oh-fours need checking against backlinks and Search Console clicks; those with links or traffic are high priority.
```

Scene 5 — Step 4: template checks

```text
Step four: template-level checks. For each key template, open a representative URL and review the rendered HTML: title and H1, canonical, robots directives, main content, internal links as real anchors, and valid JSON-LD matching visible data. For Kiran Home, category pages render only the first twenty-four products, and load more is a button with no paginated URLs. So products beyond the first twenty-four have few internal links. That's a discovery issue, and evidence for hypothesis two.
```

Scene 6 — Step 5: content signals

```text
Step five: content signals. Duplicate titles are mostly on facets, which is a symptom, not a separate issue. About four hundred products have fewer than thirty words of description, which is genuinely thin. And market pages that differ only by currency are near-duplicates, acceptable for regional pages if hreflang is correct, but worth noting.
```

Scene 7 — Hands-on: pivot and crosstab

```text
Hands-on. The lesson text has a pandas script that assigns each crawled URL a segment and a market, then builds the segment-by-indexability pivot. It then takes canonicalised product URLs and cross-tabulates their market against the market their canonical points to. For Kiran Home, that crosstab makes the pattern unmistakable in a single table: Pakistan and UAE products all pointing to the UK.
```

Scene 8 — Record findings

```text
Recording findings properly. Each finding gets an ID, a title, the evidence with counts and example URLs, the source, the affected templates and markets, the hypothesis it links to, and a severity to be scored later. For Kiran Home, finding F three: non-UK product pages canonicalise to UK versions. Evidence: sixteen thousand two hundred product URLs crawled, twelve thousand three hundred canonicalised to UK equivalents, five examples, and URL Inspection confirming Google chose the UK URL.
```

Scene 9 — Example 2: the thin-content red herring (illustrative)

```text
Worked example one is Kiran Home, which you've just seen. Worked example two, a different and illustrative client. A Riyadh electronics store's crawler flags twelve hundred low content pages. Segmenting shows eleven hundred are colour variant URLs that already canonicalise correctly to the parent product. Working as designed. Only a hundred are genuinely thin accessory pages. The finding reported is the hundred, with examples, and the eleven hundred go into a notable non-issues list, so the client doesn't panic the next time a tool shows that warning.
```

Scene 10 — Common mistakes

```text
Common mistakes. Reporting the crawler's issue list verbatim. Treating symptoms, like duplicate titles on facets, as separate root issues. And stating a finding without URL-level examples and the evidence source. Every one of these makes your audit easier to dismiss.
```

Scene 11 — Symptom or cause?

```text
Let's pause on a skill that separates senior auditors from juniors: distinguishing a symptom from a cause while you're still reading the crawl. A useful habit is to ask, if this pattern disappeared tomorrow, what would have caused it to disappear? For duplicate titles on facets, the answer is, facets stopped being crawlable. So duplicate titles aren't the finding; facet crawlability is. For thin products, the answer is, someone wrote better descriptions. So thin content is its own finding, owned by marketing. Asking that question as you read keeps your workbook short and your tickets meaningful.
```

Scene 12 — Keep a non-issues list

```text
One more practical tip: keep a notable non-issues list from the very start. Every crawler produces warnings that are harmless in context: variant URLs that canonicalise as designed, multiple H1s on a clear page, missing meta descriptions on paginated pages, low word counts on contact pages. Listing them explicitly in your report does two things. It shows the client you looked and made a judgement. And it stops them panicking when a tool flags the same thing next month.
```

Scene 13 — Recap and try this now

```text
Recap. Pivot by segment and indexability, open real examples, review redirects and templates, separate symptoms from causes, and record each finding with evidence. Try this now. Take any crawl export, add a segment column with a few regex patterns, and build the pivot. Write down the three biggest patterns and open five example URLs for each before you call any of them a finding.
```

</details>

### 4. Search Console, logs and performance evidence

Lesson `search-console-logs-and-performance-evidence` · module “Running the crawl and gathering evidence” · target 6 min · 11 scenes · 908 words · planned 6:34 (words ≈ 6:35) · status: **to produce**

| Term | Say |
|---|---|
| CrUX | crux |
| E-07 | E seven |
| INP | I N P |
| LCP | L C P |

| # | Time | Sec | Narration | On screen | Visual |
|---|---|---|---|---|---|
| 1 | 0:00 | 32 | A crawler simulates. It shows you what could happen. Search Console shows what Google did with your URLs. Logs show what Googlebot actually requested. And field performance data shows what real users experienced. A strong audit joins all four. In this lecture, you'll gather that evidence for Kiran Home, segment it by market and template, add the newer twenty twenty-six sources, and build an evidence log that makes every claim in your report traceable. | Search Console, logs and performance evidence<br>• Four sources, one story<br>• Segment by market and template<br>• An evidence log for every claim | Four data source icons (crawler, Search Console, logs, CrUX) feed into a single 'evidence log' document. Title appears. |
| 2 | 0:32 | 42 | Start with Search Console, question by question. When did the decline start, and where? Use Performance, compare periods, and filter by page segment and country. For Kiran Home, the illustrative drop starts the week of relaunch, concentrated in UAE and Pakistan traffic to product pages. Which URLs aren't indexed, and why? Page indexing filtered by sitemap shows many product URLs as alternate page with proper canonical tag. What canonical did Google pick? URL Inspection on samples shows the UK product for UAE pages. And are rich results intact? Product items fell, with offers missing on some templates. | Search Console questions<br>• When and where did it drop?<br>• Why aren't URLs indexed?<br>• Which canonical did Google pick? Rich results intact? | Four mini report cards animate with illustrative findings: a dip by country chart, an indexing status bar, a URL Inspection canonical field, and an enhancements chart. |
| 3 | 1:14 | 34 | Use Search Console's newer features to make this faster. The branded queries filter, where it's available for the property, splits brand from non-brand, or you can use your own regex. Custom annotations let you mark the relaunch date directly on the chart. And always compare twenty-eight days before and after the relaunch, and the same windows a year earlier, to control for seasonality, for example if Ramadan fell in the comparison window one year and not the other. | Faster, fairer comparisons<br>• Branded filter or regex<br>• Annotate the relaunch<br>• Before/after + year-on-year | Performance chart with an annotation flag 'Relaunch'; a branded/non-branded toggle; a second faint line for last year's same period. |
| 4 | 1:48 | 40 | Now logs. Cloudflare logs for four weeks, filtered to verified Googlebot and classified by segment, tell a clear story for Kiran Home. The large majority of requests go to facet parameters. A noticeable share goes to internal search URLs, linked from a popular searches widget. Only a small share goes to products, and many products weren't requested at all in four weeks. And Googlebot still requests pre-relaunch URLs heavily. Two conclusions: crawl attention is being spent on URLs with no value, and legacy URLs still matter to Google, so redirect gaps matter. | Logs: what Googlebot did<br>• Facets dominate; search URLs crawled<br>• Many products never requested<br>• Legacy URLs still requested | Stacked bar of Googlebot requests by segment; the facet segment dominates; a 'never requested' counter for products ticks up. |
| 5 | 2:28 | 28 | Here's a join that makes the point unarguable. Take products with zero verified Googlebot hits in four weeks, and cross-reference them with revenue from the pre-relaunch period. If many former best-sellers haven't been crawled at all, that's a headline finding for the stakeholder report. Founders don't care about crawl budget. They care that their best-selling brass lanterns haven't been visited by Google in a month. | The unarguable join<br>• Zero-hit products × pre-relaunch revenue<br>• Best-sellers not crawled = headline<br>• Speak in revenue, not crawl budget | Venn-style join: 'zero Googlebot hits' and 'top revenue products' overlap; the overlap labelled 'headline finding' with product thumbnails. |
| 6 | 2:56 | 41 | Performance evidence. Run PageSpeed Insights for each template and read the field data first. For Kiran Home, illustratively, category pages have poor LCP because the hero carousel image is lazy-loaded, and product pages have poor INP and CLS because a reviews widget shifts layout and blocks the main thread. Where CrUX lacks data for a URL, use origin data and Search Console's Core Web Vitals groups, then use a lab trace to find the cause. Rendering evidence too: URL Inspection shows customers also bought links only load after interaction, so they add no discovery value. | Performance + rendering evidence<br>• Field data first, per template<br>• Category LCP, product INP/CLS<br>• Interaction-only links = no discovery value | Template × metric grid with coloured statuses; a product page mock with a reviews widget causing a shift. |
| 7 | 3:37 | 37 | And the newer sources for twenty twenty-six. Search Console's generative AI report shows whether Kiran Home pages appear in AI Overviews or AI Mode, by page and country, so you can see if AI exposure fell too. Bing Webmaster Tools shows whether Bing indexing and Copilot citations dropped, and a second engine's view helps separate site problems from Google-specific changes. And CDN firewall events show whether real crawlers, including Bingbot and AI search bots, are being challenged since the relaunch. That's evidence for hypothesis five. | New evidence sources<br>• GSC generative AI report<br>• Bing Site Explorer + AI Performance<br>• CDN bot/firewall events → H5 | Three new source tiles slide into the evidence board beside the original four. |
| 8 | 4:14 | 32 | Hands-on. The lesson text has a short script using the Search Console API that pulls clicks by page and country for twenty-eight days before and after the relaunch, assigns a segment from the URL, and pivots before versus after with a percentage change. It reuses the authenticated client from the Technical SEO Mastery course. For bigger sites, paginate with start row. This single table usually tells you where the drop is concentrated within minutes. | Hands-on: before/after by segment<br>• API pull: page × country<br>• Segment from URL<br>• Pivot with % change | Code walkthrough; the pivot table shows products in AE and PK with large negative changes (illustrative) highlighted. |
| 9 | 4:46 | 38 | Worked example two, a different client, illustrative, where the evidence exonerates the relaunch. A UK garden furniture retailer's segmentation shows the decline spread evenly across all templates and markets, starting two weeks before the relaunch, and matching the end of the spring buying season in last year's data too. Logs show healthy crawling, and indexing is stable. The evidence points to seasonality, not a technical regression. The audit says so, and saves the client from an unnecessary emergency rebuild. Good auditing sometimes means saying, it isn't technical. | Example 2: seasonality, not SEO (illustrative)<br>• Even decline across segments<br>• Started before relaunch; matches last year<br>• Healthy crawl and indexing → say so | Two overlaid yearly lines with the same seasonal dip; the relaunch marker sits after the dip began; a verdict card 'Seasonal'. |
| 10 | 5:24 | 34 | Build the evidence log. Every claim in your final report must trace back to evidence: an ID, the source, the date range, the filter, the finding, and the file or screenshot. For Kiran Home: E seven, Cloudflare logs for March, verified Googlebot, facets dominate requests. E eight, Search Console Page indexing for the UAE product sitemap, most URLs alternate page with proper canonical tag. This protects you when a stakeholder challenges a recommendation, and it makes verification later straightforward. | The evidence log<br>• ID, source, dates, filter, finding, file<br>• E-07 logs; E-08 Page indexing<br>• Protects you; eases verification | Evidence log rows type in; a stakeholder speech bubble 'Prove it' is answered by an arrow pointing at E-08. |
| 11 | 5:58 | 36 | Common mistakes. Reading Performance data without segmentation, so a market-specific drop gets diluted. Comparing only with the previous twenty-eight days and ignoring seasonality. Using unverified Googlebot log lines. And treating Lighthouse lab scores as the verdict on Core Web Vitals. Recap: join Search Console, logs, field data and rendering evidence; segment everything; use the new AI and Bing sources; log every piece of evidence. Try this now: run a before-and-after comparison by segment and country for your own site's last major change. | Mistakes, recap, try this now<br>• Segment; control for seasonality<br>• Verify bots; field over lab<br>• Run a before/after by segment | Mistake chips fade; recap ticks; a mini pivot table appears. End card: 'Next: the log-file analysis lab'. |

<details><summary>Narration per scene (paste into ElevenLabs)</summary>

Scene 1 — Search Console, logs and performance evidence

```text
A crawler simulates. It shows you what could happen. Search Console shows what Google did with your URLs. Logs show what Googlebot actually requested. And field performance data shows what real users experienced. A strong audit joins all four. In this lecture, you'll gather that evidence for Kiran Home, segment it by market and template, add the newer twenty twenty-six sources, and build an evidence log that makes every claim in your report traceable.
```

Scene 2 — Search Console questions

```text
Start with Search Console, question by question. When did the decline start, and where? Use Performance, compare periods, and filter by page segment and country. For Kiran Home, the illustrative drop starts the week of relaunch, concentrated in UAE and Pakistan traffic to product pages. Which URLs aren't indexed, and why? Page indexing filtered by sitemap shows many product URLs as alternate page with proper canonical tag. What canonical did Google pick? URL Inspection on samples shows the UK product for UAE pages. And are rich results intact? Product items fell, with offers missing on some templates.
```

Scene 3 — Faster, fairer comparisons

```text
Use Search Console's newer features to make this faster. The branded queries filter, where it's available for the property, splits brand from non-brand, or you can use your own regex. Custom annotations let you mark the relaunch date directly on the chart. And always compare twenty-eight days before and after the relaunch, and the same windows a year earlier, to control for seasonality, for example if Ramadan fell in the comparison window one year and not the other.
```

Scene 4 — Logs: what Googlebot did

```text
Now logs. Cloudflare logs for four weeks, filtered to verified Googlebot and classified by segment, tell a clear story for Kiran Home. The large majority of requests go to facet parameters. A noticeable share goes to internal search URLs, linked from a popular searches widget. Only a small share goes to products, and many products weren't requested at all in four weeks. And Googlebot still requests pre-relaunch URLs heavily. Two conclusions: crawl attention is being spent on URLs with no value, and legacy URLs still matter to Google, so redirect gaps matter.
```

Scene 5 — The unarguable join

```text
Here's a join that makes the point unarguable. Take products with zero verified Googlebot hits in four weeks, and cross-reference them with revenue from the pre-relaunch period. If many former best-sellers haven't been crawled at all, that's a headline finding for the stakeholder report. Founders don't care about crawl budget. They care that their best-selling brass lanterns haven't been visited by Google in a month.
```

Scene 6 — Performance + rendering evidence

```text
Performance evidence. Run PageSpeed Insights for each template and read the field data first. For Kiran Home, illustratively, category pages have poor LCP because the hero carousel image is lazy-loaded, and product pages have poor INP and CLS because a reviews widget shifts layout and blocks the main thread. Where CrUX lacks data for a URL, use origin data and Search Console's Core Web Vitals groups, then use a lab trace to find the cause. Rendering evidence too: URL Inspection shows customers also bought links only load after interaction, so they add no discovery value.
```

Scene 7 — New evidence sources

```text
And the newer sources for twenty twenty-six. Search Console's generative AI report shows whether Kiran Home pages appear in AI Overviews or AI Mode, by page and country, so you can see if AI exposure fell too. Bing Webmaster Tools shows whether Bing indexing and Copilot citations dropped, and a second engine's view helps separate site problems from Google-specific changes. And CDN firewall events show whether real crawlers, including Bingbot and AI search bots, are being challenged since the relaunch. That's evidence for hypothesis five.
```

Scene 8 — Hands-on: before/after by segment

```text
Hands-on. The lesson text has a short script using the Search Console API that pulls clicks by page and country for twenty-eight days before and after the relaunch, assigns a segment from the URL, and pivots before versus after with a percentage change. It reuses the authenticated client from the Technical SEO Mastery course. For bigger sites, paginate with start row. This single table usually tells you where the drop is concentrated within minutes.
```

Scene 9 — Example 2: seasonality, not SEO (illustrative)

```text
Worked example two, a different client, illustrative, where the evidence exonerates the relaunch. A UK garden furniture retailer's segmentation shows the decline spread evenly across all templates and markets, starting two weeks before the relaunch, and matching the end of the spring buying season in last year's data too. Logs show healthy crawling, and indexing is stable. The evidence points to seasonality, not a technical regression. The audit says so, and saves the client from an unnecessary emergency rebuild. Good auditing sometimes means saying, it isn't technical.
```

Scene 10 — The evidence log

```text
Build the evidence log. Every claim in your final report must trace back to evidence: an ID, the source, the date range, the filter, the finding, and the file or screenshot. For Kiran Home: E seven, Cloudflare logs for March, verified Googlebot, facets dominate requests. E eight, Search Console Page indexing for the UAE product sitemap, most URLs alternate page with proper canonical tag. This protects you when a stakeholder challenges a recommendation, and it makes verification later straightforward.
```

Scene 11 — Mistakes, recap, try this now

```text
Common mistakes. Reading Performance data without segmentation, so a market-specific drop gets diluted. Comparing only with the previous twenty-eight days and ignoring seasonality. Using unverified Googlebot log lines. And treating Lighthouse lab scores as the verdict on Core Web Vitals. Recap: join Search Console, logs, field data and rendering evidence; segment everything; use the new AI and Bing sources; log every piece of evidence. Try this now: run a before-and-after comparison by segment and country for your own site's last major change.
```

</details>

### 5. Log-file analysis lab: Cloudflare logs to crawl insights in Python

Lesson `log-file-analysis-lab` · module “Running the crawl and gathering evidence” · target 7 min · 12 scenes · 995 words · planned 7:12 (words ≈ 7:13) · status: **to produce**

| Term | Say |
|---|---|
| NDJSON | N D JSON |
| Logpush | log push |
| gzip | G zip |
| DNS | D N S |

| # | Time | Sec | Narration | On screen | Visual |
|---|---|---|---|---|---|
| 1 | 0:00 | 36 | This is a lab lesson. Kiran Home's developers have exported four weeks of Cloudflare request logs, and it's your job to turn millions of lines into three or four findings a founder will understand. In this lecture you'll load the logs in Python, verify real Googlebot, classify every request by template and market, build the four tables every log audit needs, and find the best-selling products Google hasn't visited in a month. Everything here is illustrative data, but the workflow is real. | Log-file analysis lab<br>• Cloudflare logs → Python<br>• Verify, classify, quantify<br>• Find the best-sellers Google ignored | A compressed log file icon unzips into a waterfall of JSON lines, which funnel into four summary tables. Title appears. |
| 2 | 0:36 | 39 | First, what does the data look like? Cloudflare Logpush exports newline-delimited JSON: one request per line, with fields like the edge timestamp, client IP, host, request URI, response status, user agent and cache status. The exact fields depend on what was selected in the Logpush job, so check the field list. If the user agent is missing, ask for it to be added, because you can't analyse bots without it. And remember, IP addresses are personal data. Store the logs securely and delete the raw files when the engagement ends. | The data<br>• NDJSON: one request per line<br>• Timestamp, IP, URI, status, UA, cache status<br>• Check fields; protect personal data | A single JSON log line expands into labelled fields; a padlock and a 'delete after engagement' calendar note appear. |
| 3 | 1:15 | 44 | Step one: load, verify and classify. The script reads every gzipped file line by line, parses the JSON, and renames the fields to short names. It filters to lines claiming to be Googlebot or bingbot, then verifies each IP with a reverse DNS lookup to a Google or Microsoft domain and a forward lookup back to the same IP, caching results so each IP is only checked once. Only verified Googlebot lines go forward. Then every request gets a segment, like facet, search, product, category, journal or asset, a market from the folder, and a legacy flag for pre-relaunch URL patterns. | Step 1: load, verify, classify<br>• Parse gzipped NDJSON<br>• Reverse + forward DNS, cached<br>• Segment, market, legacy flag | Code walkthrough: the gzip loop, the verified() function with its cache, and the SEG list mapping to a new 'segment' column. |
| 4 | 1:59 | 32 | Why does verification matter so much? Because on most real sites, a noticeable share of traffic claiming to be Googlebot is scrapers and SEO tools pretending. If you include them, you'll report crawl patterns that Google isn't actually responsible for. In one audit, unverified fake Googlebot traffic hammering the internal search pages made it look like Google was obsessed with search URLs. Once verified, the real Googlebot's pattern looked very different. Verify first, always. | Why verify?<br>• Fake 'Googlebot' is common<br>• It distorts every conclusion<br>• Verify first, always | Two pie charts: 'all Googlebot UA' vs 'verified only'; the 'search URLs' slice shrinks dramatically after verification. |
| 5 | 2:31 | 34 | Step two: the four tables every log audit needs. One: the share of crawl by segment. Two: status codes by segment. Three: how Google experiences old URLs, meaning the status codes returned to legacy URL requests. And four: weekly requests by segment, saved to a CSV so you can chart it and annotate release dates. That weekly chart will become one of the most persuasive visuals in your stakeholder report, because it shows cause and effect over time. | Step 2: four tables<br>• Share of crawl by segment<br>• Status by segment; legacy URL statuses<br>• Weekly by segment → chart + annotations | Four small tables arrange in a 2×2 grid; the weekly one transforms into a stacked area chart with a 'relaunch' annotation. |
| 6 | 3:05 | 34 | Step three is the join that gets a founder's attention. Load the list of products with their revenue from before the relaunch. Build the set of product paths that verified Googlebot requested in the last four weeks. Mark each product as crawled or not. Then look at the top two hundred revenue products and count how many had no verified Googlebot request at all in four weeks. That sentence, in plain language, is the headline of the log section. | Step 3: best-sellers not crawled<br>• Pre-relaunch revenue list<br>• Crawled in 4 weeks? yes/no<br>• Count among the top 200 | Product list sorted by revenue; a 'crawled' column fills with ticks and crosses; a counter shows 'X of top 200 not crawled' (illustrative). |
| 7 | 3:39 | 34 | Reading Kiran Home's illustrative results. Facet and search URLs take most verified Googlebot requests, and products only a small share. Legacy dot html URLs are still requested daily, and a large share return four-oh-four, which confirms the redirect gap matters. A substantial number of the top two hundred revenue products had no Googlebot request in four weeks. And the cache status shows most bot hits were served from Cloudflare's cache, so origin logs alone would have badly under-counted them. | Kiran Home results (illustrative)<br>• Facets + search dominate<br>• Legacy URLs → many 404s<br>• Best-sellers uncrawled; most hits cached | Four finding cards with small charts: segment share, legacy status bar, top-200 counter, cache hit pie. |
| 8 | 4:13 | 33 | How do these results connect to your root causes? The facet and search dominance is evidence for root cause two, crawlable facets and search. The legacy four-oh-fours support root cause three, the redirect gaps. And uncrawled deep products support root cause four, pagination that isn't crawlable. Logs rarely create new root causes on their own. What they do is quantify and confirm the ones you found in the crawl, and that confirmation is what gets tickets prioritised. | Logs confirm root causes<br>• Facet dominance → RC2<br>• Legacy 404s → RC3<br>• Uncrawled deep products → RC4 | Three arrows connect log finding cards to root-cause cards RC2, RC3 and RC4. |
| 9 | 4:46 | 37 | Worked example two, a smaller site, illustrative. A Leeds bakery chain on shared hosting has only Apache combined logs, no Logpush. The same approach works with a regular expression parser, and with only a few thousand bot hits a month, a spreadsheet pivot after parsing is enough. The key finding: Googlebot requests an old menu PDF hundreds of times a month, a file that was replaced by an HTML menu. So they redirect the PDF to the HTML menu page. Small site, same method, useful result. | Example 2: a small bakery chain (illustrative)<br>• Apache logs, spreadsheet pivot<br>• Old menu.pdf still crawled heavily<br>• Redirect to the HTML menu | An Apache log line parses into columns; a pivot shows '/menu.pdf' at the top; a redirect arrow points to '/menu/'. |
| 10 | 5:23 | 29 | Common mistakes. Analysing unverified Googlebot lines. Ignoring cached hits by using origin logs only. Reporting raw counts instead of shares and trends by segment, which makes big sites look scary and small sites look fine regardless of reality. And keeping raw log files full of IP addresses after the engagement ends. Measure your log work by whether each finding links to a root cause and a ticket. | Common mistakes<br>• Unverified bots; origin-only logs<br>• Raw counts instead of shares and trends<br>• Keeping IP data too long | Four mistake chips; a final chip 'every log finding → a root cause → a ticket'. |
| 11 | 5:52 | 47 | Tables don't persuade people. Findings do. So convert each table into a sentence with a number, a comparison and a consequence, and link it to a root cause. For example: most of Google's crawling goes to filter and search URLs that shouldn't be indexed. Google still requests old URLs daily, and a large share return four-oh-four, including URLs with backlinks. And many best-sellers weren't visited by Google in four weeks. Then chart the weekly table as a stacked area by segment, annotate the relaunch, and reuse that same chart after the fixes. A before and after on one visual is the most convincing evidence you can show. | From tables to findings<br>• Number + comparison + consequence<br>• Link to a root cause<br>• One chart, reused before and after | A table morphs into a plain-English finding sentence; then a stacked area chart gains 'relaunch' and 'fix release' annotations. |
| 12 | 6:39 | 33 | Recap. Load the logs, verify bots, classify by segment and market, build the four tables, and join with revenue to find the best-sellers Google ignored. Use the results to confirm and quantify root causes. Try this now. If you have access to any site's logs, run step one and step two on a single week. Just the share of verified Googlebot requests by segment will tell you whether crawl waste is a real problem there. | Recap and try this now<br>• Verify → classify → four tables → revenue join<br>• Confirm root causes<br>• Run steps 1–2 on one week | Pipeline recap animation; a single week's segment share pie appears. End card: 'Next: rendering and structured data checks'. |

<details><summary>Narration per scene (paste into ElevenLabs)</summary>

Scene 1 — Log-file analysis lab

```text
This is a lab lesson. Kiran Home's developers have exported four weeks of Cloudflare request logs, and it's your job to turn millions of lines into three or four findings a founder will understand. In this lecture you'll load the logs in Python, verify real Googlebot, classify every request by template and market, build the four tables every log audit needs, and find the best-selling products Google hasn't visited in a month. Everything here is illustrative data, but the workflow is real.
```

Scene 2 — The data

```text
First, what does the data look like? Cloudflare Logpush exports newline-delimited JSON: one request per line, with fields like the edge timestamp, client IP, host, request URI, response status, user agent and cache status. The exact fields depend on what was selected in the Logpush job, so check the field list. If the user agent is missing, ask for it to be added, because you can't analyse bots without it. And remember, IP addresses are personal data. Store the logs securely and delete the raw files when the engagement ends.
```

Scene 3 — Step 1: load, verify, classify

```text
Step one: load, verify and classify. The script reads every gzipped file line by line, parses the JSON, and renames the fields to short names. It filters to lines claiming to be Googlebot or bingbot, then verifies each IP with a reverse DNS lookup to a Google or Microsoft domain and a forward lookup back to the same IP, caching results so each IP is only checked once. Only verified Googlebot lines go forward. Then every request gets a segment, like facet, search, product, category, journal or asset, a market from the folder, and a legacy flag for pre-relaunch URL patterns.
```

Scene 4 — Why verify?

```text
Why does verification matter so much? Because on most real sites, a noticeable share of traffic claiming to be Googlebot is scrapers and SEO tools pretending. If you include them, you'll report crawl patterns that Google isn't actually responsible for. In one audit, unverified fake Googlebot traffic hammering the internal search pages made it look like Google was obsessed with search URLs. Once verified, the real Googlebot's pattern looked very different. Verify first, always.
```

Scene 5 — Step 2: four tables

```text
Step two: the four tables every log audit needs. One: the share of crawl by segment. Two: status codes by segment. Three: how Google experiences old URLs, meaning the status codes returned to legacy URL requests. And four: weekly requests by segment, saved to a CSV so you can chart it and annotate release dates. That weekly chart will become one of the most persuasive visuals in your stakeholder report, because it shows cause and effect over time.
```

Scene 6 — Step 3: best-sellers not crawled

```text
Step three is the join that gets a founder's attention. Load the list of products with their revenue from before the relaunch. Build the set of product paths that verified Googlebot requested in the last four weeks. Mark each product as crawled or not. Then look at the top two hundred revenue products and count how many had no verified Googlebot request at all in four weeks. That sentence, in plain language, is the headline of the log section.
```

Scene 7 — Kiran Home results (illustrative)

```text
Reading Kiran Home's illustrative results. Facet and search URLs take most verified Googlebot requests, and products only a small share. Legacy dot html URLs are still requested daily, and a large share return four-oh-four, which confirms the redirect gap matters. A substantial number of the top two hundred revenue products had no Googlebot request in four weeks. And the cache status shows most bot hits were served from Cloudflare's cache, so origin logs alone would have badly under-counted them.
```

Scene 8 — Logs confirm root causes

```text
How do these results connect to your root causes? The facet and search dominance is evidence for root cause two, crawlable facets and search. The legacy four-oh-fours support root cause three, the redirect gaps. And uncrawled deep products support root cause four, pagination that isn't crawlable. Logs rarely create new root causes on their own. What they do is quantify and confirm the ones you found in the crawl, and that confirmation is what gets tickets prioritised.
```

Scene 9 — Example 2: a small bakery chain (illustrative)

```text
Worked example two, a smaller site, illustrative. A Leeds bakery chain on shared hosting has only Apache combined logs, no Logpush. The same approach works with a regular expression parser, and with only a few thousand bot hits a month, a spreadsheet pivot after parsing is enough. The key finding: Googlebot requests an old menu PDF hundreds of times a month, a file that was replaced by an HTML menu. So they redirect the PDF to the HTML menu page. Small site, same method, useful result.
```

Scene 10 — Common mistakes

```text
Common mistakes. Analysing unverified Googlebot lines. Ignoring cached hits by using origin logs only. Reporting raw counts instead of shares and trends by segment, which makes big sites look scary and small sites look fine regardless of reality. And keeping raw log files full of IP addresses after the engagement ends. Measure your log work by whether each finding links to a root cause and a ticket.
```

Scene 11 — From tables to findings

```text
Tables don't persuade people. Findings do. So convert each table into a sentence with a number, a comparison and a consequence, and link it to a root cause. For example: most of Google's crawling goes to filter and search URLs that shouldn't be indexed. Google still requests old URLs daily, and a large share return four-oh-four, including URLs with backlinks. And many best-sellers weren't visited by Google in four weeks. Then chart the weekly table as a stacked area by segment, annotate the relaunch, and reuse that same chart after the fixes. A before and after on one visual is the most convincing evidence you can show.
```

Scene 12 — Recap and try this now

```text
Recap. Load the logs, verify bots, classify by segment and market, build the four tables, and join with revenue to find the best-sellers Google ignored. Use the results to confirm and quantify root causes. Try this now. If you have access to any site's logs, run step one and step two on a single week. Just the share of verified Googlebot requests by segment will tell you whether crawl waste is a real problem there.
```

</details>

### 6. Rendering and structured data checks: raw vs rendered, markup vs visible

Lesson `rendering-and-structured-data-checks` · module “Running the crawl and gathering evidence” · target 7 min · 12 scenes · 928 words · planned 6:44 (words ≈ 6:43) · status: **to produce**

| Term | Say |
|---|---|
| JSON-LD | jason L D |
| SSR | S S R |
| Playwright | play-write |

| # | Time | Sec | Narration | On screen | Visual |
|---|---|---|---|---|---|
| 1 | 0:00 | 33 | Server-side rendering is a big improvement, but it isn't a guarantee. Components can still fetch content on the client. Hydration can change canonicals. And structured data can quietly drift away from the price on the page. In this lecture you'll run three checks on Kiran Home's key templates: raw versus rendered parity, JSON-LD versus visible price and availability, and eligibility in Google's tools, and you'll turn the results into hard numbers for your root causes. | Rendering and structured data checks<br>• Raw vs rendered parity<br>• Markup vs visible facts<br>• Eligibility and reports | A product page splits into two layers, 'raw HTML' and 'rendered', with a few elements present only in one; a JSON-LD price tag disagrees with the visible price tag. Title appears. |
| 2 | 0:33 | 32 | Check one: raw versus rendered parity. Take twenty URLs per template, product, category and journal, and compare the raw crawl with the rendered crawl, or use the Playwright diff script from the Technical SEO Mastery course. Compare the canonical, the robots meta, the main content, the internal links and the JSON-LD. Anything that differs between raw and rendered is a risk, and anything that exists only after interaction effectively doesn't exist for crawlers. | Check 1: raw vs rendered<br>• 20 URLs per template<br>• Canonical, robots, content, links, JSON-LD<br>• Interaction-only = invisible | Comparison table with three template columns builds; cells turn green or red. |
| 3 | 1:05 | 34 | For Kiran Home, the illustrative results bring two new findings. Journal posts have a canonical that changes after hydration: the server sends one market's canonical, then a locale cookie switches it on the client. That feeds root cause one. And on product pages, the customers also bought links only exist after interaction, so they provide no discovery value. Category pages confirm what we already knew: only the first twenty-four products and no pagination links in the raw HTML. | Kiran Home findings (illustrative)<br>• Journal canonical changes after hydration → RC1<br>• 'Customers also bought' links only after interaction<br>• Categories: 24 products, no pagination | Journal page head: canonical line animates from '/en-pk/' to '/en-gb/' after a cookie icon appears. A 'Customers also bought' carousel shows links only after a click icon. |
| 4 | 1:39 | 30 | Check two: structured data versus visible facts. The script in the lesson text fetches each product URL, reads the visible price from the page's price element, then parses every JSON-LD block, handling graphs and lists, and finds the Product offer's price, currency and availability. It flags invalid JSON-LD, missing Product markup, and price mismatches. Run it on a sample that includes variants and sale items, because that's where bugs hide. | Check 2: JSON-LD vs visible<br>• Visible price from the page<br>• Product offers from JSON-LD (@graph aware)<br>• Flag invalid, missing, mismatched | Code walkthrough: select_one for the visible price, the JSON-LD loop, and the MISMATCH condition; output rows with statuses. |
| 5 | 2:09 | 28 | Kiran Home's illustrative results: of sixty sampled products, eighteen have no offers at all, because the template omits offers when a product has variants. And five show a sale price on the page while the JSON-LD still has the old price. That's root cause six with hard numbers, and it's exactly the kind of inconsistency that also leads AI assistants to quote wrong prices. | Results (illustrative)<br>• 18/60: offers missing on variant products<br>• 5/60: stale sale prices in JSON-LD<br>• RC6, quantified | A 60-dot grid: 18 dots red 'no offers', 5 amber 'price mismatch', the rest green. |
| 6 | 2:37 | 34 | Check three: eligibility and reports. Run one URL per template through the Rich Results Test, using its rendered view, and note errors versus warnings. Errors block eligibility; warnings are optional properties. Check Search Console's product and merchant listing reports and the breadcrumbs report for trend changes since the relaunch. And don't recommend markup for features that no longer exist. FAQ rich results ended in May twenty twenty-six, so existing FAQ markup is harmless, but it's not a recommendation. | Check 3: eligibility<br>• Rich Results Test per template<br>• GSC enhancement trends since relaunch<br>• No recommendations for retired features | Rich Results Test mock with 'errors' and 'warnings' tabs; an enhancement report chart drops at the relaunch marker. |
| 7 | 3:11 | 25 | Here's a subtle point worth remembering. Valid isn't the same as correct. The Rich Results Test tells you whether markup is structurally valid and eligible. It doesn't tell you whether the price is right, or whether two blocks contradict each other. That's why check two exists. Your job as an auditor is to verify facts, not just syntax. | Valid ≠ correct<br>• Tests check structure and eligibility<br>• They don't check truth or consistency<br>• Auditors verify facts | A green 'Valid' badge on a JSON card; a magnifier reveals a wrong price inside, and the badge gets an asterisk 'but incorrect'. |
| 8 | 3:36 | 31 | Worked example two, a different client, illustrative. An Abu Dhabi clinic group has three plugins each outputting clinic markup with different opening hours. The Rich Results Test shows no errors, because all three blocks are valid. But the facts conflict. So the finding isn't invalid markup. It's conflicting facts. The fix: keep one source, generated from the same fields as the visible opening hours, and align the Business Profile hours too. | Example 2: three plugins, three sets of hours (illustrative)<br>• All valid, facts conflict<br>• Keep one source from visible data<br>• Align Business Profile | Three JSON cards with different 'openingHours' values; two fade away; the remaining one links to the visible hours table and a Business Profile card. |
| 9 | 4:07 | 35 | How do you write these up? Each check produces a finding with a count, examples and a root-cause link. Journal canonical changes after hydration: twenty of twenty sampled journal URLs, linked to root cause one. Offers missing on variant products: eighteen of sixty, linked to root cause six. Stale sale prices: five of sixty, also root cause six. Customers also bought links only after interaction: a discovery finding linked to root cause four. Numbers and examples make tickets easy to prioritise. | Write it up<br>• Count + examples + root-cause link<br>• Journal canonicals → RC1<br>• Offers/prices → RC6; interaction links → RC4 | Finding cards slide into the workbook, each tagged with an RC label. |
| 10 | 4:42 | 41 | Because Kiran Home serves four markets, there's a fourth check: market parity. Take one product in each market folder and confirm that the canonical is its own URL, the hreflang set lists all four markets plus x-default, the visible currency matches the market, whether that's rupees, dirhams or pounds, and the JSON-LD price currency matches the visible currency. On the relaunched storefront, illustratively, the Arabic UAE template shows prices in dirhams but its JSON-LD says pounds, inherited from the UK template. Another example for root cause six, and a very easy ticket to write. | Check 4: market parity<br>• Own canonical; full hreflang set<br>• Visible currency = market<br>• JSON-LD currency = visible currency | Four product cards, one per market; the /ar-ae/ card shows 'AED' on the page but 'GBP' in its JSON-LD with a red mismatch line. |
| 11 | 5:23 | 43 | Let's pull the checks into one summary table, because that's what goes into the workbook. Journal canonicals changing after hydration: twenty out of twenty sampled, root cause one, ticket: render the canonical from the URL path on the server. Customers also bought links only after interaction: all product templates, root cause four, ticket: server-render related links as anchors. Offers missing on variants and stale sale prices: root cause six, ticket: always output offers from live data. And the Arabic template's currency: root cause six, ticket: currency from market configuration. Every row has a count, a cause and a fix. | The summary table<br>• Result + count<br>• Root cause<br>• Ticket | Summary table animates row by row with RC and ticket columns filling in. |
| 12 | 6:06 | 38 | Common mistakes. Treating valid in the Rich Results Test as correct. Checking one URL per template when the bug only appears on variants or sale items. And recommending markup for rich result types that no longer exist. Recap: check parity between raw and rendered, verify markup against visible facts on a realistic sample, confirm eligibility and trends, and write findings with counts. Try this now: run the price-check script on twenty of your own product or service pages, including at least five on sale or with variants. | Mistakes, recap, try this now<br>• Valid ≠ correct; sample variants<br>• No retired features<br>• Run the price check on 20 pages | Mistake chips; recap ticks; a 20-row output table with a couple of mismatches. End card: 'Next module: triage and prioritisation'. |

<details><summary>Narration per scene (paste into ElevenLabs)</summary>

Scene 1 — Rendering and structured data checks

```text
Server-side rendering is a big improvement, but it isn't a guarantee. Components can still fetch content on the client. Hydration can change canonicals. And structured data can quietly drift away from the price on the page. In this lecture you'll run three checks on Kiran Home's key templates: raw versus rendered parity, JSON-LD versus visible price and availability, and eligibility in Google's tools, and you'll turn the results into hard numbers for your root causes.
```

Scene 2 — Check 1: raw vs rendered

```text
Check one: raw versus rendered parity. Take twenty URLs per template, product, category and journal, and compare the raw crawl with the rendered crawl, or use the Playwright diff script from the Technical SEO Mastery course. Compare the canonical, the robots meta, the main content, the internal links and the JSON-LD. Anything that differs between raw and rendered is a risk, and anything that exists only after interaction effectively doesn't exist for crawlers.
```

Scene 3 — Kiran Home findings (illustrative)

```text
For Kiran Home, the illustrative results bring two new findings. Journal posts have a canonical that changes after hydration: the server sends one market's canonical, then a locale cookie switches it on the client. That feeds root cause one. And on product pages, the customers also bought links only exist after interaction, so they provide no discovery value. Category pages confirm what we already knew: only the first twenty-four products and no pagination links in the raw HTML.
```

Scene 4 — Check 2: JSON-LD vs visible

```text
Check two: structured data versus visible facts. The script in the lesson text fetches each product URL, reads the visible price from the page's price element, then parses every JSON-LD block, handling graphs and lists, and finds the Product offer's price, currency and availability. It flags invalid JSON-LD, missing Product markup, and price mismatches. Run it on a sample that includes variants and sale items, because that's where bugs hide.
```

Scene 5 — Results (illustrative)

```text
Kiran Home's illustrative results: of sixty sampled products, eighteen have no offers at all, because the template omits offers when a product has variants. And five show a sale price on the page while the JSON-LD still has the old price. That's root cause six with hard numbers, and it's exactly the kind of inconsistency that also leads AI assistants to quote wrong prices.
```

Scene 6 — Check 3: eligibility

```text
Check three: eligibility and reports. Run one URL per template through the Rich Results Test, using its rendered view, and note errors versus warnings. Errors block eligibility; warnings are optional properties. Check Search Console's product and merchant listing reports and the breadcrumbs report for trend changes since the relaunch. And don't recommend markup for features that no longer exist. FAQ rich results ended in May twenty twenty-six, so existing FAQ markup is harmless, but it's not a recommendation.
```

Scene 7 — Valid ≠ correct

```text
Here's a subtle point worth remembering. Valid isn't the same as correct. The Rich Results Test tells you whether markup is structurally valid and eligible. It doesn't tell you whether the price is right, or whether two blocks contradict each other. That's why check two exists. Your job as an auditor is to verify facts, not just syntax.
```

Scene 8 — Example 2: three plugins, three sets of hours (illustrative)

```text
Worked example two, a different client, illustrative. An Abu Dhabi clinic group has three plugins each outputting clinic markup with different opening hours. The Rich Results Test shows no errors, because all three blocks are valid. But the facts conflict. So the finding isn't invalid markup. It's conflicting facts. The fix: keep one source, generated from the same fields as the visible opening hours, and align the Business Profile hours too.
```

Scene 9 — Write it up

```text
How do you write these up? Each check produces a finding with a count, examples and a root-cause link. Journal canonical changes after hydration: twenty of twenty sampled journal URLs, linked to root cause one. Offers missing on variant products: eighteen of sixty, linked to root cause six. Stale sale prices: five of sixty, also root cause six. Customers also bought links only after interaction: a discovery finding linked to root cause four. Numbers and examples make tickets easy to prioritise.
```

Scene 10 — Check 4: market parity

```text
Because Kiran Home serves four markets, there's a fourth check: market parity. Take one product in each market folder and confirm that the canonical is its own URL, the hreflang set lists all four markets plus x-default, the visible currency matches the market, whether that's rupees, dirhams or pounds, and the JSON-LD price currency matches the visible currency. On the relaunched storefront, illustratively, the Arabic UAE template shows prices in dirhams but its JSON-LD says pounds, inherited from the UK template. Another example for root cause six, and a very easy ticket to write.
```

Scene 11 — The summary table

```text
Let's pull the checks into one summary table, because that's what goes into the workbook. Journal canonicals changing after hydration: twenty out of twenty sampled, root cause one, ticket: render the canonical from the URL path on the server. Customers also bought links only after interaction: all product templates, root cause four, ticket: server-render related links as anchors. Offers missing on variants and stale sale prices: root cause six, ticket: always output offers from live data. And the Arabic template's currency: root cause six, ticket: currency from market configuration. Every row has a count, a cause and a fix.
```

Scene 12 — Mistakes, recap, try this now

```text
Common mistakes. Treating valid in the Rich Results Test as correct. Checking one URL per template when the bug only appears on variants or sale items. And recommending markup for rich result types that no longer exist. Recap: check parity between raw and rendered, verify markup against visible facts on a realistic sample, confirm eligibility and trends, and write findings with counts. Try this now: run the price-check script on twenty of your own product or service pages, including at least five on sale or with variants.
```

</details>

### 7. From findings to root causes

Lesson `from-findings-to-root-causes` · module “Triage and prioritisation” · target 7 min · 12 scenes · 953 words · planned 6:56 (words ≈ 6:54) · status: **to produce**

| Term | Say |
|---|---|
| RC | R C |
| YAML | yam-ul |
| H1 | H one |

| # | Time | Sec | Narration | On screen | Visual |
|---|---|---|---|---|---|
| 1 | 0:00 | 32 | Your Kiran Home workbook now holds about forty findings. If you hand those to the developers as forty tickets, most will never be done, because many are symptoms of the same problem, and several touch the same component. In this lecture you'll group findings into root causes using the five whys, label each cause by type, estimate its scope, weed out false positives, and tie everything back to the hypotheses you wrote before crawling. | From findings to root causes<br>• 40 findings → a handful of causes<br>• Five whys, types and scope<br>• False positives and hypotheses | Forty small finding cards swirl, then cluster into seven larger root-cause cards. Title appears. |
| 2 | 0:32 | 22 | Why group? Because developers and managers act on causes, not symptoms. Imagine a leaking roof. You could put forty buckets under forty drips, or fix three broken tiles. Grouping turns forty issues into seven problems with clear fixes, and it makes your audit dramatically easier to implement and to verify. | Why group?<br>• People act on causes<br>• 40 buckets vs 3 tiles<br>• Easier to implement and verify | Leaking roof illustration: many buckets on the floor, then the camera pans up to three broken tiles being replaced and the drips stop. |
| 3 | 0:54 | 40 | The tool for this is the five whys, applied to SEO. Take the finding, three thousand one hundred duplicate title tags. Why? Because facet URLs reuse the category title. Why are facet URLs crawled? Because filters are links with parameters and nothing blocks them. Why are they links? Because the new filter component outputs anchor links for every combination. Why wasn't that caught? Because no SEO requirements were specified for the component. Why? Because the relaunch had no SEO acceptance criteria. Two causes emerge: facets crawlable by default, and no SEO release checks. | The five whys<br>• Duplicate titles → facet URLs<br>• → crawlable filter links → component spec<br>• → no SEO acceptance criteria | A vertical ladder of five 'Why?' steps animates downward, ending in two root-cause cards: 'Facets crawlable' and 'No release checks'. |
| 4 | 1:34 | 37 | Here's Kiran Home's root-cause map, illustratively. RC one: cross-market canonicals on product and journal templates. RC two: facets and internal search crawlable and indexable. RC three: incomplete relaunch redirects. RC four: category pagination not crawlable. RC five: product template performance. RC six: product structured data incomplete. RC seven: no SEO release checks. And a new one from hypothesis five, RC eight: CDN bot rules challenging Bingbot and AI search bots on product URLs. Each cause lists the findings and evidence IDs that support it. | Kiran Home root causes<br>• RC1 canonicals · RC2 facets/search · RC3 redirects<br>• RC4 pagination · RC5 performance · RC6 schema<br>• RC7 process · RC8 CDN bot rules | Root-cause map table builds row by row; symptom IDs appear as small chips in each row. |
| 5 | 2:11 | 30 | Label each root cause by type, because the type changes who owns it and how it's fixed. Configuration: a setting or rule, like robots.txt, canonical logic or the redirect manager. Template or code: component behaviour, like filters, pagination or JSON-LD output. Content: thin or duplicate copy, owned by marketing. Infrastructure: server response, CDN and caching, like RC eight. And process: missing checks, ownership and documentation, like RC seven. | Label by type<br>• Configuration · Template/code · Content<br>• Infrastructure · Process<br>• Type → owner and fix path | Five type badges; each root-cause card receives its badge and an owner icon. |
| 6 | 2:41 | 37 | Estimate the scope of each cause with your data: the number of URLs and templates affected, the share of revenue or organic traffic flowing through them, from GA4 and Search Console, and which markets are affected. RC one affects every non-UK product and journal URL, in the three markets the founder said drive most revenue. A very large scope. RC six affects all products, but its impact is limited to rich result presentation and data accuracy. Scope feeds straight into prioritisation in the next lesson. | Estimate scope<br>• URLs and templates<br>• Share of revenue/traffic<br>• Markets affected | Two root-cause cards with scope gauges; RC1's gauge fills much higher than RC6's. |
| 7 | 3:18 | 32 | Watch for false positives. Not every crawler warning is a problem. Missing meta descriptions on paginated category pages: low value, and Google often rewrites snippets anyway. Multiple H1s: allowed in HTML, and not meaningful if the page is clear. Low word count on a contact page: expected. Canonicalised UTM variants: working as designed. List notable non-issues explicitly in the report, so stakeholders don't panic when a tool shows the same alerts next month. | False positives<br>• Missing meta descriptions on pagination<br>• Multiple H1s; short contact pages<br>• Canonicalised UTM variants | Warning cards drop into a 'Notable non-issues' tray with a calm grey colour. |
| 8 | 3:50 | 35 | Then tie back to your hypotheses. Redirect gaps: confirmed, partially, by RC three. Rendering and indexability regressions: confirmed, by RC one and RC four. International signals broke: confirmed, by RC one. Mobile performance regression: confirmed on product and category templates, RC five. Bot access changed: confirmed for Bingbot and AI search bots, RC eight. And the alternative, an algorithm update: not supported, because no confirmed update aligned with the drop, and the drop is concentrated in affected templates and markets. | Hypothesis verdicts<br>• H1–H5 confirmed (to varying degrees)<br>• Alternative: algorithm update — not supported<br>• Evidence cited for each | The hypothesis board from lesson 1 returns; verdict boxes fill with 'Confirmed' or 'Not supported' stamps and RC references. |
| 9 | 4:25 | 29 | Hands-on. Keep the root-cause map as data, in a small YAML file: for each cause, a title, type, symptom IDs, hypotheses and scope. A few lines of Python then print the table for your report, and the same file can generate ticket links later. It sounds like overkill, until the client asks for the fifth time which findings sit under which cause, and you answer in seconds. | Hands-on: root causes as data<br>• YAML: title, type, symptoms, hypotheses, scope<br>• Python prints the report table<br>• Reuse for tickets | YAML file with RC1 and RC8 entries; a terminal prints a neat table from it. |
| 10 | 4:54 | 36 | Worked example two, a different client, illustrative. A Riyadh furniture retailer receives a tool report with twelve critical issues: duplicate titles, duplicate descriptions, canonicalised URLs in sitemaps, thin pages, depth warnings and more. Grouping shows ten of the twelve trace to one cause: a colour-swatch component that generates a crawlable URL per colour for every product. One component fix plus a robots rule addresses ten warnings at once. The remaining two, missing alt text and slow home-page LCP, become separate, smaller tickets. | Example 2: one cause, ten warnings (illustrative)<br>• Colour-swatch URLs per product<br>• One component fix + robots rule<br>• Two separate small tickets remain | Twelve warning chips; ten fly into one 'swatch component' cause card; two stay separate. |
| 11 | 5:30 | 51 | Let's look more closely at RC eight, because it's a good example of how a hypothesis turns into a root cause. The CDN firewall events showed challenge pages served to requests from verified Bingbot IPs and to OpenAI's and Perplexity's search bots on product URLs, starting the week of the relaunch. Bing Webmaster Tools showed product pages dropping out of Bing's index over the following weeks. Googlebot wasn't affected, because the CDN had a separate rule for known search engines that only covered Google. So the cause is infrastructure, the owner is the developer who manages Cloudflare, and the fix is a rule change aligned with the written crawler policy. Three pieces of evidence, one clear cause. | From H5 to RC8<br>• CDN events: challenges for Bingbot + AI search bots<br>• Bing index drop for products<br>• Google unaffected → infrastructure cause | Evidence chips E-12 and E-13 slide into an 'RC8' card; a CDN rule panel shows a Google-only allow rule, with other bots hitting a challenge page. |
| 12 | 6:21 | 35 | Common mistakes. Handing developers forty symptom tickets that all touch the same component. Omitting process causes, so the next release reintroduces the problems. And presenting tool warnings as issues without judging their relevance. Recap: group with the five whys, label by type, estimate scope, list non-issues, and give every hypothesis a verdict. Try this now: take your last audit or tool report, and group its issues into no more than seven root causes, each with a type and an owner. | Mistakes, recap, try this now<br>• Symptoms ≠ tickets; include process causes<br>• Judge relevance<br>• Group your last report into ≤ 7 causes | Mistake chips; recap ticks; a messy issue list collapses into seven labelled cause cards. End card: 'Next: prioritising with impact × effort × confidence'. |

<details><summary>Narration per scene (paste into ElevenLabs)</summary>

Scene 1 — From findings to root causes

```text
Your Kiran Home workbook now holds about forty findings. If you hand those to the developers as forty tickets, most will never be done, because many are symptoms of the same problem, and several touch the same component. In this lecture you'll group findings into root causes using the five whys, label each cause by type, estimate its scope, weed out false positives, and tie everything back to the hypotheses you wrote before crawling.
```

Scene 2 — Why group?

```text
Why group? Because developers and managers act on causes, not symptoms. Imagine a leaking roof. You could put forty buckets under forty drips, or fix three broken tiles. Grouping turns forty issues into seven problems with clear fixes, and it makes your audit dramatically easier to implement and to verify.
```

Scene 3 — The five whys

```text
The tool for this is the five whys, applied to SEO. Take the finding, three thousand one hundred duplicate title tags. Why? Because facet URLs reuse the category title. Why are facet URLs crawled? Because filters are links with parameters and nothing blocks them. Why are they links? Because the new filter component outputs anchor links for every combination. Why wasn't that caught? Because no SEO requirements were specified for the component. Why? Because the relaunch had no SEO acceptance criteria. Two causes emerge: facets crawlable by default, and no SEO release checks.
```

Scene 4 — Kiran Home root causes

```text
Here's Kiran Home's root-cause map, illustratively. RC one: cross-market canonicals on product and journal templates. RC two: facets and internal search crawlable and indexable. RC three: incomplete relaunch redirects. RC four: category pagination not crawlable. RC five: product template performance. RC six: product structured data incomplete. RC seven: no SEO release checks. And a new one from hypothesis five, RC eight: CDN bot rules challenging Bingbot and AI search bots on product URLs. Each cause lists the findings and evidence IDs that support it.
```

Scene 5 — Label by type

```text
Label each root cause by type, because the type changes who owns it and how it's fixed. Configuration: a setting or rule, like robots.txt, canonical logic or the redirect manager. Template or code: component behaviour, like filters, pagination or JSON-LD output. Content: thin or duplicate copy, owned by marketing. Infrastructure: server response, CDN and caching, like RC eight. And process: missing checks, ownership and documentation, like RC seven.
```

Scene 6 — Estimate scope

```text
Estimate the scope of each cause with your data: the number of URLs and templates affected, the share of revenue or organic traffic flowing through them, from GA4 and Search Console, and which markets are affected. RC one affects every non-UK product and journal URL, in the three markets the founder said drive most revenue. A very large scope. RC six affects all products, but its impact is limited to rich result presentation and data accuracy. Scope feeds straight into prioritisation in the next lesson.
```

Scene 7 — False positives

```text
Watch for false positives. Not every crawler warning is a problem. Missing meta descriptions on paginated category pages: low value, and Google often rewrites snippets anyway. Multiple H1s: allowed in HTML, and not meaningful if the page is clear. Low word count on a contact page: expected. Canonicalised UTM variants: working as designed. List notable non-issues explicitly in the report, so stakeholders don't panic when a tool shows the same alerts next month.
```

Scene 8 — Hypothesis verdicts

```text
Then tie back to your hypotheses. Redirect gaps: confirmed, partially, by RC three. Rendering and indexability regressions: confirmed, by RC one and RC four. International signals broke: confirmed, by RC one. Mobile performance regression: confirmed on product and category templates, RC five. Bot access changed: confirmed for Bingbot and AI search bots, RC eight. And the alternative, an algorithm update: not supported, because no confirmed update aligned with the drop, and the drop is concentrated in affected templates and markets.
```

Scene 9 — Hands-on: root causes as data

```text
Hands-on. Keep the root-cause map as data, in a small YAML file: for each cause, a title, type, symptom IDs, hypotheses and scope. A few lines of Python then print the table for your report, and the same file can generate ticket links later. It sounds like overkill, until the client asks for the fifth time which findings sit under which cause, and you answer in seconds.
```

Scene 10 — Example 2: one cause, ten warnings (illustrative)

```text
Worked example two, a different client, illustrative. A Riyadh furniture retailer receives a tool report with twelve critical issues: duplicate titles, duplicate descriptions, canonicalised URLs in sitemaps, thin pages, depth warnings and more. Grouping shows ten of the twelve trace to one cause: a colour-swatch component that generates a crawlable URL per colour for every product. One component fix plus a robots rule addresses ten warnings at once. The remaining two, missing alt text and slow home-page LCP, become separate, smaller tickets.
```

Scene 11 — From H5 to RC8

```text
Let's look more closely at RC eight, because it's a good example of how a hypothesis turns into a root cause. The CDN firewall events showed challenge pages served to requests from verified Bingbot IPs and to OpenAI's and Perplexity's search bots on product URLs, starting the week of the relaunch. Bing Webmaster Tools showed product pages dropping out of Bing's index over the following weeks. Googlebot wasn't affected, because the CDN had a separate rule for known search engines that only covered Google. So the cause is infrastructure, the owner is the developer who manages Cloudflare, and the fix is a rule change aligned with the written crawler policy. Three pieces of evidence, one clear cause.
```

Scene 12 — Mistakes, recap, try this now

```text
Common mistakes. Handing developers forty symptom tickets that all touch the same component. Omitting process causes, so the next release reintroduces the problems. And presenting tool warnings as issues without judging their relevance. Recap: group with the five whys, label by type, estimate scope, list non-issues, and give every hypothesis a verdict. Try this now: take your last audit or tool report, and group its issues into no more than seven root causes, each with a type and an owner.
```

</details>

### 8. Prioritising with impact × effort × confidence

Lesson `prioritising-impact-effort-confidence` · module “Triage and prioritisation” · target 6 min · 12 scenes · 910 words · planned 6:35 (words ≈ 6:36) · status: **to produce**

| Term | Say |
|---|---|
| RC | R C |
| Gantt | gant |

| # | Time | Sec | Narration | On screen | Visual |
|---|---|---|---|---|---|
| 1 | 0:00 | 31 | Every stakeholder has a favourite issue. The founder wants the site faster. The developer wants to refactor the filter component. The marketing manager wants rich results back. Without a shared model, prioritisation becomes whoever argues loudest. In this lecture you'll score Kiran Home's root causes with a transparent impact, effort and confidence model, stress-test the ranking, sequence the work into sprints, and learn how to communicate expected outcomes without promising traffic numbers. | Prioritising: impact × effort × confidence<br>• A shared, transparent model<br>• Score, stress-test, sequence<br>• Communicate outcomes honestly | Three stakeholder silhouettes each holding a different sign; a scoring matrix slides between them and they turn to face it. Title appears. |
| 2 | 0:31 | 38 | The model scores each root cause from one to five on three dimensions. Impact: how much organic performance or business value is at stake, considering revenue templates, severity and markets. Effort, scored inverted, so five means trivial and one means a major project. And confidence: how sure you are that fixing it produces the benefit. High when evidence is direct, like a wrong Google-selected canonical. Lower when it's inferred. The priority score is impact times inverted effort times confidence, with a maximum of a hundred and twenty-five. | The model<br>• Impact 1–5<br>• Effort (inverted) 1–5<br>• Confidence 1–5 → score ≤ 125 | Three dials labelled Impact, Effort (inverted), Confidence multiply into a score counter. |
| 3 | 1:09 | 33 | Here's how the scale reads. A five for impact blocks indexing of key revenue templates. A three affects discovery or rich results on important templates. A one is cosmetic. A five for inverted effort is a configuration change under a day; a three is a sprint of work; a one is a multi-sprint rebuild. A five for confidence is direct evidence plus documented Google behaviour; a three is strong but indirect evidence; a one is speculative. | The scale<br>• Impact: blocks indexing (5) → cosmetic (1)<br>• Effort: < 1 day (5) → rebuild (1)<br>• Confidence: direct (5) → speculative (1) | Scale table with anchors for 5, 3 and 1 in each column. |
| 4 | 1:42 | 35 | Scoring Kiran Home, illustratively. RC one, cross-market canonicals: impact five, effort four, confidence five, score one hundred. It's one template change that directly explains the non-UK losses. RC eight, the CDN bot rules: three, five, four, sixty, because it's one rule change. RC three, redirects: sixty-four. RC two, facets: forty-eight. RC seven, release checks: forty-eight. RC four, pagination, and RC six, structured data: thirty-six each. And RC five, performance: eighteen, because it's a third-party widget replacement with an uncertain ranking effect. | Kiran Home scores (illustrative)<br>• RC1 100 · RC3 64 · RC8 60<br>• RC2 48 · RC7 48 · RC4 36 · RC6 36<br>• RC5 18 | Scoring table fills row by row; the rows re-sort by score with a smooth animation. |
| 5 | 2:17 | 28 | Notice RC five. It scores lowest on SEO grounds, but performance often affects conversion rate, so the business may still schedule it early. Say that explicitly and let the business weigh it. The model is a decision aid, not a decision machine. Its job is to make trade-offs visible, so that if someone later asks why something wasn't done first, the reasoning is written down. | The model supports decisions<br>• RC5 low on SEO, high on conversion?<br>• Make trade-offs visible<br>• Document the reasoning | RC5 card sits low in the list; a side note 'conversion impact' lifts it with a dotted arrow labelled 'business decision'. |
| 6 | 2:45 | 34 | Hands-on. The lesson text has a pandas script that scores and ranks the root causes, then stress-tests the result. For every root cause and every dimension, it nudges the score up or down by one point and checks whether the top three changes. If a one-point change flips the top of the list, you say so in the report. It's a simple way to show that your ranking is robust, or to be honest that it's a close call. | Hands-on: score + stress-test<br>• Score and rank in pandas<br>• ±1 on every input<br>• Report if the top 3 is sensitive | Code walkthrough; a sensitivity heatmap shows which cells flip the top three when nudged. |
| 7 | 3:19 | 41 | Scores give an order. Dependencies and risk shape the plan. Quick wins with high impact go first: RC one, RC eight and the top slice of RC three, meaning legacy URLs with backlinks or historic traffic. Sequence dependent changes: for facets, stop linking and add robots rules only after confirming none are URLs you want indexed, and if some are indexed and should go, noindex first, then block. Bundle work by component: RC four and RC two both touch the category and filter component, so ship them together. And fix process early with RC seven. | Sequence, don't just rank<br>• Quick high-impact wins first<br>• Respect dependencies (noindex → block)<br>• Bundle by component; process early | A roadmap Gantt builds with three sprints; RC2 and RC4 share a bar labelled 'category component'. |
| 8 | 4:00 | 36 | Communicating uncertainty. Don't forecast precise traffic gains. Frame expected outcomes as observable leading indicators. For RC one: non-UK product URLs move from alternate page with proper canonical tag to indexed, with Google-selected canonicals matching yours. For RC two: the share of Googlebot requests on facet URLs falls in the logs. For RC three: legacy four-oh-fours with links drop to near zero. For RC eight: Bingbot and AI search bots get two hundreds on product URLs. Leading indicators are what you can commit to. | Communicate outcomes<br>• No precise traffic forecasts<br>• Leading indicators per root cause<br>• Commit to what you can observe | Four indicator tiles, each with a small before/after arrow and an RC label. |
| 9 | 4:36 | 32 | Worked example one: that's Kiran Home. Worked example two, a different client, illustrative. A Dubai SaaS company has three root causes: a slow, client-rendered pricing page, orphaned integration pages, and missing Organization markup. Scoring puts the orphaned pages first, a quick win with direct evidence of lost impressions. But the team schedules the pricing page in parallel, because it also affects paid-traffic conversion. The model made the trade-off explicit rather than hiding it. | Example 2: Dubai SaaS (illustrative)<br>• Orphaned pages score highest<br>• Pricing page scheduled in parallel (conversion)<br>• Trade-off made explicit | Three cause cards scored; two move into the current sprint side by side with notes 'SEO quick win' and 'conversion'. |
| 10 | 5:08 | 18 | Common mistakes. Prioritising by crawler severity labels instead of business impact. Ignoring effort, so the roadmap starts with a six-month rebuild. Promising specific traffic recovery numbers. And treating the score as the final decision instead of a starting point for discussion. | Common mistakes<br>• Tool severity ≠ business impact<br>• Ignoring effort<br>• Traffic promises; score as dictator | Four mistake chips fall into a bin. |
| 11 | 5:26 | 43 | How do you score confidence honestly? Ask what kind of evidence you have. Direct evidence plus documented behaviour, like URL Inspection showing Google chose the UK canonical, plus Google's documentation on canonicals, earns a five. Strong but indirect evidence, like logs showing uncrawled deep products plus a missing pagination path, earns a three or four. Speculation, like we think faster pages will rank better, earns a one or two. Being honest here protects your credibility. When you tell a founder a fix is high-confidence, it should be, and when it isn't, you should say so before they ask. | Scoring confidence honestly<br>• Direct evidence + docs → 5<br>• Strong but indirect → 3–4<br>• Speculative → 1–2 | Three evidence cards of decreasing strength each map to a confidence dial position. |
| 12 | 6:09 | 26 | Recap. Score impact, inverted effort and confidence from one to five, multiply, rank, and stress-test. Then sequence by dependencies and components, fix process early, and communicate leading indicators instead of traffic promises. Try this now. Score your own top five root causes with the model, run the stress test, and draft a three-sprint roadmap with one leading indicator per item. | Recap and try this now<br>• Score 5 root causes<br>• Stress-test<br>• 3-sprint roadmap + indicators | A scored table becomes a three-sprint roadmap with indicator tags. End card: 'Next module: writing tickets developers act on'. |

<details><summary>Narration per scene (paste into ElevenLabs)</summary>

Scene 1 — Prioritising: impact × effort × confidence

```text
Every stakeholder has a favourite issue. The founder wants the site faster. The developer wants to refactor the filter component. The marketing manager wants rich results back. Without a shared model, prioritisation becomes whoever argues loudest. In this lecture you'll score Kiran Home's root causes with a transparent impact, effort and confidence model, stress-test the ranking, sequence the work into sprints, and learn how to communicate expected outcomes without promising traffic numbers.
```

Scene 2 — The model

```text
The model scores each root cause from one to five on three dimensions. Impact: how much organic performance or business value is at stake, considering revenue templates, severity and markets. Effort, scored inverted, so five means trivial and one means a major project. And confidence: how sure you are that fixing it produces the benefit. High when evidence is direct, like a wrong Google-selected canonical. Lower when it's inferred. The priority score is impact times inverted effort times confidence, with a maximum of a hundred and twenty-five.
```

Scene 3 — The scale

```text
Here's how the scale reads. A five for impact blocks indexing of key revenue templates. A three affects discovery or rich results on important templates. A one is cosmetic. A five for inverted effort is a configuration change under a day; a three is a sprint of work; a one is a multi-sprint rebuild. A five for confidence is direct evidence plus documented Google behaviour; a three is strong but indirect evidence; a one is speculative.
```

Scene 4 — Kiran Home scores (illustrative)

```text
Scoring Kiran Home, illustratively. RC one, cross-market canonicals: impact five, effort four, confidence five, score one hundred. It's one template change that directly explains the non-UK losses. RC eight, the CDN bot rules: three, five, four, sixty, because it's one rule change. RC three, redirects: sixty-four. RC two, facets: forty-eight. RC seven, release checks: forty-eight. RC four, pagination, and RC six, structured data: thirty-six each. And RC five, performance: eighteen, because it's a third-party widget replacement with an uncertain ranking effect.
```

Scene 5 — The model supports decisions

```text
Notice RC five. It scores lowest on SEO grounds, but performance often affects conversion rate, so the business may still schedule it early. Say that explicitly and let the business weigh it. The model is a decision aid, not a decision machine. Its job is to make trade-offs visible, so that if someone later asks why something wasn't done first, the reasoning is written down.
```

Scene 6 — Hands-on: score + stress-test

```text
Hands-on. The lesson text has a pandas script that scores and ranks the root causes, then stress-tests the result. For every root cause and every dimension, it nudges the score up or down by one point and checks whether the top three changes. If a one-point change flips the top of the list, you say so in the report. It's a simple way to show that your ranking is robust, or to be honest that it's a close call.
```

Scene 7 — Sequence, don't just rank

```text
Scores give an order. Dependencies and risk shape the plan. Quick wins with high impact go first: RC one, RC eight and the top slice of RC three, meaning legacy URLs with backlinks or historic traffic. Sequence dependent changes: for facets, stop linking and add robots rules only after confirming none are URLs you want indexed, and if some are indexed and should go, noindex first, then block. Bundle work by component: RC four and RC two both touch the category and filter component, so ship them together. And fix process early with RC seven.
```

Scene 8 — Communicate outcomes

```text
Communicating uncertainty. Don't forecast precise traffic gains. Frame expected outcomes as observable leading indicators. For RC one: non-UK product URLs move from alternate page with proper canonical tag to indexed, with Google-selected canonicals matching yours. For RC two: the share of Googlebot requests on facet URLs falls in the logs. For RC three: legacy four-oh-fours with links drop to near zero. For RC eight: Bingbot and AI search bots get two hundreds on product URLs. Leading indicators are what you can commit to.
```

Scene 9 — Example 2: Dubai SaaS (illustrative)

```text
Worked example one: that's Kiran Home. Worked example two, a different client, illustrative. A Dubai SaaS company has three root causes: a slow, client-rendered pricing page, orphaned integration pages, and missing Organization markup. Scoring puts the orphaned pages first, a quick win with direct evidence of lost impressions. But the team schedules the pricing page in parallel, because it also affects paid-traffic conversion. The model made the trade-off explicit rather than hiding it.
```

Scene 10 — Common mistakes

```text
Common mistakes. Prioritising by crawler severity labels instead of business impact. Ignoring effort, so the roadmap starts with a six-month rebuild. Promising specific traffic recovery numbers. And treating the score as the final decision instead of a starting point for discussion.
```

Scene 11 — Scoring confidence honestly

```text
How do you score confidence honestly? Ask what kind of evidence you have. Direct evidence plus documented behaviour, like URL Inspection showing Google chose the UK canonical, plus Google's documentation on canonicals, earns a five. Strong but indirect evidence, like logs showing uncrawled deep products plus a missing pagination path, earns a three or four. Speculation, like we think faster pages will rank better, earns a one or two. Being honest here protects your credibility. When you tell a founder a fix is high-confidence, it should be, and when it isn't, you should say so before they ask.
```

Scene 12 — Recap and try this now

```text
Recap. Score impact, inverted effort and confidence from one to five, multiply, rank, and stress-test. Then sequence by dependencies and components, fix process early, and communicate leading indicators instead of traffic promises. Try this now. Score your own top five root causes with the model, run the stress test, and draft a three-sprint roadmap with one leading indicator per item.
```

</details>

### 9. Anatomy of a great SEO ticket

Lesson `anatomy-of-a-great-seo-ticket` · module “Writing tickets developers act on” · target 7 min · 12 scenes · 956 words · planned 6:53 (words ≈ 6:56) · status: **to produce**

| Term | Say |
|---|---|
| Gherkin | gur-kin |
| QA | Q A |
| hreflang | H ref lang |
| x-default | X default |

| # | Time | Sec | Narration | On screen | Visual |
|---|---|---|---|---|---|
| 1 | 0:00 | 31 | Most SEO recommendations are never implemented. Not because developers don't care, but because they receive requests like fix canonicals or improve site speed. You can't estimate that, test it or close it. In this lecture you'll learn the anatomy of a ticket developers actually act on: a template, worked tickets for Kiran Home's biggest root causes, how to write binary acceptance criteria, including Given-When-Then scenarios, and how to size and split work. | Anatomy of a great SEO ticket<br>• Why vague tickets stall<br>• A template and worked tickets<br>• Binary acceptance criteria | A sticky note 'Fix canonicals' fades; a structured ticket card assembles with labelled sections. Title appears. |
| 2 | 0:31 | 39 | A good ticket reads like a small specification. What's wrong, where, why it matters, exactly what should change, and how to prove it's done. The template in the lesson text has a title in the form SEO, component, specific change. Type and priority from your roadmap score. A one or two sentence summary a non-SEO can understand. Business impact. Current behaviour with two to five example URLs. Expected behaviour, including edge cases. Acceptance criteria. How to test. Out of scope. Dependencies and risks. And references to documentation and evidence IDs. | The ticket template<br>• Title, priority, summary, impact<br>• Current vs expected (with examples)<br>• Acceptance, testing, scope, risks, references | Ticket template sections light up one by one as they're named. |
| 3 | 1:10 | 40 | Let's look at ticket one, for root cause one. Title: product and journal templates, canonical must self-reference the current market URL. Summary: pages in the Pakistan and UAE folders set their canonical to the UK equivalent, so Google treats them as duplicates and shows UK pages with pound prices. Current behaviour, with an example URL. Expected behaviour: the canonical equals the page's own absolute URL, https, www, current market folder, no query string, lowercase, and the site's trailing slash rule. Parameter variants canonicalise to the clean URL of the same market. hreflang unchanged. | Ticket 1: RC1 canonicals<br>• Clear summary for non-SEOs<br>• Current behaviour with example<br>• Precise expected rule incl. edge cases | Ticket document scrolls; the 'Expected behaviour' section highlights each rule as a checklist. |
| 4 | 1:50 | 37 | The acceptance criteria for ticket one are binary. For every product and journal URL in each market, the rendered HTML contains exactly one canonical link in the head, equal to its own clean URL. The canonical is present in the server-rendered raw HTML, not only after hydration. No canonical points to a URL that redirects or returns non-two-hundred. And an automated test is added to the template test suite. Then how to test: a curl command against staging, and a list crawl checking canonical equals address. | Ticket 1: acceptance criteria<br>• Exactly one canonical = own clean URL<br>• Present in raw HTML<br>• No non-200 targets; automated test | Four checkboxes tick; a terminal shows the curl + grep test returning the expected canonical. |
| 5 | 2:27 | 38 | Ticket two, for facets. Expected behaviour: filter options for size, price, sort and multi-select combinations update the listing without outputting crawlable anchor links; brand and colour filters on the approved list keep clean static URLs. Parameter order is normalised. Zero-result combinations return four-oh-four. And robots.txt adds disallow rules for the low-value parameters and search, but only after another ticket confirms no approved facets use them. Acceptance: no anchor hrefs with those parameters in the rendered HTML, approved facet pages self-canonical with unique titles, and zero-result combos returning four-oh-four. | Ticket 2: RC2 facets<br>• No crawlable links for low-value filters<br>• Normalised order; 404 for zero results<br>• robots.txt rules after dependency check | Filter component mock: size/price chips become buttons (no href); brand/colour chips remain links to static URLs. A dependency arrow points from ticket SEO-12 to the robots.txt step. |
| 6 | 3:05 | 41 | Ticket three, for the CDN bot rule, is a great place to use Given-When-Then scenarios, which QA teams can automate. Given a request from a verified bot's IP, and a user agent containing its token, when it requests a public product URL, then the response is two hundred and no challenge page is served. The examples table lists Googlebot, Bingbot, OAI-SearchBot and PerplexityBot. And a second scenario confirms protected paths like account and checkout stay protected. Pair it with the written AI crawler policy, so the rule is a business decision, not an SEO whim. | Ticket 3: RC8 in Gherkin<br>• Given verified bot → When public URL → Then 200<br>• Examples table of bots<br>• Protected paths stay protected | Gherkin scenario text animates; the examples table rows light up; a second scenario with a lock icon for /account/ and /checkout/. |
| 7 | 3:46 | 27 | What makes acceptance criteria good? They're binary, pass or fail, and checkable by a QA engineer without SEO knowledge. Good: returns HTTP four-oh-four for zero-result combinations. Bad: handle empty filters properly. Good: the LCP image on category templates has no loading lazy attribute and has fetchpriority high. Bad: improve LCP. If a tester can't tell whether it passed, it isn't an acceptance criterion. | Good acceptance criteria<br>• Binary, testable, no SEO knowledge needed<br>• 'Returns 404' ✓ vs 'handle properly' ✗<br>• 'No loading=lazy on LCP image' ✓ vs 'improve LCP' ✗ | Two columns 'Good' and 'Bad'; pairs of criteria slide in; the bad ones get a red cross. |
| 8 | 4:13 | 27 | Give developers the why, briefly. Link to Google's documentation on canonicalisation, faceted navigation or redirects, and keep the explanation short. Developers respect specificity; long SEO essays inside tickets get skipped. And size and split. If a ticket would take more than a sprint, split it by template or by market. Tickets that ship independently deliver value sooner and are easier to roll back. | Why, briefly; size and split<br>• Link docs; keep it short<br>• > 1 sprint → split by template/market<br>• Independent tickets ship sooner | A long essay block shrinks to two lines plus a doc link; a large ticket splits into three smaller ones labelled by market. |
| 9 | 4:40 | 39 | Worked example two, illustrative. A Lahore agency's junior writes a ticket: fix hreflang issues on the site. The developer asks five questions and parks it. Rewritten, it becomes: product template, hreflang set must be complete and reciprocal. Three example URLs with current output. Expected behaviour listing the four market codes plus x-default, absolute URLs, self-reference, and targets that return two hundred and are self-canonical. Acceptance criteria as a checklist plus a crawl-based test. Out of scope: journal templates, in a separate ticket. It was estimated and shipped the next sprint. | Example 2: rewriting a vague ticket (illustrative)<br>• 'Fix hreflang issues' → parked<br>• Rewritten with examples, rules, criteria, scope<br>• Shipped next sprint | Before/after ticket cards; the after card has all template sections filled and a 'Done' label. |
| 10 | 5:19 | 25 | Common mistakes. Tickets without example URLs. Expected behaviour that ignores edge cases like parameters, trailing slashes, uppercase and pagination. Mixing several unrelated fixes into one ticket. And not specifying whether a tag must be in the raw HTML or can be added client-side. Each of these leads to questions, delays or a fix that doesn't work for Google. | Common mistakes<br>• No examples<br>• Missing edge cases<br>• Mixed fixes; raw vs client-side unspecified | Mistake chips with small question-mark bubbles popping from a developer icon. |
| 11 | 5:44 | 41 | A word on where the tag must live, because it's the most common source of back-and-forth. For anything search engines must see reliably, like canonicals, robots meta, hreflang and structured data, specify that it must be in the server-rendered raw HTML. Client-side injection adds rendering risk and can create raw versus rendered mismatches, and remember that a noindex in raw HTML may stop Google rendering at all. Say it explicitly in the expected behaviour and in the acceptance criteria. Developers can't guess this requirement, and many frameworks make the client-side path the easy default. | Raw HTML or client-side?<br>• Canonical, robots, hreflang, JSON-LD → raw HTML<br>• Client-side adds risk and mismatches<br>• State it in expected behaviour + criteria | Two code paths: 'server render' leading to a green raw HTML panel, and 'client inject' leading to an amber rendered-only panel with a warning. |
| 12 | 6:25 | 28 | Recap. Write tickets as small specifications: specific title, plain-language summary, business impact, current versus expected behaviour with examples and edge cases, binary acceptance criteria, how to test, scope and risks. Use Given-When-Then when QA will automate. Try this now. Take the vaguest SEO request in your backlog and rewrite it with the template, including at least three example URLs and four binary acceptance criteria. | Recap and try this now<br>• Tickets = small specs<br>• Binary criteria; Gherkin for automation<br>• Rewrite your vaguest ticket | The vague sticky note from the start transforms into a complete ticket. End card: 'Next: working with development teams'. |

<details><summary>Narration per scene (paste into ElevenLabs)</summary>

Scene 1 — Anatomy of a great SEO ticket

```text
Most SEO recommendations are never implemented. Not because developers don't care, but because they receive requests like fix canonicals or improve site speed. You can't estimate that, test it or close it. In this lecture you'll learn the anatomy of a ticket developers actually act on: a template, worked tickets for Kiran Home's biggest root causes, how to write binary acceptance criteria, including Given-When-Then scenarios, and how to size and split work.
```

Scene 2 — The ticket template

```text
A good ticket reads like a small specification. What's wrong, where, why it matters, exactly what should change, and how to prove it's done. The template in the lesson text has a title in the form SEO, component, specific change. Type and priority from your roadmap score. A one or two sentence summary a non-SEO can understand. Business impact. Current behaviour with two to five example URLs. Expected behaviour, including edge cases. Acceptance criteria. How to test. Out of scope. Dependencies and risks. And references to documentation and evidence IDs.
```

Scene 3 — Ticket 1: RC1 canonicals

```text
Let's look at ticket one, for root cause one. Title: product and journal templates, canonical must self-reference the current market URL. Summary: pages in the Pakistan and UAE folders set their canonical to the UK equivalent, so Google treats them as duplicates and shows UK pages with pound prices. Current behaviour, with an example URL. Expected behaviour: the canonical equals the page's own absolute URL, https, www, current market folder, no query string, lowercase, and the site's trailing slash rule. Parameter variants canonicalise to the clean URL of the same market. hreflang unchanged.
```

Scene 4 — Ticket 1: acceptance criteria

```text
The acceptance criteria for ticket one are binary. For every product and journal URL in each market, the rendered HTML contains exactly one canonical link in the head, equal to its own clean URL. The canonical is present in the server-rendered raw HTML, not only after hydration. No canonical points to a URL that redirects or returns non-two-hundred. And an automated test is added to the template test suite. Then how to test: a curl command against staging, and a list crawl checking canonical equals address.
```

Scene 5 — Ticket 2: RC2 facets

```text
Ticket two, for facets. Expected behaviour: filter options for size, price, sort and multi-select combinations update the listing without outputting crawlable anchor links; brand and colour filters on the approved list keep clean static URLs. Parameter order is normalised. Zero-result combinations return four-oh-four. And robots.txt adds disallow rules for the low-value parameters and search, but only after another ticket confirms no approved facets use them. Acceptance: no anchor hrefs with those parameters in the rendered HTML, approved facet pages self-canonical with unique titles, and zero-result combos returning four-oh-four.
```

Scene 6 — Ticket 3: RC8 in Gherkin

```text
Ticket three, for the CDN bot rule, is a great place to use Given-When-Then scenarios, which QA teams can automate. Given a request from a verified bot's IP, and a user agent containing its token, when it requests a public product URL, then the response is two hundred and no challenge page is served. The examples table lists Googlebot, Bingbot, OAI-SearchBot and PerplexityBot. And a second scenario confirms protected paths like account and checkout stay protected. Pair it with the written AI crawler policy, so the rule is a business decision, not an SEO whim.
```

Scene 7 — Good acceptance criteria

```text
What makes acceptance criteria good? They're binary, pass or fail, and checkable by a QA engineer without SEO knowledge. Good: returns HTTP four-oh-four for zero-result combinations. Bad: handle empty filters properly. Good: the LCP image on category templates has no loading lazy attribute and has fetchpriority high. Bad: improve LCP. If a tester can't tell whether it passed, it isn't an acceptance criterion.
```

Scene 8 — Why, briefly; size and split

```text
Give developers the why, briefly. Link to Google's documentation on canonicalisation, faceted navigation or redirects, and keep the explanation short. Developers respect specificity; long SEO essays inside tickets get skipped. And size and split. If a ticket would take more than a sprint, split it by template or by market. Tickets that ship independently deliver value sooner and are easier to roll back.
```

Scene 9 — Example 2: rewriting a vague ticket (illustrative)

```text
Worked example two, illustrative. A Lahore agency's junior writes a ticket: fix hreflang issues on the site. The developer asks five questions and parks it. Rewritten, it becomes: product template, hreflang set must be complete and reciprocal. Three example URLs with current output. Expected behaviour listing the four market codes plus x-default, absolute URLs, self-reference, and targets that return two hundred and are self-canonical. Acceptance criteria as a checklist plus a crawl-based test. Out of scope: journal templates, in a separate ticket. It was estimated and shipped the next sprint.
```

Scene 10 — Common mistakes

```text
Common mistakes. Tickets without example URLs. Expected behaviour that ignores edge cases like parameters, trailing slashes, uppercase and pagination. Mixing several unrelated fixes into one ticket. And not specifying whether a tag must be in the raw HTML or can be added client-side. Each of these leads to questions, delays or a fix that doesn't work for Google.
```

Scene 11 — Raw HTML or client-side?

```text
A word on where the tag must live, because it's the most common source of back-and-forth. For anything search engines must see reliably, like canonicals, robots meta, hreflang and structured data, specify that it must be in the server-rendered raw HTML. Client-side injection adds rendering risk and can create raw versus rendered mismatches, and remember that a noindex in raw HTML may stop Google rendering at all. Say it explicitly in the expected behaviour and in the acceptance criteria. Developers can't guess this requirement, and many frameworks make the client-side path the easy default.
```

Scene 12 — Recap and try this now

```text
Recap. Write tickets as small specifications: specific title, plain-language summary, business impact, current versus expected behaviour with examples and edge cases, binary acceptance criteria, how to test, scope and risks. Use Given-When-Then when QA will automate. Try this now. Take the vaguest SEO request in your backlog and rewrite it with the template, including at least three example URLs and four binary acceptance criteria.
```

</details>

### 10. Working with development teams: sprints, QA and release risk

Lesson `working-with-development-teams` · module “Writing tickets developers act on” · target 7 min · 12 scenes · 934 words · planned 6:46 (words ≈ 6:46) · status: **to produce**

| Term | Say |
|---|---|
| Jira | jee-ra |
| CI | C I |
| pytest | pie-test |
| YAML | yam-ul |

| # | Time | Sec | Narration | On screen | Visual |
|---|---|---|---|---|---|
| 1 | 0:00 | 34 | Even perfect tickets fail if they arrive at the wrong time, to the wrong person, in the wrong format. SEO is a team sport, and developers are your most important teammates. In this lecture you'll learn how to fit into a development team's process, agree an SEO definition of done, automate checks in CI, run QA on staging, manage release risk, and handle pushback productively, all in the context of getting Kiran Home's two developers to ship the roadmap. | Working with development teams<br>• Fit their process<br>• Definition of done + automation<br>• Release risk and pushback | An SEO icon and two developer icons at a shared board; tickets move from 'To do' to 'Done'. Title appears. |
| 2 | 0:34 | 35 | Fit into their process, not yours. Use their tool, whether that's Jira, Linear, GitHub Issues or Trello; tickets in a PDF appendix aren't tickets. Attend sprint planning, or send a short brief before it, and explain the top three tickets in five minutes: problem, evidence, expected outcome. Respect estimation: let developers size the work, and if an estimate is bigger than you expected, ask what drives it, because there's often a simpler implementation. And offer alternatives when capacity is tight. | Fit their process<br>• Their tool, their ceremonies<br>• Five-minute brief: problem, evidence, outcome<br>• Respect estimates; offer alternatives | Kanban board in a generic issue tracker; a five-minute timer next to three ticket cards being presented. |
| 3 | 1:09 | 33 | Agree a definition of done for SEO-affecting changes, and add it to the pull request template. For Kiran Home: status codes correct for new or changed URLs. One absolute, self-referencing canonical in the raw HTML. No unexpected noindex or nofollow. hreflang complete and reciprocal on affected templates. Internal links as anchors with hrefs. Structured data valid on one URL per template. robots.txt unchanged unless the ticket says otherwise. And no lazy-loaded LCP image or new layout shift. | SEO definition of done<br>• Status, canonical, robots, hreflang<br>• Anchor links, valid structured data<br>• robots.txt unchanged; no LCP/CLS regressions | Pull request template with an SEO checklist section; boxes tick as a developer reviews. |
| 4 | 1:42 | 31 | Automate what you can, because manual checks don't scale. Template tests that assert canonical, robots meta and hreflang output for sample routes. A robots.txt check in CI that fails the build if production robots.txt contains a sitewide disallow. Lighthouse CI or similar on key templates, with budgets for LCP and CLS in the lab. And a post-deploy smoke test that requests ten critical URLs and asserts status codes and key tags. | Automate checks<br>• Template tests<br>• robots.txt CI check<br>• Lighthouse CI budgets; post-deploy smoke test | CI pipeline diagram with four stages lighting green; one stage turns red and blocks the deploy. |
| 5 | 2:13 | 37 | Hands-on. The lesson text has a GitHub Actions workflow that runs after a successful deployment, installs Python and pytest, and runs a smoke test file. The tests request key URLs without following redirects, check the expected status, confirm there's no noindex in meta robots or the X-Robots-Tag header, confirm the canonical equals the page's own URL, and check robots.txt isn't blocking the whole site. Adapt it for GitLab, Bitbucket or whatever the team uses. The important part is agreeing what happens when it fails. | Hands-on: CI smoke test<br>• Runs after each production deploy<br>• Status, noindex, canonical, robots.txt<br>• Agree the failure response | YAML workflow scrolls; then the pytest file; a run log shows '5 passed' and then a simulated failure '1 failed: noindex on /en-ae/products/...'. |
| 6 | 2:50 | 27 | QA on staging before each release. List-crawl the URLs affected by the tickets on staging, with authentication. Check acceptance criteria per ticket and record pass or fail in the ticket itself. And diff against the pre-change crawl to catch unintended side effects, for example a canonical fix that also changed trailing-slash behaviour sitewide. Side effects are where most SEO regressions come from. | Staging QA<br>• List-crawl affected URLs<br>• Pass/fail per acceptance criterion<br>• Diff for side effects | Staging crawl results in a diff view; an unexpected trailing-slash change is highlighted as a side effect. |
| 7 | 3:17 | 31 | Release risk management. Some SEO changes are high-risk: robots.txt edits, sitewide canonical logic, bulk redirects, rendering changes and CDN bot rules. For these, deploy when someone can monitor, not late on a Thursday before Eid holidays or a bank-holiday weekend. Write a rollback plan in the ticket. Monitor logs and Search Console for forty-eight to seventy-two hours. And for robots.txt, confirm in Search Console's robots.txt report that Google fetched the new version. | Release risk<br>• High-risk change types<br>• Deploy when monitored; rollback plan<br>• 48–72 h monitoring; confirm robots fetch | Calendar with a 'Thursday before Eid' slot crossed out and a Tuesday morning slot ticked; a rollback plan card attached to the ticket. |
| 8 | 3:48 | 37 | Handling pushback. When someone says Google can handle it: sometimes, and here's the Search Console evidence that it currently isn't. When someone says it's just SEO, it can wait: tie it to revenue, these are the best-selling products not crawled in four weeks. When someone says our framework does it automatically: great, here's a staging URL where the output differs, can we check the config? And when someone says it's too big for this sprint: offer a smaller first step that captures most of the value. | Handling pushback<br>• 'Google can handle it' → show evidence<br>• 'Just SEO' → tie to revenue<br>• 'Framework does it' → show a URL; 'too big' → smaller step | Four speech bubbles from a developer; each receives a calm reply bubble with a small evidence icon. |
| 9 | 4:25 | 30 | Worked example one: Kiran Home. The developers say the facet change is too big for this sprint. The SEO lead offers a smaller first step: remove the popular searches widget links and add the robots.txt rule for search and sort parameters, after confirming no approved pages use them. That captures most of the crawl benefit in a day, and the full component change moves to the next sprint. | Example 1: Kiran Home's smaller first step<br>• Full facet change too big<br>• Remove widget links + robots rules first<br>• Most of the benefit in a day | Large ticket card splits into a small 'step 1' card moving into the current sprint and a 'step 2' card into the next. |
| 10 | 4:55 | 33 | Worked example two, illustrative. A London agency works with a client's outsourced developers in another time zone. What made it work: tickets in the developers' Jira using the template from the previous lesson; a fifteen-minute weekly call to walk through the top three; the smoke test in their pipeline; and an agreed SEO-sensitive change label that triggers a check before release. Ticket cycle time fell, because questions were answered in the ticket, not over email. | Example 2: remote dev team (illustrative)<br>• Their Jira + template<br>• 15-minute weekly walkthrough<br>• Smoke test + 'SEO-sensitive' label | World clock showing two time zones; a weekly calendar block; a label 'SEO-sensitive change' attaches to a ticket and triggers a checklist. |
| 11 | 5:28 | 40 | Here's something that transforms relationships with development teams: make their lives easier, not harder. Give them test URLs, not just rules. Give them the exact command to verify, so they can check their own work before QA. Group SEO work by component so they touch each file once. Celebrate shipped fixes publicly, with the before and after chart, and credit the developers by name in the stakeholder report. Teams that feel their SEO work is noticed and valued prioritise it. Teams that only hear from SEO when something's broken quietly deprioritise it. | Make developers' lives easier<br>• Test URLs + verification commands<br>• Group work by component<br>• Credit shipped fixes publicly | A developer icon receives a neat bundle (URLs, commands, grouped tasks); later a stakeholder report shows a before/after chart with 'Shipped by: dev team' credit. |
| 12 | 6:08 | 38 | Common mistakes. Sending tickets directly to developers and bypassing their lead or product owner. Assuming a merged pull request means the issue is fixed in production. And treating developers as an audience for SEO lectures, instead of partners with constraints. Recap: fit their process, agree a definition of done, automate checks in CI, QA on staging, manage risky releases, and meet pushback with evidence and smaller steps. Try this now: add three SEO checks to your team's pull request template, and propose the smoke test for your pipeline. | Mistakes, recap, try this now<br>• Respect the lead; merged ≠ fixed; partners not audience<br>• Definition of done + CI + QA + risk<br>• Add 3 PR checks; propose the smoke test | Mistake chips; recap ticks; a PR template gains three SEO checkboxes. End card: 'Next module: verifying fixes and reporting'. |

<details><summary>Narration per scene (paste into ElevenLabs)</summary>

Scene 1 — Working with development teams

```text
Even perfect tickets fail if they arrive at the wrong time, to the wrong person, in the wrong format. SEO is a team sport, and developers are your most important teammates. In this lecture you'll learn how to fit into a development team's process, agree an SEO definition of done, automate checks in CI, run QA on staging, manage release risk, and handle pushback productively, all in the context of getting Kiran Home's two developers to ship the roadmap.
```

Scene 2 — Fit their process

```text
Fit into their process, not yours. Use their tool, whether that's Jira, Linear, GitHub Issues or Trello; tickets in a PDF appendix aren't tickets. Attend sprint planning, or send a short brief before it, and explain the top three tickets in five minutes: problem, evidence, expected outcome. Respect estimation: let developers size the work, and if an estimate is bigger than you expected, ask what drives it, because there's often a simpler implementation. And offer alternatives when capacity is tight.
```

Scene 3 — SEO definition of done

```text
Agree a definition of done for SEO-affecting changes, and add it to the pull request template. For Kiran Home: status codes correct for new or changed URLs. One absolute, self-referencing canonical in the raw HTML. No unexpected noindex or nofollow. hreflang complete and reciprocal on affected templates. Internal links as anchors with hrefs. Structured data valid on one URL per template. robots.txt unchanged unless the ticket says otherwise. And no lazy-loaded LCP image or new layout shift.
```

Scene 4 — Automate checks

```text
Automate what you can, because manual checks don't scale. Template tests that assert canonical, robots meta and hreflang output for sample routes. A robots.txt check in CI that fails the build if production robots.txt contains a sitewide disallow. Lighthouse CI or similar on key templates, with budgets for LCP and CLS in the lab. And a post-deploy smoke test that requests ten critical URLs and asserts status codes and key tags.
```

Scene 5 — Hands-on: CI smoke test

```text
Hands-on. The lesson text has a GitHub Actions workflow that runs after a successful deployment, installs Python and pytest, and runs a smoke test file. The tests request key URLs without following redirects, check the expected status, confirm there's no noindex in meta robots or the X-Robots-Tag header, confirm the canonical equals the page's own URL, and check robots.txt isn't blocking the whole site. Adapt it for GitLab, Bitbucket or whatever the team uses. The important part is agreeing what happens when it fails.
```

Scene 6 — Staging QA

```text
QA on staging before each release. List-crawl the URLs affected by the tickets on staging, with authentication. Check acceptance criteria per ticket and record pass or fail in the ticket itself. And diff against the pre-change crawl to catch unintended side effects, for example a canonical fix that also changed trailing-slash behaviour sitewide. Side effects are where most SEO regressions come from.
```

Scene 7 — Release risk

```text
Release risk management. Some SEO changes are high-risk: robots.txt edits, sitewide canonical logic, bulk redirects, rendering changes and CDN bot rules. For these, deploy when someone can monitor, not late on a Thursday before Eid holidays or a bank-holiday weekend. Write a rollback plan in the ticket. Monitor logs and Search Console for forty-eight to seventy-two hours. And for robots.txt, confirm in Search Console's robots.txt report that Google fetched the new version.
```

Scene 8 — Handling pushback

```text
Handling pushback. When someone says Google can handle it: sometimes, and here's the Search Console evidence that it currently isn't. When someone says it's just SEO, it can wait: tie it to revenue, these are the best-selling products not crawled in four weeks. When someone says our framework does it automatically: great, here's a staging URL where the output differs, can we check the config? And when someone says it's too big for this sprint: offer a smaller first step that captures most of the value.
```

Scene 9 — Example 1: Kiran Home's smaller first step

```text
Worked example one: Kiran Home. The developers say the facet change is too big for this sprint. The SEO lead offers a smaller first step: remove the popular searches widget links and add the robots.txt rule for search and sort parameters, after confirming no approved pages use them. That captures most of the crawl benefit in a day, and the full component change moves to the next sprint.
```

Scene 10 — Example 2: remote dev team (illustrative)

```text
Worked example two, illustrative. A London agency works with a client's outsourced developers in another time zone. What made it work: tickets in the developers' Jira using the template from the previous lesson; a fifteen-minute weekly call to walk through the top three; the smoke test in their pipeline; and an agreed SEO-sensitive change label that triggers a check before release. Ticket cycle time fell, because questions were answered in the ticket, not over email.
```

Scene 11 — Make developers' lives easier

```text
Here's something that transforms relationships with development teams: make their lives easier, not harder. Give them test URLs, not just rules. Give them the exact command to verify, so they can check their own work before QA. Group SEO work by component so they touch each file once. Celebrate shipped fixes publicly, with the before and after chart, and credit the developers by name in the stakeholder report. Teams that feel their SEO work is noticed and valued prioritise it. Teams that only hear from SEO when something's broken quietly deprioritise it.
```

Scene 12 — Mistakes, recap, try this now

```text
Common mistakes. Sending tickets directly to developers and bypassing their lead or product owner. Assuming a merged pull request means the issue is fixed in production. And treating developers as an audience for SEO lectures, instead of partners with constraints. Recap: fit their process, agree a definition of done, automate checks in CI, QA on staging, manage risky releases, and meet pushback with evidence and smaller steps. Try this now: add three SEO checks to your team's pull request template, and propose the smoke test for your pipeline.
```

</details>

### 11. Verifying fixes: recrawl, Search Console and logs

Lesson `verifying-fixes` · module “Verifying fixes and reporting to stakeholders” · target 7 min · 11 scenes · 925 words · planned 6:43 (words ≈ 6:42) · status: **to produce**

| Term | Say |
|---|---|
| RC | R C |
| CWV | core web vitals |

| # | Time | Sec | Narration | On screen | Visual |
|---|---|---|---|---|---|
| 1 | 0:00 | 34 | A ticket isn't done when the pull request merges. It's done when production behaves as specified, and search engines have processed the change. In this lecture you'll verify Kiran Home's fixes in three layers: production behaviour on the same day, Google's processing over days to weeks, and behaviour change in the logs over weeks. You'll also learn how to diagnose a fix that didn't work, and how to record verification so nobody has to take your word for it. | Verifying fixes<br>• Merged ≠ fixed<br>• Three layers: production, Google, logs<br>• When a fix 'doesn't work' | A 'Merged' badge slides onto a ticket, then a larger 'Verified' stamp waits above it with three unchecked boxes. Title appears. |
| 2 | 0:34 | 40 | Layer one: production behaviour, the same day. Use the same crawl configuration you saved in Module one, and list-crawl the affected URLs in production. For RC one, the canonicals, the check in the lesson text loads the production crawl export, keeps product and journal URLs, and counts those whose canonical doesn't equal their address, writing the first twenty failures to a file. For RC three, the redirects, recrawl the legacy list and confirm one three-oh-one to a relevant two-hundred page, no chains in the priority tier, and four-tens only on the agreed list. | Layer 1: production (same day)<br>• Same crawl config<br>• RC1: canonical = address count<br>• RC3: one hop, no chains, agreed 410s | A production crawl runs with the saved 'v1.2' config; a counter for 'not self-canonical' drops to zero; a redirect table shows all green. |
| 3 | 1:14 | 43 | Layer two: Google's processing, over days to weeks. Use URL Inspection on a sample of fixed URLs, with a live test to confirm Google sees the new canonical, and request indexing for a handful of key pages only; it's rate-limited and not needed for thousands of URLs. In the Page indexing report, use Validate fix for the relevant issue type once the fix is live. Validation can take a couple of weeks or longer on big sites. Resubmit updated sitemaps. Watch enhancement reports for structured data, and remember Core Web Vitals field data updates on a twenty-eight-day window. | Layer 2: Google (days–weeks)<br>• URL Inspection live tests on samples<br>• Validate fix; resubmit sitemaps<br>• Enhancements; CWV 28-day window | URL Inspection mock shows 'Google-selected canonical = user-declared'; a 'Validate fix' progress bar moves from 'Started' to 'Passed'. |
| 4 | 1:57 | 40 | Layer three: behaviour change in the logs, over weeks. After RC two, Googlebot requests to sort, price and size URLs should fall sharply once the robots rules are fetched, and product URLs should become a larger share. After RC four, previously uncrawled deep products should start appearing. After RC three, hits on legacy four-oh-fours should decline. The hands-on in the lesson text turns the weekly segment table from the log lab into a share-of-crawl area chart, with dashed lines for each release. It's the most persuasive visual in your stakeholder report. | Layer 3: logs (weeks)<br>• Facet share falls; product share rises<br>• Deep products appear; legacy 404 hits fall<br>• Annotated share-of-crawl chart | Stacked area chart animates over weeks; the facet band shrinks after a dashed 'RC2+RC4 live' line and the product band grows. |
| 5 | 2:37 | 30 | Verifying RC eight, the bot access fix, needs its own checks. Request product URLs with the bots' user agents to confirm no challenge page, knowing that's only header simulation. Check the CDN firewall events for verified Bingbot and AI search bots: challenges on public paths should drop to zero. And in Bing Webmaster Tools, indexed product counts should recover over the following weeks, and AI Performance citations may reappear. | Verifying RC8<br>• UA simulation (limited)<br>• CDN events: zero challenges on public paths<br>• Bing indexed products recover | CDN events chart: a red 'challenged' series drops to zero at the fix date; a Bing indexed-pages line recovers. |
| 6 | 3:07 | 30 | Set up a regression watch for the first months, because fixes regress when unrelated code changes. Run a weekly scheduled crawl of a fixed URL sample per template and diff it against the post-fix baseline. Alert if any product canonical differs from its address, if any noindex appears on product or category templates, or if robots.txt changes. The change monitor from the Technical SEO Mastery course does exactly this. | Regression watch<br>• Weekly sample crawl vs baseline<br>• Alerts: canonical ≠ address, noindex, robots change<br>• Reuse the change monitor | A weekly calendar with sample crawls; an alert bell fires when a canonical changes on one sample URL. |
| 7 | 3:37 | 36 | What if a fix didn't work? Diagnose with the pipeline. Production correct, but Google still picks the old canonical? Check for conflicting signals, like internal links, sitemaps and hreflang pointing elsewhere, and check the last crawl date in URL Inspection. Validation failed on some samples? Open every failed URL, because it's often an edge case: uppercase URLs, trailing slashes, or a second template variant. Log behaviour unchanged? Confirm which robots.txt Google fetched, and that internal links to the blocked patterns were actually removed. | When a fix 'doesn't work'<br>• Conflicting signals? Last crawl date?<br>• Open every failed sample<br>• Confirm robots fetch + link removal | Troubleshooting flowchart with three branches, each ending in a check icon. |
| 8 | 4:13 | 38 | Worked example two, a different client, illustrative. A Jeddah retailer's validation for duplicate, Google chose different canonical than user fails on a third of the samples, a month after a canonical fix. Opening the failed URLs reveals a pattern: they're all uppercase variants, linked from an old email template, still returning two hundred with a self-referencing canonical. The fix: redirect uppercase paths to lowercase, and correct the email template. Validation passes on the next attempt. The failure wasn't the fix. It was an edge case nobody had listed. | Example 2: the uppercase edge case (illustrative)<br>• 1/3 of validation samples fail<br>• All uppercase variants from an email template<br>• 301 to lowercase → validation passes | Failed sample list; each URL's uppercase segment highlights; a redirect rule maps /Products/ to /products/; validation badge turns green. |
| 9 | 4:51 | 30 | Close each ticket with a verification record. For example: SEO seven verified on a date. The production crawl, using config version one point two, found zero of twelve thousand three hundred product URLs non-self-canonical. Search Console validation started the next day. Ten of ten sampled URLs show Google-selected canonical equal to user-declared, checked two weeks later. And evidence files attached. Anyone can re-check your work from that record. | The verification record<br>• Production result with config version<br>• Validation start date<br>• Sample results + evidence files | A verification note types into a ticket comment with each line highlighting. |
| 10 | 5:21 | 49 | Let me give you a realistic timeline for Kiran Home, so you can set expectations with stakeholders. Day zero: RC one and RC eight go live, and the production crawl confirms them the same day. Days one to three: URL Inspection live tests show Google sees the new canonicals, and CDN events show zero challenges for verified bots. Weeks one to three: Page indexing validation progresses, and logs start showing product share rising. Weeks two to six: non-UK product pages move to indexed, and Bing's product coverage recovers. Core Web Vitals changes show up only after the twenty-eight-day window rolls over. Sharing this timeline up front prevents panicked messages in week one. | A realistic timeline (illustrative)<br>• Day 0: production verified<br>• Days 1–3: live tests, CDN events<br>• Weeks 1–6: validation, logs, indexing, Bing; CWV after 28 days | Horizontal timeline with milestones appearing at day 0, day 3, week 3 and week 6, each with a small icon. |
| 11 | 6:10 | 33 | Common mistakes. Verifying on staging only. Requesting indexing for thousands of URLs manually instead of fixing signals and sitemaps. Declaring victory from one URL Inspection sample. And forgetting that Core Web Vitals field data lags by weeks. Recap: production the same day, Google over days to weeks, logs over weeks, a regression watch, and written verification records. Try this now: pick one fix your team shipped recently, and write a three-layer verification record for it. | Mistakes, recap, try this now<br>• Staging only; mass indexing requests<br>• One-sample victory; forgetting CWV lag<br>• Write a 3-layer record for one fix | Mistake chips; three-layer stack ticks; a blank verification record template. End card: 'Next: reporting to stakeholders'. |

<details><summary>Narration per scene (paste into ElevenLabs)</summary>

Scene 1 — Verifying fixes

```text
A ticket isn't done when the pull request merges. It's done when production behaves as specified, and search engines have processed the change. In this lecture you'll verify Kiran Home's fixes in three layers: production behaviour on the same day, Google's processing over days to weeks, and behaviour change in the logs over weeks. You'll also learn how to diagnose a fix that didn't work, and how to record verification so nobody has to take your word for it.
```

Scene 2 — Layer 1: production (same day)

```text
Layer one: production behaviour, the same day. Use the same crawl configuration you saved in Module one, and list-crawl the affected URLs in production. For RC one, the canonicals, the check in the lesson text loads the production crawl export, keeps product and journal URLs, and counts those whose canonical doesn't equal their address, writing the first twenty failures to a file. For RC three, the redirects, recrawl the legacy list and confirm one three-oh-one to a relevant two-hundred page, no chains in the priority tier, and four-tens only on the agreed list.
```

Scene 3 — Layer 2: Google (days–weeks)

```text
Layer two: Google's processing, over days to weeks. Use URL Inspection on a sample of fixed URLs, with a live test to confirm Google sees the new canonical, and request indexing for a handful of key pages only; it's rate-limited and not needed for thousands of URLs. In the Page indexing report, use Validate fix for the relevant issue type once the fix is live. Validation can take a couple of weeks or longer on big sites. Resubmit updated sitemaps. Watch enhancement reports for structured data, and remember Core Web Vitals field data updates on a twenty-eight-day window.
```

Scene 4 — Layer 3: logs (weeks)

```text
Layer three: behaviour change in the logs, over weeks. After RC two, Googlebot requests to sort, price and size URLs should fall sharply once the robots rules are fetched, and product URLs should become a larger share. After RC four, previously uncrawled deep products should start appearing. After RC three, hits on legacy four-oh-fours should decline. The hands-on in the lesson text turns the weekly segment table from the log lab into a share-of-crawl area chart, with dashed lines for each release. It's the most persuasive visual in your stakeholder report.
```

Scene 5 — Verifying RC8

```text
Verifying RC eight, the bot access fix, needs its own checks. Request product URLs with the bots' user agents to confirm no challenge page, knowing that's only header simulation. Check the CDN firewall events for verified Bingbot and AI search bots: challenges on public paths should drop to zero. And in Bing Webmaster Tools, indexed product counts should recover over the following weeks, and AI Performance citations may reappear.
```

Scene 6 — Regression watch

```text
Set up a regression watch for the first months, because fixes regress when unrelated code changes. Run a weekly scheduled crawl of a fixed URL sample per template and diff it against the post-fix baseline. Alert if any product canonical differs from its address, if any noindex appears on product or category templates, or if robots.txt changes. The change monitor from the Technical SEO Mastery course does exactly this.
```

Scene 7 — When a fix 'doesn't work'

```text
What if a fix didn't work? Diagnose with the pipeline. Production correct, but Google still picks the old canonical? Check for conflicting signals, like internal links, sitemaps and hreflang pointing elsewhere, and check the last crawl date in URL Inspection. Validation failed on some samples? Open every failed URL, because it's often an edge case: uppercase URLs, trailing slashes, or a second template variant. Log behaviour unchanged? Confirm which robots.txt Google fetched, and that internal links to the blocked patterns were actually removed.
```

Scene 8 — Example 2: the uppercase edge case (illustrative)

```text
Worked example two, a different client, illustrative. A Jeddah retailer's validation for duplicate, Google chose different canonical than user fails on a third of the samples, a month after a canonical fix. Opening the failed URLs reveals a pattern: they're all uppercase variants, linked from an old email template, still returning two hundred with a self-referencing canonical. The fix: redirect uppercase paths to lowercase, and correct the email template. Validation passes on the next attempt. The failure wasn't the fix. It was an edge case nobody had listed.
```

Scene 9 — The verification record

```text
Close each ticket with a verification record. For example: SEO seven verified on a date. The production crawl, using config version one point two, found zero of twelve thousand three hundred product URLs non-self-canonical. Search Console validation started the next day. Ten of ten sampled URLs show Google-selected canonical equal to user-declared, checked two weeks later. And evidence files attached. Anyone can re-check your work from that record.
```

Scene 10 — A realistic timeline (illustrative)

```text
Let me give you a realistic timeline for Kiran Home, so you can set expectations with stakeholders. Day zero: RC one and RC eight go live, and the production crawl confirms them the same day. Days one to three: URL Inspection live tests show Google sees the new canonicals, and CDN events show zero challenges for verified bots. Weeks one to three: Page indexing validation progresses, and logs start showing product share rising. Weeks two to six: non-UK product pages move to indexed, and Bing's product coverage recovers. Core Web Vitals changes show up only after the twenty-eight-day window rolls over. Sharing this timeline up front prevents panicked messages in week one.
```

Scene 11 — Mistakes, recap, try this now

```text
Common mistakes. Verifying on staging only. Requesting indexing for thousands of URLs manually instead of fixing signals and sitemaps. Declaring victory from one URL Inspection sample. And forgetting that Core Web Vitals field data lags by weeks. Recap: production the same day, Google over days to weeks, logs over weeks, a regression watch, and written verification records. Try this now: pick one fix your team shipped recently, and write a three-layer verification record for it.
```

</details>

### 12. Reporting to stakeholders

Lesson `reporting-to-stakeholders` · module “Verifying fixes and reporting to stakeholders” · target 6 min · 11 scenes · 876 words · planned 6:23 (words ≈ 6:21) · status: **to produce**

| Term | Say |
|---|---|
| YoY | year on year |
| CrUX | crux |
| Looker Studio | looker studio |

| # | Time | Sec | Narration | On screen | Visual |
|---|---|---|---|---|---|
| 1 | 0:00 | 33 | An audit that isn't understood doesn't get funded, and an audit that isn't funded doesn't get implemented. The Kiran Home founder, the marketing manager and the developers all need different things from your work. In this lecture you'll write a one-page executive summary that leads with the answer, choose visuals that persuade, set up a monthly progress report with leading and lagging indicators, attribute results honestly, and run a thirty-minute walkthrough that ends with decisions. | Reporting to stakeholders<br>• Different audiences, different formats<br>• Executive summary, visuals, monthly report<br>• Honest attribution and a decisive walkthrough | Three audience icons (founder, marketing manager, developers) each receive a different document shape: one page, a tracker, tickets. Title appears. |
| 2 | 0:33 | 28 | Start with audiences. The founder wants to know what happened, what it costs, what you'll do, and when they'll know it's working, in one page and three to five slides. The marketing manager wants the plan, the progress and what they own: a roadmap, a status tracker and a monthly report. The developers want exact tasks, which are your tickets. One audit, three formats. | Three audiences<br>• Founder: one page, 3–5 slides<br>• Marketing manager: roadmap + tracker + monthly<br>• Developers: tickets | A table with three rows animates, each row pairing an audience icon with its format icon. |
| 3 | 1:01 | 39 | The executive summary leads with the answer, never the methodology. For Kiran Home: what happened. After the relaunch, the new storefront told Google that UAE and Pakistan product pages were duplicates of UK pages, so Google stopped showing most local pages, and UK pages with pound prices appeared instead. Meanwhile, Google spent most of its crawling on filter and search URLs, and the CDN was blocking Bing and AI search crawlers. What it means: the markets that drive most revenue lost much of their product visibility. Illustrative, of course. | Lead with the answer<br>• What happened, in plain language<br>• What it means for revenue<br>• No methodology first | One-page summary document with the 'What happened' paragraph highlighting; a methodology section is pushed to the appendix. |
| 4 | 1:40 | 43 | Then what we'll do in the next six weeks, in five numbered lines: fix canonicals, restore redirects with links or traffic, stop filters and search being crawled and make categories fully crawlable, fix product structured data and the slow reviews widget, fix the CDN bot rule, and add SEO checks to every release. How we'll know it's working: local product pages move to indexed, Google's crawling shifts from filters to products, and organic revenue from the UAE and Pakistan trends back, with timing that depends on recrawl and competition. And decisions needed: approve two sprints and the widget budget. | Plan, proof, decisions<br>• 5 actions, 6 weeks<br>• Leading indicators + honest timing<br>• Decisions needed | Summary continues: numbered actions tick in; indicator icons; a 'Decisions needed' box with two checkboxes. |
| 5 | 2:23 | 33 | Visuals that persuade. Traffic by market and segment, with the relaunch annotated, shows where the drop is. Googlebot requests by segment over time tells the crawl-waste story in one chart. Indexed versus not indexed by sitemap segment, before and after. And a roadmap timeline with owners. Keep charts simple: one message per chart, and a title that states the conclusion, like Googlebot spends most of its time on filter pages, not a label like crawl by segment. | Visuals that persuade<br>• Traffic by market with annotations<br>• Googlebot by segment over time<br>• Indexed by segment; roadmap<br>• Titles that state conclusions | Four chart thumbnails; one chart's generic title 'Crawl by segment' rewrites itself to 'Googlebot spends most of its time on filter pages'. |
| 6 | 2:56 | 37 | The monthly progress report, for the marketing manager. A status tracker for each ticket: owner, sprint, and status from planned to in progress to live to verified. Leading indicators: index coverage by segment, the Googlebot request mix, Core Web Vitals by template, rich result items, Bing indexed pages, and AI surface exposure from Search Console's generative AI report and Bing's AI Performance citations. Lagging indicators: organic clicks, sessions and revenue by market and template, year on year. Risks and blockers. And next month's focus. | The monthly report<br>• Ticket status tracker<br>• Leading indicators (incl. Bing + AI exposure)<br>• Lagging indicators YoY; risks; next focus | Monthly report layout with sections filling in; small KPI tiles for leading and lagging indicators. |
| 7 | 3:33 | 34 | Hands-on. The lesson text lists the data pull for the monthly report: non-brand clicks by market and template from the BigQuery export, indexed versus not indexed by sitemap segment, Googlebot share by segment from the log script, Core Web Vitals from the CrUX API, Bing indexed products and AI citations, AI impressions from Search Console, and ticket status from the tracker. Same sources, same method, every month, in a dashboard with release annotations, and a one-page written summary. | Hands-on: the monthly data pull<br>• Same sources, same method<br>• Dashboard with annotations<br>• One page + appendix | A source → metric → tool table animates; then a dashboard with annotation flags assembles. |
| 8 | 4:07 | 37 | Honest attribution. Organic performance is affected by seasonality, demand, competitors, Google's updates, pricing and stock. So be careful with claims. Say, after the fix went live, local product pages returned to the index and organic sessions to those pages recovered over the following weeks. Don't say, our fix increased revenue by a precise amount. Compare fixed segments with an unaffected control; UK pages weren't affected by RC one, which makes a natural comparison. And annotate confounders like big promotions, core updates and Eid sales. | Honest attribution<br>• Describe sequence, not certainty<br>• Use a control segment (UK)<br>• Annotate confounders | Two lines: fixed segment (UAE/PK) recovering and control segment (UK) steady; annotation flags for 'Eid sale' and 'core update'. |
| 9 | 4:44 | 34 | Worked example two, illustrative. Three months in, Kiran Home's UAE and Pakistan product pages are back in the index, and crawl has shifted to products. But revenue from UAE organic is only partly recovered, because a competitor launched a large Eid campaign. The report says exactly that. Leading indicators fixed and verified. Lagging indicators improving. One external factor annotated. Next actions listed. The founder approves the next phase because the report was credible, not because it was rosy. | Example 2: a partial recovery (illustrative)<br>• Leading indicators fixed<br>• Revenue partly recovered; competitor campaign<br>• Credible → next phase approved | Report page with green leading indicators, an amber revenue line, and a competitor annotation; an 'Approved' stamp lands. |
| 10 | 5:18 | 29 | The thirty-minute walkthrough. Five minutes for the story, the executive summary. Ten minutes for the top three root causes, with one piece of evidence each. Ten minutes for the roadmap and what each person owns. And five minutes for decisions and next steps. Send materials in advance, record decisions in writing afterwards, and set the date for the first progress check before you leave the room. | The 30-minute walkthrough<br>• 5 story · 10 causes · 10 roadmap · 5 decisions<br>• Materials in advance<br>• Decisions in writing; next check date | A 30-minute clock divided into four coloured segments with labels. |
| 11 | 5:47 | 36 | Common mistakes. Opening with methodology instead of the answer. Burying the three things that matter in a ninety-page PDF. Taking full credit for recovery without acknowledging other factors. And reporting only rankings, not revenue-relevant outcomes and leading indicators. Recap: tailor formats to audiences, lead with the answer, use conclusion-titled visuals, report leading and lagging indicators monthly, attribute honestly, and run a decisive walkthrough. Try this now: write the what happened paragraph for your current project in three sentences a founder would understand. | Mistakes, recap, try this now<br>• Methodology first; 90-page PDFs<br>• Over-claiming; rankings only<br>• Write a 3-sentence 'what happened' | Mistake chips; recap ticks; a three-line text box ready for input. End card: 'Next module: AI search readiness and the audit toolkit'. |

<details><summary>Narration per scene (paste into ElevenLabs)</summary>

Scene 1 — Reporting to stakeholders

```text
An audit that isn't understood doesn't get funded, and an audit that isn't funded doesn't get implemented. The Kiran Home founder, the marketing manager and the developers all need different things from your work. In this lecture you'll write a one-page executive summary that leads with the answer, choose visuals that persuade, set up a monthly progress report with leading and lagging indicators, attribute results honestly, and run a thirty-minute walkthrough that ends with decisions.
```

Scene 2 — Three audiences

```text
Start with audiences. The founder wants to know what happened, what it costs, what you'll do, and when they'll know it's working, in one page and three to five slides. The marketing manager wants the plan, the progress and what they own: a roadmap, a status tracker and a monthly report. The developers want exact tasks, which are your tickets. One audit, three formats.
```

Scene 3 — Lead with the answer

```text
The executive summary leads with the answer, never the methodology. For Kiran Home: what happened. After the relaunch, the new storefront told Google that UAE and Pakistan product pages were duplicates of UK pages, so Google stopped showing most local pages, and UK pages with pound prices appeared instead. Meanwhile, Google spent most of its crawling on filter and search URLs, and the CDN was blocking Bing and AI search crawlers. What it means: the markets that drive most revenue lost much of their product visibility. Illustrative, of course.
```

Scene 4 — Plan, proof, decisions

```text
Then what we'll do in the next six weeks, in five numbered lines: fix canonicals, restore redirects with links or traffic, stop filters and search being crawled and make categories fully crawlable, fix product structured data and the slow reviews widget, fix the CDN bot rule, and add SEO checks to every release. How we'll know it's working: local product pages move to indexed, Google's crawling shifts from filters to products, and organic revenue from the UAE and Pakistan trends back, with timing that depends on recrawl and competition. And decisions needed: approve two sprints and the widget budget.
```

Scene 5 — Visuals that persuade

```text
Visuals that persuade. Traffic by market and segment, with the relaunch annotated, shows where the drop is. Googlebot requests by segment over time tells the crawl-waste story in one chart. Indexed versus not indexed by sitemap segment, before and after. And a roadmap timeline with owners. Keep charts simple: one message per chart, and a title that states the conclusion, like Googlebot spends most of its time on filter pages, not a label like crawl by segment.
```

Scene 6 — The monthly report

```text
The monthly progress report, for the marketing manager. A status tracker for each ticket: owner, sprint, and status from planned to in progress to live to verified. Leading indicators: index coverage by segment, the Googlebot request mix, Core Web Vitals by template, rich result items, Bing indexed pages, and AI surface exposure from Search Console's generative AI report and Bing's AI Performance citations. Lagging indicators: organic clicks, sessions and revenue by market and template, year on year. Risks and blockers. And next month's focus.
```

Scene 7 — Hands-on: the monthly data pull

```text
Hands-on. The lesson text lists the data pull for the monthly report: non-brand clicks by market and template from the BigQuery export, indexed versus not indexed by sitemap segment, Googlebot share by segment from the log script, Core Web Vitals from the CrUX API, Bing indexed products and AI citations, AI impressions from Search Console, and ticket status from the tracker. Same sources, same method, every month, in a dashboard with release annotations, and a one-page written summary.
```

Scene 8 — Honest attribution

```text
Honest attribution. Organic performance is affected by seasonality, demand, competitors, Google's updates, pricing and stock. So be careful with claims. Say, after the fix went live, local product pages returned to the index and organic sessions to those pages recovered over the following weeks. Don't say, our fix increased revenue by a precise amount. Compare fixed segments with an unaffected control; UK pages weren't affected by RC one, which makes a natural comparison. And annotate confounders like big promotions, core updates and Eid sales.
```

Scene 9 — Example 2: a partial recovery (illustrative)

```text
Worked example two, illustrative. Three months in, Kiran Home's UAE and Pakistan product pages are back in the index, and crawl has shifted to products. But revenue from UAE organic is only partly recovered, because a competitor launched a large Eid campaign. The report says exactly that. Leading indicators fixed and verified. Lagging indicators improving. One external factor annotated. Next actions listed. The founder approves the next phase because the report was credible, not because it was rosy.
```

Scene 10 — The 30-minute walkthrough

```text
The thirty-minute walkthrough. Five minutes for the story, the executive summary. Ten minutes for the top three root causes, with one piece of evidence each. Ten minutes for the roadmap and what each person owns. And five minutes for decisions and next steps. Send materials in advance, record decisions in writing afterwards, and set the date for the first progress check before you leave the room.
```

Scene 11 — Mistakes, recap, try this now

```text
Common mistakes. Opening with methodology instead of the answer. Burying the three things that matter in a ninety-page PDF. Taking full credit for recovery without acknowledging other factors. And reporting only rankings, not revenue-relevant outcomes and leading indicators. Recap: tailor formats to audiences, lead with the answer, use conclusion-titled visuals, report leading and lagging indicators monthly, attribute honestly, and run a decisive walkthrough. Try this now: write the what happened paragraph for your current project in three sentences a founder would understand.
```

</details>

### 13. Auditing AI search readiness: access, accuracy and Bing

Lesson `auditing-ai-search-readiness` · module “AI search readiness and the audit toolkit” · target 7 min · 11 scenes · 927 words · planned 6:42 (words ≈ 6:43) · status: **to produce**

| Term | Say |
|---|---|
| GEO | G E O |
| CDN | C D N |
| RC8 | R C eight |
| AED | U A E dirhams |

| # | Time | Sec | Narration | On screen | Visual |
|---|---|---|---|---|---|
| 1 | 0:00 | 36 | Kiran Home's customers don't only use classic search. They ask Google's AI Mode for handmade brass lanterns delivered to Dubai, ChatGPT for Eid gift ideas from Pakistan, and Copilot for comparisons. In twenty twenty-six, a technical audit that ignores these surfaces is incomplete. In this lecture you'll add an AI search readiness section to the audit: six checks, a script that tests robots rules and challenges for AI bots, accuracy checks on assistant answers, and a clear way to write it up. | Auditing AI search readiness<br>• Six checks<br>• Robots + challenge test for AI bots<br>• Accuracy checks and write-up | Three assistant chat bubbles ask about Kiran Home products; audit checklist cards slide in beside them. Title appears. |
| 2 | 0:36 | 29 | Here's the key idea. AI surfaces depend on the same foundations you've already audited: crawlability, indexing, rendering and structured data. They add a few AI-specific controls and a second engine, Bing, that matters much more than before. So this isn't a separate GEO project. It's six extra checks that plug into your existing workbook and root causes. The strategy side lives in the AI Search Optimization course. | Same foundations, six extra checks<br>• Crawl, index, render, structured data<br>• Plus AI controls and Bing<br>• Plugs into existing root causes | A foundation block labelled 'technical SEO' supports a smaller block 'AI search readiness' on top. |
| 3 | 1:05 | 40 | Check one: does a written AI crawler policy exist, and does robots.txt match it? For Kiran Home, there's no policy, and robots.txt has no AI groups at all. That's not necessarily wrong, but it means nobody decided. Check two: does the CDN or firewall block or challenge the bots you intend to allow? For Kiran Home, Bingbot and AI search bots are challenged on product pages. That's RC eight. Check three: are key facts in raw HTML? Delivery to the UAE only appears in the basket, and the returns policy is an image. | Checks 1–3<br>• Policy exists and matches robots.txt?<br>• CDN challenges intended bots? (RC8)<br>• Key facts in raw HTML? | Three check cards flip, revealing Kiran Home's illustrative results with red and amber markers. |
| 4 | 1:45 | 39 | Check four: are key pages indexed in both Google and Bing? Bing's product coverage fell after the relaunch, which is consistent with the CDN challenges. Check five: are structured data and feeds consistent with the page? The Arabic UAE template shows dirhams on the page but pounds in its JSON-LD, part of RC six. Check six: is an AI exposure baseline recorded? That means the Search Console generative AI report, Bing's AI Performance report and the GA4 AI assistants channel, plus a documented decision on Google's Search generative AI control. | Checks 4–6<br>• Indexed in Google AND Bing?<br>• Markup and feeds match the page? (RC6)<br>• AI exposure baseline + control decision | Three more check cards flip; the last shows three mini dashboards captured as a 'baseline' snapshot. |
| 5 | 2:24 | 37 | Hands-on. The script in the lesson text combines two tests. For each bot in your policy, it reads robots.txt and says whether each key path is allowed or blocked. Then it requests each path with that bot's user agent and flags four-oh-three, four-twenty-nine, five-oh-three or a challenge page. Remember, this is header simulation from your own IP, so it isn't proof of how the real bot is treated. Confirm with CDN firewall events for verified bot traffic. And include only the bots your policy actually mentions. | Hands-on: robots + challenge test<br>• robots.txt allow/BLOCK per bot and path<br>• Request with bot UA; flag challenges<br>• Confirm with CDN firewall events | Code walkthrough; output rows show 'robots=allow http=503 CHALLENGED' for bingbot on a product URL. |
| 6 | 3:01 | 37 | Now accuracy. Ask three assistants five factual questions a customer might ask. Does Kiran Home deliver to Dubai, and how long does it take? What's the returns policy? How much is the large brass lantern? Record wrong answers and the sources cited. For Kiran Home, illustratively, assistants quote an old UK-only delivery policy from a cached marketplace listing, and a pre-relaunch price. Here's the key idea: the finding goes to the fact sources, meaning site text, feeds and the marketplace listing. Not to the AI. | Accuracy checks<br>• 3 assistants × 5 factual questions<br>• Record wrong answers + cited sources<br>• Fix the sources, not 'the AI' | Assistant answer cards with two wrong facts highlighted; arrows trace each to its cited source card (marketplace listing, old page). |
| 7 | 3:38 | 34 | How do you write it up? Add an AI search readiness section to the findings workbook, with the six checks, evidence IDs, and links to root causes, RC six and RC eight here, plus any new marketing actions, like publishing the returns policy as text and showing delivery information on product pages. And recommend a written AI crawler policy as a business decision with the founder, not as an SEO default. Different businesses legitimately choose differently on training crawlers. | Writing it up<br>• Six checks with evidence IDs<br>• Link to RC6, RC8 + marketing actions<br>• Policy = founder's decision | Workbook tab 'AI search readiness' fills with six rows; a 'Decision: founder' tag attaches to the policy row. |
| 8 | 4:12 | 40 | Worked example one: Kiran Home, as you've seen. The CDN challenge becomes a high-scoring, low-effort root cause, and the accuracy checks produce two easy content actions. Worked example two, a different client, illustrative. A Birmingham industrial supplier is never cited by Copilot. The AI section finds the site was never added to Bing Webmaster Tools, a Bing sitemap error from a relative URL, and product specs only in PDFs. Fixes: verify in Bing, fix the sitemap, publish HTML spec tables. Bing indexing improves, and AI Performance starts showing citations for spec-related grounding queries. | Example 2: invisible in Copilot (illustrative)<br>• Never verified in Bing; sitemap error<br>• Specs only in PDFs<br>• Verify, fix, HTML tables → citations appear | A Bing Webmaster Tools 'Add site' step completes; a sitemap error clears; a PDF spec sheet converts into an HTML table; an AI Performance tile ticks up. |
| 9 | 4:52 | 30 | A note on scope, so this doesn't balloon. The AI readiness section of a technical audit covers access, indexing, raw HTML facts, consistency, and a measurement baseline. It doesn't cover content strategy for AI answers, prompt panels, or earning third-party mentions. If the client needs those, recommend the AI search strategy work as a separate phase. Keeping the boundary clear protects your timeline and keeps the audit actionable. | Keep scope tight<br>• Audit: access, indexing, facts, consistency, baseline<br>• Not: content strategy, prompt panels, PR<br>• Recommend a separate phase if needed | A scope boundary line; items inside are technical checks, items outside are strategy tasks with a 'phase 2' tag. |
| 10 | 5:22 | 44 | How do you read the script's output? If robots says block for a bot your policy allows, that's a robots.txt bug, or an undocumented decision. If robots says allow, but the request is challenged, that's a CDN or firewall rule, so confirm it in the firewall events for verified IPs and write a ticket. If a training crawler your policy blocks still gets a two hundred, remember robots.txt only asks; if enforcement matters, use CDN rules. And if every bot gets the same result, your CDN may be judging your IP, not the user agent, so rely on the firewall events instead. | Reading the output<br>• BLOCK for an allowed bot → robots bug<br>• allow + CHALLENGED → CDN rule (ticket)<br>• 200 for blocked trainer → robots only asks | Four output patterns appear as rows, each with a diagnosis chip sliding in beside it. |
| 11 | 6:06 | 36 | Common mistakes. Treating AI readiness as separate from technical SEO. Recommending blanket AI bot blocks or allows without a business decision. And blaming the AI for wrong answers instead of fixing the sources it cites. Recap: six checks, a robots and challenge test confirmed with CDN events, accuracy checks traced to sources, and a write-up linked to root causes. Try this now: run the six checks on a site you know, even roughly, and write down the one that surprises you most. | Mistakes, recap, try this now<br>• Not separate; no blanket rules; fix sources<br>• Six checks + test + accuracy<br>• Run the six checks on one site | Mistake chips; recap ticks; a six-item checklist ready to fill. End card: 'Next: the 2026 technical audit checklist'. |

<details><summary>Narration per scene (paste into ElevenLabs)</summary>

Scene 1 — Auditing AI search readiness

```text
Kiran Home's customers don't only use classic search. They ask Google's AI Mode for handmade brass lanterns delivered to Dubai, ChatGPT for Eid gift ideas from Pakistan, and Copilot for comparisons. In twenty twenty-six, a technical audit that ignores these surfaces is incomplete. In this lecture you'll add an AI search readiness section to the audit: six checks, a script that tests robots rules and challenges for AI bots, accuracy checks on assistant answers, and a clear way to write it up.
```

Scene 2 — Same foundations, six extra checks

```text
Here's the key idea. AI surfaces depend on the same foundations you've already audited: crawlability, indexing, rendering and structured data. They add a few AI-specific controls and a second engine, Bing, that matters much more than before. So this isn't a separate GEO project. It's six extra checks that plug into your existing workbook and root causes. The strategy side lives in the AI Search Optimization course.
```

Scene 3 — Checks 1–3

```text
Check one: does a written AI crawler policy exist, and does robots.txt match it? For Kiran Home, there's no policy, and robots.txt has no AI groups at all. That's not necessarily wrong, but it means nobody decided. Check two: does the CDN or firewall block or challenge the bots you intend to allow? For Kiran Home, Bingbot and AI search bots are challenged on product pages. That's RC eight. Check three: are key facts in raw HTML? Delivery to the UAE only appears in the basket, and the returns policy is an image.
```

Scene 4 — Checks 4–6

```text
Check four: are key pages indexed in both Google and Bing? Bing's product coverage fell after the relaunch, which is consistent with the CDN challenges. Check five: are structured data and feeds consistent with the page? The Arabic UAE template shows dirhams on the page but pounds in its JSON-LD, part of RC six. Check six: is an AI exposure baseline recorded? That means the Search Console generative AI report, Bing's AI Performance report and the GA4 AI assistants channel, plus a documented decision on Google's Search generative AI control.
```

Scene 5 — Hands-on: robots + challenge test

```text
Hands-on. The script in the lesson text combines two tests. For each bot in your policy, it reads robots.txt and says whether each key path is allowed or blocked. Then it requests each path with that bot's user agent and flags four-oh-three, four-twenty-nine, five-oh-three or a challenge page. Remember, this is header simulation from your own IP, so it isn't proof of how the real bot is treated. Confirm with CDN firewall events for verified bot traffic. And include only the bots your policy actually mentions.
```

Scene 6 — Accuracy checks

```text
Now accuracy. Ask three assistants five factual questions a customer might ask. Does Kiran Home deliver to Dubai, and how long does it take? What's the returns policy? How much is the large brass lantern? Record wrong answers and the sources cited. For Kiran Home, illustratively, assistants quote an old UK-only delivery policy from a cached marketplace listing, and a pre-relaunch price. Here's the key idea: the finding goes to the fact sources, meaning site text, feeds and the marketplace listing. Not to the AI.
```

Scene 7 — Writing it up

```text
How do you write it up? Add an AI search readiness section to the findings workbook, with the six checks, evidence IDs, and links to root causes, RC six and RC eight here, plus any new marketing actions, like publishing the returns policy as text and showing delivery information on product pages. And recommend a written AI crawler policy as a business decision with the founder, not as an SEO default. Different businesses legitimately choose differently on training crawlers.
```

Scene 8 — Example 2: invisible in Copilot (illustrative)

```text
Worked example one: Kiran Home, as you've seen. The CDN challenge becomes a high-scoring, low-effort root cause, and the accuracy checks produce two easy content actions. Worked example two, a different client, illustrative. A Birmingham industrial supplier is never cited by Copilot. The AI section finds the site was never added to Bing Webmaster Tools, a Bing sitemap error from a relative URL, and product specs only in PDFs. Fixes: verify in Bing, fix the sitemap, publish HTML spec tables. Bing indexing improves, and AI Performance starts showing citations for spec-related grounding queries.
```

Scene 9 — Keep scope tight

```text
A note on scope, so this doesn't balloon. The AI readiness section of a technical audit covers access, indexing, raw HTML facts, consistency, and a measurement baseline. It doesn't cover content strategy for AI answers, prompt panels, or earning third-party mentions. If the client needs those, recommend the AI search strategy work as a separate phase. Keeping the boundary clear protects your timeline and keeps the audit actionable.
```

Scene 10 — Reading the output

```text
How do you read the script's output? If robots says block for a bot your policy allows, that's a robots.txt bug, or an undocumented decision. If robots says allow, but the request is challenged, that's a CDN or firewall rule, so confirm it in the firewall events for verified IPs and write a ticket. If a training crawler your policy blocks still gets a two hundred, remember robots.txt only asks; if enforcement matters, use CDN rules. And if every bot gets the same result, your CDN may be judging your IP, not the user agent, so rely on the firewall events instead.
```

Scene 11 — Mistakes, recap, try this now

```text
Common mistakes. Treating AI readiness as separate from technical SEO. Recommending blanket AI bot blocks or allows without a business decision. And blaming the AI for wrong answers instead of fixing the sources it cites. Recap: six checks, a robots and challenge test confirmed with CDN events, accuracy checks traced to sources, and a write-up linked to root causes. Try this now: run the six checks on a site you know, even roughly, and write down the one that surprises you most.
```

</details>

### 14. The 2026 technical audit checklist and deliverables pack

Lesson `the-2026-technical-audit-checklist` · module “AI search readiness and the audit toolkit” · target 7 min · 12 scenes · 918 words · planned 6:39 (words ≈ 6:39) · status: **to produce**

| Term | Say |
|---|---|
| regex | rej-ex |
| curl | curl |
| N/A | not applicable |
| CrUX | crux |

| # | Time | Sec | Narration | On screen | Visual |
|---|---|---|---|---|---|
| 1 | 0:00 | 33 | You've now run a complete technical SEO audit on Kiran Home, from scoping to verification. In this final lesson, we'll distil everything into a master checklist for twenty twenty-six and a standard deliverables pack you can reuse on every client. You'll learn how to use the checklist to plan, run and quality-assure an audit, see the handiest regex and commands, and see how one agency turned it into a proportionate product for small and large sites alike. | The 2026 audit checklist<br>• Plan, run and QA with one checklist<br>• Regex and commands<br>• A reusable deliverables pack | A thick checklist document with nine tabbed sections animates open. Title appears. |
| 2 | 0:33 | 31 | First, how to use it. Use the checklist to plan: which checks apply to this site? Use it to run: what evidence proves each one? And use it to quality-assure your own deliverable before it goes to the client. Not every item applies to every site, so mark items as not applicable deliberately, rather than skipping them silently. That one habit makes your audits more consistent and much easier to defend. | How to use it<br>• Plan: what applies?<br>• Run: what evidence proves it?<br>• QA: before sending; mark N/A deliberately | Three icons (map, magnifier, stamp) over the checklist; some items get an 'N/A' tag rather than disappearing. |
| 3 | 1:04 | 40 | Sections one and two: access and crawling. Access means a Search Console Domain property with bulk export on, Bing Webmaster Tools with sitemaps and AI Performance, GA4 with an AI assistants channel, four or more weeks of logs, and a saved, versioned crawl config with the crawler allow-listed. Crawling means robots.txt returns two hundred on every host with no sitewide disallow and no blocked resources, AI crawler groups match a written policy with CDN rules aligned, verified Googlebot crawl share by segment is quantified, and server errors and response times are checked. | 1–2: Access and crawling<br>• GSC Domain + bulk export; Bing; GA4; logs<br>• robots.txt health; AI policy alignment<br>• Crawl share by segment; errors and speed | Checklist sections 1 and 2 expand; items tick with small icons. |
| 4 | 1:44 | 29 | Section three: indexing and duplication. Page indexing by sitemap segment, with the top not-indexed reasons explained by samples. Canonicals: one, absolute, self-referencing, in raw HTML, pointing to indexable two-hundred URLs. Redirects: one hop, permanent for permanent moves, no home-page dumping, and legacy URLs with links covered. Soft four-oh-fours by cause, with zero-result facets returning four-oh-four. And sitemaps with only canonical, indexable, two-hundred URLs, accurate lastmod, and segmentation. | 3: Indexing and duplication<br>• Page indexing by segment + samples<br>• Canonicals, redirects, soft 404s<br>• Clean, segmented sitemaps | Section 3 items tick; a mini sitemap icon splits into segments. |
| 5 | 2:13 | 34 | Section four: rendering and content access. A raw versus rendered diff per template for canonical, robots, content, links and JSON-LD. No content or links that only appear after interaction. HTML under Googlebot's two-megabyte limit with key content early. And no reliance on JavaScript to change indexing on non-two-hundred or noindexed pages. Section five: architecture and international. Sensible depth, no orphans, descriptive anchors, breadcrumbs, crawlable pagination, and hreflang that's complete, reciprocal, self-referencing and valid, with no forced IP redirects. | 4–5: Rendering, architecture, international<br>• Raw vs rendered; no interaction-only links; 2 MB<br>• Depth, orphans, pagination<br>• hreflang valid and reciprocal; no IP redirects | Sections 4 and 5 expand; a raw/rendered split icon and a globe icon tick. |
| 6 | 2:47 | 38 | Section six: structured data and feeds. The Rich Results Test per template with errors fixed, and no recommendations for retired types like FAQ or HowTo. JSON-LD that matches visible price, currency and availability, with one entity per fact source. And product feeds consistent with pages. Section seven: performance and page experience. CrUX seventy-fifth percentile metrics per template, phone first, with the dominant sub-part identified. Real-user monitoring with INP attribution on key templates. Mobile parity, clean HTTPS, a security header baseline, and security issues and manual actions checked. | 6–7: Structured data and performance<br>• Valid AND correct markup; feeds consistent<br>• CrUX by template; INP attribution RUM<br>• Mobile parity, HTTPS, security | Sections 6 and 7 expand with a JSON icon and three CWV gauges. |
| 7 | 3:25 | 43 | Section eight: AI search readiness. Key facts, like price, delivery, returns and eligibility, as text in raw HTML. Indexed in Google and Bing. An AI exposure baseline from Search Console's generative AI report, Bing's AI Performance and GA4. The Search generative AI control decision documented. And an accuracy spot-check of assistant answers, traced back to sources. Section nine is deliverables QA: evidence IDs and examples on every finding, typed and scored root causes with the top three stress-tested, tickets with binary criteria, an executive summary that leads with the answer, listed non-issues and caveats, and a verification plan. | 8–9: AI readiness and deliverables QA<br>• Facts as text; Google + Bing; AI baseline<br>• Evidence, scoring, tickets, summary<br>• Non-issues, caveats, verification plan | Sections 8 and 9 expand; a QA stamp hovers over the deliverables list. |
| 8 | 4:08 | 34 | Now the handy regex and commands from the lesson text. A non-brand filter using a negative lookahead for the brand name. A page filter for UAE product URLs. A pattern for facet and sort parameters. A pattern for legacy URL formats. And two curl one-liners: one that shows status, redirect location, X-Robots-Tag and Link headers, and one that extracts the raw canonical tag from the HTML. Keep them in a snippets file; you'll use them on every audit. | Regex and commands<br>• Non-brand, market, facet, legacy patterns<br>• curl for status + headers<br>• curl + grep for raw canonical | Regex lines type out with parts highlighted (lookahead, groups); a terminal runs the curl commands and prints headers. |
| 9 | 4:42 | 30 | The deliverables pack has five parts. A findings workbook with tabs for scope, crawl config, evidence log, findings, root causes, scores, non-issues and AI readiness. A roadmap with sprints, owners and leading indicators. Tickets in the client's tracker using the template. An executive summary: one page plus three to five slides. And a verification and monitoring plan with checks, dates, alerts and owners. Five artefacts, each with a clear audience. | The deliverables pack<br>• Workbook · Roadmap · Tickets<br>• Executive summary<br>• Verification & monitoring plan | Five document icons fan out, each labelled with its audience. |
| 10 | 5:12 | 30 | Worked example, illustrative. A Karachi agency turns this checklist and pack into its standard audit product. For a three-hundred-page Lahore law firm, it marks half the e-commerce and international items not applicable, completes the rest in four days, and delivers eight tickets. For a fifty-thousand-URL Dubai marketplace, it uses every section and a three-week timeline. Same structure, proportionate depth, and every deliverable passes the QA section before it's sent. | Example: one pack, two sizes (illustrative)<br>• 300-page law firm: 4 days, 8 tickets<br>• 50k-URL marketplace: 3 weeks, full checklist<br>• QA before sending, always | Two project cards side by side sharing the same checklist; one with many N/A tags, one with all sections active. |
| 11 | 5:42 | 22 | Common mistakes when using any checklist. Treating it as the audit, rather than a tool for thinking. Ticking items without evidence. Silently skipping items instead of marking them not applicable. And sending deliverables that haven't passed your own QA section. A checklist makes you consistent. Judgement and evidence make you valuable. | Checklist pitfalls<br>• Checklist ≠ audit<br>• Ticks without evidence<br>• Silent skips; unchecked deliverables | Mistake chips; a final card 'Consistency from the checklist, value from judgement'. |
| 12 | 6:04 | 35 | Recap and congratulations. You've scoped, crawled, gathered evidence, analysed logs, checked rendering and structured data, found root causes, prioritised, written tickets, worked with developers, verified fixes, reported honestly, and audited AI search readiness. Try this now: copy the checklist, mark the not-applicable items for your next audit, and run section nine against your last deliverable. For deeper study, continue with Technical SEO Mastery, AI Search Optimization, and, if your clients publish at scale, Programmatic SEO and AI Content at Scale. | Recap and next steps<br>• The full audit, end to end<br>• Run section 9 on your last deliverable<br>• Next courses: Mastery, AI Search, Programmatic SEO | Workshop journey map from Module 1 to Module 6 lights up; badge 'Certified Technical SEO Auditor' animates; three next-course cards appear. |

<details><summary>Narration per scene (paste into ElevenLabs)</summary>

Scene 1 — The 2026 audit checklist

```text
You've now run a complete technical SEO audit on Kiran Home, from scoping to verification. In this final lesson, we'll distil everything into a master checklist for twenty twenty-six and a standard deliverables pack you can reuse on every client. You'll learn how to use the checklist to plan, run and quality-assure an audit, see the handiest regex and commands, and see how one agency turned it into a proportionate product for small and large sites alike.
```

Scene 2 — How to use it

```text
First, how to use it. Use the checklist to plan: which checks apply to this site? Use it to run: what evidence proves each one? And use it to quality-assure your own deliverable before it goes to the client. Not every item applies to every site, so mark items as not applicable deliberately, rather than skipping them silently. That one habit makes your audits more consistent and much easier to defend.
```

Scene 3 — 1–2: Access and crawling

```text
Sections one and two: access and crawling. Access means a Search Console Domain property with bulk export on, Bing Webmaster Tools with sitemaps and AI Performance, GA4 with an AI assistants channel, four or more weeks of logs, and a saved, versioned crawl config with the crawler allow-listed. Crawling means robots.txt returns two hundred on every host with no sitewide disallow and no blocked resources, AI crawler groups match a written policy with CDN rules aligned, verified Googlebot crawl share by segment is quantified, and server errors and response times are checked.
```

Scene 4 — 3: Indexing and duplication

```text
Section three: indexing and duplication. Page indexing by sitemap segment, with the top not-indexed reasons explained by samples. Canonicals: one, absolute, self-referencing, in raw HTML, pointing to indexable two-hundred URLs. Redirects: one hop, permanent for permanent moves, no home-page dumping, and legacy URLs with links covered. Soft four-oh-fours by cause, with zero-result facets returning four-oh-four. And sitemaps with only canonical, indexable, two-hundred URLs, accurate lastmod, and segmentation.
```

Scene 5 — 4–5: Rendering, architecture, international

```text
Section four: rendering and content access. A raw versus rendered diff per template for canonical, robots, content, links and JSON-LD. No content or links that only appear after interaction. HTML under Googlebot's two-megabyte limit with key content early. And no reliance on JavaScript to change indexing on non-two-hundred or noindexed pages. Section five: architecture and international. Sensible depth, no orphans, descriptive anchors, breadcrumbs, crawlable pagination, and hreflang that's complete, reciprocal, self-referencing and valid, with no forced IP redirects.
```

Scene 6 — 6–7: Structured data and performance

```text
Section six: structured data and feeds. The Rich Results Test per template with errors fixed, and no recommendations for retired types like FAQ or HowTo. JSON-LD that matches visible price, currency and availability, with one entity per fact source. And product feeds consistent with pages. Section seven: performance and page experience. CrUX seventy-fifth percentile metrics per template, phone first, with the dominant sub-part identified. Real-user monitoring with INP attribution on key templates. Mobile parity, clean HTTPS, a security header baseline, and security issues and manual actions checked.
```

Scene 7 — 8–9: AI readiness and deliverables QA

```text
Section eight: AI search readiness. Key facts, like price, delivery, returns and eligibility, as text in raw HTML. Indexed in Google and Bing. An AI exposure baseline from Search Console's generative AI report, Bing's AI Performance and GA4. The Search generative AI control decision documented. And an accuracy spot-check of assistant answers, traced back to sources. Section nine is deliverables QA: evidence IDs and examples on every finding, typed and scored root causes with the top three stress-tested, tickets with binary criteria, an executive summary that leads with the answer, listed non-issues and caveats, and a verification plan.
```

Scene 8 — Regex and commands

```text
Now the handy regex and commands from the lesson text. A non-brand filter using a negative lookahead for the brand name. A page filter for UAE product URLs. A pattern for facet and sort parameters. A pattern for legacy URL formats. And two curl one-liners: one that shows status, redirect location, X-Robots-Tag and Link headers, and one that extracts the raw canonical tag from the HTML. Keep them in a snippets file; you'll use them on every audit.
```

Scene 9 — The deliverables pack

```text
The deliverables pack has five parts. A findings workbook with tabs for scope, crawl config, evidence log, findings, root causes, scores, non-issues and AI readiness. A roadmap with sprints, owners and leading indicators. Tickets in the client's tracker using the template. An executive summary: one page plus three to five slides. And a verification and monitoring plan with checks, dates, alerts and owners. Five artefacts, each with a clear audience.
```

Scene 10 — Example: one pack, two sizes (illustrative)

```text
Worked example, illustrative. A Karachi agency turns this checklist and pack into its standard audit product. For a three-hundred-page Lahore law firm, it marks half the e-commerce and international items not applicable, completes the rest in four days, and delivers eight tickets. For a fifty-thousand-URL Dubai marketplace, it uses every section and a three-week timeline. Same structure, proportionate depth, and every deliverable passes the QA section before it's sent.
```

Scene 11 — Checklist pitfalls

```text
Common mistakes when using any checklist. Treating it as the audit, rather than a tool for thinking. Ticking items without evidence. Silently skipping items instead of marking them not applicable. And sending deliverables that haven't passed your own QA section. A checklist makes you consistent. Judgement and evidence make you valuable.
```

Scene 12 — Recap and next steps

```text
Recap and congratulations. You've scoped, crawled, gathered evidence, analysed logs, checked rendering and structured data, found root causes, prioritised, written tickets, worked with developers, verified fixes, reported honestly, and audited AI search readiness. Try this now: copy the checklist, mark the not-applicable items for your next audit, and run section nine against your last deliverable. For deeper study, continue with Technical SEO Mastery, AI Search Optimization, and, if your clients publish at scale, Programmatic SEO and AI Content at Scale.
```

</details>
