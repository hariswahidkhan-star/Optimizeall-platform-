# Integrating AI Platforms: Claude, OpenAI, Gemini and Open Models via API — lecture production script

Course `ai-platform-apis-integration` · pack v1 · reviewed 2026-09 · level advanced · category ai

19 lectures · 24,120 narration words · planned runtime 2:54:44 (≈ 172 min at 140 wpm)

> Generated by `scripts/export-lecture-scripts.py` from the course pack — edit the pack, not this file. Production workflow: [README](README.md).

## Voice direction

Conversational expert, second person, ~140 wpm; precise and technical, calm confidence; spell out code identifiers when read aloud; audience level: advanced. Pause ~0.5 s between scenes; no music under narration (added in edit).

## Lectures

1. [The AI platform landscape: providers, APIs and how to choose](#1-the-ai-platform-landscape-providers-apis-and-how-to-choose)
2. [API keys, authentication and secrets management](#2-api-keys-authentication-and-secrets-management)
3. [Messages, roles and conversation state across Claude, OpenAI and Gemini](#3-messages-roles-and-conversation-state-across-claude-openai-and-gemini)
4. [Streaming responses to users and between services](#4-streaming-responses-to-users-and-between-services)
5. [Tool calling across Claude, OpenAI and Gemini](#5-tool-calling-across-claude-openai-and-gemini)
6. [Structured outputs: reliable JSON from every provider](#6-structured-outputs-reliable-json-from-every-provider)
7. [Multimodal inputs: images, PDFs and audio](#7-multimodal-inputs-images-pdfs-and-audio)
8. [Embeddings APIs and semantic search in production](#8-embeddings-apis-and-semantic-search-in-production)
9. [Batch APIs: bulk processing at a discount](#9-batch-apis-bulk-processing-at-a-discount)
10. [Prompt caching across providers](#10-prompt-caching-across-providers)
11. [Rate limits, errors, retries and backoff](#11-rate-limits-errors-retries-and-backoff)
12. [Cost tracking, attribution and budgets](#12-cost-tracking-attribution-and-budgets)
13. [Multi-provider abstraction and fallbacks](#13-multi-provider-abstraction-and-fallbacks)
14. [Model routing: the right model for each request](#14-model-routing-the-right-model-for-each-request)
15. [Cloud AI platforms: Amazon Bedrock, Microsoft Foundry and Gemini Enterprise Agent Platform](#15-cloud-ai-platforms-amazon-bedrock-microsoft-foundry-and-gemini-enterprise-agent-platform)
16. [Open-weight models via hosted APIs and self-hosting](#16-open-weight-models-via-hosted-apis-and-self-hosting)
17. [Security, data privacy and the production checklist](#17-security-data-privacy-and-the-production-checklist)
18. [Capstone part 1: build a provider-agnostic content-generation microservice](#18-capstone-part-1-build-a-provider-agnostic-content-generation-microservice)
19. [Capstone part 2: test, evaluate, deploy and ship a TypeScript client](#19-capstone-part-2-test-evaluate-deploy-and-ship-a-typescript-client)

### 1. The AI platform landscape: providers, APIs and how to choose

Lesson `provider-landscape-and-choosing` · module “Platform foundations” · target 9 min · 15 scenes · 1321 words · planned 9:35 (words ≈ 9:34) · status: **to produce**

| Term | Say |
|---|---|
| Qwen | chwen |
| gpt-oss | G P T O S S |
| vLLM | V L L M |
| Ollama | oh-lah-mah |

| # | Time | Sec | Narration | On screen | Visual |
|---|---|---|---|---|---|
| 1 | 0:00 | 27 | Picking an AI provider feels like picking a phone plan in a shop where the plans change every month. There are first party APIs, cloud platforms, open weight models and self hosting, and every vendor claims to be best. In this lesson, you'll get a clear map of the landscape and a decision method you can reuse every time a new model launches. | The AI platform landscape<br>• Four ways to access models<br>• What really differs<br>• A reusable decision method | Layout: a busy shop wall of plan cards flickering with changing prices, then the cards sort themselves into four labeled columns: First-party APIs, Cloud platforms, Open-weight hosted, Self-hosted. 0s: title 'The AI platform landscape' fades in top-left. 2s: main graphic begins animating. 5s, 9s, 13s: on-screen bullets reveal one by one, each highlighted as narrated. 23s: hold final frame. |
| 2 | 0:27 | 33 | Why does this matter? Because the provider decision touches everything downstream: quality, cost, speed, data privacy, and how painful it is to change your mind later. Think of it like choosing where to buy ingredients for a restaurant. The supplier's quality matters, but so do delivery times, contracts, food safety paperwork and whether you can switch suppliers if prices jump. The best restaurants don't marry a supplier; they design the kitchen so ingredients can change. | Why it matters<br>• Quality, cost, speed, privacy, flexibility<br>• The restaurant supplier analogy<br>• Design the kitchen for change | Layout: a restaurant kitchen with ingredient crates from three suppliers; labels appear on each crate: Quality, Delivery time, Contract, Safety papers. A 'switch supplier' lever flips smoothly. 0s: title 'Why it matters' fades in top-left. 2s: main graphic begins animating. 5s, 11s, 17s: on-screen bullets reveal one by one, each highlighted as narrated. 29s: hold final frame. |
| 3 | 1:00 | 39 | There are four ways to get model intelligence into your product. First party APIs from Anthropic, OpenAI and Google, where new models and features usually land first. Cloud AI platforms: Amazon Bedrock, Microsoft Foundry, which used to be called Azure AI Foundry, and Google's Gemini Enterprise Agent Platform, which used to be Vertex AI. They add enterprise contracts, identity controls, private networking and regional hosting. Open weight models like Llama, Mistral, Qwen, DeepSeek, Gemma and OpenAI's gpt oss, served by hosted providers. And self hosting on your own GPUs. | Four access routes<br>• First-party APIs<br>• Cloud platforms (Bedrock, Foundry, Gemini Enterprise Agent Platform)<br>• Hosted open-weight models<br>• Self-hosting | Layout: four lanes on a road diagram converging on a product icon. Each lane lights up in sequence with its label and two or three example logos rendered as neutral text chips (no brand logos). 0s: title 'Four access routes' fades in top-left. 2s: main graphic begins animating. 5s, 11s, 17s, 23s: on-screen bullets reveal one by one, each highlighted as narrated. 35s: hold final frame. |
| 4 | 1:39 | 32 | What actually differs between providers? Seven things. Quality on your tasks, which public benchmarks rarely predict. Features, like tool use, structured outputs, vision, audio, caching and batch. Price per token, cached input and batch discounts. Latency and rate limits. Data terms: training use, retention and zero retention options. Availability: regions, platforms and service levels. And the ecosystem around the SDKs. No provider wins all seven, which is why the answer is usually a mix. | Seven differences<br>• Quality • Features • Price<br>• Latency • Data terms<br>• Availability • Ecosystem | Layout: a seven-row comparison grid animates in row by row with three anonymous provider columns; cells fill with green, amber and red dots showing that no column is all green. 0s: title 'Seven differences' fades in top-left. 2s: main graphic begins animating. 5s, 11s, 17s: on-screen bullets reveal one by one, each highlighted as narrated. 28s: hold final frame. |
| 5 | 2:11 | 36 | A quick word on the current families, with a warning that names change every few months. Claude has Opus, Sonnet and Haiku lines plus a top tier Fable line, with very long context on current top models. OpenAI's GPT five series runs through the Responses API, its recommended interface, with the older Chat Completions API still supported. Gemini has Pro and Flash tiers with latest aliases and strong multimodal input. The durable lesson: treat model ids as configuration, never as constants in code. | Current families (verify!)<br>• Claude: Opus, Sonnet, Haiku, Fable<br>• OpenAI: GPT-5 series, Responses API<br>• Gemini: Pro, Flash, -latest aliases<br>• Model IDs = configuration | Layout: three family cards with tier bars (large, medium, small). A config file panel slides in showing CLAUDE_MODEL, OPENAI_MODEL, GEMINI_MODEL lines, with a 'check docs' sticky note. 0s: title 'Current families (verify!)' fades in top-left. 2s: main graphic begins animating. 5s, 10s, 15s, 20s: on-screen bullets reveal one by one, each highlighted as narrated. 32s: hold final frame. |
| 6 | 2:47 | 47 | Let's pause on the cloud platforms, because they confuse a lot of people. When you use Claude through Amazon Bedrock, or a GPT model through Microsoft Foundry, you're still using the same underlying model family, but the platform wraps it in its own identity system, billing, networking and regions. Here's the key idea: the model is the engine, and the platform is the car around it. The engine might be identical, but the dashboard, the insurance and where you're allowed to drive can differ. Sometimes a brand new feature reaches the first party API weeks before it reaches a cloud platform, so always check the platform's own feature list. | Engine vs car<br>• Same model family, different platform wrapper<br>• Identity, billing, networking, regions differ<br>• Features can arrive later on platforms | Layout: an engine block labeled 'Model' drops into three different car bodies labeled First-party API, Bedrock, Foundry. Each car's dashboard lights up different gauges: IAM, Billing, Region. 0s: title 'Engine vs car' fades in top-left. 2s: main graphic begins animating. 5s, 16s, 27s: on-screen bullets reveal one by one, each highlighted as narrated. 43s: hold final frame. |
| 7 | 3:34 | 38 | Here's a simple example. You want AI to write short product descriptions for a craft shop. You collect thirty real products, write what a good description looks like, and try three models: one from each provider. You score each output from one to five, note the cost per description and the time taken. Maybe the cheapest model scores four point two and the priciest scores four point four. For product blurbs, that small gain may not justify three times the price. The eval made the decision obvious. | Simple example: product blurbs<br>• 30 real products, 1–5 rubric<br>• Three models, one per provider<br>• Small quality gain vs 3× price (illustrative) | Layout: a table with three model columns and rows Quality, Cost per description, Seconds. Numbers count up (illustrative). A highlight box wraps the best value column. 0s: title 'Simple example: product blurbs' fades in top-left. 2s: main graphic begins animating. 5s, 13s, 21s: on-screen bullets reveal one by one, each highlighted as narrated. 34s: hold final frame. |
| 8 | 4:12 | 40 | Now a realistic business example, with illustrative numbers. A company in Lahore builds online stores for three hundred small merchants. It needs product copy in English and Urdu, support replies grounded in each store's policies, and invoice data pulled from photos. Their eval shows one provider's mid tier model writes the best Urdu, another provider's small model is cheapest for invoice extraction, and a larger model handles multi turn support best. Because bank partners need contractual data controls, support traffic runs through a cloud platform contract. One internal gateway routes each task. | Business example: Lahore storefront builder<br>• Urdu copy → provider A mid-tier<br>• Invoice extraction → provider B small model<br>• Support → larger model via cloud contract<br>• One gateway, per-task routing | Layout: three task cards (Urdu copy, Invoice photos, Support chat) on the left; arrows route through a central 'Gateway' box to three different model cards on the right; a padlock. 0s: title 'Business example: Lahore storefront builder' fades in top-left. 2s: main graphic begins animating. 5s, 11s, 17s, 23s: on-screen bullets reveal one by one, each highlighted as narrated. 36s: hold final frame. |
| 9 | 4:52 | 33 | The decision method in six steps. List your tasks. Define quality with thirty to a hundred real examples and a scoring method. Shortlist two or three models across providers and price points. Run the eval and compare quality, latency and cost per completed task. Check constraints like data residency, contracts and compliance. And design for change, with an abstraction layer so switching is a configuration change, not a rewrite. Repeat whenever a new model launches. | Six-step method<br>• Tasks → quality definition<br>• Shortlist → eval<br>• Constraints → design for change<br>• Repeat on each launch | Layout: a six-step circular process diagram; each step lights up in order, ending with an arrow looping back to 'Shortlist' labeled 'new model launched'. 0s: title 'Six-step method' fades in top-left. 2s: main graphic begins animating. 5s, 10s, 15s, 20s: on-screen bullets reveal one by one, each highlighted as narrated. 29s: hold final frame. |
| 10 | 5:25 | 39 | How big should your eval be? For a first decision, thirty to fifty examples per task is usually enough to spot big differences. If two models are within a few points, add more examples before you decide, or pick the cheaper one and keep watching. Include the awkward cases too: long inputs, mixed languages like English with Urdu or Arabic, messy formatting and questions the model should refuse or escalate. Those edge cases are where models actually differ, and they're exactly what a public benchmark never tests for your business. | How big an eval?<br>• 30–50 examples per task to start<br>• Close call? Add examples or pick cheaper<br>• Include messy and mixed-language cases | Layout: a dataset card grows from 30 to 50 rows; highlighted rows labeled 'long input', 'EN + UR mix', 'messy PDF', 'should refuse' pulse in amber. 0s: title 'How big an eval?' fades in top-left. 2s: main graphic begins animating. 5s, 13s, 21s: on-screen bullets reveal one by one, each highlighted as narrated. 35s: hold final frame. |
| 11 | 6:04 | 27 | Common mistakes. Choosing from benchmark headlines instead of your own data. Hard coding model ids, then scrambling when a model is retired. Leaving data terms until legal review blocks the launch. And assuming every feature exists on every cloud platform's version of a model. Features often arrive on first party APIs first, so check each platform's feature table before you commit. | Common mistakes<br>• Benchmark-driven choices<br>• Hard-coded model IDs<br>• Late data-terms review<br>• Assuming feature parity across platforms | Layout: four mistake cards flip over one at a time, each revealing a one-line fix beneath. 0s: title 'Common mistakes' fades in top-left. 2s: main graphic begins animating. 5s, 8s, 11s, 14s: on-screen bullets reveal one by one, each highlighted as narrated. 23s: hold final frame. |
| 12 | 6:31 | 36 | One more habit that separates professionals from hobbyists: keep a simple decision log. For each task, write the date, the models you compared, the eval scores, the cost per completed task, and why you chose what you chose. When a new model launches in three months, you rerun the same eval and add a line. When a stakeholder asks why you're using a particular provider, you have evidence instead of opinions. It takes five minutes per decision and saves hours of debate later. | Keep a decision log<br>• Date, candidates, scores, cost<br>• Reason for the choice<br>• Rerun and append on each launch | Layout: a notebook page titled 'Model decisions' fills with dated rows; a new row is appended with a 'new model launched' tag and updated scores. 0s: title 'Keep a decision log' fades in top-left. 2s: main graphic begins animating. 5s, 12s, 19s: on-screen bullets reveal one by one, each highlighted as narrated. 32s: hold final frame. |
| 13 | 7:07 | 47 | Let's deepen the Lahore storefront example with illustrative numbers. The company ran thirty products per merchant category through three models. For Urdu product copy, native speakers rated one provider's mid tier model clearly best. For invoice extraction from phone photos, a small model from another provider was accurate enough and several times cheaper per invoice. For multi turn support, only a larger model handled policy questions reliably. Instead of one provider for everything, they used three routes behind one gateway. Their monthly AI bill ended up lower than the single premium model plan they had first budgeted, and quality improved where customers actually noticed it: the Urdu copy. | Deeper: the Lahore storefront choice (illustrative)<br>• Urdu copy → best-rated mid-tier model<br>• Invoices → small, cheap, accurate enough<br>• Support → larger model<br>• Lower bill than one premium model | Layout: three task rows. 0s: 'Urdu copy' row shows three model scores; one highlights. 10s: 'Invoices' row highlights the cheapest adequate model. 20s: 'Support' row highlights the larger model. 30s: a budget bar compares 'single premium' vs 'three routes' (illustrative). 42s: hold. |
| 14 | 7:54 | 68 | Watch me do it. Let's walk through the first calls script. I import the three SDKs. The prompt asks for a twenty word description of a handmade Multani blue pottery mug. For Claude, I create the client, which reads the API key from the environment, and call messages create with the model from an environment variable, max tokens three hundred, and one user message. I join the text blocks and print them. For OpenAI, I create the client and call responses create with the model and the prompt as input, then print output text. For Gemini, I create the client, which reads the Gemini API key, and call models generate content with the model and contents, then print dot text. I run it: three short descriptions appear. Notice three different call shapes and three different ways to read the answer, but the same idea every time. That's exactly what we'll normalize later in the adapter lesson. | Watch me do it: first calls<br>• Claude: messages.create → join text blocks<br>• OpenAI: responses.create → output_text<br>• Gemini: generate_content → .text<br>• Models from env vars | Layout: code editor top, terminal bottom. 0s: imports and PROMPT highlight. 10s: the Claude block highlights; terminal prints 'Claude: …'. 24s: the OpenAI block highlights; terminal prints 'OpenAI: …'. 36s: the Gemini block highlights; terminal prints 'Gemini: …'. 50s: the three read-the-answer lines glow in different colors. 64s: hold. |
| 15 | 9:02 | 33 | Quick recap. There are four routes to model intelligence. Providers differ on seven dimensions, and none wins them all. Model ids are configuration. And your own eval, measured as cost per completed task, beats any leaderboard. Try this now: run the three first calls in the lesson's code, one per provider, using your own prompt. Then list three tasks from your product and pick two candidate models for each. That's the start of your eval plan. | Recap + try this now<br>• Four routes, seven differences<br>• IDs as config, eval-driven choice<br>• Run the three first calls<br>• Shortlist two models per task | Layout: a recap card with four icons; then a terminal shows three short outputs labeled Claude, OpenAI, Gemini appearing one after another. 0s: title 'Recap + try this now' fades in top-left. 2s: main graphic begins animating. 5s, 10s, 15s, 20s: on-screen bullets reveal one by one, each highlighted as narrated. 29s: hold final frame. |

<details><summary>Narration per scene (paste into ElevenLabs)</summary>

Scene 1 — The AI platform landscape

```text
Picking an AI provider feels like picking a phone plan in a shop where the plans change every month. There are first party APIs, cloud platforms, open weight models and self hosting, and every vendor claims to be best. In this lesson, you'll get a clear map of the landscape and a decision method you can reuse every time a new model launches.
```

Scene 2 — Why it matters

```text
Why does this matter? Because the provider decision touches everything downstream: quality, cost, speed, data privacy, and how painful it is to change your mind later. Think of it like choosing where to buy ingredients for a restaurant. The supplier's quality matters, but so do delivery times, contracts, food safety paperwork and whether you can switch suppliers if prices jump. The best restaurants don't marry a supplier; they design the kitchen so ingredients can change.
```

Scene 3 — Four access routes

```text
There are four ways to get model intelligence into your product. First party APIs from Anthropic, OpenAI and Google, where new models and features usually land first. Cloud AI platforms: Amazon Bedrock, Microsoft Foundry, which used to be called Azure AI Foundry, and Google's Gemini Enterprise Agent Platform, which used to be Vertex AI. They add enterprise contracts, identity controls, private networking and regional hosting. Open weight models like Llama, Mistral, Qwen, DeepSeek, Gemma and OpenAI's gpt oss, served by hosted providers. And self hosting on your own GPUs.
```

Scene 4 — Seven differences

```text
What actually differs between providers? Seven things. Quality on your tasks, which public benchmarks rarely predict. Features, like tool use, structured outputs, vision, audio, caching and batch. Price per token, cached input and batch discounts. Latency and rate limits. Data terms: training use, retention and zero retention options. Availability: regions, platforms and service levels. And the ecosystem around the SDKs. No provider wins all seven, which is why the answer is usually a mix.
```

Scene 5 — Current families (verify!)

```text
A quick word on the current families, with a warning that names change every few months. Claude has Opus, Sonnet and Haiku lines plus a top tier Fable line, with very long context on current top models. OpenAI's GPT five series runs through the Responses API, its recommended interface, with the older Chat Completions API still supported. Gemini has Pro and Flash tiers with latest aliases and strong multimodal input. The durable lesson: treat model ids as configuration, never as constants in code.
```

Scene 6 — Engine vs car

```text
Let's pause on the cloud platforms, because they confuse a lot of people. When you use Claude through Amazon Bedrock, or a GPT model through Microsoft Foundry, you're still using the same underlying model family, but the platform wraps it in its own identity system, billing, networking and regions. Here's the key idea: the model is the engine, and the platform is the car around it. The engine might be identical, but the dashboard, the insurance and where you're allowed to drive can differ. Sometimes a brand new feature reaches the first party API weeks before it reaches a cloud platform, so always check the platform's own feature list.
```

Scene 7 — Simple example: product blurbs

```text
Here's a simple example. You want AI to write short product descriptions for a craft shop. You collect thirty real products, write what a good description looks like, and try three models: one from each provider. You score each output from one to five, note the cost per description and the time taken. Maybe the cheapest model scores four point two and the priciest scores four point four. For product blurbs, that small gain may not justify three times the price. The eval made the decision obvious.
```

Scene 8 — Business example: Lahore storefront builder

```text
Now a realistic business example, with illustrative numbers. A company in Lahore builds online stores for three hundred small merchants. It needs product copy in English and Urdu, support replies grounded in each store's policies, and invoice data pulled from photos. Their eval shows one provider's mid tier model writes the best Urdu, another provider's small model is cheapest for invoice extraction, and a larger model handles multi turn support best. Because bank partners need contractual data controls, support traffic runs through a cloud platform contract. One internal gateway routes each task.
```

Scene 9 — Six-step method

```text
The decision method in six steps. List your tasks. Define quality with thirty to a hundred real examples and a scoring method. Shortlist two or three models across providers and price points. Run the eval and compare quality, latency and cost per completed task. Check constraints like data residency, contracts and compliance. And design for change, with an abstraction layer so switching is a configuration change, not a rewrite. Repeat whenever a new model launches.
```

Scene 10 — How big an eval?

```text
How big should your eval be? For a first decision, thirty to fifty examples per task is usually enough to spot big differences. If two models are within a few points, add more examples before you decide, or pick the cheaper one and keep watching. Include the awkward cases too: long inputs, mixed languages like English with Urdu or Arabic, messy formatting and questions the model should refuse or escalate. Those edge cases are where models actually differ, and they're exactly what a public benchmark never tests for your business.
```

Scene 11 — Common mistakes

```text
Common mistakes. Choosing from benchmark headlines instead of your own data. Hard coding model ids, then scrambling when a model is retired. Leaving data terms until legal review blocks the launch. And assuming every feature exists on every cloud platform's version of a model. Features often arrive on first party APIs first, so check each platform's feature table before you commit.
```

Scene 12 — Keep a decision log

```text
One more habit that separates professionals from hobbyists: keep a simple decision log. For each task, write the date, the models you compared, the eval scores, the cost per completed task, and why you chose what you chose. When a new model launches in three months, you rerun the same eval and add a line. When a stakeholder asks why you're using a particular provider, you have evidence instead of opinions. It takes five minutes per decision and saves hours of debate later.
```

Scene 13 — Deeper: the Lahore storefront choice (illustrative)

```text
Let's deepen the Lahore storefront example with illustrative numbers. The company ran thirty products per merchant category through three models. For Urdu product copy, native speakers rated one provider's mid tier model clearly best. For invoice extraction from phone photos, a small model from another provider was accurate enough and several times cheaper per invoice. For multi turn support, only a larger model handled policy questions reliably. Instead of one provider for everything, they used three routes behind one gateway. Their monthly AI bill ended up lower than the single premium model plan they had first budgeted, and quality improved where customers actually noticed it: the Urdu copy.
```

Scene 14 — Watch me do it: first calls

```text
Watch me do it. Let's walk through the first calls script. I import the three SDKs. The prompt asks for a twenty word description of a handmade Multani blue pottery mug. For Claude, I create the client, which reads the API key from the environment, and call messages create with the model from an environment variable, max tokens three hundred, and one user message. I join the text blocks and print them. For OpenAI, I create the client and call responses create with the model and the prompt as input, then print output text. For Gemini, I create the client, which reads the Gemini API key, and call models generate content with the model and contents, then print dot text. I run it: three short descriptions appear. Notice three different call shapes and three different ways to read the answer, but the same idea every time. That's exactly what we'll normalize later in the adapter lesson.
```

Scene 15 — Recap + try this now

```text
Quick recap. There are four routes to model intelligence. Providers differ on seven dimensions, and none wins them all. Model ids are configuration. And your own eval, measured as cost per completed task, beats any leaderboard. Try this now: run the three first calls in the lesson's code, one per provider, using your own prompt. Then list three tasks from your product and pick two candidate models for each. That's the start of your eval plan.
```

</details>

### 2. API keys, authentication and secrets management

Lesson `api-keys-auth-and-secrets` · module “Platform foundations” · target 9 min · 15 scenes · 1250 words · planned 9:02 (words ≈ 9:03) · status: **to produce**

| Term | Say |
|---|---|
| SigV4 | sig V four |
| Entra | en-tra |
| gitleaks | git leaks |

| # | Time | Sec | Narration | On screen | Visual |
|---|---|---|---|---|---|
| 1 | 0:00 | 30 | Here's a story that repeats every week somewhere. A developer pushes code to a public repository with an AI API key inside. Within minutes, automated scanners find it, and by morning there's a bill for thousands of dollars in someone else's compute. In this lesson, you'll learn how to authenticate to AI platforms safely, organize keys, and make sure a leak is a small problem instead of a crisis. | API keys and secrets<br>• Why keys are dangerous<br>• Authentication options<br>• Storing, rotating, scanning | Layout: a code commit animation pushes to a public repo; a radar sweep finds a highlighted key string; a bill counter spins upward in red. 0s: title 'API keys and secrets' fades in top-left. 2s: main graphic begins animating. 5s, 10s, 15s: on-screen bullets reveal one by one, each highlighted as narrated. 26s: hold final frame. |
| 2 | 0:30 | 30 | Why does this matter so much? Because an API key is a bearer credential, like cash. Whoever holds it can spend it, no questions asked. Think of your house keys. You wouldn't tape a copy to the front door, give the same key to every contractor, or never change the locks after losing one. Yet that's exactly how many teams treat AI keys: one shared key, pasted everywhere, never rotated. | Why it matters<br>• Bearer credential = cash<br>• The house-keys analogy<br>• Shared, pasted, never rotated | Layout: a key ring with one master key being copied to many hands; then the scene resets to individual labeled keys, a lockbox and a 'change locks' calendar icon. 0s: title 'Why it matters' fades in top-left. 2s: main graphic begins animating. 5s, 10s, 15s: on-screen bullets reveal one by one, each highlighted as narrated. 26s: hold final frame. |
| 3 | 1:00 | 37 | Each platform authenticates differently. The Claude API uses workspace API keys, plus options like OAuth profiles through its command line tool and workload identity federation for cloud services. OpenAI uses project scoped keys, service accounts, and workload identity in its current SDKs. The Gemini Developer API uses an API key, while Google's enterprise platform uses cloud identity and application default credentials. Bedrock uses AWS identity, and Microsoft Foundry supports keys or Entra ID. The rule: for servers, prefer short lived identity based credentials over static keys. | Auth by platform<br>• Claude: workspace keys, OAuth, WIF<br>• OpenAI: project keys, service accounts, workload identity<br>• Gemini: API key or Google Cloud IAM<br>• Bedrock: AWS IAM • Foundry: keys or Entra ID | Layout: a grid of platform tiles, each flipping to show its credential type icon (key, ID badge, cloud role). A banner slides across: 'Servers: prefer short-lived identity'. 0s: title 'Auth by platform' fades in top-left. 2s: main graphic begins animating. 5s, 11s, 17s, 23s: on-screen bullets reveal one by one, each highlighted as narrated. 33s: hold final frame. |
| 4 | 1:37 | 46 | Let's make workload identity concrete, because it sounds more complicated than it is. Instead of storing a long lived key, your service proves who it is using the identity its cloud already gives it, like a Kubernetes service account or an Azure managed identity. The AI platform trusts that identity and hands back a short lived token that expires on its own. Think of a hotel key card that stops working at checkout, versus a metal key that works forever. If someone steals the card after checkout, it's useless. Current OpenAI and Anthropic SDKs both support identity federation for servers; check their docs for your cloud. | Workload identity, simply<br>• Service proves identity via its cloud<br>• Gets a short-lived token<br>• Hotel card vs metal key | Layout: a container icon shows an ID badge to a gate labeled 'AI platform'; the gate issues a token card with a countdown timer that reaches zero and turns gray. 0s: title 'Workload identity, simply' fades in top-left. 2s: main graphic begins animating. 5s, 16s, 27s: on-screen bullets reveal one by one, each highlighted as narrated. 42s: hold final frame. |
| 5 | 2:23 | 33 | Organize before you scale. Create separate projects or workspaces for each environment, development, staging and production, and for each product or client. Set spend limits and alerts where the platform allows. Give each service its own key or identity. Restrict who can create keys, and review them every quarter. It feels like admin work, but it's what lets you answer two questions instantly: what did this client cost us, and what breaks if this key leaks? | Organize keys<br>• Projects per environment and client<br>• Spend limits and alerts<br>• One identity per service<br>• Quarterly review | Layout: an org chart of projects (Client A dev/prod, Client B dev/prod), each with a small key icon and a budget gauge; a calendar icon marks 'quarterly review'. 0s: title 'Organize keys' fades in top-left. 2s: main graphic begins animating. 5s, 10s, 15s, 20s: on-screen bullets reveal one by one, each highlighted as narrated. 29s: hold final frame. |
| 6 | 2:56 | 34 | Where should keys live? In development, a dot env file loaded by a small library and listed in your git ignore file. In production, a secret manager, like AWS Secrets Manager, Azure Key Vault, Google Secret Manager or Vault, injected as environment variables. In CI, the platform's encrypted secrets. And the golden rule: never put provider keys in a browser or mobile app. Front end code is public. Your backend holds the key, authenticates your users and enforces budgets. | Where keys live<br>• Dev: .env + .gitignore<br>• Prod: secret manager<br>• CI: encrypted secrets<br>• Never in browsers or apps | Layout: three environment boxes (Laptop, CI, Production) each receiving a key from a vault icon; a browser window tries to receive a key and is blocked by a red shield. 0s: title 'Where keys live' fades in top-left. 2s: main graphic begins animating. 5s, 10s, 15s, 20s: on-screen bullets reveal one by one, each highlighted as narrated. 30s: hold final frame. |
| 7 | 3:30 | 30 | A simple example. You're building a small chatbot for your portfolio site. The tempting shortcut is calling the model API straight from your JavaScript with the key embedded. Instead, you write a tiny backend endpoint that receives the user's message, checks a rate limit, calls the model with the key from an environment variable, and returns the reply. Ten extra lines of code, and your key never leaves your server. | Simple example: portfolio chatbot<br>• Shortcut: key in JavaScript (leaks)<br>• Right way: tiny backend endpoint<br>• Rate limit + env-var key | Layout: left, a browser bundle opened in dev tools revealing a key in red. Right, a request flows Browser → 'your /chat endpoint' → Model API, with the key icon shown only inside the server box. 0s: title 'Simple example: portfolio chatbot' fades in top-left. 2s: main graphic begins animating. 5s, 10s, 15s: on-screen bullets reveal one by one, each highlighted as narrated. 26s: hold final frame. |
| 8 | 4:00 | 35 | Now a realistic business example. A Dubai agency with forty client projects used one shared key for every developer and client. A contractor's laptop was lost, and the monthly bill spiked. Their fix: one project per client and environment with spend alerts, workload identity for production services, personal low limit keys for developers, secret scanning on every repository, and automatic quarterly rotation. Now a leaked key has a small blast radius, and they can bill clients accurately for AI usage. | Business example: Dubai agency<br>• One shared key → leak + bill spike<br>• Per-client projects + alerts<br>• Workload identity in prod<br>• Scanning + rotation | Layout: before/after. Before: one giant key connected to 40 client icons, with a red alarm. After: 40 small keys each inside its own project box with a budget gauge; a scanner beam sweeps a. 0s: title 'Business example: Dubai agency' fades in top-left. 2s: main graphic begins animating. 5s, 10s, 15s, 20s: on-screen bullets reveal one by one, each highlighted as narrated. 31s: hold final frame. |
| 9 | 4:35 | 38 | What should you do in the first hour after a key leaks? First, revoke the key in the provider console, immediately, before investigating. Second, create a replacement and deploy it through your secret manager. Third, check usage dashboards for unusual spend or activity since the leak. Fourth, find how it leaked: a commit, a log, a screenshot, a shared document, and close that path. Finally, write two lines in your incident log. Teams that practice this once, with a fake key, handle the real thing calmly in minutes. | First hour after a leak<br>• Revoke immediately<br>• Replace via secret manager<br>• Check usage since leak<br>• Close the leak path, log it | Layout: a stopwatch labeled '60 min' with five checklist steps ticking in sequence; a red key icon turns gray on 'Revoke'. 0s: title 'First hour after a leak' fades in top-left. 2s: main graphic begins animating. 5s, 11s, 17s, 23s: on-screen bullets reveal one by one, each highlighted as narrated. 34s: hold final frame. |
| 10 | 5:13 | 26 | The lesson's code shows two small safety nets. It refuses to start if required secrets are missing, with a helpful message. And it redacts anything that looks like an API key before logging. Key formats change, so treat patterns as a safety net, not a guarantee. Pair them with a secret scanner in CI that blocks merges containing keys. | Hands-on safety nets<br>• Fail fast on missing secrets<br>• Redact key-like strings in logs<br>• Secret scanning in CI | Layout: a code editor highlights the REQUIRED list and the redact function; a log line transforms from showing a key to '[REDACTED_KEY]'. 0s: title 'Hands-on safety nets' fades in top-left. 2s: main graphic begins animating. 5s, 9s, 13s: on-screen bullets reveal one by one, each highlighted as narrated. 22s: hold final frame. |
| 11 | 5:39 | 23 | Common mistakes to avoid. Keys in front end bundles or mobile apps. One key for every environment and team. Keys pasted into chat tools, tickets, or even prompts. And no spend limits, so a leak becomes a huge bill before anyone notices. Each of these is cheap to prevent and expensive to discover. | Common mistakes<br>• Keys in front ends<br>• One key everywhere<br>• Keys in chats, tickets, prompts<br>• No spend limits | Layout: four warning tiles appear with a small price tag on each labeled 'cheap to prevent'. 0s: title 'Common mistakes' fades in top-left. 2s: main graphic begins animating. 5s, 7s, 9s, 11s: on-screen bullets reveal one by one, each highlighted as narrated. 19s: hold final frame. |
| 12 | 6:02 | 35 | A final practical tip: make the secure path the easy path. Give developers a one line command or script that fetches their personal development key from the secret manager into their local environment. Provide a project template that already has the dot env file ignored, the secret scanner configured, and a backend endpoint pattern for model calls. When doing the right thing takes less effort than the shortcut, people stop taking shortcuts, and you stop finding keys in chat threads. | Make secure the easy path<br>• One-command dev key setup<br>• Project template with safeguards<br>• Backend endpoint pattern ready | Layout: two paths diverge: a steep rocky path labeled 'Shortcut' and a smooth paved path labeled 'Template + one command'. A developer icon chooses the paved path. 0s: title 'Make secure the easy path' fades in top-left. 2s: main graphic begins animating. 5s, 12s, 19s: on-screen bullets reveal one by one, each highlighted as narrated. 31s: hold final frame. |
| 13 | 6:37 | 48 | Let's deepen the Dubai agency's cleanup with illustrative numbers. They found keys in three repositories, two shared documents and one chat channel. After the reorganisation, forty client projects each had their own workspace and spend alert. Two months later, a contractor accidentally pasted a development key into a public gist. The secret scanner alerted within minutes, the key was revoked in the provider console, and because it was a low limit development key in one client's project, the potential spend was capped at a few dollars. Finance could also, for the first time, invoice AI usage per client accurately, which more than paid for the half day the cleanup took. | Deeper: the Dubai cleanup (illustrative)<br>• Keys found in 3 repos, 2 docs, 1 chat<br>• 40 client workspaces + alerts<br>• Later leak: detected in minutes, capped spend<br>• Per-client AI invoicing | Layout: an audit map. 0s: key icons appear in repo, doc and chat locations. 8s: they move into 40 small workspace boxes with alert bells. 18s: a gist icon leaks a dev key; a scanner alert fires; a 'revoked' stamp lands. 27s: a spend cap shows a few dollars. 35s: an invoice per client appears. 45s: hold. |
| 14 | 7:25 | 67 | Watch me do it. Let's walk through the safe loading script. First, load dot env reads a local dot env file, which I only use in development; in production the variables come from the secret manager. Next, a required list names the three provider keys, and I compute which are missing. If any are, the script exits immediately with a message naming them and saying where to set them. That's much better than failing later with a confusing authentication error. Then the redaction helper: a list of patterns for Anthropic, OpenAI and Google style keys. Redact loops over them and replaces any match with redacted key. I call it on a debug line containing a fake key, and the output shows the placeholder instead. Every log call in the service goes through this function. Finally, I'd add a secret scanner to CI so a key never reaches the main branch in the first place. | Watch me do it: safe key loading<br>• load_dotenv() for dev only<br>• Fail fast on missing secrets<br>• redact() before logging<br>• Secret scanning in CI | Layout: code editor top, terminal bottom. 0s: load_dotenv highlights with a 'dev only' tag. 10s: REQUIRED and the missing check highlight; terminal shows a friendly exit message. 26s: KEY_PATTERNS highlight. 36s: redact() runs; a key string morphs into [REDACTED_KEY]. 50s: a CI strip adds a 'secret scan' step. 62s: hold. |
| 15 | 8:32 | 30 | Quick recap. Keys are bearer credentials. Prefer identity based access for servers, separate projects by environment and client, store secrets in a manager, never ship keys to front ends, and automate scanning and rotation. Try this now: find every place your AI keys live today. Move one service to a secret manager or workload identity, add secret scanning to one repository, and set a spend alert on your production project. | Recap + try this now<br>• Audit where keys live<br>• Move one service to a manager/identity<br>• Add scanning + a spend alert | Layout: a checklist with three items ticking green; a small vault icon closes with a satisfying lock animation. 0s: title 'Recap + try this now' fades in top-left. 2s: main graphic begins animating. 5s, 10s, 15s: on-screen bullets reveal one by one, each highlighted as narrated. 26s: hold final frame. |

<details><summary>Narration per scene (paste into ElevenLabs)</summary>

Scene 1 — API keys and secrets

```text
Here's a story that repeats every week somewhere. A developer pushes code to a public repository with an AI API key inside. Within minutes, automated scanners find it, and by morning there's a bill for thousands of dollars in someone else's compute. In this lesson, you'll learn how to authenticate to AI platforms safely, organize keys, and make sure a leak is a small problem instead of a crisis.
```

Scene 2 — Why it matters

```text
Why does this matter so much? Because an API key is a bearer credential, like cash. Whoever holds it can spend it, no questions asked. Think of your house keys. You wouldn't tape a copy to the front door, give the same key to every contractor, or never change the locks after losing one. Yet that's exactly how many teams treat AI keys: one shared key, pasted everywhere, never rotated.
```

Scene 3 — Auth by platform

```text
Each platform authenticates differently. The Claude API uses workspace API keys, plus options like OAuth profiles through its command line tool and workload identity federation for cloud services. OpenAI uses project scoped keys, service accounts, and workload identity in its current SDKs. The Gemini Developer API uses an API key, while Google's enterprise platform uses cloud identity and application default credentials. Bedrock uses AWS identity, and Microsoft Foundry supports keys or Entra ID. The rule: for servers, prefer short lived identity based credentials over static keys.
```

Scene 4 — Workload identity, simply

```text
Let's make workload identity concrete, because it sounds more complicated than it is. Instead of storing a long lived key, your service proves who it is using the identity its cloud already gives it, like a Kubernetes service account or an Azure managed identity. The AI platform trusts that identity and hands back a short lived token that expires on its own. Think of a hotel key card that stops working at checkout, versus a metal key that works forever. If someone steals the card after checkout, it's useless. Current OpenAI and Anthropic SDKs both support identity federation for servers; check their docs for your cloud.
```

Scene 5 — Organize keys

```text
Organize before you scale. Create separate projects or workspaces for each environment, development, staging and production, and for each product or client. Set spend limits and alerts where the platform allows. Give each service its own key or identity. Restrict who can create keys, and review them every quarter. It feels like admin work, but it's what lets you answer two questions instantly: what did this client cost us, and what breaks if this key leaks?
```

Scene 6 — Where keys live

```text
Where should keys live? In development, a dot env file loaded by a small library and listed in your git ignore file. In production, a secret manager, like AWS Secrets Manager, Azure Key Vault, Google Secret Manager or Vault, injected as environment variables. In CI, the platform's encrypted secrets. And the golden rule: never put provider keys in a browser or mobile app. Front end code is public. Your backend holds the key, authenticates your users and enforces budgets.
```

Scene 7 — Simple example: portfolio chatbot

```text
A simple example. You're building a small chatbot for your portfolio site. The tempting shortcut is calling the model API straight from your JavaScript with the key embedded. Instead, you write a tiny backend endpoint that receives the user's message, checks a rate limit, calls the model with the key from an environment variable, and returns the reply. Ten extra lines of code, and your key never leaves your server.
```

Scene 8 — Business example: Dubai agency

```text
Now a realistic business example. A Dubai agency with forty client projects used one shared key for every developer and client. A contractor's laptop was lost, and the monthly bill spiked. Their fix: one project per client and environment with spend alerts, workload identity for production services, personal low limit keys for developers, secret scanning on every repository, and automatic quarterly rotation. Now a leaked key has a small blast radius, and they can bill clients accurately for AI usage.
```

Scene 9 — First hour after a leak

```text
What should you do in the first hour after a key leaks? First, revoke the key in the provider console, immediately, before investigating. Second, create a replacement and deploy it through your secret manager. Third, check usage dashboards for unusual spend or activity since the leak. Fourth, find how it leaked: a commit, a log, a screenshot, a shared document, and close that path. Finally, write two lines in your incident log. Teams that practice this once, with a fake key, handle the real thing calmly in minutes.
```

Scene 10 — Hands-on safety nets

```text
The lesson's code shows two small safety nets. It refuses to start if required secrets are missing, with a helpful message. And it redacts anything that looks like an API key before logging. Key formats change, so treat patterns as a safety net, not a guarantee. Pair them with a secret scanner in CI that blocks merges containing keys.
```

Scene 11 — Common mistakes

```text
Common mistakes to avoid. Keys in front end bundles or mobile apps. One key for every environment and team. Keys pasted into chat tools, tickets, or even prompts. And no spend limits, so a leak becomes a huge bill before anyone notices. Each of these is cheap to prevent and expensive to discover.
```

Scene 12 — Make secure the easy path

```text
A final practical tip: make the secure path the easy path. Give developers a one line command or script that fetches their personal development key from the secret manager into their local environment. Provide a project template that already has the dot env file ignored, the secret scanner configured, and a backend endpoint pattern for model calls. When doing the right thing takes less effort than the shortcut, people stop taking shortcuts, and you stop finding keys in chat threads.
```

Scene 13 — Deeper: the Dubai cleanup (illustrative)

```text
Let's deepen the Dubai agency's cleanup with illustrative numbers. They found keys in three repositories, two shared documents and one chat channel. After the reorganisation, forty client projects each had their own workspace and spend alert. Two months later, a contractor accidentally pasted a development key into a public gist. The secret scanner alerted within minutes, the key was revoked in the provider console, and because it was a low limit development key in one client's project, the potential spend was capped at a few dollars. Finance could also, for the first time, invoice AI usage per client accurately, which more than paid for the half day the cleanup took.
```

Scene 14 — Watch me do it: safe key loading

```text
Watch me do it. Let's walk through the safe loading script. First, load dot env reads a local dot env file, which I only use in development; in production the variables come from the secret manager. Next, a required list names the three provider keys, and I compute which are missing. If any are, the script exits immediately with a message naming them and saying where to set them. That's much better than failing later with a confusing authentication error. Then the redaction helper: a list of patterns for Anthropic, OpenAI and Google style keys. Redact loops over them and replaces any match with redacted key. I call it on a debug line containing a fake key, and the output shows the placeholder instead. Every log call in the service goes through this function. Finally, I'd add a secret scanner to CI so a key never reaches the main branch in the first place.
```

Scene 15 — Recap + try this now

```text
Quick recap. Keys are bearer credentials. Prefer identity based access for servers, separate projects by environment and client, store secrets in a manager, never ship keys to front ends, and automate scanning and rotation. Try this now: find every place your AI keys live today. Move one service to a secret manager or workload identity, add secret scanning to one repository, and set a spend alert on your production project.
```

</details>

### 3. Messages, roles and conversation state across Claude, OpenAI and Gemini

Lesson `messages-and-conversation-formats` · module “Core API calls across providers” · target 9 min · 15 scenes · 1285 words · planned 9:18 (words ≈ 9:19) · status: **to produce**

| Term | Say |
|---|---|
| Gemini | jem-in-eye |

| # | Time | Sec | Narration | On screen | Visual |
|---|---|---|---|---|---|
| 1 | 0:00 | 30 | Three providers, three different ways of saying the same thing. One calls it system, another calls it instructions, the third calls it system instruction. One says assistant, another says model. If you've ever copied code between providers and watched it fail on a single field name, this lesson is for you. You'll learn the shared mental model behind every chat API, and exactly how Claude, OpenAI and Gemini express it. | Messages across providers<br>• The shared mental model<br>• Field-by-field differences<br>• Managing conversation state | Layout: three speech bubbles labeled 'system', 'instructions', 'system_instruction' collide and then merge into one bubble labeled 'Instructions'. Title text fades in beneath. 0s: title 'Messages across providers' fades in top-left. 2s: main graphic begins animating. 5s, 10s, 15s: on-screen bullets reveal one by one, each highlighted as narrated. 26s: hold final frame. |
| 2 | 0:30 | 31 | Why does this matter? Because the message format is the foundation of every feature you'll build: streaming, tools, structured outputs, caching. If you understand it once, every provider's docs become quick to read. Think of it like driving in different countries. The steering wheel might be on the left or right, and road signs look different, but a car is still a car. Learn the car, then learn each country's signs. | Why it matters<br>• Foundation for every feature<br>• Driving in different countries<br>• Learn the car, then the signs | Layout: a car icon drives across a map of three countries; at each border the steering wheel flips side and a road sign changes style, but the car stays the same. 0s: title 'Why it matters' fades in top-left. 2s: main graphic begins animating. 5s, 11s, 17s: on-screen bullets reveal one by one, each highlighted as narrated. 27s: hold final frame. |
| 3 | 1:01 | 33 | Here's the shared model. Every modern API takes instructions, which set behavior, plus a list of conversation turns, and returns a response made of content blocks. Instructions say who the assistant is and what rules it follows. User turns carry the request and data. Assistant turns are the model's earlier replies. The response tells you what was generated, why it stopped, and how many tokens were used. Hold that picture, and the differences become details. | The shared model<br>• Instructions + turns in<br>• Content blocks out<br>• Stop reason + usage | Layout: a central diagram: an 'Instructions' card and a stack of 'Turn' cards enter a model box; out come 'Content blocks', 'Stop reason' and 'Usage' cards. 0s: title 'The shared model' fades in top-left. 2s: main graphic begins animating. 5s, 11s, 17s: on-screen bullets reveal one by one, each highlighted as narrated. 29s: hold final frame. |
| 4 | 1:34 | 38 | Now the details. With Claude, you call messages create, pass system at the top level, a messages list with user and assistant roles, and a required max tokens value. The reply has a content list of blocks and a stop reason. With OpenAI's Responses API, you call responses create, pass instructions and an input, and read output text for convenience. With Gemini, you call models generate content with contents, and put the system instruction and max output tokens in a config object, then read the text property. | Field by field<br>• Claude: system, messages, max_tokens (required)<br>• OpenAI: instructions, input, output_text<br>• Gemini: contents, config.system_instruction, .text | Layout: a three-column table animates row by row: Call, Instructions, Turns, Length cap, Output, Stop reason, Usage. Each provider's cell highlights as it's named. 0s: title 'Field by field' fades in top-left. 2s: main graphic begins animating. 5s, 13s, 21s: on-screen bullets reveal one by one, each highlighted as narrated. 34s: hold final frame. |
| 5 | 2:12 | 35 | A word on OpenAI's two APIs. The Responses API is OpenAI's recommended interface for new work. The older Chat Completions API is still supported, and it matters beyond OpenAI because many other providers copy its shape as an OpenAI compatible endpoint. So you'll meet Chat Completions often, even when you're not calling OpenAI at all. Also note: on current Claude models you can't prefill the start of the assistant's reply, so use instructions or structured outputs to control format instead. | Two OpenAI APIs + a Claude note<br>• Responses API: recommended<br>• Chat Completions: supported, widely copied<br>• Current Claude: no assistant prefill | Layout: two OpenAI API cards side by side, 'Responses (recommended)' and 'Chat Completions (supported)'. Several generic provider icons connect to the Chat Completions card with 'compatible' labels. 0s: title 'Two OpenAI APIs + a Claude note' fades in top-left. 2s: main graphic begins animating. 5s, 12s, 19s: on-screen bullets reveal one by one, each highlighted as narrated. 31s: hold final frame. |
| 6 | 2:47 | 36 | Where does conversation state live? Claude's API is stateless: you resend the history every turn. Gemini calls are stateless too, with a chat helper that keeps history on your side. OpenAI's Responses API can store responses and chain them with a previous response id, and storage is on by default unless you set store to false. Storing history yourself gives you portability and control over retention. Provider side state reduces payload size, but ties the conversation to that provider and its retention settings. | Where state lives<br>• Claude, Gemini: you resend history<br>• OpenAI: optional previous_response_id; store defaults true<br>• Own state = portability + retention control | Layout: three conversation timelines. Two show a database icon on 'your side'; the OpenAI one shows an optional cloud storage icon with a toggle labeled 'store: true/false'. 0s: title 'Where state lives' fades in top-left. 2s: main graphic begins animating. 5s, 12s, 19s: on-screen bullets reveal one by one, each highlighted as narrated. 32s: hold final frame. |
| 7 | 3:23 | 45 | Let's talk about tokens, because conversation state and cost are tightly linked. Every turn you resend is billed again as input. A support chat that runs to thirty turns can easily send ten times more tokens on its last turn than its first. So decide early how much history each request really needs. Many teams keep the last six to ten turns in full, plus a short summary of anything older, plus the pinned facts that must never be forgotten, like the customer's plan and country. Count tokens with each provider's counting endpoint, because tokenizers differ between providers and even between model generations. | History costs tokens<br>• Every resent turn is billed again<br>• Keep recent turns + summary + pinned facts<br>• Count with each provider's tokenizer | Layout: a growing stack of turn cards with a token meter climbing on each turn; then the stack compresses into 'Summary' + 'Last 8 turns' + a pinned 'Facts' card, and the meter drops. 0s: title 'History costs tokens' fades in top-left. 2s: main graphic begins animating. 5s, 15s, 25s: on-screen bullets reveal one by one, each highlighted as narrated. 41s: hold final frame. |
| 8 | 4:08 | 32 | A simple example. You ask all three providers, write a friendly one line reminder that the gym closes early on Friday. With Claude, you pass the request in the messages list and read the text block. With OpenAI, you pass it as input and read output text. With Gemini, you pass it as contents and read text. Same request, three shapes, three nearly identical answers. Once you've done this once, switching feels routine. | Simple example: gym reminder<br>• Claude: messages → text block<br>• OpenAI: input → output_text<br>• Gemini: contents → .text | Layout: three short code cards appear in sequence with the request string highlighted; beneath each, a one-line answer bubble appears. 0s: title 'Simple example: gym reminder' fades in top-left. 2s: main graphic begins animating. 5s, 11s, 17s: on-screen bullets reveal one by one, each highlighted as narrated. 28s: hold final frame. |
| 9 | 4:40 | 37 | Now a realistic business example. A UK subscription box company answers, can I pause my box for a month, using its policy text. The system instructions say: answer only from the policy, and offer a human handover if unsure. The policy and question go in the user turn. The code in the lesson sends this to all three providers, sets store to false on OpenAI so the response isn't kept server side, and prints each answer. The team compares tone, accuracy and token usage before choosing. | Business example: pause-my-box<br>• System: answer only from policy<br>• Policy + question in the user turn<br>• store=False on OpenAI<br>• Compare tone, accuracy, tokens | Layout: a support chat mock-up with the customer's question; three answer cards slide in from the right labeled Claude, OpenAI, Gemini, each with a small token count badge. 0s: title 'Business example: pause-my-box' fades in top-left. 2s: main graphic begins animating. 5s, 11s, 17s, 23s: on-screen bullets reveal one by one, each highlighted as narrated. 33s: hold final frame. |
| 10 | 5:17 | 37 | To keep your code portable, define your own small message type, a role and some text, and convert it at the edges to each provider's format. Claude and OpenAI both accept user and assistant roles; Gemini needs model instead of assistant and wraps text in parts. Module five turns this into a full adapter layer. For multi turn chat, append the last answer and the new user message, and keep stable content like the policy in the same place each turn so caching can reuse it. | Normalize at the edges<br>• Internal Turn(role, text)<br>• Convert per provider<br>• Gemini: 'model' + parts<br>• Stable content in the same place | Layout: a single 'Turn' object in the center with three converter arrows to three provider formats; the Gemini arrow shows role renamed to 'model' and text wrapped in a 'parts' box. 0s: title 'Normalize at the edges' fades in top-left. 2s: main graphic begins animating. 5s, 11s, 17s, 23s: on-screen bullets reveal one by one, each highlighted as narrated. 33s: hold final frame. |
| 11 | 5:54 | 23 | Common mistakes. Putting per request data in the system prompt, which breaks caching and mixes concerns. Forgetting max tokens on Claude. Relying on provider side storage without checking retention and privacy. And assuming role names match everywhere. Each one causes a confusing bug, usually at the worst possible moment, like during a demo. | Common mistakes<br>• Per-request data in system<br>• Missing max_tokens (Claude)<br>• Unchecked provider-side storage<br>• Assuming role names match | Layout: four warning cards, each with a tiny code diff showing the fix. 0s: title 'Common mistakes' fades in top-left. 2s: main graphic begins animating. 5s, 7s, 9s, 11s: on-screen bullets reveal one by one, each highlighted as narrated. 19s: hold final frame. |
| 12 | 6:17 | 35 | One more practical tip. Decide deliberately where your stable content lives. The system instructions and any reference text that doesn't change between turns should sit at the start of the request, in the same position and with identical wording every time. Variable things, like today's date, the user's latest question and retrieved snippets, belong at the end. That single habit makes prompt caching work across providers, reduces cost and speeds up responses, and it costs you nothing but a little discipline. | Stable first, variable last<br>• Same instructions, same position, same words<br>• Dates, questions, snippets at the end<br>• Enables caching across providers | Layout: a request ribbon with a green 'stable' left section and an orange 'variable' right section; a date chip tries to enter the green section and is redirected to the orange section. 0s: title 'Stable first, variable last' fades in top-left. 2s: main graphic begins animating. 5s, 12s, 19s: on-screen bullets reveal one by one, each highlighted as narrated. 31s: hold final frame. |
| 13 | 6:52 | 48 | Let's deepen the BoxJoy UK example with illustrative numbers. The company handles about four thousand support chats a month. When they compared providers on two hundred real questions, all three answered the pause question correctly, but differed on edge cases, like pausing during a promotion. Because the policy lived in the user turn and the instructions in the system prompt, switching providers for the test took minutes. They also set store to false on OpenAI and kept history in their own database for thirty days, matching their privacy notice. The winning provider was chosen on edge case accuracy and cost per resolved chat, not on the easy questions everyone got right. | Deeper: BoxJoy's provider test (illustrative)<br>• 200 real questions<br>• Easy ones: all correct<br>• Edge cases decided the winner<br>• Own history store, 30-day retention | Layout: an eval table with rows 'easy' and 'edge case'. 0s: all three providers tick the easy row. 10s: the edge-case row shows different scores. 20s: a database icon labeled '30-day retention' appears. 30s: a trophy moves to the provider with the best edge-case score and cost per resolved chat. 44s: hold. |
| 14 | 7:40 | 67 | Watch me do it. Let's walk through the three provider support example. The system string says: answer only from the policy, and offer a human if unsure. The policy and the question are combined into one user turn. For Claude, I pass system at the top level, the user turn in messages, and max tokens five hundred, then print the text and the stop reason. For OpenAI, instructions carries the system text, input carries the user turn, and store false keeps the response off OpenAI's servers; I print output text. For Gemini, contents is the user turn, and the config holds the system instruction and max output tokens; I print dot text. Then the normalized Turn class: role and text. To Claude and to OpenAI map it almost directly, while to Gemini renames assistant to model and wraps text in parts. Now a follow up question just means appending two turns and converting again. | Watch me do it: one question, three providers<br>• system / instructions / system_instruction<br>• store=False on OpenAI<br>• Turn(role, text) + three converters<br>• Gemini: 'model' role + parts | Layout: code editor. 0s: SYSTEM, POLICY, QUESTION highlight. 10s: Claude call highlights with 'system=' circled. 22s: OpenAI call highlights with 'instructions=' and 'store=False' circled. 34s: Gemini call highlights with 'config=' circled. 46s: Turn dataclass and three converters highlight; the Gemini converter's role mapping glows. 62s: hold. |
| 15 | 8:47 | 31 | Quick recap. Every API is instructions plus turns in, content blocks out. Claude uses system, messages and a required max tokens. OpenAI's Responses API uses instructions and input. Gemini uses contents and a config. Own your conversation state for portability. Try this now: run the three provider example with your own policy text and question, then send a three turn conversation to two providers using the normalized format, and compare token usage. | Recap + try this now<br>• Same model, different signs<br>• Own your state<br>• Run the three-provider example<br>• Compare a 3-turn conversation | Layout: recap card with the shared-model diagram; a terminal runs and prints three answers with token counts. 0s: title 'Recap + try this now' fades in top-left. 2s: main graphic begins animating. 5s, 9s, 13s, 17s: on-screen bullets reveal one by one, each highlighted as narrated. 27s: hold final frame. |

<details><summary>Narration per scene (paste into ElevenLabs)</summary>

Scene 1 — Messages across providers

```text
Three providers, three different ways of saying the same thing. One calls it system, another calls it instructions, the third calls it system instruction. One says assistant, another says model. If you've ever copied code between providers and watched it fail on a single field name, this lesson is for you. You'll learn the shared mental model behind every chat API, and exactly how Claude, OpenAI and Gemini express it.
```

Scene 2 — Why it matters

```text
Why does this matter? Because the message format is the foundation of every feature you'll build: streaming, tools, structured outputs, caching. If you understand it once, every provider's docs become quick to read. Think of it like driving in different countries. The steering wheel might be on the left or right, and road signs look different, but a car is still a car. Learn the car, then learn each country's signs.
```

Scene 3 — The shared model

```text
Here's the shared model. Every modern API takes instructions, which set behavior, plus a list of conversation turns, and returns a response made of content blocks. Instructions say who the assistant is and what rules it follows. User turns carry the request and data. Assistant turns are the model's earlier replies. The response tells you what was generated, why it stopped, and how many tokens were used. Hold that picture, and the differences become details.
```

Scene 4 — Field by field

```text
Now the details. With Claude, you call messages create, pass system at the top level, a messages list with user and assistant roles, and a required max tokens value. The reply has a content list of blocks and a stop reason. With OpenAI's Responses API, you call responses create, pass instructions and an input, and read output text for convenience. With Gemini, you call models generate content with contents, and put the system instruction and max output tokens in a config object, then read the text property.
```

Scene 5 — Two OpenAI APIs + a Claude note

```text
A word on OpenAI's two APIs. The Responses API is OpenAI's recommended interface for new work. The older Chat Completions API is still supported, and it matters beyond OpenAI because many other providers copy its shape as an OpenAI compatible endpoint. So you'll meet Chat Completions often, even when you're not calling OpenAI at all. Also note: on current Claude models you can't prefill the start of the assistant's reply, so use instructions or structured outputs to control format instead.
```

Scene 6 — Where state lives

```text
Where does conversation state live? Claude's API is stateless: you resend the history every turn. Gemini calls are stateless too, with a chat helper that keeps history on your side. OpenAI's Responses API can store responses and chain them with a previous response id, and storage is on by default unless you set store to false. Storing history yourself gives you portability and control over retention. Provider side state reduces payload size, but ties the conversation to that provider and its retention settings.
```

Scene 7 — History costs tokens

```text
Let's talk about tokens, because conversation state and cost are tightly linked. Every turn you resend is billed again as input. A support chat that runs to thirty turns can easily send ten times more tokens on its last turn than its first. So decide early how much history each request really needs. Many teams keep the last six to ten turns in full, plus a short summary of anything older, plus the pinned facts that must never be forgotten, like the customer's plan and country. Count tokens with each provider's counting endpoint, because tokenizers differ between providers and even between model generations.
```

Scene 8 — Simple example: gym reminder

```text
A simple example. You ask all three providers, write a friendly one line reminder that the gym closes early on Friday. With Claude, you pass the request in the messages list and read the text block. With OpenAI, you pass it as input and read output text. With Gemini, you pass it as contents and read text. Same request, three shapes, three nearly identical answers. Once you've done this once, switching feels routine.
```

Scene 9 — Business example: pause-my-box

```text
Now a realistic business example. A UK subscription box company answers, can I pause my box for a month, using its policy text. The system instructions say: answer only from the policy, and offer a human handover if unsure. The policy and question go in the user turn. The code in the lesson sends this to all three providers, sets store to false on OpenAI so the response isn't kept server side, and prints each answer. The team compares tone, accuracy and token usage before choosing.
```

Scene 10 — Normalize at the edges

```text
To keep your code portable, define your own small message type, a role and some text, and convert it at the edges to each provider's format. Claude and OpenAI both accept user and assistant roles; Gemini needs model instead of assistant and wraps text in parts. Module five turns this into a full adapter layer. For multi turn chat, append the last answer and the new user message, and keep stable content like the policy in the same place each turn so caching can reuse it.
```

Scene 11 — Common mistakes

```text
Common mistakes. Putting per request data in the system prompt, which breaks caching and mixes concerns. Forgetting max tokens on Claude. Relying on provider side storage without checking retention and privacy. And assuming role names match everywhere. Each one causes a confusing bug, usually at the worst possible moment, like during a demo.
```

Scene 12 — Stable first, variable last

```text
One more practical tip. Decide deliberately where your stable content lives. The system instructions and any reference text that doesn't change between turns should sit at the start of the request, in the same position and with identical wording every time. Variable things, like today's date, the user's latest question and retrieved snippets, belong at the end. That single habit makes prompt caching work across providers, reduces cost and speeds up responses, and it costs you nothing but a little discipline.
```

Scene 13 — Deeper: BoxJoy's provider test (illustrative)

```text
Let's deepen the BoxJoy UK example with illustrative numbers. The company handles about four thousand support chats a month. When they compared providers on two hundred real questions, all three answered the pause question correctly, but differed on edge cases, like pausing during a promotion. Because the policy lived in the user turn and the instructions in the system prompt, switching providers for the test took minutes. They also set store to false on OpenAI and kept history in their own database for thirty days, matching their privacy notice. The winning provider was chosen on edge case accuracy and cost per resolved chat, not on the easy questions everyone got right.
```

Scene 14 — Watch me do it: one question, three providers

```text
Watch me do it. Let's walk through the three provider support example. The system string says: answer only from the policy, and offer a human if unsure. The policy and the question are combined into one user turn. For Claude, I pass system at the top level, the user turn in messages, and max tokens five hundred, then print the text and the stop reason. For OpenAI, instructions carries the system text, input carries the user turn, and store false keeps the response off OpenAI's servers; I print output text. For Gemini, contents is the user turn, and the config holds the system instruction and max output tokens; I print dot text. Then the normalized Turn class: role and text. To Claude and to OpenAI map it almost directly, while to Gemini renames assistant to model and wraps text in parts. Now a follow up question just means appending two turns and converting again.
```

Scene 15 — Recap + try this now

```text
Quick recap. Every API is instructions plus turns in, content blocks out. Claude uses system, messages and a required max tokens. OpenAI's Responses API uses instructions and input. Gemini uses contents and a config. Own your conversation state for portability. Try this now: run the three provider example with your own policy text and question, then send a three turn conversation to two providers using the normalized format, and compare token usage.
```

</details>

### 4. Streaming responses to users and between services

Lesson `streaming-responses` · module “Core API calls across providers” · target 9 min · 16 scenes · 1296 words · planned 9:23 (words ≈ 9:23) · status: **to produce**

| Term | Say |
|---|---|
| SSE | S S E |
| EventSource | event source |
| TTFT | time to first token |

| # | Time | Sec | Narration | On screen | Visual |
|---|---|---|---|---|---|
| 1 | 0:00 | 31 | Picture two apps that both take ten seconds to write a reply. In the first, you stare at a spinner for ten seconds. In the second, words start appearing in under a second, and you're reading while it writes. Same total time, completely different experience. That's streaming. In this lesson you'll learn how each provider streams, how to relay streams safely to a browser, and the gotchas that trip up production apps. | Streaming responses<br>• Why it matters<br>• Provider stream shapes<br>• Relaying to the browser<br>• Production gotchas | Layout: split screen of two phone mock-ups. Left shows a spinner with a 10-second timer. Right shows words typing out immediately while the same timer runs. A 'Same total time' label bridges them. 0s: title 'Streaming responses' fades in top-left. 2s: main graphic begins animating. 5s, 9s, 13s, 17s: on-screen bullets reveal one by one, each highlighted as narrated. 27s: hold final frame. |
| 2 | 0:31 | 33 | Why does this matter? Users judge speed by the first sign of life, not the finish line. Think of a restaurant that brings bread while your meal cooks. The kitchen isn't faster, but the wait feels shorter, and you're less likely to walk out. Streaming is the bread basket for AI apps. It also has a technical benefit: long outputs can exceed HTTP timeouts if you wait for the whole response, and streaming avoids that. | Why it matters<br>• First sign of life = perceived speed<br>• The bread-basket analogy<br>• Avoids timeouts on long outputs | Layout: a restaurant table where a bread basket arrives at 0:30 while a main-course timer continues; a timeout clock icon on the side is crossed out. 0s: title 'Why it matters' fades in top-left. 2s: main graphic begins animating. 5s, 11s, 17s: on-screen bullets reveal one by one, each highlighted as narrated. 29s: hold final frame. |
| 3 | 1:04 | 38 | There's a second kind of streaming worth knowing about: progress streaming. For multi step tasks, like an agent that searches, reads and then writes, users don't just want words appearing. They want to know what's happening. Searching your CRM. Found three contacts. Drafting the email. Those status events can travel over the same connection as the text. Think of a parcel tracking page. The parcel isn't faster, but seeing each step builds trust and stops people refreshing the page, which, in AI apps, often means paying twice. | Progress streaming<br>• Status events for multi-step tasks<br>• 'Searching… Found 3… Drafting'<br>• Like parcel tracking: builds trust | Layout: a parcel-tracking style vertical timeline with steps 'Searching CRM', 'Found 3 contacts', 'Drafting email' lighting up in sequence, while a text area below starts streaming the draft. 0s: title 'Progress streaming' fades in top-left. 2s: main graphic begins animating. 5s, 13s, 21s: on-screen bullets reveal one by one, each highlighted as narrated. 34s: hold final frame. |
| 4 | 1:42 | 30 | How does it work? Most providers send server sent events: a long lived HTTP response where the server pushes small messages, one after another, each describing a piece of the output. The first events announce the message has started, then many small deltas carry text, then a final event carries the stop reason and token usage. Your code reads the events as they arrive and displays or forwards the text. | How streaming works<br>• Server-Sent Events<br>• Start → many deltas → final event<br>• Usage arrives at the end | Layout: a horizontal timeline with small event capsules flowing left to right: 'start', then many 'delta' capsules carrying word fragments, then 'final: stop_reason, usage'. 0s: title 'How streaming works' fades in top-left. 2s: main graphic begins animating. 5s, 10s, 15s: on-screen bullets reveal one by one, each highlighted as narrated. 26s: hold final frame. |
| 5 | 2:12 | 36 | With Claude in Python, the stream helper is the easiest path. You open it in a with block, loop over text stream to print text as it arrives, and call get final message at the end for the full message, stop reason and usage. In TypeScript, you iterate the events and write each text delta, then await final message. Underneath, Claude sends message start, content block deltas for text, thinking or tool arguments, a message delta with the stop reason, and message stop. | Claude streaming<br>• Python: messages.stream → text_stream<br>• get_final_message() for usage<br>• TypeScript: for await events → finalMessage() | Layout: code panel showing the Python snippet; the 'for text in stream.text_stream' line highlights while a console panel on the right fills with words; then 'get_final_message()' highlights and a usage badge. 0s: title 'Claude streaming' fades in top-left. 2s: main graphic begins animating. 5s, 12s, 19s: on-screen bullets reveal one by one, each highlighted as narrated. 32s: hold final frame. |
| 6 | 2:48 | 33 | OpenAI's Responses API streams when you pass stream equals true. You loop over typed events, and text arrives in events called response output text delta, with the finished response and usage in the completed event. Gemini's approach is the simplest: call generate content stream and each chunk has a text property you can print. Event names grow as providers add features, so check the docs, and write your handler to ignore event types it doesn't recognize. | OpenAI and Gemini streaming<br>• OpenAI: stream=True, response.output_text.delta<br>• response.completed → usage<br>• Gemini: generate_content_stream → chunk.text<br>• Ignore unknown event types | Layout: two code cards side by side with the delta-handling lines highlighted; a small filter icon labeled 'ignore unknown events' sits beneath. 0s: title 'OpenAI and Gemini streaming' fades in top-left. 2s: main graphic begins animating. 5s, 10s, 15s, 20s: on-screen bullets reveal one by one, each highlighted as narrated. 29s: hold final frame. |
| 7 | 3:21 | 28 | A simple example. You're building a command line helper that drafts emails. Without streaming, it prints nothing for eight seconds, and you wonder if it crashed. With streaming, the first words appear almost immediately, and you can even press Control C to stop a draft that's heading the wrong way, saving tokens. Five lines of code changed, and the tool feels twice as fast. | Simple example: CLI email drafter<br>• Before: 8 s of silence<br>• After: words appear immediately<br>• Ctrl+C stops bad drafts early | Layout: a terminal window; first run shows a blinking cursor for 8 seconds; second run shows text streaming and a '^C' interrupt cleanly stopping it. 0s: title 'Simple example: CLI email drafter' fades in top-left. 2s: main graphic begins animating. 5s, 10s, 15s: on-screen bullets reveal one by one, each highlighted as narrated. 24s: hold final frame. |
| 8 | 3:49 | 32 | In real products, the browser never talks to the provider directly, because the key would be exposed. Instead, your backend opens the provider stream and relays it to the browser, usually as server sent events. The lesson's FastAPI example does exactly that: it streams text deltas as small JSON data lines, sends a final line with output tokens, and on errors sends a generic error message without leaking internals. The browser listens with EventSource. | Relay through your backend<br>• Provider → your server → browser<br>• SSE data lines with JSON deltas<br>• Final line with usage<br>• Generic error messages | Layout: three boxes Provider → Backend (key icon inside) → Browser; small data capsules flow along both arrows; an error capsule is shown sanitized at the backend before reaching the browser. 0s: title 'Relay through your backend' fades in top-left. 2s: main graphic begins animating. 5s, 9s, 13s, 17s: on-screen bullets reveal one by one, each highlighted as narrated. 28s: hold final frame. |
| 9 | 4:21 | 32 | Now a realistic business example, with illustrative numbers. Property agents in Riyadh generate Arabic and English listing descriptions. Before streaming, they waited eight to ten seconds and often clicked generate twice, doubling cost. After adding a streaming relay, the first words appear quickly, the button is disabled while streaming, and duplicate requests dropped sharply. Usage from the final event now feeds a cost report per agent, so managers can see who uses AI most. | Business example: Riyadh listings<br>• 8–10 s waits → double clicks<br>• Streaming + disabled button<br>• Duplicates drop; usage per agent | Layout: a listing editor UI with Arabic and English text streaming into two panes; a 'Generate' button greys out; a small bar chart labeled illustrative shows duplicate requests falling. 0s: title 'Business example: Riyadh listings' fades in top-left. 2s: main graphic begins animating. 5s, 11s, 17s: on-screen bullets reveal one by one, each highlighted as narrated. 28s: hold final frame. |
| 10 | 4:53 | 34 | Let's cover the production gotchas. Proxies sometimes buffer responses, so disable buffering on the stream route. Streams can fail halfway, so show the partial text with a clear retry button. Usage usually arrives at the end, so record it from the final event. Tool arguments stream as partial JSON, so only parse them once the block is complete. And if the user closes the tab, cancel the upstream request, or you'll keep paying for text nobody will read. | Production gotchas<br>• Proxy buffering<br>• Mid-stream failures<br>• Usage at the end<br>• Partial JSON for tools<br>• Cancel on disconnect | Layout: five gotcha tiles light up in sequence; the last tile animates a browser tab closing and a red 'cancel upstream' arrow cutting the provider stream. 0s: title 'Production gotchas' fades in top-left. 2s: main graphic begins animating. 5s, 9s, 13s, 17s, 21s: on-screen bullets reveal one by one, each highlighted as narrated. 30s: hold final frame. |
| 11 | 5:27 | 18 | Common mistakes, briefly. Calling the provider directly from the browser and exposing your key. Ignoring client disconnects, so generation continues after the user leaves. And parsing partial JSON as if it were complete, which causes random crashes that are hard to reproduce. | Common mistakes<br>• Browser → provider directly<br>• Ignoring disconnects<br>• Parsing partial JSON | Layout: three red-bordered cards with a one-line fix sliding beneath each. 0s: title 'Common mistakes' fades in top-left. 2s: main graphic begins animating. 5s, 6s, 7s: on-screen bullets reveal one by one, each highlighted as narrated. 14s: hold final frame. |
| 12 | 5:45 | 36 | A question that comes up with streaming and structured data: can you stream JSON? You can, but the partial JSON isn't valid until the end, so don't parse it as you go unless your SDK provides a tolerant partial parser. A good pattern for user interfaces is to stream a human readable draft first, then send a final validated structured object at the end, which your app uses for saving or routing. Users see progress, and your code only acts on validated data. | Streaming structured data<br>• Partial JSON isn't valid mid-stream<br>• Stream readable draft, then final validated object<br>• Act only on validated data | Layout: a text draft streams into a preview pane while a JSON object assembles faintly in the background; at the end, a 'validated' badge stamps the JSON and a save button activates. 0s: title 'Streaming structured data' fades in top-left. 2s: main graphic begins animating. 5s, 12s, 19s: on-screen bullets reveal one by one, each highlighted as narrated. 32s: hold final frame. |
| 13 | 6:21 | 39 | How do you know streaming is working well? Measure time to first token, the gap between the request leaving your server and the first text arriving. Track it at the median and the ninety fifth percentile, per model and per region. Watch abandonment, meaning users who leave before the stream ends, and duplicate submits. If time to first token jumps after a change, check whether you've added slow work before the model call, like retrieval or a large prompt, and consider streaming a status event while that work runs. | Measure streaming quality<br>• TTFT p50/p95 per model and region<br>• Abandonment and duplicate submits<br>• Slow pre-work? Stream a status first | Layout: a line chart of TTFT p50 and p95 over a week (illustrative), with a spike annotated 'retrieval added'; a status-event capsule is inserted before the first text capsule and the perceived-wait bar. 0s: title 'Measure streaming quality' fades in top-left. 2s: main graphic begins animating. 5s, 13s, 21s: on-screen bullets reveal one by one, each highlighted as narrated. 35s: hold final frame. |
| 14 | 7:00 | 46 | Let's deepen the Riyadh listings example with illustrative numbers. Two hundred property agents generated about six thousand descriptions a month. Before streaming, about one generation in six was a duplicate because impatient agents clicked again. After the streaming relay and a disabled button, duplicates almost disappeared, which saved more money than any prompt optimization had. The team also noticed a quality benefit: agents started reading the Arabic draft as it streamed and pressing stop when it went off track, then adjusting the brief. The final usage event fed a per agent cost report, and managers used it to coach the heaviest users on writing better briefs. | Deeper: Riyadh listings (illustrative)<br>• ~1 in 6 were duplicates before<br>• Streaming + disabled button → near zero<br>• Stop button improved briefs<br>• Usage per agent for coaching | Layout: a listings UI. 0s: a duplicate-rate gauge at ~16% (illustrative). 10s: streaming text appears and the Generate button greys out; the gauge drops. 22s: an agent presses Stop mid-draft. 32s: a per-agent cost table appears. 44s: hold. |
| 15 | 7:46 | 65 | Watch me do it. Let's walk through the FastAPI relay. The endpoint takes a topic query parameter. Inside, an async generator opens the Claude stream with the async client. For each text chunk from text stream, it yields an SSE line: the word data, a colon, and a small JSON object with the delta, followed by a blank line. When the stream ends, it awaits the final message and yields one last line with done true and the output token count. If an API error happens, it yields a generic error object, so the browser never sees internal details. The endpoint returns a streaming response with the event stream media type and two headers: no cache, and X accel buffering no, so Nginx doesn't buffer it. In the browser, an EventSource listens, appends each delta to the page, and closes on done. I run it and watch the words arrive. | Watch me do it: the SSE relay<br>• async generator over text_stream<br>• 'data: {delta}' lines<br>• Final line: done + output_tokens<br>• Generic error, no-buffer headers | Layout: code editor left, browser right. 0s: the endpoint signature highlights. 9s: 'async for text in s.text_stream' highlights; SSE lines scroll in a network panel. 23s: the final done line highlights. 32s: the except block highlights. 41s: StreamingResponse headers highlight. 50s: the browser pane fills with streaming text. 62s: hold. |
| 16 | 8:51 | 32 | Quick recap. Streaming improves perceived speed and avoids timeouts. Claude has a stream helper, OpenAI streams typed events, and Gemini streams chunks. Relay through your backend, and handle buffering, failures, usage and disconnects. Try this now: build the FastAPI relay from the lesson, connect a tiny web page using EventSource, and log time to first token and output tokens for ten requests. Compare it with a non streaming version and feel the difference. | Recap + try this now<br>• Stream for perceived speed<br>• Relay via backend<br>• Build the SSE relay<br>• Log TTFT for 10 requests | Layout: recap card; then a small web page with a text area filling in live; a metrics panel shows 'TTFT' values for ten requests (illustrative). 0s: title 'Recap + try this now' fades in top-left. 2s: main graphic begins animating. 5s, 9s, 13s, 17s: on-screen bullets reveal one by one, each highlighted as narrated. 28s: hold final frame. |

<details><summary>Narration per scene (paste into ElevenLabs)</summary>

Scene 1 — Streaming responses

```text
Picture two apps that both take ten seconds to write a reply. In the first, you stare at a spinner for ten seconds. In the second, words start appearing in under a second, and you're reading while it writes. Same total time, completely different experience. That's streaming. In this lesson you'll learn how each provider streams, how to relay streams safely to a browser, and the gotchas that trip up production apps.
```

Scene 2 — Why it matters

```text
Why does this matter? Users judge speed by the first sign of life, not the finish line. Think of a restaurant that brings bread while your meal cooks. The kitchen isn't faster, but the wait feels shorter, and you're less likely to walk out. Streaming is the bread basket for AI apps. It also has a technical benefit: long outputs can exceed HTTP timeouts if you wait for the whole response, and streaming avoids that.
```

Scene 3 — Progress streaming

```text
There's a second kind of streaming worth knowing about: progress streaming. For multi step tasks, like an agent that searches, reads and then writes, users don't just want words appearing. They want to know what's happening. Searching your CRM. Found three contacts. Drafting the email. Those status events can travel over the same connection as the text. Think of a parcel tracking page. The parcel isn't faster, but seeing each step builds trust and stops people refreshing the page, which, in AI apps, often means paying twice.
```

Scene 4 — How streaming works

```text
How does it work? Most providers send server sent events: a long lived HTTP response where the server pushes small messages, one after another, each describing a piece of the output. The first events announce the message has started, then many small deltas carry text, then a final event carries the stop reason and token usage. Your code reads the events as they arrive and displays or forwards the text.
```

Scene 5 — Claude streaming

```text
With Claude in Python, the stream helper is the easiest path. You open it in a with block, loop over text stream to print text as it arrives, and call get final message at the end for the full message, stop reason and usage. In TypeScript, you iterate the events and write each text delta, then await final message. Underneath, Claude sends message start, content block deltas for text, thinking or tool arguments, a message delta with the stop reason, and message stop.
```

Scene 6 — OpenAI and Gemini streaming

```text
OpenAI's Responses API streams when you pass stream equals true. You loop over typed events, and text arrives in events called response output text delta, with the finished response and usage in the completed event. Gemini's approach is the simplest: call generate content stream and each chunk has a text property you can print. Event names grow as providers add features, so check the docs, and write your handler to ignore event types it doesn't recognize.
```

Scene 7 — Simple example: CLI email drafter

```text
A simple example. You're building a command line helper that drafts emails. Without streaming, it prints nothing for eight seconds, and you wonder if it crashed. With streaming, the first words appear almost immediately, and you can even press Control C to stop a draft that's heading the wrong way, saving tokens. Five lines of code changed, and the tool feels twice as fast.
```

Scene 8 — Relay through your backend

```text
In real products, the browser never talks to the provider directly, because the key would be exposed. Instead, your backend opens the provider stream and relays it to the browser, usually as server sent events. The lesson's FastAPI example does exactly that: it streams text deltas as small JSON data lines, sends a final line with output tokens, and on errors sends a generic error message without leaking internals. The browser listens with EventSource.
```

Scene 9 — Business example: Riyadh listings

```text
Now a realistic business example, with illustrative numbers. Property agents in Riyadh generate Arabic and English listing descriptions. Before streaming, they waited eight to ten seconds and often clicked generate twice, doubling cost. After adding a streaming relay, the first words appear quickly, the button is disabled while streaming, and duplicate requests dropped sharply. Usage from the final event now feeds a cost report per agent, so managers can see who uses AI most.
```

Scene 10 — Production gotchas

```text
Let's cover the production gotchas. Proxies sometimes buffer responses, so disable buffering on the stream route. Streams can fail halfway, so show the partial text with a clear retry button. Usage usually arrives at the end, so record it from the final event. Tool arguments stream as partial JSON, so only parse them once the block is complete. And if the user closes the tab, cancel the upstream request, or you'll keep paying for text nobody will read.
```

Scene 11 — Common mistakes

```text
Common mistakes, briefly. Calling the provider directly from the browser and exposing your key. Ignoring client disconnects, so generation continues after the user leaves. And parsing partial JSON as if it were complete, which causes random crashes that are hard to reproduce.
```

Scene 12 — Streaming structured data

```text
A question that comes up with streaming and structured data: can you stream JSON? You can, but the partial JSON isn't valid until the end, so don't parse it as you go unless your SDK provides a tolerant partial parser. A good pattern for user interfaces is to stream a human readable draft first, then send a final validated structured object at the end, which your app uses for saving or routing. Users see progress, and your code only acts on validated data.
```

Scene 13 — Measure streaming quality

```text
How do you know streaming is working well? Measure time to first token, the gap between the request leaving your server and the first text arriving. Track it at the median and the ninety fifth percentile, per model and per region. Watch abandonment, meaning users who leave before the stream ends, and duplicate submits. If time to first token jumps after a change, check whether you've added slow work before the model call, like retrieval or a large prompt, and consider streaming a status event while that work runs.
```

Scene 14 — Deeper: Riyadh listings (illustrative)

```text
Let's deepen the Riyadh listings example with illustrative numbers. Two hundred property agents generated about six thousand descriptions a month. Before streaming, about one generation in six was a duplicate because impatient agents clicked again. After the streaming relay and a disabled button, duplicates almost disappeared, which saved more money than any prompt optimization had. The team also noticed a quality benefit: agents started reading the Arabic draft as it streamed and pressing stop when it went off track, then adjusting the brief. The final usage event fed a per agent cost report, and managers used it to coach the heaviest users on writing better briefs.
```

Scene 15 — Watch me do it: the SSE relay

```text
Watch me do it. Let's walk through the FastAPI relay. The endpoint takes a topic query parameter. Inside, an async generator opens the Claude stream with the async client. For each text chunk from text stream, it yields an SSE line: the word data, a colon, and a small JSON object with the delta, followed by a blank line. When the stream ends, it awaits the final message and yields one last line with done true and the output token count. If an API error happens, it yields a generic error object, so the browser never sees internal details. The endpoint returns a streaming response with the event stream media type and two headers: no cache, and X accel buffering no, so Nginx doesn't buffer it. In the browser, an EventSource listens, appends each delta to the page, and closes on done. I run it and watch the words arrive.
```

Scene 16 — Recap + try this now

```text
Quick recap. Streaming improves perceived speed and avoids timeouts. Claude has a stream helper, OpenAI streams typed events, and Gemini streams chunks. Relay through your backend, and handle buffering, failures, usage and disconnects. Try this now: build the FastAPI relay from the lesson, connect a tiny web page using EventSource, and log time to first token and output tokens for ten requests. Compare it with a non streaming version and feel the difference.
```

</details>

### 5. Tool calling across Claude, OpenAI and Gemini

Lesson `tool-calling-across-providers` · module “Core API calls across providers” · target 9 min · 15 scenes · 1293 words · planned 9:24 (words ≈ 9:22) · status: **to produce**

| # | Time | Sec | Narration | On screen | Visual |
|---|---|---|---|---|---|
| 1 | 0:00 | 29 | Tool calling is where language models stop being chatbots and start doing work: checking an order, booking a slot, updating a record. Every major provider supports it, but each speaks a slightly different dialect. In this lesson you'll learn the one loop that never changes, the three dialects of Claude, OpenAI and Gemini, and how to write tools once so they work with all of them. | Tool calling across providers<br>• The universal loop<br>• Three dialects<br>• Write once, run anywhere | Layout: a chatbot bubble transforms into a small robot holding a wrench; three flag-like dialect cards labeled Claude, OpenAI, Gemini fan out beneath it. 0s: title 'Tool calling across providers' fades in top-left. 2s: main graphic begins animating. 5s, 10s, 15s: on-screen bullets reveal one by one, each highlighted as narrated. 25s: hold final frame. |
| 2 | 0:29 | 32 | Why does this matter? Because tools are how AI connects to your business: your orders, your calendar, your CRM. If you tie your tool code tightly to one provider, switching later means rewriting every integration. Think of an interpreter at a conference. The speaker's message is the same, but it's delivered in three languages. Your tool is the message. The provider format is just the language. Keep the message separate, and translation becomes easy. | Why it matters<br>• Tools connect AI to your business<br>• Avoid provider lock-in in tool code<br>• The conference interpreter | Layout: a speaker at a podium; three interpreter booths labeled Claude, OpenAI, Gemini each emit the same message icon in a different speech-bubble style. 0s: title 'Why it matters' fades in top-left. 2s: main graphic begins animating. 5s, 11s, 17s: on-screen bullets reveal one by one, each highlighted as narrated. 28s: hold final frame. |
| 3 | 1:01 | 33 | Here's the loop that never changes. You send the model your request and a list of tools, each with a name, a description and a JSON schema for its arguments. The model either answers, or asks for a tool with specific arguments. Your code runs the function. You send the result back. The model continues, maybe calling another tool, until it answers. The model never runs anything itself. It proposes; your code decides and executes. | The universal loop<br>• Send tools + request<br>• Model asks for a call<br>• Your code executes<br>• Return result → continue | Layout: a four-station circular diagram animating one full pass, with a padlock icon between the model and 'your code' labeled 'model proposes, code executes'. 0s: title 'The universal loop' fades in top-left. 2s: main graphic begins animating. 5s, 10s, 15s, 20s: on-screen bullets reveal one by one, each highlighted as narrated. 29s: hold final frame. |
| 4 | 1:34 | 36 | Dialect one, Claude. You define tools with a name, description and input schema, and can set strict to true so arguments always match the schema. When Claude wants a tool, the stop reason is tool use, and the content has tool use blocks with an id, a name and the input as an object. You append the assistant's content, then send a user message containing tool result blocks, each pointing to its tool use id, with an is error flag if something failed. | Claude dialect<br>• input_schema (+ strict)<br>• stop_reason: tool_use<br>• tool_use blocks: id, name, input<br>• Reply: tool_result with tool_use_id | Layout: a JSON card for the tool definition; then an assistant block labeled tool_use id=t1 slides in; a user block labeled tool_result tool_use_id=t1 answers it, connected by a matching-ID line. 0s: title 'Claude dialect' fades in top-left. 2s: main graphic begins animating. 5s, 10s, 15s, 20s: on-screen bullets reveal one by one, each highlighted as narrated. 32s: hold final frame. |
| 5 | 2:10 | 45 | Dialect two, OpenAI's Responses API. Tools have a type of function, a name, a description, parameters and a strict flag. The model's output contains function call items with a name, a call id, and arguments as a JSON string, so you must parse it. You add the model's items to your input list, then add function call output items with the matching call id and your result as a string. Dialect three, Gemini. You declare functions, wrap them in a tool object, and read response dot function calls, with arguments already parsed. You reply with function response parts in a tool role message. | OpenAI and Gemini dialects<br>• OpenAI: function_call, arguments = JSON string, call_id<br>• Reply: function_call_output<br>• Gemini: function_calls (args parsed)<br>• Reply: from_function_response, role='tool' | Layout: two columns. Left: an OpenAI function_call item with 'arguments: "{...}"' highlighted as a string and a json.loads chip. 0s: title 'OpenAI and Gemini dialects' fades in top-left. 2s: main graphic begins animating. 5s, 13s, 21s, 29s: on-screen bullets reveal one by one, each highlighted as narrated. 41s: hold final frame. |
| 6 | 2:55 | 36 | A simple example first. One tool: get order status, for a Karachi online store. The customer asks, where is my order K H I ten forty two. All three providers call the tool with that order id, your code returns out for delivery, arriving today between six and nine in the evening, and each model writes a friendly answer. The lesson's code shows all three loops side by side. Read them together and you'll see the same four steps in three spellings. | Simple example: order status<br>• Question: 'Where is KHI-1042?'<br>• All three call get_order_status<br>• Result: out for delivery, 6–9pm<br>• Same loop, three spellings | Layout: a chat bubble with the customer's question; three parallel lanes show each provider's call and result chips; three answer bubbles pop up at the end. 0s: title 'Simple example: order status' fades in top-left. 2s: main graphic begins animating. 5s, 10s, 15s, 20s: on-screen bullets reveal one by one, each highlighted as narrated. 32s: hold final frame. |
| 7 | 3:31 | 38 | Now a realistic business example. A logistics startup in Dubai supports customers in English and Arabic and wants the freedom to switch providers as prices change. They keep one canonical JSON schema per tool: track shipment, reschedule delivery and open a claim. A tiny converter generates each provider's format. The same eval of forty real customer messages runs against two providers every month. When one provider's tool selection accuracy dropped after a model update, they switched the routing in configuration and fixed nothing in their tool code. | Business example: Dubai logistics<br>• One canonical schema per tool<br>• Converter → provider formats<br>• Monthly 40-message eval, two providers<br>• Switch via config when accuracy drops | Layout: a 'canonical schemas' folder with three tool cards; a converter machine outputs three provider-format versions; a monthly calendar triggers an eval scoreboard comparing two. 0s: title 'Business example: Dubai logistics' fades in top-left. 2s: main graphic begins animating. 5s, 11s, 17s, 23s: on-screen bullets reveal one by one, each highlighted as narrated. 34s: hold final frame. |
| 8 | 4:09 | 37 | A few design rules make tools portable. Keep schemas simple: objects, strings, numbers, booleans, enums and arrays. Strict modes need additional properties set to false and every required field listed. Parse arguments defensively, since OpenAI sends a string and the others send objects. When several tools are requested at once, run them and return all the results together. Send errors back as results the model can read. And always validate arguments and permissions in your code, because the model's request is only a suggestion. | Portable tool rules<br>• Simple schemas<br>• Strict: additionalProperties false + required<br>• Parse defensively<br>• Return all parallel results<br>• Validate in code | Layout: six rule cards stack into a checklist; the 'Validate in code' card glows with a shield icon. 0s: title 'Portable tool rules' fades in top-left. 2s: main graphic begins animating. 5s, 9s, 13s, 17s, 21s: on-screen bullets reveal one by one, each highlighted as narrated. 33s: hold final frame. |
| 9 | 4:46 | 42 | How should you test tool calling across providers? Build a small eval of realistic user messages, each labeled with the tool you expect and the key arguments. For the logistics example, that might be: where's my parcel, with a tracking number, which should call track shipment; can you deliver on Saturday instead, which should call reschedule; and my box arrived crushed, which should open a claim. Run the same forty messages through each provider and score three things: right tool, valid arguments and calls per task. Differences here are often bigger than differences in writing quality. | Testing tool calling<br>• Labeled messages → expected tool + args<br>• Score: right tool, valid args, calls per task<br>• Run the same set on each provider | Layout: an eval table with columns Message, Expected tool, Provider A, Provider B; ticks and crosses populate row by row; summary bars at the bottom show accuracy per provider (illustrative). 0s: title 'Testing tool calling' fades in top-left. 2s: main graphic begins animating. 5s, 14s, 23s: on-screen bullets reveal one by one, each highlighted as narrated. 38s: hold final frame. |
| 10 | 5:28 | 25 | Providers also offer hosted tools that run on their infrastructure, like web search, code execution, file search and connectors to remote MCP servers. They save you building infrastructure, but availability varies by provider, model and cloud platform, and so do data handling terms. Use them deliberately, and check the current docs before sending sensitive data through them. | Hosted tools<br>• Web search, code execution, file search, MCP<br>• Vary by provider, model, platform<br>• Check data terms | Layout: a provider cloud with small hosted tool icons orbiting it; a checklist tag 'availability?' and 'data terms?' hangs from each. 0s: title 'Hosted tools' fades in top-left. 2s: main graphic begins animating. 5s, 9s, 13s: on-screen bullets reveal one by one, each highlighted as narrated. 21s: hold final frame. |
| 11 | 5:53 | 27 | Common mistakes. Forgetting to add the model's tool call turn to the history before sending results, which makes the API reject your request. Returning results out of order, or dropping one of several parallel calls. Loops with no step cap. And running write actions, like refunds, without validation or approval. Each of these shows up quickly in testing if you look for it. | Common mistakes<br>• Missing tool-call turn in history<br>• Dropped or misordered parallel results<br>• No step cap<br>• Unvalidated write actions | Layout: four mistake cards with a red X; each flips to a green fix. 0s: title 'Common mistakes' fades in top-left. 2s: main graphic begins animating. 5s, 8s, 11s, 14s: on-screen bullets reveal one by one, each highlighted as narrated. 23s: hold final frame. |
| 12 | 6:20 | 34 | Here's a question many teams ask: how many tools can I give a model before quality drops? There's no single number, and it varies by model and provider. The practical signal is your eval: if tool selection accuracy falls as you add tools, or calls per task rise, you've gone too far. Group tools into focused sets per feature, give each tool a distinct name and description, and consider tool search or deferred loading features where providers offer them. | How many tools?<br>• No universal limit<br>• Watch accuracy and calls per task<br>• Focused tool sets per feature<br>• Tool search where available | Layout: a line chart where tool-selection accuracy dips as the number of tools grows (illustrative); tool icons regroup into three focused sets and the line recovers. 0s: title 'How many tools?' fades in top-left. 2s: main graphic begins animating. 5s, 10s, 15s, 20s: on-screen bullets reveal one by one, each highlighted as narrated. 30s: hold final frame. |
| 13 | 6:54 | 47 | Let's deepen the Dubai logistics startup example with illustrative numbers. Forty real customer messages per month, in English and Arabic, run against two providers. For three months both scored within a couple of points on tool selection. Then, after a model update, one provider started calling reschedule delivery for messages that were really complaints, and accuracy dropped several points on Arabic messages. Because routing was configuration, they switched the Arabic route to the other provider the same afternoon and opened a ticket with the vendor. A month later, after a prompt tweak for the affected model, both were close again. No tool code changed at any point. | Deeper: the Dubai logistics monitor (illustrative)<br>• Monthly 40-message eval, EN + AR<br>• Model update → Arabic accuracy dropped<br>• Arabic route switched the same afternoon<br>• No tool code changed | Layout: a line chart of tool-selection accuracy for two providers over months. 0s: both lines close. 10s: one line dips on an 'Arabic' filter after a 'model update' marker. 22s: a routing switch flips for the Arabic route. 32s: lines converge again after a 'prompt tweak' marker. 44s: hold. |
| 14 | 7:41 | 69 | Watch me do it. Let's trace the Claude part of the order status example. The canonical schema has one property, order id, required, with no extra properties, and the tool definition adds strict true. The first call sends the question and the tool list. Claude replies with stop reason tool use. My while loop appends the assistant content, then builds a result for each tool use block: it calls get order status with the block's input and wraps the JSON output in a tool result with the matching id. I append all results as one user message and call again. This time the stop reason is end turn, so I print the text. The OpenAI loop is the same shape, except I extend my input list with the model's output items, parse the arguments string with json loads, and send function call output items. Gemini appends the model's content, then function response parts in a tool role message. | Watch me do it: order status loop<br>• One canonical SCHEMA, strict<br>• Claude: tool_use → tool_result (same id)<br>• OpenAI: json.loads(arguments) → function_call_output<br>• Gemini: function_response parts | Layout: code editor with three highlighted regions. 0s: SCHEMA highlights. 9s: the Claude while loop highlights; a tool_use id and tool_result id link visually. 26s: the OpenAI loop highlights; 'json.loads(o.arguments)' circled. 40s: the Gemini loop highlights; 'role="tool"' circled. 53s: three answers print in a terminal strip. 66s: hold. |
| 15 | 8:50 | 34 | Quick recap. The loop is universal: define, call, execute, return, continue. Claude uses tool use and tool result blocks. OpenAI uses function call items with string arguments. Gemini uses function calls and function responses. Keep one canonical schema, validate in code, and cap every loop. Try this now: take one of your own tools, implement it against two providers with the canonical schema approach, add a step cap and an error path, and compare accuracy on ten realistic requests. | Recap + try this now<br>• Universal loop, three dialects<br>• Canonical schema + validation<br>• Implement one tool on two providers<br>• Compare on ten requests | Layout: recap card with the loop diagram; a two-column scoreboard fills with illustrative accuracy for two providers. 0s: title 'Recap + try this now' fades in top-left. 2s: main graphic begins animating. 5s, 10s, 15s, 20s: on-screen bullets reveal one by one, each highlighted as narrated. 30s: hold final frame. |

<details><summary>Narration per scene (paste into ElevenLabs)</summary>

Scene 1 — Tool calling across providers

```text
Tool calling is where language models stop being chatbots and start doing work: checking an order, booking a slot, updating a record. Every major provider supports it, but each speaks a slightly different dialect. In this lesson you'll learn the one loop that never changes, the three dialects of Claude, OpenAI and Gemini, and how to write tools once so they work with all of them.
```

Scene 2 — Why it matters

```text
Why does this matter? Because tools are how AI connects to your business: your orders, your calendar, your CRM. If you tie your tool code tightly to one provider, switching later means rewriting every integration. Think of an interpreter at a conference. The speaker's message is the same, but it's delivered in three languages. Your tool is the message. The provider format is just the language. Keep the message separate, and translation becomes easy.
```

Scene 3 — The universal loop

```text
Here's the loop that never changes. You send the model your request and a list of tools, each with a name, a description and a JSON schema for its arguments. The model either answers, or asks for a tool with specific arguments. Your code runs the function. You send the result back. The model continues, maybe calling another tool, until it answers. The model never runs anything itself. It proposes; your code decides and executes.
```

Scene 4 — Claude dialect

```text
Dialect one, Claude. You define tools with a name, description and input schema, and can set strict to true so arguments always match the schema. When Claude wants a tool, the stop reason is tool use, and the content has tool use blocks with an id, a name and the input as an object. You append the assistant's content, then send a user message containing tool result blocks, each pointing to its tool use id, with an is error flag if something failed.
```

Scene 5 — OpenAI and Gemini dialects

```text
Dialect two, OpenAI's Responses API. Tools have a type of function, a name, a description, parameters and a strict flag. The model's output contains function call items with a name, a call id, and arguments as a JSON string, so you must parse it. You add the model's items to your input list, then add function call output items with the matching call id and your result as a string. Dialect three, Gemini. You declare functions, wrap them in a tool object, and read response dot function calls, with arguments already parsed. You reply with function response parts in a tool role message.
```

Scene 6 — Simple example: order status

```text
A simple example first. One tool: get order status, for a Karachi online store. The customer asks, where is my order K H I ten forty two. All three providers call the tool with that order id, your code returns out for delivery, arriving today between six and nine in the evening, and each model writes a friendly answer. The lesson's code shows all three loops side by side. Read them together and you'll see the same four steps in three spellings.
```

Scene 7 — Business example: Dubai logistics

```text
Now a realistic business example. A logistics startup in Dubai supports customers in English and Arabic and wants the freedom to switch providers as prices change. They keep one canonical JSON schema per tool: track shipment, reschedule delivery and open a claim. A tiny converter generates each provider's format. The same eval of forty real customer messages runs against two providers every month. When one provider's tool selection accuracy dropped after a model update, they switched the routing in configuration and fixed nothing in their tool code.
```

Scene 8 — Portable tool rules

```text
A few design rules make tools portable. Keep schemas simple: objects, strings, numbers, booleans, enums and arrays. Strict modes need additional properties set to false and every required field listed. Parse arguments defensively, since OpenAI sends a string and the others send objects. When several tools are requested at once, run them and return all the results together. Send errors back as results the model can read. And always validate arguments and permissions in your code, because the model's request is only a suggestion.
```

Scene 9 — Testing tool calling

```text
How should you test tool calling across providers? Build a small eval of realistic user messages, each labeled with the tool you expect and the key arguments. For the logistics example, that might be: where's my parcel, with a tracking number, which should call track shipment; can you deliver on Saturday instead, which should call reschedule; and my box arrived crushed, which should open a claim. Run the same forty messages through each provider and score three things: right tool, valid arguments and calls per task. Differences here are often bigger than differences in writing quality.
```

Scene 10 — Hosted tools

```text
Providers also offer hosted tools that run on their infrastructure, like web search, code execution, file search and connectors to remote MCP servers. They save you building infrastructure, but availability varies by provider, model and cloud platform, and so do data handling terms. Use them deliberately, and check the current docs before sending sensitive data through them.
```

Scene 11 — Common mistakes

```text
Common mistakes. Forgetting to add the model's tool call turn to the history before sending results, which makes the API reject your request. Returning results out of order, or dropping one of several parallel calls. Loops with no step cap. And running write actions, like refunds, without validation or approval. Each of these shows up quickly in testing if you look for it.
```

Scene 12 — How many tools?

```text
Here's a question many teams ask: how many tools can I give a model before quality drops? There's no single number, and it varies by model and provider. The practical signal is your eval: if tool selection accuracy falls as you add tools, or calls per task rise, you've gone too far. Group tools into focused sets per feature, give each tool a distinct name and description, and consider tool search or deferred loading features where providers offer them.
```

Scene 13 — Deeper: the Dubai logistics monitor (illustrative)

```text
Let's deepen the Dubai logistics startup example with illustrative numbers. Forty real customer messages per month, in English and Arabic, run against two providers. For three months both scored within a couple of points on tool selection. Then, after a model update, one provider started calling reschedule delivery for messages that were really complaints, and accuracy dropped several points on Arabic messages. Because routing was configuration, they switched the Arabic route to the other provider the same afternoon and opened a ticket with the vendor. A month later, after a prompt tweak for the affected model, both were close again. No tool code changed at any point.
```

Scene 14 — Watch me do it: order status loop

```text
Watch me do it. Let's trace the Claude part of the order status example. The canonical schema has one property, order id, required, with no extra properties, and the tool definition adds strict true. The first call sends the question and the tool list. Claude replies with stop reason tool use. My while loop appends the assistant content, then builds a result for each tool use block: it calls get order status with the block's input and wraps the JSON output in a tool result with the matching id. I append all results as one user message and call again. This time the stop reason is end turn, so I print the text. The OpenAI loop is the same shape, except I extend my input list with the model's output items, parse the arguments string with json loads, and send function call output items. Gemini appends the model's content, then function response parts in a tool role message.
```

Scene 15 — Recap + try this now

```text
Quick recap. The loop is universal: define, call, execute, return, continue. Claude uses tool use and tool result blocks. OpenAI uses function call items with string arguments. Gemini uses function calls and function responses. Keep one canonical schema, validate in code, and cap every loop. Try this now: take one of your own tools, implement it against two providers with the canonical schema approach, add a step cap and an error path, and compare accuracy on ten realistic requests.
```

</details>

### 6. Structured outputs: reliable JSON from every provider

Lesson `structured-outputs-json-schema` · module “Core API calls across providers” · target 9 min · 16 scenes · 1296 words · planned 9:24 (words ≈ 9:23) · status: **to produce**

| Term | Say |
|---|---|
| Pydantic | pie-dan-tick |
| JSON | jay-son |

| # | Time | Sec | Narration | On screen | Visual |
|---|---|---|---|---|---|
| 1 | 0:00 | 27 | Here's a quiet truth about AI in business: most outputs aren't read by people. They're read by code. A ticket gets routed, a record gets saved, a dashboard gets updated. And code hates surprises. In this lesson you'll learn structured outputs, which force a model's answer into the exact JSON shape you define, and how to do it with Claude, OpenAI and Gemini. | Structured outputs<br>• Code reads most AI output<br>• Guaranteed JSON shapes<br>• Three providers, one model | Layout: a text answer bubble flows into a code pipeline and jams a gear; then a neatly shaped JSON block flows through smoothly and fills database columns. 0s: title 'Structured outputs' fades in top-left. 2s: main graphic begins animating. 5s, 9s, 13s: on-screen bullets reveal one by one, each highlighted as narrated. 23s: hold final frame. |
| 2 | 0:27 | 37 | Why does this matter? Because parsing free text is fragile. The model adds a friendly sentence before the JSON, or forgets a field, or writes high priority instead of high, and your code breaks at two in the morning. Think of a paper form versus a blank sheet of paper. If you ask someone to write their details on a blank sheet, you get every format imaginable. Give them a form with boxes and tick options, and every answer lands in the right place. | Why it matters<br>• Free text breaks parsers<br>• Blank page vs printed form<br>• Boxes and tick options | Layout: left, a blank page with messy handwriting in various formats; right, a clean form with labeled boxes and checkboxes being filled neatly. 0s: title 'Why it matters' fades in top-left. 2s: main graphic begins animating. 5s, 13s, 21s: on-screen bullets reveal one by one, each highlighted as narrated. 33s: hold final frame. |
| 3 | 1:04 | 29 | There are three levels of reliability. Level one: asking nicely for JSON in the prompt. It usually works, until it doesn't. Level two: JSON mode, which guarantees valid JSON, but not your fields. Level three: schema constrained output, where the provider guarantees the response matches your JSON schema. All three major providers support level three on current models, and that's what you should use in production. | Three reliability levels<br>• 1: Ask for JSON<br>• 2: JSON mode (valid JSON)<br>• 3: Schema-constrained (your shape) | Layout: a three-step staircase; each step shows an example output. Step 1 has stray prose, step 2 valid JSON with a missing field, step 3 perfect JSON with a green schema badge. 0s: title 'Three reliability levels' fades in top-left. 2s: main graphic begins animating. 5s, 10s, 15s: on-screen bullets reveal one by one, each highlighted as narrated. 25s: hold final frame. |
| 4 | 1:33 | 35 | How does each provider do it? Claude takes an output config with a JSON schema format, and the SDK's parse helper accepts a Pydantic model and returns a parsed object. OpenAI's Responses API takes a text format with a JSON schema and strict mode, and its parse helper also accepts a Pydantic model. Gemini takes a response mime type of application JSON and a response JSON schema, which you can generate from the same Pydantic model. One model definition, three providers. | Provider mechanisms<br>• Claude: output_config / messages.parse<br>• OpenAI: text.format / responses.parse<br>• Gemini: response_json_schema<br>• One Pydantic model for all | Layout: a central Pydantic class card with three arrows to three provider cards, each showing its parameter name highlighted. 0s: title 'Provider mechanisms' fades in top-left. 2s: main graphic begins animating. 5s, 10s, 15s, 20s: on-screen bullets reveal one by one, each highlighted as narrated. 31s: hold final frame. |
| 5 | 2:08 | 30 | A simple example. You want to turn restaurant reviews into three fields: rating from one to five, a sentiment of positive, neutral or negative, and the dish mentioned, or null if none. With a schema, every review becomes a tidy row. Without one, you'll see four stars in one answer, eight out of ten in another, and a paragraph in a third. The schema makes your spreadsheet trustworthy. | Simple example: restaurant reviews<br>• rating: 1–5<br>• sentiment: enum<br>• dish: string or null<br>• Every review → a tidy row | Layout: three messy review snippets on the left transform into three uniform table rows on the right with columns rating, sentiment, dish. 0s: title 'Simple example: restaurant reviews' fades in top-left. 2s: main graphic begins animating. 5s, 9s, 13s, 17s: on-screen bullets reveal one by one, each highlighted as narrated. 26s: hold final frame. |
| 6 | 2:38 | 40 | Let's look at what happens under the hood, because it explains the guarantee. With schema constrained output, the provider restricts which tokens the model is allowed to produce at each step, so it literally cannot write a field name that isn't in your schema or a value outside your enum. Think of a vending machine keypad that only lights up valid product codes. You can't press a code that doesn't exist. That's why constrained output is so much more reliable than asking nicely, and why the schema itself deserves careful design. | Under the hood<br>• Provider constrains allowed tokens<br>• Can't produce invalid fields or enum values<br>• The vending-machine keypad | Layout: a vending machine keypad where only valid codes light up as a cursor moves; invalid keys stay dark. The lit codes transform into schema field names. 0s: title 'Under the hood' fades in top-left. 2s: main graphic begins animating. 5s, 14s, 23s: on-screen bullets reveal one by one, each highlighted as narrated. 36s: hold final frame. |
| 7 | 3:18 | 36 | Now the realistic business example in the lesson. A UK B2B agency receives about five thousand inbound emails a month. A Pydantic model called Lead defines company, contact name, budget in pounds or null, the service from a fixed list, urgency, and a needs human flag. The system prompt says: don't guess missing values. The code runs the same model through Claude, OpenAI and Gemini. Each email is routed by service and urgency, and anything flagged needs human goes to a manager. | Business example: UK agency leads<br>• Lead model: company, contact, budget\|null<br>• service + urgency enums<br>• needs_human escape hatch<br>• Routing by fields | Layout: an inbound email card is parsed into a Lead JSON card; routing arrows send it to team channels labeled SEO, Paid social, Web design, and a manager icon for needs_human. 0s: title 'Business example: UK agency leads' fades in top-left. 2s: main graphic begins animating. 5s, 10s, 15s, 20s: on-screen bullets reveal one by one, each highlighted as narrated. 32s: hold final frame. |
| 8 | 3:54 | 33 | Schema design is where most of the quality comes from. Use enums for anything you route on. Make fields nullable when information may be missing, and say don't guess, otherwise models fill gaps with plausible inventions. Add an escape hatch, like needs human or a confidence level. Describe each field, because descriptions are part of the prompt. Keep schemas small and flat. And stick to widely supported schema features, so the same schema works across providers. | Schema design tips<br>• Enums for routing fields<br>• Nullable + 'don't guess'<br>• Escape hatch field<br>• Descriptions, small and flat<br>• Portable subset | Layout: a schema card where each tip adds a visible annotation: enum chips, a 'null' badge, a 'needs_human' toggle, and description tooltips. 0s: title 'Schema design tips' fades in top-left. 2s: main graphic begins animating. 5s, 9s, 13s, 17s, 21s: on-screen bullets reveal one by one, each highlighted as narrated. 29s: hold final frame. |
| 9 | 4:27 | 34 | Here's a subtle but important point. A schema guarantees shape, not truth. The JSON can be perfectly formed and still wrong: the wrong company name, the wrong urgency. So validate on your side with Pydantic for business rules, like budget must be positive, and measure field accuracy by having a person label a sample every week. In the agency example, adding the nullable budget field and the don't guess instruction made invented budgets disappear from the weekly sample. | Shape ≠ truth<br>• Validate business rules yourself<br>• Weekly human-labeled sample<br>• Measure field accuracy | Layout: a perfectly formatted JSON card with a green 'schema valid' badge, but one field glows red 'wrong value'; a reviewer icon labels a sample and an accuracy gauge appears. 0s: title 'Shape ≠ truth' fades in top-left. 2s: main graphic begins animating. 5s, 12s, 19s: on-screen bullets reveal one by one, each highlighted as narrated. 30s: hold final frame. |
| 10 | 5:01 | 27 | When should you use structured outputs versus tool calling? Structured outputs shape the model's final answer. Tools let the model take an action or fetch data in the middle of its work. Many older pipelines forced a fake tool call just to get JSON. Switch those to structured outputs, especially since some of the newest models no longer allow forcing a specific tool. | Structured outputs vs tools<br>• Final answer shape → structured outputs<br>• Actions, data fetching → tools<br>• Replace fake 'forced tool' tricks | Layout: two lanes: 'Final answer' lane ends in a JSON card; 'Action' lane shows a tool icon calling an API mid-way. A crossed-out 'forced fake tool' card sits between them. 0s: title 'Structured outputs vs tools' fades in top-left. 2s: main graphic begins animating. 5s, 9s, 13s: on-screen bullets reveal one by one, each highlighted as narrated. 23s: hold final frame. |
| 11 | 5:28 | 20 | Common mistakes. Parsing free text with regular expressions in production. Making fields required when the information is often missing, which forces hallucination. Treating valid JSON as correct data. And combining structured outputs with features that don't support them, like some citation features, without checking the docs. | Common mistakes<br>• Regex parsing in production<br>• Required fields for often-missing info<br>• Valid JSON ≠ correct<br>• Incompatible feature combos | Layout: four red cards flip to green fixes one after another. 0s: title 'Common mistakes' fades in top-left. 2s: main graphic begins animating. 5s, 6s, 7s, 8s: on-screen bullets reveal one by one, each highlighted as narrated. 16s: hold final frame. |
| 12 | 5:48 | 35 | A question from product teams: what about outputs that are mostly text, like an email, but need a few fields too? Put the text inside the schema as a string field, next to the structured fields, like subject, body, tone and needs review. You keep one call, one validated object, and your app can route or store the metadata while displaying the body. Just set sensible length limits on long text fields, so a runaway answer can't blow your token budget. | Text plus fields<br>• Body as a string field<br>• Metadata alongside (subject, tone, flags)<br>• Length limits on long fields | Layout: an email preview on the right; on the left, the JSON object with 'subject', 'body', 'tone', 'needs_review' fields, each connected to where it appears in the UI. 0s: title 'Text plus fields' fades in top-left. 2s: main graphic begins animating. 5s, 12s, 19s: on-screen bullets reveal one by one, each highlighted as narrated. 31s: hold final frame. |
| 13 | 6:23 | 33 | A quick note on performance and cost. The first time you use a new schema, some providers need a moment to prepare it, so the very first request can be slower; later requests reuse that work. Large schemas also add input tokens to every call. Keep schemas stable, reuse them across requests, and split a giant extraction into two smaller calls if one schema grows past a few dozen fields. Your latency charts will thank you. | Performance notes<br>• First use of a schema may be slower<br>• Big schemas cost input tokens<br>• Keep stable; split giant schemas | Layout: a latency chart with a tall first bar labeled 'first use' followed by short bars; then one large schema card splits into two smaller cards. 0s: title 'Performance notes' fades in top-left. 2s: main graphic begins animating. 5s, 11s, 17s: on-screen bullets reveal one by one, each highlighted as narrated. 29s: hold final frame. |
| 14 | 6:56 | 48 | Let's deepen the UK agency's email routing with illustrative numbers. Five thousand inbound emails a month. Before structured outputs, a regex parser failed on about one email in twenty, usually when the model added a friendly sentence before the JSON, and failures landed in a manual queue. After switching to schema constrained output with the Lead model, parsing failures went to practically zero. The weekly human sample of fifty extractions showed high accuracy on company and service, and the needs human flag caught complaints and legal threats that the old system routed to sales by mistake. The operations lead's comment: we stopped noticing the AI, which is exactly what we wanted. | Deeper: 5,000 emails/month (illustrative)<br>• Regex parser failed ~1 in 20<br>• Schema-constrained → ~0 parse failures<br>• needs_human caught complaints<br>• 'We stopped noticing the AI' | Layout: an inbox flowing into a router. 0s: a red failure pile grows under 'regex'. 10s: the parser swaps to 'schema' and the pile disappears. 22s: a weekly sample panel shows accuracy bars (illustrative). 32s: a complaint email diverts to a manager via 'needs_human'. 44s: hold. |
| 15 | 7:44 | 70 | Watch me do it. Let's walk through the lead extractor. The Lead model has company, contact name, an optional budget in pounds with a description telling the model to use null if it isn't stated, a service limited to four values, urgency limited to three, and a needs human flag for unclear, complaint or legal emails. The system prompt says: extract lead details; don't guess missing values. For Claude, messages parse takes output format equal to the Lead class and returns parsed output as a Lead object. For OpenAI, responses parse takes text format equal to Lead and returns output parsed. For Gemini, I pass the JSON schema generated from the Lead model with the JSON mime type, then validate the text with model validate json. On the sample email from Tom at Greenleaf Interiors in Leeds, all three return web design, budget twelve thousand, urgency high, needs human false. Then I delete the budget sentence and rerun: budget comes back null. | Watch me do it: the Lead extractor<br>• Lead model: enums, nullable budget, needs_human<br>• Claude: messages.parse(output_format=Lead)<br>• OpenAI: responses.parse(text_format=Lead)<br>• Gemini: response_json_schema + validate | Layout: code editor left, three result cards right. 0s: Lead class highlights field by field. 15s: SYSTEM highlights. 23s: Claude parse call highlights; card 1 fills. 34s: OpenAI parse call highlights; card 2 fills. 44s: Gemini call highlights; card 3 fills. 56s: the budget sentence is deleted and the budget field becomes null. 67s: hold. |
| 16 | 8:54 | 30 | Quick recap. Structured outputs give you guaranteed shapes. Each provider has a mechanism, and one Pydantic model can drive all three. Design schemas with enums, nullable fields and an escape hatch, validate business rules yourself, and measure accuracy. Try this now: define a Pydantic model for one extraction task in your business, run it through two providers on twenty real samples, and measure how many fields are right. | Recap + try this now<br>• Guaranteed shapes, not guaranteed truth<br>• One model, three providers<br>• 20 samples, two providers, field accuracy | Layout: recap card; a field-accuracy table with rows per field and columns for two providers fills with illustrative percentages. 0s: title 'Recap + try this now' fades in top-left. 2s: main graphic begins animating. 5s, 10s, 15s: on-screen bullets reveal one by one, each highlighted as narrated. 26s: hold final frame. |

<details><summary>Narration per scene (paste into ElevenLabs)</summary>

Scene 1 — Structured outputs

```text
Here's a quiet truth about AI in business: most outputs aren't read by people. They're read by code. A ticket gets routed, a record gets saved, a dashboard gets updated. And code hates surprises. In this lesson you'll learn structured outputs, which force a model's answer into the exact JSON shape you define, and how to do it with Claude, OpenAI and Gemini.
```

Scene 2 — Why it matters

```text
Why does this matter? Because parsing free text is fragile. The model adds a friendly sentence before the JSON, or forgets a field, or writes high priority instead of high, and your code breaks at two in the morning. Think of a paper form versus a blank sheet of paper. If you ask someone to write their details on a blank sheet, you get every format imaginable. Give them a form with boxes and tick options, and every answer lands in the right place.
```

Scene 3 — Three reliability levels

```text
There are three levels of reliability. Level one: asking nicely for JSON in the prompt. It usually works, until it doesn't. Level two: JSON mode, which guarantees valid JSON, but not your fields. Level three: schema constrained output, where the provider guarantees the response matches your JSON schema. All three major providers support level three on current models, and that's what you should use in production.
```

Scene 4 — Provider mechanisms

```text
How does each provider do it? Claude takes an output config with a JSON schema format, and the SDK's parse helper accepts a Pydantic model and returns a parsed object. OpenAI's Responses API takes a text format with a JSON schema and strict mode, and its parse helper also accepts a Pydantic model. Gemini takes a response mime type of application JSON and a response JSON schema, which you can generate from the same Pydantic model. One model definition, three providers.
```

Scene 5 — Simple example: restaurant reviews

```text
A simple example. You want to turn restaurant reviews into three fields: rating from one to five, a sentiment of positive, neutral or negative, and the dish mentioned, or null if none. With a schema, every review becomes a tidy row. Without one, you'll see four stars in one answer, eight out of ten in another, and a paragraph in a third. The schema makes your spreadsheet trustworthy.
```

Scene 6 — Under the hood

```text
Let's look at what happens under the hood, because it explains the guarantee. With schema constrained output, the provider restricts which tokens the model is allowed to produce at each step, so it literally cannot write a field name that isn't in your schema or a value outside your enum. Think of a vending machine keypad that only lights up valid product codes. You can't press a code that doesn't exist. That's why constrained output is so much more reliable than asking nicely, and why the schema itself deserves careful design.
```

Scene 7 — Business example: UK agency leads

```text
Now the realistic business example in the lesson. A UK B2B agency receives about five thousand inbound emails a month. A Pydantic model called Lead defines company, contact name, budget in pounds or null, the service from a fixed list, urgency, and a needs human flag. The system prompt says: don't guess missing values. The code runs the same model through Claude, OpenAI and Gemini. Each email is routed by service and urgency, and anything flagged needs human goes to a manager.
```

Scene 8 — Schema design tips

```text
Schema design is where most of the quality comes from. Use enums for anything you route on. Make fields nullable when information may be missing, and say don't guess, otherwise models fill gaps with plausible inventions. Add an escape hatch, like needs human or a confidence level. Describe each field, because descriptions are part of the prompt. Keep schemas small and flat. And stick to widely supported schema features, so the same schema works across providers.
```

Scene 9 — Shape ≠ truth

```text
Here's a subtle but important point. A schema guarantees shape, not truth. The JSON can be perfectly formed and still wrong: the wrong company name, the wrong urgency. So validate on your side with Pydantic for business rules, like budget must be positive, and measure field accuracy by having a person label a sample every week. In the agency example, adding the nullable budget field and the don't guess instruction made invented budgets disappear from the weekly sample.
```

Scene 10 — Structured outputs vs tools

```text
When should you use structured outputs versus tool calling? Structured outputs shape the model's final answer. Tools let the model take an action or fetch data in the middle of its work. Many older pipelines forced a fake tool call just to get JSON. Switch those to structured outputs, especially since some of the newest models no longer allow forcing a specific tool.
```

Scene 11 — Common mistakes

```text
Common mistakes. Parsing free text with regular expressions in production. Making fields required when the information is often missing, which forces hallucination. Treating valid JSON as correct data. And combining structured outputs with features that don't support them, like some citation features, without checking the docs.
```

Scene 12 — Text plus fields

```text
A question from product teams: what about outputs that are mostly text, like an email, but need a few fields too? Put the text inside the schema as a string field, next to the structured fields, like subject, body, tone and needs review. You keep one call, one validated object, and your app can route or store the metadata while displaying the body. Just set sensible length limits on long text fields, so a runaway answer can't blow your token budget.
```

Scene 13 — Performance notes

```text
A quick note on performance and cost. The first time you use a new schema, some providers need a moment to prepare it, so the very first request can be slower; later requests reuse that work. Large schemas also add input tokens to every call. Keep schemas stable, reuse them across requests, and split a giant extraction into two smaller calls if one schema grows past a few dozen fields. Your latency charts will thank you.
```

Scene 14 — Deeper: 5,000 emails/month (illustrative)

```text
Let's deepen the UK agency's email routing with illustrative numbers. Five thousand inbound emails a month. Before structured outputs, a regex parser failed on about one email in twenty, usually when the model added a friendly sentence before the JSON, and failures landed in a manual queue. After switching to schema constrained output with the Lead model, parsing failures went to practically zero. The weekly human sample of fifty extractions showed high accuracy on company and service, and the needs human flag caught complaints and legal threats that the old system routed to sales by mistake. The operations lead's comment: we stopped noticing the AI, which is exactly what we wanted.
```

Scene 15 — Watch me do it: the Lead extractor

```text
Watch me do it. Let's walk through the lead extractor. The Lead model has company, contact name, an optional budget in pounds with a description telling the model to use null if it isn't stated, a service limited to four values, urgency limited to three, and a needs human flag for unclear, complaint or legal emails. The system prompt says: extract lead details; don't guess missing values. For Claude, messages parse takes output format equal to the Lead class and returns parsed output as a Lead object. For OpenAI, responses parse takes text format equal to Lead and returns output parsed. For Gemini, I pass the JSON schema generated from the Lead model with the JSON mime type, then validate the text with model validate json. On the sample email from Tom at Greenleaf Interiors in Leeds, all three return web design, budget twelve thousand, urgency high, needs human false. Then I delete the budget sentence and rerun: budget comes back null.
```

Scene 16 — Recap + try this now

```text
Quick recap. Structured outputs give you guaranteed shapes. Each provider has a mechanism, and one Pydantic model can drive all three. Design schemas with enums, nullable fields and an escape hatch, validate business rules yourself, and measure accuracy. Try this now: define a Pydantic model for one extraction task in your business, run it through two providers on twenty real samples, and measure how many fields are right.
```

</details>

### 7. Multimodal inputs: images, PDFs and audio

Lesson `vision-documents-and-audio` · module “Multimodal inputs and embeddings” · target 9 min · 15 scenes · 1211 words · planned 8:46 (words ≈ 8:47) · status: **to produce**

| Term | Say |
|---|---|
| PDF | P D F |
| MIME | mime |

| # | Time | Sec | Narration | On screen | Visual |
|---|---|---|---|---|---|
| 1 | 0:00 | 27 | Most business information doesn't arrive as neat text. It arrives as photos of receipts, PDF rate cards, screenshots of dashboards, and recordings of sales calls. Modern models can read all of these. In this lesson you'll learn how to send images, documents and audio to Claude, OpenAI and Gemini, and how to turn them into reliable data rather than impressive demos. | Multimodal inputs<br>• Images, PDFs, audio<br>• Provider request shapes<br>• From demo to reliable data | Layout: a desk scattered with a receipt photo, a PDF, a phone screenshot and an audio waveform; each item slides into a model icon and comes out as a neat data card. 0s: title 'Multimodal inputs' fades in top-left. 2s: main graphic begins animating. 5s, 9s, 13s: on-screen bullets reveal one by one, each highlighted as narrated. 23s: hold final frame. |
| 2 | 0:27 | 31 | Why does this matter? Because the most tedious work in many businesses is reading things and typing them into systems: expenses, invoices, contracts, forms. Multimodal models can take much of that off people's plates. Think of a new assistant who can read handwriting, scan a spreadsheet printout and listen to a voicemail. Useful, but you'd still double check the numbers on their first week. That's the right attitude for multimodal AI too. | Why it matters<br>• Reading-and-typing work is everywhere<br>• Assistant who reads, scans and listens<br>• Still check the numbers | Layout: a person icon surrounded by piles of paper and a keyboard; the piles shrink as an assistant icon takes them, while a magnifying glass icon labeled 'check' stays with the person. 0s: title 'Why it matters' fades in top-left. 2s: main graphic begins animating. 5s, 11s, 17s: on-screen bullets reveal one by one, each highlighted as narrated. 27s: hold final frame. |
| 3 | 0:58 | 31 | What can each provider handle? All three accept images and PDFs on current models. Claude takes image blocks and document blocks, reading PDF pages as both text and images, with optional citations. OpenAI's Responses API takes input image and input file items, and has dedicated transcription models for audio. Gemini accepts images, PDFs, audio and even video natively inside generate content. Support changes by model, so always check the specific model's documentation. | Capabilities (verify per model)<br>• All: images + PDFs<br>• Claude: image & document blocks<br>• OpenAI: input_image/input_file + transcription<br>• Gemini: native audio & video | Layout: a capability matrix with rows Images, PDFs, Audio, Video and columns Claude, OpenAI, Gemini; ticks appear cell by cell with a 'verify per model' footnote. 0s: title 'Capabilities (verify per model)' fades in top-left. 2s: main graphic begins animating. 5s, 9s, 13s, 17s: on-screen bullets reveal one by one, each highlighted as narrated. 27s: hold final frame. |
| 4 | 1:29 | 37 | Let's look at the request shapes. With Claude, a user message contains a list of blocks: an image block with base64 data and a media type, a document block for the PDF, then a text block with your question. With OpenAI, you send input text and input image items, and you can set the detail level. For audio, OpenAI's transcription endpoint turns a recording into text. With Gemini, you pass parts built from bytes with a MIME type, plus your instruction, in one contents list. | Request shapes<br>• Claude: [image, document, text] blocks<br>• OpenAI: input_text + input_image(detail)<br>• OpenAI audio: transcriptions.create<br>• Gemini: Part.from_bytes + instruction | Layout: three code cards appear in turn with the media-specific lines highlighted; small file icons (jpg, pdf, mp3) fly into each card. 0s: title 'Request shapes' fades in top-left. 2s: main graphic begins animating. 5s, 11s, 17s, 23s: on-screen bullets reveal one by one, each highlighted as narrated. 33s: hold final frame. |
| 5 | 2:06 | 37 | A simple example. You photograph a café receipt and ask a model to extract the merchant, date, total and currency. It works beautifully on a clear photo. Now try a crumpled thermal receipt photographed at an angle in dim light. The model might read a total of eighteen as eighty. The fix isn't hoping for better eyesight. Crop to the receipt, straighten it, and in code check that the line items add up to the total. If they don't, send it to a person. | Simple example: the café receipt<br>• Clear photo → accurate fields<br>• Crumpled thermal slip → '18' read as '80'<br>• Crop, straighten, check the sum | Layout: two receipt photos side by side; the blurry one shows '18.00' morphing into '80.00' in red; then a code check 'sum(items) == total' fails and routes the receipt to a human icon. 0s: title 'Simple example: the café receipt' fades in top-left. 2s: main graphic begins animating. 5s, 13s, 21s: on-screen bullets reveal one by one, each highlighted as narrated. 33s: hold final frame. |
| 6 | 2:43 | 38 | Now a realistic business example, with illustrative results. A consultancy with staff in Karachi, Dubai and London receives receipts in rupees, dirhams and pounds, some in Arabic or Urdu. Their pipeline sends each image to a model with a structured output schema: merchant, date, currency, total, tax and line items. Code checks that items add up, the date isn't in the future, and the currency matches the country. Failures go to a finance reviewer. Most receipts pass automatically, and the finance team's time on expenses drops substantially. | Business example: multi-country expenses<br>• PKR, AED, GBP; Arabic, Urdu, English<br>• Structured extraction<br>• Code checks: sum, date, currency<br>• Failures → finance reviewer | Layout: a map with three city pins sending receipt icons into a pipeline: Extract → Check → Approve or Review. A counter shows the auto-approve share rising (illustrative). 0s: title 'Business example: multi-country expenses' fades in top-left. 2s: main graphic begins animating. 5s, 11s, 17s, 23s: on-screen bullets reveal one by one, each highlighted as narrated. 34s: hold final frame. |
| 7 | 3:21 | 32 | Some practical guidance. Image quality matters, so crop to what matters and use higher detail settings for small print. Combine vision with structured outputs for any extraction task. Verify numbers in code. Remember that images and PDF pages cost tokens, so a thirty page PDF can be expensive per call: extract only the pages you need, and for documents you reuse, upload them once with a Files API and reference them by id. | Practical guidance<br>• Crop and use high detail for small text<br>• Vision + structured outputs<br>• Verify numbers in code<br>• Select pages; upload reusable files once | Layout: a PDF with 30 pages collapses to 3 highlighted pages; a Files API icon shows 'upload once, reference many' with arrows from multiple requests to one file ID. 0s: title 'Practical guidance' fades in top-left. 2s: main graphic begins animating. 5s, 9s, 13s, 17s: on-screen bullets reveal one by one, each highlighted as narrated. 28s: hold final frame. |
| 8 | 3:53 | 38 | Here's a cost saving trick many teams miss: not every document needs a multimodal model. A clean digital PDF, one where you can select the text, can be read with an ordinary PDF library, and you send plain text to the model, which is faster and cheaper. Scanned or photographed documents are where multimodal models shine. For long call recordings, transcribe first, then summarize the transcript. Building a fast path for easy documents and a multimodal path for hard ones often cuts cost dramatically without losing accuracy. | Right tool per document<br>• Digital PDF → extract text first<br>• Scans/photos → multimodal + checks<br>• Recordings → transcribe, then summarize<br>• Fast path + hard path | Layout: a document sorter machine routes a 'digital PDF' to a cheap text lane and a 'photo scan' to a multimodal lane; a cost meter on each lane shows the difference (illustrative). 0s: title 'Right tool per document' fades in top-left. 2s: main graphic begins animating. 5s, 11s, 17s, 23s: on-screen bullets reveal one by one, each highlighted as narrated. 34s: hold final frame. |
| 9 | 4:31 | 28 | Privacy deserves special attention with multimodal data. Receipts show names and card digits. IDs are highly sensitive. Screenshots reveal whatever was on screen. And call recordings capture other people's voices, which in many places requires notice or consent. Minimize what you send, redact where you can, check each provider's data terms, and involve your privacy team before processing recordings or identity documents at scale. | Privacy first<br>• Receipts, IDs, screenshots, recordings<br>• Minimize and redact<br>• Notice/consent for recordings<br>• Check provider data terms | Layout: a screenshot with personal details automatically blurred; an audio waveform with a 'consent obtained' badge; a checklist panel for provider terms. 0s: title 'Privacy first' fades in top-left. 2s: main graphic begins animating. 5s, 8s, 11s, 14s: on-screen bullets reveal one by one, each highlighted as narrated. 24s: hold final frame. |
| 10 | 4:59 | 27 | Also test with your real languages and scripts. Accuracy on Arabic or Urdu receipts, handwritten notes or unusual fonts can differ a lot from English printed text. Build your evaluation set from real documents in each language you support, including the bad photos. That's the only way to know how your pipeline performs for all your customers, not just the easy ones. | Test real scripts<br>• Arabic, Urdu, handwriting, odd fonts<br>• Include bad photos in evals<br>• Measure per language | Layout: three receipt thumbnails in English, Arabic and Urdu scripts each with its own accuracy bar (illustrative), and a 'blurry' tag on one sample. 0s: title 'Test real scripts' fades in top-left. 2s: main graphic begins animating. 5s, 9s, 13s: on-screen bullets reveal one by one, each highlighted as narrated. 23s: hold final frame. |
| 11 | 5:26 | 19 | Common mistakes. Sending full resolution photos when a crop would do, which costs more and runs slower. Trusting extracted numbers without arithmetic checks. Uploading call recordings without consent. And assuming audio or video support exists because another model from the same provider has it. | Common mistakes<br>• Full-res when a crop would do<br>• No arithmetic checks<br>• Recordings without consent<br>• Assumed audio/video support | Layout: four mistake cards flip over to reveal their fixes. 0s: title 'Common mistakes' fades in top-left. 2s: main graphic begins animating. 5s, 6s, 7s, 8s: on-screen bullets reveal one by one, each highlighted as narrated. 15s: hold final frame. |
| 12 | 5:45 | 31 | What about video? Some models, such as Gemini, accept video directly and can describe scenes, read on screen text and answer questions about moments in the footage. For others, a common approach is to sample frames at intervals and transcribe the audio, then send frames plus transcript. For marketing teams, that's useful for checking ad creatives against brand rules or summarizing webinar recordings, but check length limits, costs and data terms first. | Video inputs<br>• Native video on some models (e.g., Gemini)<br>• Otherwise: sampled frames + transcript<br>• Check limits, cost, data terms | Layout: a video timeline with frames sampled every few seconds and a transcript track; both feed into a model icon that outputs 'brand check: logo too small at 0:12'. 0s: title 'Video inputs' fades in top-left. 2s: main graphic begins animating. 5s, 11s, 17s: on-screen bullets reveal one by one, each highlighted as narrated. 27s: hold final frame. |
| 13 | 6:16 | 47 | Let's deepen the multi country expenses example with illustrative numbers. About two thousand receipts a month across Karachi, Dubai and London. In the first month, most were auto approved after passing the sum, date and currency checks. The ones sent to finance were mostly faded thermal receipts and a handful where the model read a date in the wrong format, day and month swapped, which the future date check caught. Finance added one rule: receipts above a threshold always get a human look, regardless of confidence. Staff liked that reimbursement became faster, and finance liked that every automated decision came with the extracted fields and the check results attached. | Deeper: 2,000 receipts/month (illustrative)<br>• Most auto-approved after checks<br>• Human review: faded slips, swapped dates<br>• High-value receipts always reviewed<br>• Faster reimbursement, full audit trail | Layout: a receipt pipeline. 0s: receipts from three city pins enter. 10s: most pass three check gates and turn green. 20s: faded receipts and a swapped-date receipt divert to a finance tray. 30s: a threshold rule card 'over limit → human' appears. 42s: hold. |
| 14 | 7:03 | 70 | Watch me do it. Let's walk through the Claude image and PDF request. I read the receipt photo and the rate card PDF from disk and base64 encode each. The user message content is a list of three blocks. First, an image block with a base64 source, the JPEG media type and the data. Second, a document block with the PDF media type. Third, a text block with the question: does the receipt total match the rate card price for the service listed, answer in three bullets. The media blocks come before the text. I call messages create with max tokens fifteen hundred and print the text. The OpenAI version sends input text plus an input image with a data URL and detail high. The transcription example opens the MP3 and calls audio transcriptions create. And the Gemini version passes the audio as a part built from bytes with its MIME type, followed by the summary instruction, in a single contents list. | Watch me do it: multimodal requests<br>• Claude: [image, document, text] blocks<br>• OpenAI: input_text + input_image (detail high)<br>• OpenAI audio: transcriptions.create<br>• Gemini: Part.from_bytes + instruction | Layout: code editor left, file icons right. 0s: base64 encode lines highlight; JPG and PDF icons fly in. 11s: the image block highlights. 19s: the document block highlights. 26s: the text block highlights. 35s: OpenAI input_image with 'detail: high' highlights. 47s: transcription call highlights with an MP3 icon. 56s: Gemini Part.from_bytes highlights. 67s: hold. |
| 15 | 8:13 | 33 | Quick recap. All three providers read images and PDFs, and audio and video support varies. Use each provider's block or part format, combine vision with structured outputs, check numbers in code, control cost by cropping and reusing uploads, and treat multimodal data as sensitive. Try this now: build a receipt or invoice extractor using structured outputs and a sum check, test it on twenty real documents in the languages you support, and measure field accuracy per language. | Recap + try this now<br>• Blocks/parts per provider<br>• Structure + arithmetic checks<br>• Cost and privacy controls<br>• 20 real docs, accuracy per language | Layout: recap card; then a results table with rows per language and columns Accuracy, Human-review rate, Cost per doc (illustrative). 0s: title 'Recap + try this now' fades in top-left. 2s: main graphic begins animating. 5s, 10s, 15s, 20s: on-screen bullets reveal one by one, each highlighted as narrated. 29s: hold final frame. |

<details><summary>Narration per scene (paste into ElevenLabs)</summary>

Scene 1 — Multimodal inputs

```text
Most business information doesn't arrive as neat text. It arrives as photos of receipts, PDF rate cards, screenshots of dashboards, and recordings of sales calls. Modern models can read all of these. In this lesson you'll learn how to send images, documents and audio to Claude, OpenAI and Gemini, and how to turn them into reliable data rather than impressive demos.
```

Scene 2 — Why it matters

```text
Why does this matter? Because the most tedious work in many businesses is reading things and typing them into systems: expenses, invoices, contracts, forms. Multimodal models can take much of that off people's plates. Think of a new assistant who can read handwriting, scan a spreadsheet printout and listen to a voicemail. Useful, but you'd still double check the numbers on their first week. That's the right attitude for multimodal AI too.
```

Scene 3 — Capabilities (verify per model)

```text
What can each provider handle? All three accept images and PDFs on current models. Claude takes image blocks and document blocks, reading PDF pages as both text and images, with optional citations. OpenAI's Responses API takes input image and input file items, and has dedicated transcription models for audio. Gemini accepts images, PDFs, audio and even video natively inside generate content. Support changes by model, so always check the specific model's documentation.
```

Scene 4 — Request shapes

```text
Let's look at the request shapes. With Claude, a user message contains a list of blocks: an image block with base64 data and a media type, a document block for the PDF, then a text block with your question. With OpenAI, you send input text and input image items, and you can set the detail level. For audio, OpenAI's transcription endpoint turns a recording into text. With Gemini, you pass parts built from bytes with a MIME type, plus your instruction, in one contents list.
```

Scene 5 — Simple example: the café receipt

```text
A simple example. You photograph a café receipt and ask a model to extract the merchant, date, total and currency. It works beautifully on a clear photo. Now try a crumpled thermal receipt photographed at an angle in dim light. The model might read a total of eighteen as eighty. The fix isn't hoping for better eyesight. Crop to the receipt, straighten it, and in code check that the line items add up to the total. If they don't, send it to a person.
```

Scene 6 — Business example: multi-country expenses

```text
Now a realistic business example, with illustrative results. A consultancy with staff in Karachi, Dubai and London receives receipts in rupees, dirhams and pounds, some in Arabic or Urdu. Their pipeline sends each image to a model with a structured output schema: merchant, date, currency, total, tax and line items. Code checks that items add up, the date isn't in the future, and the currency matches the country. Failures go to a finance reviewer. Most receipts pass automatically, and the finance team's time on expenses drops substantially.
```

Scene 7 — Practical guidance

```text
Some practical guidance. Image quality matters, so crop to what matters and use higher detail settings for small print. Combine vision with structured outputs for any extraction task. Verify numbers in code. Remember that images and PDF pages cost tokens, so a thirty page PDF can be expensive per call: extract only the pages you need, and for documents you reuse, upload them once with a Files API and reference them by id.
```

Scene 8 — Right tool per document

```text
Here's a cost saving trick many teams miss: not every document needs a multimodal model. A clean digital PDF, one where you can select the text, can be read with an ordinary PDF library, and you send plain text to the model, which is faster and cheaper. Scanned or photographed documents are where multimodal models shine. For long call recordings, transcribe first, then summarize the transcript. Building a fast path for easy documents and a multimodal path for hard ones often cuts cost dramatically without losing accuracy.
```

Scene 9 — Privacy first

```text
Privacy deserves special attention with multimodal data. Receipts show names and card digits. IDs are highly sensitive. Screenshots reveal whatever was on screen. And call recordings capture other people's voices, which in many places requires notice or consent. Minimize what you send, redact where you can, check each provider's data terms, and involve your privacy team before processing recordings or identity documents at scale.
```

Scene 10 — Test real scripts

```text
Also test with your real languages and scripts. Accuracy on Arabic or Urdu receipts, handwritten notes or unusual fonts can differ a lot from English printed text. Build your evaluation set from real documents in each language you support, including the bad photos. That's the only way to know how your pipeline performs for all your customers, not just the easy ones.
```

Scene 11 — Common mistakes

```text
Common mistakes. Sending full resolution photos when a crop would do, which costs more and runs slower. Trusting extracted numbers without arithmetic checks. Uploading call recordings without consent. And assuming audio or video support exists because another model from the same provider has it.
```

Scene 12 — Video inputs

```text
What about video? Some models, such as Gemini, accept video directly and can describe scenes, read on screen text and answer questions about moments in the footage. For others, a common approach is to sample frames at intervals and transcribe the audio, then send frames plus transcript. For marketing teams, that's useful for checking ad creatives against brand rules or summarizing webinar recordings, but check length limits, costs and data terms first.
```

Scene 13 — Deeper: 2,000 receipts/month (illustrative)

```text
Let's deepen the multi country expenses example with illustrative numbers. About two thousand receipts a month across Karachi, Dubai and London. In the first month, most were auto approved after passing the sum, date and currency checks. The ones sent to finance were mostly faded thermal receipts and a handful where the model read a date in the wrong format, day and month swapped, which the future date check caught. Finance added one rule: receipts above a threshold always get a human look, regardless of confidence. Staff liked that reimbursement became faster, and finance liked that every automated decision came with the extracted fields and the check results attached.
```

Scene 14 — Watch me do it: multimodal requests

```text
Watch me do it. Let's walk through the Claude image and PDF request. I read the receipt photo and the rate card PDF from disk and base64 encode each. The user message content is a list of three blocks. First, an image block with a base64 source, the JPEG media type and the data. Second, a document block with the PDF media type. Third, a text block with the question: does the receipt total match the rate card price for the service listed, answer in three bullets. The media blocks come before the text. I call messages create with max tokens fifteen hundred and print the text. The OpenAI version sends input text plus an input image with a data URL and detail high. The transcription example opens the MP3 and calls audio transcriptions create. And the Gemini version passes the audio as a part built from bytes with its MIME type, followed by the summary instruction, in a single contents list.
```

Scene 15 — Recap + try this now

```text
Quick recap. All three providers read images and PDFs, and audio and video support varies. Use each provider's block or part format, combine vision with structured outputs, check numbers in code, control cost by cropping and reusing uploads, and treat multimodal data as sensitive. Try this now: build a receipt or invoice extractor using structured outputs and a sum check, test it on twenty real documents in the languages you support, and measure field accuracy per language.
```

</details>

### 8. Embeddings APIs and semantic search in production

Lesson `embeddings-and-semantic-search` · module “Multimodal inputs and embeddings” · target 9 min · 15 scenes · 1233 words · planned 8:56 (words ≈ 8:56) · status: **to produce**

| Term | Say |
|---|---|
| pgvector | P G vector |
| MRR | M R R |
| Voyage | voy-age |

| # | Time | Sec | Narration | On screen | Visual |
|---|---|---|---|---|---|
| 1 | 0:00 | 32 | Your customer types, my parcel arrived broken, can I get my money back? Your help article is titled, refunds for damaged items. They share almost no words, so keyword search misses it. Embeddings fix that by matching meaning instead of words. In this lesson, you'll learn how to use embeddings APIs from different providers, how to build semantic search that holds up in production, and how to choose the right model for your languages. | Embeddings and semantic search<br>• Matching meaning, not words<br>• Provider APIs<br>• Production pipeline<br>• Choosing a model | Layout: a search bar with the customer's sentence; a keyword-matching beam misses the 'Refunds for damaged items' card; then a semantic beam connects them with a glowing line. 0s: title 'Embeddings and semantic search' fades in top-left. 2s: main graphic begins animating. 5s, 9s, 13s, 17s: on-screen bullets reveal one by one, each highlighted as narrated. 28s: hold final frame. |
| 2 | 0:32 | 31 | Why does this matter? Search quality quietly drives customer satisfaction, support costs and the quality of every retrieval augmented AI answer. Think of a librarian who's read every book versus a card catalog that only knows titles. Ask the catalog about ocean pollution and it finds books with those exact words. Ask the librarian and they bring you a book called The Plastic Tide. Embeddings give your search the librarian's understanding. | Why it matters<br>• Search drives satisfaction and AI answers<br>• Card catalog vs well-read librarian<br>• Meaning over matching words | Layout: left, a card catalog drawer with exact-title cards; right, a librarian icon handing over a book titled 'The Plastic Tide' in response to 'ocean pollution'. 0s: title 'Why it matters' fades in top-left. 2s: main graphic begins animating. 5s, 11s, 17s: on-screen bullets reveal one by one, each highlighted as narrated. 27s: hold final frame. |
| 3 | 1:03 | 29 | Here's the concept. An embedding model turns a piece of text into a list of numbers, a vector, positioned so that similar meanings sit close together. To search, you embed every document once, embed each query when it arrives, and find the documents whose vectors are nearest to the query's vector, usually with cosine similarity. The model does the understanding; the vector index does the fast lookup. | The concept<br>• Text → vector<br>• Similar meaning → nearby vectors<br>• Embed docs once, queries live<br>• Nearest neighbors = results | Layout: a 2D scatter plot where document dots cluster by topic; a query dot appears and a circle highlights its nearest neighbors. 0s: title 'The concept' fades in top-left. 2s: main graphic begins animating. 5s, 9s, 13s, 17s: on-screen bullets reveal one by one, each highlighted as narrated. 25s: hold final frame. |
| 4 | 1:32 | 34 | Where do you get embeddings? OpenAI offers text embedding three small and large, with an option to shorten vectors. Google's Gemini API offers a Gemini embedding model, with task types for queries versus documents. Anthropic doesn't offer its own embeddings model, and its documentation points to partners such as Voyage AI. You can also use open weight multilingual models, or embedding models hosted on the major cloud platforms. Model names change, so check the docs before you commit. | Where to get embeddings<br>• OpenAI: text-embedding-3-small/large<br>• Gemini: gemini-embedding-001 + task types<br>• Anthropic: partners such as Voyage AI<br>• Open-weight + cloud platforms | Layout: four source tiles arranged in a row, each emitting a stream of small vectors into a shared index icon. 0s: title 'Where to get embeddings' fades in top-left. 2s: main graphic begins animating. 5s, 10s, 15s, 20s: on-screen bullets reveal one by one, each highlighted as narrated. 30s: hold final frame. |
| 5 | 2:06 | 31 | A simple example from the lesson code. Four FAQs: pausing a subscription, changing an address, payment methods and refunds for damaged items. The query: my parcel arrived broken, money back? The code embeds the FAQs and the query with OpenAI, then again with Gemini using document and query task types, and picks the most similar FAQ. Both find the refund article, even though it shares almost no words with the question. | Simple example: four FAQs<br>• Query: 'parcel arrived broken, money back?'<br>• OpenAI and Gemini both → refund FAQ<br>• Almost no shared words | Layout: four FAQ cards in a column; similarity scores animate next to each for two providers; the refund card lights up green in both columns. 0s: title 'Simple example: four FAQs' fades in top-left. 2s: main graphic begins animating. 5s, 11s, 17s: on-screen bullets reveal one by one, each highlighted as narrated. 27s: hold final frame. |
| 6 | 2:37 | 35 | How do you choose a model? Test language coverage with your real mix, like Arabic, Urdu and English, including mixed language queries. Check domain fit: product catalogs, legal text and chat behave differently. Consider dimensions, since bigger vectors can be more accurate but cost storage and search time. Plan for price and rate limits, because embedding a million documents is a batch job. And remember lock in: vectors from different models aren't compatible, so switching models means re embedding everything. | Choosing a model<br>• Language coverage (incl. mixed)<br>• Domain fit<br>• Dimensions vs cost<br>• Price, rate limits<br>• Lock-in: re-embed on switch | Layout: a checklist card with five items; the 'Lock-in' item expands to show two incompatible vector spaces that can't be merged. 0s: title 'Choosing a model' fades in top-left. 2s: main graphic begins animating. 5s, 9s, 13s, 17s, 21s: on-screen bullets reveal one by one, each highlighted as narrated. 31s: hold final frame. |
| 7 | 3:12 | 35 | Now the production pipeline. Chunk documents by headings or a few hundred tokens with overlap, keeping metadata like source, date, language and permissions. Embed chunks in batches and record the model name and dimensions with every vector. Store them in an index, such as pgvector in Postgres or a vector database. At query time, embed with the same model, filter by metadata, and combine with keyword search for names, codes and product numbers. Rerank the top results when precision matters. | Production pipeline<br>• Chunk + metadata<br>• Batch embed + record model<br>• Index (pgvector, vector DB)<br>• Filter + hybrid search<br>• Rerank | Layout: a left-to-right pipeline diagram with five stages; a metadata tag travels with each chunk; a keyword-search lane merges into the vector lane before a 'Rerank' box. 0s: title 'Production pipeline' fades in top-left. 2s: main graphic begins animating. 5s, 9s, 13s, 17s, 21s: on-screen bullets reveal one by one, each highlighted as narrated. 31s: hold final frame. |
| 8 | 3:47 | 38 | Let's talk about keeping the index healthy, because search quality decays quietly. When a help article is edited, its old vectors still point to the old wording. When a document is deleted, or its permissions change, its vectors must go too, or users will find content they shouldn't. When a customer asks for their data to be deleted, remember embeddings of their messages count as their data. And when you change the model or chunking, build a new index alongside the old one, evaluate it, then switch. | Keep the index healthy<br>• Re-embed on edits<br>• Remove on delete or permission change<br>• Embeddings count in deletion requests<br>• New index side by side, then switch | Layout: an index grid where an edited document's old vector fades and a new one appears; a deleted document's vector is removed with a bin icon; two index versions sit side by side with a switch lever. 0s: title 'Keep the index healthy' fades in top-left. 2s: main graphic begins animating. 5s, 11s, 17s, 23s: on-screen bullets reveal one by one, each highlighted as narrated. 34s: hold final frame. |
| 9 | 4:25 | 39 | Now a realistic business example, with illustrative results. A retailer serving the UAE and Saudi Arabia has twelve hundred help articles in Arabic and English, and customers often mix both languages in one query. The team labeled a hundred and fifty real queries with the correct article, compared two embedding models, and chose the one with better cross language recall. They stored the model name with every vector and added keyword search for order numbers and product codes. Recall improved over keyword only search, and searches with no result dropped. | Business example: Gulf help center<br>• 1,200 articles, AR + EN<br>• 150 labeled real queries<br>• Pick best cross-language recall<br>• Hybrid for order numbers | Layout: a help-center search UI with a mixed Arabic-English query; a bar chart labeled illustrative compares recall@3 for keyword-only, model A and model B; a 'no results' counter drops. 0s: title 'Business example: Gulf help center' fades in top-left. 2s: main graphic begins animating. 5s, 11s, 17s, 23s: on-screen bullets reveal one by one, each highlighted as narrated. 35s: hold final frame. |
| 10 | 5:04 | 30 | How do you measure it? Build a labeled set of real queries, each with the correct document. Measure recall at three or five: how often the right document appears in the top results. Measure mean reciprocal rank to see how high it appears. Track the zero result rate, latency per query and embedding cost per thousand documents. Measure all of this per language, because averages hide the customers you're failing. | Measuring search<br>• Labeled real queries<br>• Recall@k, MRR<br>• Zero-result rate, latency, cost<br>• Per language | Layout: a metrics dashboard with four tiles; a language filter switches between EN, AR and UR, and the recall tile changes accordingly (illustrative). 0s: title 'Measuring search' fades in top-left. 2s: main graphic begins animating. 5s, 9s, 13s, 17s: on-screen bullets reveal one by one, each highlighted as narrated. 26s: hold final frame. |
| 11 | 5:34 | 23 | Common mistakes. Mixing vectors from different models or versions in one index. Embedding queries with a different model than the documents. Relying purely on semantic search for exact identifiers. And forgetting permission filters, so users retrieve documents they shouldn't see. The last one is a security issue, not just a quality issue. | Common mistakes<br>• Mixed-model vectors<br>• Query/doc model mismatch<br>• No keyword search for IDs<br>• Missing permission filters | Layout: four red cards; the permission card pulses with a lock icon to emphasize security. 0s: title 'Common mistakes' fades in top-left. 2s: main graphic begins animating. 5s, 7s, 9s, 11s: on-screen bullets reveal one by one, each highlighted as narrated. 19s: hold final frame. |
| 12 | 5:57 | 32 | One more habit worth building: read your failed searches. Log every query that returns nothing, or only weak matches, and review a sample every week. You'll discover missing articles, new product names customers use, and phrases in Urdu or Arabic your content never mentions. Each finding is either a new article, a synonym, or a new evaluation case. Over a few months, this simple loop does more for search quality than swapping embedding models. | Read your failed searches<br>• Log zero/weak-result queries<br>• Weekly review<br>• → new articles, synonyms, eval cases | Layout: a list of failed queries scrolls; each gets a tag chip: 'missing article', 'synonym', 'eval case'; a quality trend line rises over months (illustrative). 0s: title 'Read your failed searches' fades in top-left. 2s: main graphic begins animating. 5s, 11s, 17s: on-screen bullets reveal one by one, each highlighted as narrated. 28s: hold final frame. |
| 13 | 6:29 | 49 | Let's deepen the Gulf help center example with illustrative numbers. A hundred and fifty labeled queries, roughly half Arabic, a third English, and the rest mixed. Keyword search found the right article in the top three for about half of queries. The first embedding model tested lifted English recall well but did poorly on mixed queries. The second model performed well across all three groups, so they chose it, even though it cost a little more per thousand documents. Adding keyword search back for order numbers, in a hybrid setup, fixed the last common failure. Zero result searches fell sharply, and contact center tickets tagged, couldn't find the answer, dropped the following month. | Deeper: the Gulf help center (illustrative)<br>• 150 labeled queries: AR, EN, mixed<br>• Keyword-only: ~half found in top 3<br>• Model B best across all groups<br>• Hybrid for order numbers | Layout: grouped bar chart recall@3 for AR, EN, Mixed. 0s: keyword-only bars appear. 9s: model A bars appear, low on Mixed. 17s: model B bars appear, high across groups (illustrative). 27s: a hybrid badge adds order-number matches. 36s: a 'couldn't find' ticket counter drops. 46s: hold. |
| 14 | 7:18 | 69 | Watch me do it. Let's walk through the FAQ search script. Four FAQs sit in a list. The cosine function turns two lists into arrays and returns their dot product divided by the product of their lengths. For OpenAI, I embed all four FAQs in one call, collect the vectors, then embed the customer query, my parcel arrived broken, money back. I pick the FAQ with the highest cosine similarity and print it: the refund FAQ. For Gemini, I call embed content with the FAQs and a task type of retrieval document, then embed the query with retrieval query, because Gemini tunes vectors differently for documents and questions. I compare again, and the refund FAQ wins again. Notice two rules in action: queries and documents use the same model, and I never compare an OpenAI vector with a Gemini vector. In production, the document vectors would be stored in an index with the model name, not recomputed each time. | Watch me do it: FAQ semantic search<br>• cosine() helper<br>• OpenAI: embed FAQs + query → refund FAQ<br>• Gemini: RETRIEVAL_DOCUMENT vs RETRIEVAL_QUERY<br>• Same model per comparison; store vectors | Layout: code editor top, a similarity table bottom. 0s: FAQS and cosine() highlight. 12s: OpenAI embeddings call highlights; four similarity bars appear, refund highest. 29s: Gemini calls highlight with task_type circled. 43s: a second set of bars appears, refund highest. 54s: a warning chip 'never mix models' fades in. 66s: hold. |
| 15 | 8:27 | 29 | Quick recap. Embeddings match meaning. OpenAI and Gemini offer embeddings APIs, Anthropic points to partners, and open models and cloud platforms are options too. Store the model with every vector, use hybrid search and permission filters, and measure recall per language. Try this now: embed fifty of your own FAQs with two models, write thirty labeled test queries in your customers' languages, and compare recall at three. | Recap + try this now<br>• Meaning-based search<br>• Model per vector, hybrid, filters<br>• 50 FAQs, 2 models, 30 queries<br>• Compare recall@3 | Layout: recap card; a small results table with two model columns and a recall@3 row fills in (illustrative). 0s: title 'Recap + try this now' fades in top-left. 2s: main graphic begins animating. 5s, 9s, 13s, 17s: on-screen bullets reveal one by one, each highlighted as narrated. 25s: hold final frame. |

<details><summary>Narration per scene (paste into ElevenLabs)</summary>

Scene 1 — Embeddings and semantic search

```text
Your customer types, my parcel arrived broken, can I get my money back? Your help article is titled, refunds for damaged items. They share almost no words, so keyword search misses it. Embeddings fix that by matching meaning instead of words. In this lesson, you'll learn how to use embeddings APIs from different providers, how to build semantic search that holds up in production, and how to choose the right model for your languages.
```

Scene 2 — Why it matters

```text
Why does this matter? Search quality quietly drives customer satisfaction, support costs and the quality of every retrieval augmented AI answer. Think of a librarian who's read every book versus a card catalog that only knows titles. Ask the catalog about ocean pollution and it finds books with those exact words. Ask the librarian and they bring you a book called The Plastic Tide. Embeddings give your search the librarian's understanding.
```

Scene 3 — The concept

```text
Here's the concept. An embedding model turns a piece of text into a list of numbers, a vector, positioned so that similar meanings sit close together. To search, you embed every document once, embed each query when it arrives, and find the documents whose vectors are nearest to the query's vector, usually with cosine similarity. The model does the understanding; the vector index does the fast lookup.
```

Scene 4 — Where to get embeddings

```text
Where do you get embeddings? OpenAI offers text embedding three small and large, with an option to shorten vectors. Google's Gemini API offers a Gemini embedding model, with task types for queries versus documents. Anthropic doesn't offer its own embeddings model, and its documentation points to partners such as Voyage AI. You can also use open weight multilingual models, or embedding models hosted on the major cloud platforms. Model names change, so check the docs before you commit.
```

Scene 5 — Simple example: four FAQs

```text
A simple example from the lesson code. Four FAQs: pausing a subscription, changing an address, payment methods and refunds for damaged items. The query: my parcel arrived broken, money back? The code embeds the FAQs and the query with OpenAI, then again with Gemini using document and query task types, and picks the most similar FAQ. Both find the refund article, even though it shares almost no words with the question.
```

Scene 6 — Choosing a model

```text
How do you choose a model? Test language coverage with your real mix, like Arabic, Urdu and English, including mixed language queries. Check domain fit: product catalogs, legal text and chat behave differently. Consider dimensions, since bigger vectors can be more accurate but cost storage and search time. Plan for price and rate limits, because embedding a million documents is a batch job. And remember lock in: vectors from different models aren't compatible, so switching models means re embedding everything.
```

Scene 7 — Production pipeline

```text
Now the production pipeline. Chunk documents by headings or a few hundred tokens with overlap, keeping metadata like source, date, language and permissions. Embed chunks in batches and record the model name and dimensions with every vector. Store them in an index, such as pgvector in Postgres or a vector database. At query time, embed with the same model, filter by metadata, and combine with keyword search for names, codes and product numbers. Rerank the top results when precision matters.
```

Scene 8 — Keep the index healthy

```text
Let's talk about keeping the index healthy, because search quality decays quietly. When a help article is edited, its old vectors still point to the old wording. When a document is deleted, or its permissions change, its vectors must go too, or users will find content they shouldn't. When a customer asks for their data to be deleted, remember embeddings of their messages count as their data. And when you change the model or chunking, build a new index alongside the old one, evaluate it, then switch.
```

Scene 9 — Business example: Gulf help center

```text
Now a realistic business example, with illustrative results. A retailer serving the UAE and Saudi Arabia has twelve hundred help articles in Arabic and English, and customers often mix both languages in one query. The team labeled a hundred and fifty real queries with the correct article, compared two embedding models, and chose the one with better cross language recall. They stored the model name with every vector and added keyword search for order numbers and product codes. Recall improved over keyword only search, and searches with no result dropped.
```

Scene 10 — Measuring search

```text
How do you measure it? Build a labeled set of real queries, each with the correct document. Measure recall at three or five: how often the right document appears in the top results. Measure mean reciprocal rank to see how high it appears. Track the zero result rate, latency per query and embedding cost per thousand documents. Measure all of this per language, because averages hide the customers you're failing.
```

Scene 11 — Common mistakes

```text
Common mistakes. Mixing vectors from different models or versions in one index. Embedding queries with a different model than the documents. Relying purely on semantic search for exact identifiers. And forgetting permission filters, so users retrieve documents they shouldn't see. The last one is a security issue, not just a quality issue.
```

Scene 12 — Read your failed searches

```text
One more habit worth building: read your failed searches. Log every query that returns nothing, or only weak matches, and review a sample every week. You'll discover missing articles, new product names customers use, and phrases in Urdu or Arabic your content never mentions. Each finding is either a new article, a synonym, or a new evaluation case. Over a few months, this simple loop does more for search quality than swapping embedding models.
```

Scene 13 — Deeper: the Gulf help center (illustrative)

```text
Let's deepen the Gulf help center example with illustrative numbers. A hundred and fifty labeled queries, roughly half Arabic, a third English, and the rest mixed. Keyword search found the right article in the top three for about half of queries. The first embedding model tested lifted English recall well but did poorly on mixed queries. The second model performed well across all three groups, so they chose it, even though it cost a little more per thousand documents. Adding keyword search back for order numbers, in a hybrid setup, fixed the last common failure. Zero result searches fell sharply, and contact center tickets tagged, couldn't find the answer, dropped the following month.
```

Scene 14 — Watch me do it: FAQ semantic search

```text
Watch me do it. Let's walk through the FAQ search script. Four FAQs sit in a list. The cosine function turns two lists into arrays and returns their dot product divided by the product of their lengths. For OpenAI, I embed all four FAQs in one call, collect the vectors, then embed the customer query, my parcel arrived broken, money back. I pick the FAQ with the highest cosine similarity and print it: the refund FAQ. For Gemini, I call embed content with the FAQs and a task type of retrieval document, then embed the query with retrieval query, because Gemini tunes vectors differently for documents and questions. I compare again, and the refund FAQ wins again. Notice two rules in action: queries and documents use the same model, and I never compare an OpenAI vector with a Gemini vector. In production, the document vectors would be stored in an index with the model name, not recomputed each time.
```

Scene 15 — Recap + try this now

```text
Quick recap. Embeddings match meaning. OpenAI and Gemini offer embeddings APIs, Anthropic points to partners, and open models and cloud platforms are options too. Store the model with every vector, use hybrid search and permission filters, and measure recall per language. Try this now: embed fifty of your own FAQs with two models, write thirty labeled test queries in your customers' languages, and compare recall at three.
```

</details>

### 9. Batch APIs: bulk processing at a discount

Lesson `batch-apis-for-bulk-work` · module “Cost, scale and reliability” · target 9 min · 15 scenes · 1260 words · planned 9:07 (words ≈ 9:08) · status: **to produce**

| Term | Say |
|---|---|
| JSONL | jay-son L |
| SKU | S K U |

| # | Time | Sec | Narration | On screen | Visual |
|---|---|---|---|---|---|
| 1 | 0:00 | 35 | Imagine you need product descriptions for thirty thousand new items, or you want to classify a month of support tickets. You could fire thousands of real time requests, hit rate limits all afternoon, and pay full price. Or you could hand the whole pile to a batch API, go home, and collect the results in the morning at a discount. In this lesson you'll learn when batch is the right call, and how to run it with Claude, OpenAI and Gemini. | Batch APIs<br>• When batch beats real time<br>• Three providers' batch flows<br>• Designing reliable pipelines | Layout: a mountain of task cards on the left; a real-time lane shows a traffic jam with '429' signs; a batch lane shows a single truck loading the pile at night and unloading at sunrise with a discount tag. 0s: title 'Batch APIs' fades in top-left. 2s: main graphic begins animating. 5s, 12s, 19s: on-screen bullets reveal one by one, each highlighted as narrated. 31s: hold final frame. |
| 2 | 0:35 | 31 | Why does this matter? Because a surprising share of AI work isn't interactive at all. Enrichment, classification, content generation for catalogs, evaluation runs and embedding archives can all wait a few hours. Think of posting letters versus making phone calls. A phone call is immediate but you pay for your time. A letter takes longer, but you can send a thousand at once, cheaply. Batch is the postal service of AI APIs. | Why it matters<br>• Much AI work isn't interactive<br>• Letters vs phone calls<br>• Send a thousand at once, cheaper | Layout: a phone icon with a running meter on one side; on the other, a postbox swallowing a stack of a thousand envelopes with a lower price tag. 0s: title 'Why it matters' fades in top-left. 2s: main graphic begins animating. 5s, 11s, 17s: on-screen bullets reveal one by one, each highlighted as narrated. 27s: hold final frame. |
| 3 | 1:06 | 32 | How does it work? You submit many requests at once, each with your own id. The provider processes them in the background, usually within hours and often much sooner, and you collect the results when they're done. At the time of writing, Anthropic's message batches and OpenAI's batch API are both priced at half of standard rates, and Gemini has a discounted batch mode too. Check the current pricing pages before you plan budgets. | How batch works<br>• Submit many requests with IDs<br>• Background processing (hours max)<br>• Collect results later<br>• Discounted (check current pricing) | Layout: three-step flow Submit → Processing (a clock icon sweeping) → Results; a price tag reading 'discounted — check current pricing' swings onto the flow. 0s: title 'How batch works' fades in top-left. 2s: main graphic begins animating. 5s, 9s, 13s, 17s: on-screen bullets reveal one by one, each highlighted as narrated. 28s: hold final frame. |
| 4 | 1:38 | 48 | With Claude, you call message batches create with a list of requests, each with a custom id and the same parameters you'd send to messages create. You poll until the processing status is ended, then stream the results. Each result has your custom id and a result type: succeeded, errored, canceled or expired. With OpenAI, you write a JSON lines file where each line is a request with a custom id, upload it with the batch purpose, then create a batch pointing to that file and an endpoint like the Responses API. Gemini accepts inline requests on its developer API and cloud storage or BigQuery sources on its enterprise platform. | Provider flows<br>• Claude: batches.create(requests) → results<br>• OpenAI: JSONL file → batches.create<br>• Gemini: inline or cloud-storage source<br>• Result types: succeeded / errored / expired | Layout: three stacked flow diagrams; the Claude one shows a list of request cards, OpenAI shows a .jsonl file icon being uploaded, Gemini shows inline cards and a bucket icon. 0s: title 'Provider flows' fades in top-left. 2s: main graphic begins animating. 5s, 13s, 21s, 29s: on-screen bullets reveal one by one, each highlighted as narrated. 44s: hold final frame. |
| 5 | 2:26 | 21 | The golden rule: results can come back in any order. Never match results by position. Always use your own custom id, like the product SKU or ticket number. That one habit prevents the nightmare scenario where product A gets product B's description, and nobody notices until customers complain. | Key by custom_id<br>• Results arrive in any order<br>• Never match by position<br>• Use SKUs, ticket IDs | Layout: result cards shuffle randomly as they arrive; a 'custom_id' magnet snaps each result onto its matching product card correctly. 0s: title 'Key by custom_id' fades in top-left. 2s: main graphic begins animating. 5s, 7s, 9s: on-screen bullets reveal one by one, each highlighted as narrated. 17s: hold final frame. |
| 6 | 2:47 | 35 | A simple example. A small bakery chain wants a short, friendly description for each of its eighty cakes. Instead of generating them one by one during the day, the owner submits one batch in the evening with each cake's name and ingredients, keyed by its code. In the morning, eighty descriptions are waiting. Two came back errored because the ingredient list was empty, so she fixes those two and resubmits. The whole job cost about half what real time would have. | Simple example: 80 cake descriptions<br>• One evening batch, keyed by cake code<br>• Morning: 78 ready, 2 errored<br>• Fix + resubmit; ~half the cost | Layout: a night sky over a bakery; a batch of 80 cake cards flies off; at sunrise, 78 green cards return and 2 red ones get a 'fix & resubmit' arrow. 0s: title 'Simple example: 80 cake descriptions' fades in top-left. 2s: main graphic begins animating. 5s, 12s, 19s: on-screen bullets reveal one by one, each highlighted as narrated. 31s: hold final frame. |
| 7 | 3:22 | 33 | Now a realistic business example, with illustrative numbers. A Karachi marketplace adds about thirty thousand products a month from sellers' spreadsheets. They used to generate descriptions in real time and hit rate limits during business hours, slowing everything else down. Now a nightly batch handles new SKUs with structured outputs: title, description, feature bullets and category. Morning validation flags a small share for human review, and costs roughly halve versus real time calls on the same model. | Business example: Karachi marketplace<br>• ~30,000 new SKUs/month<br>• Nightly batch + structured outputs<br>• Morning validation, human review for flags<br>• Cost roughly halved (illustrative) | Layout: a spreadsheet of SKUs feeds a nightly batch; structured JSON cards return; a validation gate diverts a few to a reviewer; a cost bar halves. 0s: title 'Business example: Karachi marketplace' fades in top-left. 2s: main graphic begins animating. 5s, 10s, 15s, 20s: on-screen bullets reveal one by one, each highlighted as narrated. 29s: hold final frame. |
| 8 | 3:55 | 45 | How do you decide whether batch is worth it? Do the simple math. Monthly items, times average input and output tokens, times the price per token, once for real time and once for batch. Then add the engineering effort for scheduling, validation and retries, and subtract the cost of rate limit congestion you avoid during the day, when other features slow down. For recurring jobs above a few thousand items a month, batch usually wins clearly. For small or urgent jobs, the overhead may not pay off. Write the numbers down with the date and the pricing page you used, because prices change. | Is batch worth it?<br>• Items × tokens × price, both ways<br>• + engineering effort<br>• − daytime congestion avoided<br>• Record date + pricing source | Layout: a simple calculator panel with three input fields and two result bars (Real-time vs Batch) labeled illustrative; an engineering-hours chip adds to the batch side, a congestion-avoided chip. 0s: title 'Is batch worth it?' fades in top-left. 2s: main graphic begins animating. 5s, 13s, 21s, 29s: on-screen bullets reveal one by one, each highlighted as narrated. 41s: hold final frame. |
| 9 | 4:40 | 29 | Design your pipeline for reality. Record which items went into which batch, so you never resubmit blindly. Collect errored and expired items and resubmit them in a follow up batch, perhaps with a different model. Validate outputs exactly as you would in real time. Schedule submissions in the evening and notify owners when results land. And where supported, stack prompt caching with batch for shared system prompts. | Pipeline design<br>• Track item → batch<br>• Resubmit failures<br>• Same validation as real time<br>• Schedule + notify<br>• Stack caching where supported | Layout: a pipeline with a ledger icon, a retry loop for red items, a validation gate, a moon-to-sun schedule and a bell notification. 0s: title 'Pipeline design' fades in top-left. 2s: main graphic begins animating. 5s, 8s, 11s, 14s, 17s: on-screen bullets reveal one by one, each highlighted as narrated. 25s: hold final frame. |
| 10 | 5:09 | 28 | Also check availability. Batch features differ by platform: Claude's message batches are available on the Claude API and Claude Platform on AWS, but not on every cloud platform version of Claude. OpenAI's batch endpoint supports a specific list of API endpoints. Gemini's batch sources differ between its developer API and enterprise platform. Always confirm in the docs for the exact platform you deploy on. | Availability varies<br>• Claude batches: Claude API, Claude Platform on AWS<br>• OpenAI: listed endpoints only<br>• Gemini: different sources per platform | Layout: a platform matrix with batch support ticks and crosses; a 'check the docs' stamp lands on the table. 0s: title 'Availability varies' fades in top-left. 2s: main graphic begins animating. 5s, 10s, 15s: on-screen bullets reveal one by one, each highlighted as narrated. 24s: hold final frame. |
| 11 | 5:37 | 21 | Common mistakes. Relying on result order. Having no plan for expired or errored items. Treating the batch window as a guarantee for time critical work. And batching anything a person is actively waiting for. If a user is staring at a screen, use real time and streaming instead. | Common mistakes<br>• Order-dependent matching<br>• No failure plan<br>• Time-critical work in batch<br>• Batching user-facing requests | Layout: four mistake cards; the last one shows a user staring at a loading screen with a red X over a batch icon. 0s: title 'Common mistakes' fades in top-left. 2s: main graphic begins animating. 5s, 7s, 9s, 11s: on-screen bullets reveal one by one, each highlighted as narrated. 17s: hold final frame. |
| 12 | 5:58 | 36 | One more operational tip: make batches observable. Log when each batch was submitted, how many items it contained, when it ended, and how many succeeded, errored or expired. Alert if a batch hasn't ended well within the provider's window, or if the error rate jumps. And keep the raw inputs for a few days, so you can resubmit quickly if something goes wrong. Batch jobs run while you sleep, so you want the morning to start with a clear summary, not a mystery. | Make batches observable<br>• Log submit/end times and counts<br>• Alert on delays or error spikes<br>• Keep inputs for quick resubmits | Layout: a morning dashboard card: 'Batch 2026-09-24: 30,112 items — 29,870 ok, 190 errored, 52 expired — ended 03:14', with a small alert bell idle. 0s: title 'Make batches observable' fades in top-left. 2s: main graphic begins animating. 5s, 12s, 19s: on-screen bullets reveal one by one, each highlighted as narrated. 32s: hold final frame. |
| 13 | 6:34 | 46 | Let's deepen the Karachi marketplace example with illustrative numbers. About thirty thousand new products a month, arriving in sellers' spreadsheets. The nightly batch runs on new SKUs only, using structured outputs for title, description, feature bullets and category. Most requests succeed; a small number error on empty or malformed spec sheets and go back to sellers with a request to complete their data. Morning validation flags a couple of percent for human review, mostly items with claims like waterproof or organic that need evidence. Cost per description roughly halved versus real time, and daytime rate limit errors disappeared, so the customer facing assistant got faster too. | Deeper: the marketplace batch (illustrative)<br>• ~30k new SKUs/month<br>• Errored items → back to sellers<br>• ~2% flagged for claims review<br>• Cost halved, daytime 429s gone | Layout: a nightly pipeline. 0s: a spreadsheet of SKUs loads into a batch box. 10s: most results return green; a few red ones route to a 'seller fix' tray. 22s: a 'claims review' tray receives flagged items. 32s: a cost bar halves and a daytime 429 counter drops to zero (illustrative). 44s: hold. |
| 14 | 7:20 | 71 | Watch me do it. Let's walk through the Claude batch script. The products list has two items with a SKU, a name and facts. I create a batch with one request per product. Each request has a custom id, the SKU, and params exactly like a normal messages create: model, max tokens, a system prompt asking for sixty warm words without unverifiable claims, and one user message with the name and facts. The loop retrieves the batch every sixty seconds until the processing status is ended; in production a scheduler does this. Then I stream the results. For each one, if the result type is succeeded, I join the text blocks and store them under the custom id. Otherwise, I store the error type, so I can resubmit those items later. The OpenAI version writes one JSON line per request with a custom id, method, URL and body, uploads the file with the batch purpose, and creates the batch against the Responses endpoint. | Watch me do it: Claude batch<br>• One Request per SKU (custom_id)<br>• Poll until 'ended'<br>• succeeded → text; else record error<br>• OpenAI: JSONL → files.create → batches.create | Layout: code editor left, a results table right. 0s: products list highlights. 10s: batches.create with Request(custom_id=...) highlights. 24s: the polling loop highlights with a clock. 34s: the results loop highlights; the table fills keyed by SKU, one row shows an error. 50s: the OpenAI JSONL snippet highlights and a file icon uploads. 64s: hold. |
| 15 | 8:31 | 36 | Quick recap. Batch APIs process big, non urgent jobs asynchronously at a discount. Claude takes request lists, OpenAI takes JSON lines files, and Gemini takes inline or cloud sources. Key by custom id, handle failures, validate outputs, and keep user facing work real time. Try this now: pick one non urgent workload with at least two hundred items, move it to a batch API with custom id tracking and a retry path, and compare the cost and turnaround with your real time version. | Recap + try this now<br>• Batch for non-urgent bulk<br>• custom_id + failure handling<br>• Move one workload (≥200 items)<br>• Compare cost and turnaround | Layout: recap card; a comparison table with rows Real-time and Batch and columns Cost, Turnaround, Errors fills in (illustrative). 0s: title 'Recap + try this now' fades in top-left. 2s: main graphic begins animating. 5s, 10s, 15s, 20s: on-screen bullets reveal one by one, each highlighted as narrated. 32s: hold final frame. |

<details><summary>Narration per scene (paste into ElevenLabs)</summary>

Scene 1 — Batch APIs

```text
Imagine you need product descriptions for thirty thousand new items, or you want to classify a month of support tickets. You could fire thousands of real time requests, hit rate limits all afternoon, and pay full price. Or you could hand the whole pile to a batch API, go home, and collect the results in the morning at a discount. In this lesson you'll learn when batch is the right call, and how to run it with Claude, OpenAI and Gemini.
```

Scene 2 — Why it matters

```text
Why does this matter? Because a surprising share of AI work isn't interactive at all. Enrichment, classification, content generation for catalogs, evaluation runs and embedding archives can all wait a few hours. Think of posting letters versus making phone calls. A phone call is immediate but you pay for your time. A letter takes longer, but you can send a thousand at once, cheaply. Batch is the postal service of AI APIs.
```

Scene 3 — How batch works

```text
How does it work? You submit many requests at once, each with your own id. The provider processes them in the background, usually within hours and often much sooner, and you collect the results when they're done. At the time of writing, Anthropic's message batches and OpenAI's batch API are both priced at half of standard rates, and Gemini has a discounted batch mode too. Check the current pricing pages before you plan budgets.
```

Scene 4 — Provider flows

```text
With Claude, you call message batches create with a list of requests, each with a custom id and the same parameters you'd send to messages create. You poll until the processing status is ended, then stream the results. Each result has your custom id and a result type: succeeded, errored, canceled or expired. With OpenAI, you write a JSON lines file where each line is a request with a custom id, upload it with the batch purpose, then create a batch pointing to that file and an endpoint like the Responses API. Gemini accepts inline requests on its developer API and cloud storage or BigQuery sources on its enterprise platform.
```

Scene 5 — Key by custom_id

```text
The golden rule: results can come back in any order. Never match results by position. Always use your own custom id, like the product SKU or ticket number. That one habit prevents the nightmare scenario where product A gets product B's description, and nobody notices until customers complain.
```

Scene 6 — Simple example: 80 cake descriptions

```text
A simple example. A small bakery chain wants a short, friendly description for each of its eighty cakes. Instead of generating them one by one during the day, the owner submits one batch in the evening with each cake's name and ingredients, keyed by its code. In the morning, eighty descriptions are waiting. Two came back errored because the ingredient list was empty, so she fixes those two and resubmits. The whole job cost about half what real time would have.
```

Scene 7 — Business example: Karachi marketplace

```text
Now a realistic business example, with illustrative numbers. A Karachi marketplace adds about thirty thousand products a month from sellers' spreadsheets. They used to generate descriptions in real time and hit rate limits during business hours, slowing everything else down. Now a nightly batch handles new SKUs with structured outputs: title, description, feature bullets and category. Morning validation flags a small share for human review, and costs roughly halve versus real time calls on the same model.
```

Scene 8 — Is batch worth it?

```text
How do you decide whether batch is worth it? Do the simple math. Monthly items, times average input and output tokens, times the price per token, once for real time and once for batch. Then add the engineering effort for scheduling, validation and retries, and subtract the cost of rate limit congestion you avoid during the day, when other features slow down. For recurring jobs above a few thousand items a month, batch usually wins clearly. For small or urgent jobs, the overhead may not pay off. Write the numbers down with the date and the pricing page you used, because prices change.
```

Scene 9 — Pipeline design

```text
Design your pipeline for reality. Record which items went into which batch, so you never resubmit blindly. Collect errored and expired items and resubmit them in a follow up batch, perhaps with a different model. Validate outputs exactly as you would in real time. Schedule submissions in the evening and notify owners when results land. And where supported, stack prompt caching with batch for shared system prompts.
```

Scene 10 — Availability varies

```text
Also check availability. Batch features differ by platform: Claude's message batches are available on the Claude API and Claude Platform on AWS, but not on every cloud platform version of Claude. OpenAI's batch endpoint supports a specific list of API endpoints. Gemini's batch sources differ between its developer API and enterprise platform. Always confirm in the docs for the exact platform you deploy on.
```

Scene 11 — Common mistakes

```text
Common mistakes. Relying on result order. Having no plan for expired or errored items. Treating the batch window as a guarantee for time critical work. And batching anything a person is actively waiting for. If a user is staring at a screen, use real time and streaming instead.
```

Scene 12 — Make batches observable

```text
One more operational tip: make batches observable. Log when each batch was submitted, how many items it contained, when it ended, and how many succeeded, errored or expired. Alert if a batch hasn't ended well within the provider's window, or if the error rate jumps. And keep the raw inputs for a few days, so you can resubmit quickly if something goes wrong. Batch jobs run while you sleep, so you want the morning to start with a clear summary, not a mystery.
```

Scene 13 — Deeper: the marketplace batch (illustrative)

```text
Let's deepen the Karachi marketplace example with illustrative numbers. About thirty thousand new products a month, arriving in sellers' spreadsheets. The nightly batch runs on new SKUs only, using structured outputs for title, description, feature bullets and category. Most requests succeed; a small number error on empty or malformed spec sheets and go back to sellers with a request to complete their data. Morning validation flags a couple of percent for human review, mostly items with claims like waterproof or organic that need evidence. Cost per description roughly halved versus real time, and daytime rate limit errors disappeared, so the customer facing assistant got faster too.
```

Scene 14 — Watch me do it: Claude batch

```text
Watch me do it. Let's walk through the Claude batch script. The products list has two items with a SKU, a name and facts. I create a batch with one request per product. Each request has a custom id, the SKU, and params exactly like a normal messages create: model, max tokens, a system prompt asking for sixty warm words without unverifiable claims, and one user message with the name and facts. The loop retrieves the batch every sixty seconds until the processing status is ended; in production a scheduler does this. Then I stream the results. For each one, if the result type is succeeded, I join the text blocks and store them under the custom id. Otherwise, I store the error type, so I can resubmit those items later. The OpenAI version writes one JSON line per request with a custom id, method, URL and body, uploads the file with the batch purpose, and creates the batch against the Responses endpoint.
```

Scene 15 — Recap + try this now

```text
Quick recap. Batch APIs process big, non urgent jobs asynchronously at a discount. Claude takes request lists, OpenAI takes JSON lines files, and Gemini takes inline or cloud sources. Key by custom id, handle failures, validate outputs, and keep user facing work real time. Try this now: pick one non urgent workload with at least two hundred items, move it to a batch API with custom id tracking and a retry path, and compare the cost and turnaround with your real time version.
```

</details>

### 10. Prompt caching across providers

Lesson `prompt-caching-across-providers` · module “Cost, scale and reliability” · target 9 min · 15 scenes · 1225 words · planned 8:51 (words ≈ 8:53) · status: **to produce**

| Term | Say |
|---|---|
| TTL | T T L |
| no-op | no op |

| # | Time | Sec | Narration | On screen | Visual |
|---|---|---|---|---|---|
| 1 | 0:00 | 33 | Every time your support assistant answers a question, it might resend the same twelve thousand token policy document. Imagine paying full price to have a colleague re read the entire handbook before every single customer email. Prompt caching lets the provider remember the part that doesn't change, so repeated requests are cheaper and faster. In this lesson you'll learn how caching works on Claude, OpenAI and Gemini, and how to design prompts that actually hit the cache. | Prompt caching<br>• Pay once for the stable part<br>• Three providers' approaches<br>• Designing for cache hits | Layout: a colleague icon re-reading a thick handbook before each email (counter ticking up); then a bookmark labeled 'cached' appears and the re-reading animation skips straight to the email. 0s: title 'Prompt caching' fades in top-left. 2s: main graphic begins animating. 5s, 11s, 17s: on-screen bullets reveal one by one, each highlighted as narrated. 29s: hold final frame. |
| 2 | 0:33 | 36 | Why does this matter? Because in many applications the stable part of the request is far bigger than the new part. System prompts, tool definitions, policy documents and conversation history often dwarf the user's latest question. Caching can cut the cost of that stable part dramatically and make responses start faster. Think of a coffee shop that pre grinds beans for the morning rush. The first cup takes the full time; every cup after that is quicker because the prep is done. | Why it matters<br>• Stable part ≫ new part<br>• Lower cost, faster first token<br>• The pre-ground coffee analogy | Layout: a stacked bar with a large 'stable prefix' block and a tiny 'new question' block; then a coffee grinder fills a hopper once, and many cups are served quickly. 0s: title 'Why it matters' fades in top-left. 2s: main graphic begins animating. 5s, 12s, 19s: on-screen bullets reveal one by one, each highlighted as narrated. 32s: hold final frame. |
| 3 | 1:09 | 29 | Here's the key idea: caching is a prefix match. The provider can reuse processing only for the beginning of the request, up to the point where something changes. Change one character early on, and everything after it has to be processed again. So the order of your request matters: tools and system prompt first, then reference documents, then examples, then history, and finally the new user message. | Caching is a prefix match<br>• Reuse up to the first change<br>• Order: tools → system → docs → examples → history → new message | Layout: a long request ribbon segmented into labeled blocks in that order; a green 'cached' highlight sweeps from the left and stops exactly where a changed character appears. 0s: title 'Caching is a prefix match' fades in top-left. 2s: main graphic begins animating. 5s, 13s: on-screen bullets reveal one by one, each highlighted as narrated. 25s: hold final frame. |
| 4 | 1:38 | 44 | How do the providers differ? With Claude, you mark cache breakpoints on specific content blocks, up to four, or use automatic caching. The default lifetime is five minutes, with an optional one hour option, and reading the cache refreshes the timer. Cache reads cost roughly a tenth of normal input, and writes cost a little more than normal. OpenAI caches long prompts automatically, and you can pass a prompt cache key to improve reuse. Gemini offers implicit caching on supported models, plus explicit caches you create with a time to live, which are billed for storage. Check current prices before planning. | Provider approaches<br>• Claude: cache_control breakpoints; 5 min / 1 h<br>• OpenAI: automatic + prompt_cache_key<br>• Gemini: implicit + explicit caches (TTL, storage)<br>• Verify current pricing | Layout: three provider panels; Claude shows bookmark icons on blocks with timers; OpenAI shows an automatic 'auto' badge and a key icon; Gemini shows an explicit cache box with an hourglass and a storage. 0s: title 'Provider approaches' fades in top-left. 2s: main graphic begins animating. 5s, 12s, 19s, 26s: on-screen bullets reveal one by one, each highlighted as narrated. 40s: hold final frame. |
| 5 | 2:22 | 28 | How do you know it's working? Read the usage fields. Claude reports cache read and cache creation input tokens. OpenAI reports cached tokens inside the input token details. Gemini reports a cached content token count. If the read number stays at zero across repeated requests, something is changing your prefix. Also remember each provider has a minimum length, so short prompts simply won't cache. | Verify with usage fields<br>• Claude: cache_read / cache_creation tokens<br>• OpenAI: input_tokens_details.cached_tokens<br>• Gemini: cached_content_token_count<br>• Short prompts don't cache | Layout: three usage JSON snippets with the cache fields highlighted; a gauge labeled 'cache hit ratio' rises from 0% to high as requests repeat. 0s: title 'Verify with usage fields' fades in top-left. 2s: main graphic begins animating. 5s, 8s, 11s, 14s: on-screen bullets reveal one by one, each highlighted as narrated. 24s: hold final frame. |
| 6 | 2:50 | 33 | A simple example. A recipe app sends a two thousand word style guide with every request, then a user's question: make this dish vegetarian. At first, the developer adds the current date at the very top of the style guide for freshness. Cache reads stay at zero, because the date changes the prefix every day, and within the day the timestamp includes the time. Moving the date to the end, next to the question, fixes it immediately. | Simple example: the date at the top<br>• Style guide + question<br>• Date at the start → zero cache reads<br>• Move the date to the end → hits | Layout: a request ribbon with a clock chip at the very start glowing red and a 'cache: 0' counter; the chip slides to the end of the ribbon and the counter jumps up. 0s: title 'Simple example: the date at the top' fades in top-left. 2s: main graphic begins animating. 5s, 11s, 17s: on-screen bullets reveal one by one, each highlighted as narrated. 29s: hold final frame. |
| 7 | 3:23 | 37 | Now a realistic business example, with illustrative numbers. A UK retailer's support assistant sends a twelve thousand token returns and delivery policy, plus three thousand tokens of tool definitions, with every question of about a hundred and fifty tokens. The team removed a date stamp from the system prompt, sorted tools in a fixed order, and added a cache breakpoint after the policy. During business hours most requests now read the prefix from cache, input cost per conversation dropped sharply, and time to first token improved. | Business example: UK retail support<br>• 12k policy + 3k tools, 150-token questions<br>• Removed date, sorted tools, breakpoint after policy<br>• Most requests hit the cache (illustrative) | Layout: a before/after cost bar per conversation (illustrative) and a TTFT line dipping; the request ribbon shows a bookmark after the policy block. 0s: title 'Business example: UK retail support' fades in top-left. 2s: main graphic begins animating. 5s, 13s, 21s: on-screen bullets reveal one by one, each highlighted as narrated. 33s: hold final frame. |
| 8 | 4:00 | 36 | What about Gemini's explicit caches? They suit a specific situation: a large document that many requests will use over a known period, like a two hundred page product manual that the support team queries all day. You upload the content once as a cache with a lifetime, and each request references the cache by name instead of resending the manual. You pay a storage cost while it lives, so size the lifetime to the working day, and delete caches you no longer need. | Gemini explicit caches<br>• Big doc, many requests, known window<br>• caches.create with a TTL<br>• Reference by name in each request<br>• Storage cost while alive | Layout: a 200-page manual uploads into a cache box with an hourglass; many request arrows reference the box by name; at day's end, the hourglass runs out and the box is removed. 0s: title 'Gemini explicit caches' fades in top-left. 2s: main graphic begins animating. 5s, 10s, 15s, 20s: on-screen bullets reveal one by one, each highlighted as narrated. 32s: hold final frame. |
| 9 | 4:36 | 30 | Let's list the silent invalidators, because they're responsible for most caching failures. A timestamp or current date in the system prompt. Tool lists built from a dictionary with unpredictable order. Personalization, like the user's name, inserted before the shared document. Switching models mid conversation, since caches belong to each model. And JSON serialized with varying key order or spacing. Each one silently turns your cache into an expensive no op. | Silent invalidators<br>• Timestamps in system prompts<br>• Unordered tool lists<br>• Early personalization<br>• Model switching<br>• Unstable JSON serialization | Layout: five small 'gremlin' icons each tampering with a different part of the prefix ribbon; each gets caught by a net labeled with the fix. 0s: title 'Silent invalidators' fades in top-left. 2s: main graphic begins animating. 5s, 8s, 11s, 14s, 17s: on-screen bullets reveal one by one, each highlighted as narrated. 26s: hold final frame. |
| 10 | 5:06 | 32 | Timing matters too. With a five minute lifetime, a gap of more than five minutes between requests means the next one starts cold. For bursty traffic with longer gaps, a longer lifetime option can pay off, but it usually costs more to write, so compare the premium against how many reads you expect. For Gemini's explicit caches, you pay for storage while they live, so set the lifetime to match your usage window. | Timing and lifetimes<br>• Gaps > TTL = cold cache<br>• Longer TTL: higher write cost<br>• Explicit caches: storage while alive | Layout: a timeline with request dots; gaps longer than a 5-minute bracket turn the next dot blue 'cold'; a toggle to '1 h' shows fewer cold dots but a higher write cost tag. 0s: title 'Timing and lifetimes' fades in top-left. 2s: main graphic begins animating. 5s, 11s, 17s: on-screen bullets reveal one by one, each highlighted as narrated. 28s: hold final frame. |
| 11 | 5:38 | 20 | Common mistakes. Assuming caching works without checking usage fields. Caching tiny prompts that fall below the minimum. Putting per request data at the top. And building elaborate multi model routing that fragments your caches, when one model at a lower effort setting might be cheaper overall. | Common mistakes<br>• Not checking usage fields<br>• Prompts below the minimum<br>• Volatile data first<br>• Routing that fragments caches | Layout: four mistake cards with quick fixes sliding underneath. 0s: title 'Common mistakes' fades in top-left. 2s: main graphic begins animating. 5s, 6s, 7s, 8s: on-screen bullets reveal one by one, each highlighted as narrated. 16s: hold final frame. |
| 12 | 5:58 | 31 | Here's a habit that keeps caching healthy long term: add a cache hit ratio chart to your standard dashboard, next to cost and latency. Caching often breaks silently during an unrelated change, like someone adding a greeting with the user's first name to the top of the system prompt. If the hit ratio drops the day that change ships, you'll catch it in minutes instead of discovering it on next month's invoice. | Keep caching healthy<br>• Hit-ratio chart on the dashboard<br>• Catch silent regressions fast<br>• Before the invoice does | Layout: a dashboard line chart of cache hit ratio that drops sharply on a date annotated 'personalized greeting added'; an alert bell rings; after a fix, the line recovers. 0s: title 'Keep caching healthy' fades in top-left. 2s: main graphic begins animating. 5s, 11s, 17s: on-screen bullets reveal one by one, each highlighted as narrated. 27s: hold final frame. |
| 13 | 6:29 | 48 | Let's deepen the UK retailer's support assistant with illustrative numbers. Twelve thousand tokens of policy, three thousand of tools, and a hundred and fifty token question. Before the fixes, every request paid for fifteen thousand input tokens at full price. After removing the date stamp, sorting tools and adding the breakpoint, during business hours the vast majority of those tokens were cache reads at roughly a tenth of the price. Input cost per conversation fell dramatically, and time to first token improved. Overnight, traffic gaps meant the cache went cold, so the first morning requests paid for a cache write; the team accepted that rather than paying for a longer lifetime. | Deeper: UK retail caching (illustrative)<br>• 15k fixed tokens per request<br>• Business hours: mostly cache reads<br>• Input cost per conversation fell sharply<br>• Overnight cold cache accepted | Layout: a day timeline. 0s: request bars during business hours turn mostly 'cached' green. 12s: an input-cost gauge drops. 22s: overnight gaps appear; the first morning bar shows a 'cache write' segment. 34s: a decision card 'keep 5-min TTL' appears. 46s: hold. |
| 14 | 7:17 | 67 | Watch me do it. Let's walk through the caching example. The policy file is read once at start up. The system parameter is a list of two text blocks: a short instruction, then the full policy with cache control set to ephemeral. That marker says: cache everything up to and including this block. The customer's question goes in messages, after the cached part. After each call, I print four usage numbers: cache creation tokens, cache read tokens, uncached input tokens and output tokens. I ask, can I return sale items. The output shows a large cache write and zero reads. Then, within five minutes, how long do refunds take: now cache write is zero and cache read is large, while uncached input is just the question. If I add today's date to the first system block and run again, reads drop back to zero, and I've just reproduced the most common caching bug. | Watch me do it: cache breakpoints<br>• System = [instruction, policy + cache_control]<br>• Question stays uncached<br>• Call 1: write; call 2: read<br>• Add a date → reads drop to 0 | Layout: code editor top, terminal bottom. 0s: the system list highlights; cache_control circled. 12s: messages line highlights as 'volatile'. 22s: terminal line 1 shows a large cache_write, cache_read=0. 34s: line 2 shows cache_read large. 46s: a date is inserted into the first block; line 3 shows cache_read=0 in red. 60s: hold. |
| 15 | 8:24 | 27 | Quick recap. Caching reuses an identical prefix. Order content from stable to volatile, freeze the stable part, verify with usage fields, and watch for silent invalidators and timing gaps. Try this now: add caching to one workload that repeats a long prefix, log the cache usage fields for fifty requests, remove any invalidators you find, and report your hit ratio before and after. | Recap + try this now<br>• Stable first, volatile last<br>• Verify with usage fields<br>• 50 requests, fix invalidators<br>• Report hit ratio before/after | Layout: recap card; a before/after hit-ratio gauge pair (illustrative) animates from low to high. 0s: title 'Recap + try this now' fades in top-left. 2s: main graphic begins animating. 5s, 8s, 11s, 14s: on-screen bullets reveal one by one, each highlighted as narrated. 23s: hold final frame. |

<details><summary>Narration per scene (paste into ElevenLabs)</summary>

Scene 1 — Prompt caching

```text
Every time your support assistant answers a question, it might resend the same twelve thousand token policy document. Imagine paying full price to have a colleague re read the entire handbook before every single customer email. Prompt caching lets the provider remember the part that doesn't change, so repeated requests are cheaper and faster. In this lesson you'll learn how caching works on Claude, OpenAI and Gemini, and how to design prompts that actually hit the cache.
```

Scene 2 — Why it matters

```text
Why does this matter? Because in many applications the stable part of the request is far bigger than the new part. System prompts, tool definitions, policy documents and conversation history often dwarf the user's latest question. Caching can cut the cost of that stable part dramatically and make responses start faster. Think of a coffee shop that pre grinds beans for the morning rush. The first cup takes the full time; every cup after that is quicker because the prep is done.
```

Scene 3 — Caching is a prefix match

```text
Here's the key idea: caching is a prefix match. The provider can reuse processing only for the beginning of the request, up to the point where something changes. Change one character early on, and everything after it has to be processed again. So the order of your request matters: tools and system prompt first, then reference documents, then examples, then history, and finally the new user message.
```

Scene 4 — Provider approaches

```text
How do the providers differ? With Claude, you mark cache breakpoints on specific content blocks, up to four, or use automatic caching. The default lifetime is five minutes, with an optional one hour option, and reading the cache refreshes the timer. Cache reads cost roughly a tenth of normal input, and writes cost a little more than normal. OpenAI caches long prompts automatically, and you can pass a prompt cache key to improve reuse. Gemini offers implicit caching on supported models, plus explicit caches you create with a time to live, which are billed for storage. Check current prices before planning.
```

Scene 5 — Verify with usage fields

```text
How do you know it's working? Read the usage fields. Claude reports cache read and cache creation input tokens. OpenAI reports cached tokens inside the input token details. Gemini reports a cached content token count. If the read number stays at zero across repeated requests, something is changing your prefix. Also remember each provider has a minimum length, so short prompts simply won't cache.
```

Scene 6 — Simple example: the date at the top

```text
A simple example. A recipe app sends a two thousand word style guide with every request, then a user's question: make this dish vegetarian. At first, the developer adds the current date at the very top of the style guide for freshness. Cache reads stay at zero, because the date changes the prefix every day, and within the day the timestamp includes the time. Moving the date to the end, next to the question, fixes it immediately.
```

Scene 7 — Business example: UK retail support

```text
Now a realistic business example, with illustrative numbers. A UK retailer's support assistant sends a twelve thousand token returns and delivery policy, plus three thousand tokens of tool definitions, with every question of about a hundred and fifty tokens. The team removed a date stamp from the system prompt, sorted tools in a fixed order, and added a cache breakpoint after the policy. During business hours most requests now read the prefix from cache, input cost per conversation dropped sharply, and time to first token improved.
```

Scene 8 — Gemini explicit caches

```text
What about Gemini's explicit caches? They suit a specific situation: a large document that many requests will use over a known period, like a two hundred page product manual that the support team queries all day. You upload the content once as a cache with a lifetime, and each request references the cache by name instead of resending the manual. You pay a storage cost while it lives, so size the lifetime to the working day, and delete caches you no longer need.
```

Scene 9 — Silent invalidators

```text
Let's list the silent invalidators, because they're responsible for most caching failures. A timestamp or current date in the system prompt. Tool lists built from a dictionary with unpredictable order. Personalization, like the user's name, inserted before the shared document. Switching models mid conversation, since caches belong to each model. And JSON serialized with varying key order or spacing. Each one silently turns your cache into an expensive no op.
```

Scene 10 — Timing and lifetimes

```text
Timing matters too. With a five minute lifetime, a gap of more than five minutes between requests means the next one starts cold. For bursty traffic with longer gaps, a longer lifetime option can pay off, but it usually costs more to write, so compare the premium against how many reads you expect. For Gemini's explicit caches, you pay for storage while they live, so set the lifetime to match your usage window.
```

Scene 11 — Common mistakes

```text
Common mistakes. Assuming caching works without checking usage fields. Caching tiny prompts that fall below the minimum. Putting per request data at the top. And building elaborate multi model routing that fragments your caches, when one model at a lower effort setting might be cheaper overall.
```

Scene 12 — Keep caching healthy

```text
Here's a habit that keeps caching healthy long term: add a cache hit ratio chart to your standard dashboard, next to cost and latency. Caching often breaks silently during an unrelated change, like someone adding a greeting with the user's first name to the top of the system prompt. If the hit ratio drops the day that change ships, you'll catch it in minutes instead of discovering it on next month's invoice.
```

Scene 13 — Deeper: UK retail caching (illustrative)

```text
Let's deepen the UK retailer's support assistant with illustrative numbers. Twelve thousand tokens of policy, three thousand of tools, and a hundred and fifty token question. Before the fixes, every request paid for fifteen thousand input tokens at full price. After removing the date stamp, sorting tools and adding the breakpoint, during business hours the vast majority of those tokens were cache reads at roughly a tenth of the price. Input cost per conversation fell dramatically, and time to first token improved. Overnight, traffic gaps meant the cache went cold, so the first morning requests paid for a cache write; the team accepted that rather than paying for a longer lifetime.
```

Scene 14 — Watch me do it: cache breakpoints

```text
Watch me do it. Let's walk through the caching example. The policy file is read once at start up. The system parameter is a list of two text blocks: a short instruction, then the full policy with cache control set to ephemeral. That marker says: cache everything up to and including this block. The customer's question goes in messages, after the cached part. After each call, I print four usage numbers: cache creation tokens, cache read tokens, uncached input tokens and output tokens. I ask, can I return sale items. The output shows a large cache write and zero reads. Then, within five minutes, how long do refunds take: now cache write is zero and cache read is large, while uncached input is just the question. If I add today's date to the first system block and run again, reads drop back to zero, and I've just reproduced the most common caching bug.
```

Scene 15 — Recap + try this now

```text
Quick recap. Caching reuses an identical prefix. Order content from stable to volatile, freeze the stable part, verify with usage fields, and watch for silent invalidators and timing gaps. Try this now: add caching to one workload that repeats a long prefix, log the cache usage fields for fifty requests, remove any invalidators you find, and report your hit ratio before and after.
```

</details>

### 11. Rate limits, errors, retries and backoff

Lesson `rate-limits-retries-and-backoff` · module “Cost, scale and reliability” · target 9 min · 15 scenes · 1228 words · planned 8:54 (words ≈ 8:54) · status: **to produce**

| Term | Say |
|---|---|
| 429 | four twenty-nine |
| 529 | five twenty-nine |
| jitter | jitter |

| # | Time | Sec | Narration | On screen | Visual |
|---|---|---|---|---|---|
| 1 | 0:00 | 27 | Everything works in testing. Then launch day arrives, traffic spikes, and your logs fill with four twenty nines, timeouts and overload errors. Users see error pages, and your retry code makes it worse by hammering the provider. This lesson shows you how to handle rate limits and errors like a professional, so the busiest day of your year is also a calm one. | Rate limits and retries<br>• Error classes<br>• Backoff done right<br>• Protecting upstream<br>• Graceful degradation | Layout: a traffic graph spikes; a wall of red '429' and '529' badges appears; then the badges shrink as a calm control panel labeled 'Backoff • Limiter • Breaker' switches on. 0s: title 'Rate limits and retries' fades in top-left. 2s: main graphic begins animating. 5s, 8s, 11s, 14s: on-screen bullets reveal one by one, each highlighted as narrated. 23s: hold final frame. |
| 2 | 0:27 | 33 | Why does this matter? Because AI APIs fail more often and more variously than most APIs you've used. Traffic spikes hit rate limits, providers occasionally get overloaded, long generations time out, and some requests are simply invalid. Think of a busy restaurant kitchen at peak hour. A good kitchen doesn't panic when orders pile up. It has a ticket rail, a clear rule for what to cook first, and a backup plan when the grill goes down. | Why it matters<br>• Varied, frequent failure modes<br>• The peak-hour kitchen<br>• Ticket rail, priorities, backup plans | Layout: a kitchen at peak hour with a full ticket rail; a chef calmly pulls tickets in order; a 'grill down' sign triggers a switch to a second station. 0s: title 'Why it matters' fades in top-left. 2s: main graphic begins animating. 5s, 11s, 17s: on-screen bullets reveal one by one, each highlighted as narrated. 29s: hold final frame. |
| 3 | 1:00 | 37 | First, classify errors. Rate limits, the four twenty nine status, mean slow down and try again later, respecting any retry after header. Provider overloads and server errors, like five hundred, five oh three, or Anthropic's five twenty nine overloaded error, are usually temporary, so retry with backoff. Timeouts and connection errors can be retried if the operation is safe to repeat. But bad requests, the four hundreds, authentication errors and not found errors should never be retried. They mean fix your request or configuration. | Classify errors<br>• 429 → wait, respect retry-after<br>• 5xx / 529 → retry with backoff<br>• Timeouts → retry if idempotent<br>• 400/401/403/404 → fix, don't retry | Layout: a sorting machine with three chutes labeled 'Retry', 'Retry if safe', 'Fix it'; error badges drop in and slide down the correct chute. 0s: title 'Classify errors' fades in top-left. 2s: main graphic begins animating. 5s, 11s, 17s, 23s: on-screen bullets reveal one by one, each highlighted as narrated. 33s: hold final frame. |
| 4 | 1:37 | 29 | There's one more class that's easy to mishandle: content outcomes, like a model refusal or a safety block. On some newer Claude models, a refusal comes back as a normal response with a refusal stop reason. Retrying blindly usually gets the same result and wastes money. Treat it as a product case: show a helpful message, route to a human, or use a documented fallback approach. | Content outcomes<br>• Refusals and safety blocks<br>• Not transient errors<br>• Handle as product cases | Layout: a response card with 'stop_reason: refusal' highlighted; a looping retry arrow is crossed out and replaced by a 'route to human' arrow. 0s: title 'Content outcomes' fades in top-left. 2s: main graphic begins animating. 5s, 10s, 15s: on-screen bullets reveal one by one, each highlighted as narrated. 25s: hold final frame. |
| 5 | 2:06 | 30 | How do rate limits work? Providers limit requests per minute and tokens per minute, per organization or project and per model, and raise limits as your usage tier grows. Response headers tell you how much room you have left, and four twenty nine responses often tell you how long to wait. Check your current limits in each provider's console rather than hard coding numbers from a blog post. | How rate limits work<br>• Requests/min and tokens/min<br>• Per org/project and per model<br>• Headers show remaining capacity<br>• Check your console, not a blog | Layout: two gauges labeled RPM and TPM with needles moving; a response header panel shows 'ratelimit-remaining' fields ticking down; a 'retry-after: 12' badge appears on a 429. 0s: title 'How rate limits work' fades in top-left. 2s: main graphic begins animating. 5s, 9s, 13s, 17s: on-screen bullets reveal one by one, each highlighted as narrated. 26s: hold final frame. |
| 6 | 2:36 | 37 | Now backoff done right. Wait a base amount, doubling with each attempt, with a cap, and add randomness called jitter so thousands of clients don't retry at the same instant. Respect retry after when the provider sends it. Cap the number of attempts and the total time, then fail gracefully. And here's a subtle trap: the official Anthropic and OpenAI SDKs already retry some errors, twice by default. If you wrap them in your own aggressive retry loop, one failure can multiply into many calls. | Backoff done right<br>• Exponential + cap + jitter<br>• Respect retry-after<br>• Cap attempts and total time<br>• Beware double retries (SDK + yours) | Layout: a timeline of retry attempts with growing gaps (1s, 2s, 4s…) and small random offsets; a second timeline shows SDK retries nested inside custom retries multiplying into a dense cluster, labeled 'retry storm'. |
| 7 | 3:13 | 30 | A simple example. A freelancer's script summarizes two hundred articles in a loop. Halfway through, it hits a rate limit and crashes. The fix takes ten minutes: let the SDK retry with its defaults, add a short pause between requests, and catch the rate limit error to wait for the retry after period before continuing. The script now finishes every time, just a little slower during busy periods. | Simple example: the crashing loop<br>• 200 summaries → 429 → crash<br>• SDK retries + small pause<br>• Catch 429, wait retry-after<br>• Finishes every time | Layout: a progress bar at 50% crashes with a red 429; the replay shows short pauses between items, a brief amber wait at the 429, then the bar completes to 100%. 0s: title 'Simple example: the crashing loop' fades in top-left. 2s: main graphic begins animating. 5s, 9s, 13s, 17s: on-screen bullets reveal one by one, each highlighted as narrated. 26s: hold final frame. |
| 8 | 3:43 | 43 | Let's look at a circuit breaker more closely, because the name sounds more complex than it is. It's a small piece of code that counts recent failures for a provider. If failures pass a threshold, say five in thirty seconds, the breaker opens and your code stops calling that provider for a short cool down, sending traffic to a fallback instead. After the cool down, it lets a few test requests through. If they succeed, it closes and normal traffic resumes. Like the breaker in your home's fuse box, it protects the whole system from one failing part. | Circuit breaker, simply<br>• Count recent failures<br>• Threshold → open: stop calling, use fallback<br>• Cool-down → test requests<br>• Success → close | Layout: a fuse-box style switch with three states (Closed, Open, Half-open); red error dots accumulate until the switch flips Open and traffic reroutes to a fallback; after a timer, two test dots pass. 0s: title 'Circuit breaker, simply' fades in top-left. 2s: main graphic begins animating. 5s, 12s, 19s, 26s: on-screen bullets reveal one by one, each highlighted as narrated. 39s: hold final frame. |
| 9 | 4:26 | 35 | Beyond retries, protect yourself upstream. Put a client side rate limiter, like a token bucket, in front of the provider to smooth bursts. Put non interactive work on a queue with controlled concurrency. Add a circuit breaker: if a provider fails repeatedly, stop sending for a short time and switch to a fallback, instead of hammering it. Set sensible timeouts and stream long outputs. And design graceful degradation, like cached answers, a smaller model, or a we'll email you message. | Protect upstream<br>• Token-bucket limiter<br>• Queues for background work<br>• Circuit breaker + fallback<br>• Timeouts + streaming<br>• Graceful degradation | Layout: a request flow passes through a bucket icon (tokens dripping in at a steady rate), then a breaker switch that flips open after repeated red errors and diverts traffic to a fallback model. 0s: title 'Protect upstream' fades in top-left. 2s: main graphic begins animating. 5s, 9s, 13s, 17s, 21s: on-screen bullets reveal one by one, each highlighted as narrated. 31s: hold final frame. |
| 10 | 5:01 | 35 | Now a realistic business example, with illustrative numbers. On Black Friday, a Dubai fashion retailer's product question traffic jumped about eight times. Before the fix, rate limit storms hit, the SDK's retries plus a custom retry loop multiplied calls, and shoppers saw errors. After the fix: a token bucket limiter per model, the custom retry loop removed, a circuit breaker that shifted overflow to a second provider, and a queue for non urgent tasks. Error rates stayed low and conversion held. | Business example: Black Friday in Dubai<br>• ~8× traffic spike (illustrative)<br>• Before: 429 storms, double retries<br>• After: limiter, breaker, fallback, queue<br>• Errors low, conversion held | Layout: a traffic chart with a Black Friday spike; the 'before' overlay shows red error bars; the 'after' overlay shows flat low errors with a small fallback slice in a second color. 0s: title 'Business example: Black Friday in Dubai' fades in top-left. 2s: main graphic begins animating. 5s, 10s, 15s, 20s: on-screen bullets reveal one by one, each highlighted as narrated. 31s: hold final frame. |
| 11 | 5:36 | 18 | Common mistakes. Retrying four hundreds, which just repeats the bug. Retry storms without jitter. Double retries from the SDK plus your own loop. Treating refusals as temporary errors. And no graceful degradation, so the only failure mode is an error page. | Common mistakes<br>• Retrying 400s<br>• No jitter<br>• Double retries<br>• Retrying refusals<br>• No graceful degradation | Layout: five red cards fall like dominoes; a single 'backoff + breaker' card stops the chain. 0s: title 'Common mistakes' fades in top-left. 2s: main graphic begins animating. 5s, 6s, 7s, 8s, 9s: on-screen bullets reveal one by one, each highlighted as narrated. 14s: hold final frame. |
| 12 | 5:54 | 37 | How do you know your error handling works? Test it on purpose. In staging, point your client at a mock server that returns four twenty nines, five hundreds and slow responses on a schedule, or use a load testing tool to exceed your own rate limits. Watch what users would see, how many total calls are made per request, and whether the circuit breaker and fallback kick in. It's far better to discover a retry storm in a controlled test than on your biggest sales day. | Test failure on purpose<br>• Mock 429s, 500s, slow responses<br>• Load-test past your limits<br>• Check calls per request + fallbacks | Layout: a staging environment diagram with a 'chaos mock' box injecting red 429 and 500 badges; a dashboard shows calls-per-request staying flat and a fallback slice appearing. 0s: title 'Test failure on purpose' fades in top-left. 2s: main graphic begins animating. 5s, 13s, 21s: on-screen bullets reveal one by one, each highlighted as narrated. 33s: hold final frame. |
| 13 | 6:31 | 43 | Let's deepen the Dubai fashion retailer's Black Friday with illustrative numbers. Traffic reached roughly eight times normal for six hours. In the previous year, 429 errors peaked at a painful share of requests, and duplicate retries from the SDK plus a custom loop multiplied provider calls. This year, the token bucket smoothed bursts, the circuit breaker opened twice for a few minutes, shifting overflow to the second provider, and non urgent tasks like review summaries waited in a queue until the evening. Customer facing errors stayed low throughout, and the engineering team, for once, watched the dashboard instead of firefighting. | Deeper: Black Friday (illustrative)<br>• ~8× traffic for 6 hours<br>• Token bucket smoothed bursts<br>• Breaker opened twice → fallback<br>• Non-urgent tasks queued until evening | Layout: a six-hour traffic chart. 0s: the traffic line spikes. 7s: a token-bucket icon flattens the request rate line. 14s: two short breaker-open markers show traffic shifting to a second provider color. 23s: a queue icon holds 'review summaries' until evening. 31s: a low error-rate line. 40s: hold. |
| 14 | 7:14 | 69 | Watch me do it. Let's walk through the retry wrapper. I create the Anthropic client with SDK retries turned off, because this wrapper owns retries, and a sixty second timeout. Retryable lists rate limit errors, internal server errors, which cover five hundreds including overloaded responses, connection errors and timeouts. The loop tries up to five times. A bad request error is our bug, so I log it and raise immediately. An authentication error means configuration, so I raise. For retryable errors, I read the retry after header when the error carries a response, and if there isn't one, compute exponential backoff capped at thirty seconds, multiplied by a random factor between a half and one and a half. That's the jitter. I log and sleep, then try again. Any other status error, like a four oh three or four oh four, raises. After five attempts, I raise a clear provider unavailable error, which the router turns into a fallback. | Watch me do it: call_with_backoff()<br>• max_retries=0 (wrapper owns retries)<br>• 400/401 → raise now<br>• retry-after, else capped exponential × jitter<br>• After 5 → provider unavailable → fallback | Layout: code editor with a retry timeline overlay. 0s: client construction highlights 'max_retries=0'. 10s: RETRYABLE tuple highlights. 20s: BadRequestError and AuthenticationError branches highlight in red. 32s: the retry-after and backoff lines highlight; the timeline shows gaps 1s, 2s, 4s with jitter. 50s: the final RuntimeError highlights. 62s: hold. |
| 15 | 8:23 | 31 | Quick recap. Classify errors and only retry what's retryable. Use exponential backoff with jitter, respect retry after, and don't stack retries on top of the SDK's. Protect upstream with limiters, queues and circuit breakers, and degrade gracefully. Try this now: wrap your provider calls with typed error handling and backoff, add a simple token bucket limiter, and run a small load test that triggers rate limits to confirm your app stays calm. | Recap + try this now<br>• Classify, back off, cap<br>• Limiters, queues, breakers<br>• Load-test your 429 handling | Layout: recap card; a small load-test dashboard shows requests climbing, some 28s absorbed, and a steady green success line. |

<details><summary>Narration per scene (paste into ElevenLabs)</summary>

Scene 1 — Rate limits and retries

```text
Everything works in testing. Then launch day arrives, traffic spikes, and your logs fill with four twenty nines, timeouts and overload errors. Users see error pages, and your retry code makes it worse by hammering the provider. This lesson shows you how to handle rate limits and errors like a professional, so the busiest day of your year is also a calm one.
```

Scene 2 — Why it matters

```text
Why does this matter? Because AI APIs fail more often and more variously than most APIs you've used. Traffic spikes hit rate limits, providers occasionally get overloaded, long generations time out, and some requests are simply invalid. Think of a busy restaurant kitchen at peak hour. A good kitchen doesn't panic when orders pile up. It has a ticket rail, a clear rule for what to cook first, and a backup plan when the grill goes down.
```

Scene 3 — Classify errors

```text
First, classify errors. Rate limits, the four twenty nine status, mean slow down and try again later, respecting any retry after header. Provider overloads and server errors, like five hundred, five oh three, or Anthropic's five twenty nine overloaded error, are usually temporary, so retry with backoff. Timeouts and connection errors can be retried if the operation is safe to repeat. But bad requests, the four hundreds, authentication errors and not found errors should never be retried. They mean fix your request or configuration.
```

Scene 4 — Content outcomes

```text
There's one more class that's easy to mishandle: content outcomes, like a model refusal or a safety block. On some newer Claude models, a refusal comes back as a normal response with a refusal stop reason. Retrying blindly usually gets the same result and wastes money. Treat it as a product case: show a helpful message, route to a human, or use a documented fallback approach.
```

Scene 5 — How rate limits work

```text
How do rate limits work? Providers limit requests per minute and tokens per minute, per organization or project and per model, and raise limits as your usage tier grows. Response headers tell you how much room you have left, and four twenty nine responses often tell you how long to wait. Check your current limits in each provider's console rather than hard coding numbers from a blog post.
```

Scene 6 — Backoff done right

```text
Now backoff done right. Wait a base amount, doubling with each attempt, with a cap, and add randomness called jitter so thousands of clients don't retry at the same instant. Respect retry after when the provider sends it. Cap the number of attempts and the total time, then fail gracefully. And here's a subtle trap: the official Anthropic and OpenAI SDKs already retry some errors, twice by default. If you wrap them in your own aggressive retry loop, one failure can multiply into many calls.
```

Scene 7 — Simple example: the crashing loop

```text
A simple example. A freelancer's script summarizes two hundred articles in a loop. Halfway through, it hits a rate limit and crashes. The fix takes ten minutes: let the SDK retry with its defaults, add a short pause between requests, and catch the rate limit error to wait for the retry after period before continuing. The script now finishes every time, just a little slower during busy periods.
```

Scene 8 — Circuit breaker, simply

```text
Let's look at a circuit breaker more closely, because the name sounds more complex than it is. It's a small piece of code that counts recent failures for a provider. If failures pass a threshold, say five in thirty seconds, the breaker opens and your code stops calling that provider for a short cool down, sending traffic to a fallback instead. After the cool down, it lets a few test requests through. If they succeed, it closes and normal traffic resumes. Like the breaker in your home's fuse box, it protects the whole system from one failing part.
```

Scene 9 — Protect upstream

```text
Beyond retries, protect yourself upstream. Put a client side rate limiter, like a token bucket, in front of the provider to smooth bursts. Put non interactive work on a queue with controlled concurrency. Add a circuit breaker: if a provider fails repeatedly, stop sending for a short time and switch to a fallback, instead of hammering it. Set sensible timeouts and stream long outputs. And design graceful degradation, like cached answers, a smaller model, or a we'll email you message.
```

Scene 10 — Business example: Black Friday in Dubai

```text
Now a realistic business example, with illustrative numbers. On Black Friday, a Dubai fashion retailer's product question traffic jumped about eight times. Before the fix, rate limit storms hit, the SDK's retries plus a custom retry loop multiplied calls, and shoppers saw errors. After the fix: a token bucket limiter per model, the custom retry loop removed, a circuit breaker that shifted overflow to a second provider, and a queue for non urgent tasks. Error rates stayed low and conversion held.
```

Scene 11 — Common mistakes

```text
Common mistakes. Retrying four hundreds, which just repeats the bug. Retry storms without jitter. Double retries from the SDK plus your own loop. Treating refusals as temporary errors. And no graceful degradation, so the only failure mode is an error page.
```

Scene 12 — Test failure on purpose

```text
How do you know your error handling works? Test it on purpose. In staging, point your client at a mock server that returns four twenty nines, five hundreds and slow responses on a schedule, or use a load testing tool to exceed your own rate limits. Watch what users would see, how many total calls are made per request, and whether the circuit breaker and fallback kick in. It's far better to discover a retry storm in a controlled test than on your biggest sales day.
```

Scene 13 — Deeper: Black Friday (illustrative)

```text
Let's deepen the Dubai fashion retailer's Black Friday with illustrative numbers. Traffic reached roughly eight times normal for six hours. In the previous year, 429 errors peaked at a painful share of requests, and duplicate retries from the SDK plus a custom loop multiplied provider calls. This year, the token bucket smoothed bursts, the circuit breaker opened twice for a few minutes, shifting overflow to the second provider, and non urgent tasks like review summaries waited in a queue until the evening. Customer facing errors stayed low throughout, and the engineering team, for once, watched the dashboard instead of firefighting.
```

Scene 14 — Watch me do it: call_with_backoff()

```text
Watch me do it. Let's walk through the retry wrapper. I create the Anthropic client with SDK retries turned off, because this wrapper owns retries, and a sixty second timeout. Retryable lists rate limit errors, internal server errors, which cover five hundreds including overloaded responses, connection errors and timeouts. The loop tries up to five times. A bad request error is our bug, so I log it and raise immediately. An authentication error means configuration, so I raise. For retryable errors, I read the retry after header when the error carries a response, and if there isn't one, compute exponential backoff capped at thirty seconds, multiplied by a random factor between a half and one and a half. That's the jitter. I log and sleep, then try again. Any other status error, like a four oh three or four oh four, raises. After five attempts, I raise a clear provider unavailable error, which the router turns into a fallback.
```

Scene 15 — Recap + try this now

```text
Quick recap. Classify errors and only retry what's retryable. Use exponential backoff with jitter, respect retry after, and don't stack retries on top of the SDK's. Protect upstream with limiters, queues and circuit breakers, and degrade gracefully. Try this now: wrap your provider calls with typed error handling and backoff, add a simple token bucket limiter, and run a small load test that triggers rate limits to confirm your app stays calm.
```

</details>

### 12. Cost tracking, attribution and budgets

Lesson `cost-tracking-and-budgets` · module “Cost, scale and reliability” · target 9 min · 16 scenes · 1247 words · planned 9:01 (words ≈ 9:02) · status: **to produce**

| Term | Say |
|---|---|
| pseudonymized | sue-don-im-ized |

| # | Time | Sec | Narration | On screen | Visual |
|---|---|---|---|---|---|
| 1 | 0:00 | 29 | At the end of the month, the invoice arrives and it's forty percent higher than last month. Which feature caused it? Which customer? Which change? If you can't answer in five minutes, this lesson is for you. You'll learn how to track AI costs per call, attribute them to features and customers, set budgets that actually protect you, and talk about AI spend in business terms. | Cost tracking and budgets<br>• Why invoices aren't enough<br>• A per-call ledger<br>• Budgets and alerts<br>• Unit economics | Layout: an invoice card slides in with a red '+40%' badge; question marks appear over three icons labeled Feature, Customer, Change; then a ledger table slides in and the question marks turn into. 0s: title 'Cost tracking and budgets' fades in top-left. 2s: main graphic begins animating. 5s, 9s, 13s, 17s: on-screen bullets reveal one by one, each highlighted as narrated. 25s: hold final frame. |
| 2 | 0:29 | 33 | Why does this matter? AI costs behave differently from most cloud costs. They scale with usage and prompt design, they can jump overnight when a feature goes viral or an agent loops, and prices vary hugely between models. Think of a household electricity bill. The total tells you what you spent, but a smart meter per appliance tells you it was the old freezer in the garage. Your per call ledger is that smart meter. | Why it matters<br>• Usage-driven, spiky, model-dependent<br>• Total bill vs smart meter per appliance<br>• The ledger is your smart meter | Layout: a house with a single electricity meter; then small meters appear on each appliance, and one 'old freezer' meter spins fast in red. 0s: title 'Why it matters' fades in top-left. 2s: main graphic begins animating. 5s, 11s, 17s: on-screen bullets reveal one by one, each highlighted as narrated. 29s: hold final frame. |
| 3 | 1:02 | 31 | Every provider returns usage with each response. Claude reports input and output tokens, plus cache reads and cache writes. OpenAI's Responses API reports input and output tokens, cached tokens, and reasoning tokens. Gemini reports prompt tokens, output tokens, cached tokens and thinking tokens. Here's the catch: reasoning tokens are billed as output on providers that expose them, so a thoughtful model can bill far more tokens than the visible answer suggests. | Usage per provider<br>• Claude: input, output, cache read/write<br>• OpenAI: input, output, cached, reasoning<br>• Gemini: prompt, candidates, cached, thoughts<br>• Reasoning tokens are billed | Layout: three usage JSON snippets with fields highlighted; a visible-answer bubble is small, while a hidden 'reasoning tokens' iceberg below the waterline is much larger. 0s: title 'Usage per provider' fades in top-left. 2s: main graphic begins animating. 5s, 9s, 13s, 17s: on-screen bullets reveal one by one, each highlighted as narrated. 27s: hold final frame. |
| 4 | 1:33 | 30 | So build a ledger. One row per model call, with the timestamp, provider, model, feature, tenant, a pseudonymized user id, environment, every token type, whether it was a batch request, latency, outcome and estimated cost. Calculate cost from prices loaded from configuration with an effective date, never hard coded. And once a month, reconcile your estimates against the provider's usage and billing reports, so you know your numbers are right. | The cost ledger<br>• One row per call<br>• Feature, tenant, tokens, outcome<br>• Prices from config with dates<br>• Monthly reconciliation | Layout: a ledger table fills row by row with columns scrolling horizontally; a 'prices.json (effective 2026-09-01)' card feeds the cost column; a monthly calendar ticks 'reconciled'. 0s: title 'The cost ledger' fades in top-left. 2s: main graphic begins animating. 5s, 9s, 13s, 17s: on-screen bullets reveal one by one, each highlighted as narrated. 26s: hold final frame. |
| 5 | 2:03 | 28 | A simple example. A solo developer runs a writing assistant with three features: rewrite, summarize and translate. After adding a ledger, she sees translation is a third of her requests but two thirds of her cost, because users paste whole documents. She adds an input size limit with a friendly message and moves long documents to a cheaper model. Her bill drops, and nobody complains. | Simple example: three features<br>• Translate: ⅓ of requests, ⅔ of cost<br>• Cause: whole documents pasted<br>• Fix: size limit + cheaper model for long docs | Layout: a pie chart of requests beside a pie chart of cost; the 'Translate' slice grows dramatically in the cost pie; a size-limit gate and a model-switch icon shrink it. 0s: title 'Simple example: three features' fades in top-left. 2s: main graphic begins animating. 5s, 10s, 15s: on-screen bullets reveal one by one, each highlighted as narrated. 24s: hold final frame. |
| 6 | 2:31 | 33 | The lesson's code gives you a provider agnostic cost meter. Small adapter functions convert each provider's usage object into one common shape: input, cached input, cache writes and output. A cost function applies prices from your config, with a batch multiplier where relevant. And a record function writes a row with feature and tenant tags. Check each provider's token accounting, like whether cached tokens are included in the input total, and adjust the adapters if needed. | Hands-on: cost meter<br>• Adapters: from_claude / from_openai / from_gemini<br>• Common Usage shape<br>• cost() with config prices<br>• record() with tags | Layout: three provider usage objects flow into three adapter funnels and emerge as identical 'Usage' cards; a 'cost()' gear stamps a dollar figure; a ledger row appends. 0s: title 'Hands-on: cost meter' fades in top-left. 2s: main graphic begins animating. 5s, 10s, 15s, 20s: on-screen bullets reveal one by one, each highlighted as narrated. 29s: hold final frame. |
| 7 | 3:04 | 31 | Now budgets that protect you. Per request caps, like max tokens, input size limits and agent step caps. Per user and per tenant quotas enforced in your backend. Per feature budgets with alerts at fifty, eighty and a hundred percent. Provider side spend limits and separate projects. And anomaly alerts when cost per request or per tenant jumps compared with the recent average. One customer's runaway loop shouldn't become everyone's problem. | Budgets that protect<br>• Per-request caps<br>• Per-user/tenant quotas<br>• Per-feature budgets + 50/80/100% alerts<br>• Provider limits + anomaly alerts | Layout: a layered shield with five rings labeled as the bullets; a runaway-loop icon hits the tenant quota ring and stops. 0s: title 'Budgets that protect' fades in top-left. 2s: main graphic begins animating. 5s, 9s, 13s, 17s: on-screen bullets reveal one by one, each highlighted as narrated. 27s: hold final frame. |
| 8 | 3:35 | 37 | Here's a practical detail that saves real money: per tenant limits. If you sell an AI feature to many customers, one customer's integration can accidentally call you in a loop overnight. Without limits, their mistake becomes your invoice. Give every tenant a daily token or cost allowance, enforced in your backend before calling the provider. When they hit it, return a clear message and alert your team. Most customers will never notice the limit; the one who hits it will thank you for catching their bug. | Per-tenant limits<br>• Daily token/cost allowance per customer<br>• Enforced before the provider call<br>• Clear message + team alert | Layout: a row of tenant buckets filling at different rates; one overflowing bucket hits a lid labeled 'daily limit' and triggers a bell; other buckets continue normally. 0s: title 'Per-tenant limits' fades in top-left. 2s: main graphic begins animating. 5s, 13s, 21s: on-screen bullets reveal one by one, each highlighted as narrated. 33s: hold final frame. |
| 9 | 4:12 | 34 | Now a realistic business example, with illustrative numbers. An agency working across the UK and Pakistan runs AI workflows for twenty five clients. With the ledger tagged by client and feature, finance produces a monthly AI cost report per client, adds a margin, and invoices it. The report also revealed that one client's weekly report feature cost about ten times more than similar clients, because it attached a sixty page PDF every run. Caching and page selection fixed it. | Business example: agency billing<br>• 25 clients, ledger by client + feature<br>• Monthly cost report + margin<br>• One feature 10× costlier (illustrative)<br>• Fixed with caching + page selection | Layout: a per-client bar chart where one bar towers over the rest; zooming in reveals a 60-page PDF icon; after the fix, the bar drops in line with others. 0s: title 'Business example: agency billing' fades in top-left. 2s: main graphic begins animating. 5s, 10s, 15s, 20s: on-screen bullets reveal one by one, each highlighted as narrated. 30s: hold final frame. |
| 10 | 4:46 | 30 | Finally, speak the language of the business: unit economics. Translate tokens into cost per support conversation resolved, per lead enriched, per product description published, or per active user per month. Then compare that with the value: time saved, revenue, conversion. This is how you decide where a bigger model is worth it, where caching or batch deserve engineering time, and how to price AI features for your own customers. | Unit economics<br>• Cost per outcome, not per token<br>• Compare with value created<br>• Guides model choice and pricing | Layout: a token counter morphs into business icons (support ticket, lead card, product page) each with a 'cost per outcome' tag beside a 'value' tag. 0s: title 'Unit economics' fades in top-left. 2s: main graphic begins animating. 5s, 10s, 15s: on-screen bullets reveal one by one, each highlighted as narrated. 26s: hold final frame. |
| 11 | 5:16 | 22 | Common mistakes. Relying only on the provider invoice. Hard coded prices that silently go stale. Ignoring reasoning tokens and cache writes. And no per tenant limits, so one customer's loop becomes everyone's bill. Each of these is cheap to prevent today and painful to untangle after a surprising invoice arrives. | Common mistakes<br>• Invoice-only visibility<br>• Stale hard-coded prices<br>• Ignoring reasoning and cache writes<br>• No tenant limits | Layout: four red cards with quick green fixes sliding beneath. 0s: title 'Common mistakes' fades in top-left. 2s: main graphic begins animating. 5s, 7s, 9s, 11s: on-screen bullets reveal one by one, each highlighted as narrated. 18s: hold final frame. |
| 12 | 5:38 | 31 | A common question: should you show AI costs to your customers or users? For internal teams, yes: showing each team its monthly AI spend changes behavior quickly. For external customers, it depends on your pricing model. Many products bundle AI into plan tiers with fair use limits, while agencies often pass through costs with a margin. Either way, your ledger gives you the numbers to decide pricing with confidence rather than guesswork. | Showing costs<br>• Internal teams: show spend<br>• Customers: bundle tiers or pass-through + margin<br>• Ledger enables confident pricing | Layout: an internal dashboard with team spend bars; beside it, a pricing page with plan tiers and an 'AI included (fair use)' badge. 0s: title 'Showing costs' fades in top-left. 2s: main graphic begins animating. 5s, 11s, 17s: on-screen bullets reveal one by one, each highlighted as narrated. 27s: hold final frame. |
| 13 | 6:09 | 35 | One last habit: forecast. Take the last four weeks of ledger data per feature, work out cost per outcome and outcomes per week, and project forward with known changes, like a new client, a marketing campaign or a model switch. Add a buffer for growth and price changes, share it with finance, and compare forecast with actual every month. The gap tells you which assumption was wrong, and over time your forecasts become boringly accurate, which is exactly what finance wants. | Forecast monthly<br>• 4 weeks of ledger data per feature<br>• Cost per outcome × expected outcomes<br>• Known changes + buffer<br>• Compare forecast vs actual | Layout: a line chart with four weeks of actuals and a dashed forecast line with a shaded buffer band; next month's actual lands inside the band with a green tick. 0s: title 'Forecast monthly' fades in top-left. 2s: main graphic begins animating. 5s, 10s, 15s, 20s: on-screen bullets reveal one by one, each highlighted as narrated. 31s: hold final frame. |
| 14 | 6:44 | 44 | Let's deepen the UK and Pakistan agency's billing example with illustrative numbers. Twenty five clients, three AI features each. The ledger showed one client's weekly report costing about ten times the median, caused by a sixty page PDF attached to every run. After caching the document and selecting only the relevant pages, that client's cost dropped in line with the others. Finance then introduced a simple pricing rule: AI usage passed through at cost plus a fixed margin, shown as a line on each monthly invoice. Clients appreciated the transparency, and the agency stopped absorbing AI costs it couldn't see before. | Deeper: agency AI billing (illustrative)<br>• One client ~10× the median<br>• Cause: 60-page PDF every run<br>• Fix: cache + page selection<br>• Pass-through + margin on invoices | Layout: a per-client cost bar chart. 0s: bars appear; one towers. 10s: zoom reveals a 60-page PDF icon on that client. 20s: the bar shrinks after 'cache + page selection'. 30s: an invoice line 'AI usage (cost + margin)' appears. 42s: hold. |
| 15 | 7:28 | 65 | Watch me do it. Let's walk through the cost meter. Prices load from a JSON file with an effective date, never hard coded. The usage data class has provider, model, input, cached input, cache writes and output. Three adapter functions convert each provider's usage object. From Claude reads input tokens, cache read and cache creation tokens and output tokens. From OpenAI subtracts cached tokens from input so they aren't double counted. From Gemini subtracts cached tokens from the prompt count and adds thinking tokens to output, because they're billed as output. The cost function multiplies each part by its price per million, with a batch multiplier when relevant. Record builds a row with the feature, tenant, batch flag, cost and timestamp, and prints it, standing in for your data warehouse. I record one call from each provider for the feature product copy and tenant noor, and three comparable rows appear. | Watch me do it: provider-agnostic cost meter<br>• PRICES from config<br>• Usage dataclass + 3 adapters<br>• Avoid double-counting cached tokens<br>• record() rows by feature + tenant | Layout: code editor left, ledger table right. 0s: PRICES load highlights with an 'effective date' tag. 10s: Usage dataclass highlights. 19s: from_claude, from_openai, from_gemini highlight in turn; the cached-token subtraction circled. 39s: cost() highlights. 48s: three ledger rows appear with feature 'product_copy' and tenant 'noor'. 62s: hold. |
| 16 | 8:33 | 28 | Quick recap. Invoices show totals, so build your own per call ledger with feature and tenant tags, prices from config, and monthly reconciliation. Enforce budgets at every level, and express spend as cost per business outcome. Try this now: add a cost ledger to one application, set one budget alert, and calculate the cost per outcome for one feature, like cost per resolved support conversation. | Recap + try this now<br>• Ledger + tags + config prices<br>• Budgets at every level<br>• Cost per outcome for one feature | Layout: recap card; a single KPI tile animates 'Cost per resolved conversation' with an illustrative value and a trend arrow. 0s: title 'Recap + try this now' fades in top-left. 2s: main graphic begins animating. 5s, 10s, 15s: on-screen bullets reveal one by one, each highlighted as narrated. 24s: hold final frame. |

<details><summary>Narration per scene (paste into ElevenLabs)</summary>

Scene 1 — Cost tracking and budgets

```text
At the end of the month, the invoice arrives and it's forty percent higher than last month. Which feature caused it? Which customer? Which change? If you can't answer in five minutes, this lesson is for you. You'll learn how to track AI costs per call, attribute them to features and customers, set budgets that actually protect you, and talk about AI spend in business terms.
```

Scene 2 — Why it matters

```text
Why does this matter? AI costs behave differently from most cloud costs. They scale with usage and prompt design, they can jump overnight when a feature goes viral or an agent loops, and prices vary hugely between models. Think of a household electricity bill. The total tells you what you spent, but a smart meter per appliance tells you it was the old freezer in the garage. Your per call ledger is that smart meter.
```

Scene 3 — Usage per provider

```text
Every provider returns usage with each response. Claude reports input and output tokens, plus cache reads and cache writes. OpenAI's Responses API reports input and output tokens, cached tokens, and reasoning tokens. Gemini reports prompt tokens, output tokens, cached tokens and thinking tokens. Here's the catch: reasoning tokens are billed as output on providers that expose them, so a thoughtful model can bill far more tokens than the visible answer suggests.
```

Scene 4 — The cost ledger

```text
So build a ledger. One row per model call, with the timestamp, provider, model, feature, tenant, a pseudonymized user id, environment, every token type, whether it was a batch request, latency, outcome and estimated cost. Calculate cost from prices loaded from configuration with an effective date, never hard coded. And once a month, reconcile your estimates against the provider's usage and billing reports, so you know your numbers are right.
```

Scene 5 — Simple example: three features

```text
A simple example. A solo developer runs a writing assistant with three features: rewrite, summarize and translate. After adding a ledger, she sees translation is a third of her requests but two thirds of her cost, because users paste whole documents. She adds an input size limit with a friendly message and moves long documents to a cheaper model. Her bill drops, and nobody complains.
```

Scene 6 — Hands-on: cost meter

```text
The lesson's code gives you a provider agnostic cost meter. Small adapter functions convert each provider's usage object into one common shape: input, cached input, cache writes and output. A cost function applies prices from your config, with a batch multiplier where relevant. And a record function writes a row with feature and tenant tags. Check each provider's token accounting, like whether cached tokens are included in the input total, and adjust the adapters if needed.
```

Scene 7 — Budgets that protect

```text
Now budgets that protect you. Per request caps, like max tokens, input size limits and agent step caps. Per user and per tenant quotas enforced in your backend. Per feature budgets with alerts at fifty, eighty and a hundred percent. Provider side spend limits and separate projects. And anomaly alerts when cost per request or per tenant jumps compared with the recent average. One customer's runaway loop shouldn't become everyone's problem.
```

Scene 8 — Per-tenant limits

```text
Here's a practical detail that saves real money: per tenant limits. If you sell an AI feature to many customers, one customer's integration can accidentally call you in a loop overnight. Without limits, their mistake becomes your invoice. Give every tenant a daily token or cost allowance, enforced in your backend before calling the provider. When they hit it, return a clear message and alert your team. Most customers will never notice the limit; the one who hits it will thank you for catching their bug.
```

Scene 9 — Business example: agency billing

```text
Now a realistic business example, with illustrative numbers. An agency working across the UK and Pakistan runs AI workflows for twenty five clients. With the ledger tagged by client and feature, finance produces a monthly AI cost report per client, adds a margin, and invoices it. The report also revealed that one client's weekly report feature cost about ten times more than similar clients, because it attached a sixty page PDF every run. Caching and page selection fixed it.
```

Scene 10 — Unit economics

```text
Finally, speak the language of the business: unit economics. Translate tokens into cost per support conversation resolved, per lead enriched, per product description published, or per active user per month. Then compare that with the value: time saved, revenue, conversion. This is how you decide where a bigger model is worth it, where caching or batch deserve engineering time, and how to price AI features for your own customers.
```

Scene 11 — Common mistakes

```text
Common mistakes. Relying only on the provider invoice. Hard coded prices that silently go stale. Ignoring reasoning tokens and cache writes. And no per tenant limits, so one customer's loop becomes everyone's bill. Each of these is cheap to prevent today and painful to untangle after a surprising invoice arrives.
```

Scene 12 — Showing costs

```text
A common question: should you show AI costs to your customers or users? For internal teams, yes: showing each team its monthly AI spend changes behavior quickly. For external customers, it depends on your pricing model. Many products bundle AI into plan tiers with fair use limits, while agencies often pass through costs with a margin. Either way, your ledger gives you the numbers to decide pricing with confidence rather than guesswork.
```

Scene 13 — Forecast monthly

```text
One last habit: forecast. Take the last four weeks of ledger data per feature, work out cost per outcome and outcomes per week, and project forward with known changes, like a new client, a marketing campaign or a model switch. Add a buffer for growth and price changes, share it with finance, and compare forecast with actual every month. The gap tells you which assumption was wrong, and over time your forecasts become boringly accurate, which is exactly what finance wants.
```

Scene 14 — Deeper: agency AI billing (illustrative)

```text
Let's deepen the UK and Pakistan agency's billing example with illustrative numbers. Twenty five clients, three AI features each. The ledger showed one client's weekly report costing about ten times the median, caused by a sixty page PDF attached to every run. After caching the document and selecting only the relevant pages, that client's cost dropped in line with the others. Finance then introduced a simple pricing rule: AI usage passed through at cost plus a fixed margin, shown as a line on each monthly invoice. Clients appreciated the transparency, and the agency stopped absorbing AI costs it couldn't see before.
```

Scene 15 — Watch me do it: provider-agnostic cost meter

```text
Watch me do it. Let's walk through the cost meter. Prices load from a JSON file with an effective date, never hard coded. The usage data class has provider, model, input, cached input, cache writes and output. Three adapter functions convert each provider's usage object. From Claude reads input tokens, cache read and cache creation tokens and output tokens. From OpenAI subtracts cached tokens from input so they aren't double counted. From Gemini subtracts cached tokens from the prompt count and adds thinking tokens to output, because they're billed as output. The cost function multiplies each part by its price per million, with a batch multiplier when relevant. Record builds a row with the feature, tenant, batch flag, cost and timestamp, and prints it, standing in for your data warehouse. I record one call from each provider for the feature product copy and tenant noor, and three comparable rows appear.
```

Scene 16 — Recap + try this now

```text
Quick recap. Invoices show totals, so build your own per call ledger with feature and tenant tags, prices from config, and monthly reconciliation. Enforce budgets at every level, and express spend as cost per business outcome. Try this now: add a cost ledger to one application, set one budget alert, and calculate the cost per outcome for one feature, like cost per resolved support conversation.
```

</details>

### 13. Multi-provider abstraction and fallbacks

Lesson `provider-abstraction-and-fallbacks` · module “Multi-provider architecture” · target 9 min · 16 scenes · 1291 words · planned 9:20 (words ≈ 9:21) · status: **to produce**

| Term | Say |
|---|---|
| LiteLLM | lite L L M |
| YAML | yam-ul |

| # | Time | Sec | Narration | On screen | Visual |
|---|---|---|---|---|---|
| 1 | 0:00 | 30 | What happens to your product when your AI provider has an outage, raises prices, or releases a model that's worse at your language? If the answer is, we'd have to rewrite a lot of code, this lesson is for you. You'll learn how to build a thin abstraction layer across providers, when to use libraries or gateways instead, and how to design fallbacks that actually work when you need them. | Abstraction and fallbacks<br>• Why abstract<br>• Build, buy or both<br>• The adapter contract<br>• Fallbacks that work | Layout: a product icon wired directly to one provider; a lightning bolt hits the provider and the product goes dark. 0s: title 'Abstraction and fallbacks' fades in top-left. 2s: main graphic begins animating. 5s, 9s, 13s, 17s: on-screen bullets reveal one by one, each highlighted as narrated. 26s: hold final frame. |
| 2 | 0:30 | 30 | Why does this matter? Models improve and prices change every few months, outages happen, and different providers are best at different tasks. Think of a power strip with universal sockets. Your devices don't care which brand of plug is in the wall, because the adapter handles it. An abstraction layer is that power strip for AI: your business logic plugs into one interface, and providers plug in behind it. | Why it matters<br>• Frequent change, outages, task fit<br>• The universal power strip<br>• One interface, swappable providers | Layout: a universal power strip; three differently shaped plugs (provider logos as neutral shapes) click into adapters, while a row of appliances stays plugged into the strip unchanged. 0s: title 'Why it matters' fades in top-left. 2s: main graphic begins animating. 5s, 10s, 15s: on-screen bullets reveal one by one, each highlighted as narrated. 26s: hold final frame. |
| 3 | 1:00 | 38 | You have four options. Build your own thin adapter with the official SDKs, which gives maximum control and full access to each provider's features. Use an open source library or gateway, like LiteLLM or the abstractions in popular frameworks, which is faster to start but may lag on the newest features. Use a managed AI gateway for central keys, logging and fallbacks as a service. Or use OpenAI compatible endpoints, which many providers offer. They're handy for quick switching, but often lag or omit provider specific features. | Build, buy or both<br>• Own thin adapter (official SDKs)<br>• Open-source libraries/gateways<br>• Managed AI gateways<br>• OpenAI-compatible endpoints (with caveats) | Layout: four option cards arranged in a 2×2 grid, each with two small bars labeled 'control' and 'speed to start' at different heights. 0s: title 'Build, buy or both' fades in top-left. 2s: main graphic begins animating. 5s, 11s, 17s, 23s: on-screen bullets reveal one by one, each highlighted as narrated. 34s: hold final frame. |
| 4 | 1:38 | 26 | A common, balanced pattern: define your own small interface, implement it with official SDKs for your top two providers, and add an OpenAI compatible implementation for long tail open models. And for Claude specifically, prefer the official Anthropic SDK or its cloud platform clients rather than compatibility shims, so features like caching controls, structured outputs and thinking work properly. | A balanced pattern<br>• Your interface<br>• Official SDKs for top providers<br>• OpenAI-compatible for the long tail<br>• Official SDK for Claude features | Layout: an interface box in the center; two thick arrows to 'official SDK' provider boxes; a thinner arrow to an 'OpenAI-compatible' box feeding several small open-model icons. 0s: title 'A balanced pattern' fades in top-left. 2s: main graphic begins animating. 5s, 8s, 11s, 14s: on-screen bullets reveal one by one, each highlighted as narrated. 22s: hold final frame. |
| 5 | 2:04 | 36 | What goes in the interface? Keep it small and honest. A request with a system prompt, messages, a max tokens value, an optional JSON schema for structured output, and metadata like feature and tenant for cost tracking. A result with the text, the provider and model used, input and output tokens, and a normalized stop reason: end, length, refusal or error. Normalizing stop reasons and usage is what lets your upper layers decide on retries, fallbacks and billing without caring which provider answered. | The adapter contract<br>• GenRequest: system, messages, max_tokens, json_schema, metadata<br>• GenResult: text, provider, model, tokens, stop<br>• Normalized stop reasons + usage | Layout: two dataclass cards (GenRequest, GenResult) animate field by field; three provider-specific stop-reason labels funnel into four normalized values. 0s: title 'The adapter contract' fades in top-left. 2s: main graphic begins animating. 5s, 12s, 19s: on-screen bullets reveal one by one, each highlighted as narrated. 32s: hold final frame. |
| 6 | 2:40 | 33 | A simple example. A small startup has one feature: rewrite a paragraph in a friendlier tone. Their code calls one generate function with the task name. A configuration file says: use provider A's model, and if it fails, use provider B's. When they later find provider B is just as good and cheaper, they swap the order in the config and rerun their eval. No code changes, no deployment of new logic, just configuration and evidence. | Simple example: tone rewrite<br>• Code calls generate('rewrite')<br>• Config: primary A, fallback B<br>• Swap order after eval → no code change | Layout: a YAML config card with two lines; the lines swap order with a smooth animation; an eval badge 'B ≥ A, cheaper' appears; the code panel stays unchanged. 0s: title 'Simple example: tone rewrite' fades in top-left. 2s: main graphic begins animating. 5s, 11s, 17s: on-screen bullets reveal one by one, each highlighted as narrated. 29s: hold final frame. |
| 7 | 3:13 | 33 | The lesson shows implementations for Claude, OpenAI and Gemini. Each converts the common request into the provider's format, adds structured output settings when a schema is given, and converts the response back, mapping stop reasons and usage. Watch the details: Claude needs max tokens, OpenAI's Responses API takes instructions and input, with store set to false here, and Gemini uses model instead of assistant for earlier turns. Schemas must also satisfy each provider's strict mode rules. | Three implementations<br>• Claude: system + messages + max_tokens<br>• OpenAI: instructions + input, store=False<br>• Gemini: contents with 'model' role<br>• Strict-mode schema rules | Layout: three code panels scroll in turn with the conversion lines highlighted; a small 'schema rules' checklist pins beside them. 0s: title 'Three implementations' fades in top-left. 2s: main graphic begins animating. 5s, 10s, 15s, 20s: on-screen bullets reveal one by one, each highlighted as narrated. 29s: hold final frame. |
| 8 | 3:46 | 32 | Now fallbacks. Define routes per task in configuration: a primary model and one or more fallbacks. The router tries each in order and logs every fallback. But decide carefully what triggers a fallback. Outages, overloads and timeouts should. Bad requests shouldn't, because they'd fail everywhere. And refusals need a product decision rather than an automatic retry elsewhere. Some providers now offer documented server side fallback options for refusals, so check before building your own. | Fallback rules<br>• Routes per task in config<br>• Fall back on outages, overloads, timeouts<br>• Not on 400s<br>• Refusals: product decision | Layout: a router diagram with a decision diamond: 'Outage/timeout?' → yes → next provider; '400?' → stop and fix; 'Refusal?' → product policy box. 0s: title 'Fallback rules' fades in top-left. 2s: main graphic begins animating. 5s, 9s, 13s, 17s: on-screen bullets reveal one by one, each highlighted as narrated. 28s: hold final frame. |
| 9 | 4:18 | 37 | Now a realistic business example, with illustrative numbers. A Riyadh media startup summarizes Arabic and English news for subscribers. Its primary is provider A's mid tier model, which won their Arabic eval. The fallback is provider B's model with an Arabic specific prompt variant, approved under the same data agreement. During a primary provider incident, the circuit breaker opened, traffic moved to the fallback within seconds, and subscribers noticed only slightly different phrasing. They run weekly evals on both, so the fallback stays fit for purpose. | Business example: Riyadh news summaries<br>• Primary: A (best Arabic)<br>• Fallback: B + Arabic prompt variant<br>• Same data agreement<br>• Weekly evals on both | Layout: a subscriber app with Arabic and English summaries; a status panel shows 'Provider A: incident' and traffic shifting to B within seconds; an eval calendar shows weekly checks. 0s: title 'Business example: Riyadh news summaries' fades in top-left. 2s: main graphic begins animating. 5s, 11s, 17s, 23s: on-screen bullets reveal one by one, each highlighted as narrated. 33s: hold final frame. |
| 10 | 4:55 | 26 | Keep your fallbacks honest. Evaluate every model in the chain on the same eval. Maintain per model prompt variants where quality differs. Only route data to providers covered by your data agreements, because failing over to an unapproved provider during an outage can breach commitments. And expect higher cost during incidents, because you lose cache hits on the primary. | Honest fallbacks<br>• Same eval for every model<br>• Per-model prompt variants<br>• Respect data agreements<br>• Expect higher cost during incidents | Layout: four shield icons, one per bullet; the 'data agreements' shield blocks an arrow to an unapproved provider icon. 0s: title 'Honest fallbacks' fades in top-left. 2s: main graphic begins animating. 5s, 8s, 11s, 14s: on-screen bullets reveal one by one, each highlighted as narrated. 22s: hold final frame. |
| 11 | 5:21 | 25 | Common mistakes. Lowest common denominator abstractions that block useful provider features. Untested fallbacks that turn out to be broken during an outage. Falling back on errors that will fail everywhere. And ignoring data agreement boundaries when routing. Any one of these can turn a well intentioned abstraction into a source of outages, poor quality or compliance problems. | Common mistakes<br>• Lowest-common-denominator design<br>• Untested fallbacks<br>• Falling back on 400s<br>• Ignoring data boundaries | Layout: four red cards flip to green fixes. 0s: title 'Common mistakes' fades in top-left. 2s: main graphic begins animating. 5s, 8s, 11s, 14s: on-screen bullets reveal one by one, each highlighted as narrated. 21s: hold final frame. |
| 12 | 5:46 | 32 | A question architects ask: where should the abstraction live, in every service or in one gateway? For a small team, a shared library is simplest. As you grow, a central internal gateway service often wins: one place for keys, routing, budgets, logging and fallbacks, used by every product team over a simple internal API. Either way, keep the interface small, version it, and make provider specific features available when a team genuinely needs them. | Library or gateway?<br>• Small team: shared library<br>• Growing org: internal gateway service<br>• Small, versioned interface<br>• Escape hatch for special features | Layout: several service icons each importing a library, then consolidating to call one central gateway box that holds keys, routes and budgets. 0s: title 'Library or gateway?' fades in top-left. 2s: main graphic begins animating. 5s, 9s, 13s, 17s: on-screen bullets reveal one by one, each highlighted as narrated. 28s: hold final frame. |
| 13 | 6:18 | 35 | A quick word on testing fallbacks, because untested fallbacks are the most common surprise during real outages. Schedule a monthly game day. In staging, block the primary provider with a firewall rule or an invalid endpoint, and run your eval suite through the router. Check three things: that the fallback activates within seconds, that quality stays inside your agreed range, and that logs and cost reports clearly show which requests used the fallback. Then restore the primary and check traffic returns. | Monthly fallback game day<br>• Block the primary in staging<br>• Run the eval through the router<br>• Check activation, quality, logging<br>• Confirm traffic returns | Layout: a calendar with a monthly 'game day' marker; a staging diagram where a wall blocks the primary provider; traffic flows to the fallback; three check badges light up green; the wall is removed. 0s: title 'Monthly fallback game day' fades in top-left. 2s: main graphic begins animating. 5s, 10s, 15s, 20s: on-screen bullets reveal one by one, each highlighted as narrated. 31s: hold final frame. |
| 14 | 6:53 | 49 | Let's deepen the Riyadh news summary example with illustrative numbers. The service sends morning summaries to subscribers at six a.m. During one primary provider incident, requests started timing out at five fifty. The breaker opened after five failures within thirty seconds, and the router sent traffic to the fallback with its Arabic prompt variant. The six a.m. send went out on time. Weekly evals had shown the fallback scored a few points lower on Arabic fluency, so the editor on duty spot checked the summaries that morning and found them acceptable. When the primary recovered, traffic returned automatically, and the incident report was two paragraphs long instead of an apology email to subscribers. | Deeper: the 6 a.m. incident (illustrative)<br>• Timeouts at 5:50<br>• Breaker → fallback + Arabic variant<br>• Send on time; editor spot-check<br>• Auto-return on recovery | Layout: a clock at 5:50 with red timeout marks. 0s: a breaker icon trips. 9s: traffic arrows switch to the fallback provider with an 'AR prompt' tag. 18s: the clock reaches 6:00 and a 'sent' check appears. 27s: an editor icon spot-checks. 35s: traffic switches back when the primary turns green. 46s: hold. |
| 15 | 7:42 | 67 | Watch me do it. Let's walk through the adapter and router. GenRequest carries the system prompt, messages, max tokens, an optional JSON schema and metadata. GenResult carries text, provider, model, tokens and a normalized stop value. The Claude provider builds messages create arguments, adds output config when there's a schema, and maps end turn to end, max tokens to length and refusal to refusal. The OpenAI provider uses instructions, input, max output tokens and store false, adds a text format when there's a schema, and marks length when the response is incomplete. The Gemini provider builds a config, renames assistant to model, and reads usage metadata. The router has routes per task: product copy tries Claude, then Gemini. Generate loops through the route, returns the first success, logs any fallback, and raises if all fail. I point the primary at a broken endpoint and rerun: the log says fallback, and the result comes from Gemini. | Watch me do it: adapter + router<br>• GenRequest / GenResult<br>• Stop-reason mapping per provider<br>• ROUTES per task (config)<br>• Broken primary → Gemini fallback | Layout: code editor left, a router diagram right. 0s: GenRequest and GenResult highlight. 11s: ClaudeProvider stop mapping highlights. 22s: OpenAIProvider 'store=False' and incomplete check highlight. 31s: GeminiProvider role mapping highlights. 40s: ROUTES and generate() highlight. 51s: terminal prints 'fallback: anthropic/... failed' then a Gemini result. 64s: hold. |
| 16 | 8:49 | 31 | Quick recap. A thin adapter makes models swappable by configuration. Normalize usage and stop reasons, route per task with fallbacks for outages, and keep fallbacks evaluated, prompt tuned and within your data agreements. Try this now: implement the adapter for two providers, configure a primary and fallback for one task, run your eval on both, and simulate an outage by pointing the primary at an invalid endpoint to confirm the fallback activates. | Recap + try this now<br>• Adapter + config routes<br>• Honest, evaluated fallbacks<br>• Simulate an outage | Layout: recap card; a test console shows 'primary: connection error → fallback: success' with a green check. 0s: title 'Recap + try this now' fades in top-left. 2s: main graphic begins animating. 5s, 11s, 17s: on-screen bullets reveal one by one, each highlighted as narrated. 27s: hold final frame. |

<details><summary>Narration per scene (paste into ElevenLabs)</summary>

Scene 1 — Abstraction and fallbacks

```text
What happens to your product when your AI provider has an outage, raises prices, or releases a model that's worse at your language? If the answer is, we'd have to rewrite a lot of code, this lesson is for you. You'll learn how to build a thin abstraction layer across providers, when to use libraries or gateways instead, and how to design fallbacks that actually work when you need them.
```

Scene 2 — Why it matters

```text
Why does this matter? Models improve and prices change every few months, outages happen, and different providers are best at different tasks. Think of a power strip with universal sockets. Your devices don't care which brand of plug is in the wall, because the adapter handles it. An abstraction layer is that power strip for AI: your business logic plugs into one interface, and providers plug in behind it.
```

Scene 3 — Build, buy or both

```text
You have four options. Build your own thin adapter with the official SDKs, which gives maximum control and full access to each provider's features. Use an open source library or gateway, like LiteLLM or the abstractions in popular frameworks, which is faster to start but may lag on the newest features. Use a managed AI gateway for central keys, logging and fallbacks as a service. Or use OpenAI compatible endpoints, which many providers offer. They're handy for quick switching, but often lag or omit provider specific features.
```

Scene 4 — A balanced pattern

```text
A common, balanced pattern: define your own small interface, implement it with official SDKs for your top two providers, and add an OpenAI compatible implementation for long tail open models. And for Claude specifically, prefer the official Anthropic SDK or its cloud platform clients rather than compatibility shims, so features like caching controls, structured outputs and thinking work properly.
```

Scene 5 — The adapter contract

```text
What goes in the interface? Keep it small and honest. A request with a system prompt, messages, a max tokens value, an optional JSON schema for structured output, and metadata like feature and tenant for cost tracking. A result with the text, the provider and model used, input and output tokens, and a normalized stop reason: end, length, refusal or error. Normalizing stop reasons and usage is what lets your upper layers decide on retries, fallbacks and billing without caring which provider answered.
```

Scene 6 — Simple example: tone rewrite

```text
A simple example. A small startup has one feature: rewrite a paragraph in a friendlier tone. Their code calls one generate function with the task name. A configuration file says: use provider A's model, and if it fails, use provider B's. When they later find provider B is just as good and cheaper, they swap the order in the config and rerun their eval. No code changes, no deployment of new logic, just configuration and evidence.
```

Scene 7 — Three implementations

```text
The lesson shows implementations for Claude, OpenAI and Gemini. Each converts the common request into the provider's format, adds structured output settings when a schema is given, and converts the response back, mapping stop reasons and usage. Watch the details: Claude needs max tokens, OpenAI's Responses API takes instructions and input, with store set to false here, and Gemini uses model instead of assistant for earlier turns. Schemas must also satisfy each provider's strict mode rules.
```

Scene 8 — Fallback rules

```text
Now fallbacks. Define routes per task in configuration: a primary model and one or more fallbacks. The router tries each in order and logs every fallback. But decide carefully what triggers a fallback. Outages, overloads and timeouts should. Bad requests shouldn't, because they'd fail everywhere. And refusals need a product decision rather than an automatic retry elsewhere. Some providers now offer documented server side fallback options for refusals, so check before building your own.
```

Scene 9 — Business example: Riyadh news summaries

```text
Now a realistic business example, with illustrative numbers. A Riyadh media startup summarizes Arabic and English news for subscribers. Its primary is provider A's mid tier model, which won their Arabic eval. The fallback is provider B's model with an Arabic specific prompt variant, approved under the same data agreement. During a primary provider incident, the circuit breaker opened, traffic moved to the fallback within seconds, and subscribers noticed only slightly different phrasing. They run weekly evals on both, so the fallback stays fit for purpose.
```

Scene 10 — Honest fallbacks

```text
Keep your fallbacks honest. Evaluate every model in the chain on the same eval. Maintain per model prompt variants where quality differs. Only route data to providers covered by your data agreements, because failing over to an unapproved provider during an outage can breach commitments. And expect higher cost during incidents, because you lose cache hits on the primary.
```

Scene 11 — Common mistakes

```text
Common mistakes. Lowest common denominator abstractions that block useful provider features. Untested fallbacks that turn out to be broken during an outage. Falling back on errors that will fail everywhere. And ignoring data agreement boundaries when routing. Any one of these can turn a well intentioned abstraction into a source of outages, poor quality or compliance problems.
```

Scene 12 — Library or gateway?

```text
A question architects ask: where should the abstraction live, in every service or in one gateway? For a small team, a shared library is simplest. As you grow, a central internal gateway service often wins: one place for keys, routing, budgets, logging and fallbacks, used by every product team over a simple internal API. Either way, keep the interface small, version it, and make provider specific features available when a team genuinely needs them.
```

Scene 13 — Monthly fallback game day

```text
A quick word on testing fallbacks, because untested fallbacks are the most common surprise during real outages. Schedule a monthly game day. In staging, block the primary provider with a firewall rule or an invalid endpoint, and run your eval suite through the router. Check three things: that the fallback activates within seconds, that quality stays inside your agreed range, and that logs and cost reports clearly show which requests used the fallback. Then restore the primary and check traffic returns.
```

Scene 14 — Deeper: the 6 a.m. incident (illustrative)

```text
Let's deepen the Riyadh news summary example with illustrative numbers. The service sends morning summaries to subscribers at six a.m. During one primary provider incident, requests started timing out at five fifty. The breaker opened after five failures within thirty seconds, and the router sent traffic to the fallback with its Arabic prompt variant. The six a.m. send went out on time. Weekly evals had shown the fallback scored a few points lower on Arabic fluency, so the editor on duty spot checked the summaries that morning and found them acceptable. When the primary recovered, traffic returned automatically, and the incident report was two paragraphs long instead of an apology email to subscribers.
```

Scene 15 — Watch me do it: adapter + router

```text
Watch me do it. Let's walk through the adapter and router. GenRequest carries the system prompt, messages, max tokens, an optional JSON schema and metadata. GenResult carries text, provider, model, tokens and a normalized stop value. The Claude provider builds messages create arguments, adds output config when there's a schema, and maps end turn to end, max tokens to length and refusal to refusal. The OpenAI provider uses instructions, input, max output tokens and store false, adds a text format when there's a schema, and marks length when the response is incomplete. The Gemini provider builds a config, renames assistant to model, and reads usage metadata. The router has routes per task: product copy tries Claude, then Gemini. Generate loops through the route, returns the first success, logs any fallback, and raises if all fail. I point the primary at a broken endpoint and rerun: the log says fallback, and the result comes from Gemini.
```

Scene 16 — Recap + try this now

```text
Quick recap. A thin adapter makes models swappable by configuration. Normalize usage and stop reasons, route per task with fallbacks for outages, and keep fallbacks evaluated, prompt tuned and within your data agreements. Try this now: implement the adapter for two providers, configure a primary and fallback for one task, run your eval on both, and simulate an outage by pointing the primary at an invalid endpoint to confirm the fallback activates.
```

</details>

### 14. Model routing: the right model for each request

Lesson `model-routing-strategies` · module “Multi-provider architecture” · target 9 min · 15 scenes · 1225 words · planned 8:52 (words ≈ 8:53) · status: **to produce**

| Term | Say |
|---|---|
| Roman-Urdu | roman ur-doo |

| # | Time | Sec | Narration | On screen | Visual |
|---|---|---|---|---|---|
| 1 | 0:00 | 33 | Sending every request to your most powerful model is like sending a senior partner to answer every phone call. The quality is great, but most calls didn't need them, and the bill is huge. Sending everything to the cheapest model is the opposite mistake. Model routing is the art of sending each request to the right model. In this lesson you'll learn the main routing strategies, when they're worth it, and how to test them honestly. | Model routing<br>• Right model per request<br>• Routing strategies<br>• Baselines and cascades<br>• Measuring the router | Layout: a receptionist desk with a phone; calls light up and are routed to three offices labeled Junior, Senior, Partner, with cost tags on each door. 0s: title 'Model routing' fades in top-left. 2s: main graphic begins animating. 5s, 10s, 15s, 20s: on-screen bullets reveal one by one, each highlighted as narrated. 29s: hold final frame. |
| 2 | 0:33 | 32 | Why does this matter? In most products, the mix of requests is lopsided: lots of easy ones, some medium ones, and a few hard ones. If you pay top tier prices for the easy majority, costs balloon. If you send the hard minority to a weak model, quality suffers exactly where it matters most. Think of a hospital triage desk: minor cuts, broken arms and emergencies each go to the right team. That's routing. | Why it matters<br>• Lopsided request mix<br>• Overpaying on easy, underserving hard<br>• The hospital triage desk | Layout: a triage desk with three colored lanes (green, amber, red); patient icons with different conditions are directed into the right lane. 0s: title 'Why it matters' fades in top-left. 2s: main graphic begins animating. 5s, 11s, 17s: on-screen bullets reveal one by one, each highlighted as narrated. 28s: hold final frame. |
| 3 | 1:05 | 34 | Here are the strategies. Static by task: each feature has a configured model, simple and predictable, and right for most products. Rule based: route by input length, language, customer tier or attachments. Classifier based: a small model labels difficulty or intent first. Cascade: try a cheap model, check the output automatically, and escalate if it fails. And effort routing: the same model with different reasoning effort per request, which keeps one cache and is often the simplest win. | Routing strategies<br>• Static by task<br>• Rule-based<br>• Classifier-based<br>• Cascade<br>• Effort routing | Layout: five strategy cards slide in; each shows a tiny diagram (one arrow; if/else; a classifier chip; a two-step ladder; a single model with a dial). 0s: title 'Routing strategies' fades in top-left. 2s: main graphic begins animating. 5s, 9s, 13s, 17s, 21s: on-screen bullets reveal one by one, each highlighted as narrated. 30s: hold final frame. |
| 4 | 1:39 | 31 | Here's the key idea that saves teams months: measure the simple alternative first. Before building a cascade across several models, test the strongest model at a lower effort setting, or a mid tier model for the whole task. Current reasoning models at lower effort often match older top tier quality for much less, and one model keeps your prompt cache working. Compare everything by cost per completed task, including retries and escalations. | Measure the simple baseline first<br>• Strong model at lower effort<br>• Or mid-tier for everything<br>• One model keeps caches warm<br>• Compare cost per completed task | Layout: a scale weighing a complex multi-model router diagram against a single model with an effort dial; an eval scoreboard shows them close in quality, with the single model cheaper. 0s: title 'Measure the simple baseline first' fades in top-left. 2s: main graphic begins animating. 5s, 9s, 13s, 17s: on-screen bullets reveal one by one, each highlighted as narrated. 27s: hold final frame. |
| 5 | 2:10 | 31 | How do cascades work? First, a cheap attempt with a small model. Second, an automatic check: schema validation, business rules, a confidence field or a lightweight judge. Third, escalate to a stronger model when the check fails, sending the original input. Fourth, log which path each request took, and review escalations. The whole thing depends on step two. If you can't reliably detect a bad output automatically, route by input characteristics instead. | Cascades<br>• Cheap attempt<br>• Automatic check<br>• Escalate on failure<br>• Log the path<br>• Only as good as the check | Layout: a ladder diagram: Small model → Check gate → (pass) Done / (fail) Strong model → Done; a spotlight on the check gate labeled 'the critical step'. 0s: title 'Cascades' fades in top-left. 2s: main graphic begins animating. 5s, 8s, 11s, 14s, 17s: on-screen bullets reveal one by one, each highlighted as narrated. 27s: hold final frame. |
| 6 | 2:41 | 36 | A simple example. An online bookshop's assistant gets two kinds of questions: do you have this title in stock, and can you recommend a book for my grandmother who loved a particular novel. Stock questions are simple lookups, so a small fast model plus a tool handles them. Recommendations need taste and nuance, so they go to a stronger model. A single keyword rule based on intent covers almost all traffic. Nothing fancy, but it roughly halves costs without anyone noticing a difference. | Simple example: the bookshop<br>• 'In stock?' → small model + tool<br>• 'Recommend for grandma' → stronger model<br>• One intent rule | Layout: two chat bubbles enter a router; one routes to a small model with a stock-lookup tool icon, the other to a larger model with a book-recommendation card. 0s: title 'Simple example: the bookshop' fades in top-left. 2s: main graphic begins animating. 5s, 12s, 19s: on-screen bullets reveal one by one, each highlighted as narrated. 32s: hold final frame. |
| 7 | 3:17 | 33 | The lesson's code shows a router combining rules and a cheap heuristic classifier. Short questions without sensitive words are simple, long or analytical requests are complex, and everything else is default. Two business rules then adjust it: Arabic or Urdu text never goes to the small model, because the team's eval showed it was weaker there, and enterprise customers never get the small model, because of a contractual quality commitment. Routes themselves come from configuration. | Hands-on router<br>• Heuristic difficulty: simple/default/complex<br>• Rule: Arabic/Urdu → not the small model<br>• Rule: enterprise tier → not the small model<br>• Routes from config | Layout: code panel highlighting classify_difficulty, then the Arabic/Urdu regex line, then the enterprise rule; a flow diagram mirrors each decision. 0s: title 'Hands-on router' fades in top-left. 2s: main graphic begins animating. 5s, 10s, 15s, 20s: on-screen bullets reveal one by one, each highlighted as narrated. 29s: hold final frame. |
| 8 | 3:50 | 41 | Now a realistic business example, with illustrative numbers. A telecom reseller in Pakistan sees about sixty percent simple balance and package questions, thirty percent troubleshooting, and ten percent complaints and billing disputes. Simple English questions go to a small fast model. Urdu and Roman Urdu messages, and troubleshooting, go to a mid tier model. Complaints and disputes go to the strongest model with a human handover rule. Their baseline, mid tier for everything at low effort, was good, but routing beat it on cost at equal quality, so they kept it and review escalations weekly. | Business example: PK telecom support<br>• 60% simple, 30% troubleshooting, 10% disputes (illustrative)<br>• EN simple → small; UR/Roman-Urdu → mid-tier<br>• Disputes → strongest + human handover<br>• Beat the baseline on cost | Layout: a donut chart of traffic mix; three routed lanes with language badges (EN, UR, Roman-Urdu); a comparison bar of 'baseline vs router' cost with equal quality markers. 0s: title 'Business example: PK telecom support' fades in top-left. 2s: main graphic begins animating. 5s, 12s, 19s, 26s: on-screen bullets reveal one by one, each highlighted as narrated. 37s: hold final frame. |
| 9 | 4:31 | 38 | Routers need looking after, like any production system. Re evaluate every time a new model launches, because a new small model might absorb traffic that needed a mid tier model last quarter. Watch the traffic mix too: a marketing campaign or a new market can suddenly raise the share of hard requests. Keep a conversation on one model once it starts, for consistent tone and cache reuse. And log the reason for every routing decision, so support can answer, why did this customer get a weaker answer? | Operating a router<br>• Re-evaluate on model launches<br>• Watch traffic-mix drift<br>• Keep conversations sticky<br>• Log the reason for each route | Layout: a router dashboard with route-share bars shifting over weeks, a 'new model launched' marker, and a log row showing 'route=default reason=arabic_rule'. 0s: title 'Operating a router' fades in top-left. 2s: main graphic begins animating. 5s, 11s, 17s, 23s: on-screen bullets reveal one by one, each highlighted as narrated. 34s: hold final frame. |
| 10 | 5:09 | 25 | How do you evaluate the router itself? Label a few hundred real requests with the cheapest model that gives an acceptable answer. Then measure how often your router picks a model that's too weak, which risks quality, or too strong, which wastes money. Break results down by language and customer tier, because averages hide the groups you're underserving. | Evaluate the router<br>• Label cheapest acceptable model<br>• Under-routing vs over-routing rates<br>• Break down by language and tier | Layout: a confusion-matrix style grid (router choice vs ideal choice) filling with counts; off-diagonal cells highlighted as 'too weak' and 'too strong'. 0s: title 'Evaluate the router' fades in top-left. 2s: main graphic begins animating. 5s, 9s, 13s: on-screen bullets reveal one by one, each highlighted as narrated. 21s: hold final frame. |
| 11 | 5:34 | 19 | Common mistakes. Building a complex router before measuring the one model baseline. Cascades without a reliable automatic check. Ignoring language, which silently lowers quality for some customers. And spreading one conversation across models, which loses cache efficiency and can make the assistant's tone inconsistent. | Common mistakes<br>• Skipping the baseline<br>• Unreliable cascade checks<br>• Ignoring language<br>• One conversation, many models | Layout: four red cards with quick fixes sliding under each. 0s: title 'Common mistakes' fades in top-left. 2s: main graphic begins animating. 5s, 6s, 7s, 8s: on-screen bullets reveal one by one, each highlighted as narrated. 15s: hold final frame. |
| 12 | 5:53 | 35 | One more idea that's easy to overlook: routing by effort within a single model. Instead of sending easy questions to a different, smaller model, you send them to the same strong model with a low reasoning effort, and hard ones with high effort. You keep one prompt style, one cache and one set of behaviors to test, while still saving money on the easy majority. For many teams, this is the best first step before any multi model routing at all. | Effort routing first<br>• Same model, different effort per request<br>• One prompt style, one cache<br>• Often the best first step | Layout: a single model icon with an effort dial; easy request chips set the dial to Low, hard ones to High; a cache icon stays green throughout. 0s: title 'Effort routing first' fades in top-left. 2s: main graphic begins animating. 5s, 12s, 19s: on-screen bullets reveal one by one, each highlighted as narrated. 31s: hold final frame. |
| 13 | 6:28 | 46 | Let's deepen the Pakistani telecom reseller example with illustrative numbers. About sixty thousand messages a month. The baseline, one mid tier model at low effort for everything, was good and simple. The router sent English balance questions to a small model, Urdu, Roman Urdu and troubleshooting to the mid tier model, and disputes to the strongest model with a human handover. Cost per resolved conversation fell by a meaningful margin compared with the baseline, and quality held across all three languages in the eval. The weekly escalation review found one recurring issue: Roman Urdu balance questions misclassified as simple, so they tightened the language rule. | Deeper: telecom routing (illustrative)<br>• ~60k messages/month<br>• Router beat the baseline on cost<br>• Quality held in EN, UR, Roman-Urdu<br>• Weekly review fixed a Roman-Urdu rule | Layout: a Sankey-style flow. 0s: 60k messages enter. 9s: they split into small, mid-tier and strongest lanes with language tags. 21s: a cost-per-resolution bar compares baseline vs router (illustrative). 32s: a review card highlights 'Roman-Urdu balance → misrouted' and a rule update. 43s: hold. |
| 14 | 7:14 | 66 | Watch me do it. Let's walk through the router function. Routes maps three levels to a provider and model: simple, default and complex. A regular expression detects Arabic script characters, which also covers Urdu. Classify difficulty returns simple for short questions without sensitive words like contract, refund or complaint; complex for very long inputs or analytical words like compare or forecast; otherwise default. Route calls the classifier, then applies two business rules. If the text contains Arabic script and the level is simple, it bumps it to default, because the eval showed the small model is weaker there. If the tenant is enterprise and the level is simple, it also bumps to default, honoring a contract. Finally it returns the provider and model for that level. I test four messages: an English balance question goes small, an Urdu one goes default, a refund complaint goes default, and a forecast request goes complex. | Watch me do it: route()<br>• ROUTES: simple / default / complex<br>• Arabic-script regex (covers Urdu)<br>• classify_difficulty() heuristics<br>• Language + enterprise rules | Layout: code editor left, a test table right. 0s: ROUTES highlights. 10s: ARABIC_OR_URDU regex highlights. 20s: classify_difficulty branches highlight. 34s: route() rules highlight. 46s: the test table fills: 'balance?' → small, Urdu text → default, refund complaint → default, forecast → complex. 62s: hold. |
| 15 | 8:20 | 32 | Quick recap. Routing matches requests to models proactively. Start static by task, measure a simple baseline, add rules, classifiers or cascades only when data justifies them, make cascade checks reliable, and evaluate the router by language and tier. Try this now: label a hundred real requests with the cheapest acceptable model, compare a one model baseline against a simple router on cost and quality, and decide with evidence whether routing is worth it. | Recap + try this now<br>• Static first, baseline always<br>• Reliable checks for cascades<br>• 100 labeled requests: baseline vs router | Layout: recap card; a two-row results table (Baseline, Router) with Cost and Quality columns fills in (illustrative). 0s: title 'Recap + try this now' fades in top-left. 2s: main graphic begins animating. 5s, 11s, 17s: on-screen bullets reveal one by one, each highlighted as narrated. 28s: hold final frame. |

<details><summary>Narration per scene (paste into ElevenLabs)</summary>

Scene 1 — Model routing

```text
Sending every request to your most powerful model is like sending a senior partner to answer every phone call. The quality is great, but most calls didn't need them, and the bill is huge. Sending everything to the cheapest model is the opposite mistake. Model routing is the art of sending each request to the right model. In this lesson you'll learn the main routing strategies, when they're worth it, and how to test them honestly.
```

Scene 2 — Why it matters

```text
Why does this matter? In most products, the mix of requests is lopsided: lots of easy ones, some medium ones, and a few hard ones. If you pay top tier prices for the easy majority, costs balloon. If you send the hard minority to a weak model, quality suffers exactly where it matters most. Think of a hospital triage desk: minor cuts, broken arms and emergencies each go to the right team. That's routing.
```

Scene 3 — Routing strategies

```text
Here are the strategies. Static by task: each feature has a configured model, simple and predictable, and right for most products. Rule based: route by input length, language, customer tier or attachments. Classifier based: a small model labels difficulty or intent first. Cascade: try a cheap model, check the output automatically, and escalate if it fails. And effort routing: the same model with different reasoning effort per request, which keeps one cache and is often the simplest win.
```

Scene 4 — Measure the simple baseline first

```text
Here's the key idea that saves teams months: measure the simple alternative first. Before building a cascade across several models, test the strongest model at a lower effort setting, or a mid tier model for the whole task. Current reasoning models at lower effort often match older top tier quality for much less, and one model keeps your prompt cache working. Compare everything by cost per completed task, including retries and escalations.
```

Scene 5 — Cascades

```text
How do cascades work? First, a cheap attempt with a small model. Second, an automatic check: schema validation, business rules, a confidence field or a lightweight judge. Third, escalate to a stronger model when the check fails, sending the original input. Fourth, log which path each request took, and review escalations. The whole thing depends on step two. If you can't reliably detect a bad output automatically, route by input characteristics instead.
```

Scene 6 — Simple example: the bookshop

```text
A simple example. An online bookshop's assistant gets two kinds of questions: do you have this title in stock, and can you recommend a book for my grandmother who loved a particular novel. Stock questions are simple lookups, so a small fast model plus a tool handles them. Recommendations need taste and nuance, so they go to a stronger model. A single keyword rule based on intent covers almost all traffic. Nothing fancy, but it roughly halves costs without anyone noticing a difference.
```

Scene 7 — Hands-on router

```text
The lesson's code shows a router combining rules and a cheap heuristic classifier. Short questions without sensitive words are simple, long or analytical requests are complex, and everything else is default. Two business rules then adjust it: Arabic or Urdu text never goes to the small model, because the team's eval showed it was weaker there, and enterprise customers never get the small model, because of a contractual quality commitment. Routes themselves come from configuration.
```

Scene 8 — Business example: PK telecom support

```text
Now a realistic business example, with illustrative numbers. A telecom reseller in Pakistan sees about sixty percent simple balance and package questions, thirty percent troubleshooting, and ten percent complaints and billing disputes. Simple English questions go to a small fast model. Urdu and Roman Urdu messages, and troubleshooting, go to a mid tier model. Complaints and disputes go to the strongest model with a human handover rule. Their baseline, mid tier for everything at low effort, was good, but routing beat it on cost at equal quality, so they kept it and review escalations weekly.
```

Scene 9 — Operating a router

```text
Routers need looking after, like any production system. Re evaluate every time a new model launches, because a new small model might absorb traffic that needed a mid tier model last quarter. Watch the traffic mix too: a marketing campaign or a new market can suddenly raise the share of hard requests. Keep a conversation on one model once it starts, for consistent tone and cache reuse. And log the reason for every routing decision, so support can answer, why did this customer get a weaker answer?
```

Scene 10 — Evaluate the router

```text
How do you evaluate the router itself? Label a few hundred real requests with the cheapest model that gives an acceptable answer. Then measure how often your router picks a model that's too weak, which risks quality, or too strong, which wastes money. Break results down by language and customer tier, because averages hide the groups you're underserving.
```

Scene 11 — Common mistakes

```text
Common mistakes. Building a complex router before measuring the one model baseline. Cascades without a reliable automatic check. Ignoring language, which silently lowers quality for some customers. And spreading one conversation across models, which loses cache efficiency and can make the assistant's tone inconsistent.
```

Scene 12 — Effort routing first

```text
One more idea that's easy to overlook: routing by effort within a single model. Instead of sending easy questions to a different, smaller model, you send them to the same strong model with a low reasoning effort, and hard ones with high effort. You keep one prompt style, one cache and one set of behaviors to test, while still saving money on the easy majority. For many teams, this is the best first step before any multi model routing at all.
```

Scene 13 — Deeper: telecom routing (illustrative)

```text
Let's deepen the Pakistani telecom reseller example with illustrative numbers. About sixty thousand messages a month. The baseline, one mid tier model at low effort for everything, was good and simple. The router sent English balance questions to a small model, Urdu, Roman Urdu and troubleshooting to the mid tier model, and disputes to the strongest model with a human handover. Cost per resolved conversation fell by a meaningful margin compared with the baseline, and quality held across all three languages in the eval. The weekly escalation review found one recurring issue: Roman Urdu balance questions misclassified as simple, so they tightened the language rule.
```

Scene 14 — Watch me do it: route()

```text
Watch me do it. Let's walk through the router function. Routes maps three levels to a provider and model: simple, default and complex. A regular expression detects Arabic script characters, which also covers Urdu. Classify difficulty returns simple for short questions without sensitive words like contract, refund or complaint; complex for very long inputs or analytical words like compare or forecast; otherwise default. Route calls the classifier, then applies two business rules. If the text contains Arabic script and the level is simple, it bumps it to default, because the eval showed the small model is weaker there. If the tenant is enterprise and the level is simple, it also bumps to default, honoring a contract. Finally it returns the provider and model for that level. I test four messages: an English balance question goes small, an Urdu one goes default, a refund complaint goes default, and a forecast request goes complex.
```

Scene 15 — Recap + try this now

```text
Quick recap. Routing matches requests to models proactively. Start static by task, measure a simple baseline, add rules, classifiers or cascades only when data justifies them, make cascade checks reliable, and evaluate the router by language and tier. Try this now: label a hundred real requests with the cheapest acceptable model, compare a one model baseline against a simple router on cost and quality, and decide with evidence whether routing is worth it.
```

</details>

### 15. Cloud AI platforms: Amazon Bedrock, Microsoft Foundry and Gemini Enterprise Agent Platform

Lesson `cloud-ai-platforms` · module “Cloud platforms and open-weight models” · target 9 min · 15 scenes · 1255 words · planned 9:05 (words ≈ 9:06) · status: **to produce**

| Term | Say |
|---|---|
| Bedrock | bed-rock |
| Mantle | man-tul |
| Entra | en-tra |
| BigQuery | big query |
| Vertex | ver-tex |

| # | Time | Sec | Narration | On screen | Visual |
|---|---|---|---|---|---|
| 1 | 0:00 | 29 | If you work in a bank, a hospital, a government agency or a large enterprise, there's a good chance you won't call AI models directly from their makers. You'll go through your company's cloud: Amazon, Microsoft or Google. In this lesson you'll learn why, what each major cloud AI platform offers, what changed with their recent renaming, and how to decide which route fits each workload. | Cloud AI platforms<br>• Why enterprises use them<br>• Bedrock, Microsoft Foundry, Gemini Enterprise Agent Platform<br>• Choosing the route | Layout: an office tower icon with a lock; three cloud icons labeled AWS, Azure, Google Cloud connect to it via private-line cables; model icons sit inside each cloud. 0s: title 'Cloud AI platforms' fades in top-left. 2s: main graphic begins animating. 5s, 10s, 15s: on-screen bullets reveal one by one, each highlighted as narrated. 25s: hold final frame. |
| 2 | 0:29 | 35 | Why do enterprises prefer cloud platforms? Contracts and billing through existing agreements. Identity and access through the cloud's own roles and managed identities, instead of static keys. Private networking, so traffic never crosses the public internet. Regional deployment for data residency. Centralized logging and governance. And a catalog of many vendors' models in one place. Think of buying through your company's approved supplier portal instead of a personal credit card. Slower sometimes, but procurement, security and finance are already happy. | Why cloud platforms<br>• Contracts + billing<br>• IAM, private networking<br>• Data residency<br>• Governance + model catalogs<br>• The approved-supplier portal | Layout: a checklist of six enterprise needs ticks green beside a 'company supplier portal' screen; a personal credit card icon sits grayed out to the side. 0s: title 'Why cloud platforms' fades in top-left. 2s: main graphic begins animating. 5s, 9s, 13s, 17s, 21s: on-screen bullets reveal one by one, each highlighted as narrated. 31s: hold final frame. |
| 3 | 1:04 | 24 | The trade offs are real, though. New models and features may arrive later than on first party APIs, some features may be missing, request shapes can differ, and prices and quotas are set by the platform. So the question is never just which model. It's which model, on which platform, with which features, in which region. | The trade-offs<br>• Features may lag<br>• Some features missing<br>• Different shapes, prices, quotas<br>• Model × platform × region | Layout: a three-axis diagram labeled Model, Platform, Region; a point moves within it while a 'feature availability' badge changes color. 0s: title 'The trade-offs' fades in top-left. 2s: main graphic begins animating. 5s, 7s, 9s, 11s: on-screen bullets reveal one by one, each highlighted as narrated. 20s: hold final frame. |
| 4 | 1:28 | 36 | First, Amazon Bedrock. It hosts models from Anthropic, Meta, Mistral, Amazon, OpenAI and others. You can use its model agnostic Converse API, or newer Mantle endpoints that keep each provider's native request shape. The Anthropic SDK has a Bedrock Mantle client for Claude, and the OpenAI SDK has a Bedrock provider for OpenAI models hosted there. Separately, Anthropic offers Claude Platform on AWS, which is operated by Anthropic, uses AWS identity and billing, and matches the Claude API's features on the same day. | Amazon Bedrock<br>• Many vendors' models<br>• Converse API + Mantle endpoints<br>• Anthropic and OpenAI SDK clients<br>• Separate: Claude Platform on AWS | Layout: an AWS-styled cloud (no logo) containing model tiles; two SDK plug icons connect; a separate adjacent box labeled 'Claude Platform on AWS (Anthropic-operated)' appears with a parity badge. 0s: title 'Amazon Bedrock' fades in top-left. 2s: main graphic begins animating. 5s, 10s, 15s, 20s: on-screen bullets reveal one by one, each highlighted as narrated. 32s: hold final frame. |
| 5 | 2:04 | 30 | Second, Microsoft Foundry. It used to be called Azure AI Foundry and was renamed at Microsoft Ignite in November twenty twenty five. It offers Azure OpenAI models, plus Claude, Llama, Mistral and many others through its catalog. You authenticate with keys or Microsoft Entra ID, and it integrates with Azure networking, Key Vault, monitoring and content safety. Both the OpenAI and Anthropic SDKs have dedicated clients for it. | Microsoft Foundry<br>• Formerly Azure AI Foundry (renamed Nov 2025)<br>• Azure OpenAI + Claude, Llama, Mistral…<br>• Keys or Entra ID<br>• Azure networking, Key Vault, safety | Layout: a name-change animation from 'Azure AI Foundry' to 'Microsoft Foundry'; beneath it, a catalog grid of model tiles and integration badges (Key Vault, Monitor, Content Safety). 0s: title 'Microsoft Foundry' fades in top-left. 2s: main graphic begins animating. 5s, 9s, 13s, 17s: on-screen bullets reveal one by one, each highlighted as narrated. 26s: hold final frame. |
| 6 | 2:34 | 38 | Third, Google's Gemini Enterprise Agent Platform, which was called Vertex AI until its renaming in twenty twenty six. Existing API endpoints keep working. It offers Gemini models plus partner and open models, including Claude, through its Model Garden. You authenticate with Google Cloud identity. The Google Gen AI SDK switches to it with an enterprise flag and your project and location, and the Anthropic SDK has a client for Claude on Google Cloud. It adds batch prediction from BigQuery or Cloud Storage, context caching and agent services. | Gemini Enterprise Agent Platform<br>• Formerly Vertex AI (renamed 2026)<br>• Gemini + partner/open models incl. Claude<br>• Google Cloud IAM<br>• genai.Client(enterprise=True, project, location) | Layout: a name-change animation from 'Vertex AI' to 'Gemini Enterprise Agent Platform' with a note 'endpoints unchanged'; a Model Garden grid appears with Gemini and partner tiles. 0s: title 'Gemini Enterprise Agent Platform' fades in top-left. 2s: main graphic begins animating. 5s, 11s, 17s, 23s: on-screen bullets reveal one by one, each highlighted as narrated. 34s: hold final frame. |
| 7 | 3:12 | 37 | Here's a practical tip that saves real frustration: check quotas early. On cloud platforms, default quotas for a newly enabled model are often modest, and raising them can take a request and some waiting. Teams build a feature, test it happily with a few users, then launch and hit throttling on day one. Before launch, estimate your peak requests and tokens per minute, compare with your current quota in the platform console, and file increase requests well ahead of time, per model and per region. | Check quotas early<br>• Defaults are often modest<br>• Increases take time<br>• Estimate peak RPM/TPM<br>• Request per model and region, before launch | Layout: a platform console gauge showing 'Quota: 50k TPM' against a projected peak line at '400k TPM'; a 'request increase' form submits, and a calendar shows a lead time before launch day. 0s: title 'Check quotas early' fades in top-left. 2s: main graphic begins animating. 5s, 11s, 17s, 23s: on-screen bullets reveal one by one, each highlighted as narrated. 33s: hold final frame. |
| 8 | 3:49 | 37 | A simple example. A developer at a mid sized company already uses Claude through the first party API for prototypes. Their production team runs on Google Cloud. Moving the same feature into production is mostly a client swap: construct the Claude client for Google Cloud with the project and region, keep the same messages call, and confirm the model is available in that region. The lesson's code shows the same call on the first party API, on Bedrock and on Google Cloud side by side. | Simple example: prototype → production<br>• Prototype: first-party API<br>• Production: company's cloud<br>• Mostly a client swap<br>• Confirm model + region | Layout: three code cards side by side (Anthropic(), AnthropicBedrockMantle(), AnthropicVertex()) with identical messages.create lines highlighted, and different constructor lines. 0s: title 'Simple example: prototype → production' fades in top-left. 2s: main graphic begins animating. 5s, 11s, 17s, 23s: on-screen bullets reveal one by one, each highlighted as narrated. 33s: hold final frame. |
| 9 | 4:26 | 33 | How do you choose? Startups wanting the newest features fastest usually lean to first party APIs. Organizations with existing cloud agreements and identity standards lean to that cloud's platform. Strict data residency or private networking points to a platform with regional deployment and private endpoints. Multi vendor experimentation with one bill suits platform catalogs. And if you need a feature only the first party API has, use it, alongside the platform where appropriate. Many companies run both. | Choosing a route<br>• New features fast → first-party<br>• Existing cloud standards → platform<br>• Residency/private network → platform<br>• Multi-vendor, one bill → catalog<br>• Often both | Layout: a decision table animates row by row with a highlighted 'Leaning' column; at the end, a split diagram shows 'Innovation: first-party' and 'Regulated production: platform'. 0s: title 'Choosing a route' fades in top-left. 2s: main graphic begins animating. 5s, 9s, 13s, 17s, 21s: on-screen bullets reveal one by one, each highlighted as narrated. 29s: hold final frame. |
| 10 | 4:59 | 40 | Now a realistic business example. A bank in the UAE wants a customer service assistant. Requirements: in region processing where available, private networking, bank wide identity, audit logs, and only approved vendors. It deploys through its existing cloud platform contract, with private endpoints and managed identities, so there are no API keys at all. It enables the platform's guardrail and logging services. And it keeps a small first party sandbox, using only synthetic data, to evaluate new models before they reach the platform. Compliance signs off faster because the platform is already approved. | Business example: UAE bank<br>• In-region, private endpoints, managed identity<br>• Platform guardrails + logging<br>• First-party sandbox with synthetic data only<br>• Faster compliance sign-off | Layout: a bank building connected by a private cable to a regional cloud box; inside, badges for Guardrails and Audit logs; a separate small sandbox box labeled 'synthetic data only' sits outside. 0s: title 'Business example: UAE bank' fades in top-left. 2s: main graphic begins animating. 5s, 11s, 17s, 23s: on-screen bullets reveal one by one, each highlighted as narrated. 36s: hold final frame. |
| 11 | 5:39 | 21 | Common mistakes. Assuming every first party feature exists on the platform. Hard coding platform specific model ids throughout your code. Forgetting quotas, which are often lower by default on platforms and need requests to raise. And mixing platforms without clear rules about which data class may go where. | Common mistakes<br>• Assuming feature parity<br>• Hard-coded platform model IDs<br>• Ignoring default quotas<br>• No data-class rules across platforms | Layout: four warning cards; the quota card shows a gauge near its limit with a 'request increase' button. 0s: title 'Common mistakes' fades in top-left. 2s: main graphic begins animating. 5s, 7s, 9s, 11s: on-screen bullets reveal one by one, each highlighted as narrated. 17s: hold final frame. |
| 12 | 6:00 | 36 | One more habit for platform users: keep a living feature matrix. For each model you use, list which platforms and regions it's on, and whether the features you rely on, like structured outputs, prompt caching, batch, citations or server side tools, are available there. Update it when providers announce changes. It sounds like paperwork, but it prevents the classic surprise where a feature works perfectly in the prototype on the first party API, then turns out to be missing in the production region. | Keep a feature matrix<br>• Model × platform × region<br>• Features you rely on<br>• Update on announcements<br>• Avoid prototype-vs-production surprises | Layout: a grid with rows (models) and columns (platform-region pairs); feature icons fill cells; one cell flashes red 'batch not available' next to a prototype badge. 0s: title 'Keep a feature matrix' fades in top-left. 2s: main graphic begins animating. 5s, 10s, 15s, 20s: on-screen bullets reveal one by one, each highlighted as narrated. 32s: hold final frame. |
| 13 | 6:36 | 47 | Let's deepen the UAE bank example. The bank's first AI assistant took months to approve, mainly because the security team had to assess a new vendor from scratch. For the second, they used their existing cloud platform, where the model provider was already on the approved list, identity came from managed identities, and traffic used private endpoints. The assessment focused only on the new use case: what data, which model, what guardrails. Approval took weeks rather than months, illustrative again. They still keep a first party sandbox with synthetic data for trying new models early, and move a model into production only once it appears in their platform region. | Deeper: the bank's second assistant<br>• First: months of vendor assessment<br>• Second: existing platform, approved vendor<br>• Review only the new use case<br>• Weeks instead of months (illustrative) | Layout: two timelines. 0s: 'Assistant 1' shows a long 'vendor assessment' bar. 10s: 'Assistant 2' shows a short 'use-case review' bar with platform badges (IAM, private endpoint). 22s: a sandbox box labeled 'synthetic data' feeds a 'promote when in region' arrow. 36s: hold. |
| 14 | 7:23 | 68 | Watch me do it. Let's walk through the same Claude call on three routes. The prompt is one user message asking for a two sentence policy summary. Route one, the first party API: the default client reads a key or workload identity, and I pass the bare model id. Route two, Amazon Bedrock through the Mantle client: I pass the AWS region, and the model id carries the anthropic dot prefix; credentials come from the AWS chain, so there's no Anthropic key. Route three, Google Cloud: the Vertex client takes my project id and a region such as global, uses application default credentials, and takes the bare model id. In every case, the messages create call is identical. That's the point: moving a workload between routes is mostly constructor and model id changes. Before switching, I check the platform's model list for my region and whether features I use, like batch or caching options, are available there. | Watch me do it: one call, three routes<br>• Anthropic() + bare model ID<br>• AnthropicBedrockMantle(region) + 'anthropic.' prefix<br>• AnthropicVertex(project, region) + bare ID<br>• Same messages.create | Layout: three stacked code cards. 0s: the shared prompt highlights. 9s: card 1 constructor highlights. 20s: card 2 constructor and 'anthropic.' prefix highlight. 33s: card 3 constructor highlights with project and region. 45s: the three identical messages.create lines glow together. 56s: a 'check region + features' sticky note appears. 65s: hold. |
| 15 | 8:31 | 34 | Quick recap. Cloud platforms wrap models in enterprise contracts, identity, networking, residency and governance. Bedrock, Microsoft Foundry and Gemini Enterprise Agent Platform all host many vendors' models, including Claude, and two of them were recently renamed. Features, ids and quotas differ, so verify, and consider using both first party and platform routes. Try this now: map your cloud agreements, residency needs and required features, decide which workloads go where, and make one test call through your chosen platform. | Recap + try this now<br>• Platforms = enterprise wrapper<br>• Verify features, IDs, quotas<br>• Map workloads to routes<br>• One test call on your platform | Layout: recap card; a two-column mapping table (Workload → Route) fills in with three example rows. 0s: title 'Recap + try this now' fades in top-left. 2s: main graphic begins animating. 5s, 10s, 15s, 20s: on-screen bullets reveal one by one, each highlighted as narrated. 30s: hold final frame. |

<details><summary>Narration per scene (paste into ElevenLabs)</summary>

Scene 1 — Cloud AI platforms

```text
If you work in a bank, a hospital, a government agency or a large enterprise, there's a good chance you won't call AI models directly from their makers. You'll go through your company's cloud: Amazon, Microsoft or Google. In this lesson you'll learn why, what each major cloud AI platform offers, what changed with their recent renaming, and how to decide which route fits each workload.
```

Scene 2 — Why cloud platforms

```text
Why do enterprises prefer cloud platforms? Contracts and billing through existing agreements. Identity and access through the cloud's own roles and managed identities, instead of static keys. Private networking, so traffic never crosses the public internet. Regional deployment for data residency. Centralized logging and governance. And a catalog of many vendors' models in one place. Think of buying through your company's approved supplier portal instead of a personal credit card. Slower sometimes, but procurement, security and finance are already happy.
```

Scene 3 — The trade-offs

```text
The trade offs are real, though. New models and features may arrive later than on first party APIs, some features may be missing, request shapes can differ, and prices and quotas are set by the platform. So the question is never just which model. It's which model, on which platform, with which features, in which region.
```

Scene 4 — Amazon Bedrock

```text
First, Amazon Bedrock. It hosts models from Anthropic, Meta, Mistral, Amazon, OpenAI and others. You can use its model agnostic Converse API, or newer Mantle endpoints that keep each provider's native request shape. The Anthropic SDK has a Bedrock Mantle client for Claude, and the OpenAI SDK has a Bedrock provider for OpenAI models hosted there. Separately, Anthropic offers Claude Platform on AWS, which is operated by Anthropic, uses AWS identity and billing, and matches the Claude API's features on the same day.
```

Scene 5 — Microsoft Foundry

```text
Second, Microsoft Foundry. It used to be called Azure AI Foundry and was renamed at Microsoft Ignite in November twenty twenty five. It offers Azure OpenAI models, plus Claude, Llama, Mistral and many others through its catalog. You authenticate with keys or Microsoft Entra ID, and it integrates with Azure networking, Key Vault, monitoring and content safety. Both the OpenAI and Anthropic SDKs have dedicated clients for it.
```

Scene 6 — Gemini Enterprise Agent Platform

```text
Third, Google's Gemini Enterprise Agent Platform, which was called Vertex AI until its renaming in twenty twenty six. Existing API endpoints keep working. It offers Gemini models plus partner and open models, including Claude, through its Model Garden. You authenticate with Google Cloud identity. The Google Gen AI SDK switches to it with an enterprise flag and your project and location, and the Anthropic SDK has a client for Claude on Google Cloud. It adds batch prediction from BigQuery or Cloud Storage, context caching and agent services.
```

Scene 7 — Check quotas early

```text
Here's a practical tip that saves real frustration: check quotas early. On cloud platforms, default quotas for a newly enabled model are often modest, and raising them can take a request and some waiting. Teams build a feature, test it happily with a few users, then launch and hit throttling on day one. Before launch, estimate your peak requests and tokens per minute, compare with your current quota in the platform console, and file increase requests well ahead of time, per model and per region.
```

Scene 8 — Simple example: prototype → production

```text
A simple example. A developer at a mid sized company already uses Claude through the first party API for prototypes. Their production team runs on Google Cloud. Moving the same feature into production is mostly a client swap: construct the Claude client for Google Cloud with the project and region, keep the same messages call, and confirm the model is available in that region. The lesson's code shows the same call on the first party API, on Bedrock and on Google Cloud side by side.
```

Scene 9 — Choosing a route

```text
How do you choose? Startups wanting the newest features fastest usually lean to first party APIs. Organizations with existing cloud agreements and identity standards lean to that cloud's platform. Strict data residency or private networking points to a platform with regional deployment and private endpoints. Multi vendor experimentation with one bill suits platform catalogs. And if you need a feature only the first party API has, use it, alongside the platform where appropriate. Many companies run both.
```

Scene 10 — Business example: UAE bank

```text
Now a realistic business example. A bank in the UAE wants a customer service assistant. Requirements: in region processing where available, private networking, bank wide identity, audit logs, and only approved vendors. It deploys through its existing cloud platform contract, with private endpoints and managed identities, so there are no API keys at all. It enables the platform's guardrail and logging services. And it keeps a small first party sandbox, using only synthetic data, to evaluate new models before they reach the platform. Compliance signs off faster because the platform is already approved.
```

Scene 11 — Common mistakes

```text
Common mistakes. Assuming every first party feature exists on the platform. Hard coding platform specific model ids throughout your code. Forgetting quotas, which are often lower by default on platforms and need requests to raise. And mixing platforms without clear rules about which data class may go where.
```

Scene 12 — Keep a feature matrix

```text
One more habit for platform users: keep a living feature matrix. For each model you use, list which platforms and regions it's on, and whether the features you rely on, like structured outputs, prompt caching, batch, citations or server side tools, are available there. Update it when providers announce changes. It sounds like paperwork, but it prevents the classic surprise where a feature works perfectly in the prototype on the first party API, then turns out to be missing in the production region.
```

Scene 13 — Deeper: the bank's second assistant

```text
Let's deepen the UAE bank example. The bank's first AI assistant took months to approve, mainly because the security team had to assess a new vendor from scratch. For the second, they used their existing cloud platform, where the model provider was already on the approved list, identity came from managed identities, and traffic used private endpoints. The assessment focused only on the new use case: what data, which model, what guardrails. Approval took weeks rather than months, illustrative again. They still keep a first party sandbox with synthetic data for trying new models early, and move a model into production only once it appears in their platform region.
```

Scene 14 — Watch me do it: one call, three routes

```text
Watch me do it. Let's walk through the same Claude call on three routes. The prompt is one user message asking for a two sentence policy summary. Route one, the first party API: the default client reads a key or workload identity, and I pass the bare model id. Route two, Amazon Bedrock through the Mantle client: I pass the AWS region, and the model id carries the anthropic dot prefix; credentials come from the AWS chain, so there's no Anthropic key. Route three, Google Cloud: the Vertex client takes my project id and a region such as global, uses application default credentials, and takes the bare model id. In every case, the messages create call is identical. That's the point: moving a workload between routes is mostly constructor and model id changes. Before switching, I check the platform's model list for my region and whether features I use, like batch or caching options, are available there.
```

Scene 15 — Recap + try this now

```text
Quick recap. Cloud platforms wrap models in enterprise contracts, identity, networking, residency and governance. Bedrock, Microsoft Foundry and Gemini Enterprise Agent Platform all host many vendors' models, including Claude, and two of them were recently renamed. Features, ids and quotas differ, so verify, and consider using both first party and platform routes. Try this now: map your cloud agreements, residency needs and required features, decide which workloads go where, and make one test call through your chosen platform.
```

</details>

### 16. Open-weight models via hosted APIs and self-hosting

Lesson `open-weight-models-via-apis` · module “Cloud platforms and open-weight models” · target 9 min · 15 scenes · 1268 words · planned 9:10 (words ≈ 9:11) · status: **to produce**

| Term | Say |
|---|---|
| Qwen | chwen |
| gpt-oss | G P T O S S |
| vLLM | V L L M |
| SGLang | S G lang |
| TGI | T G I |
| Ollama | oh-lah-mah |
| Groq | grok |

| # | Time | Sec | Narration | On screen | Visual |
|---|---|---|---|---|---|
| 1 | 0:00 | 32 | There's a whole world of AI models you can download and run yourself, or rent from specialist providers for a fraction of the price: Llama, Mistral, Qwen, DeepSeek, Gemma, and even OpenAI's own open weight models. For some tasks they're a bargain. For others they fall short. In this lesson you'll learn what open weight really means, the three ways to use these models, and how to decide honestly when they're the right choice. | Open-weight models<br>• What 'open-weight' means<br>• Three ways to use them<br>• Honest evaluation<br>• Licenses and responsibility | Layout: a download icon releases model weight files labeled with family names as text chips; three paths branch out: Hosted API, Cloud catalog, Self-host. 0s: title 'Open-weight models' fades in top-left. 2s: main graphic begins animating. 5s, 9s, 13s, 17s: on-screen bullets reveal one by one, each highlighted as narrated. 28s: hold final frame. |
| 2 | 0:32 | 37 | Why does this matter? Because for high volume, simpler tasks, like classification, tagging or short extraction, the cost difference can be dramatic, and open models give you control: you can pin a version for years and run it inside your own environment. Think of renting versus owning a car. Renting a premium car is great for a special trip. For a daily commute of the same short route, a reliable car you own might be far cheaper, as long as you're ready to handle the maintenance. | Why it matters<br>• Cost at high volume<br>• Version control and data control<br>• Renting vs owning a car | Layout: a premium rental car icon beside a practical owned car with a maintenance toolbox; a daily-commute route loops many times under the owned car with a lower cost meter. 0s: title 'Why it matters' fades in top-left. 2s: main graphic begins animating. 5s, 13s, 21s: on-screen bullets reveal one by one, each highlighted as narrated. 33s: hold final frame. |
| 3 | 1:09 | 30 | What does open weight mean? The trained weights are published so anyone can run the model, under a license. Some licenses are permissive, like Apache two point oh. Others are custom, with conditions such as usage thresholds or acceptable use rules. And open weight doesn't always mean fully open source; training data and code are often not released. So before any commercial use, read the model card and the license. | What 'open-weight' means<br>• Weights published, license applies<br>• Permissive (e.g., Apache 2.0) or custom conditions<br>• Not always fully open source<br>• Read card + license | Layout: a model card document with sections Weights ✓, Training data ?, License highlighted; two license stamps appear: 'Permissive' and 'Custom conditions'. 0s: title 'What 'open-weight' means' fades in top-left. 2s: main graphic begins animating. 5s, 9s, 13s, 17s: on-screen bullets reveal one by one, each highlighted as narrated. 26s: hold final frame. |
| 4 | 1:39 | 32 | There are three ways to use them. First, hosted inference APIs: specialist providers such as Groq, Together AI and Fireworks, Hugging Face Inference Providers, and routers like OpenRouter, all pay per token. Second, cloud catalogs: Bedrock, Microsoft Foundry and Google's Model Garden host popular open models with enterprise controls. Third, self hosting with inference servers like vLLM, SGLang or Hugging Face TGI on your own GPUs, or Ollama for local development and small deployments. | Three ways<br>• Hosted inference APIs<br>• Cloud platform catalogs<br>• Self-hosting (vLLM, SGLang, TGI, Ollama) | Layout: three lanes; lane 1 shows a pay-per-token meter; lane 2 shows a cloud with an enterprise badge; lane 3 shows a GPU server rack with a wrench. 0s: title 'Three ways' fades in top-left. 2s: main graphic begins animating. 5s, 11s, 17s: on-screen bullets reveal one by one, each highlighted as narrated. 28s: hold final frame. |
| 5 | 2:11 | 31 | Here's the key idea that makes switching easy: most hosts, and self hosting servers like vLLM and Ollama, expose an OpenAI compatible chat completions endpoint. So you can use the OpenAI SDK and just change the base URL and the API key. But compatibility is partial. Tool calling, structured outputs, streaming details and token accounting vary by provider and model, so test every feature you depend on before you switch production traffic. | The OpenAI-compatible pattern<br>• Same SDK, new base_url + key<br>• Works with most hosts, vLLM, Ollama<br>• Partial compatibility: test features | Layout: an OpenAI SDK client icon with a 'base_url' dial that clicks between several endpoint labels; a feature checklist beside it shows some ticks and some question marks. 0s: title 'The OpenAI-compatible pattern' fades in top-left. 2s: main graphic begins animating. 5s, 11s, 17s: on-screen bullets reveal one by one, each highlighted as narrated. 27s: hold final frame. |
| 6 | 2:42 | 31 | A simple example. On your laptop, install Ollama, pull a small model, and point the OpenAI SDK at localhost port one one four three four. Ask it to say hello in Urdu and Arabic. In a few minutes you have a private model running with no data leaving your machine. It's perfect for experiments, offline demos and learning, though a small local model won't match a frontier model on hard tasks. | Simple example: Ollama on a laptop<br>• ollama pull <model><br>• base_url = localhost:11434/v1<br>• Private, offline experiments<br>• Not frontier quality | Layout: a laptop with a terminal running 'ollama pull'; a small code snippet shows the base_url line; a chat bubble returns greetings in Urdu and Arabic script. 0s: title 'Simple example: Ollama on a laptop' fades in top-left. 2s: main graphic begins animating. 5s, 9s, 13s, 17s: on-screen bullets reveal one by one, each highlighted as narrated. 27s: hold final frame. |
| 7 | 3:13 | 42 | Now a realistic business example, with illustrative numbers. A Karachi courier company handles about two hundred thousand customer messages a month and classifies each into eight categories. That ran on a frontier closed model. The team evaluated an open weight mid size model via a hosted provider on five hundred labeled messages in English, Urdu and Roman Urdu. Accuracy was equal, cost per message was much lower, and responses were faster. They switched classification, kept the frontier model for complex complaint drafting, and routed everything through their adapter so they can change hosts if prices move. | Business example: Karachi courier<br>• ~200k messages/month, 8 categories<br>• 500 labeled EN/UR/Roman-Urdu messages<br>• Equal accuracy, lower cost, faster<br>• Frontier model kept for complaints | Layout: a message stream flows into a classifier; a comparison panel shows 'Closed frontier' vs 'Open mid-size' with equal accuracy bars and a much smaller cost bar (illustrative). 0s: title 'Business example: Karachi courier' fades in top-left. 2s: main graphic begins animating. 5s, 12s, 19s, 26s: on-screen bullets reveal one by one, each highlighted as narrated. 38s: hold final frame. |
| 8 | 3:55 | 41 | Let's talk about fine tuning, because it's one of the main reasons teams choose open weights. If you have thousands of high quality examples of a narrow task, like classifying your own ticket categories or writing in a very specific house style, fine tuning an open model can make a small, cheap model perform like a much larger one on that task. But start with prompting and a good eval first. Many teams discover that clear instructions and a few examples get them most of the way, without the cost of maintaining a custom model. | Fine-tuning, in context<br>• Narrow task + thousands of examples<br>• Small model can punch above its weight<br>• Try prompting + eval first | Layout: a small model icon receives a stack of labeled examples and grows a 'specialist' badge; beside it, a simpler path labeled 'prompt + examples' reaches nearly the same score line (illustrative). 0s: title 'Fine-tuning, in context' fades in top-left. 2s: main graphic begins animating. 5s, 14s, 23s: on-screen bullets reveal one by one, each highlighted as narrated. 37s: hold final frame. |
| 9 | 4:36 | 33 | What about self hosting? Serving a model with vLLM can be one command, and it gives you an OpenAI compatible API. But production means planning GPU capacity, since model size and context length drive memory, tuning batching and throughput, autoscaling, monitoring, security patching and model updates. Self hosting usually pays off at steady high volume, or when data must never leave your environment. Otherwise, a hosted API is often cheaper once you count engineering time. | Self-hosting reality<br>• One command to start (vLLM)<br>• GPUs, batching, autoscaling, monitoring, patching<br>• Pays off at steady volume or strict data control | Layout: a 'vllm serve' command line expands into an operations checklist around a GPU rack; a break-even chart compares hosted vs self-hosted cost by volume (illustrative). 0s: title 'Self-hosting reality' fades in top-left. 2s: main graphic begins animating. 5s, 11s, 17s: on-screen bullets reveal one by one, each highlighted as narrated. 29s: hold final frame. |
| 10 | 5:09 | 32 | Evaluate open models honestly, with the same eval you use for closed ones. Include your languages, safety relevant cases, like refusing harmful requests and not leaking the system prompt, and structured output reliability. Open models may need stronger prompting, guardrails or fine tuning to match closed models on your tasks, and sometimes they win outright on narrow ones. And add your own safety layer, because some open models are less restrictive by default. | Honest evaluation<br>• Same eval as closed models<br>• Languages, safety, structured output<br>• May need prompting/guardrails/fine-tuning<br>• Add your own safety layer | Layout: an eval scoreboard with rows Quality, Safety, JSON reliability, Latency, Cost for two models; a shield icon labeled 'your safety layer' attaches to the open model. 0s: title 'Honest evaluation' fades in top-left. 2s: main graphic begins animating. 5s, 9s, 13s, 17s: on-screen bullets reveal one by one, each highlighted as narrated. 28s: hold final frame. |
| 11 | 5:41 | 22 | Common mistakes. Assuming open means no license obligations. Comparing on public benchmarks instead of your data. Underestimating the operations of self hosting. And relying on OpenAI compatible endpoints for features they only partially support. Test the exact features you need, on the exact host and model, before you move real traffic. | Common mistakes<br>• 'Open' ≠ no obligations<br>• Benchmarks over your data<br>• Underestimated ops<br>• Over-trusting compatibility | Layout: four red cards flip to green fixes. 0s: title 'Common mistakes' fades in top-left. 2s: main graphic begins animating. 5s, 7s, 9s, 11s: on-screen bullets reveal one by one, each highlighted as narrated. 18s: hold final frame. |
| 12 | 6:03 | 33 | Finally, keep a short responsibility checklist for every open model you ship. Record the exact model name and version, where it's hosted, and the license terms that apply. Note known limitations from the model card. Confirm your own safety layer is in front of it, with moderation and refusal tests. And put a reminder in the calendar to review it every quarter, because hosts change prices and models get updated. Auditors and future colleagues will thank you. | Responsibility checklist<br>• Exact model + version + host<br>• License terms<br>• Known limitations<br>• Your safety layer<br>• Quarterly review | Layout: a one-page checklist card fills field by field, ending with a calendar icon marked 'quarterly review'. 0s: title 'Responsibility checklist' fades in top-left. 2s: main graphic begins animating. 5s, 9s, 13s, 17s, 21s: on-screen bullets reveal one by one, each highlighted as narrated. 29s: hold final frame. |
| 13 | 6:36 | 50 | Let's deepen the Karachi courier example with illustrative numbers. Two hundred thousand messages a month, eight categories, English, Urdu and Roman Urdu. On five hundred labeled messages, the open weight mid size model matched the frontier model's accuracy within a point, responded faster through a specialist host, and cost a small fraction per message. The team kept an eye on two risks. First, host dependency: they configured a second host serving the same open model, so switching was a base URL change. Second, license: legal confirmed the model's license allowed their commercial use at their scale. Six months later, a new version of the model arrived, and they reran the same eval before upgrading. | Deeper: courier classification (illustrative)<br>• Accuracy within ~1 point of frontier<br>• Faster and much cheaper<br>• Second host configured<br>• License checked; eval before upgrades | Layout: a comparison panel. 0s: accuracy bars for frontier vs open model nearly equal. 9s: latency and cost bars favor the open model (illustrative). 19s: two host icons with a base_url switch. 28s: a license document with a green check. 37s: a 'new version → rerun eval' card. 47s: hold. |
| 14 | 7:26 | 68 | Watch me do it. Let's walk through the OpenAI compatible calls. The hosted client is just the OpenAI SDK with a base URL and an API key from environment variables, both taken from the host's documentation. I call chat completions create with the model id from an environment variable, because every host names models slightly differently. The system message says: classify the ticket as billing, delivery, product or other, one word. The user message is the customer's complaint. Max tokens is five and temperature zero, for short, stable labels. The answer comes back in choices zero message content: delivery. Then the local version: after ollama pull, I point the same SDK at localhost port one one four three four slash v1, with a dummy key, and ask for a greeting in Urdu and Arabic. Finally, for self hosting, one vllm serve command exposes the same kind of endpoint on port eight thousand. Three setups, one client library. | Watch me do it: OpenAI-compatible endpoints<br>• OpenAI(base_url, api_key) from env<br>• chat.completions.create, max_tokens=5, temperature=0<br>• Ollama at localhost:11434/v1<br>• vLLM serve on :8000 | Layout: code editor top, terminal bottom. 0s: hosted client construction highlights base_url and api_key. 12s: the classification call highlights; terminal prints 'delivery'. 28s: the Ollama client highlights with the localhost URL; terminal prints greetings in Urdu and Arabic script. 44s: a 'vllm serve' command appears in the terminal. 58s: hold. |
| 15 | 8:34 | 36 | Quick recap. Open weight models publish weights under licenses you must read. Use them through hosted APIs, cloud catalogs or self hosting, usually via OpenAI compatible endpoints, and test the features you need. Evaluate on your own data, add safety layers, and self host only when volume or data control justify it. Try this now: pick one high volume, simpler task, evaluate an open weight model through a hosted API against your current model on two hundred labeled examples, and review its license. | Recap + try this now<br>• Read licenses<br>• Hosted, catalog or self-host<br>• Test features you depend on<br>• 200-example head-to-head | Layout: recap card; a head-to-head results table for two models fills with illustrative metrics. 0s: title 'Recap + try this now' fades in top-left. 2s: main graphic begins animating. 5s, 10s, 15s, 20s: on-screen bullets reveal one by one, each highlighted as narrated. 32s: hold final frame. |

<details><summary>Narration per scene (paste into ElevenLabs)</summary>

Scene 1 — Open-weight models

```text
There's a whole world of AI models you can download and run yourself, or rent from specialist providers for a fraction of the price: Llama, Mistral, Qwen, DeepSeek, Gemma, and even OpenAI's own open weight models. For some tasks they're a bargain. For others they fall short. In this lesson you'll learn what open weight really means, the three ways to use these models, and how to decide honestly when they're the right choice.
```

Scene 2 — Why it matters

```text
Why does this matter? Because for high volume, simpler tasks, like classification, tagging or short extraction, the cost difference can be dramatic, and open models give you control: you can pin a version for years and run it inside your own environment. Think of renting versus owning a car. Renting a premium car is great for a special trip. For a daily commute of the same short route, a reliable car you own might be far cheaper, as long as you're ready to handle the maintenance.
```

Scene 3 — What 'open-weight' means

```text
What does open weight mean? The trained weights are published so anyone can run the model, under a license. Some licenses are permissive, like Apache two point oh. Others are custom, with conditions such as usage thresholds or acceptable use rules. And open weight doesn't always mean fully open source; training data and code are often not released. So before any commercial use, read the model card and the license.
```

Scene 4 — Three ways

```text
There are three ways to use them. First, hosted inference APIs: specialist providers such as Groq, Together AI and Fireworks, Hugging Face Inference Providers, and routers like OpenRouter, all pay per token. Second, cloud catalogs: Bedrock, Microsoft Foundry and Google's Model Garden host popular open models with enterprise controls. Third, self hosting with inference servers like vLLM, SGLang or Hugging Face TGI on your own GPUs, or Ollama for local development and small deployments.
```

Scene 5 — The OpenAI-compatible pattern

```text
Here's the key idea that makes switching easy: most hosts, and self hosting servers like vLLM and Ollama, expose an OpenAI compatible chat completions endpoint. So you can use the OpenAI SDK and just change the base URL and the API key. But compatibility is partial. Tool calling, structured outputs, streaming details and token accounting vary by provider and model, so test every feature you depend on before you switch production traffic.
```

Scene 6 — Simple example: Ollama on a laptop

```text
A simple example. On your laptop, install Ollama, pull a small model, and point the OpenAI SDK at localhost port one one four three four. Ask it to say hello in Urdu and Arabic. In a few minutes you have a private model running with no data leaving your machine. It's perfect for experiments, offline demos and learning, though a small local model won't match a frontier model on hard tasks.
```

Scene 7 — Business example: Karachi courier

```text
Now a realistic business example, with illustrative numbers. A Karachi courier company handles about two hundred thousand customer messages a month and classifies each into eight categories. That ran on a frontier closed model. The team evaluated an open weight mid size model via a hosted provider on five hundred labeled messages in English, Urdu and Roman Urdu. Accuracy was equal, cost per message was much lower, and responses were faster. They switched classification, kept the frontier model for complex complaint drafting, and routed everything through their adapter so they can change hosts if prices move.
```

Scene 8 — Fine-tuning, in context

```text
Let's talk about fine tuning, because it's one of the main reasons teams choose open weights. If you have thousands of high quality examples of a narrow task, like classifying your own ticket categories or writing in a very specific house style, fine tuning an open model can make a small, cheap model perform like a much larger one on that task. But start with prompting and a good eval first. Many teams discover that clear instructions and a few examples get them most of the way, without the cost of maintaining a custom model.
```

Scene 9 — Self-hosting reality

```text
What about self hosting? Serving a model with vLLM can be one command, and it gives you an OpenAI compatible API. But production means planning GPU capacity, since model size and context length drive memory, tuning batching and throughput, autoscaling, monitoring, security patching and model updates. Self hosting usually pays off at steady high volume, or when data must never leave your environment. Otherwise, a hosted API is often cheaper once you count engineering time.
```

Scene 10 — Honest evaluation

```text
Evaluate open models honestly, with the same eval you use for closed ones. Include your languages, safety relevant cases, like refusing harmful requests and not leaking the system prompt, and structured output reliability. Open models may need stronger prompting, guardrails or fine tuning to match closed models on your tasks, and sometimes they win outright on narrow ones. And add your own safety layer, because some open models are less restrictive by default.
```

Scene 11 — Common mistakes

```text
Common mistakes. Assuming open means no license obligations. Comparing on public benchmarks instead of your data. Underestimating the operations of self hosting. And relying on OpenAI compatible endpoints for features they only partially support. Test the exact features you need, on the exact host and model, before you move real traffic.
```

Scene 12 — Responsibility checklist

```text
Finally, keep a short responsibility checklist for every open model you ship. Record the exact model name and version, where it's hosted, and the license terms that apply. Note known limitations from the model card. Confirm your own safety layer is in front of it, with moderation and refusal tests. And put a reminder in the calendar to review it every quarter, because hosts change prices and models get updated. Auditors and future colleagues will thank you.
```

Scene 13 — Deeper: courier classification (illustrative)

```text
Let's deepen the Karachi courier example with illustrative numbers. Two hundred thousand messages a month, eight categories, English, Urdu and Roman Urdu. On five hundred labeled messages, the open weight mid size model matched the frontier model's accuracy within a point, responded faster through a specialist host, and cost a small fraction per message. The team kept an eye on two risks. First, host dependency: they configured a second host serving the same open model, so switching was a base URL change. Second, license: legal confirmed the model's license allowed their commercial use at their scale. Six months later, a new version of the model arrived, and they reran the same eval before upgrading.
```

Scene 14 — Watch me do it: OpenAI-compatible endpoints

```text
Watch me do it. Let's walk through the OpenAI compatible calls. The hosted client is just the OpenAI SDK with a base URL and an API key from environment variables, both taken from the host's documentation. I call chat completions create with the model id from an environment variable, because every host names models slightly differently. The system message says: classify the ticket as billing, delivery, product or other, one word. The user message is the customer's complaint. Max tokens is five and temperature zero, for short, stable labels. The answer comes back in choices zero message content: delivery. Then the local version: after ollama pull, I point the same SDK at localhost port one one four three four slash v1, with a dummy key, and ask for a greeting in Urdu and Arabic. Finally, for self hosting, one vllm serve command exposes the same kind of endpoint on port eight thousand. Three setups, one client library.
```

Scene 15 — Recap + try this now

```text
Quick recap. Open weight models publish weights under licenses you must read. Use them through hosted APIs, cloud catalogs or self hosting, usually via OpenAI compatible endpoints, and test the features you need. Evaluate on your own data, add safety layers, and self host only when volume or data control justify it. Try this now: pick one high volume, simpler task, evaluate an open weight model through a hosted API against your current model on two hundred labeled examples, and review its license.
```

</details>

### 17. Security, data privacy and the production checklist

Lesson `security-privacy-production-checklist` · module “Production readiness and capstone” · target 9 min · 15 scenes · 1306 words · planned 9:29 (words ≈ 9:28) · status: **to produce**

| Term | Say |
|---|---|
| CNIC | C N I C |
| GDPR | G D P R |
| ZDR | Z D R |
| DPA | D P A |
| NER | N E R |

| # | Time | Sec | Narration | On screen | Visual |
|---|---|---|---|---|---|
| 1 | 0:00 | 31 | Your AI feature works, the demo impressed everyone, and launch is next week. Then the security team asks: what data leaves our systems, where is it stored, for how long, and who can see it? If you can't answer with evidence, launch slips. In this lesson you'll learn the data privacy questions to settle with every provider, the technical controls that protect you, and a production checklist that gets you through review. | Security, privacy, production<br>• What happens to your data<br>• Legal context<br>• Technical controls<br>• The production checklist | Layout: a launch calendar with 'next week' circled; a security reviewer icon appears with four question cards: What data? Where? How long? Who sees it? 0s: title 'Security, privacy, production' fades in top-left. 2s: main graphic begins animating. 5s, 9s, 13s, 17s: on-screen bullets reveal one by one, each highlighted as narrated. 27s: hold final frame. |
| 2 | 0:31 | 31 | Why does this matter? When you call an AI API, your users' content leaves your systems and goes to a third party processor. That's normal, but it's a responsibility. Think of sending documents to an external translation agency. You'd want a contract, you'd want to know how long they keep copies, where their office is, and you'd black out anything they don't need to see. AI providers deserve exactly the same diligence. | Why it matters<br>• Data leaves your systems<br>• Providers are processors<br>• The translation-agency analogy | Layout: a document envelope travels from your company building to an external agency building; stamps appear on it: 'Contract', 'Retention', 'Location', 'Redacted'. 0s: title 'Why it matters' fades in top-left. 2s: main graphic begins animating. 5s, 11s, 17s: on-screen bullets reveal one by one, each highlighted as narrated. 27s: hold final frame. |
| 3 | 1:02 | 44 | Ask four questions of every provider, product and plan. Training: business APIs from Anthropic, OpenAI and Google say they don't train on your API data by default, but consumer apps and free tiers can differ, so read the terms for the exact product. Retention: providers typically keep inputs and outputs for a limited period for abuse monitoring, and offer zero data retention arrangements for eligible customers. Some features store data by design, like files, batches and caches, and OpenAI's Responses API stores responses unless you set store to false. Location: where processing happens. And agreements: a data processing agreement, subprocessors and certifications. | Four questions per provider<br>• Training use<br>• Retention (+ ZDR options, stored features)<br>• Processing location<br>• DPA, subprocessors, certifications | Layout: a four-quadrant checklist card; each quadrant fills with a short note; the Retention quadrant shows a small 'store=False' toggle. 0s: title 'Four questions per provider' fades in top-left. 2s: main graphic begins animating. 5s, 12s, 19s, 26s: on-screen bullets reveal one by one, each highlighted as narrated. 40s: hold final frame. |
| 4 | 1:46 | 38 | Now the legal context, and this isn't legal advice. GDPR and UK GDPR require a lawful basis, data minimization, transparency, processor contracts and safe international transfers. Saudi Arabia's personal data protection law, the UAE's federal law and free zone regimes like DIFC and ADGM add their own requirements, plus sector rules in banking and health. Pakistan has been developing data protection legislation, so check its current status. And the EU AI Act brings phased obligations, like transparency for some AI interactions. Involve your privacy and legal team early. | Legal context (not legal advice)<br>• GDPR / UK GDPR<br>• KSA PDPL; UAE PDPL; DIFC, ADGM<br>• Sector regulators<br>• Pakistan: check status • EU AI Act phases | Layout: a map highlighting the UK, EU, UAE, KSA and Pakistan with regulation badges; a 'talk to legal early' speech bubble appears. 0s: title 'Legal context (not legal advice)' fades in top-left. 2s: main graphic begins animating. 5s, 11s, 17s, 23s: on-screen bullets reveal one by one, each highlighted as narrated. 34s: hold final frame. |
| 5 | 2:24 | 37 | Next, technical controls. Minimize: send only what the task needs. Redact personal data before sending where you can. Keep keys on the backend. Defend against prompt injection by treating user content and documents as untrusted, and never letting model output trigger sensitive actions without validation. Handle outputs safely: never render them as raw HTML. Prevent abuse with authentication, rate limits and moderation, and pass a pseudonymous user id where supported, like OpenAI's safety identifier. Log carefully. And keep humans in the loop for consequential decisions. | Technical controls<br>• Minimize + redact<br>• Backend-only keys<br>• Injection defenses<br>• Safe output handling<br>• Abuse prevention, careful logging, human oversight | Layout: a request pipeline with eight small gates, each labeled with a control, lighting up in sequence as a data packet passes through. 0s: title 'Technical controls' fades in top-left. 2s: main graphic begins animating. 5s, 9s, 13s, 17s, 21s: on-screen bullets reveal one by one, each highlighted as narrated. 33s: hold final frame. |
| 6 | 3:01 | 32 | A simple example of minimization. A gym wants AI to write personalized renewal reminders. The first draft of the code sends each member's full record: name, date of birth, address, phone, payment history and medical notes. The reminder only needs a first name, the membership type and the renewal date. Cutting the payload to those three fields removes most of the risk, cuts tokens, and makes the privacy review a two minute conversation. | Simple example: gym reminders<br>• Before: full member record<br>• Needed: first name, plan, renewal date<br>• Less risk, fewer tokens, easy review | Layout: a member record card with eight fields; five fields fade and slide away, leaving three; a token counter drops and a privacy checkmark appears. 0s: title 'Simple example: gym reminders' fades in top-left. 2s: main graphic begins animating. 5s, 11s, 17s: on-screen bullets reveal one by one, each highlighted as narrated. 28s: hold final frame. |
| 7 | 3:33 | 32 | The lesson includes a simple redaction layer. It replaces emails, phone numbers, card numbers, Pakistani CNIC numbers and Emirates ID numbers with placeholder tokens before sending text to a model, and can restore them afterwards if needed. But be honest about its limits. Regular expressions miss names, addresses and free form identifiers, and can over match. Add a named entity detection step, test on real samples, and treat redaction as one layer among several. | Hands-on: redaction layer<br>• Emails, phones, cards, CNIC, Emirates ID<br>• Placeholder tokens + restore<br>• Add NER for names/addresses<br>• Test on real samples | Layout: a sentence with personal details; highlighted spans morph into tokens like [PHONE_1] and [UAE_EID_3]; a 'names still present' warning points to a person's name, and an NER badge fixes it. 0s: title 'Hands-on: redaction layer' fades in top-left. 2s: main graphic begins animating. 5s, 9s, 13s, 17s: on-screen bullets reveal one by one, each highlighted as narrated. 28s: hold final frame. |
| 8 | 4:05 | 43 | Now a realistic business example. A Lahore fintech built a support copilot that drafts replies for agents. They redact CNIC, card and phone numbers before every call. They chose a provider with a signed data processing agreement and zero data retention on the relevant endpoint. Drafts are never sent automatically, and agents see a label saying the draft was generated with AI. Logs keep metadata for ninety days and redacted text for fourteen. And they run injection tests every quarter using real attack patterns from their inbox. Their security review passed in one cycle, because every item had evidence. | Business example: Lahore fintech copilot<br>• Redaction before every call<br>• DPA + ZDR endpoint<br>• Human sends; AI label shown<br>• 90-day metadata, 14-day redacted text<br>• Quarterly injection tests | Layout: an agent console with a draft reply labeled 'Draft generated with AI'; side panel lists controls with green ticks and evidence links. 0s: title 'Business example: Lahore fintech copilot' fades in top-left. 2s: main graphic begins animating. 5s, 11s, 17s, 23s, 29s: on-screen bullets reveal one by one, each highlighted as narrated. 39s: hold final frame. |
| 9 | 4:48 | 33 | Finally, the production checklist. Quality: an eval per task with agreed thresholds and per language results. Reliability: error handling, backoff, circuit breaker, tested fallback. Cost: ledger, budgets, cost per outcome. Security: backend keys, secret manager, scanning, injection tests, output sanitization. Privacy: terms reviewed, retention configured, redaction, updated privacy notice. Observability: traces, dashboards and alerts. Operations: versioned config, tested rollback, an owner and a runbook. And compliance: disclosure where required and human oversight for consequential uses. | Production checklist<br>• Quality • Reliability • Cost<br>• Security • Privacy<br>• Observability • Operations • Compliance | Layout: an eight-row checklist table with an 'Evidence' column; each row ticks as a document icon attaches. 0s: title 'Production checklist' fades in top-left. 2s: main graphic begins animating. 5s, 11s, 17s: on-screen bullets reveal one by one, each highlighted as narrated. 29s: hold final frame. |
| 10 | 5:21 | 25 | Common mistakes. Sending full records when a few fields would do. Assuming not used for training means not stored. Logging raw prompts with personal data indefinitely. And rendering model output as HTML, which can open you to script injection. Each of these is a common finding in security reviews, and each is easy to fix before launch. | Common mistakes<br>• Oversharing data<br>• 'No training' ≠ 'not stored'<br>• Indefinite raw logs<br>• Raw HTML output | Layout: four red cards flip to green fixes. 0s: title 'Common mistakes' fades in top-left. 2s: main graphic begins animating. 5s, 8s, 11s, 14s: on-screen bullets reveal one by one, each highlighted as narrated. 21s: hold final frame. |
| 11 | 5:46 | 33 | What should you do when a customer asks what happens to their data in your AI features? Have a short, accurate answer ready, agreed with legal: which providers process it, for what purpose, how long it's kept, where it's processed, and how they can request deletion. Put it in your privacy notice and your help center. Being able to answer clearly and quickly is one of the strongest trust signals you can give business customers. | Answering data questions<br>• Providers, purpose, retention, location<br>• How to request deletion<br>• In the privacy notice + help center | Layout: a customer chat asking 'what happens to my data?'; a help-center card slides in with five short labeled lines. 0s: title 'Answering data questions' fades in top-left. 2s: main graphic begins animating. 5s, 11s, 17s: on-screen bullets reveal one by one, each highlighted as narrated. 29s: hold final frame. |
| 12 | 6:19 | 39 | A quick word on disclosure and marketing rules, since many of you will generate customer facing content. Where AI generated content could mislead people, be transparent about it, and follow your market's advertising rules: sponsored posts need clear labels like ad, and claims about health, performance or prices need evidence, whether a human or a model wrote them. Regulators in the UK, the EU, the Gulf and the US all expect marketers to stand behind their claims. Put these rules into your prompts, your validators and your review steps. | Disclosure and advertising rules<br>• Be transparent where AI could mislead<br>• Label sponsored content (#ad)<br>• Evidence for health/performance/price claims<br>• Encode rules in prompts, validators, reviews | Layout: a social post mock-up gains an '#ad' label; a claim 'clinically proven' is highlighted and routed to a 'needs evidence' review box; a small globe shows UK, EU, Gulf and US markers. 0s: title 'Disclosure and advertising rules' fades in top-left. 2s: main graphic begins animating. 5s, 11s, 17s, 23s: on-screen bullets reveal one by one, each highlighted as narrated. 35s: hold final frame. |
| 13 | 6:58 | 50 | Let's deepen the Lahore fintech copilot example. Before launch, the security reviewer asked for evidence on every checklist row. The team provided a data flow diagram showing that CNIC, card and phone numbers were redacted before any provider call, the signed DPA and the zero retention configuration, screenshots of the AI label agents see, log retention settings, and results from their quarterly injection tests using real attack patterns. One gap surfaced: names in free text were not redacted. They added a named entity step and a test set of fifty real, anonymized messages. The review passed in one cycle, and the reviewer reused their checklist as the bank's template for the next AI project. | Deeper: the fintech review<br>• Evidence for every row<br>• Gap found: names in free text<br>• Added NER + 50-message test set<br>• Checklist became the bank's template | Layout: a checklist with evidence thumbnails. 0s: rows tick with attached documents. 12s: one row 'names redacted' flashes amber. 22s: an NER step is added to the data-flow diagram and the row turns green. 34s: a 'template' stamp copies the checklist to a new project folder. 46s: hold. |
| 14 | 7:48 | 70 | Watch me do it. Let's run the redaction layer. Five patterns: email addresses, phone numbers with international prefixes like plus nine two, plus nine seven one or plus nine six six, card numbers, Pakistani CNIC numbers in the five dash seven dash one format, and Emirates ID numbers starting with seven eight four. Redact loops over each pattern, and for every unique match assigns a numbered placeholder such as PHONE one, stores the original in a mapping, and replaces it in the text. Restore swaps the placeholders back, for example after the model drafts a reply that must include the customer's email. I run it on, call Aisha on plus nine seven one fifty, one two three, four five six seven, or her email, about Emirates ID seven eight four and so on. The output replaces the phone, email and ID with placeholders. But Aisha's name is still there, which is exactly why the lesson adds a named entity step. | Watch me do it: redact() and restore()<br>• EMAIL, PHONE, CARD, PK_CNIC, UAE_EID patterns<br>• Numbered placeholders + mapping<br>• restore() after generation<br>• Names remain → add NER | Layout: code editor top, a sentence panel bottom. 0s: PATTERNS highlight one by one. 14s: redact() loop highlights. 24s: the sample sentence appears; phone, email and ID spans morph into [PHONE_2], [EMAIL_1], [UAE_EID_3]. 42s: 'Aisha' remains highlighted amber with an 'add NER' tag. 54s: restore() highlights. 64s: hold. |
| 15 | 8:58 | 31 | Quick recap. Ask every provider about training, retention, location and agreements. Know your legal context and involve legal early. Minimize, redact, protect keys, defend against injection and handle outputs safely. And ship with a checklist backed by evidence. Try this now: complete the production checklist for one AI feature, with a link to evidence for every row, add a redaction layer, and review your provider's current data terms and retention settings. | Recap + try this now<br>• Four provider questions<br>• Minimize, redact, defend<br>• Checklist with evidence for one feature | Layout: recap card; the checklist table appears with evidence links filling in one by one. 0s: title 'Recap + try this now' fades in top-left. 2s: main graphic begins animating. 5s, 11s, 17s: on-screen bullets reveal one by one, each highlighted as narrated. 27s: hold final frame. |

<details><summary>Narration per scene (paste into ElevenLabs)</summary>

Scene 1 — Security, privacy, production

```text
Your AI feature works, the demo impressed everyone, and launch is next week. Then the security team asks: what data leaves our systems, where is it stored, for how long, and who can see it? If you can't answer with evidence, launch slips. In this lesson you'll learn the data privacy questions to settle with every provider, the technical controls that protect you, and a production checklist that gets you through review.
```

Scene 2 — Why it matters

```text
Why does this matter? When you call an AI API, your users' content leaves your systems and goes to a third party processor. That's normal, but it's a responsibility. Think of sending documents to an external translation agency. You'd want a contract, you'd want to know how long they keep copies, where their office is, and you'd black out anything they don't need to see. AI providers deserve exactly the same diligence.
```

Scene 3 — Four questions per provider

```text
Ask four questions of every provider, product and plan. Training: business APIs from Anthropic, OpenAI and Google say they don't train on your API data by default, but consumer apps and free tiers can differ, so read the terms for the exact product. Retention: providers typically keep inputs and outputs for a limited period for abuse monitoring, and offer zero data retention arrangements for eligible customers. Some features store data by design, like files, batches and caches, and OpenAI's Responses API stores responses unless you set store to false. Location: where processing happens. And agreements: a data processing agreement, subprocessors and certifications.
```

Scene 4 — Legal context (not legal advice)

```text
Now the legal context, and this isn't legal advice. GDPR and UK GDPR require a lawful basis, data minimization, transparency, processor contracts and safe international transfers. Saudi Arabia's personal data protection law, the UAE's federal law and free zone regimes like DIFC and ADGM add their own requirements, plus sector rules in banking and health. Pakistan has been developing data protection legislation, so check its current status. And the EU AI Act brings phased obligations, like transparency for some AI interactions. Involve your privacy and legal team early.
```

Scene 5 — Technical controls

```text
Next, technical controls. Minimize: send only what the task needs. Redact personal data before sending where you can. Keep keys on the backend. Defend against prompt injection by treating user content and documents as untrusted, and never letting model output trigger sensitive actions without validation. Handle outputs safely: never render them as raw HTML. Prevent abuse with authentication, rate limits and moderation, and pass a pseudonymous user id where supported, like OpenAI's safety identifier. Log carefully. And keep humans in the loop for consequential decisions.
```

Scene 6 — Simple example: gym reminders

```text
A simple example of minimization. A gym wants AI to write personalized renewal reminders. The first draft of the code sends each member's full record: name, date of birth, address, phone, payment history and medical notes. The reminder only needs a first name, the membership type and the renewal date. Cutting the payload to those three fields removes most of the risk, cuts tokens, and makes the privacy review a two minute conversation.
```

Scene 7 — Hands-on: redaction layer

```text
The lesson includes a simple redaction layer. It replaces emails, phone numbers, card numbers, Pakistani CNIC numbers and Emirates ID numbers with placeholder tokens before sending text to a model, and can restore them afterwards if needed. But be honest about its limits. Regular expressions miss names, addresses and free form identifiers, and can over match. Add a named entity detection step, test on real samples, and treat redaction as one layer among several.
```

Scene 8 — Business example: Lahore fintech copilot

```text
Now a realistic business example. A Lahore fintech built a support copilot that drafts replies for agents. They redact CNIC, card and phone numbers before every call. They chose a provider with a signed data processing agreement and zero data retention on the relevant endpoint. Drafts are never sent automatically, and agents see a label saying the draft was generated with AI. Logs keep metadata for ninety days and redacted text for fourteen. And they run injection tests every quarter using real attack patterns from their inbox. Their security review passed in one cycle, because every item had evidence.
```

Scene 9 — Production checklist

```text
Finally, the production checklist. Quality: an eval per task with agreed thresholds and per language results. Reliability: error handling, backoff, circuit breaker, tested fallback. Cost: ledger, budgets, cost per outcome. Security: backend keys, secret manager, scanning, injection tests, output sanitization. Privacy: terms reviewed, retention configured, redaction, updated privacy notice. Observability: traces, dashboards and alerts. Operations: versioned config, tested rollback, an owner and a runbook. And compliance: disclosure where required and human oversight for consequential uses.
```

Scene 10 — Common mistakes

```text
Common mistakes. Sending full records when a few fields would do. Assuming not used for training means not stored. Logging raw prompts with personal data indefinitely. And rendering model output as HTML, which can open you to script injection. Each of these is a common finding in security reviews, and each is easy to fix before launch.
```

Scene 11 — Answering data questions

```text
What should you do when a customer asks what happens to their data in your AI features? Have a short, accurate answer ready, agreed with legal: which providers process it, for what purpose, how long it's kept, where it's processed, and how they can request deletion. Put it in your privacy notice and your help center. Being able to answer clearly and quickly is one of the strongest trust signals you can give business customers.
```

Scene 12 — Disclosure and advertising rules

```text
A quick word on disclosure and marketing rules, since many of you will generate customer facing content. Where AI generated content could mislead people, be transparent about it, and follow your market's advertising rules: sponsored posts need clear labels like ad, and claims about health, performance or prices need evidence, whether a human or a model wrote them. Regulators in the UK, the EU, the Gulf and the US all expect marketers to stand behind their claims. Put these rules into your prompts, your validators and your review steps.
```

Scene 13 — Deeper: the fintech review

```text
Let's deepen the Lahore fintech copilot example. Before launch, the security reviewer asked for evidence on every checklist row. The team provided a data flow diagram showing that CNIC, card and phone numbers were redacted before any provider call, the signed DPA and the zero retention configuration, screenshots of the AI label agents see, log retention settings, and results from their quarterly injection tests using real attack patterns. One gap surfaced: names in free text were not redacted. They added a named entity step and a test set of fifty real, anonymized messages. The review passed in one cycle, and the reviewer reused their checklist as the bank's template for the next AI project.
```

Scene 14 — Watch me do it: redact() and restore()

```text
Watch me do it. Let's run the redaction layer. Five patterns: email addresses, phone numbers with international prefixes like plus nine two, plus nine seven one or plus nine six six, card numbers, Pakistani CNIC numbers in the five dash seven dash one format, and Emirates ID numbers starting with seven eight four. Redact loops over each pattern, and for every unique match assigns a numbered placeholder such as PHONE one, stores the original in a mapping, and replaces it in the text. Restore swaps the placeholders back, for example after the model drafts a reply that must include the customer's email. I run it on, call Aisha on plus nine seven one fifty, one two three, four five six seven, or her email, about Emirates ID seven eight four and so on. The output replaces the phone, email and ID with placeholders. But Aisha's name is still there, which is exactly why the lesson adds a named entity step.
```

Scene 15 — Recap + try this now

```text
Quick recap. Ask every provider about training, retention, location and agreements. Know your legal context and involve legal early. Minimize, redact, protect keys, defend against injection and handle outputs safely. And ship with a checklist backed by evidence. Try this now: complete the production checklist for one AI feature, with a link to evidence for every row, add a redaction layer, and review your provider's current data terms and retention settings.
```

</details>

### 18. Capstone part 1: build a provider-agnostic content-generation microservice

Lesson `capstone-build-content-microservice` · module “Production readiness and capstone” · target 9 min · 16 scenes · 1306 words · planned 9:29 (words ≈ 9:28) · status: **to produce**

| Term | Say |
|---|---|
| FastAPI | fast A P I |
| Pydantic | pie-dan-tick |
| YAML | yam-ul |

| # | Time | Sec | Narration | On screen | Visual |
|---|---|---|---|---|---|
| 1 | 0:00 | 25 | Everything in this course comes together now. You'll build Crescent Content API, a provider agnostic microservice that generates marketing content in English, Arabic and Urdu, in each client's brand voice, with structured outputs, fallbacks, streaming, budgets and privacy controls. By the end of this part, you'll have a working service you can call with a single request. | Capstone part 1<br>• Crescent Content API<br>• Multi-provider, structured, budgeted<br>• EN, AR, UR content | Layout: a service box labeled 'Crescent Content API' sits between a client portal on the left and three provider clouds on the right; language chips EN, AR, UR float above the portal. 0s: title 'Capstone part 1' fades in top-left. 2s: main graphic begins animating. 5s, 9s, 13s: on-screen bullets reveal one by one, each highlighted as narrated. 21s: hold final frame. |
| 2 | 0:25 | 30 | Why build it as a separate service? Because content generation will be used by many tools: a client portal, an internal dashboard, a scheduling app. Centralizing it means one place for keys, routing, budgets, redaction and logs. Think of a hotel's central kitchen serving the restaurant, room service and the banquet hall. Each outlet orders what it needs, but hygiene, stock and costs are managed in one place. | Why a microservice?<br>• Many consumers, one control point<br>• Keys, routing, budgets, redaction, logs<br>• The hotel central kitchen | Layout: a central kitchen hub with three outlets (Portal, Dashboard, Scheduler) placing orders through service windows; a control panel in the kitchen shows Keys, Budget, Logs. 0s: title 'Why a microservice?' fades in top-left. 2s: main graphic begins animating. 5s, 10s, 15s: on-screen bullets reveal one by one, each highlighted as narrated. 26s: hold final frame. |
| 3 | 0:55 | 32 | Here's the brief. The service must work with at least two providers per task, chosen by configuration. It returns structured content: a title, a body, hashtags, and a needs review flag with reasons. It streams long drafts. It tracks cost per tenant and task, and enforces daily budgets. And it redacts personal data, keeps keys on the server and logs safely. Every one of those requirements maps to a lesson you've already completed. | The requirements<br>• 2+ providers per task (config)<br>• Structured, validated output<br>• Streaming for long drafts<br>• Cost tracking + budgets<br>• Redaction, server-side keys, safe logs | Layout: a requirements checklist where each item links with a thin line to a small lesson badge (Formats, Streaming, Structured, Budgets, Security). 0s: title 'The requirements' fades in top-left. 2s: main graphic begins animating. 5s, 8s, 11s, 14s, 17s: on-screen bullets reveal one by one, each highlighted as narrated. 28s: hold final frame. |
| 4 | 1:27 | 33 | The project has a handful of small files. The app file holds the FastAPI endpoints. Providers holds the adapters for Claude, OpenAI and Gemini. Router handles routes, fallback and a simple circuit breaker. Schemas defines the request and response models. Budget keeps the cost ledger and per tenant limits. Redact removes personal data. And two configuration files: routes, mapping each task to a primary and fallback model, and prices, loaded from pricing pages with an effective date. | Project layout<br>• app.py • providers.py • router.py<br>• schemas.py • budget.py • redact.py<br>• routes.yaml • prices.json | Layout: a file tree expands file by file, each file icon briefly showing a one-line caption. 0s: title 'Project layout' fades in top-left. 2s: main graphic begins animating. 5s, 11s, 17s: on-screen bullets reveal one by one, each highlighted as narrated. 29s: hold final frame. |
| 5 | 2:00 | 32 | Start with the schemas. The request carries the tenant, the task, the language, the brand voice and the brief, each with length limits. The content output has a title, body, hashtags, and the needs review flag with reasons, which is the escape hatch that lets the model say, a claim here needs checking. For providers' strict modes, there's also a JSON schema where every property is required and no extra properties are allowed. | Schemas<br>• GenerateIn: tenant, task, language, brand voice, brief<br>• ContentOut: title, body, hashtags, needs_review, reasons<br>• Strict JSON Schema for providers | Layout: two Pydantic class cards animate field by field; a third card 'CONTENT_SCHEMA' shows 'required: all' and 'additionalProperties: false' highlighted. 0s: title 'Schemas' fades in top-left. 2s: main graphic begins animating. 5s, 11s, 17s: on-screen bullets reveal one by one, each highlighted as narrated. 28s: hold final frame. |
| 6 | 2:32 | 35 | A simple example of one request. The client posts: tenant demo, task product description, English, brand voice warm, modern, proudly handmade in Pakistan, and the brief blue pottery mug, three fifty milliliters, dishwasher safe. The service checks the budget, redacts the brief, builds the system prompt with the brand voice and task rules, asks the router for the product description route, validates the JSON, records the cost, and returns the content with the provider, model, cost and a request id. | Simple example: one request<br>• Budget check → redact<br>• System = brand voice + rules<br>• Router → validated JSON<br>• Response with provider, cost, request_id | Layout: a curl command on the left; a vertical pipeline in the middle with six labeled steps lighting up; a JSON response card on the right with provider, model and cost fields highlighted. 0s: title 'Simple example: one request' fades in top-left. 2s: main graphic begins animating. 5s, 10s, 15s, 20s: on-screen bullets reveal one by one, each highlighted as narrated. 31s: hold final frame. |
| 7 | 3:07 | 34 | Let's look at the providers file for a moment, because it's where reliability starts. Each adapter wraps its SDK's retryable errors, like rate limits after the SDK's own retries, timeouts, connection errors and server errors, in one common retryable provider error. Non retryable errors, like a bad request, pass through unchanged. It also returns normalized usage, including cached tokens. That small contract is what lets the router make good decisions without knowing anything about Claude, OpenAI or Gemini specifically. | providers.py contract<br>• Wrap retryable SDK errors → RetryableProviderError<br>• Let 400s propagate<br>• Normalized usage incl. cached tokens | Layout: three SDK error icons (different shapes) enter a funnel labeled 'adapter' and emerge as one uniform 'RetryableProviderError' shape; a separate '400' shape bypasses the funnel. 0s: title 'providers.py contract' fades in top-left. 2s: main graphic begins animating. 5s, 12s, 19s: on-screen bullets reveal one by one, each highlighted as narrated. 30s: hold final frame. |
| 8 | 3:41 | 33 | The router reads routes from configuration. For product descriptions, it tries Claude first and Gemini as the fallback. For social posts, OpenAI first and Claude as the fallback. Before calling a provider, it checks a simple circuit breaker: if a provider has failed five times in thirty seconds, it's skipped for a minute. Only retryable errors, like timeouts, rate limits after retries and server errors, trigger a fallback. Bad requests propagate, because they'd fail everywhere. | The router<br>• routes.yaml per task<br>• Circuit breaker: 5 fails / 30 s → skip 60 s<br>• Fallback only on retryable errors<br>• 400s propagate | Layout: a routes.yaml panel beside a flow: Primary → (fail) breaker counter increments → Fallback; a separate red '400' arrow goes straight back to the caller. 0s: title 'The router' fades in top-left. 2s: main graphic begins animating. 5s, 10s, 15s, 20s: on-screen bullets reveal one by one, each highlighted as narrated. 29s: hold final frame. |
| 9 | 4:14 | 31 | The budget module checks each tenant's spend for the day before calling any provider, and returns a clear four twenty nine style error when the budget is reached. After each successful call, it records input, cached and output tokens with the cost from the prices file, and emits a ledger line. In production, keep that spend in a shared store like Redis or your database, so all replicas see the same numbers. | Budgets and ledger<br>• Check before spending<br>• Clear error at the limit<br>• Record tokens + cost per call<br>• Shared store in production | Layout: a tenant budget gauge filling up; at 100% a 'Daily AI budget reached' message appears before any provider call; a ledger line appends after each success. 0s: title 'Budgets and ledger' fades in top-left. 2s: main graphic begins animating. 5s, 9s, 13s, 17s: on-screen bullets reveal one by one, each highlighted as narrated. 27s: hold final frame. |
| 10 | 4:45 | 38 | Now a realistic business scenario, with illustrative numbers. The agency's portal serves twenty five clients. On a busy morning, a client in Dubai requests Arabic social posts for a Ramadan campaign, a Lahore client requests Urdu product descriptions, and a London client requests English email drafts. Each request goes to its task's primary model. When one provider has a brief incident, the breaker opens and social posts shift to the fallback. Every response is validated, costs are attributed per client, and nothing personal leaves the service unredacted. | Business scenario: a busy morning<br>• Dubai: Arabic social posts<br>• Lahore: Urdu product copy<br>• London: English emails<br>• Breaker → fallback; costs per client | Layout: three client pins on a map send requests into the service; a provider icon flickers red and traffic for 'social_post' reroutes; a per-client cost table updates. 0s: title 'Business scenario: a busy morning' fades in top-left. 2s: main graphic begins animating. 5s, 11s, 17s, 23s: on-screen bullets reveal one by one, each highlighted as narrated. 34s: hold final frame. |
| 11 | 5:23 | 34 | Look at the key design choices in the app file. The brand voice sits in the system prompt, because it's stable per tenant, which lets prompt caching help. The brief goes in the user turn. Personal data is redacted before any provider sees it. Incomplete generations and invalid JSON become clear errors instead of broken content. And when every provider fails, the client gets a five oh three with a friendly message, while the logs keep the details. | Design choices<br>• Stable brand voice in system<br>• Brief in user turn<br>• Redact before sending<br>• 502 on invalid output, 503 if all fail | Layout: code panel scrolls through app.py with each design choice highlighted; small HTTP status badges (429, 502, 503) appear next to their handling lines. 0s: title 'Design choices' fades in top-left. 2s: main graphic begins animating. 5s, 10s, 15s, 20s: on-screen bullets reveal one by one, each highlighted as narrated. 30s: hold final frame. |
| 12 | 5:57 | 23 | Common mistakes to avoid while building. Putting provider keys in the client portal. Skipping validation because structured outputs usually work. Keeping budget state only in memory on one replica. And hard coding model ids instead of reading them from routes. Each of these shows up in production, usually at the worst time. | Common mistakes<br>• Keys in the portal<br>• Skipping validation<br>• In-memory budget state<br>• Hard-coded model IDs | Layout: four red cards flip to green fixes. 0s: title 'Common mistakes' fades in top-left. 2s: main graphic begins animating. 5s, 7s, 9s, 11s: on-screen bullets reveal one by one, each highlighted as narrated. 19s: hold final frame. |
| 13 | 6:20 | 33 | Why does the service return a needs review flag instead of just refusing risky content? Because in marketing, many briefs are fine with small edits, and a human reviewer can fix them quickly. The flag, with reasons, turns the model into a helpful first drafter that also highlights risk, like an unsupported health claim or a missing sponsorship label. Your portal can show flagged drafts with a yellow banner, so reviewers know exactly where to look. | Why a needs_review flag?<br>• Many risky briefs just need edits<br>• Reasons point reviewers to the issue<br>• Portal shows a yellow banner | Layout: a draft card with a yellow banner 'needs review: health claim unsupported'; the reviewer edits one sentence and approves. 0s: title 'Why a needs_review flag?' fades in top-left. 2s: main graphic begins animating. 5s, 11s, 17s: on-screen bullets reveal one by one, each highlighted as narrated. 29s: hold final frame. |
| 14 | 6:53 | 51 | Let's deepen the Crescent Content API with a realistic morning, illustrative numbers. Between nine and eleven, the portal handles a few hundred requests from fourteen clients. A Lahore home goods brand generates forty Urdu product descriptions; the router uses Claude, and six are flagged for review because the briefs mentioned health benefits. A Dubai café chain generates Arabic social posts for a Ramadan campaign; the sponsored ones include the ad tag automatically because of the task rules. A London fitness studio's budget hits its daily limit at ten thirty, and its marketer sees a clear message instead of an error page. Every request has a request id, a provider, a model and a cost in the ledger. | Deeper: a morning of traffic (illustrative)<br>• Lahore: 40 Urdu descriptions, 6 flagged<br>• Dubai: Arabic Ramadan posts with #ad<br>• London: daily budget reached, clear message<br>• Every request in the ledger | Layout: a live traffic board. 0s: three client lanes (Lahore, Dubai, London) fill with request dots. 9s: six Lahore items get yellow 'needs review' flags. 18s: Dubai posts show an '#ad' tag. 28s: the London lane hits a budget gauge at 100% with a friendly message. 37s: a ledger scrolls with request IDs and costs. 48s: hold. |
| 15 | 7:44 | 70 | Watch me do it. Let's trace one request through app dot py. The auth dependency checks the API key header and maps it to a caller. Budget check raises if the tenant's spend today has reached its limit, which becomes a four twenty nine. Redact removes personal data from the brief. The system prompt combines the brand voice, the task rules and the language name. The request object carries the system prompt, one user message, a twelve hundred token limit, the strict content schema and metadata. Router generate reads the routes for this task, skips any provider whose breaker is open, and returns the first success. If every provider fails, the endpoint returns a five oh three. If the stop value isn't end, it returns a five oh two. Content out model validate json checks the JSON against the schema. Budget record writes the ledger line and returns the cost. The response includes the content, provider, model, cost and request id. | Watch me do it: one request through app.py<br>• auth → budget.check → redact<br>• System = brand voice + rules + language<br>• router.generate (breaker, fallback)<br>• validate → budget.record → response | Layout: app.py with a request token moving down the code. 0s: auth highlights. 7s: budget.check highlights with a 429 badge. 14s: redact highlights. 22s: system prompt assembly highlights. 31s: GenRequest highlights. 38s: router.generate highlights with 503 badge. 47s: stop check (502) and ContentOut validation highlight. 56s: budget.record and the response highlight. 67s: hold. |
| 16 | 8:54 | 35 | Quick recap. You've built a multi provider content service with configuration based routing, fallbacks and a circuit breaker, strict structured outputs validated with Pydantic, budgets and a cost ledger, redaction and server side keys. Try this now: build it with at least two providers, set up the routes file, and generate one piece of content for each task in two languages, recording the provider and cost for each. In part two, you'll test, evaluate, observe, deploy and add a TypeScript client. | Recap + try this now<br>• Routing, fallbacks, breaker<br>• Validated structured output<br>• Budgets + redaction<br>• Generate 3 tasks × 2 languages | Layout: recap card; a results grid (tasks × languages) fills with provider and cost chips; an arrow points to 'Part 2'. 0s: title 'Recap + try this now' fades in top-left. 2s: main graphic begins animating. 5s, 10s, 15s, 20s: on-screen bullets reveal one by one, each highlighted as narrated. 31s: hold final frame. |

<details><summary>Narration per scene (paste into ElevenLabs)</summary>

Scene 1 — Capstone part 1

```text
Everything in this course comes together now. You'll build Crescent Content API, a provider agnostic microservice that generates marketing content in English, Arabic and Urdu, in each client's brand voice, with structured outputs, fallbacks, streaming, budgets and privacy controls. By the end of this part, you'll have a working service you can call with a single request.
```

Scene 2 — Why a microservice?

```text
Why build it as a separate service? Because content generation will be used by many tools: a client portal, an internal dashboard, a scheduling app. Centralizing it means one place for keys, routing, budgets, redaction and logs. Think of a hotel's central kitchen serving the restaurant, room service and the banquet hall. Each outlet orders what it needs, but hygiene, stock and costs are managed in one place.
```

Scene 3 — The requirements

```text
Here's the brief. The service must work with at least two providers per task, chosen by configuration. It returns structured content: a title, a body, hashtags, and a needs review flag with reasons. It streams long drafts. It tracks cost per tenant and task, and enforces daily budgets. And it redacts personal data, keeps keys on the server and logs safely. Every one of those requirements maps to a lesson you've already completed.
```

Scene 4 — Project layout

```text
The project has a handful of small files. The app file holds the FastAPI endpoints. Providers holds the adapters for Claude, OpenAI and Gemini. Router handles routes, fallback and a simple circuit breaker. Schemas defines the request and response models. Budget keeps the cost ledger and per tenant limits. Redact removes personal data. And two configuration files: routes, mapping each task to a primary and fallback model, and prices, loaded from pricing pages with an effective date.
```

Scene 5 — Schemas

```text
Start with the schemas. The request carries the tenant, the task, the language, the brand voice and the brief, each with length limits. The content output has a title, body, hashtags, and the needs review flag with reasons, which is the escape hatch that lets the model say, a claim here needs checking. For providers' strict modes, there's also a JSON schema where every property is required and no extra properties are allowed.
```

Scene 6 — Simple example: one request

```text
A simple example of one request. The client posts: tenant demo, task product description, English, brand voice warm, modern, proudly handmade in Pakistan, and the brief blue pottery mug, three fifty milliliters, dishwasher safe. The service checks the budget, redacts the brief, builds the system prompt with the brand voice and task rules, asks the router for the product description route, validates the JSON, records the cost, and returns the content with the provider, model, cost and a request id.
```

Scene 7 — providers.py contract

```text
Let's look at the providers file for a moment, because it's where reliability starts. Each adapter wraps its SDK's retryable errors, like rate limits after the SDK's own retries, timeouts, connection errors and server errors, in one common retryable provider error. Non retryable errors, like a bad request, pass through unchanged. It also returns normalized usage, including cached tokens. That small contract is what lets the router make good decisions without knowing anything about Claude, OpenAI or Gemini specifically.
```

Scene 8 — The router

```text
The router reads routes from configuration. For product descriptions, it tries Claude first and Gemini as the fallback. For social posts, OpenAI first and Claude as the fallback. Before calling a provider, it checks a simple circuit breaker: if a provider has failed five times in thirty seconds, it's skipped for a minute. Only retryable errors, like timeouts, rate limits after retries and server errors, trigger a fallback. Bad requests propagate, because they'd fail everywhere.
```

Scene 9 — Budgets and ledger

```text
The budget module checks each tenant's spend for the day before calling any provider, and returns a clear four twenty nine style error when the budget is reached. After each successful call, it records input, cached and output tokens with the cost from the prices file, and emits a ledger line. In production, keep that spend in a shared store like Redis or your database, so all replicas see the same numbers.
```

Scene 10 — Business scenario: a busy morning

```text
Now a realistic business scenario, with illustrative numbers. The agency's portal serves twenty five clients. On a busy morning, a client in Dubai requests Arabic social posts for a Ramadan campaign, a Lahore client requests Urdu product descriptions, and a London client requests English email drafts. Each request goes to its task's primary model. When one provider has a brief incident, the breaker opens and social posts shift to the fallback. Every response is validated, costs are attributed per client, and nothing personal leaves the service unredacted.
```

Scene 11 — Design choices

```text
Look at the key design choices in the app file. The brand voice sits in the system prompt, because it's stable per tenant, which lets prompt caching help. The brief goes in the user turn. Personal data is redacted before any provider sees it. Incomplete generations and invalid JSON become clear errors instead of broken content. And when every provider fails, the client gets a five oh three with a friendly message, while the logs keep the details.
```

Scene 12 — Common mistakes

```text
Common mistakes to avoid while building. Putting provider keys in the client portal. Skipping validation because structured outputs usually work. Keeping budget state only in memory on one replica. And hard coding model ids instead of reading them from routes. Each of these shows up in production, usually at the worst time.
```

Scene 13 — Why a needs_review flag?

```text
Why does the service return a needs review flag instead of just refusing risky content? Because in marketing, many briefs are fine with small edits, and a human reviewer can fix them quickly. The flag, with reasons, turns the model into a helpful first drafter that also highlights risk, like an unsupported health claim or a missing sponsorship label. Your portal can show flagged drafts with a yellow banner, so reviewers know exactly where to look.
```

Scene 14 — Deeper: a morning of traffic (illustrative)

```text
Let's deepen the Crescent Content API with a realistic morning, illustrative numbers. Between nine and eleven, the portal handles a few hundred requests from fourteen clients. A Lahore home goods brand generates forty Urdu product descriptions; the router uses Claude, and six are flagged for review because the briefs mentioned health benefits. A Dubai café chain generates Arabic social posts for a Ramadan campaign; the sponsored ones include the ad tag automatically because of the task rules. A London fitness studio's budget hits its daily limit at ten thirty, and its marketer sees a clear message instead of an error page. Every request has a request id, a provider, a model and a cost in the ledger.
```

Scene 15 — Watch me do it: one request through app.py

```text
Watch me do it. Let's trace one request through app dot py. The auth dependency checks the API key header and maps it to a caller. Budget check raises if the tenant's spend today has reached its limit, which becomes a four twenty nine. Redact removes personal data from the brief. The system prompt combines the brand voice, the task rules and the language name. The request object carries the system prompt, one user message, a twelve hundred token limit, the strict content schema and metadata. Router generate reads the routes for this task, skips any provider whose breaker is open, and returns the first success. If every provider fails, the endpoint returns a five oh three. If the stop value isn't end, it returns a five oh two. Content out model validate json checks the JSON against the schema. Budget record writes the ledger line and returns the cost. The response includes the content, provider, model, cost and request id.
```

Scene 16 — Recap + try this now

```text
Quick recap. You've built a multi provider content service with configuration based routing, fallbacks and a circuit breaker, strict structured outputs validated with Pydantic, budgets and a cost ledger, redaction and server side keys. Try this now: build it with at least two providers, set up the routes file, and generate one piece of content for each task in two languages, recording the provider and cost for each. In part two, you'll test, evaluate, observe, deploy and add a TypeScript client.
```

</details>

### 19. Capstone part 2: test, evaluate, deploy and ship a TypeScript client

Lesson `capstone-test-deploy-typescript-client` · module “Production readiness and capstone” · target 9 min · 16 scenes · 1324 words · planned 9:38 (words ≈ 9:36) · status: **to produce**

| Term | Say |
|---|---|
| TypeScript | type script |
| uvicorn | you-vee-corn |
| Redis | red-iss |

| # | Time | Sec | Narration | On screen | Visual |
|---|---|---|---|---|---|
| 1 | 0:00 | 25 | Your content service works on your machine. Now let's make it something twenty five clients can rely on. In this final lesson you'll test it without calling real providers, evaluate every route across languages, add observability, deploy it in a container, and ship a TypeScript client for the agency's web apps. Then you'll assemble the launch pack. | Capstone part 2<br>• Tests with fakes<br>• Evals across providers and languages<br>• Observability + deployment<br>• TypeScript client | Layout: the Crescent Content API box moves from a laptop onto a launch pad; four milestone flags appear along the path: Tests, Evals, Observability, Client. 0s: title 'Capstone part 2' fades in top-left. 2s: main graphic begins animating. 5s, 8s, 11s, 14s: on-screen bullets reveal one by one, each highlighted as narrated. 21s: hold final frame. |
| 2 | 0:25 | 27 | Why test with fakes? Because you want to check your own logic, like routing, validation, redaction and budgets, without spending money or depending on provider uptime. Think of a flight simulator. Pilots practice engine failures in a simulator because you can't wait for a real one. A fake provider is your simulator: it can succeed, fail, or return broken JSON on demand. | Why fakes?<br>• Test your logic, not the provider<br>• Free, fast, deterministic<br>• The flight simulator | Layout: a cockpit simulator screen triggers 'engine failure'; the pilot handles it calmly. The screen then switches to a test runner triggering 'provider outage'. 0s: title 'Why fakes?' fades in top-left. 2s: main graphic begins animating. 5s, 9s, 13s: on-screen bullets reveal one by one, each highlighted as narrated. 23s: hold final frame. |
| 3 | 0:52 | 33 | The lesson includes four tests. The happy path: a fake provider returns good JSON and the service returns content. Fallback: the primary fake raises a retryable error, and the response shows the fallback provider. Invalid JSON: the fake returns plain text and the service returns a five oh two instead of broken content. And redaction: a spy provider records what it received, and the test confirms the email address in the brief never reached it. | Four tests<br>• Happy path<br>• Fallback on outage<br>• Invalid JSON → 502<br>• PII never reaches the provider | Layout: a test runner lists four tests; each one expands briefly to show its fake/spy provider icon, then turns green. 0s: title 'Four tests' fades in top-left. 2s: main graphic begins animating. 5s, 10s, 15s, 20s: on-screen bullets reveal one by one, each highlighted as narrated. 29s: hold final frame. |
| 4 | 1:25 | 36 | Step two is an evaluation harness. Write thirty to sixty briefs per task across English, Arabic and Urdu. Score outputs on brand voice, fidelity to the brief with no invented claims, length limits, language correctness, and whether needs review is set when it should be. For example, a brief claiming clinically proven without evidence should be flagged. Combine a calibrated judge with code checks, like counting hashtags and requiring the ad tag when content is sponsored. Run every route's primary and fallback. | Eval harness<br>• 30–60 briefs per task × 3 languages<br>• Voice, fidelity, length, language, flags<br>• Judge + code checks<br>• Primary and fallback | Layout: a grid of tasks × languages; each cell shows two small score bars (primary, fallback); a 'clinically proven' brief card gets a red 'needs_review' flag. 0s: title 'Eval harness' fades in top-left. 2s: main graphic begins animating. 5s, 10s, 15s, 20s: on-screen bullets reveal one by one, each highlighted as narrated. 32s: hold final frame. |
| 5 | 2:01 | 30 | A simple example from the eval. A brief for a skincare cream says: our customers love it, it reduces wrinkles. The rubric checks that the output doesn't invent a percentage or a clinical claim, and that needs review is true, with a reason like, performance claim needs evidence. If a model writes, clinically proven to reduce wrinkles by forty percent, the case fails, no matter how beautiful the prose is. | Simple eval example<br>• Brief: 'reduces wrinkles'<br>• Must not invent stats or 'clinically proven'<br>• needs_review = true with reason | Layout: two output cards side by side; one with an invented '40%' claim stamped FAIL; the other with neutral wording and a needs_review flag stamped PASS. 0s: title 'Simple eval example' fades in top-left. 2s: main graphic begins animating. 5s, 10s, 15s: on-screen bullets reveal one by one, each highlighted as narrated. 26s: hold final frame. |
| 6 | 2:31 | 27 | Step three is observability. Write structured logs with the request id, tenant, task, provider, model, tokens, cost, latency and outcome, but never the raw brief. Track metrics: requests, errors by class, fallback activations, breaker state, latency percentiles and cost per tenant per day. Add traces around provider calls. And set alerts on error rate, fallback rate, spend anomalies and validation failures. | Observability<br>• Structured logs (no raw briefs)<br>• Metrics: errors, fallbacks, latency, cost<br>• Traces around provider calls<br>• Alerts on anomalies | Layout: a dashboard assembles tile by tile: error-rate chart, fallback counter, breaker status light, cost-per-tenant bars; an alert bell sits in the corner. 0s: title 'Observability' fades in top-left. 2s: main graphic begins animating. 5s, 8s, 11s, 14s: on-screen bullets reveal one by one, each highlighted as narrated. 23s: hold final frame. |
| 7 | 2:58 | 38 | Here's how to read your eval results. Look at each task, language and provider combination separately, because averages hide problems. If Arabic social posts score lower on the fallback than the primary, that fallback may need an Arabic specific prompt, or shouldn't be used for Arabic at all. If a model rarely sets needs review on risky briefs, strengthen the instruction or add a code check. And look at cost per item next to quality, because the best route is the cheapest one that clears your quality bar. | Reading eval results<br>• Break down by task × language × provider<br>• Fix weak combos with prompts or routes<br>• Watch needs_review behavior<br>• Cheapest route that clears the bar | Layout: a heatmap grid (tasks × languages) for two providers; one cell 'Arabic social post / fallback' glows amber; a note attaches 'add AR prompt variant'; a cost column sits beside quality. 0s: title 'Reading eval results' fades in top-left. 2s: main graphic begins animating. 5s, 11s, 17s, 23s: on-screen bullets reveal one by one, each highlighted as narrated. 34s: hold final frame. |
| 8 | 3:36 | 31 | Step four is deployment. The Dockerfile installs requirements, runs as a non root user and starts uvicorn with a couple of workers. Deploy behind TLS on a container platform in the region your data agreements require. Inject provider keys or workload identity from a secret manager. Run at least two replicas with shared budget state. And set timeouts above your longest expected generation, or use the streaming endpoint for long drafts. | Deployment<br>• Dockerfile, non-root user<br>• TLS, right region<br>• Secrets from a manager<br>• 2+ replicas, shared budget state<br>• Timeouts or streaming | Layout: a container ships to a regional cloud; a secret-manager vault feeds it; two replicas appear sharing a Redis-style store icon for budgets. 0s: title 'Deployment' fades in top-left. 2s: main graphic begins animating. 5s, 8s, 11s, 14s, 17s: on-screen bullets reveal one by one, each highlighted as narrated. 27s: hold final frame. |
| 9 | 4:07 | 38 | Step five is a TypeScript client for the agency's web apps. The generate function posts to your service with the caller's key, maps a four twenty nine to a friendly budget message and a five oh three to try again shortly, and returns typed content. A stream draft helper consumes your server sent events for long drafts. Remember: the browser never sees provider keys, and for server rendered apps, call the service from your server. For Arabic and Urdu, set the output container's direction to right to left. | TypeScript client<br>• Typed generate() with friendly errors<br>• streamDraft() via EventSource<br>• No provider keys in the browser<br>• dir='rtl' for Arabic/Urdu | Layout: a TypeScript code panel highlights the status mapping lines; beside it, a web page renders Arabic text right-to-left while a draft streams in. 0s: title 'TypeScript client' fades in top-left. 2s: main graphic begins animating. 5s, 11s, 17s, 23s: on-screen bullets reveal one by one, each highlighted as narrated. 34s: hold final frame. |
| 10 | 4:45 | 43 | Now the realistic business story, with illustrative numbers. In week one, two clients run in shadow mode, and the eval flags awkward Urdu hashtags, fixed with a language specific rule. In week two, five clients go live, and a provider incident opens the circuit breaker for twelve minutes; the fallback handles traffic and clients notice nothing. In week three, the cost report shows social posts are cheaper on the fallback at equal quality, so the team swaps the route after rerunning the eval. In week four, all twenty five clients are live, with per client AI costs invoiced monthly. | Business story: the first month<br>• W1: shadow, Urdu hashtag fix<br>• W2: incident → fallback, no impact<br>• W3: route swap after eval<br>• W4: 25 clients live, costs invoiced | Layout: a four-week timeline with an event card per week; week 2 shows a breaker icon open for '12 min'; week 3 shows routes.yaml lines swapping; week 4 shows 25 client icons lit. 0s: title 'Business story: the first month' fades in top-left. 2s: main graphic begins animating. 5s, 12s, 19s, 26s: on-screen bullets reveal one by one, each highlighted as narrated. 39s: hold final frame. |
| 11 | 5:28 | 30 | Step six is the runbook. Document how to switch a task's primary model: edit the routes file, run the eval, deploy. How to force a fallback during an incident. How to raise a tenant's budget. How to rotate keys. And who's on call. Pair it with the production checklist from the security lesson, with evidence for each row. That combination turns a clever project into a dependable service. | Runbook essentials<br>• Switch a primary model<br>• Force a fallback<br>• Raise a tenant budget<br>• Rotate keys<br>• On-call owner | Layout: a runbook document with five numbered procedures; each procedure shows a tiny command or config snippet. 0s: title 'Runbook essentials' fades in top-left. 2s: main graphic begins animating. 5s, 8s, 11s, 14s, 17s: on-screen bullets reveal one by one, each highlighted as narrated. 26s: hold final frame. |
| 12 | 5:58 | 20 | Common mistakes at this stage. Tests that call real providers, which makes them slow, flaky and expensive. Evals run only in English. Logs that include raw briefs with personal data. And a browser client holding provider keys. Each one undermines the careful work you've done. | Common mistakes<br>• Tests hitting real providers<br>• English-only evals<br>• Raw briefs in logs<br>• Provider keys in the browser | Layout: four red cards flip to green fixes. 0s: title 'Common mistakes' fades in top-left. 2s: main graphic begins animating. 5s, 6s, 7s, 8s: on-screen bullets reveal one by one, each highlighted as narrated. 16s: hold final frame. |
| 13 | 6:18 | 32 | One more question you'll face after launch: how do you add a fourth task, like blog outlines? Add the task to the schemas, write its rules, add a route in the configuration with a primary and fallback, write thirty eval briefs across your languages, and run the eval before exposing it in the client. Because the architecture separates configuration, schemas and evaluation, new tasks become a routine change rather than a new project. | Adding a new task<br>• Schema + rules<br>• Route in config<br>• 30 eval briefs across languages<br>• Expose in the client after eval | Layout: a checklist for 'blog_outline' ticks through Schema, Rules, Route, Eval, Client; the service diagram gains a new task lane. 0s: title 'Adding a new task' fades in top-left. 2s: main graphic begins animating. 5s, 9s, 13s, 17s: on-screen bullets reveal one by one, each highlighted as narrated. 28s: hold final frame. |
| 14 | 6:50 | 55 | Let's deepen the first month's story with illustrative numbers. In shadow mode, the eval showed Urdu hashtags were often awkward transliterations, so the team added a rule: Urdu posts use English hashtags only. During the provider incident in week two, the breaker opened for twelve minutes and about two hundred requests went to the fallback; the eval had already shown the fallback's English quality was equal and its Arabic slightly lower, so they temporarily routed Arabic requests to a queue for the few minutes it mattered. In week three, the cost report showed the fallback was cheaper for social posts at equal quality, so the routes swapped after a fresh eval run. By week four, the agency invoiced AI usage per client for the first time. | Deeper: month one (illustrative)<br>• Urdu hashtag rule from shadow eval<br>• Incident: ~200 requests via fallback<br>• Arabic briefly queued<br>• Week 3 route swap; week 4 per-client invoicing | Layout: a four-week timeline. 0s: week 1 card shows a hashtag rule. 10s: week 2 shows a breaker icon and '~200 via fallback', with an Arabic queue icon. 22s: week 3 shows routes.yaml lines swapping. 32s: week 4 shows invoices per client. 44s: hold. |
| 15 | 7:45 | 73 | Watch me do it. Let's run the test suite and the client. The fake provider returns good JSON, or raises a retryable error, or returns plain text, depending on how I construct it. The test client posts the same body each time, which includes an email address in the brief. Happy path: I replace Anthropic with a fake and expect status two hundred and the title blue pottery mug. Fallback: Anthropic fails, Google succeeds, and the response says the provider was Google. Invalid JSON: the fake returns not json and the service returns five oh two. Redaction: a spy provider records the brief it received, and the test asserts the email isn't in it. I run pytest: four passes, no API calls. Then the TypeScript client: generate posts to slash v1 slash generate, maps four twenty nine and five oh three to friendly messages, and returns typed content. I call it from a small page, and the title and body render, with right to left direction for Arabic. | Watch me do it: tests + TS client<br>• FakeProvider: good / fail / bad JSON<br>• 4 tests incl. redaction spy<br>• pytest: 4 passed, 0 API calls<br>• generate(): typed, friendly errors, RTL render | Layout: pytest file left, terminal right, then a browser. 0s: FakeProvider highlights. 10s: test_happy_path and test_fallback_on_outage highlight. 23s: test_invalid_json and the redaction Spy highlight. 35s: terminal shows '4 passed'. 45s: crescentClient.ts generate() highlights with status mappings circled. 56s: a browser renders an Arabic post right-to-left. 70s: hold. |
| 16 | 8:58 | 40 | Quick recap, and congratulations. You've tested with fakes, evaluated routes across languages, added observability, deployed securely and shipped a TypeScript client and a runbook. Across the course you've learned the provider landscape, keys, message formats, streaming, tools, structured outputs, multimodal inputs, embeddings, batch, caching, reliability, cost tracking, abstraction, routing, cloud platforms, open models and security. Try this now: add the four tests, run the eval across two providers and three languages, deploy to staging, build a tiny web page with the client, write the runbook, and then take the final exam. | Recap + try this now<br>• Tests, evals, observability, deploy, client<br>• Assemble the launch pack<br>• Then take the final exam | Layout: a montage of icons from every module assembling into a launch-pack folder stamped 'Ready'; a final 'Take the exam' button pulses. 0s: title 'Recap + try this now' fades in top-left. 2s: main graphic begins animating. 5s, 14s, 23s: on-screen bullets reveal one by one, each highlighted as narrated. 36s: hold final frame. |

<details><summary>Narration per scene (paste into ElevenLabs)</summary>

Scene 1 — Capstone part 2

```text
Your content service works on your machine. Now let's make it something twenty five clients can rely on. In this final lesson you'll test it without calling real providers, evaluate every route across languages, add observability, deploy it in a container, and ship a TypeScript client for the agency's web apps. Then you'll assemble the launch pack.
```

Scene 2 — Why fakes?

```text
Why test with fakes? Because you want to check your own logic, like routing, validation, redaction and budgets, without spending money or depending on provider uptime. Think of a flight simulator. Pilots practice engine failures in a simulator because you can't wait for a real one. A fake provider is your simulator: it can succeed, fail, or return broken JSON on demand.
```

Scene 3 — Four tests

```text
The lesson includes four tests. The happy path: a fake provider returns good JSON and the service returns content. Fallback: the primary fake raises a retryable error, and the response shows the fallback provider. Invalid JSON: the fake returns plain text and the service returns a five oh two instead of broken content. And redaction: a spy provider records what it received, and the test confirms the email address in the brief never reached it.
```

Scene 4 — Eval harness

```text
Step two is an evaluation harness. Write thirty to sixty briefs per task across English, Arabic and Urdu. Score outputs on brand voice, fidelity to the brief with no invented claims, length limits, language correctness, and whether needs review is set when it should be. For example, a brief claiming clinically proven without evidence should be flagged. Combine a calibrated judge with code checks, like counting hashtags and requiring the ad tag when content is sponsored. Run every route's primary and fallback.
```

Scene 5 — Simple eval example

```text
A simple example from the eval. A brief for a skincare cream says: our customers love it, it reduces wrinkles. The rubric checks that the output doesn't invent a percentage or a clinical claim, and that needs review is true, with a reason like, performance claim needs evidence. If a model writes, clinically proven to reduce wrinkles by forty percent, the case fails, no matter how beautiful the prose is.
```

Scene 6 — Observability

```text
Step three is observability. Write structured logs with the request id, tenant, task, provider, model, tokens, cost, latency and outcome, but never the raw brief. Track metrics: requests, errors by class, fallback activations, breaker state, latency percentiles and cost per tenant per day. Add traces around provider calls. And set alerts on error rate, fallback rate, spend anomalies and validation failures.
```

Scene 7 — Reading eval results

```text
Here's how to read your eval results. Look at each task, language and provider combination separately, because averages hide problems. If Arabic social posts score lower on the fallback than the primary, that fallback may need an Arabic specific prompt, or shouldn't be used for Arabic at all. If a model rarely sets needs review on risky briefs, strengthen the instruction or add a code check. And look at cost per item next to quality, because the best route is the cheapest one that clears your quality bar.
```

Scene 8 — Deployment

```text
Step four is deployment. The Dockerfile installs requirements, runs as a non root user and starts uvicorn with a couple of workers. Deploy behind TLS on a container platform in the region your data agreements require. Inject provider keys or workload identity from a secret manager. Run at least two replicas with shared budget state. And set timeouts above your longest expected generation, or use the streaming endpoint for long drafts.
```

Scene 9 — TypeScript client

```text
Step five is a TypeScript client for the agency's web apps. The generate function posts to your service with the caller's key, maps a four twenty nine to a friendly budget message and a five oh three to try again shortly, and returns typed content. A stream draft helper consumes your server sent events for long drafts. Remember: the browser never sees provider keys, and for server rendered apps, call the service from your server. For Arabic and Urdu, set the output container's direction to right to left.
```

Scene 10 — Business story: the first month

```text
Now the realistic business story, with illustrative numbers. In week one, two clients run in shadow mode, and the eval flags awkward Urdu hashtags, fixed with a language specific rule. In week two, five clients go live, and a provider incident opens the circuit breaker for twelve minutes; the fallback handles traffic and clients notice nothing. In week three, the cost report shows social posts are cheaper on the fallback at equal quality, so the team swaps the route after rerunning the eval. In week four, all twenty five clients are live, with per client AI costs invoiced monthly.
```

Scene 11 — Runbook essentials

```text
Step six is the runbook. Document how to switch a task's primary model: edit the routes file, run the eval, deploy. How to force a fallback during an incident. How to raise a tenant's budget. How to rotate keys. And who's on call. Pair it with the production checklist from the security lesson, with evidence for each row. That combination turns a clever project into a dependable service.
```

Scene 12 — Common mistakes

```text
Common mistakes at this stage. Tests that call real providers, which makes them slow, flaky and expensive. Evals run only in English. Logs that include raw briefs with personal data. And a browser client holding provider keys. Each one undermines the careful work you've done.
```

Scene 13 — Adding a new task

```text
One more question you'll face after launch: how do you add a fourth task, like blog outlines? Add the task to the schemas, write its rules, add a route in the configuration with a primary and fallback, write thirty eval briefs across your languages, and run the eval before exposing it in the client. Because the architecture separates configuration, schemas and evaluation, new tasks become a routine change rather than a new project.
```

Scene 14 — Deeper: month one (illustrative)

```text
Let's deepen the first month's story with illustrative numbers. In shadow mode, the eval showed Urdu hashtags were often awkward transliterations, so the team added a rule: Urdu posts use English hashtags only. During the provider incident in week two, the breaker opened for twelve minutes and about two hundred requests went to the fallback; the eval had already shown the fallback's English quality was equal and its Arabic slightly lower, so they temporarily routed Arabic requests to a queue for the few minutes it mattered. In week three, the cost report showed the fallback was cheaper for social posts at equal quality, so the routes swapped after a fresh eval run. By week four, the agency invoiced AI usage per client for the first time.
```

Scene 15 — Watch me do it: tests + TS client

```text
Watch me do it. Let's run the test suite and the client. The fake provider returns good JSON, or raises a retryable error, or returns plain text, depending on how I construct it. The test client posts the same body each time, which includes an email address in the brief. Happy path: I replace Anthropic with a fake and expect status two hundred and the title blue pottery mug. Fallback: Anthropic fails, Google succeeds, and the response says the provider was Google. Invalid JSON: the fake returns not json and the service returns five oh two. Redaction: a spy provider records the brief it received, and the test asserts the email isn't in it. I run pytest: four passes, no API calls. Then the TypeScript client: generate posts to slash v1 slash generate, maps four twenty nine and five oh three to friendly messages, and returns typed content. I call it from a small page, and the title and body render, with right to left direction for Arabic.
```

Scene 16 — Recap + try this now

```text
Quick recap, and congratulations. You've tested with fakes, evaluated routes across languages, added observability, deployed securely and shipped a TypeScript client and a runbook. Across the course you've learned the provider landscape, keys, message formats, streaming, tools, structured outputs, multimodal inputs, embeddings, batch, caching, reliability, cost tracking, abstraction, routing, cloud platforms, open models and security. Try this now: add the four tests, run the eval across two providers and three languages, deploy to staging, build a tiny web page with the client, write the runbook, and then take the final exam.
```

</details>
