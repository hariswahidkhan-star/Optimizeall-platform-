# Privacy-First Measurement: Server-Side Tagging, Consent Mode and Conversion APIs — lecture production script

Course `privacy-first-measurement` · pack v1 · reviewed 2026-09 · level advanced · category data

14 lectures · 16,589 narration words · planned runtime 2:00:11 (≈ 118 min at 140 wpm)

> Generated by `scripts/export-lecture-scripts.py` from the course pack — edit the pack, not this file. Production workflow: [README](README.md).

## Voice direction

Conversational expert, second person, ~140 wpm; clear and exact with numbers; say units; audience level: advanced. Pause ~0.5 s between scenes; no music under narration (added in edit).

## Lectures

1. [The state of cookies and tracking in 2026](#1-the-state-of-cookies-and-tracking-in-2026)
2. [Privacy laws for marketers: EU, UK, US, KSA and UAE](#2-privacy-laws-for-marketers-eu-uk-us-ksa-and-uae)
3. [Consent management: CMPs, IAB TCF 2.3 and GPP](#3-consent-management-cmps-iab-tcf-2-3-and-gpp)
4. [Google Consent Mode v2: basic vs advanced, done right](#4-google-consent-mode-v2-basic-vs-advanced-done-right)
5. [GA4 with consent: modeling, settings and the Measurement Protocol](#5-ga4-with-consent-modeling-settings-and-the-measurement-protocol)
6. [Server-side tagging with server-side GTM and Google tag gateway](#6-server-side-tagging-with-server-side-gtm-and-google-tag-gateway)
7. [Meta Conversions API: payloads, matching and deduplication](#7-meta-conversions-api-payloads-matching-and-deduplication)
8. [Google enhanced conversions, offline imports and Data Manager](#8-google-enhanced-conversions-offline-imports-and-data-manager)
9. [TikTok Events API and LinkedIn Conversions API](#9-tiktok-events-api-and-linkedin-conversions-api)
10. [Data quality: event IDs, deduplication and a tracking plan](#10-data-quality-event-ids-deduplication-and-a-tracking-plan)
11. [Debugging and QA for consent-aware tracking](#11-debugging-and-qa-for-consent-aware-tracking)
12. [First-party data strategy: value exchange, identity and activation](#12-first-party-data-strategy-value-exchange-identity-and-activation)
13. [Modeled conversions, aggregated measurement and data clean rooms](#13-modeled-conversions-aggregated-measurement-and-data-clean-rooms)
14. [Capstone: design and document a consent-compliant server-side tracking setup](#14-capstone-design-and-document-a-consent-compliant-server-side-tracking-setup)

### 1. The state of cookies and tracking in 2026

Lesson `state-of-tracking-2026` · module “The tracking landscape and privacy law” · target 9 min · 14 scenes · 1231 words · planned 8:55 (words ≈ 8:55) · status: **to produce**

| Term | Say |
|---|---|
| ITP | I T P |
| ATT | A T T |
| CHIPS | chips |
| FedCM | fed C M |
| gclid | G click I D |

| # | Time | Sec | Narration | On screen | Visual |
|---|---|---|---|---|---|
| 1 | 0:00 | 32 | For five years, marketers were told that third-party cookies were about to die in Chrome. Then, in twenty twenty-five, Google changed course and kept them, with user choice. So can we all relax? Not at all. In this lecture you'll learn what actually changed, the seven forces still eroding your measurement, the three layers of a privacy-first measurement stack, and a fifteen-minute check that shows you how much your own tracking is missing. | The state of tracking in 2026<br>• What changed with Chrome<br>• Seven forces eroding measurement<br>• Three measurement layers<br>• A 15-minute reality check | Layout: A cookie icon shown with a countdown timer that stops and resets; then seven small arrows chip away at a pie chart labeled 'observed conversions'. Timing (32s): title card 0–3s; then the 4 on-screen points build in narration order about every 6s, each animating its element; hold and highlight the key idea in the final 5s. |
| 2 | 0:32 | 46 | Why does this matter? Because every automated ad system, every attribution report and every budget decision rests on the data your tags collect. Here's an analogy. Imagine a shop with a door counter that only counts people wearing red. For years most people wore red, so the counter was roughly right. Now more and more people wear blue: they declined consent, they use Safari, they block scripts, they switch devices. The counter still ticks, but it's counting a smaller and different crowd. If you plan staffing from that counter, you'll get it wrong. Privacy-first measurement is about counting the whole crowd lawfully, or estimating it honestly. | Why it matters<br>• Counter only sees 'red' visitors<br>• More visitors now 'blue'<br>• Smaller, different crowd counted<br>• Count lawfully or estimate honestly | Layout: shop doorway with a counter above. Animate a stream of red and blue visitor icons; the counter ticks only for red. Over time the blue share grows and a gap bar labeled 'unseen' widens. |
| 3 | 1:18 | 33 | First, the facts. In twenty twenty-five Google announced it would keep its current approach to third-party cookies in Chrome. Users can block them in settings, and they're blocked in Incognito, but there's no forced deprecation. Then in October twenty twenty-five, Google retired most Privacy Sandbox technologies, including the Attribution Reporting API, Topics and Protected Audience, while continuing a few with broad adoption, like CHIPS and FedCM. Always check Google's Privacy Sandbox site for the current list. | Chrome and Privacy Sandbox<br>• Third-party cookies kept, with user choice<br>• Oct 2025: most Sandbox APIs retired<br>• CHIPS and FedCM continue<br>• Check current status | Layout: Timeline graphic from 2020 to 2026 with milestones: 'deprecation planned', 'plan changed 2025', 'Sandbox APIs retired Oct 2025'. Timing (33s): title card 0–3s; then the 4 on-screen points build in narration order about every 6s, each animating its element; hold and highlight the key idea in the final 5s. |
| 4 | 1:51 | 34 | So why does this course exist? Because cookies were only one piece. Consent laws in Europe, the UK, Saudi Arabia and elsewhere mean many tags can't fire before a user agrees. Safari's Intelligent Tracking Prevention blocks third-party cookies and limits some first-party cookies set by JavaScript. Firefox partitions cookies by site. Apple's App Tracking Transparency means many app users decline tracking. Ad blockers stop common tag scripts. People switch devices. And regulators fine companies whose pixels leak sensitive data. | Seven forces<br>• Consent laws<br>• Safari ITP<br>• Firefox cookie partitioning<br>• Apple ATT<br>• Ad blockers<br>• Cross-device journeys<br>• Enforcement | Layout: Seven icons orbit a website icon: a gavel, a compass, a fox-like browser silhouette (generic), a phone with a permission dialog, a shield, two devices, and a fine notice. Timing (34s): title card 0–3s; then the 7 on-screen points build in narration order about every 3s, each animating its element; hold and highlight the key idea in the final 5s. |
| 5 | 2:25 | 30 | The result is that browser-only tags see a shrinking and biased slice of reality. Biased is the important word. The people you can't observe aren't random. They're more likely to use Safari, to decline consent, or to buy on a different device. So a platform optimizing only on observed conversions is learning from a skewed sample. That's why accuracy is a performance issue, not just a reporting issue. | Shrinking and biased<br>• Missing users aren't random<br>• Safari, refusals, cross-device<br>• Skewed learning for ad AI | Layout: A population of dots where one color group fades out disproportionately; a model icon learns only from the remaining dots. Timing (30s): title card 0–3s; then the 3 on-screen points build in narration order about every 7s, each animating its element; hold and highlight the key idea in the final 5s. |
| 6 | 2:55 | 47 | Here's a simple worked example. A small Karachi electronics store checks last month's numbers. The store backend shows one thousand orders. GA4 shows seven hundred and twenty purchases. Meta shows five hundred and eighty. Before doing anything else, they break down the gap. Safari and in-app browsers make up a large share of their traffic. Their cookie banner blocks tags until consent, and many visitors decline. And some buyers see an ad on their phone and buy on a laptop. None of this means the ads don't work. It means the observed data is partial and biased, and the fixes are different for each cause. These numbers are illustrative. | Simple example: Karachi electronics (illustrative)<br>• Backend 1,000 \| GA4 720 \| Meta 580<br>• Causes: Safari/in-app, declines, cross-device<br>• Different fix for each cause | Layout: three horizontal bars (backend, GA4, Meta) animate to their lengths. Then the gap segment splits into three labeled pieces: browser limits, consent declines, cross-device. |
| 7 | 3:42 | 33 | Think of a privacy-first stack as three layers. Layer one, observed client-side: browser tags like the Google tag and the Meta Pixel. Easy and rich in context, but the most vulnerable. Layer two, observed server-side: your server sends events to platforms, through the Meta Conversions API, Google enhanced conversions, the TikTok Events API and the LinkedIn Conversions API. More durable, more control. Layer three, modeled and aggregated: consent mode modeling, conversion modeling, mix models and lift tests. | Three layers<br>• Client-side observed<br>• Server-side observed<br>• Modeled and aggregated | Layout: Three horizontal layers stacking up: browser icon, server rack icon, and a statistical chart icon. Timing (33s): title card 0–3s; then the 3 on-screen points build in narration order about every 8s, each animating its element; hold and highlight the key idea in the final 5s. |
| 8 | 4:15 | 29 | Let me be very clear on one point. Server-side tracking is not a way around consent. If a user says no to advertising cookies, sending their data server-side for advertising purposes is still a problem, and regulators have said so. Server-side is about accuracy, efficiency and control over what data leaves your systems. The legal basis follows the purpose and the data, not the technical route. | Not a consent workaround<br>• Consent follows purpose and data<br>• Not the technical route<br>• Server-side = accuracy and control | Layout: A server icon with a red 'no entry' sign on a data path when the consent toggle is off, and a green path when it's on. Timing (29s): title card 0–3s; then the 3 on-screen points build in narration order about every 7s, each animating its element; hold and highlight the key idea in the final 5s. |
| 9 | 4:44 | 33 | The foundation of the new stack is first-party context. Cookies set by your own server in HTTP responses are more durable than cookies written by JavaScript in Safari. Logged-in users and CRM identifiers let you match with hashed email or phone, with consent where required. And click identifiers in your landing URLs, like gclid, fbclid and ttclid, should be captured and stored with the lead or order, so you can attribute and upload conversions later. | First-party foundation<br>• Server-set first-party cookies<br>• Login and CRM identifiers (hashed)<br>• Stored click IDs | Layout: A browser address bar showing a URL with a click ID parameter highlighted, flowing into a database record next to an order ID. Timing (33s): title card 0–3s; then the 3 on-screen points build in narration order about every 8s, each animating its element; hold and highlight the key idea in the final 5s. |
| 10 | 5:17 | 35 | Here's an illustrative audit. A Dubai fashion site fired Meta and TikTok pixels before the cookie banner was answered. Safari visitors appeared as new users every few days. And the ad platforms reported far fewer purchases than Shopify. The plan: fix consent gating, implement Consent Mode version two with a proper consent platform, add a server-side container on a first-party subdomain, capture click IDs and hashed email at checkout with notice, and use modeling and lift tests for the rest. | Example: Dubai fashion site (illustrative)<br>• Pixels fired before consent<br>• Safari users looked new<br>• Platforms under-reported<br>• Five-step fix plan | Layout: Audit report card with three red findings flipping to a five-step plan list with icons. Timing (35s): title card 0–3s; then the 4 on-screen points build in narration order about every 6s, each animating its element; hold and highlight the key idea in the final 5s. |
| 11 | 5:52 | 30 | Now your fifteen-minute reality check. Open your site in a fresh browser profile and look at cookies before touching the banner. Watch the network panel for tag requests firing before consent. Accept all, then reject all in another profile, and compare. Repeat in Safari and note cookie expiry dates. Then compare last month's backend orders with GA4 and each ad platform. Write down the gaps. That gap is your roadmap. | Your 15-minute check<br>• Cookies before consent<br>• Requests before consent<br>• Accept vs reject comparison<br>• Safari expiry dates<br>• Backend vs GA4 vs platforms | Layout: Screen-recording style mock of browser developer tools with the Application and Network tabs highlighted; generic site, no real brand. Timing (30s): title card 0–3s; then the 5 on-screen points build in narration order about every 4s, each animating its element; hold and highlight the key idea in the final 5s. |
| 12 | 6:22 | 43 | Let's name the common mistakes. Believing that because Chrome kept third-party cookies, nothing changed. Treating server-side tracking as a way around consent. Leaving pixels firing before the banner is answered. Ignoring what's hidden in URLs, like email addresses in query strings or health terms in paths. And trying to close the gap with fingerprinting, which regulators treat as needing consent anyway. Try this now: open your site in a private window, don't touch the banner, and check the network panel for requests to ad or analytics domains. If anything fires, write it down. That's your first compliance fix. | Mistakes + try this now<br>• 'Cookies are back' complacency<br>• Server-side as a workaround<br>• Pre-consent pixels<br>• Data in URLs<br>• Fingerprinting<br>• Try: private window network check | Layout: five mistake chips; then a mock browser developer tools network panel with requests streaming in and two rows highlighted red as 'fired before consent'. |
| 13 | 7:05 | 83 | Watch me do it. I'll run the fifteen-minute reality check on an illustrative Abu Dhabi furniture site, narrating what I see. I open a fresh browser profile and load the home page without touching the banner. In the application tab, I see an underscore g a cookie and an underscore f b p cookie already set. That's a problem: analytics and the Meta pixel fired before consent. In the network tab, filtered for collect and tr, I see requests to Google Analytics and to Meta, both before any choice. I click reject all, visit a product page, and the Meta requests continue. Now I know the banner is decorative. Next I open Safari, accept all, and inspect cookies: underscore g a is set by JavaScript with a long expiry, but I know Safari may cap script-set cookies, so returning visitors will look new sooner. Finally, the numbers: last month the store recorded eight hundred orders, GA4 six hundred and ten, Meta four hundred and ninety. I write my gap analysis in three sentences: pre-consent firing to fix immediately, no server-side events, and Safari durability. That's a roadmap, built in fifteen minutes. | Watch me do it: reality check (illustrative)<br>• Cookies set before consent<br>• Requests before and after reject<br>• Safari: script-set cookies<br>• Orders 800 \| GA4 610 \| Meta 490<br>• 3-sentence gap analysis | Layout: mock browser with developer tools docked right. Timing: application tab highlights offending cookies at ~15s; network rows stream at ~40s with pre-consent rows flashing red; Safari panel at ~80s; comparison bars at ~110s; gap-analysis card types in over the final 20s. |
| 14 | 8:28 | 27 | Recap. Chrome kept third-party cookies, but consent, browsers, Apple, blockers and cross-device journeys still erode measurement, and the missing data is biased. Build three layers: client-side, server-side and modeled, all lawful. Anchor on first-party context. Your next step: run the fifteen-minute check on your own site and write a one-paragraph gap analysis. The rest of this course shows you how to close it. | Recap and next step<br>• Measurement still eroding<br>• Three lawful layers<br>• First-party foundation<br>• Write your gap analysis | Layout: The three-layer stack reappears with a gap marker between 'reality' and 'observed', and a pen writing 'gap analysis'. Timing (27s): title card 0–3s; then the 4 on-screen points build in narration order about every 4s, each animating its element; hold and highlight the key idea in the final 5s. |

<details><summary>Narration per scene (paste into ElevenLabs)</summary>

Scene 1 — The state of tracking in 2026

```text
For five years, marketers were told that third-party cookies were about to die in Chrome. Then, in twenty twenty-five, Google changed course and kept them, with user choice. So can we all relax? Not at all. In this lecture you'll learn what actually changed, the seven forces still eroding your measurement, the three layers of a privacy-first measurement stack, and a fifteen-minute check that shows you how much your own tracking is missing.
```

Scene 2 — Why it matters

```text
Why does this matter? Because every automated ad system, every attribution report and every budget decision rests on the data your tags collect. Here's an analogy. Imagine a shop with a door counter that only counts people wearing red. For years most people wore red, so the counter was roughly right. Now more and more people wear blue: they declined consent, they use Safari, they block scripts, they switch devices. The counter still ticks, but it's counting a smaller and different crowd. If you plan staffing from that counter, you'll get it wrong. Privacy-first measurement is about counting the whole crowd lawfully, or estimating it honestly.
```

Scene 3 — Chrome and Privacy Sandbox

```text
First, the facts. In twenty twenty-five Google announced it would keep its current approach to third-party cookies in Chrome. Users can block them in settings, and they're blocked in Incognito, but there's no forced deprecation. Then in October twenty twenty-five, Google retired most Privacy Sandbox technologies, including the Attribution Reporting API, Topics and Protected Audience, while continuing a few with broad adoption, like CHIPS and FedCM. Always check Google's Privacy Sandbox site for the current list.
```

Scene 4 — Seven forces

```text
So why does this course exist? Because cookies were only one piece. Consent laws in Europe, the UK, Saudi Arabia and elsewhere mean many tags can't fire before a user agrees. Safari's Intelligent Tracking Prevention blocks third-party cookies and limits some first-party cookies set by JavaScript. Firefox partitions cookies by site. Apple's App Tracking Transparency means many app users decline tracking. Ad blockers stop common tag scripts. People switch devices. And regulators fine companies whose pixels leak sensitive data.
```

Scene 5 — Shrinking and biased

```text
The result is that browser-only tags see a shrinking and biased slice of reality. Biased is the important word. The people you can't observe aren't random. They're more likely to use Safari, to decline consent, or to buy on a different device. So a platform optimizing only on observed conversions is learning from a skewed sample. That's why accuracy is a performance issue, not just a reporting issue.
```

Scene 6 — Simple example: Karachi electronics (illustrative)

```text
Here's a simple worked example. A small Karachi electronics store checks last month's numbers. The store backend shows one thousand orders. GA4 shows seven hundred and twenty purchases. Meta shows five hundred and eighty. Before doing anything else, they break down the gap. Safari and in-app browsers make up a large share of their traffic. Their cookie banner blocks tags until consent, and many visitors decline. And some buyers see an ad on their phone and buy on a laptop. None of this means the ads don't work. It means the observed data is partial and biased, and the fixes are different for each cause. These numbers are illustrative.
```

Scene 7 — Three layers

```text
Think of a privacy-first stack as three layers. Layer one, observed client-side: browser tags like the Google tag and the Meta Pixel. Easy and rich in context, but the most vulnerable. Layer two, observed server-side: your server sends events to platforms, through the Meta Conversions API, Google enhanced conversions, the TikTok Events API and the LinkedIn Conversions API. More durable, more control. Layer three, modeled and aggregated: consent mode modeling, conversion modeling, mix models and lift tests.
```

Scene 8 — Not a consent workaround

```text
Let me be very clear on one point. Server-side tracking is not a way around consent. If a user says no to advertising cookies, sending their data server-side for advertising purposes is still a problem, and regulators have said so. Server-side is about accuracy, efficiency and control over what data leaves your systems. The legal basis follows the purpose and the data, not the technical route.
```

Scene 9 — First-party foundation

```text
The foundation of the new stack is first-party context. Cookies set by your own server in HTTP responses are more durable than cookies written by JavaScript in Safari. Logged-in users and CRM identifiers let you match with hashed email or phone, with consent where required. And click identifiers in your landing URLs, like gclid, fbclid and ttclid, should be captured and stored with the lead or order, so you can attribute and upload conversions later.
```

Scene 10 — Example: Dubai fashion site (illustrative)

```text
Here's an illustrative audit. A Dubai fashion site fired Meta and TikTok pixels before the cookie banner was answered. Safari visitors appeared as new users every few days. And the ad platforms reported far fewer purchases than Shopify. The plan: fix consent gating, implement Consent Mode version two with a proper consent platform, add a server-side container on a first-party subdomain, capture click IDs and hashed email at checkout with notice, and use modeling and lift tests for the rest.
```

Scene 11 — Your 15-minute check

```text
Now your fifteen-minute reality check. Open your site in a fresh browser profile and look at cookies before touching the banner. Watch the network panel for tag requests firing before consent. Accept all, then reject all in another profile, and compare. Repeat in Safari and note cookie expiry dates. Then compare last month's backend orders with GA4 and each ad platform. Write down the gaps. That gap is your roadmap.
```

Scene 12 — Mistakes + try this now

```text
Let's name the common mistakes. Believing that because Chrome kept third-party cookies, nothing changed. Treating server-side tracking as a way around consent. Leaving pixels firing before the banner is answered. Ignoring what's hidden in URLs, like email addresses in query strings or health terms in paths. And trying to close the gap with fingerprinting, which regulators treat as needing consent anyway. Try this now: open your site in a private window, don't touch the banner, and check the network panel for requests to ad or analytics domains. If anything fires, write it down. That's your first compliance fix.
```

Scene 13 — Watch me do it: reality check (illustrative)

```text
Watch me do it. I'll run the fifteen-minute reality check on an illustrative Abu Dhabi furniture site, narrating what I see. I open a fresh browser profile and load the home page without touching the banner. In the application tab, I see an underscore g a cookie and an underscore f b p cookie already set. That's a problem: analytics and the Meta pixel fired before consent. In the network tab, filtered for collect and tr, I see requests to Google Analytics and to Meta, both before any choice. I click reject all, visit a product page, and the Meta requests continue. Now I know the banner is decorative. Next I open Safari, accept all, and inspect cookies: underscore g a is set by JavaScript with a long expiry, but I know Safari may cap script-set cookies, so returning visitors will look new sooner. Finally, the numbers: last month the store recorded eight hundred orders, GA4 six hundred and ten, Meta four hundred and ninety. I write my gap analysis in three sentences: pre-consent firing to fix immediately, no server-side events, and Safari durability. That's a roadmap, built in fifteen minutes.
```

Scene 14 — Recap and next step

```text
Recap. Chrome kept third-party cookies, but consent, browsers, Apple, blockers and cross-device journeys still erode measurement, and the missing data is biased. Build three layers: client-side, server-side and modeled, all lawful. Anchor on first-party context. Your next step: run the fifteen-minute check on your own site and write a one-paragraph gap analysis. The rest of this course shows you how to close it.
```

</details>

### 2. Privacy laws for marketers: EU, UK, US, KSA and UAE

Lesson `privacy-laws-for-marketers` · module “The tracking landscape and privacy law” · target 9 min · 14 scenes · 1242 words · planned 8:59 (words ≈ 9:00) · status: **to produce**

| Term | Say |
|---|---|
| PECR | pecker |
| GDPR | G D P R |
| SDAIA | sed-ah-ya |
| PDPL | P D P L |
| DIFC | D I F C |
| ADGM | A D G M |
| GPC | G P C |

| # | Time | Sec | Narration | On screen | Visual |
|---|---|---|---|---|---|
| 1 | 0:00 | 37 | Every tag you add to a website is a legal decision, whether you realize it or not. Which data goes to which company, where, for how long, and whether the user agreed. In this lecture you'll get a practical map of the privacy laws that shape marketing measurement across Europe, the UK, the United States, Saudi Arabia and the UAE. It's not legal advice, and laws change, so always confirm current guidance, but it will help you ask the right questions and build the right setup. | Privacy laws for marketers<br>• EU and UK<br>• United States<br>• Saudi Arabia<br>• UAE and free zones<br>• Your tag-to-law register | Layout: A website with tags shown as small plugs; each plug gets a legal label tag attached as the regions are named on a world map. Timing (37s): title card 0–3s; then the 5 on-screen points build in narration order about every 5s, each animating its element; hold and highlight the key idea in the final 5s. |
| 2 | 0:37 | 47 | Why does this matter? Because the cost of getting it wrong isn't just a fine. It's having to switch off tracking in a hurry, losing your data history, and explaining to customers why their data went somewhere it shouldn't. Here's an analogy. Think of privacy laws like traffic laws in different countries. The basic idea is the same everywhere, don't hurt people, but the details differ: which side you drive on, speed limits, whether you can turn on red. A marketer running ads across the UK, the Gulf and the US is a driver crossing borders, and needs to know the local rules before the first mile. | Why it matters<br>• Costs: fines, shutdowns, lost trust<br>• Laws are like traffic rules<br>• Same idea, different details per country | Layout: stylized map with road signs popping up over the UK, EU, US, UAE and Saudi Arabia, each sign showing a different rule icon. |
| 3 | 1:24 | 32 | Start with Europe and the UK, where there are two layers. Layer one is about the device. The EU's ePrivacy rules and the UK's PECR say you need consent to store or access information on someone's device, unless it's strictly necessary for a service they asked for. That covers cookies, local storage, pixels and fingerprinting. Layer two is data protection, the GDPR and UK GDPR: lawful basis, transparency, minimization, retention, security, transfers and rights. | EU and UK: two layers<br>• Device access: ePrivacy / PECR<br>• Data protection: GDPR / UK GDPR<br>• Consent for non-essential device access | Layout: Two stacked translucent layers over a browser: the top labeled 'device access', the bottom 'data protection'. Timing (32s): title card 0–3s; then the 3 on-screen points build in narration order about every 8s, each animating its element; hold and highlight the key idea in the final 5s. |
| 4 | 1:56 | 32 | There's an important UK update. The Data Use and Access Act twenty twenty-five added exemptions from cookie consent for some low-risk purposes, including analytics used only to produce statistics that improve your service, provided people get clear information and an easy way to object. Its provisions have been coming into force in stages through twenty twenty-six. Advertising and cross-site tracking still need consent. Check the ICO's current guidance before you rely on the exemption. | UK: Data (Use and Access) Act 2025<br>• Some analytics may be exempt<br>• Clear info + easy objection<br>• Advertising still needs consent<br>• Check current ICO guidance | Layout: A UK map with a small analytics chart icon passing through a gate marked 'exemption', while an advertising icon is stopped at a gate marked 'consent required'. Timing (32s): title card 0–3s; then the 4 on-screen points build in narration order about every 6s, each animating its element; hold and highlight the key idea in the final 5s. |
| 5 | 2:28 | 32 | Now the United States. There's no single federal privacy law. Instead, around twenty states have comprehensive laws, including California, Virginia, Colorado, Connecticut, Texas and Oregon, with Indiana, Kentucky and Rhode Island taking effect in January twenty twenty-six. Most use an opt-out model for selling data and targeted advertising. Several require you to honor Global Privacy Control, a browser signal that says don't sell or share my data. Sensitive data often needs opt-in consent. | United States<br>• ~20 state laws<br>• Opt-out for sale / targeted ads<br>• Honor Global Privacy Control<br>• Opt-in for sensitive data | Layout: A US map where states light up in a patchwork; a browser icon emits a small 'GPC' signal wave. Timing (32s): title card 0–3s; then the 4 on-screen points build in narration order about every 6s, each animating its element; hold and highlight the key idea in the final 5s. |
| 6 | 3:00 | 44 | Let's do a simple worked example. A Lahore-based online course seller runs ads to students in the UK, Saudi Arabia and the US. For UK visitors, the advertising pixel must wait for consent. Analytics might qualify for the new UK statistical exemption, but only after checking the conditions with ICO guidance. For Saudi visitors, they collect consent for marketing tags and check where the data goes, because transfers outside the Kingdom need safeguards. For US visitors, they add a privacy choices link and honor Global Privacy Control. Same website, three rule sets, all driven by the visitor's region in the consent platform. | Simple example: Lahore course seller (illustrative)<br>• UK: consent for ads, analytics exemption check<br>• KSA: consent + transfer safeguards<br>• US: choices link + GPC<br>• One site, three rule sets | Layout: website mock in the center; three arrows to region cards (UK, KSA, US), each card listing its rules with icons animating in. |
| 7 | 3:44 | 28 | US enforcement has also focused on pixels leaking sensitive data. The FTC and state attorneys general have acted against companies whose tracking sent health or financial information to ad platforms, sometimes simply through page URLs like a condition name in the path. So if you're in health, finance or anything involving children, audit exactly what each tag sends. A URL can be personal data. | Sensitive data leaks<br>• Health, finance, children<br>• URLs can leak conditions<br>• Audit exactly what tags send | Layout: A URL bar showing a path like /conditions/diabetes/book highlighted in red, with an arrow flowing toward a generic ad platform icon and a stop sign. Timing (28s): title card 0–3s; then the 3 on-screen points build in narration order about every 6s, each animating its element; hold and highlight the key idea in the final 5s. |
| 8 | 4:12 | 29 | Saudi Arabia's Personal Data Protection Law, overseen by SDAIA, has been enforceable since September twenty twenty-four. For marketers, the key points are a clear legal basis, with consent central for marketing, proper privacy notices, individuals' rights, and restrictions on transferring data outside the Kingdom. The updated Transfer Regulation allows transfers based on adequacy or appropriate safeguards like standard contractual clauses. SDAIA's committees have already issued violation decisions. | Saudi Arabia: PDPL<br>• Regulator: SDAIA<br>• Enforceable since Sep 2024<br>• Legal basis, notices, rights<br>• Transfer Regulation: safeguards | Layout: A map of Saudi Arabia with a data file icon at a border gate labeled 'transfer safeguards' and a checklist beside it. Timing (29s): title card 0–3s; then the 4 on-screen points build in narration order about every 5s, each animating its element; hold and highlight the key idea in the final 5s. |
| 9 | 4:41 | 42 | In the UAE, Federal Decree-Law number forty-five of twenty twenty-one, the PDPL, sets out consent and other lawful bases, rights, and transfer rules, overseen by the UAE Data Office. Check the current status of its executive regulations before relying on specific details. The financial free zones have their own laws: the DIFC Data Protection Law and ADGM's Data Protection Regulations, both closely modeled on GDPR. And in Pakistan, a data protection bill has been under discussion for years, so check its status. Remember: if you target Europeans or Saudis from Pakistan, their laws apply to you. | UAE and Pakistan<br>• UAE PDPL (Decree-Law 45/2021)<br>• DIFC and ADGM regimes<br>• Pakistan: check bill status<br>• Target a region → its law applies | Layout: A UAE map with two small zones highlighted for DIFC and ADGM; an arrow from Pakistan to Europe and the Gulf labeled 'their rules apply'. Timing (42s): title card 0–3s; then the 4 on-screen points build in narration order about every 8s, each animating its element; hold and highlight the key idea in the final 5s. |
| 10 | 5:23 | 34 | Here's an illustrative example. A Riyadh marketplace advertises in Saudi Arabia, the UAE and the UK. They mapped every tag to a purpose, a lawful basis and a vendor location. Their consent platform behaves differently by region: opt-in for UK and EU visitors, clear consent collection in Saudi Arabia and the UAE, and Global Privacy Control honored for US visitors. Their server-side events forward only the fields each purpose needs, and transfers outside the Kingdom rely on documented safeguards. | Example: Riyadh marketplace (illustrative)<br>• Tag → purpose → basis → location<br>• Region-specific consent behavior<br>• Minimal fields per purpose<br>• Documented transfer safeguards | Layout: A routing diagram: visitors from four regions pass through a consent platform that branches into different rule paths. Timing (34s): title card 0–3s; then the 4 on-screen points build in narration order about every 6s, each animating its element; hold and highlight the key idea in the final 5s. |
| 11 | 5:57 | 31 | The practical tool that ties this together is a tag-to-law register. It's a simple spreadsheet. For every tag: the vendor, the purpose, the data sent, the cookies or device access, the lawful basis per region, whether US opt-outs apply, the vendor's location, the transfer mechanism, and retention. The lesson has a CSV template. Get it signed off by legal, and then verify in testing that what actually fires matches the register. | Tag-to-law register<br>• Vendor, purpose, data<br>• Cookies / device access<br>• Basis per region, opt-outs<br>• Location, transfers, retention | Layout: Spreadsheet columns scroll horizontally with the headings from the lesson; a 'legal sign-off' stamp lands on it. Timing (31s): title card 0–3s; then the 4 on-screen points build in narration order about every 5s, each animating its element; hold and highlight the key idea in the final 5s. |
| 12 | 6:28 | 42 | Common legal mistakes for marketers. Copying a US-style opt-out banner onto EU and UK traffic. Assuming the law of your own country is the only one that matters. Sending health or financial details to ad platforms through events or page paths. Forgetting cross-border transfer rules for Gulf data. And treating the consent platform as compliance done, without checking what actually fires. Try this now: list every tag on your site and, next to each, write the purpose and the vendor's location. Then circle any tag where you can't say what lawful basis applies in your main markets. | Mistakes + try this now<br>• One banner for all regions<br>• Home-country-only thinking<br>• Sensitive data leaks<br>• Ignoring transfers<br>• 'CMP = compliant'<br>• Try: tag, purpose, location list | Layout: a three-column table (tag, purpose, vendor location) fills in; rows with unknown lawful basis get a red circle drawn around them. |
| 13 | 7:10 | 80 | Watch me do it. Let's fill in the tag-to-law register for an illustrative Jeddah online florist selling in Saudi Arabia, the UAE and the UK. Row one, GA4: vendor Google, purpose analytics, data sent includes page URL, events and a client ID, cookie underscore g a. Lawful basis: consent for UK visitors unless legal confirms the new statistical exemption applies; consent-based for Saudi and UAE visitors under our policy. Vendor location includes the United States, so for Saudi personal data I note that the transfer relies on appropriate safeguards and must be documented. Retention fourteen months. Row two, Meta Pixel plus Conversions API: advertising purpose, hashed email and phone on purchase, consent required in all three markets under our policy, same transfer note. Row three, WhatsApp order updates via a messaging provider: purpose service messages, lawful basis contract performance, not advertising, so it must never feed ad audiences without separate consent. Row four, a heatmap tool: we discover nobody uses it anymore, so we remove it. That last row is common. Registers aren't only about compliance; they often delete tools that add risk without value. | Watch me do it: tag-to-law register (illustrative)<br>• GA4: consent / UK exemption check<br>• Meta: consent + transfer safeguards<br>• WhatsApp: contract, never ads<br>• Heatmap tool: unused → removed | Layout: register spreadsheet centered with frozen headers. Timing: four rows fill in over ~25s each; transfer notes flash amber on rows one and two; row four gets a strikethrough and 'removed' tag in the final 10s. |
| 14 | 8:30 | 29 | Recap. In Europe and the UK, device access needs consent and data protection applies on top, with a new UK analytics exemption to check. The US is an opt-out patchwork where Global Privacy Control matters. Saudi Arabia and the UAE have real laws with transfer rules, and the free zones have their own. Your next step: build your tag-to-law register, and then test that reality matches it. | Recap and next step<br>• EU/UK: two layers<br>• US: opt-out + GPC<br>• KSA/UAE: consent + transfers<br>• Build your register | Layout: The world map returns with each region summarized by a single icon; a register document slides in. Timing (29s): title card 0–3s; then the 4 on-screen points build in narration order about every 5s, each animating its element; hold and highlight the key idea in the final 5s. |

<details><summary>Narration per scene (paste into ElevenLabs)</summary>

Scene 1 — Privacy laws for marketers

```text
Every tag you add to a website is a legal decision, whether you realize it or not. Which data goes to which company, where, for how long, and whether the user agreed. In this lecture you'll get a practical map of the privacy laws that shape marketing measurement across Europe, the UK, the United States, Saudi Arabia and the UAE. It's not legal advice, and laws change, so always confirm current guidance, but it will help you ask the right questions and build the right setup.
```

Scene 2 — Why it matters

```text
Why does this matter? Because the cost of getting it wrong isn't just a fine. It's having to switch off tracking in a hurry, losing your data history, and explaining to customers why their data went somewhere it shouldn't. Here's an analogy. Think of privacy laws like traffic laws in different countries. The basic idea is the same everywhere, don't hurt people, but the details differ: which side you drive on, speed limits, whether you can turn on red. A marketer running ads across the UK, the Gulf and the US is a driver crossing borders, and needs to know the local rules before the first mile.
```

Scene 3 — EU and UK: two layers

```text
Start with Europe and the UK, where there are two layers. Layer one is about the device. The EU's ePrivacy rules and the UK's PECR say you need consent to store or access information on someone's device, unless it's strictly necessary for a service they asked for. That covers cookies, local storage, pixels and fingerprinting. Layer two is data protection, the GDPR and UK GDPR: lawful basis, transparency, minimization, retention, security, transfers and rights.
```

Scene 4 — UK: Data (Use and Access) Act 2025

```text
There's an important UK update. The Data Use and Access Act twenty twenty-five added exemptions from cookie consent for some low-risk purposes, including analytics used only to produce statistics that improve your service, provided people get clear information and an easy way to object. Its provisions have been coming into force in stages through twenty twenty-six. Advertising and cross-site tracking still need consent. Check the ICO's current guidance before you rely on the exemption.
```

Scene 5 — United States

```text
Now the United States. There's no single federal privacy law. Instead, around twenty states have comprehensive laws, including California, Virginia, Colorado, Connecticut, Texas and Oregon, with Indiana, Kentucky and Rhode Island taking effect in January twenty twenty-six. Most use an opt-out model for selling data and targeted advertising. Several require you to honor Global Privacy Control, a browser signal that says don't sell or share my data. Sensitive data often needs opt-in consent.
```

Scene 6 — Simple example: Lahore course seller (illustrative)

```text
Let's do a simple worked example. A Lahore-based online course seller runs ads to students in the UK, Saudi Arabia and the US. For UK visitors, the advertising pixel must wait for consent. Analytics might qualify for the new UK statistical exemption, but only after checking the conditions with ICO guidance. For Saudi visitors, they collect consent for marketing tags and check where the data goes, because transfers outside the Kingdom need safeguards. For US visitors, they add a privacy choices link and honor Global Privacy Control. Same website, three rule sets, all driven by the visitor's region in the consent platform.
```

Scene 7 — Sensitive data leaks

```text
US enforcement has also focused on pixels leaking sensitive data. The FTC and state attorneys general have acted against companies whose tracking sent health or financial information to ad platforms, sometimes simply through page URLs like a condition name in the path. So if you're in health, finance or anything involving children, audit exactly what each tag sends. A URL can be personal data.
```

Scene 8 — Saudi Arabia: PDPL

```text
Saudi Arabia's Personal Data Protection Law, overseen by SDAIA, has been enforceable since September twenty twenty-four. For marketers, the key points are a clear legal basis, with consent central for marketing, proper privacy notices, individuals' rights, and restrictions on transferring data outside the Kingdom. The updated Transfer Regulation allows transfers based on adequacy or appropriate safeguards like standard contractual clauses. SDAIA's committees have already issued violation decisions.
```

Scene 9 — UAE and Pakistan

```text
In the UAE, Federal Decree-Law number forty-five of twenty twenty-one, the PDPL, sets out consent and other lawful bases, rights, and transfer rules, overseen by the UAE Data Office. Check the current status of its executive regulations before relying on specific details. The financial free zones have their own laws: the DIFC Data Protection Law and ADGM's Data Protection Regulations, both closely modeled on GDPR. And in Pakistan, a data protection bill has been under discussion for years, so check its status. Remember: if you target Europeans or Saudis from Pakistan, their laws apply to you.
```

Scene 10 — Example: Riyadh marketplace (illustrative)

```text
Here's an illustrative example. A Riyadh marketplace advertises in Saudi Arabia, the UAE and the UK. They mapped every tag to a purpose, a lawful basis and a vendor location. Their consent platform behaves differently by region: opt-in for UK and EU visitors, clear consent collection in Saudi Arabia and the UAE, and Global Privacy Control honored for US visitors. Their server-side events forward only the fields each purpose needs, and transfers outside the Kingdom rely on documented safeguards.
```

Scene 11 — Tag-to-law register

```text
The practical tool that ties this together is a tag-to-law register. It's a simple spreadsheet. For every tag: the vendor, the purpose, the data sent, the cookies or device access, the lawful basis per region, whether US opt-outs apply, the vendor's location, the transfer mechanism, and retention. The lesson has a CSV template. Get it signed off by legal, and then verify in testing that what actually fires matches the register.
```

Scene 12 — Mistakes + try this now

```text
Common legal mistakes for marketers. Copying a US-style opt-out banner onto EU and UK traffic. Assuming the law of your own country is the only one that matters. Sending health or financial details to ad platforms through events or page paths. Forgetting cross-border transfer rules for Gulf data. And treating the consent platform as compliance done, without checking what actually fires. Try this now: list every tag on your site and, next to each, write the purpose and the vendor's location. Then circle any tag where you can't say what lawful basis applies in your main markets.
```

Scene 13 — Watch me do it: tag-to-law register (illustrative)

```text
Watch me do it. Let's fill in the tag-to-law register for an illustrative Jeddah online florist selling in Saudi Arabia, the UAE and the UK. Row one, GA4: vendor Google, purpose analytics, data sent includes page URL, events and a client ID, cookie underscore g a. Lawful basis: consent for UK visitors unless legal confirms the new statistical exemption applies; consent-based for Saudi and UAE visitors under our policy. Vendor location includes the United States, so for Saudi personal data I note that the transfer relies on appropriate safeguards and must be documented. Retention fourteen months. Row two, Meta Pixel plus Conversions API: advertising purpose, hashed email and phone on purchase, consent required in all three markets under our policy, same transfer note. Row three, WhatsApp order updates via a messaging provider: purpose service messages, lawful basis contract performance, not advertising, so it must never feed ad audiences without separate consent. Row four, a heatmap tool: we discover nobody uses it anymore, so we remove it. That last row is common. Registers aren't only about compliance; they often delete tools that add risk without value.
```

Scene 14 — Recap and next step

```text
Recap. In Europe and the UK, device access needs consent and data protection applies on top, with a new UK analytics exemption to check. The US is an opt-out patchwork where Global Privacy Control matters. Saudi Arabia and the UAE have real laws with transfer rules, and the free zones have their own. Your next step: build your tag-to-law register, and then test that reality matches it.
```

</details>

### 3. Consent management: CMPs, IAB TCF 2.3 and GPP

Lesson `consent-management-cmps-and-tcf` · module “Consent management and Google Consent Mode” · target 9 min · 14 scenes · 1193 words · planned 8:39 (words ≈ 8:39) · status: **to produce**

| Term | Say |
|---|---|
| CMP | C M P |
| TCF | T C F |
| GPP | G P P |
| IAB | I A B |
| CNIL | C N I L |

| # | Time | Sec | Narration | On screen | Visual |
|---|---|---|---|---|---|
| 1 | 0:00 | 34 | Your consent banner is the most viewed piece of content on your website, and probably the least designed. It decides what you can measure, what you can show in ads, and whether a regulator ever knocks on your door. In this lecture you'll learn what a consent management platform must do, where IAB's TCF and GPP standards fit, how to design a banner that's lawful and still effective, and how to check consent state in the browser yourself. | Consent management<br>• What a CMP must do<br>• IAB TCF 2.3 and GPP<br>• Lawful, effective design<br>• Checking consent state | Layout: A website loads with a consent banner sliding up; a spotlight focuses on it as the most important element on the page. Timing (34s): title card 0–3s; then the 4 on-screen points build in narration order about every 6s, each animating its element; hold and highlight the key idea in the final 5s. |
| 2 | 0:34 | 41 | Why does this matter? Because consent is the gate every other part of your measurement passes through. Here's an analogy. A consent platform is like the reception desk in an office building. It asks visitors who they're here to see, gives them the right badge, and every door in the building checks that badge. If reception hands out the wrong badges, or the doors don't check them, it doesn't matter how good your security policy looks on paper. Your tags are the doors. The consent state is the badge. And the CMP is reception. | Why it matters<br>• CMP = reception desk<br>• Consent state = badge<br>• Tags = doors that must check it | Layout: building cross-section with a reception desk at the entrance; visitor icons receive colored badges; doors labeled GA4, Meta, TikTok open or stay shut depending on badge color. |
| 3 | 1:15 | 31 | A consent management platform, or CMP, does six jobs. It presents clear choices. It blocks non-essential tags until consent, or passes consent state to tags that can adapt, like Google's consent mode. It stores proof of consent: what the user saw, when, and what they chose. It exposes that state through APIs. It applies different rules by region. And it lets people change their mind easily, with a persistent link or icon. | Six jobs of a CMP<br>• Present choices<br>• Block or adapt tags<br>• Store proof<br>• Expose APIs<br>• Regional rules<br>• Easy to change mind | Layout: Six icons arranged in a hexagon around a CMP shield icon: a dialog, a padlock, a receipt, a plug, a globe, and a circular arrow. Timing (31s): title card 0–3s; then the 6 on-screen points build in narration order about every 3s, each animating its element; hold and highlight the key idea in the final 5s. |
| 4 | 1:46 | 33 | Now the standards. The IAB Transparency and Consent Framework, TCF, is how consent travels across the advertising supply chain in Europe, packed into something called a TC string. The current version is two point three. IAB Europe set a transition deadline of the end of February twenty twenty-six, and the key change is proving which vendors were actually disclosed to the user. If your CMP supports TCF, make sure it's on version two point three. | IAB TCF<br>• Consent across the ad supply chain<br>• TC string<br>• Current: v2.3<br>• Transition deadline 28 Feb 2026 | Layout: A consent string token travels along a chain of generic vendor boxes, each checking it; a version badge reads '2.3'. Timing (33s): title card 0–3s; then the 4 on-screen points build in narration order about every 6s, each animating its element; hold and highlight the key idea in the final 5s. |
| 5 | 2:19 | 29 | Do you need TCF? If you're a publisher earning from programmatic ads, usually yes. If you're an advertiser measuring your own site with Google, Meta and TikTok, it's optional. Many advertisers use a non-TCF CMP together with Google consent mode and tag-level blocking. For the United States, IAB Tech Lab's Global Privacy Platform, GPP, carries state-by-state signals in one string, and ad tech vendors increasingly expect it. | Do you need it?<br>• Publishers: usually TCF<br>• Advertisers: optional<br>• US: GPP for state signals | Layout: Decision tree: 'Do you sell ad inventory?' yes leads to TCF; no leads to 'CMP + consent mode'; a US branch leads to GPP. Timing (29s): title card 0–3s; then the 3 on-screen points build in narration order about every 7s, each animating its element; hold and highlight the key idea in the final 5s. |
| 6 | 2:48 | 45 | Here's a simple worked example. A Manchester gift shop uses a CMP with three first-layer buttons: accept all, reject all, and manage choices. A visitor clicks manage, turns on analytics, and leaves advertising off. What should happen? GA4 runs, according to their Consent Mode setup. The Meta and TikTok pixels stay silent. The server container doesn't forward anything to ad platforms for that visitor. And the CMP stores a record of what the visitor saw and chose, with a timestamp. If a month later the visitor clicks the privacy link in the footer and turns advertising on, the tags start working from that moment. | Simple example: Manchester gift shop (illustrative)<br>• Visitor: analytics on, ads off<br>• GA4 runs; Meta/TikTok silent<br>• Server forwards nothing to ads<br>• Choice recorded, changeable later | Layout: banner with three buttons; cursor clicks 'manage'; two toggles set analytics on, ads off. Then a row of tag icons: GA4 lights green, Meta and TikTok stay gray; a receipt icon prints 'consent record'. |
| 7 | 3:33 | 33 | Let's talk design, because regulators across Europe and the UK have repeatedly acted against dark patterns. Where consent is required, make rejecting as easy as accepting on the first layer. No pre-ticked boxes. No hiding advertising cookies behind legitimate interest. Explain purposes in plain language and name key vendors. Don't block the whole site unless you offer a fair alternative the regulator accepts. And localize: Arabic and English for Gulf sites, Urdu where it helps Pakistani audiences. | Lawful design<br>• Reject as easy as Accept<br>• No pre-ticked boxes<br>• Plain-language purposes<br>• No unfair cookie walls<br>• Localize | Layout: Two banner mockups side by side: one with a big Accept and a tiny gray link, crossed out; one with equal buttons, ticked. Timing (33s): title card 0–3s; then the 5 on-screen points build in narration order about every 5s, each animating its element; hold and highlight the key idea in the final 5s. |
| 8 | 4:06 | 28 | Can you improve consent rates without tricks? Yes. Be honest about the value exchange, for example, we use this to show you relevant offers and keep the site free of irrelevant ads. Keep the banner short and readable on mobile. Load the CMP early and fast, because a slow banner delays everything else. And don't nag people who said no. Test wording, not deception. | Better consent rates, honestly<br>• Explain the value exchange<br>• Short and mobile-friendly<br>• Fast loading<br>• No nagging | Layout: A mobile phone showing a concise banner; a stopwatch shows fast load; a simple A/B label compares two honest wordings. Timing (28s): title card 0–3s; then the 4 on-screen points build in narration order about every 5s, each animating its element; hold and highlight the key idea in the final 5s. |
| 9 | 4:34 | 33 | Here's an illustrative setup for a UK retailer with EU and Gulf visitors. UK and EU visitors get an opt-in banner with Accept, Reject and Manage on the first layer. Advertising tags wait for consent. For UK analytics, they only use the new statistical exemption after legal review, with an easy objection link. Gulf visitors get an Arabic and English banner. US visitors get a privacy choices link, Global Privacy Control honored, and a GPP string generated. | Example: UK retailer (illustrative)<br>• UK/EU: opt-in, equal choices<br>• UK analytics: exemption after review<br>• Gulf: Arabic/English banner<br>• US: choices link, GPC, GPP | Layout: World map with three differently designed banner mockups pinned to the UK/EU, the Gulf and the US. Timing (33s): title card 0–3s; then the 4 on-screen points build in narration order about every 6s, each animating its element; hold and highlight the key idea in the final 5s. |
| 10 | 5:07 | 31 | You can check consent state yourself in the browser console, and the lesson has the code. If your CMP supports TCF, call the TCF API and read the TC string, whether purpose one, storing and accessing information on the device, was consented, and whether a specific vendor has consent. Check the navigator's global privacy control flag. And look for consent commands in the data layer to see what Google's tags received. | Check it yourself<br>• __tcfapi: TC string, purposes, vendors<br>• navigator.globalPrivacyControl<br>• dataLayer consent commands | Layout: Browser console mock with the JavaScript snippet running and outputs printed: a TC string, true/false flags. Timing (31s): title card 0–3s; then the 3 on-screen points build in narration order about every 7s, each animating its element; hold and highlight the key idea in the final 5s. |
| 11 | 5:38 | 27 | Common failures. A beautiful banner that doesn't actually block anything. Consent that's stored but never passed to server-side events. One global banner for every region. And forgetting to ask again when you add new vendors or purposes. Measure success with consent rates by region and device, zero pre-consent firing where consent is required, and proof-of-consent records you can retrieve on request. | Common failures<br>• Banner doesn't block<br>• Consent not passed server-side<br>• One banner for all regions<br>• No re-prompt after changes | Layout: Four broken-link icons that repair themselves one by one. Timing (27s): title card 0–3s; then the 4 on-screen points build in narration order about every 4s, each animating its element; hold and highlight the key idea in the final 5s. |
| 12 | 6:05 | 40 | Common consent mistakes. A banner that looks right but doesn't block anything. Consent that's recorded but never passed to server-side events. A reject option hidden on the second layer where regulators expect it on the first. No Arabic version for Gulf visitors. And forgetting to ask again after adding new vendors. Try this now: open your site in two fresh browser profiles. In one, accept all. In the other, reject all. Compare the cookies and network requests in each. If they look almost the same, your banner isn't doing its job. | Mistakes + try this now<br>• Banner that blocks nothing<br>• Consent not passed server-side<br>• Hidden reject<br>• No Arabic version<br>• No re-prompt<br>• Try: accept vs reject comparison | Layout: split screen of two browser windows labeled 'accept' and 'reject'; cookie lists populate on each side; a diff highlight shows where they should differ. |
| 13 | 6:45 | 84 | Watch me do it. Let's configure an illustrative CMP for a Karachi fashion brand that ships to the UK, the UAE and the US. Step one, geolocation rules: UK and EU visitors get opt-in with accept all, reject all and manage choices on the first layer. Gulf visitors get an Arabic and English banner with marketing consent. US visitors get a your privacy choices link, and Global Privacy Control is honored automatically. Step two, purposes: necessary, analytics, advertising and personalization, each described in one plain sentence. Step three, vendors: Google, Meta, TikTok and the email platform, each mapped to a purpose. Step four, Google Consent Mode integration: the CMP's template sets defaults and updates. Step five, tag blocking: in Tag Manager, Meta and TikTok tags require advertising consent. Step six, proof: consent records exported monthly. Step seven, testing: I open the site through a UK VPN location, reject all, and confirm no advertising cookies; then a Dubai location, check the Arabic layout reads right to left properly; then a US browser with GPC enabled. Only after all three pass do we publish. TCF isn't needed here because the brand doesn't sell ad inventory. | Watch me do it: CMP setup (illustrative)<br>• Region rules: UK/EU, Gulf, US<br>• Purposes + vendors mapped<br>• Consent Mode template<br>• GTM consent checks<br>• Test 3 regions before publishing | Layout: CMP admin mock on the left, live site preview on the right. Timing: each of seven steps highlights its admin panel ~15s; from ~95s the preview switches between UK, Arabic RTL and US versions with green checks; final 5s show 'published'. |
| 14 | 8:09 | 30 | Recap. A CMP presents choices, controls tags, stores proof and exposes consent to everything downstream. TCF version two point three is the current European standard, mostly for publishers, and GPP covers US states. Design without dark patterns. Your next step: audit your banner. Record the first-layer choices, whether reject is as easy as accept, which tags fire before and after each choice, and whether Global Privacy Control is honored. | Recap and next step<br>• CMP: choices, control, proof, APIs<br>• TCF 2.3, GPP<br>• No dark patterns<br>• Audit your banner | Layout: Audit checklist with four items appears beside a banner mockup. Timing (30s): title card 0–3s; then the 4 on-screen points build in narration order about every 5s, each animating its element; hold and highlight the key idea in the final 5s. |

<details><summary>Narration per scene (paste into ElevenLabs)</summary>

Scene 1 — Consent management

```text
Your consent banner is the most viewed piece of content on your website, and probably the least designed. It decides what you can measure, what you can show in ads, and whether a regulator ever knocks on your door. In this lecture you'll learn what a consent management platform must do, where IAB's TCF and GPP standards fit, how to design a banner that's lawful and still effective, and how to check consent state in the browser yourself.
```

Scene 2 — Why it matters

```text
Why does this matter? Because consent is the gate every other part of your measurement passes through. Here's an analogy. A consent platform is like the reception desk in an office building. It asks visitors who they're here to see, gives them the right badge, and every door in the building checks that badge. If reception hands out the wrong badges, or the doors don't check them, it doesn't matter how good your security policy looks on paper. Your tags are the doors. The consent state is the badge. And the CMP is reception.
```

Scene 3 — Six jobs of a CMP

```text
A consent management platform, or CMP, does six jobs. It presents clear choices. It blocks non-essential tags until consent, or passes consent state to tags that can adapt, like Google's consent mode. It stores proof of consent: what the user saw, when, and what they chose. It exposes that state through APIs. It applies different rules by region. And it lets people change their mind easily, with a persistent link or icon.
```

Scene 4 — IAB TCF

```text
Now the standards. The IAB Transparency and Consent Framework, TCF, is how consent travels across the advertising supply chain in Europe, packed into something called a TC string. The current version is two point three. IAB Europe set a transition deadline of the end of February twenty twenty-six, and the key change is proving which vendors were actually disclosed to the user. If your CMP supports TCF, make sure it's on version two point three.
```

Scene 5 — Do you need it?

```text
Do you need TCF? If you're a publisher earning from programmatic ads, usually yes. If you're an advertiser measuring your own site with Google, Meta and TikTok, it's optional. Many advertisers use a non-TCF CMP together with Google consent mode and tag-level blocking. For the United States, IAB Tech Lab's Global Privacy Platform, GPP, carries state-by-state signals in one string, and ad tech vendors increasingly expect it.
```

Scene 6 — Simple example: Manchester gift shop (illustrative)

```text
Here's a simple worked example. A Manchester gift shop uses a CMP with three first-layer buttons: accept all, reject all, and manage choices. A visitor clicks manage, turns on analytics, and leaves advertising off. What should happen? GA4 runs, according to their Consent Mode setup. The Meta and TikTok pixels stay silent. The server container doesn't forward anything to ad platforms for that visitor. And the CMP stores a record of what the visitor saw and chose, with a timestamp. If a month later the visitor clicks the privacy link in the footer and turns advertising on, the tags start working from that moment.
```

Scene 7 — Lawful design

```text
Let's talk design, because regulators across Europe and the UK have repeatedly acted against dark patterns. Where consent is required, make rejecting as easy as accepting on the first layer. No pre-ticked boxes. No hiding advertising cookies behind legitimate interest. Explain purposes in plain language and name key vendors. Don't block the whole site unless you offer a fair alternative the regulator accepts. And localize: Arabic and English for Gulf sites, Urdu where it helps Pakistani audiences.
```

Scene 8 — Better consent rates, honestly

```text
Can you improve consent rates without tricks? Yes. Be honest about the value exchange, for example, we use this to show you relevant offers and keep the site free of irrelevant ads. Keep the banner short and readable on mobile. Load the CMP early and fast, because a slow banner delays everything else. And don't nag people who said no. Test wording, not deception.
```

Scene 9 — Example: UK retailer (illustrative)

```text
Here's an illustrative setup for a UK retailer with EU and Gulf visitors. UK and EU visitors get an opt-in banner with Accept, Reject and Manage on the first layer. Advertising tags wait for consent. For UK analytics, they only use the new statistical exemption after legal review, with an easy objection link. Gulf visitors get an Arabic and English banner. US visitors get a privacy choices link, Global Privacy Control honored, and a GPP string generated.
```

Scene 10 — Check it yourself

```text
You can check consent state yourself in the browser console, and the lesson has the code. If your CMP supports TCF, call the TCF API and read the TC string, whether purpose one, storing and accessing information on the device, was consented, and whether a specific vendor has consent. Check the navigator's global privacy control flag. And look for consent commands in the data layer to see what Google's tags received.
```

Scene 11 — Common failures

```text
Common failures. A beautiful banner that doesn't actually block anything. Consent that's stored but never passed to server-side events. One global banner for every region. And forgetting to ask again when you add new vendors or purposes. Measure success with consent rates by region and device, zero pre-consent firing where consent is required, and proof-of-consent records you can retrieve on request.
```

Scene 12 — Mistakes + try this now

```text
Common consent mistakes. A banner that looks right but doesn't block anything. Consent that's recorded but never passed to server-side events. A reject option hidden on the second layer where regulators expect it on the first. No Arabic version for Gulf visitors. And forgetting to ask again after adding new vendors. Try this now: open your site in two fresh browser profiles. In one, accept all. In the other, reject all. Compare the cookies and network requests in each. If they look almost the same, your banner isn't doing its job.
```

Scene 13 — Watch me do it: CMP setup (illustrative)

```text
Watch me do it. Let's configure an illustrative CMP for a Karachi fashion brand that ships to the UK, the UAE and the US. Step one, geolocation rules: UK and EU visitors get opt-in with accept all, reject all and manage choices on the first layer. Gulf visitors get an Arabic and English banner with marketing consent. US visitors get a your privacy choices link, and Global Privacy Control is honored automatically. Step two, purposes: necessary, analytics, advertising and personalization, each described in one plain sentence. Step three, vendors: Google, Meta, TikTok and the email platform, each mapped to a purpose. Step four, Google Consent Mode integration: the CMP's template sets defaults and updates. Step five, tag blocking: in Tag Manager, Meta and TikTok tags require advertising consent. Step six, proof: consent records exported monthly. Step seven, testing: I open the site through a UK VPN location, reject all, and confirm no advertising cookies; then a Dubai location, check the Arabic layout reads right to left properly; then a US browser with GPC enabled. Only after all three pass do we publish. TCF isn't needed here because the brand doesn't sell ad inventory.
```

Scene 14 — Recap and next step

```text
Recap. A CMP presents choices, controls tags, stores proof and exposes consent to everything downstream. TCF version two point three is the current European standard, mostly for publishers, and GPP covers US states. Design without dark patterns. Your next step: audit your banner. Record the first-layer choices, whether reject is as easy as accept, which tags fire before and after each choice, and whether Global Privacy Control is honored.
```

</details>

### 4. Google Consent Mode v2: basic vs advanced, done right

Lesson `google-consent-mode-v2` · module “Consent management and Google Consent Mode” · target 8 min · 14 scenes · 1169 words · planned 8:27 (words ≈ 8:28) · status: **to produce**

| Term | Say |
|---|---|
| gtag | G tag |
| GTM | G T M |
| EEA | E E A |
| gcs | G C S |
| gcd | G C D |

| # | Time | Sec | Narration | On screen | Visual |
|---|---|---|---|---|---|
| 1 | 0:00 | 30 | If you advertise to Europe or the UK with Google and your audiences have quietly stopped growing, or your conversion modeling looks thin, the cause is often one missing detail in Consent Mode. In this lecture you'll learn what Consent Mode version two actually does, the difference between basic and advanced implementations, how to set it up with code or Tag Manager, and three ways to verify it's working. | Google Consent Mode v2<br>• What it does<br>• Basic vs advanced<br>• Implementation<br>• Verification | Layout: A Google-style tag icon (generic) with a dial that switches between 'granted' and 'denied', causing different data flows. Timing (30s): title card 0–3s; then the 4 on-screen points build in narration order about every 5s, each animating its element; hold and highlight the key idea in the final 5s. |
| 2 | 0:30 | 43 | Why does this matter? Because Consent Mode is the difference between Google's tools working properly under consent rules and silently degrading. Here's an analogy. Think of a hotel with a do not disturb sign. The guest decides, the sign communicates the decision, and housekeeping adapts. Your CMP is the guest's decision. Consent Mode is the sign hanging on the door. Google's tags are housekeeping. Without the sign, housekeeping either barges in, which is a compliance problem, or stays away from every room, which is a measurement problem. Consent Mode lets them do the right thing for each room. | Why it matters<br>• CMP = guest's decision<br>• Consent Mode = the door sign<br>• Google tags = housekeeping that adapts | Layout: hotel corridor with doors; some doors show 'do not disturb' signs; a housekeeping cart icon moves along, entering only rooms without signs. |
| 3 | 1:13 | 28 | First, what it isn't. Consent Mode is not a consent banner. Your consent platform collects choices. Consent Mode passes those choices to Google's tags, like Analytics, Google Ads and Floodlight, so they change their behavior. Version two added two parameters to the original storage settings: ad user data, which covers sending user data to Google for advertising, and ad personalization, which covers things like remarketing. | What it is<br>• CMP collects consent<br>• Consent Mode passes it to Google tags<br>• v2 adds ad_user_data, ad_personalization | Layout: Diagram: banner icon hands a consent card to a messenger labeled 'Consent Mode', who delivers it to three tag icons. Timing (28s): title card 0–3s; then the 3 on-screen points build in narration order about every 6s, each animating its element; hold and highlight the key idea in the final 5s. |
| 4 | 1:41 | 27 | Why does v2 matter? For traffic from the European Economic Area and the UK, Google requires these consent signals before it uses data for certain ads features, including building audiences and some measurement. Without them, remarketing lists stop growing and some features degrade. So if you only implemented the original two parameters years ago, you're probably missing out, even if users are consenting. | Why v2 matters<br>• Required for EEA/UK ads features<br>• Audiences stop growing without it<br>• Old setups miss v2 signals | Layout: A remarketing audience bar chart flattening over time, then growing again after a 'v2 signals' switch. Timing (27s): title card 0–3s; then the 3 on-screen points build in narration order about every 6s, each animating its element; hold and highlight the key idea in the final 5s. |
| 5 | 2:08 | 35 | Now the big choice: basic or advanced. In basic mode, Google tags don't load at all until the user chooses. If they say no, nothing goes to Google. In advanced mode, tags load immediately with a default of denied. If the user says no, the tags send cookieless pings, which don't read or write cookies, and Google uses them for more precise, advertiser-specific modeling. Advanced gives better data. Basic is more conservative. It's a legal and business decision, so document it. | Basic vs advanced<br>• Basic: tags wait, nothing sent if denied<br>• Advanced: cookieless pings if denied<br>• Advanced = better modeling<br>• Document the decision | Layout: Two lanes: in the basic lane, a tag sits behind a closed gate; in the advanced lane, a tag sends small gray 'no cookie' pings through a filter. Timing (35s): title card 0–3s; then the 4 on-screen points build in narration order about every 6s, each animating its element; hold and highlight the key idea in the final 5s. |
| 6 | 2:43 | 49 | Here's a simple worked example of the event sequence. A visitor from Germany lands on your store. First, the consent default command runs: ad storage, analytics storage, ad user data and ad personalization all denied, with a five hundred millisecond wait for update. Then the Google tag loads. In advanced mode it sends a cookieless ping. The visitor clicks accept all on the banner. The CMP fires a consent update with all four set to granted. Now the Google tag sets its cookies and sends full measurement. In Tag Assistant, you should see exactly that order: default, then update. If you see the tag fire before the default, you've found a race condition. | Simple example: the event sequence<br>1 Default: all denied, wait 500 ms<br>2 Google tag loads (cookieless ping)<br>3 User accepts<br>4 Update: all granted<br>5 Cookies + full measurement | Layout: vertical timeline with five numbered steps; each step animates in with a small icon; a warning callout shows the incorrect order 'tag before default' crossed out. |
| 7 | 3:32 | 33 | Implementation has one golden rule: set the default before any Google tag loads. With gtag, you push a consent default command with all four key parameters, typically denied for EEA and UK regions, with a short wait for update so the banner can respond. You can set different defaults for other regions based on your legal assessment. Then, when the user chooses, you send a consent update with their choices. The lesson has copy-ready code. | gtag implementation<br>• Default before tags load<br>• Region-specific defaults<br>• wait_for_update<br>• Update on user choice | Layout: Code editor showing the consent default snippet with the region array, then the update function; an arrow marks 'runs first'. Timing (33s): title card 0–3s; then the 4 on-screen points build in narration order about every 6s, each animating its element; hold and highlight the key idea in the final 5s. |
| 8 | 4:05 | 19 | Two optional settings are worth knowing. Ads data redaction removes ad click identifiers from requests when ad storage is denied. URL passthrough carries click information through your URLs between pages, so conversions can still be connected without cookies. Consider both with your legal team. | Optional settings<br>• ads_data_redaction<br>• url_passthrough<br>• Review with legal | Layout: A URL with a click parameter highlighted being passed from a landing page to a checkout page; a redaction bar covers an identifier in another request. Timing (19s): title card 0–3s; then the 3 on-screen points build in narration order about every 3s, each animating its element; hold and highlight the key idea in the final 5s. |
| 9 | 4:24 | 30 | If you use Google Tag Manager, enable Consent Overview, use your CMP's template from the Community Template Gallery, and fire the default on the Consent Initialization trigger, which runs before everything else. Then the crucial bit: Consent Mode only controls Google tags. Meta, TikTok and LinkedIn tags don't understand it. Add additional consent checks in Tag Manager, for example requiring ad storage, or fire them only after a consent event. | With Tag Manager<br>• Consent Overview<br>• CMP template<br>• Consent Initialization trigger<br>• Extra checks for non-Google tags | Layout: Tag Manager style interface mock showing a tag list with consent check columns; Meta and TikTok rows get an extra 'requires ad_storage' badge. Timing (30s): title card 0–3s; then the 4 on-screen points build in narration order about every 5s, each animating its element; hold and highlight the key idea in the final 5s. |
| 10 | 4:54 | 27 | Here's an illustrative example. An electronics retailer in Kuwait and Saudi Arabia uses advanced mode for Gulf traffic after legal review, and basic mode for EU visitors as a conservative choice. They add consent checks to their Meta and TikTok tags in Tag Manager. After rollout, GA4's consent settings confirm signals are arriving, and Google Ads diagnostics show Consent Mode as active. | Example: Gulf electronics retailer (illustrative)<br>• Advanced for Gulf (after review)<br>• Basic for EU<br>• Consent checks on Meta/TikTok<br>• Verified in GA4 and Ads | Layout: Map with Kuwait and Saudi Arabia marked 'advanced', Europe marked 'basic'; a green 'active' status badge appears. Timing (27s): title card 0–3s; then the 4 on-screen points build in narration order about every 4s, each animating its element; hold and highlight the key idea in the final 5s. |
| 11 | 5:21 | 32 | Verify three ways. In Tag Assistant, the consent tab should show the default and then the update, in that order. In the network panel, Google requests carry g c s and g c d parameters describing consent state, and with storage denied, no Google Analytics or Ads cookies should appear. And in GA4 admin, under data collection, the consent settings page shows whether ad user data and ad personalization signals are being received. | Verify three ways<br>• Tag Assistant consent tab<br>• gcs / gcd in requests, no cookies when denied<br>• GA4 consent settings | Layout: Three verification panels: a Tag Assistant timeline, a network request with gcs highlighted, and a GA4 settings card with green checks. Timing (32s): title card 0–3s; then the 3 on-screen points build in narration order about every 8s, each animating its element; hold and highlight the key idea in the final 5s. |
| 12 | 5:53 | 43 | Common Consent Mode mistakes. Setting the default after the Google tag has already loaded. Implementing only the original two parameters and missing ad user data and ad personalization. Assuming Consent Mode controls Meta, TikTok and LinkedIn. Granting everything by default in every region without a legal basis. And never checking GA4's consent settings. Try this now: open Tag Assistant on your site, load a page without touching the banner, and read the consent tab. Write down the default values you see. Then accept, and check the update values. If you can't find a default at all, that's your first fix. | Mistakes + try this now<br>• Late default<br>• Missing v2 parameters<br>• Expecting non-Google control<br>• Blanket 'granted' defaults<br>• No GA4 check<br>• Try: read the Tag Assistant consent tab | Layout: Tag Assistant-style panel with a consent tab; default row fills with 'denied' values, then an update row fills with 'granted'; a missing-default state flashes red. |
| 13 | 6:36 | 83 | Watch me do it. Let's verify Consent Mode on an illustrative London shoe retailer after deployment. First, I open Tag Assistant and connect to the site. On the first page view, the consent tab shows a default command: ad storage denied, analytics storage denied, ad user data denied, ad personalization denied, with region set for the UK. Good, and it appears before the Google tag, which is the order we need. Second, I open the network tab and find a request to Google Analytics. The g c s parameter shows everything denied, and no underscore g a cookie exists. That confirms advanced mode's cookieless ping. Third, I click accept all. Tag Assistant now shows an update with all four granted, and the next request's g c s shows granted, with cookies now set. Fourth, I test the partial path: manage choices, analytics on, ads off. The update shows analytics granted, ad storage denied, and the Meta tag doesn't fire because of its additional consent check. Fifth, in GA4 admin, consent settings shows ad user data and ad personalization signals being received. Five checks, fifteen minutes, and I can sign off with evidence. | Watch me do it: verifying Consent Mode (illustrative)<br>• Default before tag, all denied<br>• gcs denied, no cookies<br>• Accept → update granted<br>• Partial: analytics only, Meta silent<br>• GA4 signals received | Layout: Tag Assistant timeline on the left, network request detail on the right. Timing: default row appears ~10s; gcs parameter highlights ~40s; update row after a click animation ~70s; partial path at ~100s with Meta tag grayed; GA4 settings card with green checks in the final 20s. |
| 14 | 7:59 | 28 | Recap. Consent Mode passes choices to Google tags, and version two adds ad user data and ad personalization. Choose basic or advanced deliberately. Set defaults before tags load, per region, and update on choice. Add separate checks for non-Google tags. Your next step: implement or audit Consent Mode on a test page, recording defaults, updates, the g c s values and GA4's consent status. | Recap and next step<br>• v2 parameters<br>• Basic vs advanced<br>• Defaults first<br>• Audit your implementation | Layout: Checklist of four audit items with a test page mock beside it. Timing (28s): title card 0–3s; then the 4 on-screen points build in narration order about every 5s, each animating its element; hold and highlight the key idea in the final 5s. |

<details><summary>Narration per scene (paste into ElevenLabs)</summary>

Scene 1 — Google Consent Mode v2

```text
If you advertise to Europe or the UK with Google and your audiences have quietly stopped growing, or your conversion modeling looks thin, the cause is often one missing detail in Consent Mode. In this lecture you'll learn what Consent Mode version two actually does, the difference between basic and advanced implementations, how to set it up with code or Tag Manager, and three ways to verify it's working.
```

Scene 2 — Why it matters

```text
Why does this matter? Because Consent Mode is the difference between Google's tools working properly under consent rules and silently degrading. Here's an analogy. Think of a hotel with a do not disturb sign. The guest decides, the sign communicates the decision, and housekeeping adapts. Your CMP is the guest's decision. Consent Mode is the sign hanging on the door. Google's tags are housekeeping. Without the sign, housekeeping either barges in, which is a compliance problem, or stays away from every room, which is a measurement problem. Consent Mode lets them do the right thing for each room.
```

Scene 3 — What it is

```text
First, what it isn't. Consent Mode is not a consent banner. Your consent platform collects choices. Consent Mode passes those choices to Google's tags, like Analytics, Google Ads and Floodlight, so they change their behavior. Version two added two parameters to the original storage settings: ad user data, which covers sending user data to Google for advertising, and ad personalization, which covers things like remarketing.
```

Scene 4 — Why v2 matters

```text
Why does v2 matter? For traffic from the European Economic Area and the UK, Google requires these consent signals before it uses data for certain ads features, including building audiences and some measurement. Without them, remarketing lists stop growing and some features degrade. So if you only implemented the original two parameters years ago, you're probably missing out, even if users are consenting.
```

Scene 5 — Basic vs advanced

```text
Now the big choice: basic or advanced. In basic mode, Google tags don't load at all until the user chooses. If they say no, nothing goes to Google. In advanced mode, tags load immediately with a default of denied. If the user says no, the tags send cookieless pings, which don't read or write cookies, and Google uses them for more precise, advertiser-specific modeling. Advanced gives better data. Basic is more conservative. It's a legal and business decision, so document it.
```

Scene 6 — Simple example: the event sequence

```text
Here's a simple worked example of the event sequence. A visitor from Germany lands on your store. First, the consent default command runs: ad storage, analytics storage, ad user data and ad personalization all denied, with a five hundred millisecond wait for update. Then the Google tag loads. In advanced mode it sends a cookieless ping. The visitor clicks accept all on the banner. The CMP fires a consent update with all four set to granted. Now the Google tag sets its cookies and sends full measurement. In Tag Assistant, you should see exactly that order: default, then update. If you see the tag fire before the default, you've found a race condition.
```

Scene 7 — gtag implementation

```text
Implementation has one golden rule: set the default before any Google tag loads. With gtag, you push a consent default command with all four key parameters, typically denied for EEA and UK regions, with a short wait for update so the banner can respond. You can set different defaults for other regions based on your legal assessment. Then, when the user chooses, you send a consent update with their choices. The lesson has copy-ready code.
```

Scene 8 — Optional settings

```text
Two optional settings are worth knowing. Ads data redaction removes ad click identifiers from requests when ad storage is denied. URL passthrough carries click information through your URLs between pages, so conversions can still be connected without cookies. Consider both with your legal team.
```

Scene 9 — With Tag Manager

```text
If you use Google Tag Manager, enable Consent Overview, use your CMP's template from the Community Template Gallery, and fire the default on the Consent Initialization trigger, which runs before everything else. Then the crucial bit: Consent Mode only controls Google tags. Meta, TikTok and LinkedIn tags don't understand it. Add additional consent checks in Tag Manager, for example requiring ad storage, or fire them only after a consent event.
```

Scene 10 — Example: Gulf electronics retailer (illustrative)

```text
Here's an illustrative example. An electronics retailer in Kuwait and Saudi Arabia uses advanced mode for Gulf traffic after legal review, and basic mode for EU visitors as a conservative choice. They add consent checks to their Meta and TikTok tags in Tag Manager. After rollout, GA4's consent settings confirm signals are arriving, and Google Ads diagnostics show Consent Mode as active.
```

Scene 11 — Verify three ways

```text
Verify three ways. In Tag Assistant, the consent tab should show the default and then the update, in that order. In the network panel, Google requests carry g c s and g c d parameters describing consent state, and with storage denied, no Google Analytics or Ads cookies should appear. And in GA4 admin, under data collection, the consent settings page shows whether ad user data and ad personalization signals are being received.
```

Scene 12 — Mistakes + try this now

```text
Common Consent Mode mistakes. Setting the default after the Google tag has already loaded. Implementing only the original two parameters and missing ad user data and ad personalization. Assuming Consent Mode controls Meta, TikTok and LinkedIn. Granting everything by default in every region without a legal basis. And never checking GA4's consent settings. Try this now: open Tag Assistant on your site, load a page without touching the banner, and read the consent tab. Write down the default values you see. Then accept, and check the update values. If you can't find a default at all, that's your first fix.
```

Scene 13 — Watch me do it: verifying Consent Mode (illustrative)

```text
Watch me do it. Let's verify Consent Mode on an illustrative London shoe retailer after deployment. First, I open Tag Assistant and connect to the site. On the first page view, the consent tab shows a default command: ad storage denied, analytics storage denied, ad user data denied, ad personalization denied, with region set for the UK. Good, and it appears before the Google tag, which is the order we need. Second, I open the network tab and find a request to Google Analytics. The g c s parameter shows everything denied, and no underscore g a cookie exists. That confirms advanced mode's cookieless ping. Third, I click accept all. Tag Assistant now shows an update with all four granted, and the next request's g c s shows granted, with cookies now set. Fourth, I test the partial path: manage choices, analytics on, ads off. The update shows analytics granted, ad storage denied, and the Meta tag doesn't fire because of its additional consent check. Fifth, in GA4 admin, consent settings shows ad user data and ad personalization signals being received. Five checks, fifteen minutes, and I can sign off with evidence.
```

Scene 14 — Recap and next step

```text
Recap. Consent Mode passes choices to Google tags, and version two adds ad user data and ad personalization. Choose basic or advanced deliberately. Set defaults before tags load, per region, and update on choice. Add separate checks for non-Google tags. Your next step: implement or audit Consent Mode on a test page, recording defaults, updates, the g c s values and GA4's consent status.
```

</details>

### 5. GA4 with consent: modeling, settings and the Measurement Protocol

Lesson `ga4-with-consent` · module “GA4 and server-side tagging” · target 8 min · 14 scenes · 1180 words · planned 8:32 (words ≈ 8:33) · status: **to produce**

| Term | Say |
|---|---|
| GA4 | G A four |
| PII | P I I |
| MP | measurement protocol |

| # | Time | Sec | Narration | On screen | Visual |
|---|---|---|---|---|---|
| 1 | 0:00 | 28 | Here's a question worth asking about your GA4 data: when someone says no to analytics, what does GA4 actually see? The answer depends on choices you may not know you made. In this lecture you'll learn how GA4 behaves under consent, how behavioral modeling fills gaps, which privacy settings to review, and how to send server-confirmed events with the Measurement Protocol while respecting consent. | GA4 with consent<br>• What GA4 sees when denied<br>• Behavioral modeling<br>• Privacy settings<br>• The Measurement Protocol | Layout: A GA4-style dashboard (generic) with some users shown as solid dots and others as faint outlines representing unconsented visitors. Timing (28s): title card 0–3s; then the 4 on-screen points build in narration order about every 5s, each animating its element; hold and highlight the key idea in the final 5s. |
| 2 | 0:28 | 41 | Why does this matter? Because GA4 is often the number leadership looks at, and if nobody understands how consent shapes it, people make decisions on numbers they misread. Here's an analogy. Think of a weather station that only measures when the sun is out, and estimates the rainy days from patterns. That's useful, as long as everyone knows which days were measured and which were estimated. GA4 under consent is similar. Some users are observed, some are modeled, and some aren't there at all. Knowing which is which is the difference between insight and illusion. | Why it matters<br>• Leadership reads GA4<br>• Observed vs modeled vs missing<br>• Know which is which | Layout: weather station icon over a calendar; sunny days get solid data points, rainy days get dotted estimated points, some days are blank. |
| 3 | 1:09 | 27 | With advanced Consent Mode, when analytics storage is denied, GA4 still receives pings, but they're cookieless. No analytics cookie is read or written, so each ping can't be linked to a returning user. With basic mode, GA4 receives nothing at all from people who decline. That difference matters because GA4's behavioral modeling learns from those cookieless pings. No pings, no advertiser-specific modeling. | Denied consent in GA4<br>• Advanced: cookieless pings<br>• Basic: nothing sent<br>• Modeling needs the pings | Layout: Two pipes: one carrying faint gray pings into GA4 (advanced), one closed off with a valve (basic). Timing (27s): title card 0–3s; then the 3 on-screen points build in narration order about every 6s, each animating its element; hold and highlight the key idea in the final 5s. |
| 4 | 1:36 | 28 | Behavioral modeling uses machine learning to estimate what unconsented users did, based on similar consented users. You'll see modeled data when your reporting identity is set to Blended and your property meets Google's eligibility thresholds, which require minimum daily volumes of both denied and consented traffic over a period. Check the current numbers in the help center. And remember, modeled numbers are estimates, not observations. | Behavioral modeling<br>• Estimates unconsented behavior<br>• Requires Blended identity<br>• Eligibility thresholds apply<br>• Estimates, not observations | Layout: A chart with solid observed bars and lighter modeled bar segments stacked on top, labeled 'modeled'. Timing (28s): title card 0–3s; then the 4 on-screen points build in narration order about every 5s, each animating its element; hold and highlight the key idea in the final 5s. |
| 5 | 2:04 | 26 | Reporting identity has three options. Blended uses User-ID, device ID and modeling. Observed uses only User-ID and device ID. Device-based uses only the device. Switching doesn't change the data you collected, only how reports are computed, so you can flip between them to see how much of a number is modeled. That's a great way to explain gaps to stakeholders. | Reporting identity<br>• Blended: IDs + modeling<br>• Observed: IDs only<br>• Device-based: device only<br>• Switch to compare | Layout: Three toggle buttons; as each is selected, the same chart redraws with slightly different totals. Timing (26s): title card 0–3s; then the 4 on-screen points build in narration order about every 4s, each animating its element; hold and highlight the key idea in the final 5s. |
| 6 | 2:30 | 44 | Here's a simple worked example. A Dubai travel agency sees two numbers for last week's users. With reporting identity set to Blended, GA4 shows twelve thousand users. Switch to Observed, and it shows nine thousand six hundred. The difference, about two thousand four hundred, is modeled from cookieless pings of visitors who declined analytics. Neither number is wrong. When they present results, they say: we observed nine thousand six hundred users, and GA4 estimates about twelve thousand in total. That one sentence prevents a lot of confusion, especially when comparing with periods before Consent Mode was set up. The numbers are illustrative. | Simple example: Dubai travel agency (illustrative)<br>• Blended: 12,000 users<br>• Observed: 9,600 users<br>• ~2,400 modeled<br>• Say both, clearly | Layout: toggle switch between 'Blended' and 'Observed'; a big number flips from 12,000 to 9,600; the difference animates into a labeled 'modeled' segment. |
| 7 | 3:14 | 30 | Next, check the consent settings in GA4's admin, under data collection. It shows whether ad user data and ad personalization signals are arriving for European traffic. If they're missing, audiences for ads and some features linked to Google Ads will be limited. While you're there, review data retention, granular location and device collection, Google signals, and data redaction, which can strip email addresses and chosen query parameters from page URLs. | GA4 admin checks<br>• Consent signals received?<br>• Data retention<br>• Granular location/device<br>• Google signals<br>• PII redaction | Layout: Settings panel mock with five rows being ticked; one row 'consent signals' shows a green status. Timing (30s): title card 0–3s; then the 5 on-screen points build in narration order about every 4s, each animating its element; hold and highlight the key idea in the final 5s. |
| 8 | 3:44 | 35 | Now the Measurement Protocol. It lets your server send events to GA4, like a purchase confirmed by the payment provider, a subscription renewal or an offline sale. It complements the tag. Each request needs your measurement ID and an API secret, which must stay on the server. Include the client ID captured from the user's analytics cookie at checkout, so the event joins the right session. And pass consent, ad user data and ad personalization, reflecting what the user chose. | Measurement Protocol<br>• Server → GA4 events<br>• measurement_id + secret (server only)<br>• client_id to join the session<br>• Consent object | Layout: Diagram: a payment server sends a 'purchase' event to GA4 carrying badges labeled client_id and consent. Timing (35s): title card 0–3s; then the 4 on-screen points build in narration order about every 6s, each animating its element; hold and highlight the key idea in the final 5s. |
| 9 | 4:19 | 29 | The lesson includes a Python function that sends a purchase, with a debug switch that uses the validation server during development. Use it before going live, because the validation server tells you if your payload is malformed, whereas the live endpoint silently accepts almost anything. Store the client ID and consent state with every order in your database, so server events always carry the right context. | Build and validate<br>• Python send_purchase function<br>• /debug/mp/collect for validation<br>• Store client_id + consent per order | Layout: Code editor showing the Python function; a terminal prints a validation response with no errors. Timing (29s): title card 0–3s; then the 3 on-screen points build in narration order about every 7s, each animating its element; hold and highlight the key idea in the final 5s. |
| 10 | 4:48 | 37 | A word on duplicates. GA4 often deduplicates purchases with the same transaction ID, but don't rely on it blindly. Decide which source is the truth for each event, the browser or the server, and avoid sending the same purchase from both unless you've tested the result. Here's an illustrative example: a Lahore meal-kit subscription never saw renewals in GA4, because they happened when cards were charged. Sending renewal events server-side, with stored client IDs and consent, finally let them analyze first order to renewal paths. | Duplicates and an example<br>• One source of truth per event<br>• Test before double-sending<br>• Lahore meal-kit: renewals via MP (illustrative) | Layout: A single purchase icon arriving from two directions with a warning sign; then a subscription timeline with renewal markers flowing into GA4. Timing (37s): title card 0–3s; then the 3 on-screen points build in narration order about every 9s, each animating its element; hold and highlight the key idea in the final 5s. |
| 11 | 5:25 | 40 | Common GA4 consent mistakes. Putting the Measurement Protocol secret in front-end code. Sending server events without a client ID, which creates orphan users. Forgetting the consent object in server events. Comparing modeled and observed numbers across periods without saying so. And never checking data redaction, so email addresses end up in page URLs. Try this now: in GA4 admin, open the data collection settings and write down three things: your data retention period, whether consent signals are being received, and whether URL query parameter redaction is on. Fix anything that surprises you. | Mistakes + try this now<br>• Exposed API secret<br>• Missing client_id<br>• Missing consent object<br>• Mixed modeled/observed<br>• No redaction<br>• Try: check three admin settings | Layout: GA4 admin-style settings list; three rows highlight in sequence with values appearing: retention, consent signals, redaction. |
| 12 | 6:05 | 35 | Quick self-check. Your GA4 users dropped fifteen percent the week you launched a new consent banner, while orders in your backend stayed flat. Did traffic fall? Pause. Almost certainly not. More people declined analytics, so GA4 observes fewer of them. If you're on advanced Consent Mode with Blended identity, modeling may fill some of the gap once you're eligible. The right response is to annotate the change in GA4, compare with backend orders, and avoid reading the drop as lost demand. | Quick self-check<br>• GA4 users −15% after new banner<br>• Backend orders flat<br>• Traffic fell? No — observation fell<br>• Annotate + compare with backend | Layout: GA4 user line dipping at a marked 'new banner' annotation while a backend orders line stays flat; an answer card highlights 'observation changed, not demand'. |
| 13 | 6:40 | 80 | Watch me do it. Let's send a server-confirmed renewal event for an illustrative Dubai gym subscription using the Measurement Protocol. First, at signup, our site stores the GA4 client ID from the underscore g a cookie on the customer record, along with the consent choices from the CMP. Second, a month later the payment provider's webhook tells our server that the renewal succeeded for customer one two three. Third, the server looks up the stored client ID and consent. Analytics consent was granted; advertising consent was not. Fourth, I build the payload: client ID, the event name subscription renewal, value and currency in dirhams, and the consent object with ad user data and ad personalization both denied. Fifth, I send it to the debug endpoint first. The validation response lists no messages, so the format is right. Sixth, I switch to the live endpoint, then check GA4's real-time report, where the renewal appears. Finally, I build an exploration: first purchase to renewal, by acquisition channel. For the first time, the gym can see which channels bring members who stay, not just members who join. | Watch me do it: server renewal event (illustrative)<br>• Store client_id + consent at signup<br>• Webhook: renewal succeeded<br>• Payload with consent denied for ads<br>• Validate on debug endpoint<br>• Live, then retention by channel | Layout: sequence diagram with lanes for browser, server, payment provider and GA4. Timing: arrows animate in order roughly every 20s; payload JSON card expands at ~70s; validation response shows 'no messages' at ~95s; final 20s show an exploration chart by channel. |
| 14 | 8:00 | 32 | Recap. With advanced Consent Mode, GA4 gets cookieless pings and can model under Blended identity if you're eligible. Check consent signals and privacy settings in admin. Use the Measurement Protocol for server-confirmed events, keep the secret on the server, and send client ID and consent. Your next step: in a test property, send a purchase through the validation server with consent parameters, and document how you'll store client ID and consent with each order. | Recap and next step<br>• Pings + modeling<br>• Admin checks<br>• MP with client_id + consent<br>• Validate a test purchase | Layout: A checklist with three items and a test-property badge. Timing (32s): title card 0–3s; then the 4 on-screen points build in narration order about every 6s, each animating its element; hold and highlight the key idea in the final 5s. |

<details><summary>Narration per scene (paste into ElevenLabs)</summary>

Scene 1 — GA4 with consent

```text
Here's a question worth asking about your GA4 data: when someone says no to analytics, what does GA4 actually see? The answer depends on choices you may not know you made. In this lecture you'll learn how GA4 behaves under consent, how behavioral modeling fills gaps, which privacy settings to review, and how to send server-confirmed events with the Measurement Protocol while respecting consent.
```

Scene 2 — Why it matters

```text
Why does this matter? Because GA4 is often the number leadership looks at, and if nobody understands how consent shapes it, people make decisions on numbers they misread. Here's an analogy. Think of a weather station that only measures when the sun is out, and estimates the rainy days from patterns. That's useful, as long as everyone knows which days were measured and which were estimated. GA4 under consent is similar. Some users are observed, some are modeled, and some aren't there at all. Knowing which is which is the difference between insight and illusion.
```

Scene 3 — Denied consent in GA4

```text
With advanced Consent Mode, when analytics storage is denied, GA4 still receives pings, but they're cookieless. No analytics cookie is read or written, so each ping can't be linked to a returning user. With basic mode, GA4 receives nothing at all from people who decline. That difference matters because GA4's behavioral modeling learns from those cookieless pings. No pings, no advertiser-specific modeling.
```

Scene 4 — Behavioral modeling

```text
Behavioral modeling uses machine learning to estimate what unconsented users did, based on similar consented users. You'll see modeled data when your reporting identity is set to Blended and your property meets Google's eligibility thresholds, which require minimum daily volumes of both denied and consented traffic over a period. Check the current numbers in the help center. And remember, modeled numbers are estimates, not observations.
```

Scene 5 — Reporting identity

```text
Reporting identity has three options. Blended uses User-ID, device ID and modeling. Observed uses only User-ID and device ID. Device-based uses only the device. Switching doesn't change the data you collected, only how reports are computed, so you can flip between them to see how much of a number is modeled. That's a great way to explain gaps to stakeholders.
```

Scene 6 — Simple example: Dubai travel agency (illustrative)

```text
Here's a simple worked example. A Dubai travel agency sees two numbers for last week's users. With reporting identity set to Blended, GA4 shows twelve thousand users. Switch to Observed, and it shows nine thousand six hundred. The difference, about two thousand four hundred, is modeled from cookieless pings of visitors who declined analytics. Neither number is wrong. When they present results, they say: we observed nine thousand six hundred users, and GA4 estimates about twelve thousand in total. That one sentence prevents a lot of confusion, especially when comparing with periods before Consent Mode was set up. The numbers are illustrative.
```

Scene 7 — GA4 admin checks

```text
Next, check the consent settings in GA4's admin, under data collection. It shows whether ad user data and ad personalization signals are arriving for European traffic. If they're missing, audiences for ads and some features linked to Google Ads will be limited. While you're there, review data retention, granular location and device collection, Google signals, and data redaction, which can strip email addresses and chosen query parameters from page URLs.
```

Scene 8 — Measurement Protocol

```text
Now the Measurement Protocol. It lets your server send events to GA4, like a purchase confirmed by the payment provider, a subscription renewal or an offline sale. It complements the tag. Each request needs your measurement ID and an API secret, which must stay on the server. Include the client ID captured from the user's analytics cookie at checkout, so the event joins the right session. And pass consent, ad user data and ad personalization, reflecting what the user chose.
```

Scene 9 — Build and validate

```text
The lesson includes a Python function that sends a purchase, with a debug switch that uses the validation server during development. Use it before going live, because the validation server tells you if your payload is malformed, whereas the live endpoint silently accepts almost anything. Store the client ID and consent state with every order in your database, so server events always carry the right context.
```

Scene 10 — Duplicates and an example

```text
A word on duplicates. GA4 often deduplicates purchases with the same transaction ID, but don't rely on it blindly. Decide which source is the truth for each event, the browser or the server, and avoid sending the same purchase from both unless you've tested the result. Here's an illustrative example: a Lahore meal-kit subscription never saw renewals in GA4, because they happened when cards were charged. Sending renewal events server-side, with stored client IDs and consent, finally let them analyze first order to renewal paths.
```

Scene 11 — Mistakes + try this now

```text
Common GA4 consent mistakes. Putting the Measurement Protocol secret in front-end code. Sending server events without a client ID, which creates orphan users. Forgetting the consent object in server events. Comparing modeled and observed numbers across periods without saying so. And never checking data redaction, so email addresses end up in page URLs. Try this now: in GA4 admin, open the data collection settings and write down three things: your data retention period, whether consent signals are being received, and whether URL query parameter redaction is on. Fix anything that surprises you.
```

Scene 12 — Quick self-check

```text
Quick self-check. Your GA4 users dropped fifteen percent the week you launched a new consent banner, while orders in your backend stayed flat. Did traffic fall? Pause. Almost certainly not. More people declined analytics, so GA4 observes fewer of them. If you're on advanced Consent Mode with Blended identity, modeling may fill some of the gap once you're eligible. The right response is to annotate the change in GA4, compare with backend orders, and avoid reading the drop as lost demand.
```

Scene 13 — Watch me do it: server renewal event (illustrative)

```text
Watch me do it. Let's send a server-confirmed renewal event for an illustrative Dubai gym subscription using the Measurement Protocol. First, at signup, our site stores the GA4 client ID from the underscore g a cookie on the customer record, along with the consent choices from the CMP. Second, a month later the payment provider's webhook tells our server that the renewal succeeded for customer one two three. Third, the server looks up the stored client ID and consent. Analytics consent was granted; advertising consent was not. Fourth, I build the payload: client ID, the event name subscription renewal, value and currency in dirhams, and the consent object with ad user data and ad personalization both denied. Fifth, I send it to the debug endpoint first. The validation response lists no messages, so the format is right. Sixth, I switch to the live endpoint, then check GA4's real-time report, where the renewal appears. Finally, I build an exploration: first purchase to renewal, by acquisition channel. For the first time, the gym can see which channels bring members who stay, not just members who join.
```

Scene 14 — Recap and next step

```text
Recap. With advanced Consent Mode, GA4 gets cookieless pings and can model under Blended identity if you're eligible. Check consent signals and privacy settings in admin. Use the Measurement Protocol for server-confirmed events, keep the secret on the server, and send client ID and consent. Your next step: in a test property, send a purchase through the validation server with consent parameters, and document how you'll store client ID and consent with each order.
```

</details>

### 6. Server-side tagging with server-side GTM and Google tag gateway

Lesson `server-side-tagging-sgtm` · module “GA4 and server-side tagging” · target 9 min · 14 scenes · 1209 words · planned 8:46 (words ≈ 8:46) · status: **to produce**

| Term | Say |
|---|---|
| sGTM | server-side G T M |
| CDN | C D N |
| TLS | T L S |

| # | Time | Sec | Narration | On screen | Visual |
|---|---|---|---|---|---|
| 1 | 0:00 | 32 | Imagine your website sending one clean stream of events to a server you control, and that server deciding exactly what each advertising and analytics vendor gets. Fewer scripts slowing your pages, less data leaking, and one place to plug in every conversion API. That's server-side tagging. In this lecture you'll learn the architecture, the building blocks of server-side Google Tag Manager, hosting choices, how Google tag gateway fits in, and how consent works server-side. | Server-side tagging<br>• Architecture<br>• Server GTM building blocks<br>• Hosting<br>• Google tag gateway<br>• Consent server-side | Layout: Animated architecture: browser sends a single stream to a server labeled with the site's subdomain, which branches to several vendor icons. Timing (32s): title card 0–3s; then the 5 on-screen points build in narration order about every 4s, each animating its element; hold and highlight the key idea in the final 5s. |
| 2 | 0:32 | 43 | Why does this matter? Because server-side tagging moves the decision about what data leaves your business from dozens of third-party scripts into one place you control. Here's an analogy. Traditional tagging is like every courier company having a key to your warehouse and taking whatever they think they need. Server-side tagging is like having a dispatch desk. Everything comes to the desk first. The desk checks the paperwork, removes anything that shouldn't go out, and hands each courier only its own parcel. It's more work to staff the desk, but you finally know exactly what leaves the building. | Why it matters<br>• Traditional: every courier has a key<br>• Server-side: one dispatch desk<br>• You decide what leaves | Layout: warehouse with multiple courier vans. First, each van drives straight to open shelves. Then a dispatch desk appears at the door; parcels pass through it and each van receives a single labeled parcel. |
| 3 | 1:15 | 31 | In a traditional setup, every vendor's JavaScript runs in the visitor's browser and sends data straight to that vendor. You've got little control over what leaves. In a server-side setup, the browser sends events to your tagging server on your own subdomain, like s g t m dot your domain. The server then forwards what's needed to Google Analytics, Google Ads, Meta's Conversions API, TikTok's Events API, or your own data warehouse. | Traditional vs server-side<br>• Traditional: many scripts, direct to vendors<br>• Server-side: one stream to your server<br>• Server forwards what's needed | Layout: Split screen: left, a browser with many arrows shooting out to vendors; right, one arrow to a server which then fans out carefully. Timing (31s): title card 0–3s; then the 3 on-screen points build in narration order about every 7s, each animating its element; hold and highlight the key idea in the final 5s. |
| 4 | 1:46 | 33 | The benefits are real. Fewer third-party scripts, so pages get faster. Data minimization, because you can strip IP addresses, redact personal data and drop fields before any vendor sees them. First-party cookies set by your server, which tend to last longer in some browsers. Central consent enforcement. And one place to add conversion APIs. The costs are real too: hosting bills, uptime, security, monitoring and harder debugging. You become responsible for everything the server forwards. | Benefits and costs<br>• Faster pages<br>• Minimize before sharing<br>• Durable first-party cookies<br>• Central consent<br>• Costs: hosting, uptime, responsibility | Layout: Balance scale with benefits on one side and responsibilities on the other, roughly balanced. Timing (33s): title card 0–3s; then the 5 on-screen points build in narration order about every 5s, each animating its element; hold and highlight the key idea in the final 5s. |
| 5 | 2:19 | 30 | Server-side Tag Manager has four building blocks. Clients receive incoming requests and turn them into event data; the GA4 client handles GA4-format requests. Tags send data to vendors when triggers match. Transformations allow, exclude or add parameters before tags see them, which is perfect for enforcing minimization. Variables read event data, headers or cookies. And preview mode shows you every incoming request, the event data, and every outgoing request. | Server GTM building blocks<br>• Clients<br>• Tags<br>• Transformations<br>• Variables<br>• Preview mode | Layout: Exploded diagram of a server container with four labeled compartments; an event travels through them in order. Timing (30s): title card 0–3s; then the 5 on-screen points build in narration order about every 4s, each animating its element; hold and highlight the key idea in the final 5s. |
| 6 | 2:49 | 45 | Here's a simple worked example. A Riyadh furniture store sends a purchase event from the browser to its server container. The event contains the order ID, value, currency, items, the user's IP address, and the page URL, which accidentally includes the customer's email in a query parameter. In the server container, a transformation removes the email from the URL for every vendor. The GA4 tag receives the event without the IP override. The Meta Conversions API tag receives hashed email from the order system, but only fires if advertising consent is granted. One incoming event, three different outgoing payloads, each minimized for its purpose. | Simple example: Riyadh furniture store (illustrative)<br>• Incoming: purchase + IP + URL with email<br>• Transform: strip email from URL<br>• GA4: no IP override<br>• Meta: hashed email, only with ad consent | Layout: one incoming event card on the left splits through a transformation filter into three outgoing cards on the right; the email string in the URL visibly gets redacted. |
| 7 | 3:34 | 36 | Where do you host it? Google documents Cloud Run as the path for server-side Tag Manager, and you should follow its current guidance on production sizing. Managed hosting providers offer simpler setup and extra features; check their data location, security and support. Either way, map a subdomain of your own site, or serve the container on the same origin through your CDN, so it's truly first-party. For Gulf data, think about hosting region and transfer rules like those under Saudi Arabia's PDPL. | Hosting<br>• Google Cloud Run<br>• Managed hosts<br>• First-party subdomain or same origin<br>• Consider data location rules | Layout: Map with server icons in different cloud regions; a DNS record card showing a subdomain pointing to the server. Timing (36s): title card 0–3s; then the 4 on-screen points build in narration order about every 7s, each animating its element; hold and highlight the key idea in the final 5s. |
| 8 | 4:10 | 35 | Now Google tag gateway for advertisers, previously called first-party mode. It serves the Google tag and routes its measurement requests through your own domain, via your CDN or load balancer. Integrations exist for Cloudflare, Akamai and Google Cloud, with others in beta. It's a quick win for Google measurement durability. But it's not a full server container. You can't transform data or add other vendors. Many teams start with tag gateway and add server-side Tag Manager when they need multi-vendor control. | Google tag gateway<br>• Google tag via your domain<br>• CDN / load balancer integrations<br>• Quick durability win<br>• Not a full server container | Layout: A CDN edge node icon sitting between the browser and Google; a comparison table shows tag gateway vs server GTM features. Timing (35s): title card 0–3s; then the 4 on-screen points build in narration order about every 6s, each animating its element; hold and highlight the key idea in the final 5s. |
| 9 | 4:45 | 34 | Consent must travel with every event. Google's consent state arrives in the g c s and g c d parameters, and your server container can read them and block vendor tags accordingly. For Meta, TikTok and others, add consent conditions to their server triggers, so the Meta Conversions API tag only fires when advertising consent is granted. And never use the server to restore data a user refused. That's both a legal risk and a breach of trust. | Consent server-side<br>• Read gcs / gcd<br>• Consent conditions on vendor triggers<br>• Never restore refused data | Layout: An event entering the server with a consent badge; vendor tags light up green or stay gray depending on the badge. Timing (34s): title card 0–3s; then the 3 on-screen points build in narration order about every 8s, each animating its element; hold and highlight the key idea in the final 5s. |
| 10 | 5:19 | 35 | Here's an illustrative example. A Karachi electronics marketplace had eight vendor scripts slowing the site, weak Safari data, and no conversion APIs. They moved to one GA4 stream to their own subdomain, removed other pixels from the page, and added server tags for GA4, Google Ads, Meta and TikTok, with a transformation stripping IP overrides and redacting emails in URLs. Consent checks on every non-Google tag. Hosted nearby with uptime alerts. The site got lighter and their event match quality improved. | Example: Karachi marketplace (illustrative)<br>• 8 scripts → 1 stream<br>• Server tags for 4 vendors<br>• Transformations for minimization<br>• Consent checks + monitoring | Layout: Before and after page-weight bars; a server container diagram with four vendor outputs and a transformation filter. Timing (35s): title card 0–3s; then the 4 on-screen points build in narration order about every 6s, each animating its element; hold and highlight the key idea in the final 5s. |
| 11 | 5:54 | 21 | The lesson has an eight-step setup outline, from creating the server container and mapping your subdomain, to adding vendor templates, transformations, preview, publishing and monitoring. Don't skip monitoring. If your server goes down silently, all your measurement stops at once. Alert on request volume drops, error rates and latency. | Set up in 8 steps<br>• Container → domain → web config<br>• Clients, tags, templates<br>• Transformations, preview<br>• Publish and monitor | Layout: Numbered step list animating down the screen, ending with a monitoring dashboard showing request volume. Timing (21s): title card 0–3s; then the 4 on-screen points build in narration order about every 3s, each animating its element; hold and highlight the key idea in the final 5s. |
| 12 | 6:15 | 39 | Common server-side mistakes. Hosting the container on a domain that isn't yours, which loses the first-party benefit. Forwarding every field to every vendor because it's easy. No monitoring, so an outage silently stops all measurement. Forgetting that consent must be checked on server-side triggers too. And underestimating the ongoing cost and ownership. Try this now: list the fields your browser currently sends in a purchase event. Next to each, write which vendors truly need it. Anything that no vendor needs is a candidate for removal in your first transformation. | Mistakes + try this now<br>• Third-party hosting domain<br>• Forwarding everything<br>• No monitoring<br>• No server-side consent checks<br>• Underestimated ownership<br>• Try: field-by-vendor needs table | Layout: table with fields as rows and vendors as columns; checkmarks appear where needed; rows with no checkmarks highlight as 'remove'. |
| 13 | 6:54 | 82 | Watch me do it. Let's trace one event through an illustrative server container for a Lahore electronics store. I open server preview mode and place a test order. First, the incoming request: a GA4-format request arrives at our subdomain, and the GA4 client claims it. Event data shows event name purchase, transaction ID, value in rupees, items, and consent state from the g c s parameter: analytics granted, advertising granted. Second, the transformation: our rule removes the page location's query string and drops the IP override for GA4. I can see the before and after in preview. Third, tags: the GA4 tag fires and I inspect its outgoing request, no email anywhere, good. The Google Ads conversion tag fires with the transaction ID. The Meta Conversions API tag fires with event ID order underscore the order number and hashed email looked up from our order system. The TikTok tag fires similarly. Fourth, I repeat with a test order where advertising consent was refused: only GA4 fires, and the three ad tags show not fired, with the consent condition as the reason. That screenshot goes into our evidence folder. | Watch me do it: tracing one event (illustrative)<br>• GA4 client claims request<br>• Transformation strips query string, IP<br>• Outgoing: GA4, Ads, Meta, TikTok<br>• Refused ads → only GA4 fires<br>• Screenshot to evidence folder | Layout: server preview mock with three panes: incoming request, event data, outgoing requests. Timing: incoming pane fills ~10s; transformation before/after diff ~40s; four outgoing requests list ~70s; replay with refused consent from ~110s showing three grayed tags. |
| 14 | 8:16 | 30 | Recap. Server-side tagging gives you one first-party stream and full control over what each vendor receives. Learn the clients, tags, transformations and variables. Host first-party and mind data location. Use tag gateway for a quick Google win. Enforce consent server-side and monitor uptime. Your next step: draw your architecture, showing what the browser sends, which tags forward to which vendors, which fields each gets, and where consent is checked. | Recap and next step<br>• One stream, your control<br>• Building blocks<br>• Consent + monitoring<br>• Draw your architecture | Layout: A blank architecture canvas with boxes being sketched in. Timing (30s): title card 0–3s; then the 4 on-screen points build in narration order about every 5s, each animating its element; hold and highlight the key idea in the final 5s. |

<details><summary>Narration per scene (paste into ElevenLabs)</summary>

Scene 1 — Server-side tagging

```text
Imagine your website sending one clean stream of events to a server you control, and that server deciding exactly what each advertising and analytics vendor gets. Fewer scripts slowing your pages, less data leaking, and one place to plug in every conversion API. That's server-side tagging. In this lecture you'll learn the architecture, the building blocks of server-side Google Tag Manager, hosting choices, how Google tag gateway fits in, and how consent works server-side.
```

Scene 2 — Why it matters

```text
Why does this matter? Because server-side tagging moves the decision about what data leaves your business from dozens of third-party scripts into one place you control. Here's an analogy. Traditional tagging is like every courier company having a key to your warehouse and taking whatever they think they need. Server-side tagging is like having a dispatch desk. Everything comes to the desk first. The desk checks the paperwork, removes anything that shouldn't go out, and hands each courier only its own parcel. It's more work to staff the desk, but you finally know exactly what leaves the building.
```

Scene 3 — Traditional vs server-side

```text
In a traditional setup, every vendor's JavaScript runs in the visitor's browser and sends data straight to that vendor. You've got little control over what leaves. In a server-side setup, the browser sends events to your tagging server on your own subdomain, like s g t m dot your domain. The server then forwards what's needed to Google Analytics, Google Ads, Meta's Conversions API, TikTok's Events API, or your own data warehouse.
```

Scene 4 — Benefits and costs

```text
The benefits are real. Fewer third-party scripts, so pages get faster. Data minimization, because you can strip IP addresses, redact personal data and drop fields before any vendor sees them. First-party cookies set by your server, which tend to last longer in some browsers. Central consent enforcement. And one place to add conversion APIs. The costs are real too: hosting bills, uptime, security, monitoring and harder debugging. You become responsible for everything the server forwards.
```

Scene 5 — Server GTM building blocks

```text
Server-side Tag Manager has four building blocks. Clients receive incoming requests and turn them into event data; the GA4 client handles GA4-format requests. Tags send data to vendors when triggers match. Transformations allow, exclude or add parameters before tags see them, which is perfect for enforcing minimization. Variables read event data, headers or cookies. And preview mode shows you every incoming request, the event data, and every outgoing request.
```

Scene 6 — Simple example: Riyadh furniture store (illustrative)

```text
Here's a simple worked example. A Riyadh furniture store sends a purchase event from the browser to its server container. The event contains the order ID, value, currency, items, the user's IP address, and the page URL, which accidentally includes the customer's email in a query parameter. In the server container, a transformation removes the email from the URL for every vendor. The GA4 tag receives the event without the IP override. The Meta Conversions API tag receives hashed email from the order system, but only fires if advertising consent is granted. One incoming event, three different outgoing payloads, each minimized for its purpose.
```

Scene 7 — Hosting

```text
Where do you host it? Google documents Cloud Run as the path for server-side Tag Manager, and you should follow its current guidance on production sizing. Managed hosting providers offer simpler setup and extra features; check their data location, security and support. Either way, map a subdomain of your own site, or serve the container on the same origin through your CDN, so it's truly first-party. For Gulf data, think about hosting region and transfer rules like those under Saudi Arabia's PDPL.
```

Scene 8 — Google tag gateway

```text
Now Google tag gateway for advertisers, previously called first-party mode. It serves the Google tag and routes its measurement requests through your own domain, via your CDN or load balancer. Integrations exist for Cloudflare, Akamai and Google Cloud, with others in beta. It's a quick win for Google measurement durability. But it's not a full server container. You can't transform data or add other vendors. Many teams start with tag gateway and add server-side Tag Manager when they need multi-vendor control.
```

Scene 9 — Consent server-side

```text
Consent must travel with every event. Google's consent state arrives in the g c s and g c d parameters, and your server container can read them and block vendor tags accordingly. For Meta, TikTok and others, add consent conditions to their server triggers, so the Meta Conversions API tag only fires when advertising consent is granted. And never use the server to restore data a user refused. That's both a legal risk and a breach of trust.
```

Scene 10 — Example: Karachi marketplace (illustrative)

```text
Here's an illustrative example. A Karachi electronics marketplace had eight vendor scripts slowing the site, weak Safari data, and no conversion APIs. They moved to one GA4 stream to their own subdomain, removed other pixels from the page, and added server tags for GA4, Google Ads, Meta and TikTok, with a transformation stripping IP overrides and redacting emails in URLs. Consent checks on every non-Google tag. Hosted nearby with uptime alerts. The site got lighter and their event match quality improved.
```

Scene 11 — Set up in 8 steps

```text
The lesson has an eight-step setup outline, from creating the server container and mapping your subdomain, to adding vendor templates, transformations, preview, publishing and monitoring. Don't skip monitoring. If your server goes down silently, all your measurement stops at once. Alert on request volume drops, error rates and latency.
```

Scene 12 — Mistakes + try this now

```text
Common server-side mistakes. Hosting the container on a domain that isn't yours, which loses the first-party benefit. Forwarding every field to every vendor because it's easy. No monitoring, so an outage silently stops all measurement. Forgetting that consent must be checked on server-side triggers too. And underestimating the ongoing cost and ownership. Try this now: list the fields your browser currently sends in a purchase event. Next to each, write which vendors truly need it. Anything that no vendor needs is a candidate for removal in your first transformation.
```

Scene 13 — Watch me do it: tracing one event (illustrative)

```text
Watch me do it. Let's trace one event through an illustrative server container for a Lahore electronics store. I open server preview mode and place a test order. First, the incoming request: a GA4-format request arrives at our subdomain, and the GA4 client claims it. Event data shows event name purchase, transaction ID, value in rupees, items, and consent state from the g c s parameter: analytics granted, advertising granted. Second, the transformation: our rule removes the page location's query string and drops the IP override for GA4. I can see the before and after in preview. Third, tags: the GA4 tag fires and I inspect its outgoing request, no email anywhere, good. The Google Ads conversion tag fires with the transaction ID. The Meta Conversions API tag fires with event ID order underscore the order number and hashed email looked up from our order system. The TikTok tag fires similarly. Fourth, I repeat with a test order where advertising consent was refused: only GA4 fires, and the three ad tags show not fired, with the consent condition as the reason. That screenshot goes into our evidence folder.
```

Scene 14 — Recap and next step

```text
Recap. Server-side tagging gives you one first-party stream and full control over what each vendor receives. Learn the clients, tags, transformations and variables. Host first-party and mind data location. Use tag gateway for a quick Google win. Enforce consent server-side and monitor uptime. Your next step: draw your architecture, showing what the browser sends, which tags forward to which vendors, which fields each gets, and where consent is checked.
```

</details>

### 7. Meta Conversions API: payloads, matching and deduplication

Lesson `meta-conversions-api` · module “Conversion APIs: Meta, Google, TikTok and LinkedIn” · target 9 min · 14 scenes · 1210 words · planned 8:45 (words ≈ 8:46) · status: **to produce**

| Term | Say |
|---|---|
| CAPI | cap-ee |
| EMQ | E M Q |
| fbp | F B P |
| fbc | F B C |
| SHA-256 | shah two fifty-six |

| # | Time | Sec | Narration | On screen | Visual |
|---|---|---|---|---|---|
| 1 | 0:00 | 26 | If your Meta ads report far fewer purchases than your store actually made, you're not just losing reporting. You're starving the algorithm. In this lecture you'll learn how the Meta Conversions API fills that gap: the implementation routes, the anatomy of an event, how deduplication really works, and how to use Event Match Quality and diagnostics to prove it's working. | Meta Conversions API<br>• Why CAPI<br>• Implementation routes<br>• Event anatomy<br>• Deduplication<br>• Match quality | Layout: A store backend showing a stack of orders beside a Meta-style (generic) dashboard showing a smaller stack; a pipe labeled 'server' connects them. Timing (26s): title card 0–3s; then the 5 on-screen points build in narration order about every 3s, each animating its element; hold and highlight the key idea in the final 5s. |
| 2 | 0:26 | 41 | Why does this matter? Because Meta's delivery system learns from the conversions it receives, and if it only sees part of your sales, it learns a partial lesson. Here's an analogy. Imagine coaching a sprinter while only seeing some of their races. You'd give advice based on an incomplete picture. The Pixel is watching from the stands, and sometimes the view is blocked. The Conversions API is the official race results sent straight from the timing system. Use both, make sure each race is only counted once, and the coach finally sees the whole season. | Why it matters<br>• Partial data → partial learning<br>• Pixel = view from the stands<br>• CAPI = official timing results<br>• Count each race once | Layout: running track; spectator-view camera icon with some races obscured; a timing system icon sends complete results; duplicate results merge into single entries. |
| 3 | 1:07 | 30 | The Conversions API sends events from your server straight to Meta. Meta recommends running it alongside the Pixel, not instead of it, with deduplication. The browser Pixel captures rich context. The server fills gaps when browsers block the Pixel, and it can carry stronger matching data from your order system. Together, more of your real conversions reach Meta, and they're easier to match to the people who saw your ads. | Pixel + CAPI<br>• Server → Meta directly<br>• Run alongside the Pixel<br>• Server fills browser gaps<br>• Better matching | Layout: Two arrows, one from a browser and one from a server, converging on a Meta icon with a merge symbol. Timing (30s): title card 0–3s; then the 4 on-screen points build in narration order about every 5s, each animating its element; hold and highlight the key idea in the final 5s. |
| 4 | 1:37 | 31 | There are four ways to implement it. Partner integrations, like Shopify's, are the fastest, but check which events and parameters they actually send. The Conversions API Gateway is a Meta-provided, self-hosted option. Server-side Tag Manager can run Meta's Conversions API tag template. Or you integrate directly from your backend through the Graph API. The lesson shows the direct route in Python, because understanding it makes every other route easier to debug. | Four routes<br>• Partner integrations<br>• CAPI Gateway<br>• Server-side GTM template<br>• Direct Graph API | Layout: Four road signs pointing to the same destination labeled 'Meta'. Timing (31s): title card 0–3s; then the 4 on-screen points build in narration order about every 5s, each animating its element; hold and highlight the key idea in the final 5s. |
| 5 | 2:08 | 35 | Let's look at an event. Event name, like purchase. Event time, a Unix timestamp in seconds. An event ID, ideally your order ID. The action source, for example website or physical store. The page URL. Then user data: email, phone and your customer ID, all normalized and hashed with SHA two fifty-six, plus IP address, user agent and Meta's own browser identifiers, f b p and f b c, which are sent unhashed. Finally custom data: currency, value and order ID. | Event anatomy<br>• event_name, event_time, event_id<br>• action_source, event_source_url<br>• user_data: hashed IDs + IP, UA, fbp, fbc<br>• custom_data: currency, value, order_id | Layout: The JSON payload from the lesson appears on screen; each field highlights as it is named, hashed fields shown in one color, unhashed in another. Timing (35s): title card 0–3s; then the 4 on-screen points build in narration order about every 6s, each animating its element; hold and highlight the key idea in the final 5s. |
| 6 | 2:43 | 46 | Here's a simple worked example of deduplication. Order number ten forty-two is placed at 7:15 in the evening. The browser Pixel fires Purchase with event ID order underscore ten forty-two. Two seconds later, the server sends Purchase through the Conversions API with the same event ID, plus hashed email and phone. Meta sees two events with the same name and ID, keeps one, and enriches its matching with the server's data. Now imagine the developer had used a random ID in the browser. Meta would count two purchases, cost per purchase would look half as high as reality, and the algorithm would scale the wrong things. | Simple example: order 1042<br>• Pixel: Purchase, ID order_1042<br>• CAPI: Purchase, ID order_1042 + hashed data<br>• Meta keeps one, better matching<br>• Random IDs = double counting | Layout: two event cards arriving from left (browser) and right (server) with matching ID tags; they merge into one card with a green check. Replay with mismatched IDs: two cards stack with a red '×2' badge. |
| 7 | 3:29 | 35 | Deduplication is where most setups go wrong. When the Pixel and the server both send the same purchase, Meta removes the duplicate only if the event name matches and the server's event ID equals the browser's event ID. So generate one ID per real conversion, the order ID is perfect, and pass it to both. If the IDs differ, Meta counts two purchases, your cost per purchase looks half what it really is, and the algorithm scales the wrong things. | Deduplication<br>• Same event_name<br>• Server event_id = browser eventID<br>• Use the order ID<br>• Mismatch = double counting | Layout: Two identical purchase tokens with ID tags; when tags match they merge into one, when they differ both are counted with a warning. Timing (35s): title card 0–3s; then the 4 on-screen points build in narration order about every 6s, each animating its element; hold and highlight the key idea in the final 5s. |
| 8 | 4:04 | 36 | Now the code. Read the pixel ID, access token and Graph API version from environment variables, never hard-coded. Check advertising consent first, and if the user refused, don't send. Build the event, hash the email, phone and customer ID, forward IP, user agent, f b p and f b c, and remove any empty fields. While testing, include a test event code. Post to the events endpoint, and raise an error with the response body if it fails, so problems are visible. | Direct integration in Python<br>• Secrets in environment variables<br>• Consent check first<br>• Hash, forward, clean<br>• Test event code<br>• Fail loudly | Layout: Code editor scrolling through the Python function, with the consent check line and error handling line highlighted. Timing (36s): title card 0–3s; then the 5 on-screen points build in narration order about every 5s, each animating its element; hold and highlight the key idea in the final 5s. |
| 9 | 4:40 | 30 | How do you know it's working? Events Manager gives you three tools. The Test Events tab shows events arriving in real time when you use the test code. Diagnostics flags problems like missing deduplication keys or invalid parameters. And Event Match Quality scores how well your customer information can be matched. Improve it by adding hashed email and phone, your customer ID, and the browser identifiers, wherever that's lawful. | Verify and improve<br>• Test Events (real time)<br>• Diagnostics<br>• Event Match Quality<br>• Add lawful identifiers | Layout: Events Manager style mock with three tabs; an EMQ gauge moves from 'poor' to 'good'. Timing (30s): title card 0–3s; then the 4 on-screen points build in narration order about every 5s, each animating its element; hold and highlight the key idea in the final 5s. |
| 10 | 5:10 | 34 | Here's an illustrative example. A Jeddah abaya brand ran the Pixel alone, which was blocked for a meaningful share of Safari and in-app browser traffic, and their match quality was poor. They added the Conversions API from their order system, using order IDs for deduplication, hashed email and phone captured at checkout, and the browser identifiers forwarded. Match quality improved, reported purchases moved closer to real orders, and they verified deduplication by comparing Events Manager counts with their backend. | Example: Jeddah abaya brand (illustrative)<br>• Pixel-only, poor EMQ<br>• CAPI from order system<br>• Order IDs for dedup<br>• Reported ≈ backend | Layout: Two bar charts converging: platform-reported purchases rising toward backend orders over weeks. Timing (34s): title card 0–3s; then the 4 on-screen points build in narration order about every 6s, each animating its element; hold and highlight the key idea in the final 5s. |
| 11 | 5:44 | 24 | Four pitfalls to avoid. Different IDs in browser and server events. Hashing the IP address or user agent, which must be sent as they are. Sending events for users who refused advertising consent. And sending sensitive data, like health conditions or financial details, in custom data or URLs, which Meta's terms prohibit and regulators punish. | Pitfalls<br>• Mismatched IDs<br>• Hashing IP/UA<br>• Ignoring consent<br>• Sensitive data | Layout: Four warning cards, each flipping to reveal the fix. Timing (24s): title card 0–3s; then the 4 on-screen points build in narration order about every 4s, each animating its element; hold and highlight the key idea in the final 5s. |
| 12 | 6:08 | 38 | Common Conversions API mistakes. Different IDs in browser and server events. Hashing the IP address or user agent, which must be sent as they are. Using the server's own IP instead of the customer's original IP. Leaving the test event code in production. And sending events for users who declined advertising consent. Try this now: open Events Manager, go to your purchase event, and check the deduplication and match quality indicators. Write down the score and the top recommendation Meta gives. That recommendation is usually your quickest win. | Mistakes + try this now<br>• Mismatched IDs<br>• Hashing IP/UA<br>• Server IP instead of user IP<br>• Test code in production<br>• Ignoring consent<br>• Try: check EMQ + dedup in Events Manager | Layout: Events Manager-style event detail card showing a match quality gauge and a recommendation list; the top recommendation highlights. |
| 13 | 6:46 | 89 | Watch me do it. Let's test a Conversions API integration for an illustrative Riyadh bookstore before going live. Step one, in Events Manager I copy the test event code and set it as an environment variable in staging. Step two, I place a test order. In Test Events, two purchase events appear within seconds: one from the browser and one from the server, both with event ID order underscore five five one two. The server one shows as deduplicated, so the IDs match. Step three, I open the server event's details and check parameters: hashed email present, hashed phone present, external ID present, client IP looks like a real user address, not our server's, and f b p is present, but f b c is missing. Why? This order didn't come from an ad click, so there was no f b c l i d. That's expected. Step four, I place an order through a URL with a test f b c l i d, and now f b c appears. Step five, I remove the test code, deploy to production, and after a few days check Event Match Quality and the dedup indicator. Only then do I compare Meta's purchases with backend orders. | Watch me do it: testing CAPI (illustrative)<br>• Test code in staging env<br>• Browser + server events, same ID, deduplicated<br>• Check hashed IDs, real client IP<br>• fbc only with an ad click<br>• Remove test code, monitor EMQ | Layout: Test Events mock showing an event list with a detail drawer. Timing: two events appear ~20s; 'deduplicated' badge ~35s; parameter checklist ticks one by one ~50–90s, fbc row amber then green after the second test ~110s; final 15s show an EMQ gauge. |
| 14 | 8:15 | 30 | Recap. Run the Conversions API alongside the Pixel. Deduplicate with a shared event ID. Hash personal identifiers, send the technical ones unhashed. Verify with Test Events, Diagnostics and match quality. And always respect consent. Your next step: map your purchase flow, from where the order ID is created, to how it reaches both the Pixel and the server event, which identifiers you can lawfully send, and where consent is checked. | Recap and next step<br>• Pixel + CAPI, deduplicated<br>• Hash correctly<br>• Verify<br>• Map your purchase flow | Layout: A flowchart from checkout to order ID to two outputs, Pixel and server, labeled with the same ID. Timing (30s): title card 0–3s; then the 4 on-screen points build in narration order about every 5s, each animating its element; hold and highlight the key idea in the final 5s. |

<details><summary>Narration per scene (paste into ElevenLabs)</summary>

Scene 1 — Meta Conversions API

```text
If your Meta ads report far fewer purchases than your store actually made, you're not just losing reporting. You're starving the algorithm. In this lecture you'll learn how the Meta Conversions API fills that gap: the implementation routes, the anatomy of an event, how deduplication really works, and how to use Event Match Quality and diagnostics to prove it's working.
```

Scene 2 — Why it matters

```text
Why does this matter? Because Meta's delivery system learns from the conversions it receives, and if it only sees part of your sales, it learns a partial lesson. Here's an analogy. Imagine coaching a sprinter while only seeing some of their races. You'd give advice based on an incomplete picture. The Pixel is watching from the stands, and sometimes the view is blocked. The Conversions API is the official race results sent straight from the timing system. Use both, make sure each race is only counted once, and the coach finally sees the whole season.
```

Scene 3 — Pixel + CAPI

```text
The Conversions API sends events from your server straight to Meta. Meta recommends running it alongside the Pixel, not instead of it, with deduplication. The browser Pixel captures rich context. The server fills gaps when browsers block the Pixel, and it can carry stronger matching data from your order system. Together, more of your real conversions reach Meta, and they're easier to match to the people who saw your ads.
```

Scene 4 — Four routes

```text
There are four ways to implement it. Partner integrations, like Shopify's, are the fastest, but check which events and parameters they actually send. The Conversions API Gateway is a Meta-provided, self-hosted option. Server-side Tag Manager can run Meta's Conversions API tag template. Or you integrate directly from your backend through the Graph API. The lesson shows the direct route in Python, because understanding it makes every other route easier to debug.
```

Scene 5 — Event anatomy

```text
Let's look at an event. Event name, like purchase. Event time, a Unix timestamp in seconds. An event ID, ideally your order ID. The action source, for example website or physical store. The page URL. Then user data: email, phone and your customer ID, all normalized and hashed with SHA two fifty-six, plus IP address, user agent and Meta's own browser identifiers, f b p and f b c, which are sent unhashed. Finally custom data: currency, value and order ID.
```

Scene 6 — Simple example: order 1042

```text
Here's a simple worked example of deduplication. Order number ten forty-two is placed at 7:15 in the evening. The browser Pixel fires Purchase with event ID order underscore ten forty-two. Two seconds later, the server sends Purchase through the Conversions API with the same event ID, plus hashed email and phone. Meta sees two events with the same name and ID, keeps one, and enriches its matching with the server's data. Now imagine the developer had used a random ID in the browser. Meta would count two purchases, cost per purchase would look half as high as reality, and the algorithm would scale the wrong things.
```

Scene 7 — Deduplication

```text
Deduplication is where most setups go wrong. When the Pixel and the server both send the same purchase, Meta removes the duplicate only if the event name matches and the server's event ID equals the browser's event ID. So generate one ID per real conversion, the order ID is perfect, and pass it to both. If the IDs differ, Meta counts two purchases, your cost per purchase looks half what it really is, and the algorithm scales the wrong things.
```

Scene 8 — Direct integration in Python

```text
Now the code. Read the pixel ID, access token and Graph API version from environment variables, never hard-coded. Check advertising consent first, and if the user refused, don't send. Build the event, hash the email, phone and customer ID, forward IP, user agent, f b p and f b c, and remove any empty fields. While testing, include a test event code. Post to the events endpoint, and raise an error with the response body if it fails, so problems are visible.
```

Scene 9 — Verify and improve

```text
How do you know it's working? Events Manager gives you three tools. The Test Events tab shows events arriving in real time when you use the test code. Diagnostics flags problems like missing deduplication keys or invalid parameters. And Event Match Quality scores how well your customer information can be matched. Improve it by adding hashed email and phone, your customer ID, and the browser identifiers, wherever that's lawful.
```

Scene 10 — Example: Jeddah abaya brand (illustrative)

```text
Here's an illustrative example. A Jeddah abaya brand ran the Pixel alone, which was blocked for a meaningful share of Safari and in-app browser traffic, and their match quality was poor. They added the Conversions API from their order system, using order IDs for deduplication, hashed email and phone captured at checkout, and the browser identifiers forwarded. Match quality improved, reported purchases moved closer to real orders, and they verified deduplication by comparing Events Manager counts with their backend.
```

Scene 11 — Pitfalls

```text
Four pitfalls to avoid. Different IDs in browser and server events. Hashing the IP address or user agent, which must be sent as they are. Sending events for users who refused advertising consent. And sending sensitive data, like health conditions or financial details, in custom data or URLs, which Meta's terms prohibit and regulators punish.
```

Scene 12 — Mistakes + try this now

```text
Common Conversions API mistakes. Different IDs in browser and server events. Hashing the IP address or user agent, which must be sent as they are. Using the server's own IP instead of the customer's original IP. Leaving the test event code in production. And sending events for users who declined advertising consent. Try this now: open Events Manager, go to your purchase event, and check the deduplication and match quality indicators. Write down the score and the top recommendation Meta gives. That recommendation is usually your quickest win.
```

Scene 13 — Watch me do it: testing CAPI (illustrative)

```text
Watch me do it. Let's test a Conversions API integration for an illustrative Riyadh bookstore before going live. Step one, in Events Manager I copy the test event code and set it as an environment variable in staging. Step two, I place a test order. In Test Events, two purchase events appear within seconds: one from the browser and one from the server, both with event ID order underscore five five one two. The server one shows as deduplicated, so the IDs match. Step three, I open the server event's details and check parameters: hashed email present, hashed phone present, external ID present, client IP looks like a real user address, not our server's, and f b p is present, but f b c is missing. Why? This order didn't come from an ad click, so there was no f b c l i d. That's expected. Step four, I place an order through a URL with a test f b c l i d, and now f b c appears. Step five, I remove the test code, deploy to production, and after a few days check Event Match Quality and the dedup indicator. Only then do I compare Meta's purchases with backend orders.
```

Scene 14 — Recap and next step

```text
Recap. Run the Conversions API alongside the Pixel. Deduplicate with a shared event ID. Hash personal identifiers, send the technical ones unhashed. Verify with Test Events, Diagnostics and match quality. And always respect consent. Your next step: map your purchase flow, from where the order ID is created, to how it reaches both the Pixel and the server event, which identifiers you can lawfully send, and where consent is checked.
```

</details>

### 8. Google enhanced conversions, offline imports and Data Manager

Lesson `google-enhanced-conversions-and-offline` · module “Conversion APIs: Meta, Google, TikTok and LinkedIn” · target 8 min · 13 scenes · 1161 words · planned 8:24 (words ≈ 8:25) · status: **to produce**

| Term | Say |
|---|---|
| gclid | G click I D |
| gbraid | G braid |
| wbraid | W braid |
| CRM | C R M |
| E.164 | E one sixty-four |

| # | Time | Sec | Narration | On screen | Visual |
|---|---|---|---|---|---|
| 1 | 0:00 | 31 | Google Ads can only optimize toward what it can see. If your real wins happen in a CRM weeks after the click, or your conversion tag misses a chunk of buyers, the bidding is flying half blind. In this lecture you'll learn Google's conversion toolkit: enhanced conversions for web and for leads, offline imports with click IDs, and where Data Manager fits, with code for each and the consent rules that apply. | Google's conversion toolkit<br>• Enhanced conversions (web, leads)<br>• Offline imports<br>• Data Manager<br>• Consent in uploads | Layout: A Google Ads-style bidding engine (generic) with a blindfold that gets removed as data pipes connect. Timing (31s): title card 0–3s; then the 4 on-screen points build in narration order about every 5s, each animating its element; hold and highlight the key idea in the final 5s. |
| 2 | 0:31 | 41 | Why does this matter? Because for many businesses the most valuable outcomes happen after the click, on the phone, in the showroom or in the CRM, and Google can't bid for what it can't see. Here's an analogy. It's like a fishing guide who's only told when a fish bites, never whether it was landed or how big it was. They'll take you to spots with lots of bites, even if every fish gets away. Offline conversions and enhanced conversions tell the guide which spots produced the big catches, so the next trip goes there. | Why it matters<br>• Real outcomes happen after the click<br>• Bites vs landed fish<br>• Tell the guide where the big catches were | Layout: lake with several fishing spots; some spots show many 'bite' ripples but no fish landed; one spot shows fewer bites but large fish icons; a guide boat reroutes to it. |
| 3 | 1:12 | 33 | Here's the toolkit. Enhanced conversions for web sends hashed first-party data, like email or phone, with your conversion tag. Enhanced conversions for leads captures hashed data when a form is submitted, so you can later upload that lead's outcome. Offline conversion import uploads conversions with a click ID. Store sales imports cover retail transactions. Tag gateway and server-side Tag Manager improve delivery. And Data Manager in Google Ads helps connect CRM and warehouse sources and manage uploads. | The toolkit<br>• Enhanced conversions for web<br>• Enhanced conversions for leads<br>• Click-ID offline import<br>• Store sales<br>• Tag gateway / server GTM<br>• Data Manager | Layout: Toolbox opening with six labeled tools sliding out. Timing (33s): title card 0–3s; then the 6 on-screen points build in narration order about every 4s, each animating its element; hold and highlight the key idea in the final 5s. |
| 4 | 1:45 | 34 | Enhanced conversions for web is straightforward with gtag. On the confirmation page, after the user has granted consent for ads measurement, you set user data, email, phone in international format, and name and address fields if you have them. Google's tag normalizes and hashes the values, or you can send them pre-hashed. Then the conversion event fires with value, currency and transaction ID. In Tag Manager, use a data layer variable for user-provided data rather than automatic detection. | Enhanced conversions for web<br>• Set user_data before conversion<br>• Email, phone (E.164), name, address<br>• Tag hashes (or pre-hash)<br>• Prefer data layer in GTM | Layout: Code editor showing the gtag set user_data and conversion event snippet, with the order of lines highlighted. Timing (34s): title card 0–3s; then the 4 on-screen points build in narration order about every 6s, each animating its element; hold and highlight the key idea in the final 5s. |
| 5 | 2:19 | 38 | Now offline imports. Step one, capture the click ID from the landing URL: g c l i d, or g braid and w braid in some iOS contexts. Store it with the lead in your CRM. Step two, when that lead reaches a meaningful stage, like qualified or closed-won, upload a conversion with the click ID, the conversion action, the time with its time zone, the value and the currency. Step three, choose your upload method: scheduled files, a CRM integration, Data Manager, or the Google Ads API. | Offline import<br>• Capture and store click ID<br>• Upload stage + time zone + value<br>• Methods: files, CRM, Data Manager, API | Layout: Flow diagram: landing URL with a highlighted gclid, CRM record storing it, later an upload arrow back to Google Ads. Timing (38s): title card 0–3s; then the 3 on-screen points build in narration order about every 10s, each animating its element; hold and highlight the key idea in the final 5s. |
| 6 | 2:57 | 49 | Here's a simple worked example. An Islamabad car dealership gets leads from Google Search. When someone submits a test-drive form, the page stores the gclid from the URL and the email in the CRM, and the tag captures hashed email for enhanced conversions for leads. Two weeks later, the salesperson marks the lead as test drive completed. The nightly upload sends that stage to Google with the gclid, the time with the correct time zone offset, and a value based on how often test drives turn into sales. A month later, a car is sold, and a second conversion with the actual margin is uploaded. Google now knows which searches sell cars. | Simple example: Islamabad car dealer (illustrative)<br>• Form stores gclid + hashed email<br>• Stage 1: test drive completed (valued)<br>• Stage 2: car sold (actual margin)<br>• Nightly uploads with time zone | Layout: horizontal pipeline: search ad → form (gclid chip saved) → CRM card moving through stages → upload arrow back to Google Ads with value badges. |
| 7 | 3:46 | 37 | The lesson shows an upload with the official Google Ads Python client. Credentials come from environment variables. You build a click conversion with the stored g c l i d, the conversion action path, a date time with offset, the value and currency, and the consent status for ad user data. Turn on partial failure, so one bad row doesn't block the rest, and always print partial failure messages. Google has been consolidating first-party data ingestion under Data Manager, so check the current docs. | Upload with the API<br>• Credentials from env vars<br>• ClickConversion with gclid<br>• Time with offset, value, currency<br>• consent.ad_user_data<br>• partial_failure on | Layout: Code editor with the Python upload script; a response panel shows a partial failure message for one row. Timing (37s): title card 0–3s; then the 5 on-screen points build in narration order about every 5s, each animating its element; hold and highlight the key idea in the final 5s. |
| 8 | 4:23 | 31 | What if click IDs get lost, say your form is on a different domain, or a browser strips parameters? That's where enhanced conversions for leads shines. When the form is submitted, the tag captures hashed email or phone, with consent. When the lead is qualified in your CRM, you upload the conversion matched on that same hashed data, no click ID required. Google matches it back to the original ad interaction. | Enhanced conversions for leads<br>• Hash email/phone at form submit<br>• Upload qualified outcome on same hash<br>• No click ID required | Layout: Two-step animation: form submit captures a hashed token; weeks later a CRM event with the same token links back to an ad click. Timing (31s): title card 0–3s; then the 3 on-screen points build in narration order about every 7s, each animating its element; hold and highlight the key idea in the final 5s. |
| 9 | 4:54 | 34 | Here's an illustrative example. A Dubai real-estate brokerage gets leads from Search and Performance Max, but deals close offline weeks later. They added enhanced conversions for leads to the enquiry form, stored the click ID and hashed email in the CRM, and uploaded two stages daily: viewing booked, valued at expected commission times the viewing-to-deal rate, and deal closed, with the actual commission. Once volume built up, they moved to value-based bidding, and campaigns stopped chasing low-intent enquiries. | Example: Dubai brokerage (illustrative)<br>• Enhanced conversions for leads<br>• Upload: viewing booked, deal closed<br>• Stage values from commission<br>• Then value-based bidding | Layout: A property listing card, a calendar with a viewing booked, and a handshake icon; each sends a valued conversion arrow back to Google Ads. Timing (34s): title card 0–3s; then the 4 on-screen points build in narration order about every 6s, each animating its element; hold and highlight the key idea in the final 5s. |
| 10 | 5:28 | 27 | Four common failures. Uploading conversions older than the conversion action's click-through window, which get rejected. Time zone formats that don't match, causing errors. Primary and secondary conversion actions set up wrongly, so bidding optimizes for the wrong goal. And uploading data for users who didn't consent, where consent is required. Check the Diagnostics tab weekly for enhanced conversion coverage and upload success. | Common failures<br>• Too-old conversions<br>• Time zone format errors<br>• Wrong primary actions<br>• Missing consent<br>• Check Diagnostics weekly | Layout: A diagnostics panel with four red rows turning green one by one. Timing (27s): title card 0–3s; then the 5 on-screen points build in narration order about every 3s, each animating its element; hold and highlight the key idea in the final 5s. |
| 11 | 5:55 | 41 | Common mistakes with Google's conversion tools. Uploading conversions older than the conversion action's window. Wrong time zone formats that get rows rejected. Marking every stage as primary, so bidding tries to optimize for all of them at once. Losing the gclid because a form redirect strips URL parameters. And skipping consent status in uploads for European users. Try this now: submit a test lead on your own site with a test gclid in the URL, then check your CRM. Is the click ID stored on the lead record? If not, nothing downstream can work. | Mistakes + try this now<br>• Stale uploads<br>• Time zone errors<br>• Everything primary<br>• Lost gclid on redirects<br>• Missing consent status<br>• Try: test-lead gclid check | Layout: a URL bar with a test gclid parameter; a form submits; a CRM record opens and a field labeled 'gclid' either fills (green) or stays empty (red). |
| 12 | 6:36 | 83 | Watch me do it. Let's check that an illustrative Lahore property developer's lead loop actually works. Step one, I click our own ad in a test, or add a test g c l i d to the landing URL, and submit the enquiry form with a test email. Step two, in the CRM, I open the new lead: the g c l i d field is filled, and the source shows paid search. Step three, in Google Ads, the enhanced conversions diagnostics for the lead form show user-provided data detected. Step four, I move the lead to site visit booked in the CRM. At the nightly upload, a conversion is sent with the g c l i d, the conversion action site visit, the time with the plus five hours Pakistan offset, a value of our expected revenue for that stage, and consent status. Step five, next morning, the uploads page shows the file processed with one success and zero errors. Step six, I check the conversion action's settings: site visit is primary, form submit is now secondary. Within weeks, bidding learns which searches lead to site visits, not just form fills. | Watch me do it: lead loop check (illustrative)<br>• Test click → form → CRM gclid stored<br>• Enhanced conversions detected<br>• Stage upload with +05:00 offset<br>• Upload report: 1 success<br>• Site visit primary, form secondary | Layout: four-stage horizontal flow: ad click, form, CRM record, Google Ads upload report. Timing: each stage opens ~25s apart with the relevant field highlighted; the time offset '+05:00' pulses at ~85s; final 20s show primary/secondary toggles switching. |
| 13 | 7:59 | 25 | Recap. Enhanced conversions add hashed first-party data to Google conversions. Enhanced conversions for leads and click-ID imports close the loop with your CRM. Store click IDs, upload stages with values and proper time zones, include consent, and monitor diagnostics. Your next step: design your offline loop, the stages, values, storage, upload method, schedule, and how consent is recorded. | Recap and next step<br>• Enhanced conversions<br>• Offline loop<br>• Consent in uploads<br>• Design your loop | Layout: A circular loop diagram from ad click to CRM to upload and back to bidding. Timing (25s): title card 0–3s; then the 4 on-screen points build in narration order about every 4s, each animating its element; hold and highlight the key idea in the final 5s. |

<details><summary>Narration per scene (paste into ElevenLabs)</summary>

Scene 1 — Google's conversion toolkit

```text
Google Ads can only optimize toward what it can see. If your real wins happen in a CRM weeks after the click, or your conversion tag misses a chunk of buyers, the bidding is flying half blind. In this lecture you'll learn Google's conversion toolkit: enhanced conversions for web and for leads, offline imports with click IDs, and where Data Manager fits, with code for each and the consent rules that apply.
```

Scene 2 — Why it matters

```text
Why does this matter? Because for many businesses the most valuable outcomes happen after the click, on the phone, in the showroom or in the CRM, and Google can't bid for what it can't see. Here's an analogy. It's like a fishing guide who's only told when a fish bites, never whether it was landed or how big it was. They'll take you to spots with lots of bites, even if every fish gets away. Offline conversions and enhanced conversions tell the guide which spots produced the big catches, so the next trip goes there.
```

Scene 3 — The toolkit

```text
Here's the toolkit. Enhanced conversions for web sends hashed first-party data, like email or phone, with your conversion tag. Enhanced conversions for leads captures hashed data when a form is submitted, so you can later upload that lead's outcome. Offline conversion import uploads conversions with a click ID. Store sales imports cover retail transactions. Tag gateway and server-side Tag Manager improve delivery. And Data Manager in Google Ads helps connect CRM and warehouse sources and manage uploads.
```

Scene 4 — Enhanced conversions for web

```text
Enhanced conversions for web is straightforward with gtag. On the confirmation page, after the user has granted consent for ads measurement, you set user data, email, phone in international format, and name and address fields if you have them. Google's tag normalizes and hashes the values, or you can send them pre-hashed. Then the conversion event fires with value, currency and transaction ID. In Tag Manager, use a data layer variable for user-provided data rather than automatic detection.
```

Scene 5 — Offline import

```text
Now offline imports. Step one, capture the click ID from the landing URL: g c l i d, or g braid and w braid in some iOS contexts. Store it with the lead in your CRM. Step two, when that lead reaches a meaningful stage, like qualified or closed-won, upload a conversion with the click ID, the conversion action, the time with its time zone, the value and the currency. Step three, choose your upload method: scheduled files, a CRM integration, Data Manager, or the Google Ads API.
```

Scene 6 — Simple example: Islamabad car dealer (illustrative)

```text
Here's a simple worked example. An Islamabad car dealership gets leads from Google Search. When someone submits a test-drive form, the page stores the gclid from the URL and the email in the CRM, and the tag captures hashed email for enhanced conversions for leads. Two weeks later, the salesperson marks the lead as test drive completed. The nightly upload sends that stage to Google with the gclid, the time with the correct time zone offset, and a value based on how often test drives turn into sales. A month later, a car is sold, and a second conversion with the actual margin is uploaded. Google now knows which searches sell cars.
```

Scene 7 — Upload with the API

```text
The lesson shows an upload with the official Google Ads Python client. Credentials come from environment variables. You build a click conversion with the stored g c l i d, the conversion action path, a date time with offset, the value and currency, and the consent status for ad user data. Turn on partial failure, so one bad row doesn't block the rest, and always print partial failure messages. Google has been consolidating first-party data ingestion under Data Manager, so check the current docs.
```

Scene 8 — Enhanced conversions for leads

```text
What if click IDs get lost, say your form is on a different domain, or a browser strips parameters? That's where enhanced conversions for leads shines. When the form is submitted, the tag captures hashed email or phone, with consent. When the lead is qualified in your CRM, you upload the conversion matched on that same hashed data, no click ID required. Google matches it back to the original ad interaction.
```

Scene 9 — Example: Dubai brokerage (illustrative)

```text
Here's an illustrative example. A Dubai real-estate brokerage gets leads from Search and Performance Max, but deals close offline weeks later. They added enhanced conversions for leads to the enquiry form, stored the click ID and hashed email in the CRM, and uploaded two stages daily: viewing booked, valued at expected commission times the viewing-to-deal rate, and deal closed, with the actual commission. Once volume built up, they moved to value-based bidding, and campaigns stopped chasing low-intent enquiries.
```

Scene 10 — Common failures

```text
Four common failures. Uploading conversions older than the conversion action's click-through window, which get rejected. Time zone formats that don't match, causing errors. Primary and secondary conversion actions set up wrongly, so bidding optimizes for the wrong goal. And uploading data for users who didn't consent, where consent is required. Check the Diagnostics tab weekly for enhanced conversion coverage and upload success.
```

Scene 11 — Mistakes + try this now

```text
Common mistakes with Google's conversion tools. Uploading conversions older than the conversion action's window. Wrong time zone formats that get rows rejected. Marking every stage as primary, so bidding tries to optimize for all of them at once. Losing the gclid because a form redirect strips URL parameters. And skipping consent status in uploads for European users. Try this now: submit a test lead on your own site with a test gclid in the URL, then check your CRM. Is the click ID stored on the lead record? If not, nothing downstream can work.
```

Scene 12 — Watch me do it: lead loop check (illustrative)

```text
Watch me do it. Let's check that an illustrative Lahore property developer's lead loop actually works. Step one, I click our own ad in a test, or add a test g c l i d to the landing URL, and submit the enquiry form with a test email. Step two, in the CRM, I open the new lead: the g c l i d field is filled, and the source shows paid search. Step three, in Google Ads, the enhanced conversions diagnostics for the lead form show user-provided data detected. Step four, I move the lead to site visit booked in the CRM. At the nightly upload, a conversion is sent with the g c l i d, the conversion action site visit, the time with the plus five hours Pakistan offset, a value of our expected revenue for that stage, and consent status. Step five, next morning, the uploads page shows the file processed with one success and zero errors. Step six, I check the conversion action's settings: site visit is primary, form submit is now secondary. Within weeks, bidding learns which searches lead to site visits, not just form fills.
```

Scene 13 — Recap and next step

```text
Recap. Enhanced conversions add hashed first-party data to Google conversions. Enhanced conversions for leads and click-ID imports close the loop with your CRM. Store click IDs, upload stages with values and proper time zones, include consent, and monitor diagnostics. Your next step: design your offline loop, the stages, values, storage, upload method, schedule, and how consent is recorded.
```

</details>

### 9. TikTok Events API and LinkedIn Conversions API

Lesson `tiktok-events-api-and-linkedin-capi` · module “Conversion APIs: Meta, Google, TikTok and LinkedIn” · target 8 min · 13 scenes · 1134 words · planned 8:15 (words ≈ 8:13) · status: **to produce**

| Term | Say |
|---|---|
| ttclid | T T click I D |
| li_fat_id | L I fat I D |
| URN | U R N |
| Rest.li | rest dot L I |
| SaaS | sass |

| # | Time | Sec | Narration | On screen | Visual |
|---|---|---|---|---|---|
| 1 | 0:00 | 27 | Once you've built one conversions API, the next two are mostly a matter of field names. In this lecture you'll learn how TikTok's Events API and LinkedIn's Conversions API follow the same pattern as Meta, the specific fields and gotchas each one has, and how a B2B company can use them to optimize for qualified pipeline instead of raw form fills. | TikTok Events API and LinkedIn CAPI<br>• Same pattern<br>• TikTok specifics<br>• LinkedIn specifics<br>• B2B example | Layout: Three server output pipes labeled with generic platform shapes, sharing the same connector design. Timing (27s): title card 0–3s; then the 4 on-screen points build in narration order about every 4s, each animating its element; hold and highlight the key idea in the final 5s. |
| 2 | 0:27 | 41 | Why does this matter? Because TikTok and LinkedIn often carry very different parts of your funnel. TikTok frequently drives discovery and impulse purchases, LinkedIn drives slow, high-value B2B pipeline. Both suffer when browser tracking is weak. Here's an analogy. If Meta's Conversions API is learning to drive one car, TikTok and LinkedIn are the same skill in different cars. The pedals and mirrors are in slightly different places, the timestamps are in different units, and the dashboards read differently. But once you know how to drive, switching cars takes an afternoon, not a month. | Why it matters<br>• TikTok: discovery + impulse<br>• LinkedIn: slow, high-value pipeline<br>• Same skill, different cars | Layout: three car silhouettes side by side labeled Meta, TikTok, LinkedIn; their dashboards zoom in to show similar controls in slightly different positions. |
| 3 | 1:08 | 35 | The pattern is the same everywhere. Send events from your server. Hash personal identifiers like email and phone. Include the platform's click ID and browser identifiers. And deduplicate with the browser tag by sharing an event ID. For TikTok, the browser tag is the TikTok Pixel, the click ID is t t c l i d, and the browser cookie is underscore t t p. For LinkedIn, it's the Insight Tag, and the click identifier is l i fat id. | The shared pattern<br>• Server events<br>• Hashed identifiers<br>• Click IDs + browser IDs<br>• Shared event ID for dedup | Layout: A four-part template card; TikTok and LinkedIn tags slot their specific field names into each part. Timing (35s): title card 0–3s; then the 4 on-screen points build in narration order about every 6s, each animating its element; hold and highlight the key idea in the final 5s. |
| 4 | 1:43 | 40 | Let's do TikTok first. The Events API endpoint, version one point three at the time of writing, takes an event source of web, your Pixel code as the event source ID, and a list of events. Each event has a name, like complete payment, a timestamp in seconds, an event ID, a user object with hashed email, phone and external ID plus t t c l i d, t t p, IP and user agent, the page URL, and properties like currency, value and contents. The access token goes in a header. | TikTok Events API<br>• event_source: web, event_source_id: Pixel code<br>• event, event_time (s), event_id<br>• user: hashed IDs, ttclid, ttp, IP, UA<br>• properties: currency, value, contents | Layout: The TikTok payload structure appears as a tree diagram with each node lighting up. Timing (40s): title card 0–3s; then the 4 on-screen points build in narration order about every 8s, each animating its element; hold and highlight the key idea in the final 5s. |
| 5 | 2:23 | 30 | Two TikTok gotchas. The API can return an HTTP success while the body contains an error code, so check that the response code field equals zero, not just the HTTP status. And capture t t c l i d from the landing URL and store it first-party, because by the time the purchase happens, the parameter is long gone from the address bar. The lesson's Python function handles both. | TikTok gotchas<br>• Check body code == 0<br>• Capture ttclid at landing<br>• Store first-party | Layout: An HTTP 200 badge next to a JSON body showing a non-zero code in red; then a landing URL with ttclid highlighted being saved to a cookie. Timing (30s): title card 0–3s; then the 3 on-screen points build in narration order about every 7s, each animating its element; hold and highlight the key idea in the final 5s. |
| 6 | 2:53 | 47 | Here's a simple worked example on LinkedIn. A Dubai recruitment software company sends a demo request event when someone books a demo. Two weeks later, sales qualifies the lead. The CRM triggers a LinkedIn Conversions API event for the sales qualified lead rule, with the hashed work email, the first-party tracking ID captured at landing, the company name, the country code, a value based on historical close rates, and a timestamp in milliseconds. The event ID is lead, the lead number, then SQL. LinkedIn attributes it to the campaign that drove the original click, and the campaign starts optimizing toward qualified leads rather than every form fill. | Simple example: Dubai HR software (illustrative)<br>• Demo request → later SQL<br>• CAPI: hashed email + li_fat_id<br>• Company, country, value, ms timestamp<br>• eventId: lead_####_sql | Layout: CRM stage bar moving from 'demo' to 'SQL'; a JSON event card assembles field by field; an arrow sends it to a LinkedIn-style campaign card that updates its optimization goal. |
| 7 | 3:40 | 36 | Now LinkedIn. You create a conversion rule in Campaign Manager, then stream events to the conversion events REST endpoint with an OAuth token. Every request needs a LinkedIn version header, year and month, plus the Rest.li protocol header. Each event references the conversion rule by its URN, has a timestamp in milliseconds, a value with currency, user IDs like the SHA two fifty-six email and the first-party tracking ID, user info like name, company and country, and an event ID for dedup. | LinkedIn Conversions API<br>• Conversion rule URN<br>• Headers: LinkedIn-Version, Rest.li 2.0.0<br>• Timestamp in milliseconds<br>• SHA256_EMAIL + li_fat_id<br>• eventId | Layout: Request anatomy diagram with headers on top and body fields below, the millisecond timestamp highlighted. Timing (36s): title card 0–3s; then the 5 on-screen points build in narration order about every 5s, each animating its element; hold and highlight the key idea in the final 5s. |
| 8 | 4:16 | 24 | LinkedIn's classic mistake is the timestamp. Send seconds instead of milliseconds and your conversion lands in nineteen seventy. The other classic is an unversioned or deprecated API version, which starts failing when LinkedIn retires it. Keep the version in configuration, not code, and check the Microsoft Learn documentation for LinkedIn's current versions and rate limits. | LinkedIn gotchas<br>• Milliseconds, not seconds<br>• Keep version in config<br>• Check current versions and limits | Layout: A calendar flipping back to 1970 with a red X; then a config file with a version value being updated. Timing (24s): title card 0–3s; then the 3 on-screen points build in narration order about every 5s, each animating its element; hold and highlight the key idea in the final 5s. |
| 9 | 4:40 | 37 | Here's an illustrative B2B example. A UK cybersecurity software company runs LinkedIn for pipeline and TikTok for small-business awareness. On LinkedIn, the Insight Tag tracks demo requests with an event ID, and their CRM sends sales-qualified-lead and opportunity stages through the Conversions API, valued by historical close rates. On TikTok, free trial sign-ups go through the Events API, deduplicated with the Pixel. Both respect advertising consent from the consent platform. Within a quarter, LinkedIn campaigns were optimizing for qualified leads, not raw form fills. | Example: UK cybersecurity SaaS (illustrative)<br>• LinkedIn: SQL + opportunity via CAPI<br>• TikTok: trials via Events API<br>• Both consent-aware<br>• Optimizing for qualified pipeline | Layout: CRM funnel with stages; arrows labeled SQL and Opportunity go to a LinkedIn-style box, and 'Trial' goes to a TikTok-style box. Timing (37s): title card 0–3s; then the 4 on-screen points build in narration order about every 7s, each animating its element; hold and highlight the key idea in the final 5s. |
| 10 | 5:17 | 23 | How do you know it's working? Each platform has an events or conversion diagnostics page. Check events are arriving, deduplication is working, match rates are improving, and your qualified-stage conversions are the ones steering bidding. And as always, keep tokens in environment variables, rotate them, and log errors so failures are visible. | Verify<br>• Events arriving<br>• Dedup working<br>• Match rates improving<br>• Qualified stages steer bidding<br>• Secure tokens | Layout: Dashboard mock with four green check tiles and a small key icon labeled 'env var'. Timing (23s): title card 0–3s; then the 5 on-screen points build in narration order about every 3s, each animating its element; hold and highlight the key idea in the final 5s. |
| 11 | 5:40 | 40 | Common mistakes with these two APIs. Sending LinkedIn timestamps in seconds. Hard-coding an API version that later gets retired. Trusting TikTok's HTTP status without checking the response code field. Forgetting to capture ttclid or the LinkedIn tracking ID at landing. And using different event IDs in browser and server events. Try this now: pick one of the two platforms and send a single test event from a script in a test account, using the lesson's code. Read the response carefully and confirm the event appears in the platform's test or diagnostics view. | Mistakes + try this now<br>• Seconds vs milliseconds<br>• Hard-coded versions<br>• Ignoring TikTok body code<br>• Missing click IDs<br>• Mismatched event IDs<br>• Try: send one test event | Layout: terminal window running a script; the response JSON prints; a diagnostics panel shows the test event arriving with a green tick. |
| 12 | 6:20 | 84 | Watch me do it. Let's run a first TikTok Events API test for an illustrative Dubai sneaker store. Step one, in TikTok Events Manager I generate an access token for the pixel and copy the test event code. Step two, I store both as environment variables, never in code. Step three, I place a test order with a test t t c l i d in the landing URL. Our landing handler saved it in a first-party cookie, and at checkout the server reads it. Step four, the script sends a complete payment event with event ID order underscore seven seven eight, hashed email and phone, the t t c l i d, IP and user agent, and the value in dirhams. The HTTP status is two hundred, but I print the body: code zero, message OK. Step five, in Test Events, the server event appears alongside the pixel event with the same ID, and TikTok marks the duplicate. Step six, I deliberately send a malformed phone number to see what happens. The body returns a non-zero code with a clear message, and our logger records it. Now I know failures won't be silent. | Watch me do it: TikTok Events API test (illustrative)<br>• Token + test code in env vars<br>• ttclid captured, stored, read<br>• Event with shared ID order_778<br>• Check body code 0<br>• Deliberate failure logged | Layout: terminal on the left, Test Events mock on the right. Timing: env var lines type ~10s; request payload prints ~40s; response body with code 0 highlights ~70s; Test Events shows matched pair ~95s; final 25s show an error response and a log entry. |
| 13 | 7:44 | 31 | Recap. TikTok and LinkedIn use the same server-side pattern as Meta. For TikTok: Pixel code as source ID, access token header, and check the body code. For LinkedIn: versioned headers, the conversion rule URN, and milliseconds. Capture click IDs at landing. Your next step: pick one of the two, and write the event mapping for your top two conversions, including the event ID source, identifiers, value, and where click IDs are stored. | Recap and next step<br>• Same pattern<br>• TikTok + LinkedIn specifics<br>• Write your event mapping | Layout: An event mapping table template with columns filling in. Timing (31s): title card 0–3s; then the 3 on-screen points build in narration order about every 7s, each animating its element; hold and highlight the key idea in the final 5s. |

<details><summary>Narration per scene (paste into ElevenLabs)</summary>

Scene 1 — TikTok Events API and LinkedIn CAPI

```text
Once you've built one conversions API, the next two are mostly a matter of field names. In this lecture you'll learn how TikTok's Events API and LinkedIn's Conversions API follow the same pattern as Meta, the specific fields and gotchas each one has, and how a B2B company can use them to optimize for qualified pipeline instead of raw form fills.
```

Scene 2 — Why it matters

```text
Why does this matter? Because TikTok and LinkedIn often carry very different parts of your funnel. TikTok frequently drives discovery and impulse purchases, LinkedIn drives slow, high-value B2B pipeline. Both suffer when browser tracking is weak. Here's an analogy. If Meta's Conversions API is learning to drive one car, TikTok and LinkedIn are the same skill in different cars. The pedals and mirrors are in slightly different places, the timestamps are in different units, and the dashboards read differently. But once you know how to drive, switching cars takes an afternoon, not a month.
```

Scene 3 — The shared pattern

```text
The pattern is the same everywhere. Send events from your server. Hash personal identifiers like email and phone. Include the platform's click ID and browser identifiers. And deduplicate with the browser tag by sharing an event ID. For TikTok, the browser tag is the TikTok Pixel, the click ID is t t c l i d, and the browser cookie is underscore t t p. For LinkedIn, it's the Insight Tag, and the click identifier is l i fat id.
```

Scene 4 — TikTok Events API

```text
Let's do TikTok first. The Events API endpoint, version one point three at the time of writing, takes an event source of web, your Pixel code as the event source ID, and a list of events. Each event has a name, like complete payment, a timestamp in seconds, an event ID, a user object with hashed email, phone and external ID plus t t c l i d, t t p, IP and user agent, the page URL, and properties like currency, value and contents. The access token goes in a header.
```

Scene 5 — TikTok gotchas

```text
Two TikTok gotchas. The API can return an HTTP success while the body contains an error code, so check that the response code field equals zero, not just the HTTP status. And capture t t c l i d from the landing URL and store it first-party, because by the time the purchase happens, the parameter is long gone from the address bar. The lesson's Python function handles both.
```

Scene 6 — Simple example: Dubai HR software (illustrative)

```text
Here's a simple worked example on LinkedIn. A Dubai recruitment software company sends a demo request event when someone books a demo. Two weeks later, sales qualifies the lead. The CRM triggers a LinkedIn Conversions API event for the sales qualified lead rule, with the hashed work email, the first-party tracking ID captured at landing, the company name, the country code, a value based on historical close rates, and a timestamp in milliseconds. The event ID is lead, the lead number, then SQL. LinkedIn attributes it to the campaign that drove the original click, and the campaign starts optimizing toward qualified leads rather than every form fill.
```

Scene 7 — LinkedIn Conversions API

```text
Now LinkedIn. You create a conversion rule in Campaign Manager, then stream events to the conversion events REST endpoint with an OAuth token. Every request needs a LinkedIn version header, year and month, plus the Rest.li protocol header. Each event references the conversion rule by its URN, has a timestamp in milliseconds, a value with currency, user IDs like the SHA two fifty-six email and the first-party tracking ID, user info like name, company and country, and an event ID for dedup.
```

Scene 8 — LinkedIn gotchas

```text
LinkedIn's classic mistake is the timestamp. Send seconds instead of milliseconds and your conversion lands in nineteen seventy. The other classic is an unversioned or deprecated API version, which starts failing when LinkedIn retires it. Keep the version in configuration, not code, and check the Microsoft Learn documentation for LinkedIn's current versions and rate limits.
```

Scene 9 — Example: UK cybersecurity SaaS (illustrative)

```text
Here's an illustrative B2B example. A UK cybersecurity software company runs LinkedIn for pipeline and TikTok for small-business awareness. On LinkedIn, the Insight Tag tracks demo requests with an event ID, and their CRM sends sales-qualified-lead and opportunity stages through the Conversions API, valued by historical close rates. On TikTok, free trial sign-ups go through the Events API, deduplicated with the Pixel. Both respect advertising consent from the consent platform. Within a quarter, LinkedIn campaigns were optimizing for qualified leads, not raw form fills.
```

Scene 10 — Verify

```text
How do you know it's working? Each platform has an events or conversion diagnostics page. Check events are arriving, deduplication is working, match rates are improving, and your qualified-stage conversions are the ones steering bidding. And as always, keep tokens in environment variables, rotate them, and log errors so failures are visible.
```

Scene 11 — Mistakes + try this now

```text
Common mistakes with these two APIs. Sending LinkedIn timestamps in seconds. Hard-coding an API version that later gets retired. Trusting TikTok's HTTP status without checking the response code field. Forgetting to capture ttclid or the LinkedIn tracking ID at landing. And using different event IDs in browser and server events. Try this now: pick one of the two platforms and send a single test event from a script in a test account, using the lesson's code. Read the response carefully and confirm the event appears in the platform's test or diagnostics view.
```

Scene 12 — Watch me do it: TikTok Events API test (illustrative)

```text
Watch me do it. Let's run a first TikTok Events API test for an illustrative Dubai sneaker store. Step one, in TikTok Events Manager I generate an access token for the pixel and copy the test event code. Step two, I store both as environment variables, never in code. Step three, I place a test order with a test t t c l i d in the landing URL. Our landing handler saved it in a first-party cookie, and at checkout the server reads it. Step four, the script sends a complete payment event with event ID order underscore seven seven eight, hashed email and phone, the t t c l i d, IP and user agent, and the value in dirhams. The HTTP status is two hundred, but I print the body: code zero, message OK. Step five, in Test Events, the server event appears alongside the pixel event with the same ID, and TikTok marks the duplicate. Step six, I deliberately send a malformed phone number to see what happens. The body returns a non-zero code with a clear message, and our logger records it. Now I know failures won't be silent.
```

Scene 13 — Recap and next step

```text
Recap. TikTok and LinkedIn use the same server-side pattern as Meta. For TikTok: Pixel code as source ID, access token header, and check the body code. For LinkedIn: versioned headers, the conversion rule URN, and milliseconds. Capture click IDs at landing. Your next step: pick one of the two, and write the event mapping for your top two conversions, including the event ID source, identifiers, value, and where click IDs are stored.
```

</details>

### 10. Data quality: event IDs, deduplication and a tracking plan

Lesson `deduplication-and-data-quality` · module “Data quality, deduplication and QA” · target 8 min · 14 scenes · 1177 words · planned 8:31 (words ≈ 8:32) · status: **to produce**

| Term | Say |
|---|---|
| JSON | jay-son |
| EMQ | E M Q |
| SQL | S Q L |

| # | Time | Sec | Narration | On screen | Visual |
|---|---|---|---|---|---|
| 1 | 0:00 | 30 | Here's something most marketers don't realize. The single biggest cause of bad automated bidding isn't the algorithm. It's messy events. Duplicates that make performance look twice as good, missing values, the same purchase named four different ways. In this lecture you'll learn to build a tracking plan, design event IDs that deduplicate correctly everywhere, validate events in code, and monitor data quality so problems are caught in hours, not months. | Data quality<br>• Why it drives performance<br>• The tracking plan<br>• Event ID design<br>• Validation and monitoring | Layout: A bidding engine being fed a stream of events, some duplicated and some broken; a filter labeled 'data quality' cleans the stream. Timing (30s): title card 0–3s; then the 4 on-screen points build in narration order about every 5s, each animating its element; hold and highlight the key idea in the final 5s. |
| 2 | 0:30 | 39 | Why does this matter? Because every decision downstream, bidding, budgets, reporting, is only as good as your event data. Here's an analogy. A bank that sometimes records a deposit twice, sometimes forgets the currency, and sometimes files it under the wrong account name would be shut down. Yet many marketing stacks do exactly this with conversions. Data quality isn't glamorous, but it's the accounting system of your marketing. Get it right and every tool on top of it gets smarter. Get it wrong and every tool confidently amplifies the error. | Why it matters<br>• Events are your marketing ledger<br>• Double entries, wrong currency, wrong names<br>• Clean ledger → smarter tools | Layout: bank ledger page with entries; some entries duplicate, one lacks a currency symbol, one has a mislabeled account; each error highlights red, then corrects to green. |
| 3 | 1:09 | 30 | Think about what duplicates do. If every purchase is counted twice, your cost per purchase looks half what it is. The system thinks it's winning, scales spend, and you lose money. Missing values make it undervalue good traffic. Inconsistent names split learning across events. Wrong currencies make a hundred rupees look like a hundred dollars. And a hybrid browser-plus-server setup increases these risks, unless you design for it. | What bad data does<br>• Duplicates inflate results<br>• Missing values undervalue<br>• Inconsistent names split learning<br>• Wrong currency distorts value | Layout: Four small scenes: a doubled purchase counter, an empty value field, three differently named events, and currency symbols swapping. Timing (30s): title card 0–3s; then the 4 on-screen points build in narration order about every 5s, each animating its element; hold and highlight the key idea in the final 5s. |
| 4 | 1:39 | 34 | The fix starts with a tracking plan, one source of truth for every event. For each event: the canonical name, what triggers it, which system is the source of truth, whether a browser duplicate exists, the event ID rule, required parameters, how value is defined, how it maps to each platform, which consent purposes apply, and who owns it. It can be a spreadsheet or a schema file in your code repository. What matters is that everyone uses it. | The tracking plan<br>• Name, trigger, source of truth<br>• Event ID rule<br>• Parameters and value definition<br>• Platform mappings<br>• Consent and owner | Layout: Spreadsheet with columns from the lesson; one row for 'purchase' fills in fully. Timing (34s): title card 0–3s; then the 5 on-screen points build in narration order about every 5s, each animating its element; hold and highlight the key idea in the final 5s. |
| 5 | 2:13 | 35 | Now event IDs, the key to deduplication. Make them deterministic: derive them from a business key like the order ID, so the browser and server produce the same ID independently, or generate once and pass it along. Make them unique per conversion and per stage, so a lead reaching the qualified stage has a different ID from the same lead becoming an opportunity. Keep them stable across retries. And never use personal data like an email address as an ID. | Event ID design<br>• Deterministic<br>• Unique per conversion and stage<br>• Stable across retries<br>• Never personal | Layout: An order ID 10042 turning into event IDs 'order_10042' used by both browser and server; a lead ID generating 'lead_8812_sql' and 'lead_8812_opportunity'. Timing (35s): title card 0–3s; then the 4 on-screen points build in narration order about every 6s, each animating its element; hold and highlight the key idea in the final 5s. |
| 6 | 2:48 | 43 | Here's a simple worked example. A Jeddah pharmacy chain's online store has three developers. One names the purchase event Purchase, another uses order complete, and the third uses checkout success in the server integration. Meta sees two event names and can't deduplicate. GA4 gets three different events. Reports disagree. The fix takes one afternoon: a tracking plan row that defines the canonical event, purchase, maps it to each platform's name, sets the event ID rule to order underscore the order number, and lists required parameters. The developers update their code to match, and a schema check blocks anything that doesn't. | Simple example: Jeddah pharmacy (illustrative)<br>• 3 names for one event<br>• No dedup, conflicting reports<br>• Fix: one tracking plan row<br>• Schema check enforces it | Layout: three differently named event bubbles float in; they funnel into a single tracking plan row; outgoing arrows map to GA4, Meta and TikTok names. |
| 7 | 3:31 | 30 | Each platform deduplicates slightly differently. Meta and TikTok match on the event name plus the event ID. Google Ads uses the transaction ID for web conversions, and you must avoid uploading the same offline conversion twice. GA4 uses the transaction ID for purchases. LinkedIn uses the event ID shared between the Insight Tag and the Conversions API. Put these rules in your tracking plan so every developer knows them. | Dedup keys<br>• Meta, TikTok: event + event_id<br>• Google Ads: transaction_id<br>• GA4: transaction_id<br>• LinkedIn: eventId | Layout: A table with five platform rows and their dedup key highlighted. Timing (30s): title card 0–3s; then the 4 on-screen points build in narration order about every 5s, each animating its element; hold and highlight the key idea in the final 5s. |
| 8 | 4:01 | 28 | Validate events before they leave your server. The lesson shows JSON Schema in Python: required fields, types, a pattern for the event ID, a list of allowed currencies, and one setting that's a quiet privacy hero, additional properties set to false. That stops anyone accidentally adding an email or a free-text field to an event. If validation fails, log it, alert, and don't send. | Validate in code<br>• Required fields and types<br>• ID pattern, currency list<br>• additionalProperties: false<br>• Fail closed | Layout: Code editor with the JSON schema; an event with an extra 'email' field is rejected with a red message. Timing (28s): title card 0–3s; then the 4 on-screen points build in narration order about every 5s, each animating its element; hold and highlight the key idea in the final 5s. |
| 9 | 4:29 | 32 | Then monitor. Run a daily reconciliation comparing backend, GA4 and each platform for key events, with expected ranges. Track the duplicate rate, the share of purchases missing currency or value, match-quality indicators like Meta's Event Match Quality and Google's enhanced conversion coverage, and latency from conversion to send. Alert when anything leaves its range. Most incidents come from website releases, consent platform changes or vendor API updates, so link alerts to your release calendar. | Monitor daily<br>• Backend vs GA4 vs platforms<br>• Duplicate rate<br>• Missing parameters<br>• Match quality<br>• Latency<br>• Alerts tied to releases | Layout: Reconciliation dashboard with five metric tiles and a release calendar strip underneath showing a spike aligned with a deployment. Timing (32s): title card 0–3s; then the 6 on-screen points build in narration order about every 4s, each animating its element; hold and highlight the key idea in the final 5s. |
| 10 | 5:01 | 27 | Here's an illustrative example. A Karachi agency standardized onboarding for every client: a tracking plan template, event ID conventions, a JSON schema for each key event, a reconciliation dashboard and alerts. Onboarding took a little longer, but tickets asking why the numbers don't match fell sharply, and clients started trusting the reports. That trust is worth more than any single campaign optimization. | Example: Karachi agency (illustrative)<br>• Tracking plan template<br>• ID conventions + schemas<br>• Reconciliation + alerts<br>• Fewer mismatch tickets, more trust | Layout: An onboarding checklist for a new client with five items ticked, and a support ticket counter decreasing. Timing (27s): title card 0–3s; then the 4 on-screen points build in narration order about every 4s, each animating its element; hold and highlight the key idea in the final 5s. |
| 11 | 5:28 | 38 | Common data quality mistakes. Each integration inventing its own event names. Random IDs generated separately in browser and server. Retries that create new IDs. Test events sent to production pixels without test codes. And nobody owning the tracking plan. Try this now: pick your most important conversion and trace it end to end. Where is the ID created? Which systems send it? What name does each platform receive? Draw it on one page. If the ID or name changes anywhere along the path, you've found a deduplication bug. | Mistakes + try this now<br>• Invented names<br>• Separate random IDs<br>• New IDs on retry<br>• Test events in production<br>• No owner<br>• Try: trace one conversion end to end | Layout: flow diagram from checkout to order service to browser and server outputs; the event ID label travels along each path, flashing red where it changes. |
| 12 | 6:06 | 39 | Quick self-check. Your server sends a purchase to TikTok, the request times out, and your retry logic generates a new event ID before sending again. TikTok actually received the first request. What happens? Pause. TikTok now has two purchases with different IDs, so it can't tell they're the same order, and your conversions are inflated. The fix is simple: create the event ID once, store it with the order, and reuse it on every retry. It's one line of code, and it's the difference between trustworthy and misleading numbers. | Quick self-check<br>• Timeout → retry with new ID<br>• First request actually arrived<br>• Result: double count<br>• Fix: store ID once, reuse on retry | Layout: sequence diagram with server and TikTok lanes; first request arrow succeeds but a timeout icon appears on the server; a second arrow with a different ID arrives; a '×2' badge flashes, then the fix replays with the same ID and merges. |
| 13 | 6:45 | 79 | Watch me do it. Let's set up the daily reconciliation for an illustrative Karachi online grocer. Step one, the query: yesterday's backend paid orders, GA4 purchases from the BigQuery export, and each platform's reported purchases from their APIs, all in one table. Step two, expected ranges from the last eight weeks: GA4 typically sees seventy-five to eighty-five percent of backend orders, Meta sixty to seventy-five, Google Ads sixty-five to eighty. Step three, the duplicate check: count purchase event IDs sent more than once per platform from our outbox table; expected zero. Step four, missing parameters: purchases without currency or value; expected zero. Step five, today's run: GA4 at eighty percent, fine. Meta at one hundred and thirty percent of backend orders. That's outside the range and above one hundred, which almost always means duplicates. Step six, the duplicate check confirms it: after yesterday's release, the thank-you page tag fires twice. The alert posts in the team channel with the numbers and the likely cause. The fix goes out the same day, and we annotate the data so nobody misreads yesterday's ROAS. | Watch me do it: daily reconciliation (illustrative)<br>• Backend vs GA4 vs platforms<br>• Expected ranges from 8 weeks<br>• Duplicate + missing-parameter checks<br>• Meta at 130% → duplicates<br>• Alert, fix, annotate | Layout: reconciliation dashboard with range bands per source. Timing: bands draw in ~20s; today's dots appear ~50s with Meta's dot outside the band flashing red; duplicate-check panel counts rising IDs at ~80s; final 25s show an alert message and an annotation marker. |
| 14 | 8:04 | 27 | Recap. Data quality is a performance lever. Write a tracking plan with a source of truth for every event. Design deterministic, stage-specific, retry-stable, non-personal event IDs. Know each platform's dedup key. Validate with strict schemas and reconcile daily. Your next step: write tracking plan rows for your three most important events, including the ID rule, source of truth, platform mappings and consent purposes. | Recap and next step<br>• Tracking plan<br>• Good event IDs<br>• Validate + reconcile<br>• Write three rows today | Layout: Three tracking plan rows appear filled in. Timing (27s): title card 0–3s; then the 4 on-screen points build in narration order about every 4s, each animating its element; hold and highlight the key idea in the final 5s. |

<details><summary>Narration per scene (paste into ElevenLabs)</summary>

Scene 1 — Data quality

```text
Here's something most marketers don't realize. The single biggest cause of bad automated bidding isn't the algorithm. It's messy events. Duplicates that make performance look twice as good, missing values, the same purchase named four different ways. In this lecture you'll learn to build a tracking plan, design event IDs that deduplicate correctly everywhere, validate events in code, and monitor data quality so problems are caught in hours, not months.
```

Scene 2 — Why it matters

```text
Why does this matter? Because every decision downstream, bidding, budgets, reporting, is only as good as your event data. Here's an analogy. A bank that sometimes records a deposit twice, sometimes forgets the currency, and sometimes files it under the wrong account name would be shut down. Yet many marketing stacks do exactly this with conversions. Data quality isn't glamorous, but it's the accounting system of your marketing. Get it right and every tool on top of it gets smarter. Get it wrong and every tool confidently amplifies the error.
```

Scene 3 — What bad data does

```text
Think about what duplicates do. If every purchase is counted twice, your cost per purchase looks half what it is. The system thinks it's winning, scales spend, and you lose money. Missing values make it undervalue good traffic. Inconsistent names split learning across events. Wrong currencies make a hundred rupees look like a hundred dollars. And a hybrid browser-plus-server setup increases these risks, unless you design for it.
```

Scene 4 — The tracking plan

```text
The fix starts with a tracking plan, one source of truth for every event. For each event: the canonical name, what triggers it, which system is the source of truth, whether a browser duplicate exists, the event ID rule, required parameters, how value is defined, how it maps to each platform, which consent purposes apply, and who owns it. It can be a spreadsheet or a schema file in your code repository. What matters is that everyone uses it.
```

Scene 5 — Event ID design

```text
Now event IDs, the key to deduplication. Make them deterministic: derive them from a business key like the order ID, so the browser and server produce the same ID independently, or generate once and pass it along. Make them unique per conversion and per stage, so a lead reaching the qualified stage has a different ID from the same lead becoming an opportunity. Keep them stable across retries. And never use personal data like an email address as an ID.
```

Scene 6 — Simple example: Jeddah pharmacy (illustrative)

```text
Here's a simple worked example. A Jeddah pharmacy chain's online store has three developers. One names the purchase event Purchase, another uses order complete, and the third uses checkout success in the server integration. Meta sees two event names and can't deduplicate. GA4 gets three different events. Reports disagree. The fix takes one afternoon: a tracking plan row that defines the canonical event, purchase, maps it to each platform's name, sets the event ID rule to order underscore the order number, and lists required parameters. The developers update their code to match, and a schema check blocks anything that doesn't.
```

Scene 7 — Dedup keys

```text
Each platform deduplicates slightly differently. Meta and TikTok match on the event name plus the event ID. Google Ads uses the transaction ID for web conversions, and you must avoid uploading the same offline conversion twice. GA4 uses the transaction ID for purchases. LinkedIn uses the event ID shared between the Insight Tag and the Conversions API. Put these rules in your tracking plan so every developer knows them.
```

Scene 8 — Validate in code

```text
Validate events before they leave your server. The lesson shows JSON Schema in Python: required fields, types, a pattern for the event ID, a list of allowed currencies, and one setting that's a quiet privacy hero, additional properties set to false. That stops anyone accidentally adding an email or a free-text field to an event. If validation fails, log it, alert, and don't send.
```

Scene 9 — Monitor daily

```text
Then monitor. Run a daily reconciliation comparing backend, GA4 and each platform for key events, with expected ranges. Track the duplicate rate, the share of purchases missing currency or value, match-quality indicators like Meta's Event Match Quality and Google's enhanced conversion coverage, and latency from conversion to send. Alert when anything leaves its range. Most incidents come from website releases, consent platform changes or vendor API updates, so link alerts to your release calendar.
```

Scene 10 — Example: Karachi agency (illustrative)

```text
Here's an illustrative example. A Karachi agency standardized onboarding for every client: a tracking plan template, event ID conventions, a JSON schema for each key event, a reconciliation dashboard and alerts. Onboarding took a little longer, but tickets asking why the numbers don't match fell sharply, and clients started trusting the reports. That trust is worth more than any single campaign optimization.
```

Scene 11 — Mistakes + try this now

```text
Common data quality mistakes. Each integration inventing its own event names. Random IDs generated separately in browser and server. Retries that create new IDs. Test events sent to production pixels without test codes. And nobody owning the tracking plan. Try this now: pick your most important conversion and trace it end to end. Where is the ID created? Which systems send it? What name does each platform receive? Draw it on one page. If the ID or name changes anywhere along the path, you've found a deduplication bug.
```

Scene 12 — Quick self-check

```text
Quick self-check. Your server sends a purchase to TikTok, the request times out, and your retry logic generates a new event ID before sending again. TikTok actually received the first request. What happens? Pause. TikTok now has two purchases with different IDs, so it can't tell they're the same order, and your conversions are inflated. The fix is simple: create the event ID once, store it with the order, and reuse it on every retry. It's one line of code, and it's the difference between trustworthy and misleading numbers.
```

Scene 13 — Watch me do it: daily reconciliation (illustrative)

```text
Watch me do it. Let's set up the daily reconciliation for an illustrative Karachi online grocer. Step one, the query: yesterday's backend paid orders, GA4 purchases from the BigQuery export, and each platform's reported purchases from their APIs, all in one table. Step two, expected ranges from the last eight weeks: GA4 typically sees seventy-five to eighty-five percent of backend orders, Meta sixty to seventy-five, Google Ads sixty-five to eighty. Step three, the duplicate check: count purchase event IDs sent more than once per platform from our outbox table; expected zero. Step four, missing parameters: purchases without currency or value; expected zero. Step five, today's run: GA4 at eighty percent, fine. Meta at one hundred and thirty percent of backend orders. That's outside the range and above one hundred, which almost always means duplicates. Step six, the duplicate check confirms it: after yesterday's release, the thank-you page tag fires twice. The alert posts in the team channel with the numbers and the likely cause. The fix goes out the same day, and we annotate the data so nobody misreads yesterday's ROAS.
```

Scene 14 — Recap and next step

```text
Recap. Data quality is a performance lever. Write a tracking plan with a source of truth for every event. Design deterministic, stage-specific, retry-stable, non-personal event IDs. Know each platform's dedup key. Validate with strict schemas and reconcile daily. Your next step: write tracking plan rows for your three most important events, including the ID rule, source of truth, platform mappings and consent purposes.
```

</details>

### 11. Debugging and QA for consent-aware tracking

Lesson `debugging-and-qa` · module “Data quality, deduplication and QA” · target 8 min · 14 scenes · 1149 words · planned 8:21 (words ≈ 8:20) · status: **to produce**

| Term | Say |
|---|---|
| Playwright | play-write |
| HAR | H A R |
| CI | C I |
| GPC | G P C |

| # | Time | Sec | Narration | On screen | Visual |
|---|---|---|---|---|---|
| 1 | 0:00 | 30 | Tracking has a nasty habit. It breaks silently. A new checkout design, a consent platform update or a vendor API change, and suddenly conversions stop, or worse, tags start firing without consent. Nobody notices for weeks. In this lecture you'll learn a QA mindset for tracking, the tools that matter, a consent QA matrix, how to automate consent tests, and quick debugging recipes for the most common failures. | Debugging and QA<br>• A QA mindset<br>• The toolset<br>• Consent QA matrix<br>• Automation<br>• Debugging recipes | Layout: A calm dashboard hides a small crack spreading underneath; a magnifying glass reveals it. Timing (30s): title card 0–3s; then the 5 on-screen points build in narration order about every 4s, each animating its element; hold and highlight the key idea in the final 5s. |
| 2 | 0:30 | 37 | Why does this matter? Because tracking failures cost money twice: once in wasted spend while the algorithm learns from bad data, and again in the weeks it takes to notice. Here's an analogy. Airlines don't wait for a plane to have problems mid-flight. They run pre-flight checklists, monitor instruments during the flight, and keep logs. Your tracking needs the same discipline: a checklist before every release, instruments in production, and a logbook of evidence. It's boring right up to the moment it saves you. | Why it matters<br>• Failures cost twice<br>• Pre-flight checklist<br>• In-flight instruments<br>• Logbook of evidence | Layout: aircraft icon on a runway with a clipboard checklist ticking; then in flight, instrument gauges; then a logbook closing. |
| 3 | 1:07 | 21 | The mindset is simple: treat tracking like product code. Test before every release. Monitor after release. Keep evidence. That last one matters because regulators, auditors and clients may ask you to prove what your site did on a given day, and screenshots or network recordings are your proof. | The mindset<br>• Test before release<br>• Monitor after release<br>• Keep evidence | Layout: Three pillars labeled test, monitor, evidence holding up a roof labeled 'trustworthy data'. Timing (21s): title card 0–3s; then the 3 on-screen points build in narration order about every 4s, each animating its element; hold and highlight the key idea in the final 5s. |
| 4 | 1:28 | 31 | Your toolset. Google Tag Assistant shows Google tags, consent defaults and updates, and events. Tag Manager preview, both web and server, steps through triggers and shows the server's incoming and outgoing requests. GA4 DebugView shows events in real time. Meta and TikTok Events Managers have Test Events tabs. LinkedIn shows conversion status in Campaign Manager. Browser developer tools reveal cookies and network requests. And Playwright or Puppeteer automate all of it. | The toolset<br>• Tag Assistant<br>• GTM preview (web + server)<br>• GA4 DebugView<br>• Meta/TikTok Test Events<br>• DevTools<br>• Playwright | Layout: A tool belt with seven labeled tools, each glowing briefly as named. Timing (31s): title card 0–3s; then the 6 on-screen points build in narration order about every 3s, each animating its element; hold and highlight the key idea in the final 5s. |
| 5 | 1:59 | 32 | Now the consent QA matrix. For each region profile and each choice, write down what should happen before and after the choice, and verify it. European or UK visitor accepting all: cookies and tags active, server forwarding to all vendors. Rejecting all: no advertising cookies, no Meta, TikTok or LinkedIn events, client-side or server-side. Analytics only: GA4 active, ads blocked. A US visitor with Global Privacy Control on. A Gulf visitor rejecting marketing. | Consent QA matrix<br>• Regions × choices<br>• Accept, reject, partial<br>• GPC scenarios<br>• Client and server side | Layout: Grid with rows for regions and columns for choices; cells turn green as each scenario is verified. Timing (32s): title card 0–3s; then the 4 on-screen points build in narration order about every 6s, each animating its element; hold and highlight the key idea in the final 5s. |
| 6 | 2:31 | 46 | Here's a simple worked example. A London cosmetics retailer releases a new checkout on Wednesday. Their Playwright consent test runs in the deployment pipeline and passes: no ad requests before consent, none after reject. But Thursday morning, the hourly synthetic purchase alert fires: GA4 received the test purchase, Meta didn't. The team opens Meta's test events view and sees nothing. They check the server container preview and find the Meta tag's trigger was tied to an old event name the new checkout no longer sends. They update the trigger, rerun the synthetic purchase, and Meta receives it. Total impact: one night instead of one month. | Simple example: London cosmetics (illustrative)<br>• Consent test passes in CI<br>• Synthetic purchase: Meta missing<br>• Cause: trigger on old event name<br>• Fixed next morning | Layout: timeline Wednesday to Thursday; CI badge green; an alert card pops on Thursday; server preview panel highlights a trigger with an outdated event name; fix applied and a green tick returns. |
| 7 | 3:17 | 33 | Manual testing doesn't scale, so automate. The lesson has a Playwright test that loads your staging site, records every request to known advertising hosts, asserts none fire before consent, clicks reject all, visits another page, and asserts there are still no ad requests and no advertising cookies. Adapt the host patterns and button names to your setup. If you use server-side tagging, also check your server logs for any vendor forwards from a session that rejected consent. | Automate with Playwright<br>• Record ad-host requests<br>• None before consent<br>• Reject → still none<br>• No ad cookies<br>• Check server logs too | Layout: Terminal running the Playwright test, with green check marks for each assertion. Timing (33s): title card 0–3s; then the 5 on-screen points build in narration order about every 5s, each animating its element; hold and highlight the key idea in the final 5s. |
| 8 | 3:50 | 28 | Put that test in your deployment pipeline, so a failing consent test blocks the release. After release, run a synthetic purchase on production every hour, using a test product and test event codes, and alert if GA4, Meta, TikTok or Google Ads don't receive it within a set time. That's how a Manchester retailer, illustratively, catches problems within an hour rather than at month-end reporting. | In the pipeline<br>• Consent tests block bad releases<br>• Hourly synthetic purchase<br>• Alerts if a platform misses it | Layout: CI pipeline diagram with a red stop at 'consent tests failed'; then a clock icon triggering a test purchase and four platform checkmarks. Timing (28s): title card 0–3s; then the 3 on-screen points build in narration order about every 6s, each animating its element; hold and highlight the key idea in the final 5s. |
| 9 | 4:18 | 30 | Here are quick debugging recipes. Conversions dropped to zero? Check recent releases, the thank-you page data layer, triggers, consent defaults, and server health. Double counting? Compare event IDs in Test Events and look for duplicate tags. Low match quality? Check that hashed email and phone are present and normalized, click IDs are captured, and that server events forward the user's original IP and user agent, not your server's. | Recipes, part one<br>• Zero conversions<br>• Double counting<br>• Low match quality | Layout: Three recipe cards with a symptom on the front and a checklist on the back. Timing (30s): title card 0–3s; then the 3 on-screen points build in narration order about every 7s, each animating its element; hold and highlight the key idea in the final 5s. |
| 10 | 4:48 | 39 | One more area teams forget: in-app browsers. A big share of social ad clicks open inside Instagram, Facebook, TikTok or Snapchat's own browser. Cookies there may not carry over to the phone's main browser, consent banners sometimes render badly, and payment redirects can drop parameters. So add real-device checks. Tap a test link inside the app, complete a test purchase, and confirm that the click ID, the consent state and the purchase event reach every platform. If you serve Gulf audiences, include Arabic right-to-left layouts in those checks too. | In-app browsers<br>• Cookies may not carry over<br>• Banners may render badly<br>• Redirects drop parameters<br>• Test on real devices<br>• Include Arabic RTL layouts | Layout: A phone showing a social app's in-app browser with a checkout page; a checklist overlay ticks click ID, consent and purchase event. Timing (39s): title card 0–3s; then the 5 on-screen points build in narration order about every 6s, each animating its element; hold and highlight the key idea in the final 5s. |
| 11 | 5:27 | 28 | More recipes. Values wrong? Look for missing currency, numbers formatted with commas, or tax included inconsistently. Server events missing? Check for expired tokens, retired API versions, or rate limiting, which shows up as HTTP four twenty-nine. And avoid four QA pitfalls: testing only accept all, testing only desktop Chrome while in-app browsers and Safari behave differently, leaving test codes in production, and keeping no evidence. | Recipes, part two<br>• Wrong values<br>• Missing server events<br>• Pitfalls: accept-only, Chrome-only, test codes, no evidence | Layout: Two more recipe cards, then four pitfall icons including a phone showing an in-app browser. Timing (28s): title card 0–3s; then the 3 on-screen points build in narration order about every 6s, each animating its element; hold and highlight the key idea in the final 5s. |
| 12 | 5:55 | 38 | Common QA mistakes. Only testing accept all. Only testing desktop Chrome. Checking the browser but never the server container's outgoing requests. Leaving debug mode or test codes switched on in production. And not saving any evidence of what was tested. Try this now: write the first three rows of your consent QA matrix on paper: one region, three choices, and what should fire for each. Then test just the reject-all row today, on your phone, inside the Instagram or TikTok in-app browser. Real-world conditions reveal real-world bugs. | Mistakes + try this now<br>• Accept-only testing<br>• Desktop-only testing<br>• Ignoring server output<br>• Debug left on<br>• No evidence<br>• Try: reject-all on a phone in-app browser | Layout: a paper-style matrix with three rows; the reject-all row highlights; a phone mock opens an in-app browser and a checklist overlays. |
| 13 | 6:33 | 81 | Watch me do it. Let's debug an illustrative problem: a Doha electronics store says Google Ads conversions dropped by half this week, while orders didn't. Step one, timeline: the drop started Tuesday. The release log shows a checkout update deployed Tuesday afternoon. Step two, Tag Assistant on a test purchase: the Google Ads conversion tag fires, but the transaction ID and value are empty. Step three, the data layer on the thank-you page: the new checkout pushes the order under a different key, order data instead of ecommerce, so our variables read nothing. Step four, why did only half the conversions drop? Because the new checkout was rolled out to half of visitors as an experiment. Step five, the fix: update the variables to read the new structure for both versions, then retest in preview with both checkouts. Step six, prevention: we add an assertion to the Playwright suite that the thank-you page data layer contains transaction ID, value and currency, so a structure change fails the pipeline next time. Debugging is detective work: timeline, evidence, cause, fix, and a test so it never happens again. | Watch me do it: debugging a drop (illustrative)<br>• Timeline: drop matches Tuesday release<br>• Tag fires, values empty<br>• Data layer key changed<br>• Half traffic in experiment<br>• Fix + new pipeline assertion | Layout: detective-board style canvas with a timeline strip across the top. Timing: release marker pins to Tuesday ~15s; Tag Assistant card with empty fields ~40s; data layer diff ~65s; experiment split pie ~90s; code fix and a green test badge in the final 25s. |
| 14 | 7:54 | 27 | Recap. Tracking breaks silently, so test, monitor and keep evidence. Use the right tools, run a full consent matrix across regions and choices, automate consent tests in your pipeline, run synthetic purchases, and keep recipes handy. Your next step: write your consent QA matrix for at least three region profiles and three choices, and automate one scenario with Playwright on a staging site. | Recap and next step<br>• Test, monitor, evidence<br>• Consent matrix<br>• Automate in CI<br>• Build your matrix | Layout: The consent matrix grid reappears with one scenario highlighted and a code icon beside it. Timing (27s): title card 0–3s; then the 4 on-screen points build in narration order about every 4s, each animating its element; hold and highlight the key idea in the final 5s. |

<details><summary>Narration per scene (paste into ElevenLabs)</summary>

Scene 1 — Debugging and QA

```text
Tracking has a nasty habit. It breaks silently. A new checkout design, a consent platform update or a vendor API change, and suddenly conversions stop, or worse, tags start firing without consent. Nobody notices for weeks. In this lecture you'll learn a QA mindset for tracking, the tools that matter, a consent QA matrix, how to automate consent tests, and quick debugging recipes for the most common failures.
```

Scene 2 — Why it matters

```text
Why does this matter? Because tracking failures cost money twice: once in wasted spend while the algorithm learns from bad data, and again in the weeks it takes to notice. Here's an analogy. Airlines don't wait for a plane to have problems mid-flight. They run pre-flight checklists, monitor instruments during the flight, and keep logs. Your tracking needs the same discipline: a checklist before every release, instruments in production, and a logbook of evidence. It's boring right up to the moment it saves you.
```

Scene 3 — The mindset

```text
The mindset is simple: treat tracking like product code. Test before every release. Monitor after release. Keep evidence. That last one matters because regulators, auditors and clients may ask you to prove what your site did on a given day, and screenshots or network recordings are your proof.
```

Scene 4 — The toolset

```text
Your toolset. Google Tag Assistant shows Google tags, consent defaults and updates, and events. Tag Manager preview, both web and server, steps through triggers and shows the server's incoming and outgoing requests. GA4 DebugView shows events in real time. Meta and TikTok Events Managers have Test Events tabs. LinkedIn shows conversion status in Campaign Manager. Browser developer tools reveal cookies and network requests. And Playwright or Puppeteer automate all of it.
```

Scene 5 — Consent QA matrix

```text
Now the consent QA matrix. For each region profile and each choice, write down what should happen before and after the choice, and verify it. European or UK visitor accepting all: cookies and tags active, server forwarding to all vendors. Rejecting all: no advertising cookies, no Meta, TikTok or LinkedIn events, client-side or server-side. Analytics only: GA4 active, ads blocked. A US visitor with Global Privacy Control on. A Gulf visitor rejecting marketing.
```

Scene 6 — Simple example: London cosmetics (illustrative)

```text
Here's a simple worked example. A London cosmetics retailer releases a new checkout on Wednesday. Their Playwright consent test runs in the deployment pipeline and passes: no ad requests before consent, none after reject. But Thursday morning, the hourly synthetic purchase alert fires: GA4 received the test purchase, Meta didn't. The team opens Meta's test events view and sees nothing. They check the server container preview and find the Meta tag's trigger was tied to an old event name the new checkout no longer sends. They update the trigger, rerun the synthetic purchase, and Meta receives it. Total impact: one night instead of one month.
```

Scene 7 — Automate with Playwright

```text
Manual testing doesn't scale, so automate. The lesson has a Playwright test that loads your staging site, records every request to known advertising hosts, asserts none fire before consent, clicks reject all, visits another page, and asserts there are still no ad requests and no advertising cookies. Adapt the host patterns and button names to your setup. If you use server-side tagging, also check your server logs for any vendor forwards from a session that rejected consent.
```

Scene 8 — In the pipeline

```text
Put that test in your deployment pipeline, so a failing consent test blocks the release. After release, run a synthetic purchase on production every hour, using a test product and test event codes, and alert if GA4, Meta, TikTok or Google Ads don't receive it within a set time. That's how a Manchester retailer, illustratively, catches problems within an hour rather than at month-end reporting.
```

Scene 9 — Recipes, part one

```text
Here are quick debugging recipes. Conversions dropped to zero? Check recent releases, the thank-you page data layer, triggers, consent defaults, and server health. Double counting? Compare event IDs in Test Events and look for duplicate tags. Low match quality? Check that hashed email and phone are present and normalized, click IDs are captured, and that server events forward the user's original IP and user agent, not your server's.
```

Scene 10 — In-app browsers

```text
One more area teams forget: in-app browsers. A big share of social ad clicks open inside Instagram, Facebook, TikTok or Snapchat's own browser. Cookies there may not carry over to the phone's main browser, consent banners sometimes render badly, and payment redirects can drop parameters. So add real-device checks. Tap a test link inside the app, complete a test purchase, and confirm that the click ID, the consent state and the purchase event reach every platform. If you serve Gulf audiences, include Arabic right-to-left layouts in those checks too.
```

Scene 11 — Recipes, part two

```text
More recipes. Values wrong? Look for missing currency, numbers formatted with commas, or tax included inconsistently. Server events missing? Check for expired tokens, retired API versions, or rate limiting, which shows up as HTTP four twenty-nine. And avoid four QA pitfalls: testing only accept all, testing only desktop Chrome while in-app browsers and Safari behave differently, leaving test codes in production, and keeping no evidence.
```

Scene 12 — Mistakes + try this now

```text
Common QA mistakes. Only testing accept all. Only testing desktop Chrome. Checking the browser but never the server container's outgoing requests. Leaving debug mode or test codes switched on in production. And not saving any evidence of what was tested. Try this now: write the first three rows of your consent QA matrix on paper: one region, three choices, and what should fire for each. Then test just the reject-all row today, on your phone, inside the Instagram or TikTok in-app browser. Real-world conditions reveal real-world bugs.
```

Scene 13 — Watch me do it: debugging a drop (illustrative)

```text
Watch me do it. Let's debug an illustrative problem: a Doha electronics store says Google Ads conversions dropped by half this week, while orders didn't. Step one, timeline: the drop started Tuesday. The release log shows a checkout update deployed Tuesday afternoon. Step two, Tag Assistant on a test purchase: the Google Ads conversion tag fires, but the transaction ID and value are empty. Step three, the data layer on the thank-you page: the new checkout pushes the order under a different key, order data instead of ecommerce, so our variables read nothing. Step four, why did only half the conversions drop? Because the new checkout was rolled out to half of visitors as an experiment. Step five, the fix: update the variables to read the new structure for both versions, then retest in preview with both checkouts. Step six, prevention: we add an assertion to the Playwright suite that the thank-you page data layer contains transaction ID, value and currency, so a structure change fails the pipeline next time. Debugging is detective work: timeline, evidence, cause, fix, and a test so it never happens again.
```

Scene 14 — Recap and next step

```text
Recap. Tracking breaks silently, so test, monitor and keep evidence. Use the right tools, run a full consent matrix across regions and choices, automate consent tests in your pipeline, run synthetic purchases, and keep recipes handy. Your next step: write your consent QA matrix for at least three region profiles and three choices, and automate one scenario with Playwright on a staging site.
```

</details>

### 12. First-party data strategy: value exchange, identity and activation

Lesson `first-party-data-strategy` · module “First-party data, modeling and clean rooms” · target 8 min · 14 scenes · 1178 words · planned 8:33 (words ≈ 8:32) · status: **to produce**

| Term | Say |
|---|---|
| CDP | C D P |
| dbt | D B T |
| ETL | E T L |
| LTV | L T V |

| # | Time | Sec | Narration | On screen | Visual |
|---|---|---|---|---|---|
| 1 | 0:00 | 28 | Fixing tags recovers some lost signal. But the companies that win the next decade of marketing measurement won't just track better. They'll know their customers better, because customers chose to tell them. In this lecture you'll learn how to build a first-party data strategy: designing value exchanges, stitching identity responsibly, choosing an architecture, and activating data in ad platforms, with consent traveling alongside every record. | First-party data strategy<br>• Value exchanges<br>• Identity<br>• Architecture<br>• Activation with consent | Layout: A customer icon handing a card labeled 'preferences' to a brand icon, receiving a gift icon in return. Timing (28s): title card 0–3s; then the 4 on-screen points build in narration order about every 5s, each animating its element; hold and highlight the key idea in the final 5s. |
| 2 | 0:28 | 36 | Why does this matter? Because tags recover signal, but relationships create it. Here's an analogy. A neighborhood shopkeeper who knows your name, your usual order and your kids' birthdays doesn't need to track you around town. You tell them things because they make your life easier. First-party data strategy is building that shopkeeper relationship at scale. The data is more accurate, more durable, and more lawful than anything a pixel can capture, because customers chose to share it for a reason they understand. | Why it matters<br>• Tags recover signal<br>• Relationships create it<br>• Be the neighborhood shopkeeper at scale | Layout: small shopfront with a shopkeeper icon greeting a customer; speech bubbles show 'usual order' and 'birthday reminder'; then the shop scales into many connected shopfronts. |
| 3 | 1:04 | 30 | Let's define terms. First-party data is what you collect directly from customers and prospects, with their knowledge: accounts, purchases, preferences, survey answers, support interactions. Zero-party data is what people deliberately share, like their size, their skin type, or when they plan to buy. Both are more durable and more valuable than anything a pixel captures, as long as you collect them lawfully and use them in ways people expect. | Definitions<br>• First-party: collected directly, with knowledge<br>• Zero-party: intentionally shared<br>• Durable and valuable<br>• Lawful, expected use | Layout: Two labeled data folders side by side with example contents appearing inside each. Timing (30s): title card 0–3s; then the 4 on-screen points build in narration order about every 5s, each animating its element; hold and highlight the key idea in the final 5s. |
| 4 | 1:34 | 32 | People share data when they get something worth it. Design value exchanges. An account with order tracking and faster checkout. A loyalty program, very popular in Gulf retail. A quiz or configurator. Gated content, used sparingly, in B2B. WhatsApp order updates, which work brilliantly in Pakistan, the UAE and Saudi Arabia. And a post-purchase survey asking how people heard about you. Be explicit, collect the minimum, and keep order updates and marketing consent separate. | Value exchanges<br>• Accounts<br>• Loyalty<br>• Quizzes<br>• Gated content (sparingly)<br>• WhatsApp updates<br>• Post-purchase survey | Layout: A carousel of value exchange cards with simple icons: a user profile, a loyalty card, a quiz, a document, a chat bubble, a survey. Timing (32s): title card 0–3s; then the 6 on-screen points build in narration order about every 4s, each animating its element; hold and highlight the key idea in the final 5s. |
| 5 | 2:06 | 28 | Now identity. Deterministic identity, meaning logins, emails, phone numbers and customer IDs, is accurate and requires the customer to identify themselves. Probabilistic techniques that resemble fingerprinting are a bad idea. European regulators treat device fingerprinting as needing consent, and platforms restrict it. Instead, resolve identity in your warehouse or customer data platform by joining events on customer ID, hashed email and consented device IDs. | Identity<br>• Deterministic: login, email, phone, ID<br>• Avoid fingerprinting<br>• Resolve in warehouse or CDP | Layout: Several event dots joining into one customer profile via solid lines labeled 'login' and 'email'; a dotted line labeled 'fingerprint' is crossed out. Timing (28s): title card 0–3s; then the 3 on-screen points build in narration order about every 6s, each animating its element; hold and highlight the key idea in the final 5s. |
| 6 | 2:34 | 42 | Here's a simple worked example. A Karachi bakery chain starts a WhatsApp order-updates service. At checkout, customers can opt in to receive order updates on WhatsApp, and separately, tick a box for weekly offers. Within a few months they have thousands of verified phone numbers with clear consent flags. For ads, they upload hashed phone numbers of people who consented to marketing as a custom audience to exclude recent buyers from acquisition campaigns, and to re-engage people who haven't ordered in sixty days. Customers who only opted in to order updates are never used for ads. | Simple example: Karachi bakery (illustrative)<br>• WhatsApp order updates (opt-in)<br>• Separate marketing consent<br>• Exclude recent buyers, re-engage lapsed<br>• Update-only contacts never used for ads | Layout: phone mock with a WhatsApp-style order update; two consent checkboxes animate separately; a segment diagram splits contacts into 'marketing ok' and 'updates only', with only the first flowing to ad audiences. |
| 7 | 3:16 | 29 | Three architecture options. Warehouse-native: events and CRM data land in BigQuery, Snowflake or Databricks, transformations build customer tables, and connectors send audiences and conversions to ad platforms. A packaged customer data platform that handles collection, identity, consent and activation. Or lightweight: your e-commerce platform, a CRM and native integrations, which is fine for many small businesses. Whichever you choose, consent and purpose must travel with the data. | Architectures<br>• Warehouse-native<br>• Packaged CDP<br>• Lightweight (SMB)<br>• Consent travels with data | Layout: Three architecture thumbnails; a small consent badge follows the data through each one. Timing (29s): title card 0–3s; then the 4 on-screen points build in narration order about every 5s, each animating its element; hold and highlight the key idea in the final 5s. |
| 8 | 3:45 | 36 | How do you activate it? Upload hashed customer lists as Customer Match on Google, custom audiences on Meta, matched audiences on LinkedIn, and customer file audiences on TikTok. Use them to exclude current customers from acquisition campaigns, re-engage lapsed buyers, seed similar audiences, and act as signals for automated campaigns. Feed offline conversions and CRM stages. And use lifetime value tiers to inform value rules. Google also offers confidential matching for Customer Match, using secure hardware; check if it fits your commitments. | Activation<br>• Hashed audience uploads<br>• Exclude, re-engage, seed, signal<br>• Conversion feeds<br>• LTV tiers | Layout: A warehouse icon sending hashed lists to four generic platform icons, each labeled with a purpose. Timing (36s): title card 0–3s; then the 4 on-screen points build in narration order about every 7s, each animating its element; hold and highlight the key idea in the final 5s. |
| 9 | 4:21 | 33 | Here's an illustrative example. An Abu Dhabi supermarket chain launched a loyalty app with digital receipts, personalized offers and WhatsApp order updates, with marketing consent asked separately. Loyalty IDs join online and in-store purchases in their warehouse. Consented, hashed segments like lapsed for sixty days, or high-value families, go to Meta and Google for re-engagement and exclusion, and in-store sales are uploaded as offline conversions. Every quarter they check each use against the privacy notice. | Example: Abu Dhabi supermarket (illustrative)<br>• Loyalty app + WhatsApp updates<br>• Online + in-store joined<br>• Consented segments to ads<br>• Quarterly privacy review | Layout: A loyalty card and a receipt merging into one profile; segments flow out to ad platforms with a consent check gate. Timing (33s): title card 0–3s; then the 4 on-screen points build in narration order about every 6s, each animating its element; hold and highlight the key idea in the final 5s. |
| 10 | 4:54 | 32 | The lesson includes a SQL query that builds a consented exclusion list of active customers. It hashes normalized emails and phone numbers, joins a consent table, keeps only people with ads personalization consent, and filters by recent orders. Avoid the classic mistakes: collecting data just in case, bundling marketing consent into terms of service, uploading lists without opt-out suppression, and forgetting that a deletion in your CRM must also remove someone from ad audiences. | Build it safely<br>• Consent-joined SQL export<br>• No 'just in case' data<br>• No bundled consent<br>• Suppress opt-outs<br>• Propagate deletions | Layout: SQL editor with the export query; a delete request arrow flows from CRM to every downstream audience. Timing (32s): title card 0–3s; then the 5 on-screen points build in narration order about every 4s, each animating its element; hold and highlight the key idea in the final 5s. |
| 11 | 5:26 | 36 | Common first-party data mistakes. Collecting data just in case, with no clear purpose. Bundling marketing consent into the terms and conditions. Uploading audiences without filtering opt-outs. Deleting someone in the CRM but leaving them in ad audiences. And trying to stitch identity with fingerprinting. Try this now: write down one value exchange your business could launch this quarter, what the customer gets, what data you'd collect, and the exact consent wording for marketing use. Keep the marketing consent separate from the service. | Mistakes + try this now<br>• Just-in-case collection<br>• Bundled consent<br>• Unfiltered uploads<br>• Undeleted downstream copies<br>• Fingerprinting<br>• Try: design one value exchange | Layout: a value exchange card with three fields: customer gets, data collected, consent wording; fields fill in with example text. |
| 12 | 6:02 | 41 | Quick self-check. A customer signed up for order updates by WhatsApp but did not tick the marketing box. Your team wants to include their phone number in a lookalike seed audience on Meta. Is that okay? Pause. Generally no. Their data was collected for order updates, and they didn't agree to marketing use, so using it to build advertising audiences goes beyond the purpose they accepted. The right move is to keep them out of ad uploads and, if you want marketing consent, ask clearly at a sensible moment, like after a successful delivery. | Quick self-check<br>• Opted in to order updates only<br>• Use in a lookalike seed?<br>• Answer: no — purpose limited<br>• Ask for marketing consent clearly | Layout: a contact card with two consent toggles (order updates ON, marketing OFF); an arrow toward an 'ad audience' bucket gets blocked; a follow-up message bubble asks for marketing consent after delivery. |
| 13 | 6:43 | 83 | Watch me do it. Let's design one value exchange for an illustrative Riyadh coffee chain, from idea to activation. Step one, the value: a loyalty app offering a free drink after every eight, order-ahead pickup, and birthday rewards. Step two, data collected: name, mobile number, favorite branch, orders, and optional date of birth, nothing more. Step three, consent: service terms cover the loyalty program itself; a separate, unticked checkbox asks for marketing offers by message and for using data to show relevant ads, written in Arabic and English. Step four, storage: orders and consent flags land in the warehouse, joined by loyalty ID. Step five, activation: a nightly job builds three audiences only from people who gave marketing consent: lapsed thirty days, new-branch neighbors based on favorite branch, and high-frequency customers to exclude from acquisition ads. Hashed phone numbers upload to Meta and Google. Step six, rights: when a member deletes their account, a deletion event removes them from the next nightly upload, and we request removal from existing lists. Step seven, measurement: we track opt-in rate for marketing consent at signup, a direct measure of whether the value exchange feels fair. | Watch me do it: loyalty value exchange (illustrative)<br>• Value: free drink, order-ahead, birthday<br>• Minimal data<br>• Separate marketing consent (AR/EN)<br>• 3 consented audiences nightly<br>• Deletion flows downstream | Layout: vertical journey from app screen to warehouse to ad platforms. Timing: app mock with two checkboxes ~20s; warehouse table with consent flags ~50s; three audience chips generate ~80s; deletion event arrow removing a contact ~105s; opt-in rate KPI tile in the final 15s. |
| 14 | 8:06 | 27 | Recap. First-party and zero-party data are your most durable signals when earned through real value exchanges. Use deterministic identity, pick an architecture that fits, carry consent everywhere, and activate through audiences, conversion feeds and value tiers. Your next step: list three value exchanges your business could offer, the data each collects, the consent needed, and one ad activation each would enable. | Recap and next step<br>• Earn the data<br>• Deterministic identity<br>• Consent everywhere<br>• Plan three value exchanges | Layout: A three-row planning table filling in. Timing (27s): title card 0–3s; then the 4 on-screen points build in narration order about every 4s, each animating its element; hold and highlight the key idea in the final 5s. |

<details><summary>Narration per scene (paste into ElevenLabs)</summary>

Scene 1 — First-party data strategy

```text
Fixing tags recovers some lost signal. But the companies that win the next decade of marketing measurement won't just track better. They'll know their customers better, because customers chose to tell them. In this lecture you'll learn how to build a first-party data strategy: designing value exchanges, stitching identity responsibly, choosing an architecture, and activating data in ad platforms, with consent traveling alongside every record.
```

Scene 2 — Why it matters

```text
Why does this matter? Because tags recover signal, but relationships create it. Here's an analogy. A neighborhood shopkeeper who knows your name, your usual order and your kids' birthdays doesn't need to track you around town. You tell them things because they make your life easier. First-party data strategy is building that shopkeeper relationship at scale. The data is more accurate, more durable, and more lawful than anything a pixel can capture, because customers chose to share it for a reason they understand.
```

Scene 3 — Definitions

```text
Let's define terms. First-party data is what you collect directly from customers and prospects, with their knowledge: accounts, purchases, preferences, survey answers, support interactions. Zero-party data is what people deliberately share, like their size, their skin type, or when they plan to buy. Both are more durable and more valuable than anything a pixel captures, as long as you collect them lawfully and use them in ways people expect.
```

Scene 4 — Value exchanges

```text
People share data when they get something worth it. Design value exchanges. An account with order tracking and faster checkout. A loyalty program, very popular in Gulf retail. A quiz or configurator. Gated content, used sparingly, in B2B. WhatsApp order updates, which work brilliantly in Pakistan, the UAE and Saudi Arabia. And a post-purchase survey asking how people heard about you. Be explicit, collect the minimum, and keep order updates and marketing consent separate.
```

Scene 5 — Identity

```text
Now identity. Deterministic identity, meaning logins, emails, phone numbers and customer IDs, is accurate and requires the customer to identify themselves. Probabilistic techniques that resemble fingerprinting are a bad idea. European regulators treat device fingerprinting as needing consent, and platforms restrict it. Instead, resolve identity in your warehouse or customer data platform by joining events on customer ID, hashed email and consented device IDs.
```

Scene 6 — Simple example: Karachi bakery (illustrative)

```text
Here's a simple worked example. A Karachi bakery chain starts a WhatsApp order-updates service. At checkout, customers can opt in to receive order updates on WhatsApp, and separately, tick a box for weekly offers. Within a few months they have thousands of verified phone numbers with clear consent flags. For ads, they upload hashed phone numbers of people who consented to marketing as a custom audience to exclude recent buyers from acquisition campaigns, and to re-engage people who haven't ordered in sixty days. Customers who only opted in to order updates are never used for ads.
```

Scene 7 — Architectures

```text
Three architecture options. Warehouse-native: events and CRM data land in BigQuery, Snowflake or Databricks, transformations build customer tables, and connectors send audiences and conversions to ad platforms. A packaged customer data platform that handles collection, identity, consent and activation. Or lightweight: your e-commerce platform, a CRM and native integrations, which is fine for many small businesses. Whichever you choose, consent and purpose must travel with the data.
```

Scene 8 — Activation

```text
How do you activate it? Upload hashed customer lists as Customer Match on Google, custom audiences on Meta, matched audiences on LinkedIn, and customer file audiences on TikTok. Use them to exclude current customers from acquisition campaigns, re-engage lapsed buyers, seed similar audiences, and act as signals for automated campaigns. Feed offline conversions and CRM stages. And use lifetime value tiers to inform value rules. Google also offers confidential matching for Customer Match, using secure hardware; check if it fits your commitments.
```

Scene 9 — Example: Abu Dhabi supermarket (illustrative)

```text
Here's an illustrative example. An Abu Dhabi supermarket chain launched a loyalty app with digital receipts, personalized offers and WhatsApp order updates, with marketing consent asked separately. Loyalty IDs join online and in-store purchases in their warehouse. Consented, hashed segments like lapsed for sixty days, or high-value families, go to Meta and Google for re-engagement and exclusion, and in-store sales are uploaded as offline conversions. Every quarter they check each use against the privacy notice.
```

Scene 10 — Build it safely

```text
The lesson includes a SQL query that builds a consented exclusion list of active customers. It hashes normalized emails and phone numbers, joins a consent table, keeps only people with ads personalization consent, and filters by recent orders. Avoid the classic mistakes: collecting data just in case, bundling marketing consent into terms of service, uploading lists without opt-out suppression, and forgetting that a deletion in your CRM must also remove someone from ad audiences.
```

Scene 11 — Mistakes + try this now

```text
Common first-party data mistakes. Collecting data just in case, with no clear purpose. Bundling marketing consent into the terms and conditions. Uploading audiences without filtering opt-outs. Deleting someone in the CRM but leaving them in ad audiences. And trying to stitch identity with fingerprinting. Try this now: write down one value exchange your business could launch this quarter, what the customer gets, what data you'd collect, and the exact consent wording for marketing use. Keep the marketing consent separate from the service.
```

Scene 12 — Quick self-check

```text
Quick self-check. A customer signed up for order updates by WhatsApp but did not tick the marketing box. Your team wants to include their phone number in a lookalike seed audience on Meta. Is that okay? Pause. Generally no. Their data was collected for order updates, and they didn't agree to marketing use, so using it to build advertising audiences goes beyond the purpose they accepted. The right move is to keep them out of ad uploads and, if you want marketing consent, ask clearly at a sensible moment, like after a successful delivery.
```

Scene 13 — Watch me do it: loyalty value exchange (illustrative)

```text
Watch me do it. Let's design one value exchange for an illustrative Riyadh coffee chain, from idea to activation. Step one, the value: a loyalty app offering a free drink after every eight, order-ahead pickup, and birthday rewards. Step two, data collected: name, mobile number, favorite branch, orders, and optional date of birth, nothing more. Step three, consent: service terms cover the loyalty program itself; a separate, unticked checkbox asks for marketing offers by message and for using data to show relevant ads, written in Arabic and English. Step four, storage: orders and consent flags land in the warehouse, joined by loyalty ID. Step five, activation: a nightly job builds three audiences only from people who gave marketing consent: lapsed thirty days, new-branch neighbors based on favorite branch, and high-frequency customers to exclude from acquisition ads. Hashed phone numbers upload to Meta and Google. Step six, rights: when a member deletes their account, a deletion event removes them from the next nightly upload, and we request removal from existing lists. Step seven, measurement: we track opt-in rate for marketing consent at signup, a direct measure of whether the value exchange feels fair.
```

Scene 14 — Recap and next step

```text
Recap. First-party and zero-party data are your most durable signals when earned through real value exchanges. Use deterministic identity, pick an architecture that fits, carry consent everywhere, and activate through audiences, conversion feeds and value tiers. Your next step: list three value exchanges your business could offer, the data each collects, the consent needed, and one ad activation each would enable.
```

</details>

### 13. Modeled conversions, aggregated measurement and data clean rooms

Lesson `modelled-conversions-and-clean-rooms` · module “First-party data, modeling and clean rooms” · target 8 min · 14 scenes · 1178 words · planned 8:33 (words ≈ 8:32) · status: **to produce**

| Term | Say |
|---|---|
| SKAdNetwork | S K ad network |
| PETs | pets |
| MPC | M P C |
| TEE | T E E |

| # | Time | Sec | Narration | On screen | Visual |
|---|---|---|---|---|---|
| 1 | 0:00 | 31 | Even the best, fully compliant tracking setup won't see every conversion. Some people decline consent, some switch devices, some use apps that don't share data. So platforms fill the gaps with models, and you need to know how to read those numbers honestly. In this lecture you'll learn the types of modeled measurement, how to judge them, and how data clean rooms let companies analyze combined data without sharing raw personal data. | Modeled measurement and clean rooms<br>• Types of modeling<br>• Judging modeled numbers<br>• Data clean rooms<br>• Privacy-enhancing technologies | Layout: A chart with solid observed bars and translucent modeled extensions; a locked room icon with two data streams entering. Timing (31s): title card 0–3s; then the 4 on-screen points build in narration order about every 5s, each animating its element; hold and highlight the key idea in the final 5s. |
| 2 | 0:31 | 37 | Why does this matter? Because modeled numbers are now part of almost every report you'll read, and people who can't tell estimates from observations make overconfident decisions. Here's an analogy. Election night coverage shows counted votes and projected results. Projections are useful and usually right, but everyone knows they're projections, and when the final count arrives, it wins. Modeled conversions are projections. Lift tests are the final count. Clean rooms are like secure counting centers where parties can combine ballots without seeing each other's raw papers. | Why it matters<br>• Counted vs projected<br>• Modeled = projection<br>• Lift test = final count<br>• Clean room = secure counting center | Layout: election-style results board with 'counted' solid bars and 'projected' hatched extensions; a final count stamp lands; then a secure room icon with two locked ballot boxes. |
| 3 | 1:08 | 29 | You'll meet six kinds of estimate. Google Ads conversion modeling under Consent Mode estimates conversions from people who declined. GA4 behavioral modeling estimates unconsented users' activity. Platforms model conversions lost to browsers, Apple's tracking prompt and cross-device journeys. App measurement uses privacy frameworks like Apple's SKAdNetwork and AdAttributionKit. Marketing mix models estimate channel contributions from aggregated data. And lift studies estimate causal effects with randomized holdouts. | Six kinds<br>• Consent Mode modeling<br>• GA4 behavioral modeling<br>• Platform conversion modeling<br>• App privacy frameworks<br>• MMM<br>• Lift studies | Layout: Six tiles in a grid, each with a simple icon, lighting up one by one. Timing (29s): title card 0–3s; then the 6 on-screen points build in narration order about every 3s, each animating its element; hold and highlight the key idea in the final 5s. |
| 4 | 1:37 | 31 | How do you judge modeled numbers? Three checks. Direction and scale: do they move sensibly when consent rates or traffic change? Reconciliation: do platform totals, including modeled conversions, stay within a known band of your backend orders? And experiments: lift tests are the referee. If modeled attribution and a well-designed lift test disagree, trust the lift test. And always label modeled numbers in reports, so nobody mistakes an estimate for an observation. | Judging modeled numbers<br>• Direction and scale<br>• Backend reconciliation band<br>• Lift tests referee<br>• Label clearly | Layout: A band chart showing platform totals staying within a shaded tolerance zone around backend orders; a referee whistle icon labeled 'lift test'. Timing (31s): title card 0–3s; then the 4 on-screen points build in narration order about every 5s, each animating its element; hold and highlight the key idea in the final 5s. |
| 5 | 2:08 | 30 | Now data clean rooms. A clean room lets two or more parties analyze combined data, say your purchases and a platform's or retailer's ad exposure data, without either side seeing the other's raw, individual-level records. Queries return only aggregated results, above minimum thresholds, and often with extra protections. It's a way to answer questions like did people who saw these ads buy more, without handing over customer lists. | Data clean rooms<br>• Combine data from two parties<br>• No raw data sharing<br>• Aggregated, thresholded outputs | Layout: Two parties each put a locked box into a central room; only a summary chart comes out the door. Timing (30s): title card 0–3s; then the 3 on-screen points build in narration order about every 7s, each animating its element; hold and highlight the key idea in the final 5s. |
| 6 | 2:38 | 45 | Here's a simple worked example. A UK home-goods retailer sees Google Ads report one thousand conversions for the month, including modeled ones. Their backend shows nine hundred and fifty orders attributable to paid search clicks by their own matching, and their reconciliation band says Google should be within eighty to one hundred and twenty percent of that. One thousand is within the band, so they accept it for steering. But when a Conversion Lift study later estimates that only seventy percent of Google's reported conversions were incremental, they apply that factor to planning. Three numbers, three purposes, all labeled. The numbers are illustrative. | Simple example: UK home goods (illustrative)<br>• Google: 1,000 (incl. modeled)<br>• Backend band: 80–120% of 950 → OK for steering<br>• Lift: 70% incremental → planning factor | Layout: a reconciliation band chart with a shaded zone and a dot at 1,000 inside it; then a lift result card applies a ×0.7 factor to produce a planning number. |
| 7 | 3:23 | 35 | Examples, and check current names because this market moves fast. Google's Ads Data Hub lets you analyze Google and YouTube campaign data with your own data in BigQuery. Amazon Marketing Cloud does the same for Amazon Ads. Cloud and independent clean rooms, from providers like Snowflake, AWS, LiveRamp and InfoSum, power publisher and retailer collaborations. And many retailers, including large grocery and marketplace groups in the Gulf and the UK, offer clean room access to brands buying their retail media. | Examples (check current)<br>• Google Ads Data Hub<br>• Amazon Marketing Cloud<br>• Snowflake, AWS, LiveRamp, InfoSum<br>• Retail media clean rooms | Layout: A row of generic clean-room icons, each labeled by type rather than logo: platform, marketplace, cloud, retailer. Timing (35s): title card 0–3s; then the 4 on-screen points build in narration order about every 6s, each animating its element; hold and highlight the key idea in the final 5s. |
| 8 | 3:58 | 24 | What are they good for? Reach and frequency across channels, overlap between your customers and a platform's audience, incremental sales from retail media, and audience planning without swapping lists. The limits: thresholds hide small segments, setup needs data engineering, each clean room usually covers one partner, and contracts must define purposes, retention and allowed outputs. | Uses and limits<br>• Reach, overlap, retail media impact<br>• Small segments hidden<br>• Engineering effort<br>• One partner each<br>• Contracts matter | Layout: Two columns: green uses with checkmarks, amber limits with caution signs. Timing (24s): title card 0–3s; then the 5 on-screen points build in narration order about every 3s, each animating its element; hold and highlight the key idea in the final 5s. |
| 9 | 4:22 | 27 | Clean rooms use privacy-enhancing technologies. Aggregation thresholds. Differential privacy, which adds calibrated noise so no individual can be singled out. Secure multi-party computation. And trusted execution environments, hardware that processes data in isolation, which Google uses for confidential matching. These reduce risk. But they don't replace a lawful basis, transparency, and using data only for the purposes people were told about. | Privacy-enhancing technologies<br>• Aggregation thresholds<br>• Differential privacy<br>• Secure multi-party computation<br>• Trusted execution environments<br>• Still need lawful basis | Layout: Four shields around a data core, each labeled with a PET; a legal document sits beneath labeled 'still required'. Timing (27s): title card 0–3s; then the 5 on-screen points build in narration order about every 3s, each animating its element; hold and highlight the key idea in the final 5s. |
| 10 | 4:49 | 37 | Here's an illustrative example. A UK snack brand buys retail media from a grocer. Through the grocer's clean room, it measures in aggregate whether exposed shoppers bought more than matched unexposed shoppers, and how many were new to the brand. The results feed the brand's mix model as priors. No raw loyalty data ever leaves the grocer. The lesson includes a clean-room style SQL pattern for this kind of aggregated overlap, with one warning: exposed versus unexposed isn't causal unless groups are randomized or carefully matched. | Example: UK snack brand (illustrative)<br>• Grocer clean room<br>• Exposed vs matched unexposed<br>• New-to-brand share<br>• Feeds MMM priors<br>• Not causal unless matched well | Layout: Shopping basket icon inside a secure room; an aggregated chart exits and flows into an MMM model icon. Timing (37s): title card 0–3s; then the 5 on-screen points build in narration order about every 5s, each animating its element; hold and highlight the key idea in the final 5s. |
| 11 | 5:26 | 35 | Common mistakes here. Presenting modeled numbers as if they were observed. Assuming a clean room makes any use of data lawful. Comparing exposed and unexposed customers and calling the difference incremental. Building a one-off clean room analysis with no repeatable process. And never reconciling platform totals with the backend. Try this now: take your latest marketing report and mark every number with one of three labels: observed, modeled, or experimental. If you can't label one, find out before the next meeting. | Mistakes + try this now<br>• Modeled presented as observed<br>• Clean room = lawful (not true)<br>• Exposed vs unexposed ≠ causal<br>• One-off analyzes<br>• No reconciliation<br>• Try: label every number in your report | Layout: a report page with numbers; small tags labeled observed, modeled, experimental drop onto each number; one number remains unlabeled with a question mark. |
| 12 | 6:01 | 39 | Quick self-check. In a retailer's clean room, shoppers who saw your ads bought your brand at twice the rate of shoppers who didn't. Your manager wants to announce that the ads doubled sales. Should they? Pause. Not yet. The retailer's ad system probably showed your ads to people already likely to buy your brand, so the comparison is biased. To claim a causal lift, you need randomized holdouts or carefully matched groups. Present the result as exposed shoppers bought more, and propose a proper lift design for next campaign. | Quick self-check<br>• Exposed buy 2× unexposed<br>• 'Ads doubled sales'?<br>• Answer: not causal yet<br>• Propose a randomized lift test | Layout: two shopper groups with purchase bars (exposed 2×); a 'selection bias' magnifier reveals exposed group already skewed to loyal buyers; a recommendation card reads 'run randomized lift'. |
| 13 | 6:40 | 82 | Watch me do it. Let's walk through a first clean room analysis for an illustrative UAE snack brand buying retail media from a grocery chain. Step one, the question: did shoppers exposed to our on-site ads buy the brand more in the following two weeks than similar unexposed shoppers? Step two, the contract: the retailer's clean room lets us upload our own campaign list, and outputs only aggregates above a minimum count. Step three, the design: instead of comparing all exposed with all unexposed, we ask the retailer to match unexposed shoppers on past category purchases and store region, which reduces selection bias. Step four, the query: exposed purchasers, matched-control purchasers, and new-to-brand counts, grouped by campaign. Step five, the result: exposed shoppers bought at a higher rate than matched controls, and a meaningful share were new to the brand. Step six, the caveat, written right on the slide: matched comparison, not randomized; for a causal estimate, next quarter we'll request a holdout. Step seven, we feed the result into our marketing mix model as a prior for retail media. Every number is labeled for what it is. | Watch me do it: first clean-room analysis (illustrative)<br>• Question + contract limits<br>• Matched controls reduce bias<br>• Aggregated query by campaign<br>• Result + 'not randomized' caveat<br>• Feed MMM prior; plan holdout | Layout: secure room graphic centered with two locked data boxes entering. Timing: question card ~10s; contract terms ~30s; matching diagram pairing shopper icons ~55s; aggregated result bars ~85s; caveat banner ~105s; MMM prior curve in the final 15s. |
| 14 | 8:02 | 31 | Recap. Modeled measurement fills real gaps. Label it, sanity-check it, reconcile it with the backend, and let lift tests referee. Clean rooms enable aggregated collaboration without raw data sharing, backed by privacy-enhancing technologies, but they don't make any use lawful by themselves. Your next step: mark which numbers in your reports include modeled data, set your expected reconciliation band, and pick one clean room or lift test that would validate them. | Recap and next step<br>• Label and check modeled data<br>• Lift referees<br>• Clean rooms + PETs<br>• Mark your modeled numbers | Layout: A report with small 'modeled' tags appearing next to certain figures. Timing (31s): title card 0–3s; then the 4 on-screen points build in narration order about every 5s, each animating its element; hold and highlight the key idea in the final 5s. |

<details><summary>Narration per scene (paste into ElevenLabs)</summary>

Scene 1 — Modeled measurement and clean rooms

```text
Even the best, fully compliant tracking setup won't see every conversion. Some people decline consent, some switch devices, some use apps that don't share data. So platforms fill the gaps with models, and you need to know how to read those numbers honestly. In this lecture you'll learn the types of modeled measurement, how to judge them, and how data clean rooms let companies analyze combined data without sharing raw personal data.
```

Scene 2 — Why it matters

```text
Why does this matter? Because modeled numbers are now part of almost every report you'll read, and people who can't tell estimates from observations make overconfident decisions. Here's an analogy. Election night coverage shows counted votes and projected results. Projections are useful and usually right, but everyone knows they're projections, and when the final count arrives, it wins. Modeled conversions are projections. Lift tests are the final count. Clean rooms are like secure counting centers where parties can combine ballots without seeing each other's raw papers.
```

Scene 3 — Six kinds

```text
You'll meet six kinds of estimate. Google Ads conversion modeling under Consent Mode estimates conversions from people who declined. GA4 behavioral modeling estimates unconsented users' activity. Platforms model conversions lost to browsers, Apple's tracking prompt and cross-device journeys. App measurement uses privacy frameworks like Apple's SKAdNetwork and AdAttributionKit. Marketing mix models estimate channel contributions from aggregated data. And lift studies estimate causal effects with randomized holdouts.
```

Scene 4 — Judging modeled numbers

```text
How do you judge modeled numbers? Three checks. Direction and scale: do they move sensibly when consent rates or traffic change? Reconciliation: do platform totals, including modeled conversions, stay within a known band of your backend orders? And experiments: lift tests are the referee. If modeled attribution and a well-designed lift test disagree, trust the lift test. And always label modeled numbers in reports, so nobody mistakes an estimate for an observation.
```

Scene 5 — Data clean rooms

```text
Now data clean rooms. A clean room lets two or more parties analyze combined data, say your purchases and a platform's or retailer's ad exposure data, without either side seeing the other's raw, individual-level records. Queries return only aggregated results, above minimum thresholds, and often with extra protections. It's a way to answer questions like did people who saw these ads buy more, without handing over customer lists.
```

Scene 6 — Simple example: UK home goods (illustrative)

```text
Here's a simple worked example. A UK home-goods retailer sees Google Ads report one thousand conversions for the month, including modeled ones. Their backend shows nine hundred and fifty orders attributable to paid search clicks by their own matching, and their reconciliation band says Google should be within eighty to one hundred and twenty percent of that. One thousand is within the band, so they accept it for steering. But when a Conversion Lift study later estimates that only seventy percent of Google's reported conversions were incremental, they apply that factor to planning. Three numbers, three purposes, all labeled. The numbers are illustrative.
```

Scene 7 — Examples (check current)

```text
Examples, and check current names because this market moves fast. Google's Ads Data Hub lets you analyze Google and YouTube campaign data with your own data in BigQuery. Amazon Marketing Cloud does the same for Amazon Ads. Cloud and independent clean rooms, from providers like Snowflake, AWS, LiveRamp and InfoSum, power publisher and retailer collaborations. And many retailers, including large grocery and marketplace groups in the Gulf and the UK, offer clean room access to brands buying their retail media.
```

Scene 8 — Uses and limits

```text
What are they good for? Reach and frequency across channels, overlap between your customers and a platform's audience, incremental sales from retail media, and audience planning without swapping lists. The limits: thresholds hide small segments, setup needs data engineering, each clean room usually covers one partner, and contracts must define purposes, retention and allowed outputs.
```

Scene 9 — Privacy-enhancing technologies

```text
Clean rooms use privacy-enhancing technologies. Aggregation thresholds. Differential privacy, which adds calibrated noise so no individual can be singled out. Secure multi-party computation. And trusted execution environments, hardware that processes data in isolation, which Google uses for confidential matching. These reduce risk. But they don't replace a lawful basis, transparency, and using data only for the purposes people were told about.
```

Scene 10 — Example: UK snack brand (illustrative)

```text
Here's an illustrative example. A UK snack brand buys retail media from a grocer. Through the grocer's clean room, it measures in aggregate whether exposed shoppers bought more than matched unexposed shoppers, and how many were new to the brand. The results feed the brand's mix model as priors. No raw loyalty data ever leaves the grocer. The lesson includes a clean-room style SQL pattern for this kind of aggregated overlap, with one warning: exposed versus unexposed isn't causal unless groups are randomized or carefully matched.
```

Scene 11 — Mistakes + try this now

```text
Common mistakes here. Presenting modeled numbers as if they were observed. Assuming a clean room makes any use of data lawful. Comparing exposed and unexposed customers and calling the difference incremental. Building a one-off clean room analysis with no repeatable process. And never reconciling platform totals with the backend. Try this now: take your latest marketing report and mark every number with one of three labels: observed, modeled, or experimental. If you can't label one, find out before the next meeting.
```

Scene 12 — Quick self-check

```text
Quick self-check. In a retailer's clean room, shoppers who saw your ads bought your brand at twice the rate of shoppers who didn't. Your manager wants to announce that the ads doubled sales. Should they? Pause. Not yet. The retailer's ad system probably showed your ads to people already likely to buy your brand, so the comparison is biased. To claim a causal lift, you need randomized holdouts or carefully matched groups. Present the result as exposed shoppers bought more, and propose a proper lift design for next campaign.
```

Scene 13 — Watch me do it: first clean-room analysis (illustrative)

```text
Watch me do it. Let's walk through a first clean room analysis for an illustrative UAE snack brand buying retail media from a grocery chain. Step one, the question: did shoppers exposed to our on-site ads buy the brand more in the following two weeks than similar unexposed shoppers? Step two, the contract: the retailer's clean room lets us upload our own campaign list, and outputs only aggregates above a minimum count. Step three, the design: instead of comparing all exposed with all unexposed, we ask the retailer to match unexposed shoppers on past category purchases and store region, which reduces selection bias. Step four, the query: exposed purchasers, matched-control purchasers, and new-to-brand counts, grouped by campaign. Step five, the result: exposed shoppers bought at a higher rate than matched controls, and a meaningful share were new to the brand. Step six, the caveat, written right on the slide: matched comparison, not randomized; for a causal estimate, next quarter we'll request a holdout. Step seven, we feed the result into our marketing mix model as a prior for retail media. Every number is labeled for what it is.
```

Scene 14 — Recap and next step

```text
Recap. Modeled measurement fills real gaps. Label it, sanity-check it, reconcile it with the backend, and let lift tests referee. Clean rooms enable aggregated collaboration without raw data sharing, backed by privacy-enhancing technologies, but they don't make any use lawful by themselves. Your next step: mark which numbers in your reports include modeled data, set your expected reconciliation band, and pick one clean room or lift test that would validate them.
```

</details>

### 14. Capstone: design and document a consent-compliant server-side tracking setup

Lesson `capstone-consent-compliant-server-side-setup` · module “Capstone: consent-compliant server-side setup” · target 8 min · 14 scenes · 1178 words · planned 8:31 (words ≈ 8:32) · status: **to produce**

| Term | Say |
|---|---|
| MDD | M D D |
| RACI | racy |
| GCC | G C C |
| GPC | G P C |

| # | Time | Sec | Narration | On screen | Visual |
|---|---|---|---|---|---|
| 1 | 0:00 | 28 | This is your capstone. You're going to design and document a complete measurement setup that's accurate, consent-compliant and ready to build. The output is a measurement design document that a developer can implement, a privacy lead can approve, and a marketer can trust. In this lecture I'll walk you through the scenarios, the nine-section template, an abbreviated example, an AI review prompt and the rubric. | Capstone<br>• A Measurement Design Document<br>• Scenarios<br>• Nine-section template<br>• Example, AI review, rubric | Layout: A blueprint-style document unrolls with nine section headers. Timing (28s): title card 0–3s; then the 4 on-screen points build in narration order about every 5s, each animating its element; hold and highlight the key idea in the final 5s. |
| 2 | 0:28 | 36 | Why does this capstone matter? Because measurement setups are usually built piece by piece, by different people, over years, and nobody can explain the whole thing. A written design changes that. Here's an analogy. A building's wiring diagram doesn't make the lights brighter, but it means any electrician can fix a fault safely, and an inspector can approve the building. Your measurement design document is the wiring diagram for your data. It lets developers build, privacy leads approve, and marketers trust the numbers. | Why it matters<br>• Setups grow without design<br>• MDD = wiring diagram<br>• Build, approve, trust | Layout: building cross-section with tangled wires that straighten into a clean labeled wiring diagram; three role icons (developer, privacy lead, marketer) each get a check. |
| 3 | 1:04 | 33 | Pick a scenario, or use your own business. Option A, a Shopify fashion brand in the UAE selling across the Gulf and the UK, advertising on Meta, TikTok, Google and Snapchat. Option B, a Pakistani software company selling to the UK and Saudi Arabia via LinkedIn and Google, with a CRM and a forty-five-day sales cycle. Option C, a private clinic group in the UK and Saudi Arabia with online booking, where health-related data needs extra care. | Scenarios<br>• A: UAE fashion, GCC + UK<br>• B: Pakistan B2B SaaS → UK, KSA<br>• C: UK/KSA clinic group (sensitive data) | Layout: Three scenario cards with icons: a clothes hanger, a laptop, a medical cross. Timing (33s): title card 0–3s; then the 3 on-screen points build in narration order about every 8s, each animating its element; hold and highlight the key idea in the final 5s. |
| 4 | 1:37 | 32 | Sections one and two. Scope and objectives: the goals, KPIs, platforms, regions and decisions the data must support. Then legal and consent design, the heart of a compliant setup: your tag-to-law register, the consent platform configuration by region and language, whether you need TCF or GPP, how you honor Global Privacy Control, basic or advanced Consent Mode per region with the reasoning, and a sensitive data policy listing what must never be sent. | Sections 1–2<br>• Scope and objectives<br>• Tag-to-law register<br>• CMP config by region<br>• Consent Mode choices<br>• Sensitive data policy | Layout: Document sections expanding with checklist items appearing. Timing (32s): title card 0–3s; then the 5 on-screen points build in narration order about every 4s, each animating its element; hold and highlight the key idea in the final 5s. |
| 5 | 2:09 | 25 | Sections three and four. The architecture: a diagram showing the browser, your first-party server container or tag gateway, each vendor, your CRM and backend feeding server APIs, and your warehouse, plus hosting, domain, region and monitoring. Then the tracking plan: canonical events, triggers, the source of truth, parameters, values, event ID rules, platform mappings, consent purposes and owners. | Sections 3–4<br>• Architecture diagram<br>• Hosting, domain, region, monitoring<br>• Tracking plan | Layout: An architecture diagram assembling on screen, followed by a tracking plan table. Timing (25s): title card 0–3s; then the 3 on-screen points build in narration order about every 5s, each animating its element; hold and highlight the key idea in the final 5s. |
| 6 | 2:34 | 50 | Here's a simple worked example of one section done well: the Meta feed for scenario A. Events: purchase and add payment info. Source of truth: the Shopify order webhook for purchase. Identifiers: hashed email and phone from the order, external ID from the customer ID, IP and user agent from the original browser request, f b p and f b c captured by the server container. Dedup key: order underscore the order number, also passed to the Pixel. Consent: only sent when advertising consent is granted, read from the CMP state stored with the order. Retries: up to three with backoff, same event ID. Secrets: access token in the cloud secret manager, rotated quarterly. | Simple example: one feed spec<br>• Events + source of truth<br>• Identifiers (hashed / unhashed)<br>• Dedup key shared with Pixel<br>• Consent from stored state<br>• Retries + secrets | Layout: a spec card with seven labeled rows filling in one by one; a small architecture thumbnail on the right highlights the Meta path. |
| 7 | 3:24 | 24 | Sections five and six. Server-side feeds: for each platform, the events, identifiers and whether they're hashed, how click IDs are captured and stored, dedup keys, batching and retries, and how secrets are managed. Then data minimization and security: exactly which fields each vendor receives, transformations, retention, access control, key rotation, and logging that avoids personal data. | Sections 5–6<br>• Per-platform feed specs<br>• Dedup, retries, secrets<br>• Fields per vendor<br>• Retention, access, logging | Layout: Four platform feed cards with fields listed; a filter icon shows fields removed per vendor. Timing (24s): title card 0–3s; then the 4 on-screen points build in narration order about every 4s, each animating its element; hold and highlight the key idea in the final 5s. |
| 8 | 3:48 | 27 | Sections seven to nine. QA and monitoring: your consent matrix, automated tests in the deployment pipeline, synthetic transactions, reconciliation bands and alerts, and where evidence is stored. Modeled measurement: which reports include modeled data and how you'll validate with a lift or geo test. And the rollout plan, with phases and a RACI, who's responsible, accountable, consulted and informed, plus sign-offs. | Sections 7–9<br>• QA + monitoring<br>• Modeled data + validation<br>• Rollout + RACI | Layout: A timeline with rollout phases and a small RACI grid beside it. Timing (27s): title card 0–3s; then the 3 on-screen points build in narration order about every 6s, each animating its element; hold and highlight the key idea in the final 5s. |
| 9 | 4:15 | 43 | Here's an abbreviated example for scenario A. Opt-in for EU and UK visitors, an Arabic and English banner in the Gulf with marketing consent, and Global Privacy Control honored for US visitors. Advanced Consent Mode for the Gulf after legal review, basic for Europe. A server container on a first-party subdomain in a Middle East cloud region. Shopify webhooks feed the order service. Server feeds to GA4, Google Ads with enhanced conversions, Meta, TikTok and Snapchat. Hashed identifiers only go to ad platforms when ad consent is granted. And validation through a Meta lift study and a TikTok geo test. | Example: Scenario A (abbreviated)<br>• Region-specific consent<br>• First-party server container<br>• Five server feeds<br>• Consent-gated identifiers<br>• Lift + geo validation | Layout: Map of the Gulf and UK with a server icon on a subdomain; five vendor arrows each with a consent gate. Timing (43s): title card 0–3s; then the 5 on-screen points build in narration order about every 7s, each animating its element; hold and highlight the key idea in the final 5s. |
| 10 | 4:58 | 34 | Use AI as a reviewer. The lesson's prompt asks a model, acting as a privacy engineer and measurement lead, to find any place data could reach a vendor without valid consent, check each feed's dedup, hashing, click IDs and retries, spot sensitive data leaks in URLs or parameters, identify missing QA scenarios, and list assumptions your legal counsel must confirm, in a table with severity and fixes. Never paste real personal data. Review the design, not the customers. | AI as reviewer<br>• Consent gaps<br>• Feed checks<br>• Leakage risks<br>• Missing QA<br>• Assumptions for counsel<br>• No real personal data | Layout: A document with AI margin comments tagged by severity; a padlock over a 'customer data' folder kept outside the frame. Timing (34s): title card 0–3s; then the 6 on-screen points build in narration order about every 4s, each animating its element; hold and highlight the key idea in the final 5s. |
| 11 | 5:32 | 27 | Score yourself on six criteria. Lawfulness: every purpose mapped to a basis per region, with consent enforced client-side and server-side. Architecture: first-party, minimal, monitored, secure. Accuracy: dedup, IDs, values, currencies and click IDs designed end to end. Coverage: server feeds for each platform and offline loops. QA: automated tests, synthetic transactions, reconciliation and evidence. And honesty: modeled data labeled, with a validation plan. | Rubric<br>• Lawfulness<br>• Architecture<br>• Accuracy<br>• Coverage<br>• QA<br>• Honesty | Layout: Six-row rubric with check marks sliding into 'excellent'. Timing (27s): title card 0–3s; then the 6 on-screen points build in narration order about every 3s, each animating its element; hold and highlight the key idea in the final 5s. |
| 12 | 5:59 | 40 | Common capstone mistakes. Architecture diagrams with no consent checks drawn on them. Tracking plans with event names but no ID rules. Feed sections that say we'll use CAPI, with no identifiers, dedup or retries. No sensitive data policy, especially in health scenarios. And no plan to validate modeled numbers. Try this now: before writing prose, draw your architecture diagram and mark every point where consent is checked with a small lock icon. If any data path to an ad platform has no lock, you've found the most important gap in your design. | Mistakes + try this now<br>• No consent on diagrams<br>• No ID rules<br>• Vague feed specs<br>• No sensitive data policy<br>• No validation plan<br>• Try: mark consent locks on your diagram | Layout: architecture diagram with data paths; lock icons appear on some paths; one path to an ad platform flashes red with no lock. |
| 13 | 6:39 | 83 | Watch me do it. Let's draft the QA section of an illustrative measurement design document for scenario B, the Pakistani software company selling to the UK and Saudi Arabia. First, the consent matrix: two region profiles, UK and Saudi Arabia, three choices each, accept all, reject all, analytics only, and for each cell, which tags fire client-side and which forwards happen server-side. Six rows, written in a table. Second, automation: a Playwright test for the UK reject-all row runs on every deployment and fails the release if any request to LinkedIn, Google Ads or Meta appears. Third, the synthetic conversion: every six hours a test lead is submitted with a test flag; the CRM routes it to a test pipeline stage, and the server sends a test LinkedIn and Google conversion that we verify arrived, without polluting real data. Fourth, reconciliation: weekly, CRM SQLs versus LinkedIn and Google reported conversions, with bands. Fifth, evidence: every release stores the test report and a HAR file in a dated folder. Sixth, ownership: the growth engineer owns the suite, the privacy lead reviews quarterly. That's a QA section a regulator or client would respect. | Watch me do it: MDD QA section (illustrative)<br>• 2 regions × 3 choices matrix<br>• Playwright gate on releases<br>• 6-hourly synthetic lead<br>• Weekly CRM vs platform bands<br>• Evidence + owners | Layout: document page with the QA section expanding. Timing: consent matrix table fills row by row ~0–45s; pipeline gate icon ~60s; clock with synthetic lead ~80s; reconciliation band chart ~100s; evidence folder and owner badges in the final 20s. |
| 14 | 8:02 | 29 | That's the course. You now understand the real state of tracking, the laws that govern it, consent management and Consent Mode, GA4 and server-side tagging, every major conversion API, data quality and QA, first-party data strategy, and modeled measurement. Your next step: write your measurement design document, run the AI review, fix what it finds, and you'll have a blueprint any team would be glad to build. | Your next step<br>• Write your MDD<br>• Run the AI review<br>• Fix and submit | Layout: The blueprint document closes and a subtle badge outline appears. Timing (29s): title card 0–3s; then the 3 on-screen points build in narration order about every 7s, each animating its element; hold and highlight the key idea in the final 5s. |

<details><summary>Narration per scene (paste into ElevenLabs)</summary>

Scene 1 — Capstone

```text
This is your capstone. You're going to design and document a complete measurement setup that's accurate, consent-compliant and ready to build. The output is a measurement design document that a developer can implement, a privacy lead can approve, and a marketer can trust. In this lecture I'll walk you through the scenarios, the nine-section template, an abbreviated example, an AI review prompt and the rubric.
```

Scene 2 — Why it matters

```text
Why does this capstone matter? Because measurement setups are usually built piece by piece, by different people, over years, and nobody can explain the whole thing. A written design changes that. Here's an analogy. A building's wiring diagram doesn't make the lights brighter, but it means any electrician can fix a fault safely, and an inspector can approve the building. Your measurement design document is the wiring diagram for your data. It lets developers build, privacy leads approve, and marketers trust the numbers.
```

Scene 3 — Scenarios

```text
Pick a scenario, or use your own business. Option A, a Shopify fashion brand in the UAE selling across the Gulf and the UK, advertising on Meta, TikTok, Google and Snapchat. Option B, a Pakistani software company selling to the UK and Saudi Arabia via LinkedIn and Google, with a CRM and a forty-five-day sales cycle. Option C, a private clinic group in the UK and Saudi Arabia with online booking, where health-related data needs extra care.
```

Scene 4 — Sections 1–2

```text
Sections one and two. Scope and objectives: the goals, KPIs, platforms, regions and decisions the data must support. Then legal and consent design, the heart of a compliant setup: your tag-to-law register, the consent platform configuration by region and language, whether you need TCF or GPP, how you honor Global Privacy Control, basic or advanced Consent Mode per region with the reasoning, and a sensitive data policy listing what must never be sent.
```

Scene 5 — Sections 3–4

```text
Sections three and four. The architecture: a diagram showing the browser, your first-party server container or tag gateway, each vendor, your CRM and backend feeding server APIs, and your warehouse, plus hosting, domain, region and monitoring. Then the tracking plan: canonical events, triggers, the source of truth, parameters, values, event ID rules, platform mappings, consent purposes and owners.
```

Scene 6 — Simple example: one feed spec

```text
Here's a simple worked example of one section done well: the Meta feed for scenario A. Events: purchase and add payment info. Source of truth: the Shopify order webhook for purchase. Identifiers: hashed email and phone from the order, external ID from the customer ID, IP and user agent from the original browser request, f b p and f b c captured by the server container. Dedup key: order underscore the order number, also passed to the Pixel. Consent: only sent when advertising consent is granted, read from the CMP state stored with the order. Retries: up to three with backoff, same event ID. Secrets: access token in the cloud secret manager, rotated quarterly.
```

Scene 7 — Sections 5–6

```text
Sections five and six. Server-side feeds: for each platform, the events, identifiers and whether they're hashed, how click IDs are captured and stored, dedup keys, batching and retries, and how secrets are managed. Then data minimization and security: exactly which fields each vendor receives, transformations, retention, access control, key rotation, and logging that avoids personal data.
```

Scene 8 — Sections 7–9

```text
Sections seven to nine. QA and monitoring: your consent matrix, automated tests in the deployment pipeline, synthetic transactions, reconciliation bands and alerts, and where evidence is stored. Modeled measurement: which reports include modeled data and how you'll validate with a lift or geo test. And the rollout plan, with phases and a RACI, who's responsible, accountable, consulted and informed, plus sign-offs.
```

Scene 9 — Example: Scenario A (abbreviated)

```text
Here's an abbreviated example for scenario A. Opt-in for EU and UK visitors, an Arabic and English banner in the Gulf with marketing consent, and Global Privacy Control honored for US visitors. Advanced Consent Mode for the Gulf after legal review, basic for Europe. A server container on a first-party subdomain in a Middle East cloud region. Shopify webhooks feed the order service. Server feeds to GA4, Google Ads with enhanced conversions, Meta, TikTok and Snapchat. Hashed identifiers only go to ad platforms when ad consent is granted. And validation through a Meta lift study and a TikTok geo test.
```

Scene 10 — AI as reviewer

```text
Use AI as a reviewer. The lesson's prompt asks a model, acting as a privacy engineer and measurement lead, to find any place data could reach a vendor without valid consent, check each feed's dedup, hashing, click IDs and retries, spot sensitive data leaks in URLs or parameters, identify missing QA scenarios, and list assumptions your legal counsel must confirm, in a table with severity and fixes. Never paste real personal data. Review the design, not the customers.
```

Scene 11 — Rubric

```text
Score yourself on six criteria. Lawfulness: every purpose mapped to a basis per region, with consent enforced client-side and server-side. Architecture: first-party, minimal, monitored, secure. Accuracy: dedup, IDs, values, currencies and click IDs designed end to end. Coverage: server feeds for each platform and offline loops. QA: automated tests, synthetic transactions, reconciliation and evidence. And honesty: modeled data labeled, with a validation plan.
```

Scene 12 — Mistakes + try this now

```text
Common capstone mistakes. Architecture diagrams with no consent checks drawn on them. Tracking plans with event names but no ID rules. Feed sections that say we'll use CAPI, with no identifiers, dedup or retries. No sensitive data policy, especially in health scenarios. And no plan to validate modeled numbers. Try this now: before writing prose, draw your architecture diagram and mark every point where consent is checked with a small lock icon. If any data path to an ad platform has no lock, you've found the most important gap in your design.
```

Scene 13 — Watch me do it: MDD QA section (illustrative)

```text
Watch me do it. Let's draft the QA section of an illustrative measurement design document for scenario B, the Pakistani software company selling to the UK and Saudi Arabia. First, the consent matrix: two region profiles, UK and Saudi Arabia, three choices each, accept all, reject all, analytics only, and for each cell, which tags fire client-side and which forwards happen server-side. Six rows, written in a table. Second, automation: a Playwright test for the UK reject-all row runs on every deployment and fails the release if any request to LinkedIn, Google Ads or Meta appears. Third, the synthetic conversion: every six hours a test lead is submitted with a test flag; the CRM routes it to a test pipeline stage, and the server sends a test LinkedIn and Google conversion that we verify arrived, without polluting real data. Fourth, reconciliation: weekly, CRM SQLs versus LinkedIn and Google reported conversions, with bands. Fifth, evidence: every release stores the test report and a HAR file in a dated folder. Sixth, ownership: the growth engineer owns the suite, the privacy lead reviews quarterly. That's a QA section a regulator or client would respect.
```

Scene 14 — Your next step

```text
That's the course. You now understand the real state of tracking, the laws that govern it, consent management and Consent Mode, GA4 and server-side tagging, every major conversion API, data quality and QA, first-party data strategy, and modeled measurement. Your next step: write your measurement design document, run the AI review, fix what it finds, and you'll have a blueprint any team would be glad to build.
```

</details>
