# Web Analytics with Google Analytics 4 — lecture production script

Course `web-analytics-with-ga4` · pack v2 · reviewed 2026-09 · level intermediate · category data

20 lectures · 23,407 narration words · planned runtime 2:49:43 (≈ 167 min at 140 wpm)

> Generated by `scripts/export-lecture-scripts.py` from the course pack — edit the pack, not this file. Production workflow: [README](README.md).

## Voice direction

Conversational expert, second person, ~140 wpm; clear and exact with numbers; say units; audience level: intermediate. Pause ~0.5 s between scenes; no music under narration (added in edit).

## Lectures

1. [From business goals to KPIs](#1-from-business-goals-to-kpis)
2. [Designing a tracking plan and naming conventions](#2-designing-a-tracking-plan-and-naming-conventions)
3. [Setting up a GA4 property the right way](#3-setting-up-a-ga4-property-the-right-way)
4. [Events, parameters and user properties](#4-events-parameters-and-user-properties)
5. [Key events and e-commerce tracking](#5-key-events-and-e-commerce-tracking)
6. [Custom definitions, scopes and core metrics](#6-custom-definitions-scopes-and-core-metrics)
7. [Tag manager fundamentals: tags, triggers and variables](#7-tag-manager-fundamentals-tags-triggers-and-variables)
8. [The data layer and custom events](#8-the-data-layer-and-custom-events)
9. [QA, debugging and release discipline](#9-qa-debugging-and-release-discipline)
10. [UTM discipline and channel groupings](#10-utm-discipline-and-channel-groupings)
11. [Consent and consent mode concepts](#11-consent-and-consent-mode-concepts)
12. [Privacy by design and data governance](#12-privacy-by-design-and-data-governance)
13. [Reading standard reports with confidence](#13-reading-standard-reports-with-confidence)
14. [Explorations: funnels, paths, segments and cohorts](#14-explorations-funnels-paths-segments-and-cohorts)
15. [From data to insight: an analysis workflow](#15-from-data-to-insight-an-analysis-workflow)
16. [Attribution concepts and their limits](#16-attribution-concepts-and-their-limits)
17. [Dashboards that drive decisions](#17-dashboards-that-drive-decisions)
18. [Running analytics as an ongoing practice](#18-running-analytics-as-an-ongoing-practice)
19. [The GA4 BigQuery export: setup, schema and SQL you will reuse](#19-the-ga4-bigquery-export-setup-schema-and-sql-you-will-reuse)
20. [AI-assisted GA4 analysis: Ask Advisor, Gemini, MCP and a verification ladder](#20-ai-assisted-ga4-analysis-ask-advisor-gemini-mcp-and-a-verification-ladder)

### 1. From business goals to KPIs

Lesson `from-goals-to-kpis` · module “Measurement strategy before tools” · target 9 min · 15 scenes · 1192 words · planned 8:38 (words ≈ 8:38) · status: **to produce**

| Term | Say |
|---|---|
| GA4 | G A four |
| KPI | K P I |
| KPIs | K P eyes |
| CRM | C R M |
| UTM | U T M |

| # | Time | Sec | Narration | On screen | Visual |
|---|---|---|---|---|---|
| 1 | 0:00 | 36 | Here's a question I ask every new client: if your website doubled its traffic tomorrow, would your business be better off? Most people say yes. Then I ask, how would you know? And the room goes quiet. That silence is why most analytics fails before anyone installs a tag. In this lecture you'll learn to build a measurement plan: objectives, goals, KPIs, targets and segments, each traced up to the business and down to data you can actually collect in G A four. | From goals to KPIs<br>• Why analytics fails before tagging<br>• The measurement pyramid<br>• Good KPIs versus vanity metrics<br>• A plan you can copy | Layout: a traffic chart doubles upward while a question mark over a business icon stays unresolved; the title builds beside it. 0-12s traffic line doubles; 12-24s question mark pulses over business icon; 24-36s title and bullets build. |
| 2 | 0:36 | 36 | Why does this matter? Because tools will happily show you numbers whether or not they mean anything. G A four will count page views, users and sessions from day one. But months later, nobody can answer the real questions, like which channel brings customers who buy again. The problem isn't the tool. It's that nobody agreed what success looks like and how you'd know. A measurement plan forces that conversation first, and turns tagging into an engineering task with a clear specification. | Why it matters<br>• Tools show numbers regardless of meaning<br>• Real questions stay unanswered<br>• Agree success first<br>• Tagging becomes a clear spec | Layout: a dashboard fills with generic numbers; a speech bubble 'which channel brings repeat buyers?' appears and the dashboard offers no answer. 0-12s dashboard fills with numbers; 12-24s question bubble appears; 24-36s dashboard shrugs with a blank panel. |
| 3 | 1:12 | 40 | Here's the core model: the measurement pyramid. At the top, the business objective: why the site exists. Below it, goals: what must happen on the site. Then KPIs: which numbers show progress. Then targets: what good looks like. And at the base, segments: who or what you'll compare. Think of it like a family tree. Every KPI needs parents above it, an objective it serves, and children below it, events or data sources that feed it. A KPI with no parents is a vanity metric. A KPI with no children can't be measured. | The measurement pyramid<br>• Objective<br>• Goals<br>• KPIs<br>• Targets<br>• Segments<br>• Every KPI traces up and down | Layout: a five-layer pyramid builds from the top; then a family-tree overlay connects one KPI upward to the objective and downward to event icons. 0-13s pyramid layers build top to bottom; 13-27s family tree lines draw up; 27-40s lines draw down to event icons. |
| 4 | 1:52 | 34 | What makes a good KPI? Four tests. Actionable: if it moves, someone knows what to do differently. Comparable: usually a rate or ratio, so it works when traffic fluctuates. Conversion rate beats raw conversions. Owned: a named person or team is responsible. And timely: available fast enough to influence decisions. Page views, total users and time on site rarely pass on their own. They're diagnostic metrics. They help explain why a KPI moved, but they don't define success. | Good KPI tests<br>• Actionable<br>• Comparable (a rate)<br>• Owned<br>• Timely<br>• Page views are diagnostic, not KPIs | Layout: four test stamps (actionable, comparable, owned, timely) hit a KPI card that passes; a 'page views' card fails two stamps and moves to a 'diagnostic' shelf. 0-11s four stamps hit a KPI card; 11-23s page views card fails; 23-34s moves to diagnostic shelf. |
| 5 | 2:26 | 34 | Next, macro and micro conversions. A macro conversion is the primary outcome: a purchase, a qualified lead, a subscription. Micro conversions are meaningful steps that predict it: viewing pricing, starting checkout, downloading a brochure, watching a demo. Why track them? Three reasons. They happen more often, so you get useful volumes sooner. They show where the journey breaks. And they help you evaluate upper-funnel channels, like social or video, that rarely produce last-click sales but do move people forward. | Macro and micro conversions<br>• Macro: the primary outcome<br>• Micro: steps that predict it<br>• More volume, sooner<br>• Show where journeys break<br>• Credit upper-funnel channels | Layout: a staircase with small steps labeled view pricing, start checkout, watch demo leading to a large top step labeled purchase. 0-11s small steps build; 11-23s top step appears; 23-34s a user icon climbs, pausing where it breaks. |
| 6 | 3:00 | 40 | A common worry: how do I set a target with no history? Start with a baseline period instead. Measure four to eight weeks, note seasonality, and set the first target as a modest improvement on that baseline, labeled as illustrative or provisional. Then revisit it once you have a full cycle of data. Targets can also come from finance: if the business needs two hundred new customers a quarter, and you know your approximate lead-to-customer rate, you can work backward to the lead volume and conversion rate the site must deliver. | Targets without history<br>• Measure a 4 to 8 week baseline<br>• Provisional first target<br>• Revisit after a full cycle<br>• Or work backward from finance | Layout: a baseline band forms over several weeks on a line chart; a dotted provisional target line appears slightly above; a second panel shows a backward calculation from '200 customers' to required leads. 0-13s baseline band forms; 13-27s provisional target line; 27-40s backward calculation panel. |
| 7 | 3:40 | 33 | First example, a simple one. A small online course business. Objective: grow paid enrollments profitably. Goals: visitors start a free trial, and trials convert to paid. KPIs: trial sign-up rate, trial-to-paid rate, and revenue per visitor. Target, illustratively: lift trial sign-up rate from two percent to two and a half percent this quarter. Segments: channel, device, country, new versus returning. Five lines, and suddenly everyone knows what the website is for, and what the analyst should measure. | Example 1: an online course business<br>• Objective: paid enrollments, profitably<br>• Goals: trials, trial-to-paid<br>• KPIs: sign-up rate, trial-to-paid, revenue per visitor<br>• Target: 2.0% to 2.5% (illustrative) | Layout: the pyramid from earlier fills with the course business's text layer by layer. 0-8s objective fills; 8-16s goals fill; 16-25s KPIs and target fill; 25-33s segments fill. |
| 8 | 4:13 | 38 | Second example, a business with an offline twist. A Lahore apparel brand sells nationally with cash on delivery. Its KPIs are purchase conversion rate, delivered-order rate from the courier export, repeat purchase within ninety days from the order system, and revenue per session. The plan says plainly that G A four measures orders placed, while delivered revenue lives in the order system, and the weekly report joins them by order id. That one sentence prevents months of arguments about why G A four revenue is higher than finance. | Example 2: Lahore apparel, cash on delivery<br>• GA4: orders placed<br>• Courier export: delivered orders<br>• Order system: repeat purchase<br>• Joined weekly by order id | Layout: three data source icons (GA4, courier sheet, order system) feed into a weekly report joined by an 'order id' key icon. 0-13s GA4 source appears; 13-25s courier and order sources appear; 25-38s key icon joins them into a report. |
| 9 | 4:51 | 37 | Now, a twenty twenty-six consideration. Visitors increasingly arrive from AI assistants, chat tools that cite and link sources. Google has been adding classification for this traffic in G A four's default channel group, so check your property's current channel definitions. If you need finer control, build a custom channel group with a rule that matches sources like chat G P T, perplexity, gemini, copilot and claude. Decide now whether AI assistant referrals is a segment in your plan, because leadership is already asking about it. | 2026: AI assistant traffic<br>• Growing referral source<br>• Check default channel definitions<br>• Custom channel group if needed<br>• Add it as a segment | Layout: a channel list with a new row labeled 'AI assistants' sliding in; a regex rule card appears beside it. 0-12s channel list appears; 12-25s AI assistants row slides in; 25-37s rule card appears. |
| 10 | 5:28 | 36 | Watch me build a plan row. I open a shared sheet with these columns: objective, goal, KPI, formula, data source, events or fields, baseline with period, target, owner, segments, and the decision it informs. For our B2B agency: objective, grow qualified leads. Goal, visitors book a call. KPI, form submission rate. Formula, generate lead events divided by sessions. Data source, G A four. Events, generate lead with form id and service. Baseline, one point eight percent last quarter. Owner, the growth lead. | Watch me do it, part 1<br>• One row per KPI<br>• Formula and data source<br>• Events and fields<br>• Baseline, target, owner<br>• The decision it informs | Layout: a spreadsheet builds column headers left to right, then one row fills cell by cell in sync with narration. 0-9s headers build; 9-18s objective to formula cells fill; 18-27s source and events fill; 27-36s baseline, owner and decision fill. |
| 11 | 6:04 | 41 | The last column is the one that matters most: the decision it informs. Here, the budget split across channels. If I can't write a decision, the KPI probably isn't a KPI. Then I paste the whole table into an AI assistant with a reviewer prompt. Act as a skeptical analytics lead. For each KPI, does it trace to the objective? Can it be measured with the listed events? Is it a rate? What decision changes if it moves twenty percent? Flag vanity metrics. And don't invent numbers. I get a critique, not a rewrite. | Watch me do it, part 2<br>• The decision column is the test<br>• AI as skeptical reviewer<br>• Four questions per KPI<br>• Critique, not invented numbers | Layout: the decision cell highlights; a chat panel opens with the reviewer prompt; a response lists two flagged rows in amber. 0-14s decision cell highlights; 14-27s reviewer prompt types; 27-41s response flags two rows. |
| 12 | 6:45 | 30 | Common mistakes. Tracking everything just in case, which creates noise and slows analysis. Letting the tool define success, because G A four's defaults are generic and your business isn't. No targets, so every number looks fine or alarming depending on mood. Ignoring offline outcomes, like leads that close on WhatsApp or by phone. And one plan forever. Revisit it whenever the business model, the site or the campaigns change. | Common mistakes<br>• Tracking everything<br>• Tool-defined success<br>• No targets<br>• Ignoring offline outcomes<br>• A plan that never changes | Layout: five warning cards drop in; the last shows a calendar with a 'review' reminder. 0-15s cards 1 to 3; 15-30s cards 4 and 5. |
| 13 | 7:15 | 31 | How do you know the plan is working? Three signs. Every recurring report maps to KPIs in the plan, with nothing orphaned. Meetings end with decisions tied to those KPIs, not just observations. And when someone asks for a new metric, you can say which row it belongs to, or why it doesn't. If your dashboards are full of charts nobody can connect to a decision, the plan exists on paper only. | Is the plan working?<br>• Reports map to KPIs<br>• Meetings end in decisions<br>• New metric requests find a row | Layout: three check tiles light up: a report linked to a plan row, a meeting ending with a decision stamp, a new request slotting into a row. 0-10s report tile; 10-21s meeting tile; 21-31s request tile. |
| 14 | 7:46 | 27 | Recap. Start with the business, not the tag. Build the pyramid: objective, goals, KPIs, targets, segments. Every KPI traces up to an objective and down to events or data sources. Prefer rates, name owners, set baselines. Track micro conversions for earlier signal. Plan for new sources like AI assistant traffic. And be honest about which numbers live outside G A four. | Recap<br>• Business first<br>• Trace up and down<br>• Rates, owners, baselines<br>• Micro conversions<br>• AI assistant traffic<br>• Name outside sources | Layout: the pyramid reappears with checkmarks on each layer. 0-14s pyramid reappears; 14-27s checkmarks stamp. |
| 15 | 8:13 | 25 | Try this now. Write a one-page measurement plan for a website you know: one objective, two goals, three to five KPIs with baselines or targets, the segments you'll compare, and the decision each KPI informs. Then run the skeptical reviewer prompt from the lesson on it, and fix the two weakest rows before you touch a single tag. | Try this now<br>• One objective, two goals<br>• Three to five KPIs with baselines<br>• Decision per KPI<br>• Run the reviewer prompt<br>• Fix the two weakest rows | Layout: a blank one-page template fills section by section; two rows get amber flags, then turn green after edits. 0-8s template fills; 8-17s two rows flag amber; 17-25s rows turn green. |

<details><summary>Narration per scene (paste into ElevenLabs)</summary>

Scene 1 — From goals to KPIs

```text
Here's a question I ask every new client: if your website doubled its traffic tomorrow, would your business be better off? Most people say yes. Then I ask, how would you know? And the room goes quiet. That silence is why most analytics fails before anyone installs a tag. In this lecture you'll learn to build a measurement plan: objectives, goals, KPIs, targets and segments, each traced up to the business and down to data you can actually collect in G A four.
```

Scene 2 — Why it matters

```text
Why does this matter? Because tools will happily show you numbers whether or not they mean anything. G A four will count page views, users and sessions from day one. But months later, nobody can answer the real questions, like which channel brings customers who buy again. The problem isn't the tool. It's that nobody agreed what success looks like and how you'd know. A measurement plan forces that conversation first, and turns tagging into an engineering task with a clear specification.
```

Scene 3 — The measurement pyramid

```text
Here's the core model: the measurement pyramid. At the top, the business objective: why the site exists. Below it, goals: what must happen on the site. Then KPIs: which numbers show progress. Then targets: what good looks like. And at the base, segments: who or what you'll compare. Think of it like a family tree. Every KPI needs parents above it, an objective it serves, and children below it, events or data sources that feed it. A KPI with no parents is a vanity metric. A KPI with no children can't be measured.
```

Scene 4 — Good KPI tests

```text
What makes a good KPI? Four tests. Actionable: if it moves, someone knows what to do differently. Comparable: usually a rate or ratio, so it works when traffic fluctuates. Conversion rate beats raw conversions. Owned: a named person or team is responsible. And timely: available fast enough to influence decisions. Page views, total users and time on site rarely pass on their own. They're diagnostic metrics. They help explain why a KPI moved, but they don't define success.
```

Scene 5 — Macro and micro conversions

```text
Next, macro and micro conversions. A macro conversion is the primary outcome: a purchase, a qualified lead, a subscription. Micro conversions are meaningful steps that predict it: viewing pricing, starting checkout, downloading a brochure, watching a demo. Why track them? Three reasons. They happen more often, so you get useful volumes sooner. They show where the journey breaks. And they help you evaluate upper-funnel channels, like social or video, that rarely produce last-click sales but do move people forward.
```

Scene 6 — Targets without history

```text
A common worry: how do I set a target with no history? Start with a baseline period instead. Measure four to eight weeks, note seasonality, and set the first target as a modest improvement on that baseline, labeled as illustrative or provisional. Then revisit it once you have a full cycle of data. Targets can also come from finance: if the business needs two hundred new customers a quarter, and you know your approximate lead-to-customer rate, you can work backward to the lead volume and conversion rate the site must deliver.
```

Scene 7 — Example 1: an online course business

```text
First example, a simple one. A small online course business. Objective: grow paid enrollments profitably. Goals: visitors start a free trial, and trials convert to paid. KPIs: trial sign-up rate, trial-to-paid rate, and revenue per visitor. Target, illustratively: lift trial sign-up rate from two percent to two and a half percent this quarter. Segments: channel, device, country, new versus returning. Five lines, and suddenly everyone knows what the website is for, and what the analyst should measure.
```

Scene 8 — Example 2: Lahore apparel, cash on delivery

```text
Second example, a business with an offline twist. A Lahore apparel brand sells nationally with cash on delivery. Its KPIs are purchase conversion rate, delivered-order rate from the courier export, repeat purchase within ninety days from the order system, and revenue per session. The plan says plainly that G A four measures orders placed, while delivered revenue lives in the order system, and the weekly report joins them by order id. That one sentence prevents months of arguments about why G A four revenue is higher than finance.
```

Scene 9 — 2026: AI assistant traffic

```text
Now, a twenty twenty-six consideration. Visitors increasingly arrive from AI assistants, chat tools that cite and link sources. Google has been adding classification for this traffic in G A four's default channel group, so check your property's current channel definitions. If you need finer control, build a custom channel group with a rule that matches sources like chat G P T, perplexity, gemini, copilot and claude. Decide now whether AI assistant referrals is a segment in your plan, because leadership is already asking about it.
```

Scene 10 — Watch me do it, part 1

```text
Watch me build a plan row. I open a shared sheet with these columns: objective, goal, KPI, formula, data source, events or fields, baseline with period, target, owner, segments, and the decision it informs. For our B2B agency: objective, grow qualified leads. Goal, visitors book a call. KPI, form submission rate. Formula, generate lead events divided by sessions. Data source, G A four. Events, generate lead with form id and service. Baseline, one point eight percent last quarter. Owner, the growth lead.
```

Scene 11 — Watch me do it, part 2

```text
The last column is the one that matters most: the decision it informs. Here, the budget split across channels. If I can't write a decision, the KPI probably isn't a KPI. Then I paste the whole table into an AI assistant with a reviewer prompt. Act as a skeptical analytics lead. For each KPI, does it trace to the objective? Can it be measured with the listed events? Is it a rate? What decision changes if it moves twenty percent? Flag vanity metrics. And don't invent numbers. I get a critique, not a rewrite.
```

Scene 12 — Common mistakes

```text
Common mistakes. Tracking everything just in case, which creates noise and slows analysis. Letting the tool define success, because G A four's defaults are generic and your business isn't. No targets, so every number looks fine or alarming depending on mood. Ignoring offline outcomes, like leads that close on WhatsApp or by phone. And one plan forever. Revisit it whenever the business model, the site or the campaigns change.
```

Scene 13 — Is the plan working?

```text
How do you know the plan is working? Three signs. Every recurring report maps to KPIs in the plan, with nothing orphaned. Meetings end with decisions tied to those KPIs, not just observations. And when someone asks for a new metric, you can say which row it belongs to, or why it doesn't. If your dashboards are full of charts nobody can connect to a decision, the plan exists on paper only.
```

Scene 14 — Recap

```text
Recap. Start with the business, not the tag. Build the pyramid: objective, goals, KPIs, targets, segments. Every KPI traces up to an objective and down to events or data sources. Prefer rates, name owners, set baselines. Track micro conversions for earlier signal. Plan for new sources like AI assistant traffic. And be honest about which numbers live outside G A four.
```

Scene 15 — Try this now

```text
Try this now. Write a one-page measurement plan for a website you know: one objective, two goals, three to five KPIs with baselines or targets, the segments you'll compare, and the decision each KPI informs. Then run the skeptical reviewer prompt from the lesson on it, and fix the two weakest rows before you touch a single tag.
```

</details>

### 2. Designing a tracking plan and naming conventions

Lesson `tracking-plan-and-naming` · module “Measurement strategy before tools” · target 8 min · 15 scenes · 1178 words · planned 8:31 (words ≈ 8:32) · status: **to produce**

| Term | Say |
|---|---|
| GA4 | G A four |
| KPI | K P I |
| KPIs | K P eyes |
| CRM | C R M |
| UTM | U T M |
| CNIC | C N I C |
| CSV | C S V |
| PII | P I I |
| snake_case | snake case |

| # | Time | Sec | Narration | On screen | Visual |
|---|---|---|---|---|---|
| 1 | 0:00 | 37 | Open almost any inherited analytics property and you'll find the same thing: Signup, sign underscore up, signup complete, and form submit two, all describing the same action. Nobody trusts the reports, and nobody knows which one is right. In this lecture you'll learn to write a tracking plan, the contract between marketers, analysts and developers. You'll learn to use recommended events first, to name things so they scale, to decide what not to track, and to lint the plan automatically before a developer ever sees it. | Tracking plans and naming<br>• The tracking plan as a contract<br>• Recommended events first<br>• Naming that scales<br>• What not to track<br>• Automatic linting | Layout: four near-identical event names (Signup, sign_up, signup_complete, form-submit-2) jitter on screen and then merge into one clean 'sign_up'. 0-9s four messy names appear; 9-18s they jitter; 18-28s they merge into one clean name; 28-37s title builds. |
| 2 | 0:37 | 33 | Why does a plan matter? Because analytics is a team sport, and without a written contract every player invents their own rules. Marketers ask for things vaguely. Developers implement what they think was meant. Analysts discover the gaps months later. A tracking plan says exactly what data will be collected, when, with which details, and who implements it. It's the difference between building a house from blueprints and building it from a conversation you half remember. | Why a plan<br>• Analytics is a team sport<br>• Vague asks, guessed implementations<br>• Gaps found months later<br>• Blueprints, not memories | Layout: three role icons (marketer, developer, analyst) pass a ball that gets dropped; then a blueprint document appears and the ball passes cleanly. 0-11s roles pass and drop the ball; 11-22s blueprint appears; 22-33s clean pass. |
| 3 | 1:10 | 33 | Here's the anatomy of a plan row. The event name: a stable machine name like generate lead. The trigger: precisely when it fires, for example when the server confirms the form succeeded. The parameters: context sent with it, like form id and service. Allowed values for each parameter. Whether it's a key event. Which KPI it supports. The owner and status. And where it's sent. Treat changes like code changes: proposed, reviewed, implemented, tested, then marked live. | A plan row<br>• Event name and trigger<br>• Parameters and allowed values<br>• Key event? KPI supported<br>• Owner, status, destinations<br>• Changes like code changes | Layout: a single plan row expands into labeled fields; a small workflow strip (proposed, reviewed, implemented, tested, live) runs underneath. 0-11s row expands field by field; 11-22s workflow strip draws; 22-33s status moves to live. |
| 4 | 1:43 | 34 | Next, use recommended events first. G A four has predefined names and parameters for common actions: login, sign up, generate lead, search, view item, add to cart, begin checkout and purchase. Using them matters. Some reports, notably e-commerce, expect those exact names. Future features are more likely to support them. And new team members recognize them instantly. Think of recommended events like standard electrical sockets. You could wire your own custom plug, but then nothing you buy will fit. | Recommended events first<br>• login, sign_up, generate_lead, search<br>• view_item, add_to_cart, begin_checkout, purchase<br>• Reports expect them<br>• Standard sockets, not custom plugs | Layout: a wall with standard sockets labeled with recommended event names; a custom-shaped plug fails to fit while a standard plug clicks in. 0-11s sockets labeled; 11-23s custom plug fails; 23-34s standard plug clicks in. |
| 5 | 2:17 | 37 | Now naming conventions. Agree once and write them at the top of the plan. Event names in snake case, verb then object, like download brochure. Parameter names as nouns, like file name or plan tier. Values lowercase from controlled lists. And avoid personal data, free text, dates or version numbers in names. The classic anti-pattern is encoding context into the name, like click pricing button homepage and click pricing button blog. Instead, one event, select plan, with a parameter for location. Fewer names, richer analysis. | Naming conventions<br>• Events: snake_case, verb_object<br>• Parameters: nouns<br>• Values: lowercase, controlled lists<br>• No PII, dates or versions<br>• One event plus parameters | Layout: two event names with page context collapse into one 'select_plan' event with a 'location' parameter tag showing homepage and blog values. 0-12s two long names appear; 12-25s they collapse into one event; 25-37s parameter tag attaches. |
| 6 | 2:54 | 37 | Design within G A four's limits, and check the current limits page because they can change. As documented, event and parameter names can be up to forty characters, parameter values up to a hundred characters on standard properties, and an event can carry up to twenty-five parameters. Names are case-sensitive, so capital S sign up and lowercase sign up are different events. And some prefixes, like G A underscore, google underscore and firebase underscore, are reserved. A plan that respects limits never surprises you in production. | Collection limits (check current docs)<br>• Names up to 40 characters<br>• Values up to 100 characters<br>• Up to 25 parameters per event<br>• Case-sensitive<br>• Reserved prefixes | Layout: a ruler measures an event name at forty characters; a parameter stack caps at twenty-five; two names differing only in case are shown as separate rows. 0-12s ruler measures name; 12-25s parameter stack caps; 25-37s case-sensitive rows split. |
| 7 | 3:31 | 40 | First example, a simple one. A project management software company selling in the UK, the UAE and Saudi Arabia plans its trial funnel. View pricing, custom, with billing period. Select plan, custom, with plan tier, billing period and location. Sign up, recommended, fired when the account is created and confirmed by the server, and marked as a key event. Tutorial complete, recommended. And purchase, recommended, with transaction id, value, currency and items, also a key event. Five rows, and the team can build a funnel from pricing to purchase and compare markets. | Example 1: a SaaS trial funnel<br>• view_pricing, select_plan (custom)<br>• sign_up (recommended, key event)<br>• tutorial_complete (recommended)<br>• purchase (recommended, key event) | Layout: a five-row plan table fills; then the rows turn into a funnel diagram with a market filter for UK, UAE and KSA. 0-13s table fills; 13-27s rows become funnel steps; 27-40s market filter appears. |
| 8 | 4:11 | 35 | Second example, a business scaling problem. A Manchester agency manages twenty G A four properties with twenty naming styles. It publishes one house convention, a template plan with recommended events pre-filled for e-commerce, lead generation and software, and runs a lint script during every client onboarding. For the first time, cross-client benchmarks are possible, like comparing lead rates across similar clients, and new analysts ramp up in days instead of weeks. Consistency turned twenty messy properties into one learnable system. | Example 2: an agency with 20 properties<br>• One house convention<br>• Template plans by business model<br>• Lint on onboarding<br>• Benchmarks and faster ramp-up | Layout: twenty small property tiles with different colored labels become uniformly styled; a benchmark bar chart appears comparing them. 0-12s twenty mixed tiles; 12-23s tiles standardize; 23-35s benchmark chart appears. |
| 9 | 4:46 | 35 | One more habit: version the plan like software. Every row has a status: proposed, approved, implemented, verified, live, or retired. Every change gets a date and a reason. When you retire an event, you don't delete the row, you mark it retired with the date, so anyone reading old reports knows why the event disappeared. And link the plan from your dashboards. When a stakeholder asks what a number means, the answer is one click away, not in someone's memory. | Version the plan<br>• Status per row<br>• Dated changes with reasons<br>• Retire, don't delete<br>• Link the plan from dashboards | Layout: a plan row cycles through status chips: proposed, approved, implemented, verified, live, retired; a dashboard corner shows a 'tracking plan' link icon. 0-12s status chips cycle; 12-23s retired row grays but stays; 23-35s dashboard link icon appears. |
| 10 | 5:21 | 40 | Watch me lint a plan. I export the tracking plan as a CSV with columns for event name, parameters, trigger, key event and owner. The lesson's Python script checks each row. Is the event name snake case, starting with a letter, forty characters or fewer? Does it start with a reserved prefix? Are there more than twenty-five parameters? Is each parameter name valid? Does any parameter name hint at personal data, like email, phone, or regional identifiers such as C N I C or iqama numbers? And does every row have an owner? | Watch me do it, part 1<br>• Export plan as CSV<br>• Check name format and length<br>• Reserved prefixes, parameter count<br>• PII hints incl. CNIC, iqama<br>• Owner present | Layout: code editor with the lint script; each check line highlights as narrated; a CSV preview sits beside it. 0-10s CSV preview appears; 10-20s name checks highlight; 20-30s parameter and PII checks highlight; 30-40s owner check highlights. |
| 11 | 6:01 | 37 | I run it. It prints three problems. An event called Form Submit with capitals and a space. A parameter called user email, flagged as possible personal data. And a row with no owner. I fix the name to generate lead, delete the email parameter entirely, because G A four must never receive personal data, and assign the owner. I rerun, and it prints plan looks clean. That took two minutes, and it prevented three problems that would have taken weeks to discover in reports. | Watch me do it, part 2<br>• Three problems found<br>• Rename, delete PII, assign owner<br>• Rerun: clean<br>• Two minutes versus weeks | Layout: terminal prints three red lines; the CSV rows are edited; rerun prints 'plan looks clean' in green. 0-12s three red lines; 12-25s rows edited; 25-37s green clean message. |
| 12 | 6:38 | 35 | And what not to track? Every event costs implementation time, testing, documentation and attention in reports. For each proposed event, ask three questions. Which KPI or decision does it inform? Would we act differently if this number doubled or halved? Does enhanced measurement or an existing event already capture it? If the answers are weak, leave it out. You can always add it later, but it's very hard to get teams to stop looking at a number once it exists. | What not to track<br>• Which KPI or decision?<br>• Would we act if it doubled or halved?<br>• Already captured?<br>• If weak, leave it out | Layout: a proposed event card passes through three question gates; it fails the second gate and drops into a 'maybe later' tray. 0-12s card enters gate 1; 12-23s fails gate 2; 23-35s drops into maybe-later tray. |
| 13 | 7:13 | 24 | Common mistakes. Firing on the button click rather than on success, so failed validations count as leads. Inconsistent casing. Sending personal data in parameters or URLs, which breaks Google's policies and often privacy law. And no owner, so the plan rots as the site changes. Each of these shows up months later as numbers nobody trusts. | Common mistakes<br>• Firing on click, not success<br>• Inconsistent casing<br>• PII in parameters or URLs<br>• No owner | Layout: four warning cards; the first shows a submit button with a red cross and a success check beside it. 0-12s card 1 with button example; 12-24s cards 2 to 4. |
| 14 | 7:37 | 28 | Recap. A tracking plan is the contract between marketing, analytics and development. Use recommended events first and custom events only when nothing fits. Name with one convention: snake case, verb then object, nouns for parameters, controlled values. Use one event with parameters rather than many near-duplicates. Respect G A four's limits, fire on confirmed success, never send personal data, and lint the plan automatically. | Recap<br>• The plan is a contract<br>• Recommended first<br>• One naming convention<br>• Parameters over duplicate names<br>• Limits, success states, no PII, lint | Layout: recap icons appear in a row with checkmarks. 0-14s icons appear; 14-28s checkmarks stamp. |
| 15 | 8:05 | 26 | Try this now. Draft a tracking plan with at least six rows for a site you know, using recommended events wherever possible and marking which are key events. Export it as a CSV, run the lint script from the lesson, and fix every problem it prints. Then ask a developer whether they could implement it without asking you a question. | Try this now<br>• Six-row tracking plan<br>• Recommended events, key events marked<br>• Run the lint script<br>• Developer readability test | Layout: plan template fills six rows; lint output shows clean; a developer icon gives a thumbs up. 0-9s six rows fill; 9-17s lint clean; 17-26s thumbs up. |

<details><summary>Narration per scene (paste into ElevenLabs)</summary>

Scene 1 — Tracking plans and naming

```text
Open almost any inherited analytics property and you'll find the same thing: Signup, sign underscore up, signup complete, and form submit two, all describing the same action. Nobody trusts the reports, and nobody knows which one is right. In this lecture you'll learn to write a tracking plan, the contract between marketers, analysts and developers. You'll learn to use recommended events first, to name things so they scale, to decide what not to track, and to lint the plan automatically before a developer ever sees it.
```

Scene 2 — Why a plan

```text
Why does a plan matter? Because analytics is a team sport, and without a written contract every player invents their own rules. Marketers ask for things vaguely. Developers implement what they think was meant. Analysts discover the gaps months later. A tracking plan says exactly what data will be collected, when, with which details, and who implements it. It's the difference between building a house from blueprints and building it from a conversation you half remember.
```

Scene 3 — A plan row

```text
Here's the anatomy of a plan row. The event name: a stable machine name like generate lead. The trigger: precisely when it fires, for example when the server confirms the form succeeded. The parameters: context sent with it, like form id and service. Allowed values for each parameter. Whether it's a key event. Which KPI it supports. The owner and status. And where it's sent. Treat changes like code changes: proposed, reviewed, implemented, tested, then marked live.
```

Scene 4 — Recommended events first

```text
Next, use recommended events first. G A four has predefined names and parameters for common actions: login, sign up, generate lead, search, view item, add to cart, begin checkout and purchase. Using them matters. Some reports, notably e-commerce, expect those exact names. Future features are more likely to support them. And new team members recognize them instantly. Think of recommended events like standard electrical sockets. You could wire your own custom plug, but then nothing you buy will fit.
```

Scene 5 — Naming conventions

```text
Now naming conventions. Agree once and write them at the top of the plan. Event names in snake case, verb then object, like download brochure. Parameter names as nouns, like file name or plan tier. Values lowercase from controlled lists. And avoid personal data, free text, dates or version numbers in names. The classic anti-pattern is encoding context into the name, like click pricing button homepage and click pricing button blog. Instead, one event, select plan, with a parameter for location. Fewer names, richer analysis.
```

Scene 6 — Collection limits (check current docs)

```text
Design within G A four's limits, and check the current limits page because they can change. As documented, event and parameter names can be up to forty characters, parameter values up to a hundred characters on standard properties, and an event can carry up to twenty-five parameters. Names are case-sensitive, so capital S sign up and lowercase sign up are different events. And some prefixes, like G A underscore, google underscore and firebase underscore, are reserved. A plan that respects limits never surprises you in production.
```

Scene 7 — Example 1: a SaaS trial funnel

```text
First example, a simple one. A project management software company selling in the UK, the UAE and Saudi Arabia plans its trial funnel. View pricing, custom, with billing period. Select plan, custom, with plan tier, billing period and location. Sign up, recommended, fired when the account is created and confirmed by the server, and marked as a key event. Tutorial complete, recommended. And purchase, recommended, with transaction id, value, currency and items, also a key event. Five rows, and the team can build a funnel from pricing to purchase and compare markets.
```

Scene 8 — Example 2: an agency with 20 properties

```text
Second example, a business scaling problem. A Manchester agency manages twenty G A four properties with twenty naming styles. It publishes one house convention, a template plan with recommended events pre-filled for e-commerce, lead generation and software, and runs a lint script during every client onboarding. For the first time, cross-client benchmarks are possible, like comparing lead rates across similar clients, and new analysts ramp up in days instead of weeks. Consistency turned twenty messy properties into one learnable system.
```

Scene 9 — Version the plan

```text
One more habit: version the plan like software. Every row has a status: proposed, approved, implemented, verified, live, or retired. Every change gets a date and a reason. When you retire an event, you don't delete the row, you mark it retired with the date, so anyone reading old reports knows why the event disappeared. And link the plan from your dashboards. When a stakeholder asks what a number means, the answer is one click away, not in someone's memory.
```

Scene 10 — Watch me do it, part 1

```text
Watch me lint a plan. I export the tracking plan as a CSV with columns for event name, parameters, trigger, key event and owner. The lesson's Python script checks each row. Is the event name snake case, starting with a letter, forty characters or fewer? Does it start with a reserved prefix? Are there more than twenty-five parameters? Is each parameter name valid? Does any parameter name hint at personal data, like email, phone, or regional identifiers such as C N I C or iqama numbers? And does every row have an owner?
```

Scene 11 — Watch me do it, part 2

```text
I run it. It prints three problems. An event called Form Submit with capitals and a space. A parameter called user email, flagged as possible personal data. And a row with no owner. I fix the name to generate lead, delete the email parameter entirely, because G A four must never receive personal data, and assign the owner. I rerun, and it prints plan looks clean. That took two minutes, and it prevented three problems that would have taken weeks to discover in reports.
```

Scene 12 — What not to track

```text
And what not to track? Every event costs implementation time, testing, documentation and attention in reports. For each proposed event, ask three questions. Which KPI or decision does it inform? Would we act differently if this number doubled or halved? Does enhanced measurement or an existing event already capture it? If the answers are weak, leave it out. You can always add it later, but it's very hard to get teams to stop looking at a number once it exists.
```

Scene 13 — Common mistakes

```text
Common mistakes. Firing on the button click rather than on success, so failed validations count as leads. Inconsistent casing. Sending personal data in parameters or URLs, which breaks Google's policies and often privacy law. And no owner, so the plan rots as the site changes. Each of these shows up months later as numbers nobody trusts.
```

Scene 14 — Recap

```text
Recap. A tracking plan is the contract between marketing, analytics and development. Use recommended events first and custom events only when nothing fits. Name with one convention: snake case, verb then object, nouns for parameters, controlled values. Use one event with parameters rather than many near-duplicates. Respect G A four's limits, fire on confirmed success, never send personal data, and lint the plan automatically.
```

Scene 15 — Try this now

```text
Try this now. Draft a tracking plan with at least six rows for a site you know, using recommended events wherever possible and marking which are key events. Export it as a CSV, run the lint script from the lesson, and fix every problem it prints. Then ask a developer whether they could implement it without asking you a question.
```

</details>

### 3. Setting up a GA4 property the right way

Lesson `ga4-property-setup` · module “Measurement strategy before tools” · target 8 min · 14 scenes · 1150 words · planned 8:19 (words ≈ 8:20) · status: **to produce**

| Term | Say |
|---|---|
| GA4 | G A four |
| KPI | K P I |
| KPIs | K P eyes |
| CRM | C R M |
| UTM | U T M |
| MCP | M C P |
| SSO | S S O |
| iOS | eye O S |

| # | Time | Sec | Narration | On screen | Visual |
|---|---|---|---|---|---|
| 1 | 0:00 | 38 | Some G A four settings are like the foundation of a house. You can repaint the walls any time, but if the foundation is wrong, you're living with it, and you can't fix the past. Data retention, filters and unwanted referrals all work this way. In this lecture you'll learn how accounts, properties and data streams fit together, the day-one configuration checklist, what changed in twenty twenty-six with data controls and AI in the interface, and a thirty-minute audit you can run on any property you inherit. | GA4 property setup<br>• Accounts, properties, data streams<br>• Day-one configuration<br>• 2026: data controls and Advisor<br>• A 30-minute audit | Layout: a house cross-section: walls repaint in different colors freely while the foundation is labeled 'retention, filters, referrals' and stays fixed. 0-10s house draws; 10-19s walls repaint; 19-28s foundation labels appear; 28-38s title builds. |
| 2 | 0:38 | 34 | Why is setup so important? Because several settings can't be fixed retroactively. Filtered data is gone forever. A short retention window limits how far back explorations can look. Payment-gateway referrals, if not excluded, misattribute sales from day one. And ownership matters when relationships end. Agencies and freelancers come and go. If the property lives in someone else's account, the business can lose its own history. A good setup is cheap on day one and very expensive to fix later. | Why setup matters<br>• Some settings aren't retroactive<br>• Filtered data is gone<br>• Retention limits explorations<br>• Ownership matters when people leave | Layout: a timeline where early data blocks are grayed out after a wrong setting; a padlock labeled 'owner' sits on the client's side of a fence. 0-17s timeline with grayed early data; 17-34s padlock appears on client side. |
| 3 | 1:12 | 36 | Here's the structure. The account is usually one per organization, the legal entity that owns the data. The property is a reporting unit, typically one per brand or product, and it can combine web and app data. And the data stream is a source: a website, an iOS app or an Android app. Think of it like a company, its shops and the tills in each shop. Agencies should work inside the client's account with granted access, never the other way round. | Structure<br>• Account: the organization<br>• Property: the reporting unit<br>• Data stream: web, iOS, Android<br>• Company, shops, tills<br>• Client owns the account | Layout: nested boxes: account contains two properties, each containing stream icons; an analogy overlay shows company, shops and tills. 0-9s account box; 9-18s property boxes; 18-27s stream icons; 27-36s analogy overlay. |
| 4 | 1:48 | 33 | One property or several? Use one when the same users move across domains or apps you want to analyze together, like a shop subdomain and a blog subdomain. Use separate properties for genuinely separate businesses with different audiences and owners. Then work the day-one checklist: time zone and currency, data retention, enhanced measurement, internal and developer traffic, unwanted referrals, cross-domain measurement, key events, product links to Google Ads, Search Console and BigQuery, and access with least privilege. | Day-one checklist<br>• Time zone, currency, retention<br>• Enhanced measurement review<br>• Internal traffic, unwanted referrals<br>• Cross-domain, key events<br>• Product links, least-privilege access | Layout: a checklist of ten items ticks down; two items pulse to show they can't be fixed later: retention and filters. 0-11s first five items tick; 11-22s last five items tick; 22-33s retention and filters pulse. |
| 5 | 2:21 | 34 | Let's talk about data retention and filters, because they're the foundation. Standard properties default to two months of event-level retention, adjustable up to fourteen. That setting controls how far back explorations and funnel reports can look, not the standard reports. And data filters, for internal and developer traffic, are permanent once active. Filtered data isn't recoverable. So always create a filter in testing state first, check the test dimension shows the expected traffic, and only then activate it. | Retention and filters<br>• Default 2 months, up to 14<br>• Affects explorations and funnels<br>• Filters are permanent<br>• Test state first, then activate | Layout: a retention dial turns from 2 to 14 months; a filter switch shows three states: off, testing, active, with a warning icon on active. 0-8s dial turns; 8-17s filter switch moves to testing; 17-26s verification check; 26-34s switch moves to active. |
| 6 | 2:55 | 37 | Now, what changed in twenty twenty-six. First, data controls. Google announced that from June fifteenth, twenty twenty-six, the Google signals setting controls only the association of analytics data with signed-in user information for behavioral reporting. For linked properties, Google Ads settings control Ads data, and Ads cookie collection is governed by consent mode's ad storage signal. So review your Ads links, consent setup and privacy notice together. Second, Analytics Advisor: a Gemini-powered assistant inside G A four that answers plain-language questions. Useful, but verify its answers. | 2026 changes<br>• From 15 June 2026: signals = behavioral reporting only<br>• Ads data governed by Ads settings and consent mode<br>• Review links, consent and notice together<br>• Analytics Advisor: verify answers | Layout: a settings panel with a 'Google signals' toggle whose scope narrows to 'behavioral reporting'; a link line to an Ads icon routes through a consent shield; a chat bubble icon labeled 'Advisor' appears. 0-12s signals scope narrows; 12-25s Ads link routes through consent; 25-37s Advisor bubble appears. |
| 7 | 3:32 | 41 | Enhanced measurement deserves its own review. It collects useful events for free: scrolls at around ninety percent depth, outbound clicks, site search, file downloads and embedded video engagement. But check each one. Form interactions can fire on forms you don't care about or miss forms built with unusual frameworks, so many teams switch it off and track forms explicitly. Site search relies on query parameters like q or s, so add yours if it differs. And page views on history changes help single-page apps, but can double count if developers also send manual page views. | Review enhanced measurement<br>• Useful free events<br>• Form interactions: often off, track explicitly<br>• Add your site search parameter<br>• History-change page views can double count | Layout: a toggle panel with six enhanced measurement switches; form interactions switches off with a note; a search parameter field gets 'query' added; a double-count warning appears on history changes. 0-10s toggle panel appears; 10-20s form interactions off; 20-31s search parameter added; 31-41s double-count warning. |
| 8 | 4:13 | 37 | First example, a simple one. A Karachi fashion brand's checkout runs through a third-party payment gateway on a different domain. Without configuration, every returning buyer starts a new session attributed to the gateway as a referral, and paid campaigns look like they generate no sales. The fix: add the gateway domain to unwanted referrals, confirm the purchase event fires on the confirmation page with a unique transaction id, and compare channel revenue for a week before and after. Paid and organic channels now get the credit. | Example 1: payment gateway referrals<br>• Gateway steals the credit<br>• Add to unwanted referrals<br>• Confirm purchase with transaction id<br>• Compare before and after | Layout: a user journey: store to gateway domain and back; before fix, the purchase attributes to 'gateway referral'; after fix, it attributes to 'paid social'. 0-9s journey arrows draw; 9-18s wrong attribution label; 18-28s fix applied; 28-37s correct attribution label. |
| 9 | 4:50 | 40 | Second example, a business case. A healthcare group in Abu Dhabi and Dubai runs one site per clinic brand. The audit finds the properties under a former agency's account, retention at two months, no internal traffic filter while staff check appointment pages all day, and no cross-domain setting for the shared booking domain. The fix plan: transfer ownership to the group's own account, raise retention with a documented reason, test and activate an internal filter, configure cross-domain, and restrict access. And the change log records that data before the fixes isn't comparable. | Example 2: UAE clinic group<br>• Wrong owner, short retention<br>• No internal filter, no cross-domain<br>• Transfer, raise, test, configure, restrict<br>• Log the break in comparability | Layout: an audit sheet with red rows turning green one by one; a change-log entry types 'data before fixes not comparable'. 0-13s red rows appear; 13-27s rows turn green in order; 27-40s change-log entry types. |
| 10 | 5:30 | 33 | Watch me run the thirty-minute audit. I open a sheet with a red, amber, green column, and walk through admin in order. Property details: is the client the owner? Time zone and currency right? Data retention: two or fourteen months, and is there a documented reason? Enhanced measurement: did someone actually review each toggle, especially form interactions? Tag settings: internal traffic defined, unwanted referrals including payment gateways and single sign-on domains, cross-domain where journeys span domains. | Watch me do it, part 1<br>• Owner, time zone, currency<br>• Retention with a reason<br>• Enhanced measurement reviewed<br>• Internal traffic, referrals, cross-domain | Layout: a simulated admin navigation path highlights sections in order while an audit sheet fills colors beside it. 0-8s property details; 8-16s retention; 16-25s enhanced measurement; 25-33s tag settings. |
| 11 | 6:03 | 37 | Then filters: active, and were they tested first? Key events: only true business outcomes, not add to cart and page view. Product links: Google Ads, Search Console and BigQuery linked as intended. Access: named people only, least privilege, no ex-staff or ex-agencies. And data controls: Google signals and Ads settings reviewed against the June twenty twenty-six changes and your consent setup. If you prefer, you can pull settings programmatically with the Admin API, or with Google's experimental Analytics M C P server, using read-only credentials. | Watch me do it, part 2<br>• Filters tested?<br>• Key events: true outcomes only<br>• Product links<br>• Access: least privilege<br>• Data controls vs June 2026<br>• Admin API or MCP server, read-only | Layout: the audit sheet continues filling; a small panel shows an API icon and an MCP plug icon labeled 'read-only'. 0-9s filters and key events rows; 9-18s links and access rows; 18-28s data controls row; 28-37s API and MCP panel. |
| 12 | 6:40 | 32 | Common mistakes. Creating the property under an agency or freelancer's personal account. Leaving retention at the default and discovering too late that year-over-year explorations are impossible. Activating an internal-traffic filter without testing it. Forgetting unwanted referrals for payment gateways. Marking dozens of events as key events, which makes conversion rates meaningless. And changing Ads data settings without looking at consent mode, which since June twenty twenty-six is where Ads identifier collection is controlled. | Common mistakes<br>• Agency-owned property<br>• Default retention<br>• Untested filters<br>• Missing gateway referrals<br>• Too many key events<br>• Ads settings without consent review | Layout: six warning cards drop into a grid. 0-16s cards 1 to 3; 16-32s cards 4 to 6. |
| 13 | 7:12 | 40 | Recap. Account, property, data stream: organization, reporting unit, source. The client owns the account. Work the day-one checklist, with extra care for retention, filters and unwanted referrals, because they can't fix the past. Know the twenty twenty-six changes to data controls and the new Advisor. And run the thirty-minute audit on anything you inherit. Most properties you'll meet have at least three of the problems we covered today, usually retention, filters and referrals. Finding them early is one of the most valuable things an analyst can do in their first week. | Recap<br>• Structure and ownership<br>• Day-one checklist<br>• Foundation settings<br>• 2026 changes<br>• 30-minute audit | Layout: recap card with the house foundation illustration and five checkmarks. 0-20s foundation illustration; 20-40s checkmarks. |
| 14 | 7:52 | 27 | Try this now. Audit a G A four property you have access to, using the sequence from the lesson. For every amber or red item, write the business impact and the fix, then sort them by impact. Present the top three to whoever owns the site this week, and log each change with a date so future you knows when the data changed. | Try this now<br>• Run the audit<br>• Impact and fix per issue<br>• Sort by impact<br>• Present the top three<br>• Log every change | Layout: audit sheet sorts itself by impact; top three rows lift into a small presentation card; a change log gains dated rows. 0-9s sheet sorts; 9-18s top three lift; 18-27s log rows appear. |

<details><summary>Narration per scene (paste into ElevenLabs)</summary>

Scene 1 — GA4 property setup

```text
Some G A four settings are like the foundation of a house. You can repaint the walls any time, but if the foundation is wrong, you're living with it, and you can't fix the past. Data retention, filters and unwanted referrals all work this way. In this lecture you'll learn how accounts, properties and data streams fit together, the day-one configuration checklist, what changed in twenty twenty-six with data controls and AI in the interface, and a thirty-minute audit you can run on any property you inherit.
```

Scene 2 — Why setup matters

```text
Why is setup so important? Because several settings can't be fixed retroactively. Filtered data is gone forever. A short retention window limits how far back explorations can look. Payment-gateway referrals, if not excluded, misattribute sales from day one. And ownership matters when relationships end. Agencies and freelancers come and go. If the property lives in someone else's account, the business can lose its own history. A good setup is cheap on day one and very expensive to fix later.
```

Scene 3 — Structure

```text
Here's the structure. The account is usually one per organization, the legal entity that owns the data. The property is a reporting unit, typically one per brand or product, and it can combine web and app data. And the data stream is a source: a website, an iOS app or an Android app. Think of it like a company, its shops and the tills in each shop. Agencies should work inside the client's account with granted access, never the other way round.
```

Scene 4 — Day-one checklist

```text
One property or several? Use one when the same users move across domains or apps you want to analyze together, like a shop subdomain and a blog subdomain. Use separate properties for genuinely separate businesses with different audiences and owners. Then work the day-one checklist: time zone and currency, data retention, enhanced measurement, internal and developer traffic, unwanted referrals, cross-domain measurement, key events, product links to Google Ads, Search Console and BigQuery, and access with least privilege.
```

Scene 5 — Retention and filters

```text
Let's talk about data retention and filters, because they're the foundation. Standard properties default to two months of event-level retention, adjustable up to fourteen. That setting controls how far back explorations and funnel reports can look, not the standard reports. And data filters, for internal and developer traffic, are permanent once active. Filtered data isn't recoverable. So always create a filter in testing state first, check the test dimension shows the expected traffic, and only then activate it.
```

Scene 6 — 2026 changes

```text
Now, what changed in twenty twenty-six. First, data controls. Google announced that from June fifteenth, twenty twenty-six, the Google signals setting controls only the association of analytics data with signed-in user information for behavioral reporting. For linked properties, Google Ads settings control Ads data, and Ads cookie collection is governed by consent mode's ad storage signal. So review your Ads links, consent setup and privacy notice together. Second, Analytics Advisor: a Gemini-powered assistant inside G A four that answers plain-language questions. Useful, but verify its answers.
```

Scene 7 — Review enhanced measurement

```text
Enhanced measurement deserves its own review. It collects useful events for free: scrolls at around ninety percent depth, outbound clicks, site search, file downloads and embedded video engagement. But check each one. Form interactions can fire on forms you don't care about or miss forms built with unusual frameworks, so many teams switch it off and track forms explicitly. Site search relies on query parameters like q or s, so add yours if it differs. And page views on history changes help single-page apps, but can double count if developers also send manual page views.
```

Scene 8 — Example 1: payment gateway referrals

```text
First example, a simple one. A Karachi fashion brand's checkout runs through a third-party payment gateway on a different domain. Without configuration, every returning buyer starts a new session attributed to the gateway as a referral, and paid campaigns look like they generate no sales. The fix: add the gateway domain to unwanted referrals, confirm the purchase event fires on the confirmation page with a unique transaction id, and compare channel revenue for a week before and after. Paid and organic channels now get the credit.
```

Scene 9 — Example 2: UAE clinic group

```text
Second example, a business case. A healthcare group in Abu Dhabi and Dubai runs one site per clinic brand. The audit finds the properties under a former agency's account, retention at two months, no internal traffic filter while staff check appointment pages all day, and no cross-domain setting for the shared booking domain. The fix plan: transfer ownership to the group's own account, raise retention with a documented reason, test and activate an internal filter, configure cross-domain, and restrict access. And the change log records that data before the fixes isn't comparable.
```

Scene 10 — Watch me do it, part 1

```text
Watch me run the thirty-minute audit. I open a sheet with a red, amber, green column, and walk through admin in order. Property details: is the client the owner? Time zone and currency right? Data retention: two or fourteen months, and is there a documented reason? Enhanced measurement: did someone actually review each toggle, especially form interactions? Tag settings: internal traffic defined, unwanted referrals including payment gateways and single sign-on domains, cross-domain where journeys span domains.
```

Scene 11 — Watch me do it, part 2

```text
Then filters: active, and were they tested first? Key events: only true business outcomes, not add to cart and page view. Product links: Google Ads, Search Console and BigQuery linked as intended. Access: named people only, least privilege, no ex-staff or ex-agencies. And data controls: Google signals and Ads settings reviewed against the June twenty twenty-six changes and your consent setup. If you prefer, you can pull settings programmatically with the Admin API, or with Google's experimental Analytics M C P server, using read-only credentials.
```

Scene 12 — Common mistakes

```text
Common mistakes. Creating the property under an agency or freelancer's personal account. Leaving retention at the default and discovering too late that year-over-year explorations are impossible. Activating an internal-traffic filter without testing it. Forgetting unwanted referrals for payment gateways. Marking dozens of events as key events, which makes conversion rates meaningless. And changing Ads data settings without looking at consent mode, which since June twenty twenty-six is where Ads identifier collection is controlled.
```

Scene 13 — Recap

```text
Recap. Account, property, data stream: organization, reporting unit, source. The client owns the account. Work the day-one checklist, with extra care for retention, filters and unwanted referrals, because they can't fix the past. Know the twenty twenty-six changes to data controls and the new Advisor. And run the thirty-minute audit on anything you inherit. Most properties you'll meet have at least three of the problems we covered today, usually retention, filters and referrals. Finding them early is one of the most valuable things an analyst can do in their first week.
```

Scene 14 — Try this now

```text
Try this now. Audit a G A four property you have access to, using the sequence from the lesson. For every amber or red item, write the business impact and the fix, then sort them by impact. Present the top three to whoever owns the site this week, and log each change with a date so future you knows when the data changed.
```

</details>

### 4. Events, parameters and user properties

Lesson `events-parameters-user-properties` · module “The GA4 data model” · target 8 min · 15 scenes · 1174 words · planned 8:32 (words ≈ 8:30) · status: **to produce**

| Term | Say |
|---|---|
| GA4 | G A four |
| gtag | G tag |
| SKU | S K U |
| AED | A E D |
| ISO | I S O |
| GTM | G T M |
| DebugView | debug view |
| UNNEST | un-nest |
| BigQuery | Big Query |

| # | Time | Sec | Narration | On screen | Visual |
|---|---|---|---|---|---|
| 1 | 0:00 | 37 | If you learned web analytics years ago, you might still think in page views, sessions and bounce rate. G A four thinks differently. Everything is an event: a page view, a scroll, a purchase, even the start of a session. Sessions and users still exist, but they're built from events. In this lecture you'll learn the event model properly: event names, parameters, user properties, the four families of events, how engagement is defined, and what the same event looks like in the raw BigQuery export. | Events, parameters, user properties<br>• Everything is an event<br>• Parameters versus user properties<br>• Four families of events<br>• Engagement definitions<br>• A peek at the raw export | Layout: a stream of small event chips (page_view, scroll, purchase, session_start) flows into a funnel labeled 'sessions and users are derived'. 0-9s event chips stream in; 9-18s funnel appears; 18-28s derived label; 28-37s title builds. |
| 2 | 0:37 | 37 | Why does the model matter? Because every report, exploration and export you'll ever use is built from it. If you understand that a purchase is an event with parameters like value, currency and items, you'll know why revenue breaks when currency is missing. If you understand that user properties describe the person, you'll segment correctly. And when a stakeholder asks for a new report, you'll know whether it's a new event, a new parameter, or just a new view of data you already collect. | Why the model matters<br>• Every report is built on it<br>• Diagnose broken numbers<br>• Segment correctly<br>• New event, new parameter, or new view? | Layout: a stack of report cards rests on a foundation block labeled 'event model'; a question card routes to one of three boxes: new event, new parameter, new view. 0-12s report stack on foundation; 12-25s question card appears; 25-37s routes to 'new view'. |
| 3 | 1:14 | 35 | Here's the core idea with an analogy. Think of a receipt from a coffee shop. The event name is what happened: purchase. The parameters are the lines on the receipt: the items, the price, the currency, the branch. And the user properties are on the loyalty card: gold member, prefers oat milk. The receipt describes one moment. The loyalty card describes the person across every visit. Ask yourself one question every time: does this describe the action, or the person? | The receipt analogy<br>• Event name: what happened<br>• Parameters: the receipt lines<br>• User properties: the loyalty card<br>• Action or person? | Layout: a receipt illustration with labeled lines (event name, parameters) and a loyalty card beside it labeled 'user properties'. 0-9s receipt draws; 9-18s lines labeled; 18-26s loyalty card slides in; 26-35s question banner appears. |
| 4 | 1:49 | 42 | Events come in four families, and there's a priority order. Automatically collected events are sent by the Google tag by default, like first visit, session start and user engagement. Enhanced measurement events are toggled on in the web stream, like page view, scroll, outbound click, file download and site search. Recommended events are ones you implement using Google's predefined names, like sign up, generate lead and purchase. And custom events are your own names for things nothing else covers. Rely on automatic, enable useful enhanced measurement, use recommended wherever it fits, then fill gaps with custom. | Four families, in priority order<br>• Automatic<br>• Enhanced measurement<br>• Recommended<br>• Custom | Layout: four stacked tiers light up from top to bottom with example event chips on each. 0-10s automatic tier; 10-21s enhanced tier; 21-32s recommended tier; 32-42s custom tier. |
| 5 | 2:31 | 36 | Parameters are where the insight lives. An event name alone tells you that something happened. Parameters tell you what, where and how much. On a freight quote calculator, you might send product line, origin country, destination country and a quote band, like one thousand to five thousand, rather than the exact amount, to keep the number of unique values low. And remember: most custom parameters must be registered as custom dimensions before they appear in reports. Collection and reporting are two separate steps. | Parameters<br>• What, where, how much<br>• Band values to limit cardinality<br>• Register custom dimensions to report<br>• Collection and reporting are separate steps | Layout: an event chip 'calculate_quote' expands to show four parameter tags; a band slider groups exact values into ranges; a 'register' step connects parameters to a report. 0-9s event expands; 9-18s parameter tags appear; 18-27s band slider groups values; 27-36s register connects to report. |
| 6 | 3:07 | 37 | Now engagement, which replaced the old bounce rate thinking. A session starts with session start and ends after thirty minutes of inactivity by default. An engaged session lasted longer than ten seconds, had a key event, or had at least two page or screen views. Engagement rate is engaged sessions divided by sessions, and G A four's bounce rate is simply the inverse. That's more meaningful than the old single-page bounce. A reader who spends three minutes on one article and leaves is engaged. | Engagement definitions<br>• Session: ends after 30 min inactivity (default)<br>• Engaged: >10 s, a key event, or 2+ views<br>• Engagement rate = engaged / sessions<br>• Bounce rate = the inverse | Layout: a session timeline with three condition badges; a reader icon stays three minutes on one page and gets an 'engaged' badge. 0-12s session timeline; 12-25s three condition badges; 25-37s reader gets engaged badge. |
| 7 | 3:44 | 32 | And identity, briefly. G A four can recognize users with a user id you provide for logged-in users, with device identifiers like the first-party cookie on the web, and with modeled data where consent is denied, depending on your reporting identity setting. So user counts will never exactly match other systems. Explain that to stakeholders early. Chasing perfect reconciliation wastes time. Aim for consistent, directionally reliable data, and document how users are counted. | Identity<br>• User ID for logged-in users<br>• Device ID (first-party cookie)<br>• Modeled data where consent is denied<br>• Counts won't match other tools exactly | Layout: three identity sources flow into a 'reporting identity' dial; a comparison with another tool shows slightly different user counts and a 'consistent, not identical' label. 0-8s three sources flow in; 8-16s dial settles; 16-24s comparison shows small difference; 24-32s label appears. |
| 8 | 4:16 | 39 | First example, a simple one. A recipe publisher sees healthy page views but flat newsletter sign-ups. With the event model, they check three things. Scroll events by page: do readers reach the sign-up box near the bottom? A custom event when the sign-up box enters the viewport, compared with sign ups: is the problem visibility or persuasion? And the method parameter on sign up: do inline forms beat pop-ups? Illustratively, if few readers ever see the box, moving it higher is a better first test than rewriting its copy. | Example 1: recipe publisher<br>• Scroll depth by page<br>• Box seen versus sign-ups<br>• Method parameter: inline versus pop-up<br>• Visibility before persuasion | Layout: an article page scrolls; a sign-up box near the bottom is rarely reached; a comparison bar shows 'seen' versus 'signed up'; the box moves higher. 0-10s page scrolls; 10-20s box rarely reached; 20-29s comparison bar; 29-39s box moves higher. |
| 9 | 4:55 | 33 | Second example, a business case. A freight portal serving the UAE, Saudi Arabia and Pakistan tracks one calculate quote event with origin, destination, product line and quote band, plus a customer type user property: new, returning or contract. One exploration now answers three questions that used to need three separate custom events. Which lanes are most requested? Which quote bands convert to bookings? And do contract customers behave differently from new ones? Good parameters replaced event sprawl. | Example 2: Gulf freight portal<br>• One event, four parameters<br>• customer_type user property<br>• Lanes, bands and customer types in one exploration | Layout: a map with lanes from UAE and KSA to Pakistan lights up; a pivot table beside it fills rows by lane and columns by customer type. 0-11s lanes light up; 11-22s pivot table fills; 22-33s customer type columns highlight. |
| 10 | 5:28 | 45 | Watch me send that event. With the Google tag on the page, I call gtag event, calculate quote, and pass an object with product line freight, origin a e, destination p k, and quote band one thousand to five thousand. I call it after the calculator returns a result, not on the button click. When the user logs in, I set a user property, membership tier pro. And for logged-in users, I pass my own internal, non-personal user id in the config, subject to consent and my privacy notice. Then I open DebugView and confirm the event arrives once with all four parameters. | Watch me do it, part 1<br>• gtag event with four parameters<br>• Fire after the result, not on click<br>• set user_properties<br>• Internal, non-personal user_id<br>• Confirm in DebugView | Layout: code editor with the gtag snippet; lines highlight in sync; a DebugView-style event list shows calculate_quote with four parameters expanding. 0-11s event call highlights; 11-22s user property line highlights; 22-34s user_id line highlights; 34-45s debug list shows the event. |
| 11 | 6:13 | 37 | Now the same event in the raw BigQuery export. Parameters live in a repeated field called event params, a list of key and value pairs. So to read one, I use a small subquery: select the string value from unnest event params where the key equals product line. I count quotes by product line for one day's table and sort them. Seeing that raw structure once makes the model click: every event is a row, and its parameters are a list attached to it. | Watch me do it, part 2<br>• event_params is a list of key-value pairs<br>• UNNEST to read a parameter<br>• One row per event<br>• Count by product line | Layout: a table row expands to reveal a nested list of key-value pairs; the SQL subquery highlights and pulls out 'product_line'; a result table shows counts. 0-12s row expands to nested list; 12-25s subquery highlights; 25-37s result table fills. |
| 12 | 6:50 | 29 | Common mistakes. Treating G A four like its predecessor and looking for category, action and label. That model is gone; use meaningful names and parameters. High-cardinality parameters, like full timestamps, unique ids or free-text search terms in general dimensions, which bloat reports and trigger the other row. Setting user properties once and never updating them. And sending value on non-revenue events, then summing revenue that never happened. | Common mistakes<br>• Category/action/label thinking<br>• High-cardinality parameters<br>• Stale user properties<br>• value on non-revenue events | Layout: four warning cards; the second shows a report with a large '(other)' row highlighted. 0-10s card 1; 10-19s card 2 with (other) row; 19-29s cards 3 and 4. |
| 13 | 7:19 | 26 | Here's a quick reference you can keep. Describe the action with the event name, verb then object. Describe the context with event parameters. Describe the person with a user property. Identify a logged-in user with your own internal, non-personal user id. If you remember nothing else from this lecture, remember that four-line map. It answers most design questions in seconds. | Quick reference<br>• Action: event name<br>• Context: parameter<br>• Person: user property<br>• Logged-in user: internal user_id | Layout: a four-row reference card animates in, each row with a small icon. 0-13s rows 1 and 2; 13-26s rows 3 and 4. |
| 14 | 7:45 | 27 | Recap. G A four collects everything as events, and sessions and users are derived. Use the four families in priority order. Parameters carry the insight, and most need registering before they appear in reports. User properties describe the person. Engagement is defined by time, key events or views. User counts won't match other tools exactly, and that's fine if it's consistent and explained. | Recap<br>• Everything is an event<br>• Four families, in order<br>• Parameters carry insight<br>• User properties describe people<br>• Consistent, explained identity | Layout: recap card with five icons. 0-14s icons appear; 14-27s recap text. |
| 15 | 8:12 | 20 | Try this now. Pick three events on a site you know. For each, write the event name, three useful parameters and one user property that would help segment the analysis. Then check whether any of your parameters would have high cardinality, and band them if so. | Try this now<br>• Three events<br>• Three parameters each<br>• One user property each<br>• Band high-cardinality values | Layout: a template with three event cards filling in; one parameter gets a 'band it' tag. 0-10s three cards fill; 10-20s band tag appears. |

<details><summary>Narration per scene (paste into ElevenLabs)</summary>

Scene 1 — Events, parameters, user properties

```text
If you learned web analytics years ago, you might still think in page views, sessions and bounce rate. G A four thinks differently. Everything is an event: a page view, a scroll, a purchase, even the start of a session. Sessions and users still exist, but they're built from events. In this lecture you'll learn the event model properly: event names, parameters, user properties, the four families of events, how engagement is defined, and what the same event looks like in the raw BigQuery export.
```

Scene 2 — Why the model matters

```text
Why does the model matter? Because every report, exploration and export you'll ever use is built from it. If you understand that a purchase is an event with parameters like value, currency and items, you'll know why revenue breaks when currency is missing. If you understand that user properties describe the person, you'll segment correctly. And when a stakeholder asks for a new report, you'll know whether it's a new event, a new parameter, or just a new view of data you already collect.
```

Scene 3 — The receipt analogy

```text
Here's the core idea with an analogy. Think of a receipt from a coffee shop. The event name is what happened: purchase. The parameters are the lines on the receipt: the items, the price, the currency, the branch. And the user properties are on the loyalty card: gold member, prefers oat milk. The receipt describes one moment. The loyalty card describes the person across every visit. Ask yourself one question every time: does this describe the action, or the person?
```

Scene 4 — Four families, in priority order

```text
Events come in four families, and there's a priority order. Automatically collected events are sent by the Google tag by default, like first visit, session start and user engagement. Enhanced measurement events are toggled on in the web stream, like page view, scroll, outbound click, file download and site search. Recommended events are ones you implement using Google's predefined names, like sign up, generate lead and purchase. And custom events are your own names for things nothing else covers. Rely on automatic, enable useful enhanced measurement, use recommended wherever it fits, then fill gaps with custom.
```

Scene 5 — Parameters

```text
Parameters are where the insight lives. An event name alone tells you that something happened. Parameters tell you what, where and how much. On a freight quote calculator, you might send product line, origin country, destination country and a quote band, like one thousand to five thousand, rather than the exact amount, to keep the number of unique values low. And remember: most custom parameters must be registered as custom dimensions before they appear in reports. Collection and reporting are two separate steps.
```

Scene 6 — Engagement definitions

```text
Now engagement, which replaced the old bounce rate thinking. A session starts with session start and ends after thirty minutes of inactivity by default. An engaged session lasted longer than ten seconds, had a key event, or had at least two page or screen views. Engagement rate is engaged sessions divided by sessions, and G A four's bounce rate is simply the inverse. That's more meaningful than the old single-page bounce. A reader who spends three minutes on one article and leaves is engaged.
```

Scene 7 — Identity

```text
And identity, briefly. G A four can recognize users with a user id you provide for logged-in users, with device identifiers like the first-party cookie on the web, and with modeled data where consent is denied, depending on your reporting identity setting. So user counts will never exactly match other systems. Explain that to stakeholders early. Chasing perfect reconciliation wastes time. Aim for consistent, directionally reliable data, and document how users are counted.
```

Scene 8 — Example 1: recipe publisher

```text
First example, a simple one. A recipe publisher sees healthy page views but flat newsletter sign-ups. With the event model, they check three things. Scroll events by page: do readers reach the sign-up box near the bottom? A custom event when the sign-up box enters the viewport, compared with sign ups: is the problem visibility or persuasion? And the method parameter on sign up: do inline forms beat pop-ups? Illustratively, if few readers ever see the box, moving it higher is a better first test than rewriting its copy.
```

Scene 9 — Example 2: Gulf freight portal

```text
Second example, a business case. A freight portal serving the UAE, Saudi Arabia and Pakistan tracks one calculate quote event with origin, destination, product line and quote band, plus a customer type user property: new, returning or contract. One exploration now answers three questions that used to need three separate custom events. Which lanes are most requested? Which quote bands convert to bookings? And do contract customers behave differently from new ones? Good parameters replaced event sprawl.
```

Scene 10 — Watch me do it, part 1

```text
Watch me send that event. With the Google tag on the page, I call gtag event, calculate quote, and pass an object with product line freight, origin a e, destination p k, and quote band one thousand to five thousand. I call it after the calculator returns a result, not on the button click. When the user logs in, I set a user property, membership tier pro. And for logged-in users, I pass my own internal, non-personal user id in the config, subject to consent and my privacy notice. Then I open DebugView and confirm the event arrives once with all four parameters.
```

Scene 11 — Watch me do it, part 2

```text
Now the same event in the raw BigQuery export. Parameters live in a repeated field called event params, a list of key and value pairs. So to read one, I use a small subquery: select the string value from unnest event params where the key equals product line. I count quotes by product line for one day's table and sort them. Seeing that raw structure once makes the model click: every event is a row, and its parameters are a list attached to it.
```

Scene 12 — Common mistakes

```text
Common mistakes. Treating G A four like its predecessor and looking for category, action and label. That model is gone; use meaningful names and parameters. High-cardinality parameters, like full timestamps, unique ids or free-text search terms in general dimensions, which bloat reports and trigger the other row. Setting user properties once and never updating them. And sending value on non-revenue events, then summing revenue that never happened.
```

Scene 13 — Quick reference

```text
Here's a quick reference you can keep. Describe the action with the event name, verb then object. Describe the context with event parameters. Describe the person with a user property. Identify a logged-in user with your own internal, non-personal user id. If you remember nothing else from this lecture, remember that four-line map. It answers most design questions in seconds.
```

Scene 14 — Recap

```text
Recap. G A four collects everything as events, and sessions and users are derived. Use the four families in priority order. Parameters carry the insight, and most need registering before they appear in reports. User properties describe the person. Engagement is defined by time, key events or views. User counts won't match other tools exactly, and that's fine if it's consistent and explained.
```

Scene 15 — Try this now

```text
Try this now. Pick three events on a site you know. For each, write the event name, three useful parameters and one user property that would help segment the analysis. Then check whether any of your parameters would have high cardinality, and band them if so.
```

</details>

### 5. Key events and e-commerce tracking

Lesson `key-events-and-ecommerce` · module “The GA4 data model” · target 8 min · 15 scenes · 1172 words · planned 8:29 (words ≈ 8:30) · status: **to produce**

| Term | Say |
|---|---|
| GA4 | G A four |
| gtag | G tag |
| SKU | S K U |
| AED | A E D |
| ISO | I S O |
| GTM | G T M |
| PKR | P K R |
| SAR | S A R |
| COD | cash on delivery |

| # | Time | Sec | Narration | On screen | Visual |
|---|---|---|---|---|---|
| 1 | 0:00 | 37 | In twenty twenty-four, Google renamed conversions in G A four to key events. It sounds like a small wording change, but it confuses agencies and clients to this day, because Google Ads still uses the word conversions. In this lecture you'll learn what key events are, how to choose them, how G A four's e-commerce events and items array work, the three rules that prevent almost every revenue problem, and how to handle markets where cash on delivery means a purchase isn't really a purchase yet. | Key events and e-commerce<br>• Key events versus Ads conversions<br>• Choosing key events<br>• The e-commerce event set<br>• Three revenue rules<br>• Cash on delivery | Layout: a label 'conversions' peels off a GA4-style card and is replaced by 'key events'; the peeled label sticks onto an Ads-style card. 0-9s label peels off; 9-18s key events label appears; 18-28s old label sticks to Ads card; 28-37s title builds. |
| 2 | 0:37 | 35 | Why does this matter? Because key events power conversion-focused reporting, and revenue numbers drive budget decisions. If a key event fires on the wrong trigger, conversion rates look better than reality. If a purchase fires twice, revenue inflates. If currency is missing, revenue may not record correctly. And if refunds never flow back, G A four revenue drifts higher than your real sales every month. Each of these errors ends up in a board meeting as a confident, wrong number. | Why it matters<br>• Key events drive conversion reporting<br>• Revenue drives budgets<br>• Wrong triggers, duplicates, missing currency, no refunds<br>• Confident, wrong numbers | Layout: a revenue chart climbs steeply; four small error tags (wrong trigger, duplicate, no currency, no refunds) attach and the chart deflates to a realistic line. 0-12s chart climbs; 12-23s error tags attach; 23-35s chart deflates. |
| 3 | 1:12 | 35 | Here's the distinction. A key event in G A four is an important action for analysis in your property. A conversion in Google Ads is an action configured or imported into Ads to measure and optimize campaigns. Think of key events as the goals on your team's whiteboard, and Ads conversions as the specific signals you hand to the Ads bidding system. You might import a key event into Ads as a conversion, but they're separate settings in separate products. | Key events versus Ads conversions<br>• Key event: important outcome in GA4<br>• Conversion: signal for Ads measurement and bidding<br>• Import is optional and deliberate | Layout: a whiteboard labeled 'GA4 key events' and a control panel labeled 'Ads conversions'; one item is carried from the whiteboard to the panel by an 'import' arrow. 0-12s whiteboard appears; 12-23s control panel appears; 23-35s import arrow carries one item. |
| 4 | 1:47 | 38 | How do you choose key events? Keep the list short: your macro conversions, plus one or two critical micro conversions. For e-commerce, purchase, and perhaps begin checkout. For lead generation, generate lead or a book appointment event. For software, sign up and purchase, not login. For publishers, a newsletter sign up or subscription. If everything is a key event, nothing is. You can also assign a default value to a key event, or send a dynamic value and currency, so reports can show key event value alongside revenue. | Choosing key events<br>• Macro conversions plus 1 or 2 micro<br>• E-commerce: purchase, maybe begin_checkout<br>• Lead gen: generate_lead<br>• SaaS: sign_up, purchase<br>• Not everything | Layout: a four-column table by business model fills with key event chips; a crowded column labeled 'everything' collapses with a red cross. 0-13s table fills by model; 13-25s crowded column appears; 25-38s it collapses. |
| 5 | 2:25 | 36 | Next, the e-commerce event set. A typical retail funnel runs view item list, select item, view item, add to cart, view cart, begin checkout, add shipping info, add payment info, purchase, and refund. Each carries an items array. Each item should have at least an item id or name, and ideally price, quantity, category, brand and variant. The purchase event also needs a transaction id, value and currency, and optionally tax, shipping and coupon. E-commerce reports depend on these exact names and parameters. | The e-commerce event set<br>• view_item_list to purchase and refund<br>• items array on every event<br>• item_id or item_name, plus price, quantity<br>• purchase: transaction_id, value, currency | Layout: a vertical funnel of event names animates top to bottom; an items array card attaches to each step; the purchase step shows three required fields glowing. 0-12s funnel events animate; 12-24s items card attaches; 24-36s purchase fields glow. |
| 6 | 3:01 | 40 | Here are the three rules that prevent most revenue problems. One: always send a unique transaction id. G A four uses it to deduplicate purchases, for example when a buyer reloads the confirmation page. Two: always send currency as an I S O code, like U S D, G B P, A E D, S A R or P K R. Three: agree what value includes. Tax? Shipping? Document it. Finance numbers will differ anyway, because of refunds, cancellations and refused deliveries. So define the relationship instead of hoping the numbers match. | Three revenue rules<br>• Unique transaction_id<br>• ISO currency code<br>• Agree and document what value includes | Layout: three large rule cards flip in; the currency card shows a row of ISO codes; the value card shows a small formula with tax and shipping toggles. 0-13s rule 1 card; 13-27s rule 2 with ISO codes; 27-40s rule 3 with toggles. |
| 7 | 3:41 | 36 | Now cash on delivery, common in Pakistan and parts of the Gulf. An online purchase is really an order, and some are refused at the door. You have two acceptable options. Track purchase at order time, then send refund events for refused orders, using the Measurement Protocol or a data import. Or track a custom place order event, and report realized revenue from your order system. Either works if it's documented and used consistently. What doesn't work is pretending every order was paid. | Cash on delivery<br>• Option A: purchase, then refund refused orders<br>• Option B: place_order, realized revenue from order system<br>• Document and be consistent | Layout: a delivery van icon arrives at a door; one package returns with a 'refused' tag that generates a refund event arrow back to GA4. 0-12s van arrives; 12-24s package refused; 24-36s refund arrow returns. |
| 8 | 4:17 | 36 | First example, a simple one. An electronics store sees add to cart rising but purchases flat. The funnel, with illustrative numbers: twenty thousand view item, three thousand add to cart, eighteen hundred begin checkout, fifteen hundred add shipping info, seven hundred add payment info, and five hundred sixty purchases. The steepest avoidable drop is between shipping and payment, under fifty percent. Segmenting by device shows mobile users abandon far more at payment. That points to a payment form or wallet issue, not pricing. | Example 1: finding the leak<br>• 20,000 view to 560 purchases (illustrative)<br>• Biggest drop: shipping to payment<br>• Mobile abandons at payment<br>• Payment issue, not pricing | Layout: a funnel chart with six bars; the drop between shipping and payment highlights red; a device split shows mobile's bar much shorter. 0-12s funnel bars draw; 12-24s drop highlights red; 24-36s device split appears. |
| 9 | 4:53 | 35 | Second example, a business case. A Riyadh electronics retailer offers cash on delivery. They track purchase at order placement, then send refund events through the Measurement Protocol when couriers report refused deliveries. Every week, the analyst compares G A four revenue net of refunds with delivered revenue from the order system. The gap is documented and stable, so it becomes a known quantity rather than a monthly argument. And the team can see which channels bring customers who actually accept deliveries. | Example 2: Riyadh retailer<br>• purchase at order time<br>• refund via Measurement Protocol<br>• Weekly comparison with delivered revenue<br>• Stable, documented gap | Layout: two lines, GA4 net revenue and delivered revenue, run parallel with a shaded constant gap labeled 'documented'; channel bars show acceptance rates. 0-12s two lines draw; 12-23s gap shades and labels; 23-35s channel acceptance bars. |
| 10 | 5:28 | 40 | Watch me write a purchase push. First, I clear the previous e-commerce object by pushing e-commerce null, so values don't leak from an earlier event. Then I push event purchase with an e-commerce object: transaction id O R D one oh four five two two, value one forty-nine as a number, not a string, currency A E D, shipping fifteen, coupon E I D ten, and an items array with one abaya: its id, name, category, price and quantity. Google Tag Manager reads this and sends the G A four event. | Watch me do it, part 1<br>• Push ecommerce: null first<br>• event: purchase<br>• transaction_id, numeric value, ISO currency<br>• items array | Layout: code editor with the purchase push; the null push highlights first, then each field in turn; a small arrow to a tag manager icon and then to GA4. 0-10s null push highlights; 10-20s transaction and value highlight; 20-30s currency and items highlight; 30-40s arrows to tag manager and GA4. |
| 11 | 6:08 | 33 | Then I check for duplicates in the BigQuery export, which the interface quietly hides. I select the transaction id and count purchase events for September, grouped by transaction id, keeping only those with more than one event. If any rows come back, orders are being counted twice, usually from confirmation page reloads or two tags firing. On a healthy setup, this query returns nothing. I schedule it weekly, so duplicates are caught before they reach a report. | Watch me do it, part 2<br>• Count purchases per transaction_id<br>• HAVING count > 1<br>• Rows = double counting<br>• Schedule weekly | Layout: SQL query highlights line by line; a result grid shows two duplicate rows in red; then after a fix, the grid shows 'no rows'. 0-8s query highlights; 8-16s duplicate rows appear; 16-25s fix applied; 25-33s empty result. |
| 12 | 6:41 | 33 | And one more twenty twenty-six consideration: Google Ads. If you import a G A four key event into Ads as a conversion for bidding, make sure the same action isn't also tracked by a separate Ads conversion tag, or you'll double count in Ads. Decide which source Ads should optimize on, and write it down. And check consent mode, because Ads measurement depends on the consent signals: ad storage, ad user data and ad personalization. | Ads in 2026<br>• Import key events deliberately<br>• Avoid double tracking in Ads<br>• Document the optimization source<br>• Consent signals matter | Layout: two arrows into an Ads icon, one from GA4 import and one from an Ads tag, collide with a 'double count' warning; one arrow is removed. 0-11s two arrows collide; 11-22s warning appears; 22-33s one arrow removed. |
| 13 | 7:14 | 25 | Common mistakes. Missing or duplicated transaction ids, inflating revenue. Sending value as a string with a currency symbol. Firing purchase on a confirmation page that also loads for failed payments. Marking add to cart and page view as key events, diluting conversion rates. And forgetting refunds, so G A four revenue drifts ever higher than actual sales. | Common mistakes<br>• Missing or duplicate transaction_id<br>• value as a string<br>• Purchase on failed payments<br>• Too many key events<br>• No refunds | Layout: five warning cards drop in sequence. 0-12s cards 1 to 3; 12-25s cards 4 and 5. |
| 14 | 7:39 | 27 | Recap. G A four calls important outcomes key events, while Google Ads uses conversions. Keep key events few. E-commerce reports rely on recommended events with an items array. A unique transaction id, an I S O currency and a documented definition of value are non-negotiable. Handle cash on delivery with refunds or a separate order event. And check duplicates in the export. | Recap<br>• Key events versus conversions<br>• Few key events<br>• items array<br>• transaction_id, currency, value definition<br>• COD handling, duplicate checks | Layout: recap card with six small icons. 0-14s icons appear; 14-27s recap text. |
| 15 | 8:06 | 23 | Try this now. Write the full purchase push, with items, for a real or imagined order. Then list three ways that number could differ from finance's figure, like refunds, tax or refused deliveries, and write one sentence for each explaining how you'd reconcile it. If you have BigQuery export, run the duplicate check query. | Try this now<br>• Write a full purchase push<br>• Three reasons it differs from finance<br>• One reconciliation line each<br>• Run the duplicate check | Layout: template with a code block and three reconciliation lines filling in; a query icon runs at the end. 0-8s code block fills; 8-15s three lines fill; 15-23s query runs. |

<details><summary>Narration per scene (paste into ElevenLabs)</summary>

Scene 1 — Key events and e-commerce

```text
In twenty twenty-four, Google renamed conversions in G A four to key events. It sounds like a small wording change, but it confuses agencies and clients to this day, because Google Ads still uses the word conversions. In this lecture you'll learn what key events are, how to choose them, how G A four's e-commerce events and items array work, the three rules that prevent almost every revenue problem, and how to handle markets where cash on delivery means a purchase isn't really a purchase yet.
```

Scene 2 — Why it matters

```text
Why does this matter? Because key events power conversion-focused reporting, and revenue numbers drive budget decisions. If a key event fires on the wrong trigger, conversion rates look better than reality. If a purchase fires twice, revenue inflates. If currency is missing, revenue may not record correctly. And if refunds never flow back, G A four revenue drifts higher than your real sales every month. Each of these errors ends up in a board meeting as a confident, wrong number.
```

Scene 3 — Key events versus Ads conversions

```text
Here's the distinction. A key event in G A four is an important action for analysis in your property. A conversion in Google Ads is an action configured or imported into Ads to measure and optimize campaigns. Think of key events as the goals on your team's whiteboard, and Ads conversions as the specific signals you hand to the Ads bidding system. You might import a key event into Ads as a conversion, but they're separate settings in separate products.
```

Scene 4 — Choosing key events

```text
How do you choose key events? Keep the list short: your macro conversions, plus one or two critical micro conversions. For e-commerce, purchase, and perhaps begin checkout. For lead generation, generate lead or a book appointment event. For software, sign up and purchase, not login. For publishers, a newsletter sign up or subscription. If everything is a key event, nothing is. You can also assign a default value to a key event, or send a dynamic value and currency, so reports can show key event value alongside revenue.
```

Scene 5 — The e-commerce event set

```text
Next, the e-commerce event set. A typical retail funnel runs view item list, select item, view item, add to cart, view cart, begin checkout, add shipping info, add payment info, purchase, and refund. Each carries an items array. Each item should have at least an item id or name, and ideally price, quantity, category, brand and variant. The purchase event also needs a transaction id, value and currency, and optionally tax, shipping and coupon. E-commerce reports depend on these exact names and parameters.
```

Scene 6 — Three revenue rules

```text
Here are the three rules that prevent most revenue problems. One: always send a unique transaction id. G A four uses it to deduplicate purchases, for example when a buyer reloads the confirmation page. Two: always send currency as an I S O code, like U S D, G B P, A E D, S A R or P K R. Three: agree what value includes. Tax? Shipping? Document it. Finance numbers will differ anyway, because of refunds, cancellations and refused deliveries. So define the relationship instead of hoping the numbers match.
```

Scene 7 — Cash on delivery

```text
Now cash on delivery, common in Pakistan and parts of the Gulf. An online purchase is really an order, and some are refused at the door. You have two acceptable options. Track purchase at order time, then send refund events for refused orders, using the Measurement Protocol or a data import. Or track a custom place order event, and report realized revenue from your order system. Either works if it's documented and used consistently. What doesn't work is pretending every order was paid.
```

Scene 8 — Example 1: finding the leak

```text
First example, a simple one. An electronics store sees add to cart rising but purchases flat. The funnel, with illustrative numbers: twenty thousand view item, three thousand add to cart, eighteen hundred begin checkout, fifteen hundred add shipping info, seven hundred add payment info, and five hundred sixty purchases. The steepest avoidable drop is between shipping and payment, under fifty percent. Segmenting by device shows mobile users abandon far more at payment. That points to a payment form or wallet issue, not pricing.
```

Scene 9 — Example 2: Riyadh retailer

```text
Second example, a business case. A Riyadh electronics retailer offers cash on delivery. They track purchase at order placement, then send refund events through the Measurement Protocol when couriers report refused deliveries. Every week, the analyst compares G A four revenue net of refunds with delivered revenue from the order system. The gap is documented and stable, so it becomes a known quantity rather than a monthly argument. And the team can see which channels bring customers who actually accept deliveries.
```

Scene 10 — Watch me do it, part 1

```text
Watch me write a purchase push. First, I clear the previous e-commerce object by pushing e-commerce null, so values don't leak from an earlier event. Then I push event purchase with an e-commerce object: transaction id O R D one oh four five two two, value one forty-nine as a number, not a string, currency A E D, shipping fifteen, coupon E I D ten, and an items array with one abaya: its id, name, category, price and quantity. Google Tag Manager reads this and sends the G A four event.
```

Scene 11 — Watch me do it, part 2

```text
Then I check for duplicates in the BigQuery export, which the interface quietly hides. I select the transaction id and count purchase events for September, grouped by transaction id, keeping only those with more than one event. If any rows come back, orders are being counted twice, usually from confirmation page reloads or two tags firing. On a healthy setup, this query returns nothing. I schedule it weekly, so duplicates are caught before they reach a report.
```

Scene 12 — Ads in 2026

```text
And one more twenty twenty-six consideration: Google Ads. If you import a G A four key event into Ads as a conversion for bidding, make sure the same action isn't also tracked by a separate Ads conversion tag, or you'll double count in Ads. Decide which source Ads should optimize on, and write it down. And check consent mode, because Ads measurement depends on the consent signals: ad storage, ad user data and ad personalization.
```

Scene 13 — Common mistakes

```text
Common mistakes. Missing or duplicated transaction ids, inflating revenue. Sending value as a string with a currency symbol. Firing purchase on a confirmation page that also loads for failed payments. Marking add to cart and page view as key events, diluting conversion rates. And forgetting refunds, so G A four revenue drifts ever higher than actual sales.
```

Scene 14 — Recap

```text
Recap. G A four calls important outcomes key events, while Google Ads uses conversions. Keep key events few. E-commerce reports rely on recommended events with an items array. A unique transaction id, an I S O currency and a documented definition of value are non-negotiable. Handle cash on delivery with refunds or a separate order event. And check duplicates in the export.
```

Scene 15 — Try this now

```text
Try this now. Write the full purchase push, with items, for a real or imagined order. Then list three ways that number could differ from finance's figure, like refunds, tax or refused deliveries, and write one sentence for each explaining how you'd reconcile it. If you have BigQuery export, run the duplicate check query.
```

</details>

### 6. Custom definitions, scopes and core metrics

Lesson `custom-definitions-and-metrics` · module “The GA4 data model” · target 8 min · 15 scenes · 1118 words · planned 8:07 (words ≈ 8:06) · status: **to produce**

| Term | Say |
|---|---|
| GA4 | G A four |
| gtag | G tag |
| SKU | S K U |
| AED | A E D |
| ISO | I S O |
| GTM | G T M |
| API | A P I |
| 360 | three sixty |

| # | Time | Sec | Narration | On screen | Visual |
|---|---|---|---|---|---|
| 1 | 0:00 | 39 | Here's a frustrating moment every G A four user meets. Your developer sends a perfect parameter called author on every page view. You open the reports, and it isn't there. Anywhere. Nothing's broken. You've just discovered that collection is not reporting. In this lecture you'll learn how custom definitions work, how to choose the right scope, how to control cardinality so the dreaded other row doesn't swallow your data, the core metric definitions you must be able to explain, and how to audit definitions with the Data A P I. | Custom definitions and metrics<br>• Collection is not reporting<br>• Scopes: event, user, item<br>• Cardinality and (other)<br>• Core metric definitions<br>• Auditing via API | Layout: a parameter tag 'author' flies into a report frame and bounces off a locked door labeled 'register first'. 0-13s parameter flies in; 13-26s bounces off locked door; 26-39s title builds. |
| 2 | 0:39 | 33 | Why does this matter? Because registration isn't retroactive. Data collected before you register a custom dimension won't appear under that dimension later. Register three weeks after launch, and you've lost three weeks. And definition slots are limited, with higher limits on the paid three sixty tier. So registering the right parameters, with the right scope, on day one, is one of the highest-leverage things an analyst does. It's also one of the most commonly forgotten. | Why it matters<br>• Registration isn't retroactive<br>• Slots are limited<br>• Day-one registration pays off | Layout: a timeline where data before a 'registered' marker is grayed out; a row of slot icons shows a few filled and a finite total. 0-11s timeline with grayed segment; 11-22s registered marker; 22-33s slot icons appear. |
| 3 | 1:12 | 43 | Here's the core idea: scope. Every custom definition describes something at a particular level. Event scope describes a single event occurrence, like form id, video title or author. User scope describes the user across events, like membership tier or customer type, and comes from a user property. Item scope describes a product inside the e-commerce items array, like color or size band. Think of it like labels in a supermarket. A label on the receipt, a label on the loyalty card, and a label on each product. Put the label in the wrong place, and it describes the wrong thing. | Scopes<br>• Event: one occurrence<br>• User: the person across events<br>• Item: a product in the items array<br>• Receipt, loyalty card, product label | Layout: three label types attach to a receipt, a loyalty card and a product box respectively; one label placed on the wrong object flashes red. 0-11s receipt label; 11-22s loyalty card label; 22-32s product label; 32-43s wrong placement flashes. |
| 4 | 1:55 | 33 | The classic scope mistake: registering customer type as an event-scoped dimension. Then it only appears on events where it was explicitly sent, not across the user's whole behavior. So your report of purchases by customer type shows mostly not set. If an attribute describes the person, send it as a user property and register it with user scope. If it describes the action, it's event scope. And for product attributes inside the items array, use item scope. | The classic mistake<br>• customer_type as event scope<br>• Shows (not set) on most events<br>• Person: user scope<br>• Action: event scope<br>• Product: item scope | Layout: a report table where the customer_type column is mostly '(not set)'; the definition switches from event to user scope and the column fills. 0-11s table with not set; 11-22s scope switches; 22-33s column fills. |
| 5 | 2:28 | 30 | Custom metrics and calculated metrics are the numeric side. A custom metric registers a numeric parameter, like quote amount or reading time in seconds, so it can be summed or averaged. A calculated metric is a formula over existing metrics, like revenue per engaged session. Use calculated metrics when stakeholders keep computing the same ratio by hand in spreadsheets. It reduces errors and makes the definition visible to everyone. | Custom and calculated metrics<br>• Custom metric: a numeric parameter<br>• Calculated metric: a formula over metrics<br>• Replace hand-calculated ratios | Layout: a numeric parameter chip becomes a summed metric; a small formula card 'revenue / engaged sessions' replaces a spreadsheet icon. 0-10s parameter becomes metric; 10-20s formula card appears; 20-30s spreadsheet icon fades. |
| 6 | 2:58 | 36 | Now cardinality, the number of unique values a dimension has. High-cardinality dimensions, like full URLs with query strings, unique ids, timestamps or free text, can make G A four group less common values into a row labeled other in some reports. To reduce the risk, band continuous values, like order value band fifty to one hundred. Strip unneeded query parameters. Keep ids out of general dimensions. And use explorations or the BigQuery export for granular analysis, where the other row doesn't apply. | Cardinality and (other)<br>• Many unique values cause (other)<br>• Band continuous values<br>• Strip query parameters<br>• Keep IDs out<br>• Use BigQuery for granular work | Layout: a bar chart with a huge '(other)' bar; banding and stripping steps shrink it until real categories are visible. 0-9s huge other bar; 9-18s banding step; 18-27s stripping step; 27-36s real categories visible. |
| 7 | 3:34 | 39 | And you must be able to explain the core metrics in plain language. Active users: distinct users who had an engaged session or certain first-time events, which is G A four's default users in most reports. New users: first-time visitors. Sessions. Engaged sessions. Engagement rate. Average engagement time, when the page was in focus. Key events. Session key event rate, the share of sessions with at least one key event. And total revenue, net of refunds depending on setup. Always name the metric precisely, active users, not just users. | Core definitions<br>• Active users (default 'users')<br>• New users, sessions, engaged sessions<br>• Engagement rate and time<br>• Session key event rate<br>• Total revenue | Layout: a glossary card builds line by line; the word 'users' gets a prefix 'active' snapping on. 0-20s glossary lines build; 20-39s active prefix snaps on. |
| 8 | 4:13 | 37 | First example, a simple one. A news site in Riyadh wants to know which authors drive subscriptions. They add author and content group as parameters on page view, and register both as event-scoped custom dimensions. They register subscriber status, free, trial or paid, as a user-scoped dimension from a user property. Then, in a free-form exploration, rows are authors, columns are subscriber status, values are engaged sessions and subscribe key events. Now the editor can see which authors attract engaged readers who later subscribe. | Example 1: Riyadh news site<br>• author, content_group: event scope<br>• subscriber_status: user scope<br>• Exploration: authors by subscriber status | Layout: a free-form exploration grid fills with author rows and subscriber status columns; one author row highlights with high subscribe counts. 0-12s grid axes appear; 12-25s cells fill; 25-37s top author highlights. |
| 9 | 4:50 | 33 | Second example, a business case. An online academy sends course slug, lesson type and content language as event parameters on page view and on a custom lesson complete event. It sends learner plan, free or pro, as a user property. On launch day it registers three event-scoped dimensions and one user-scoped dimension. Within a month, one exploration shows which courses have high completion but low upgrade rates. That becomes the input to the next pricing experiment. | Example 2: an online academy<br>• course_slug, lesson_type, content_language<br>• learner_plan (user scope)<br>• Registered on launch day<br>• High completion, low upgrade: a pricing test | Layout: course cards with completion bars and upgrade bars side by side; one course with high completion and low upgrade glows with a 'test pricing' tag. 0-11s course cards appear; 11-22s completion and upgrade bars grow; 22-33s outlier course glows. |
| 10 | 5:23 | 34 | Watch me audit definitions with the Data A P I. In the lesson's Python script, I create a client using application default credentials, so no keys in code. I call get metadata for the property and filter dimensions whose names start with custom event or custom user. That's my list of registered definitions. Then I run a small report: the author dimension with engaged sessions for the last seven days, limited to ten rows. I print the rows. | Watch me do it, part 1<br>• Default credentials, no keys in code<br>• get_metadata lists definitions<br>• Filter customEvent and customUser<br>• Run a 7-day test report | Layout: code editor highlights the metadata call and the filter; a printed list of registered dimensions appears; then the report request highlights. 0-8s client line; 8-17s metadata and filter; 17-26s registered list prints; 26-34s report request highlights. |
| 11 | 5:57 | 38 | The output shows the registered list, and the report shows a handful of authors, but most engaged sessions fall under not set. That tells me the definition is registered, but the parameter isn't being sent on every page view, probably only on article templates. I check with the developer, and it turns out the new video pages use a different template. They add the parameter, and a week later not set drops to a sliver. The A P I turned a vague feeling into a specific fix. | Watch me do it, part 2<br>• Mostly (not set)<br>• Registered, but not sent everywhere<br>• New template missing the parameter<br>• Fix, then (not set) shrinks | Layout: a result table with a big '(not set)' row; a template diagram shows article pages with the parameter and video pages without; after a fix the not-set row shrinks. 0-10s result with big not set; 10-19s template diagram; 19-28s fix applied; 28-38s not set shrinks. |
| 12 | 6:35 | 22 | Keep a definitions register next to your tracking plan: name, scope, parameter, created date, owner, and where it's used. Treat slots as scarce, and retire unused definitions deliberately. It's a two-minute habit that saves hours when someone asks, six months later, why a dimension exists and whether it's safe to remove. | Definitions register<br>• Name, scope, parameter<br>• Created, owner, used in<br>• Slots are scarce<br>• Retire deliberately | Layout: a register table with six columns fills three rows; a 'retire' action grays one row with a date. 0-7s columns appear; 7-15s rows fill; 15-22s one row retired. |
| 13 | 6:57 | 23 | Common mistakes. Registering definitions weeks after launch and losing early data. The wrong scope, especially user attributes registered as event scope. Using exact monetary amounts or ids as dimensions and then wondering why reports show other. And comparing users in G A four with visitors in another tool without explaining the definitions. | Common mistakes<br>• Late registration<br>• Wrong scope<br>• IDs and amounts as dimensions<br>• Unexplained metric comparisons | Layout: four warning cards appear in a two by two grid. 0-12s cards appear; 12-23s grid settles. |
| 14 | 7:20 | 23 | Recap. Custom parameters must be registered to be reported, and registration isn't retroactive. Choose the right scope: event, user or item. Control cardinality by banding and keeping ids out. Explain metrics precisely, especially active users. Keep a definitions register. And use the Data A P I, or BigQuery, to audit what's actually arriving. | Recap<br>• Register early<br>• Right scope<br>• Control cardinality<br>• Precise metric names<br>• Register and audit | Layout: recap card with icons for each point. 0-12s icons appear; 12-23s recap text. |
| 15 | 7:43 | 24 | Try this now. Create a definitions register for a site you know with at least four custom dimensions across event, user and item scopes, and justify each scope in one line. If you have API access, run the lesson's script to see what's registered and whether values are arriving, and investigate the biggest not set. | Try this now<br>• Four definitions across scopes<br>• One-line scope justification<br>• Run the audit script<br>• Investigate the biggest (not set) | Layout: register template filling four rows with scope tags; a script run icon; a magnifier over a not-set row. 0-8s four rows fill; 8-16s script runs; 16-24s magnifier over not set. |

<details><summary>Narration per scene (paste into ElevenLabs)</summary>

Scene 1 — Custom definitions and metrics

```text
Here's a frustrating moment every G A four user meets. Your developer sends a perfect parameter called author on every page view. You open the reports, and it isn't there. Anywhere. Nothing's broken. You've just discovered that collection is not reporting. In this lecture you'll learn how custom definitions work, how to choose the right scope, how to control cardinality so the dreaded other row doesn't swallow your data, the core metric definitions you must be able to explain, and how to audit definitions with the Data A P I.
```

Scene 2 — Why it matters

```text
Why does this matter? Because registration isn't retroactive. Data collected before you register a custom dimension won't appear under that dimension later. Register three weeks after launch, and you've lost three weeks. And definition slots are limited, with higher limits on the paid three sixty tier. So registering the right parameters, with the right scope, on day one, is one of the highest-leverage things an analyst does. It's also one of the most commonly forgotten.
```

Scene 3 — Scopes

```text
Here's the core idea: scope. Every custom definition describes something at a particular level. Event scope describes a single event occurrence, like form id, video title or author. User scope describes the user across events, like membership tier or customer type, and comes from a user property. Item scope describes a product inside the e-commerce items array, like color or size band. Think of it like labels in a supermarket. A label on the receipt, a label on the loyalty card, and a label on each product. Put the label in the wrong place, and it describes the wrong thing.
```

Scene 4 — The classic mistake

```text
The classic scope mistake: registering customer type as an event-scoped dimension. Then it only appears on events where it was explicitly sent, not across the user's whole behavior. So your report of purchases by customer type shows mostly not set. If an attribute describes the person, send it as a user property and register it with user scope. If it describes the action, it's event scope. And for product attributes inside the items array, use item scope.
```

Scene 5 — Custom and calculated metrics

```text
Custom metrics and calculated metrics are the numeric side. A custom metric registers a numeric parameter, like quote amount or reading time in seconds, so it can be summed or averaged. A calculated metric is a formula over existing metrics, like revenue per engaged session. Use calculated metrics when stakeholders keep computing the same ratio by hand in spreadsheets. It reduces errors and makes the definition visible to everyone.
```

Scene 6 — Cardinality and (other)

```text
Now cardinality, the number of unique values a dimension has. High-cardinality dimensions, like full URLs with query strings, unique ids, timestamps or free text, can make G A four group less common values into a row labeled other in some reports. To reduce the risk, band continuous values, like order value band fifty to one hundred. Strip unneeded query parameters. Keep ids out of general dimensions. And use explorations or the BigQuery export for granular analysis, where the other row doesn't apply.
```

Scene 7 — Core definitions

```text
And you must be able to explain the core metrics in plain language. Active users: distinct users who had an engaged session or certain first-time events, which is G A four's default users in most reports. New users: first-time visitors. Sessions. Engaged sessions. Engagement rate. Average engagement time, when the page was in focus. Key events. Session key event rate, the share of sessions with at least one key event. And total revenue, net of refunds depending on setup. Always name the metric precisely, active users, not just users.
```

Scene 8 — Example 1: Riyadh news site

```text
First example, a simple one. A news site in Riyadh wants to know which authors drive subscriptions. They add author and content group as parameters on page view, and register both as event-scoped custom dimensions. They register subscriber status, free, trial or paid, as a user-scoped dimension from a user property. Then, in a free-form exploration, rows are authors, columns are subscriber status, values are engaged sessions and subscribe key events. Now the editor can see which authors attract engaged readers who later subscribe.
```

Scene 9 — Example 2: an online academy

```text
Second example, a business case. An online academy sends course slug, lesson type and content language as event parameters on page view and on a custom lesson complete event. It sends learner plan, free or pro, as a user property. On launch day it registers three event-scoped dimensions and one user-scoped dimension. Within a month, one exploration shows which courses have high completion but low upgrade rates. That becomes the input to the next pricing experiment.
```

Scene 10 — Watch me do it, part 1

```text
Watch me audit definitions with the Data A P I. In the lesson's Python script, I create a client using application default credentials, so no keys in code. I call get metadata for the property and filter dimensions whose names start with custom event or custom user. That's my list of registered definitions. Then I run a small report: the author dimension with engaged sessions for the last seven days, limited to ten rows. I print the rows.
```

Scene 11 — Watch me do it, part 2

```text
The output shows the registered list, and the report shows a handful of authors, but most engaged sessions fall under not set. That tells me the definition is registered, but the parameter isn't being sent on every page view, probably only on article templates. I check with the developer, and it turns out the new video pages use a different template. They add the parameter, and a week later not set drops to a sliver. The A P I turned a vague feeling into a specific fix.
```

Scene 12 — Definitions register

```text
Keep a definitions register next to your tracking plan: name, scope, parameter, created date, owner, and where it's used. Treat slots as scarce, and retire unused definitions deliberately. It's a two-minute habit that saves hours when someone asks, six months later, why a dimension exists and whether it's safe to remove.
```

Scene 13 — Common mistakes

```text
Common mistakes. Registering definitions weeks after launch and losing early data. The wrong scope, especially user attributes registered as event scope. Using exact monetary amounts or ids as dimensions and then wondering why reports show other. And comparing users in G A four with visitors in another tool without explaining the definitions.
```

Scene 14 — Recap

```text
Recap. Custom parameters must be registered to be reported, and registration isn't retroactive. Choose the right scope: event, user or item. Control cardinality by banding and keeping ids out. Explain metrics precisely, especially active users. Keep a definitions register. And use the Data A P I, or BigQuery, to audit what's actually arriving.
```

Scene 15 — Try this now

```text
Try this now. Create a definitions register for a site you know with at least four custom dimensions across event, user and item scopes, and justify each scope in one line. If you have API access, run the lesson's script to see what's registered and whether values are arriving, and investigate the biggest not set.
```

</details>

### 7. Tag manager fundamentals: tags, triggers and variables

Lesson `tag-manager-fundamentals` · module “Tagging with a tag manager” · target 9 min · 15 scenes · 1199 words · planned 8:42 (words ≈ 8:41) · status: **to produce**

| Term | Say |
|---|---|
| GA4 | G A four |
| GTM | G T M |
| SPA | S P A |
| DOM | dom |
| PII | P I I |
| CSS | C S S |
| CE | C E |
| DLV | D L V |
| CONST | const |

| # | Time | Sec | Narration | On screen | Visual |
|---|---|---|---|---|---|
| 1 | 0:00 | 39 | Picture a website with forty marketing tags, installed by five different people over three years. Some fire twice. Some belong to campaigns that ended last year. And nobody's sure which ones respect the consent banner. That's what happens without a tag manager, or with one used carelessly. In this lecture you'll learn what a tag manager is for, the three building blocks of tags, triggers and variables, how to implement G A four in Google Tag Manager, and how to keep a container clean with naming, versions and publishing rules. | Tag manager fundamentals<br>• Why a tag manager<br>• Tags, triggers, variables<br>• GA4 in GTM<br>• Naming, versions, publishing | Layout: a website silhouette with dozens of tangled tag icons attached; they untangle and slot into an organized container box. 0-10s tangled tags appear; 10-20s tags untangle; 20-29s slot into container; 29-39s title builds. |
| 2 | 0:39 | 35 | Why use a tag manager? It centralizes marketing and analytics tags in one governed place. It lets you change tracking without full site releases, while still following a release process. It gives you version history, workspaces and a preview and debug mode. And it makes consent integration consistent across every tag. But a tag manager isn't a license to bypass developers. The best setups split responsibilities: developers expose clean data in a data layer, analysts configure tags that read it. | Why a tag manager<br>• One governed place<br>• Changes without full releases<br>• Versions, workspaces, preview<br>• Consistent consent<br>• Developers expose data; analysts configure tags | Layout: four benefit icons orbit a container icon; below, a split lane shows a developer icon feeding a data layer and an analyst icon configuring tags. 0-12s benefit icons orbit; 12-23s split lane appears; 23-35s developer and analyst icons animate. |
| 3 | 1:14 | 40 | Here's the core model. Three building blocks. A tag is code that sends data somewhere: the Google tag, a G A four event, an ad platform pixel. A trigger is the condition that fires a tag: all pages, a custom event named generate lead, a click on a specific element. And a variable is a value read at runtime: the page URL, a data layer value like e-commerce value, or a constant like your measurement id. Here's the sentence to remember: when, the trigger, do what, the tag, with which values, the variables. | Tags, triggers, variables<br>• Tag: sends data<br>• Trigger: when it fires<br>• Variable: values at runtime<br>• When, do what, with which values | Layout: three interlocking gears labeled trigger, tag and variable; a sentence ribbon 'when, do what, with which values' wraps around them. 0-10s trigger gear; 10-20s tag gear; 20-30s variable gear; 30-40s ribbon wraps. |
| 4 | 1:54 | 36 | How is G A four implemented in Google Tag Manager? With a Google tag configured with your measurement id, which starts with G dash, firing on all pages, typically on the initialization trigger. It sends page views by default and loads enhanced measurement. Then G A four event tags, one for each tracking plan event, referencing the same measurement id, with parameters mapped to variables. Store the measurement id in a constant variable, so you never retype it in dozens of tags. | GA4 in GTM<br>• Google tag with measurement ID<br>• Initialization trigger, all pages<br>• One event tag per plan event<br>• Measurement ID in a constant variable | Layout: a Google tag card connects to all pages; event tag cards branch from it, each referencing a constant variable chip labeled 'G-XXXXXXX'. 0-12s Google tag card; 12-24s event tag cards branch; 24-36s constant variable chip links them. |
| 5 | 2:30 | 41 | Now, which triggers should you use? Page view, DOM ready and window loaded control timing for page-level tags. Custom event triggers fire when the data layer receives an event key, and they're the most reliable type for important events. Click triggers are fine for simple interactions, but fragile if they depend on CSS classes that designers change. Form submission triggers work on standard forms but often fail on JavaScript-heavy ones. History change handles single-page apps. And element visibility answers did the user see this. Rule of thumb: key events use data layer custom events. | Trigger types<br>• Page timing triggers<br>• Custom event: most reliable<br>• Clicks: fragile with CSS classes<br>• Forms: unreliable on JS forms<br>• History change, element visibility | Layout: a trigger menu with six options; custom event gets a gold star; click trigger shows a CSS class changing and breaking the link. 0-14s trigger menu appears; 14-27s custom event starred; 27-41s click trigger breaks on class change. |
| 6 | 3:11 | 38 | Then naming and organization. A container with two hundred unnamed tags is unmaintainable. Adopt a convention. Tags: platform, type, detail, like G A four, event, generate lead. Triggers: type and detail, like C E, generate lead. Variables: type and detail, like D L V, e-commerce value, or CONST, G A four id. And folders by platform or feature. It sounds pedantic, but when something breaks at nine at night during a campaign launch, a clean container is the difference between a five-minute fix and a three-hour hunt. | Naming convention<br>• Tags: Platform - Type - Detail<br>• Triggers: CE - generate_lead<br>• Variables: DLV / CONST - detail<br>• Folders by platform or feature | Layout: a messy list of tag names reorders and renames itself into a tidy, prefixed list grouped in folders. 0-13s messy list; 13-25s renaming animation; 25-38s folders group items. |
| 7 | 3:49 | 38 | And the release process. Workspaces let people work in parallel without overwriting each other. Every publish creates a version, so name it and describe what changed, for example, added generate lead with form id, fixed duplicate page view on the single-page app. Keep publish rights with a small group, and everyone else submits for review. If something breaks, you can roll back to a previous version in minutes. Export the container after each publish and store it with your tracking plan as a backup and audit trail. | Release process<br>• Workspaces for parallel work<br>• Named, described versions<br>• Restricted publish rights<br>• Fast rollback<br>• Export after each publish | Layout: a version history list with descriptive names; a rollback arrow jumps to a previous version; an export icon saves a file into a 'tracking plan' folder. 0-13s version list; 13-25s rollback arrow; 25-38s export saves. |
| 8 | 4:27 | 37 | First example, a simple one. A property developer in Abu Dhabi wants to know which projects' brochures get downloaded. Before building anything, the analyst checks what's already collected. Enhanced measurement already captures file download events with file name and link URL. So instead of a new tag, they confirm file downloads are enabled, register file name as a custom dimension, and build an exploration of downloads by file name and source. No tag manager work at all. Always check what exists before you build. | Example 1: brochure downloads<br>• Enhanced measurement already collects them<br>• Register file_name<br>• Explore by file and source<br>• Check before you build | Layout: a 'build new tag' button is about to be pressed, then pauses as an 'already collected' badge appears on file_download; an exploration table appears instead. 0-12s build button hovers; 12-25s already collected badge; 25-37s exploration table appears. |
| 9 | 5:04 | 35 | Second example, a business case. A Karachi marketplace had three agencies adding pixels to one container. Tags fired twice, nobody knew who owned what, and a campaign pixel from last year was still collecting data. The in-house analyst created one workspace per agency, restricted publish rights to herself, required version descriptions, added an owner note to every tag, and removed eleven orphaned tags after a two-week notice. Page views returned to a single count, and consent audits became a thirty-minute task. | Example 2: three agencies, one container<br>• Workspaces per agency<br>• Restricted publishing<br>• Owner note on every tag<br>• Eleven orphans removed | Layout: a container with three colored agency zones; duplicate tags merge; eleven gray orphan tags fade out after a calendar shows two weeks. 0-12s three agency zones; 12-23s duplicates merge; 23-35s orphans fade after two weeks. |
| 10 | 5:39 | 39 | Watch me build a minimal container. First, variables: a constant for the measurement id, and two data layer variables, form id and service. Next, triggers: an initialization trigger for all pages, and a custom event trigger where the event name equals generate lead. Then tags: the G A four Google tag using the constant, fired on initialization. And an event tag, generate lead, with parameters form id and service mapped to the data layer variables, fired on the custom event trigger. Both tags use consent checks for analytics storage. | Watch me do it, part 1<br>• Variables: CONST ID, DLV form_id, DLV service<br>• Triggers: Init, CE generate_lead<br>• Tags: Google tag, event tag<br>• Consent checks on both | Layout: a container workspace builds in three columns: variables, triggers, tags; items appear in order and connecting lines draw between them. 0-10s variables appear; 10-20s triggers appear; 20-29s tags appear; 29-39s connections draw. |
| 11 | 6:18 | 37 | Now preview. I open preview mode, Tag Assistant, and load the contact page. I submit the form with a validation error. The event tag should not fire, and it doesn't. Then I submit it properly. The custom event appears in the data layer, and the event tag fires exactly once, with form id contact main and service paid social. I publish the version with a descriptive name: version twelve, generate lead via data layer push, form id and service parameters. Then I export the container. | Watch me do it, part 2<br>• Preview mode<br>• Failure path: no tag<br>• Success path: tag fires once<br>• Descriptive version name<br>• Export | Layout: a preview panel shows two form submissions: the first with a red error and no tag fired; the second with a tag fired once and parameter values displayed; a publish dialog shows the version name. 0-12s error submission, no tag; 12-25s successful submission, tag fires once; 25-37s publish dialog with version name. |
| 12 | 6:55 | 23 | Common mistakes. Double tagging, with G A four hard-coded in the site and also deployed through the tag manager, doubling page views. Relying on auto-generated CSS classes in click triggers, which change with each site build. Publishing without preview testing. Unnamed versions, making rollbacks guesswork. And giving publish rights to everyone, including third-party vendors. | Common mistakes<br>• Double tagging<br>• Fragile CSS click triggers<br>• No preview testing<br>• Unnamed versions<br>• Publish rights for everyone | Layout: five warning cards; the first shows two identical tag icons on a page with a '2x page views' label. 0-12s card 1 with double tag; 12-23s cards 2 to 5. |
| 13 | 7:18 | 30 | How do you measure container health? Four checks each quarter. Exactly one Google tag per measurement id, with no hard-coded duplicate. Every key event triggered by a data layer custom event. Every tag with an owner and a consent category. And every version named and described. If all four pass, your container is an asset. If not, it's a liability that will surprise you during your next big campaign. | Container health<br>• One Google tag per ID<br>• Key events via data layer<br>• Owner and consent category per tag<br>• Named versions | Layout: a health dashboard with four indicators turning green. 0-15s indicators 1 and 2 green; 15-30s indicators 3 and 4 green. |
| 14 | 7:48 | 24 | Recap. Tags send data, triggers decide when, variables supply values. Developers expose data in a data layer, analysts configure tags that read it. Use data layer custom events for key events. Name everything, version every publish, restrict publish rights, preview before publishing, and export the container. And always check what's already collected before building something new. | Recap<br>• Tags, triggers, variables<br>• Data layer for key events<br>• Naming and versions<br>• Preview, restrict, export<br>• Check before building | Layout: recap card with the three gears and five checkmarks. 0-12s gears appear; 12-24s checkmarks stamp. |
| 15 | 8:12 | 30 | Try this now. Open a tag manager container you can access, or a demo container. Rename five tags, triggers and variables to a consistent convention. Add an owner note to each tag. Then write a proper version description for the change, preview it, and publish or save it. If you find a hard-coded Google tag on the site as well, write it down, because that's your first fix. | Try this now<br>• Rename five items<br>• Owner notes on tags<br>• Version description<br>• Preview, then publish<br>• Look for hard-coded duplicates | Layout: a container list with five items renaming; owner notes appear; a version dialog types a description. 0-10s items rename; 10-20s owner notes; 20-30s version description types. |

<details><summary>Narration per scene (paste into ElevenLabs)</summary>

Scene 1 — Tag manager fundamentals

```text
Picture a website with forty marketing tags, installed by five different people over three years. Some fire twice. Some belong to campaigns that ended last year. And nobody's sure which ones respect the consent banner. That's what happens without a tag manager, or with one used carelessly. In this lecture you'll learn what a tag manager is for, the three building blocks of tags, triggers and variables, how to implement G A four in Google Tag Manager, and how to keep a container clean with naming, versions and publishing rules.
```

Scene 2 — Why a tag manager

```text
Why use a tag manager? It centralizes marketing and analytics tags in one governed place. It lets you change tracking without full site releases, while still following a release process. It gives you version history, workspaces and a preview and debug mode. And it makes consent integration consistent across every tag. But a tag manager isn't a license to bypass developers. The best setups split responsibilities: developers expose clean data in a data layer, analysts configure tags that read it.
```

Scene 3 — Tags, triggers, variables

```text
Here's the core model. Three building blocks. A tag is code that sends data somewhere: the Google tag, a G A four event, an ad platform pixel. A trigger is the condition that fires a tag: all pages, a custom event named generate lead, a click on a specific element. And a variable is a value read at runtime: the page URL, a data layer value like e-commerce value, or a constant like your measurement id. Here's the sentence to remember: when, the trigger, do what, the tag, with which values, the variables.
```

Scene 4 — GA4 in GTM

```text
How is G A four implemented in Google Tag Manager? With a Google tag configured with your measurement id, which starts with G dash, firing on all pages, typically on the initialization trigger. It sends page views by default and loads enhanced measurement. Then G A four event tags, one for each tracking plan event, referencing the same measurement id, with parameters mapped to variables. Store the measurement id in a constant variable, so you never retype it in dozens of tags.
```

Scene 5 — Trigger types

```text
Now, which triggers should you use? Page view, DOM ready and window loaded control timing for page-level tags. Custom event triggers fire when the data layer receives an event key, and they're the most reliable type for important events. Click triggers are fine for simple interactions, but fragile if they depend on CSS classes that designers change. Form submission triggers work on standard forms but often fail on JavaScript-heavy ones. History change handles single-page apps. And element visibility answers did the user see this. Rule of thumb: key events use data layer custom events.
```

Scene 6 — Naming convention

```text
Then naming and organization. A container with two hundred unnamed tags is unmaintainable. Adopt a convention. Tags: platform, type, detail, like G A four, event, generate lead. Triggers: type and detail, like C E, generate lead. Variables: type and detail, like D L V, e-commerce value, or CONST, G A four id. And folders by platform or feature. It sounds pedantic, but when something breaks at nine at night during a campaign launch, a clean container is the difference between a five-minute fix and a three-hour hunt.
```

Scene 7 — Release process

```text
And the release process. Workspaces let people work in parallel without overwriting each other. Every publish creates a version, so name it and describe what changed, for example, added generate lead with form id, fixed duplicate page view on the single-page app. Keep publish rights with a small group, and everyone else submits for review. If something breaks, you can roll back to a previous version in minutes. Export the container after each publish and store it with your tracking plan as a backup and audit trail.
```

Scene 8 — Example 1: brochure downloads

```text
First example, a simple one. A property developer in Abu Dhabi wants to know which projects' brochures get downloaded. Before building anything, the analyst checks what's already collected. Enhanced measurement already captures file download events with file name and link URL. So instead of a new tag, they confirm file downloads are enabled, register file name as a custom dimension, and build an exploration of downloads by file name and source. No tag manager work at all. Always check what exists before you build.
```

Scene 9 — Example 2: three agencies, one container

```text
Second example, a business case. A Karachi marketplace had three agencies adding pixels to one container. Tags fired twice, nobody knew who owned what, and a campaign pixel from last year was still collecting data. The in-house analyst created one workspace per agency, restricted publish rights to herself, required version descriptions, added an owner note to every tag, and removed eleven orphaned tags after a two-week notice. Page views returned to a single count, and consent audits became a thirty-minute task.
```

Scene 10 — Watch me do it, part 1

```text
Watch me build a minimal container. First, variables: a constant for the measurement id, and two data layer variables, form id and service. Next, triggers: an initialization trigger for all pages, and a custom event trigger where the event name equals generate lead. Then tags: the G A four Google tag using the constant, fired on initialization. And an event tag, generate lead, with parameters form id and service mapped to the data layer variables, fired on the custom event trigger. Both tags use consent checks for analytics storage.
```

Scene 11 — Watch me do it, part 2

```text
Now preview. I open preview mode, Tag Assistant, and load the contact page. I submit the form with a validation error. The event tag should not fire, and it doesn't. Then I submit it properly. The custom event appears in the data layer, and the event tag fires exactly once, with form id contact main and service paid social. I publish the version with a descriptive name: version twelve, generate lead via data layer push, form id and service parameters. Then I export the container.
```

Scene 12 — Common mistakes

```text
Common mistakes. Double tagging, with G A four hard-coded in the site and also deployed through the tag manager, doubling page views. Relying on auto-generated CSS classes in click triggers, which change with each site build. Publishing without preview testing. Unnamed versions, making rollbacks guesswork. And giving publish rights to everyone, including third-party vendors.
```

Scene 13 — Container health

```text
How do you measure container health? Four checks each quarter. Exactly one Google tag per measurement id, with no hard-coded duplicate. Every key event triggered by a data layer custom event. Every tag with an owner and a consent category. And every version named and described. If all four pass, your container is an asset. If not, it's a liability that will surprise you during your next big campaign.
```

Scene 14 — Recap

```text
Recap. Tags send data, triggers decide when, variables supply values. Developers expose data in a data layer, analysts configure tags that read it. Use data layer custom events for key events. Name everything, version every publish, restrict publish rights, preview before publishing, and export the container. And always check what's already collected before building something new.
```

Scene 15 — Try this now

```text
Try this now. Open a tag manager container you can access, or a demo container. Rename five tags, triggers and variables to a consistent convention. Add an owner note to each tag. Then write a proper version description for the change, preview it, and publish or save it. If you find a hard-coded Google tag on the site as well, write it down, because that's your first fix.
```

</details>

### 8. The data layer and custom events

Lesson `data-layer-and-custom-events` · module “Tagging with a tag manager” · target 8 min · 15 scenes · 1182 words · planned 8:34 (words ≈ 8:34) · status: **to produce**

| Term | Say |
|---|---|
| GA4 | G A four |
| GTM | G T M |
| SPA | S P A |
| DOM | dom |
| PII | P I I |
| CSS | C S S |
| JSON | jay-son |
| React | React |
| CRM | C R M |

| # | Time | Sec | Narration | On screen | Visual |
|---|---|---|---|---|---|
| 1 | 0:00 | 37 | Here's a question that separates fragile tracking from reliable tracking. When your website redesigns its contact form next month, will your lead tracking break? If your tags read button text or CSS classes, probably yes. If they read a data layer, probably not. In this lecture you'll learn what a data layer is, the push pattern, how to write a developer specification that gets implemented correctly the first time, how timing and single-page apps change things, and how to protect measurement with an automated contract test. | The data layer and custom events<br>• What a data layer is<br>• The push pattern<br>• Developer specifications<br>• Timing and single-page apps<br>• Contract tests | Layout: a contact form redesigns itself (colors and layout shift); a tag reading CSS breaks with a red spark while a tag reading the data layer keeps a steady green light. 0-9s form redesigns; 9-18s CSS-based tag breaks; 18-28s data layer tag stays green; 28-37s title builds. |
| 2 | 0:37 | 35 | Why does the data layer matter? Because it decouples what happened on the site from which tools receive it. When the site pushes generate lead, the tag manager can forward it to G A four, an ad platform and a C R M integration, without the developer knowing about any of them. The site speaks one clear language, and every tool listens. That makes tracking resilient to redesigns, easier to test, and much easier to govern for consent and privacy. | Why a data layer<br>• Decouples events from tools<br>• One push, many destinations<br>• Resilient to redesigns<br>• Easier testing and governance | Layout: a single 'generate_lead' message leaves a website and fans out through a tag manager hub to three destination icons. 0-12s message leaves site; 12-23s hub receives; 23-35s fans out to three destinations. |
| 3 | 1:12 | 32 | Here's an analogy. The data layer is like an airport departures board. The airline, that's your website, posts accurate information: flight, gate, time. Everyone who needs it, passengers, cleaners, caterers, reads the board. Nobody has to phone the pilot. If the airline changes the terminal's design, the board still shows the same fields. In G T M, the board is an array called data layer, and the website adds information with data layer push. | The departures board<br>• Website posts structured facts<br>• Tools read the board<br>• Redesigns don't change the fields<br>• In GTM: the dataLayer array | Layout: a departures board with rows labeled event, form_id, service; small icons of tools read from it; the terminal behind it changes style while the board stays the same. 0-11s board appears; 11-21s tools read from it; 21-32s terminal restyles, board unchanged. |
| 4 | 1:44 | 35 | The push pattern is simple. Initialize the array if it doesn't exist, then push an object. If the object contains an event key, it can trigger tags. For example: event generate lead, form id contact main, service paid social, lead type consultation. In G T M, you then create data layer variables for form id, service and lead type, a custom event trigger that matches generate lead, and a G A four event tag with those parameters mapped to the variables. | The push pattern<br>• Initialize dataLayer<br>• Push an object with event<br>• DLVs for each key<br>• CE trigger + GA4 event tag | Layout: code snippet with the push highlighted; three variable chips extract from it; a trigger and tag card connect. 0-12s push highlights; 12-23s variable chips extract; 23-35s trigger and tag connect. |
| 5 | 2:19 | 30 | E-commerce needs one extra habit. Google's documented structure uses an e-commerce object inside the push. Always clear the previous e-commerce object before pushing a new one, by pushing e-commerce null. Otherwise values can leak between events, and a purchase might carry items from an earlier view item. Many e-commerce platforms have plugins that produce this structure. Verify the output rather than assuming it's right, because plugins get updated too. | E-commerce pushes<br>• ecommerce object<br>• Push ecommerce: null first<br>• Prevent leaking items<br>• Verify plugin output | Layout: two consecutive pushes: without the null push, items from view_item leak into purchase (red); with the null push inserted, purchase is clean (green). 0-10s leaky sequence red; 10-20s null push inserted; 20-30s clean sequence green. |
| 6 | 2:49 | 36 | Now the developer specification, which is where most projects succeed or fail. Developers need precise instructions, not please track the form. A good spec says which event, exactly when: after the server returns success, and not on validation errors or network failures. It lists each key with its type and allowed values. It lists the pages. It gives test cases: submit valid, see one event; submit invalid, see none. And it says no personal data, ever: never name, email, phone or message text. | Developer spec<br>• Event and exact moment<br>• Keys, types, allowed values<br>• Pages<br>• Test cases<br>• No PII, ever | Layout: a spec document builds section by section: WHEN, PUSH, PAGES, TEST, NO PII, each with a small icon. 0-9s WHEN section; 9-18s PUSH section; 18-27s PAGES and TEST sections; 27-36s NO PII banner. |
| 7 | 3:25 | 40 | Timing and ordering matter too. Data needed on page load, like page type, content group or whether the user is logged in, should be pushed before the tag manager container snippet, or at least before the Google tag fires, so page views carry it. Interaction events are pushed when they happen. And single-page applications, the React, Vue or Angular kind, don't reload between views. So either developers push a virtual page view on route change, or you rely on G A four's history change page views. Not both, or you'll double count. | Timing and SPAs<br>• Page-load data before the container<br>• Interaction events when they happen<br>• SPAs: developer push OR history change<br>• Never both | Layout: a page-load timeline with a data push before the container snippet; an SPA route diagram shows two page view sources merging into one with a 'choose one' label. 0-10s page-load timeline; 10-20s data push before container; 20-30s SPA route diagram; 30-40s choose-one label. |
| 8 | 4:05 | 41 | First example, a simple one. A logistics company in Jeddah uses a WordPress form plugin that redirects to a thank-you page on success. Option one: fire generate lead on the thank-you page view. Simple, but anyone who bookmarks or revisits the page creates a false lead, and you can't tell which form was used. Option two: a developer adds a small script to the plugin's success callback that pushes generate lead with form id and service. Option two is more accurate and richer. If option one is all you can do, document the limitation. | Example 1: WordPress quote form<br>• Thank-you page trigger: simple, inaccurate<br>• Success callback push: accurate, richer<br>• Document limitations | Layout: two lanes: lane one fires from a thank-you page with a ghost 'bookmark revisit' creating a false lead; lane two fires from a success callback with form details. 0-14s lane one with false lead; 14-27s lane two with rich push; 27-41s lane two gets a check. |
| 9 | 4:46 | 36 | Second example, a business case. A Riyadh fashion retailer's site is a React single-page app. Page views were double counted, because developers pushed a virtual page view on route change while G A four's history change page views were also on. The team picked one method, the developer push with page type and content group, turned off the history change option, and added an automated contract test for route changes. Engagement metrics stabilized, and content group reporting finally matched the site's structure. | Example 2: a Riyadh SPA<br>• Double page views<br>• Keep developer push, disable history change<br>• Contract test for routes<br>• Stable engagement metrics | Layout: a page view counter doubles on each route change; one source switches off; the counter increments once per route; a test badge appears. 0-9s counter doubles; 9-18s history change switches off; 18-27s counter increments once; 27-36s test badge. |
| 10 | 5:22 | 37 | Watch me write a push from a real success callback. The form submits with fetch to the quote endpoint. I read the JSON response. If the response isn't OK, or the success flag is false, I show the error and return, with no analytics event. Only after success do I initialize the data layer and push generate lead, form id quote request, service from a controlled dropdown, and lead type quote. Never the name, email, phone or message. Then I show the thank-you message. | Watch me do it, part 1<br>• fetch, then read response<br>• On failure: return, no event<br>• On success: push with controlled values<br>• Never PII | Layout: code editor with the async function; the failure branch highlights red and exits; the success branch highlights green and shows the push. 0-9s fetch lines highlight; 9-18s failure branch red; 18-28s success branch green; 28-37s push highlights. |
| 11 | 5:59 | 38 | Then I protect it with a contract test. In the developers' end-to-end tests, for example with Playwright, the test fills the service field, clicks submit, waits for the thank-you message, and reads the data layer. It expects exactly one generate lead push, with form id quote request and service S E O, and it fails if anything that looks like an email appears in the push. Now, if a redesign breaks the push, the build fails before the site goes live, not three weeks later in a report. | Watch me do it, part 2<br>• End-to-end test submits the form<br>• Expect exactly one push<br>• Expect correct values<br>• Fail on anything email-like | Layout: a test runner panel shows steps: fill, click, wait, read data layer; assertions tick green; then a simulated redesign makes an assertion fail red and blocks a deploy button. 0-10s test steps run; 10-19s assertions green; 19-28s redesign causes red failure; 28-38s deploy blocked. |
| 12 | 6:37 | 38 | A quick word on server-side tagging, since it comes up in every data layer conversation. In server-side tagging, the browser sends data to a tagging server you control, often on your own subdomain, which forwards it to vendors. It can give you more control over what each vendor receives and reduce third-party scripts in the browser. But it adds hosting cost and complexity, and it doesn't remove the need for consent. Legal obligations follow the data, not the architecture. The Privacy-First Measurement course covers it in depth. | Server-side tagging<br>• Browser to your tagging server to vendors<br>• More control, fewer browser scripts<br>• Cost and complexity<br>• Consent still applies | Layout: browser icon sends data to a server icon on a first-party subdomain, which forwards filtered streams to vendor icons; a consent shield sits at the browser end. 0-10s browser sends; 10-19s server receives; 19-28s filtered streams forward; 28-38s consent shield appears. |
| 13 | 7:15 | 23 | Common mistakes. Pushing personal data for convenience. Never do it. Not clearing e-commerce between pushes. Relying on thank-you page URLs alone for important conversions. Changing data layer key names without telling the analytics team, so G T M variables silently go undefined. And pushing events before the data layer is initialized, which overwrites it. | Common mistakes<br>• PII in pushes<br>• No ecommerce clear<br>• Thank-you URL only<br>• Silent key renames<br>• Overwriting dataLayer | Layout: five warning cards appear; the fourth shows a variable chip turning to 'undefined'. 0-8s cards 1 to 3; 8-15s card 4 with undefined; 15-23s card 5. |
| 14 | 7:38 | 29 | Recap. The data layer separates what happened on the site from which tools receive it. Give developers exact specs: when to push, keys, allowed values and test cases. Push on confirmed success. Clear the e-commerce object before each new e-commerce push. Choose one page view method on single-page apps. Protect it all with a contract test. And remember that server-side tagging adds control, not consent exemptions. | Recap<br>• Decoupling<br>• Exact specs<br>• Success-only pushes<br>• Clear ecommerce<br>• One SPA method<br>• Contract tests | Layout: recap card with the departures board icon and six checkmarks. 0-14s board icon; 14-29s checkmarks. |
| 15 | 8:07 | 27 | Try this now. Write a developer specification for one key event on a site you know, using the template in the lesson: the event, when it fires, the keys and allowed values, the pages, test cases for success and failure, and a no personal data line. Then ask a developer to add the contract test from the lesson to their test suite. | Try this now<br>• One developer spec<br>• Success and failure test cases<br>• No-PII line<br>• Add the contract test | Layout: a spec template fills; a test file icon attaches to it. 0-14s spec fills; 14-27s test file attaches. |

<details><summary>Narration per scene (paste into ElevenLabs)</summary>

Scene 1 — The data layer and custom events

```text
Here's a question that separates fragile tracking from reliable tracking. When your website redesigns its contact form next month, will your lead tracking break? If your tags read button text or CSS classes, probably yes. If they read a data layer, probably not. In this lecture you'll learn what a data layer is, the push pattern, how to write a developer specification that gets implemented correctly the first time, how timing and single-page apps change things, and how to protect measurement with an automated contract test.
```

Scene 2 — Why a data layer

```text
Why does the data layer matter? Because it decouples what happened on the site from which tools receive it. When the site pushes generate lead, the tag manager can forward it to G A four, an ad platform and a C R M integration, without the developer knowing about any of them. The site speaks one clear language, and every tool listens. That makes tracking resilient to redesigns, easier to test, and much easier to govern for consent and privacy.
```

Scene 3 — The departures board

```text
Here's an analogy. The data layer is like an airport departures board. The airline, that's your website, posts accurate information: flight, gate, time. Everyone who needs it, passengers, cleaners, caterers, reads the board. Nobody has to phone the pilot. If the airline changes the terminal's design, the board still shows the same fields. In G T M, the board is an array called data layer, and the website adds information with data layer push.
```

Scene 4 — The push pattern

```text
The push pattern is simple. Initialize the array if it doesn't exist, then push an object. If the object contains an event key, it can trigger tags. For example: event generate lead, form id contact main, service paid social, lead type consultation. In G T M, you then create data layer variables for form id, service and lead type, a custom event trigger that matches generate lead, and a G A four event tag with those parameters mapped to the variables.
```

Scene 5 — E-commerce pushes

```text
E-commerce needs one extra habit. Google's documented structure uses an e-commerce object inside the push. Always clear the previous e-commerce object before pushing a new one, by pushing e-commerce null. Otherwise values can leak between events, and a purchase might carry items from an earlier view item. Many e-commerce platforms have plugins that produce this structure. Verify the output rather than assuming it's right, because plugins get updated too.
```

Scene 6 — Developer spec

```text
Now the developer specification, which is where most projects succeed or fail. Developers need precise instructions, not please track the form. A good spec says which event, exactly when: after the server returns success, and not on validation errors or network failures. It lists each key with its type and allowed values. It lists the pages. It gives test cases: submit valid, see one event; submit invalid, see none. And it says no personal data, ever: never name, email, phone or message text.
```

Scene 7 — Timing and SPAs

```text
Timing and ordering matter too. Data needed on page load, like page type, content group or whether the user is logged in, should be pushed before the tag manager container snippet, or at least before the Google tag fires, so page views carry it. Interaction events are pushed when they happen. And single-page applications, the React, Vue or Angular kind, don't reload between views. So either developers push a virtual page view on route change, or you rely on G A four's history change page views. Not both, or you'll double count.
```

Scene 8 — Example 1: WordPress quote form

```text
First example, a simple one. A logistics company in Jeddah uses a WordPress form plugin that redirects to a thank-you page on success. Option one: fire generate lead on the thank-you page view. Simple, but anyone who bookmarks or revisits the page creates a false lead, and you can't tell which form was used. Option two: a developer adds a small script to the plugin's success callback that pushes generate lead with form id and service. Option two is more accurate and richer. If option one is all you can do, document the limitation.
```

Scene 9 — Example 2: a Riyadh SPA

```text
Second example, a business case. A Riyadh fashion retailer's site is a React single-page app. Page views were double counted, because developers pushed a virtual page view on route change while G A four's history change page views were also on. The team picked one method, the developer push with page type and content group, turned off the history change option, and added an automated contract test for route changes. Engagement metrics stabilized, and content group reporting finally matched the site's structure.
```

Scene 10 — Watch me do it, part 1

```text
Watch me write a push from a real success callback. The form submits with fetch to the quote endpoint. I read the JSON response. If the response isn't OK, or the success flag is false, I show the error and return, with no analytics event. Only after success do I initialize the data layer and push generate lead, form id quote request, service from a controlled dropdown, and lead type quote. Never the name, email, phone or message. Then I show the thank-you message.
```

Scene 11 — Watch me do it, part 2

```text
Then I protect it with a contract test. In the developers' end-to-end tests, for example with Playwright, the test fills the service field, clicks submit, waits for the thank-you message, and reads the data layer. It expects exactly one generate lead push, with form id quote request and service S E O, and it fails if anything that looks like an email appears in the push. Now, if a redesign breaks the push, the build fails before the site goes live, not three weeks later in a report.
```

Scene 12 — Server-side tagging

```text
A quick word on server-side tagging, since it comes up in every data layer conversation. In server-side tagging, the browser sends data to a tagging server you control, often on your own subdomain, which forwards it to vendors. It can give you more control over what each vendor receives and reduce third-party scripts in the browser. But it adds hosting cost and complexity, and it doesn't remove the need for consent. Legal obligations follow the data, not the architecture. The Privacy-First Measurement course covers it in depth.
```

Scene 13 — Common mistakes

```text
Common mistakes. Pushing personal data for convenience. Never do it. Not clearing e-commerce between pushes. Relying on thank-you page URLs alone for important conversions. Changing data layer key names without telling the analytics team, so G T M variables silently go undefined. And pushing events before the data layer is initialized, which overwrites it.
```

Scene 14 — Recap

```text
Recap. The data layer separates what happened on the site from which tools receive it. Give developers exact specs: when to push, keys, allowed values and test cases. Push on confirmed success. Clear the e-commerce object before each new e-commerce push. Choose one page view method on single-page apps. Protect it all with a contract test. And remember that server-side tagging adds control, not consent exemptions.
```

Scene 15 — Try this now

```text
Try this now. Write a developer specification for one key event on a site you know, using the template in the lesson: the event, when it fires, the keys and allowed values, the pages, test cases for success and failure, and a no personal data line. Then ask a developer to add the contract test from the lesson to their test suite.
```

</details>

### 9. QA, debugging and release discipline

Lesson `qa-and-debugging` · module “Tagging with a tag manager” · target 8 min · 15 scenes · 1157 words · planned 8:22 (words ≈ 8:23) · status: **to produce**

| Term | Say |
|---|---|
| GA4 | G A four |
| GTM | G T M |
| SPA | S P A |
| DOM | dom |
| PII | P I I |
| CSS | C S S |
| GCC | G C C |
| UTMs | U T Ms |
| BigQuery | Big Query |

| # | Time | Sec | Narration | On screen | Visual |
|---|---|---|---|---|---|
| 1 | 0:00 | 37 | Here's a rule that will save your reputation as an analyst: assume every tracking implementation is broken until you've proven otherwise. Not because developers are careless, but because websites change constantly, and measurement is invisible when it breaks. In this lecture you'll learn the debugging toolkit, a step-by-step QA script, the most common bugs and how to spot them, how to reconcile G A four with your backend, and how to monitor automatically with the BigQuery export so you hear about problems before your stakeholders do. | QA and debugging<br>• Assume it's broken<br>• The debugging toolkit<br>• A step-by-step QA script<br>• Common bugs<br>• Reconciliation and monitoring | Layout: a green 'tracking OK' light flickers and then shows a hidden crack; a magnifying glass reveals it; title builds. 0-9s green light flickers; 9-18s crack revealed; 18-28s magnifier appears; 28-37s title builds. |
| 2 | 0:37 | 36 | Why is QA the analyst's job? Because bad data is worse than no data. People act on it. A key event that fires on a button click rather than on success makes a campaign look twice as good as it is. A missing currency makes revenue look tiny. And nobody notices, because the numbers still look like numbers. Every tracking change should pass a repeatable QA process before and after release, and the analyst is the person who knows what correct looks like. | Why QA is your job<br>• Bad data drives bad decisions<br>• Errors look like normal numbers<br>• Repeatable QA before and after release | Layout: two identical-looking dashboards side by side; a toggle reveals one is built on broken data with red cracks underneath. 0-18s two dashboards appear; 18-36s toggle reveals cracks under one. |
| 3 | 1:13 | 35 | Here's the toolkit. Tag manager preview mode, also called Tag Assistant, shows which tags fired on each action, which didn't, and variable values at that moment. G A four DebugView shows events arriving in G A four from debug-enabled devices, with parameters and user properties. The Realtime report is a sanity check that production events arrive at all. The browser's network tab lets you inspect requests to the collection endpoint. And a staging environment lets you test without polluting production data. | The toolkit<br>• Preview mode: what fired and values<br>• DebugView: what arrived<br>• Realtime: sanity check<br>• Network tab: raw requests<br>• Staging: safe testing | Layout: five tool icons in a toolbox; each lifts out with a one-line caption as narrated. 0-9s preview tool; 9-18s DebugView tool; 18-26s Realtime and network tools; 26-35s staging tool. |
| 4 | 1:48 | 37 | Think of it like a parcel delivery. Preview mode is the warehouse camera: did the parcel leave, with the right label? DebugView is the delivery confirmation: did it arrive at G A four, intact? Realtime is glancing out the window to see if any vans are arriving at all. And the reports, a day or two later, are the monthly statement. You need all four views, because a parcel can leave correctly and still arrive damaged, or arrive fine and be filed in the wrong place. | The parcel analogy<br>• Preview: did it leave correctly?<br>• DebugView: did it arrive intact?<br>• Realtime: any vans at all?<br>• Reports: the monthly statement | Layout: a parcel travels from a warehouse (camera icon) to a delivery point (check icon); a window view shows vans arriving; a statement document appears. 0-9s warehouse camera; 9-18s delivery confirmation; 18-28s vans at window; 28-37s statement. |
| 5 | 2:25 | 37 | Here's the QA script for each event in the plan. Trigger: perform the action as a user would, and the failure path too. Tag fired: preview shows it fired once, on the correct trigger. Values: variables resolved, no undefined, no personal data. Arrived: DebugView shows the right name and parameters. Consent: repeat with consent denied and check behavior matches your design. Devices: mobile and another browser. Reports: confirm a day or two later. And document: mark the row verified with the date and tester. | The QA script<br>• Trigger, including failure<br>• Fired once<br>• Values clean<br>• Arrived in DebugView<br>• Consent denied<br>• Devices, reports, document | Layout: an eight-step checklist builds; each step ticks with a small icon. 0-12s steps 1 to 3 tick; 12-25s steps 4 and 5 tick; 25-37s steps 6 to 8 tick. |
| 6 | 3:02 | 45 | Now the most common bugs. Page views doubled: usually a hard-coded tag plus the tag manager, or single-page app double firing. Key events far above backend counts: a trigger on the click or attempt, or confirmation page reloads. Parameters showing not set: the variable was undefined at fire time, or the dimension isn't registered. Revenue in the wrong magnitude: missing currency, or values in minor units like cents or paisa. Paid traffic appearing as referral or direct: stripped UTMs or missing unwanted referrals. And a sudden drop to zero: an unpublished container, a blocking consent banner, or a release that removed the snippet. | Common bugs<br>• Doubled page views<br>• Inflated key events<br>• (not set) parameters<br>• Wrong revenue magnitude<br>• Misattributed paid traffic<br>• Drop to zero | Layout: a symptom-to-cause table animates row by row with small icons. 0-15s rows 1 and 2; 15-30s rows 3 and 4; 30-45s rows 5 and 6. |
| 7 | 3:47 | 31 | Then reconciliation. G A four key events will rarely match your backend exactly. Consent choices, ad blockers, network failures and browser restrictions all remove some hits. So establish a routine: compare weekly G A four purchases with orders from your system. Define an acceptable variance for your context. Here's the key idea: a stable gap is manageable, a changing gap signals breakage. Explain the gap to stakeholders rather than hiding it. | Reconciliation<br>• GA4 won't match backend exactly<br>• Weekly comparison<br>• Define acceptable variance<br>• Stable gap: fine; changing gap: broken | Layout: two parallel lines (backend and GA4) with a steady shaded gap; then the gap suddenly widens and a red alert pulses. 0-8s parallel lines; 8-16s steady gap; 16-23s gap widens; 23-31s alert pulses. |
| 8 | 4:18 | 40 | First example, a simple one. A G C C e-commerce brand notices G A four purchases from Saudi Arabia dropped sharply, while backend orders were steady. Realtime shows purchases from other countries, so the tag works in general. Preview mode on the Saudi storefront reveals the purchase tag doesn't fire: a new local payment method redirects to a different confirmation URL, not matched by the trigger. The fix: move the trigger to a data layer purchase push from the order confirmation logic, and add the new payment method to the QA script. | Example 1: missing Saudi orders<br>• KSA purchases drop, orders steady<br>• Realtime: works elsewhere<br>• Preview: new payment URL not matched<br>• Fix: data layer push from order logic | Layout: a country bar chart with the KSA bar collapsing; a redirect diagram shows a new confirmation URL bypassing the trigger; the trigger moves to a data layer push and the bar recovers. 0-10s KSA bar collapses; 10-20s redirect diagram; 20-30s trigger moves; 30-40s bar recovers. |
| 9 | 4:58 | 35 | Second example, a business case with illustrative numbers. A Bristol subscription company's weekly reconciliation shows G A four purchases about twelve percent below orders for months, which the team accepts as consent and ad-blocker loss. One week the gap jumps. Their automated health query shows purchases missing a transaction id rising right after a checkout update: a new payment step sent purchase without the id. The developer restored the field, and the gap returned to its usual level within a day. | Example 2: Bristol subscriptions<br>• Stable ~12% gap (illustrative)<br>• Sudden jump<br>• Health query: missing transaction_id<br>• Fixed within a day | Layout: a gap chart with a steady band and a sudden spike; a health query result highlights 'purchases_missing_id' rising; after the fix the band returns. 0-9s steady band; 9-18s spike; 18-26s health query highlight; 26-35s band returns. |
| 10 | 5:33 | 36 | Watch me build that health query in BigQuery. For the last fourteen days, grouped by event date, I count page views and purchases. I count distinct transaction ids among purchases, which gives unique orders. I count purchases where the transaction id is null. And I count purchases missing a value. If purchases exceed unique orders, I have duplicates. Any missing ids or values are bugs. And a sudden drop in page views on one day points to a release or a consent change. | Watch me do it, part 1<br>• Last 14 days by date<br>• Page views, purchases, unique orders<br>• Missing IDs and values<br>• Read the signals | Layout: SQL editor with the health query; each COUNTIF line highlights as named; a results table shows one day with purchases above unique orders flagged. 0-9s date range highlights; 9-18s count lines highlight; 18-27s missing checks highlight; 27-36s result row flagged. |
| 11 | 6:09 | 35 | Then I automate it. I save the query as a BigQuery scheduled query that runs every morning, and write results to a table that feeds a small sheet or a chat alert. I also create custom insights in G A four for sudden drops in key events or sessions. And whenever tracking changes, I add an annotation or change log entry, so future analysts can see when data behavior changed. Now problems find me, instead of my stakeholders finding them. | Watch me do it, part 2<br>• Scheduled query each morning<br>• Results to a sheet or alert<br>• GA4 custom insights<br>• Annotate tracking changes | Layout: a clock icon triggers a query run; results flow into a sheet and a chat bubble alert; a GA4-style alert card and an annotation marker on a chart appear. 0-9s clock triggers run; 9-18s results to sheet and chat; 18-26s alert card; 26-35s annotation marker. |
| 12 | 6:44 | 20 | Common mistakes. Testing only the happy path and never the failure path. Testing only on desktop Chrome with an ad blocker disabled. Declaring success because Realtime shows something. Not re-testing after site releases. And chasing a perfect match with backend data instead of a stable, explained variance. | Common mistakes<br>• Happy path only<br>• One browser, one device<br>• 'Realtime shows something'<br>• No re-test after releases<br>• Chasing a perfect match | Layout: five warning cards drop in sequence. 0-10s cards 1 to 3; 10-20s cards 4 and 5. |
| 13 | 7:04 | 28 | And the release checklist. The QA script completed for every changed event. Consent-denied behavior verified. The version named and described, and the change log updated. Stakeholders told what changed and from which date. And an annotation added, so the next person knows when the data changed. It takes ten minutes and it's the difference between a trusted analytics function and one that's always explaining surprises. | Release checklist<br>• QA script per changed event<br>• Consent-denied verified<br>• Version and change log<br>• Stakeholders informed<br>• Annotation added | Layout: a release checklist with five items ticking; a 'trusted' badge appears at the end. 0-14s items tick; 14-28s trusted badge. |
| 14 | 7:32 | 23 | Recap. Assume tracking is broken until a repeatable QA script proves otherwise. Use preview for tags, DebugView for arrival, Realtime for sanity and reports for final confirmation. Expect a stable gap with your backend, and investigate when it changes. Re-test after every release, and automate monitoring with a scheduled health query and alerts. | Recap<br>• Assume broken<br>• Four views<br>• Stable gap, investigate changes<br>• Re-test and automate | Layout: recap card with parcel analogy icons and checkmarks. 0-12s icons; 12-23s checkmarks. |
| 15 | 7:55 | 27 | Try this now. Write a QA script for three events on a site you know and run it in preview mode, including the failure path and a consent-denied pass. Record the results in a status column in your tracking plan. If you have the BigQuery export, run the health query from the lesson for the last fourteen days and note anything suspicious. | Try this now<br>• QA script for three events<br>• Failure path and consent denied<br>• Record status<br>• Run the health query | Layout: tracking plan rows gain 'verified' status chips with dates; a query run icon appears. 0-14s status chips appear; 14-27s query runs. |

<details><summary>Narration per scene (paste into ElevenLabs)</summary>

Scene 1 — QA and debugging

```text
Here's a rule that will save your reputation as an analyst: assume every tracking implementation is broken until you've proven otherwise. Not because developers are careless, but because websites change constantly, and measurement is invisible when it breaks. In this lecture you'll learn the debugging toolkit, a step-by-step QA script, the most common bugs and how to spot them, how to reconcile G A four with your backend, and how to monitor automatically with the BigQuery export so you hear about problems before your stakeholders do.
```

Scene 2 — Why QA is your job

```text
Why is QA the analyst's job? Because bad data is worse than no data. People act on it. A key event that fires on a button click rather than on success makes a campaign look twice as good as it is. A missing currency makes revenue look tiny. And nobody notices, because the numbers still look like numbers. Every tracking change should pass a repeatable QA process before and after release, and the analyst is the person who knows what correct looks like.
```

Scene 3 — The toolkit

```text
Here's the toolkit. Tag manager preview mode, also called Tag Assistant, shows which tags fired on each action, which didn't, and variable values at that moment. G A four DebugView shows events arriving in G A four from debug-enabled devices, with parameters and user properties. The Realtime report is a sanity check that production events arrive at all. The browser's network tab lets you inspect requests to the collection endpoint. And a staging environment lets you test without polluting production data.
```

Scene 4 — The parcel analogy

```text
Think of it like a parcel delivery. Preview mode is the warehouse camera: did the parcel leave, with the right label? DebugView is the delivery confirmation: did it arrive at G A four, intact? Realtime is glancing out the window to see if any vans are arriving at all. And the reports, a day or two later, are the monthly statement. You need all four views, because a parcel can leave correctly and still arrive damaged, or arrive fine and be filed in the wrong place.
```

Scene 5 — The QA script

```text
Here's the QA script for each event in the plan. Trigger: perform the action as a user would, and the failure path too. Tag fired: preview shows it fired once, on the correct trigger. Values: variables resolved, no undefined, no personal data. Arrived: DebugView shows the right name and parameters. Consent: repeat with consent denied and check behavior matches your design. Devices: mobile and another browser. Reports: confirm a day or two later. And document: mark the row verified with the date and tester.
```

Scene 6 — Common bugs

```text
Now the most common bugs. Page views doubled: usually a hard-coded tag plus the tag manager, or single-page app double firing. Key events far above backend counts: a trigger on the click or attempt, or confirmation page reloads. Parameters showing not set: the variable was undefined at fire time, or the dimension isn't registered. Revenue in the wrong magnitude: missing currency, or values in minor units like cents or paisa. Paid traffic appearing as referral or direct: stripped UTMs or missing unwanted referrals. And a sudden drop to zero: an unpublished container, a blocking consent banner, or a release that removed the snippet.
```

Scene 7 — Reconciliation

```text
Then reconciliation. G A four key events will rarely match your backend exactly. Consent choices, ad blockers, network failures and browser restrictions all remove some hits. So establish a routine: compare weekly G A four purchases with orders from your system. Define an acceptable variance for your context. Here's the key idea: a stable gap is manageable, a changing gap signals breakage. Explain the gap to stakeholders rather than hiding it.
```

Scene 8 — Example 1: missing Saudi orders

```text
First example, a simple one. A G C C e-commerce brand notices G A four purchases from Saudi Arabia dropped sharply, while backend orders were steady. Realtime shows purchases from other countries, so the tag works in general. Preview mode on the Saudi storefront reveals the purchase tag doesn't fire: a new local payment method redirects to a different confirmation URL, not matched by the trigger. The fix: move the trigger to a data layer purchase push from the order confirmation logic, and add the new payment method to the QA script.
```

Scene 9 — Example 2: Bristol subscriptions

```text
Second example, a business case with illustrative numbers. A Bristol subscription company's weekly reconciliation shows G A four purchases about twelve percent below orders for months, which the team accepts as consent and ad-blocker loss. One week the gap jumps. Their automated health query shows purchases missing a transaction id rising right after a checkout update: a new payment step sent purchase without the id. The developer restored the field, and the gap returned to its usual level within a day.
```

Scene 10 — Watch me do it, part 1

```text
Watch me build that health query in BigQuery. For the last fourteen days, grouped by event date, I count page views and purchases. I count distinct transaction ids among purchases, which gives unique orders. I count purchases where the transaction id is null. And I count purchases missing a value. If purchases exceed unique orders, I have duplicates. Any missing ids or values are bugs. And a sudden drop in page views on one day points to a release or a consent change.
```

Scene 11 — Watch me do it, part 2

```text
Then I automate it. I save the query as a BigQuery scheduled query that runs every morning, and write results to a table that feeds a small sheet or a chat alert. I also create custom insights in G A four for sudden drops in key events or sessions. And whenever tracking changes, I add an annotation or change log entry, so future analysts can see when data behavior changed. Now problems find me, instead of my stakeholders finding them.
```

Scene 12 — Common mistakes

```text
Common mistakes. Testing only the happy path and never the failure path. Testing only on desktop Chrome with an ad blocker disabled. Declaring success because Realtime shows something. Not re-testing after site releases. And chasing a perfect match with backend data instead of a stable, explained variance.
```

Scene 13 — Release checklist

```text
And the release checklist. The QA script completed for every changed event. Consent-denied behavior verified. The version named and described, and the change log updated. Stakeholders told what changed and from which date. And an annotation added, so the next person knows when the data changed. It takes ten minutes and it's the difference between a trusted analytics function and one that's always explaining surprises.
```

Scene 14 — Recap

```text
Recap. Assume tracking is broken until a repeatable QA script proves otherwise. Use preview for tags, DebugView for arrival, Realtime for sanity and reports for final confirmation. Expect a stable gap with your backend, and investigate when it changes. Re-test after every release, and automate monitoring with a scheduled health query and alerts.
```

Scene 15 — Try this now

```text
Try this now. Write a QA script for three events on a site you know and run it in preview mode, including the failure path and a consent-denied pass. Record the results in a status column in your tracking plan. If you have the BigQuery export, run the health query from the lesson for the last fourteen days and note anything suspicious.
```

</details>

### 10. UTM discipline and channel groupings

Lesson `utm-discipline` · module “Campaign tracking, consent and privacy” · target 8 min · 15 scenes · 1135 words · planned 8:13 (words ≈ 8:13) · status: **to produce**

| Term | Say |
|---|---|
| GA4 | G A four |
| UTM | U T M |
| UTMs | U T Ms |
| CMP | C M P |
| GDPR | G D P R |
| PII | P I I |
| QR | Q R |
| gclid | G click I D |
| CPC | C P C |
| SMS | S M S |

| # | Time | Sec | Narration | On screen | Visual |
|---|---|---|---|---|---|
| 1 | 0:00 | 37 | Here's a quick quiz. A customer taps a link in your WhatsApp broadcast, lands on your site and buys. Where does G A four put that sale? If the link wasn't tagged, very possibly in Direct, or as a meaningless referral. Your best-performing channel just became invisible. In this lecture you'll learn how UTM parameters label links, how your medium values decide G A four's channel reports, how to build and enforce a taxonomy, when not to use UTMs, and how to find messy values automatically. | UTM discipline<br>• How UTMs label links<br>• Medium decides channel<br>• Build and enforce a taxonomy<br>• When not to use UTMs<br>• Find leaks automatically | Layout: a phone with a WhatsApp-style chat bubble link; a purchase lands in a 'Direct' bucket with a question mark; a UTM tag then attaches and reroutes it to the right channel. 0-9s chat link appears; 9-18s purchase drops into Direct; 18-28s UTM tag attaches; 28-37s reroutes to correct channel. |
| 2 | 0:37 | 30 | Why does this matter? Because some sources are detected automatically, like search engines, referrals and Google Ads with auto-tagging. Many aren't: emails, messaging apps, influencer bios, QR codes, many paid social ads. Untagged clicks often land in Direct or Unassigned. Then budget decisions are made on a picture where your email program, your creators or your WhatsApp list look worthless. UTMs are how you make those channels visible. | Why it matters<br>• Auto-detected: search, referrals, Ads auto-tagging<br>• Not detected: email, messaging, bios, QR<br>• Untagged = Direct or Unassigned<br>• Budget decisions go wrong | Layout: two columns of source icons: auto-detected on the left with checks; untagged on the right falling into a gray 'Direct / Unassigned' bin. 0-10s auto-detected column; 10-20s untagged column; 20-30s icons fall into gray bin. |
| 3 | 1:07 | 42 | Here are the parameters. U T M source: who sent the traffic, like instagram, newsletter or partner blog. U T M medium: the type of channel, like paid social, email, affiliate or c p c. U T M campaign: the campaign name. U T M content: which creative or link variant. U T M term: the keyword, mainly for paid search. And U T M id, a campaign id useful for cost data imports. Think of a shipping label: source is the sender, medium is the delivery service, campaign is the order number, content is which box. | The parameters<br>• source: who sent it<br>• medium: channel type<br>• campaign: campaign name<br>• content: creative or variant<br>• term, id<br>• A shipping label | Layout: a shipping label illustration with fields labeled source, medium, campaign, content; it sticks onto a link icon. 0-14s label draws; 14-28s fields label in order; 28-42s label sticks to link. |
| 4 | 1:49 | 37 | Now the key idea: your medium values decide your channel reports. G A four's default channel group assigns sessions to channels like Organic Search, Paid Social, Email, Affiliates, Referral and Direct, using rules based mainly on source and medium. A medium of email maps to Email. But if one person uses capital E Email, another e dash mail, and a third newsletter, only some of them map correctly, and the rest land in Unassigned. Check Google's current channel definitions and align your taxonomy to them. | Medium decides channel<br>• Default channel group uses source + medium<br>• 'email' maps to Email<br>• Variants land in Unassigned<br>• Align to current definitions | Layout: three medium values (Email, e-mail, newsletter) flow toward channel buckets; only lowercase 'email' lands in Email; the others fall into Unassigned. 0-12s three values flow; 12-25s email lands correctly; 25-37s variants fall into Unassigned. |
| 5 | 2:26 | 35 | So build a taxonomy, a short, enforced list of allowed values. Sources are platforms, like google, meta, instagram, tiktok, linkedin, youtube, snapchat, newsletter, whatsapp, or partner names. Mediums are channel types, like c p c, paid social, organic social, email, s m s, affiliate, influencer, display, referral and q r. Campaigns follow a pattern, like objective, offer, year and month. Content follows format and variant. Rules: lowercase only, underscores not spaces, no personal data, no dates in source or medium. | A taxonomy<br>• Allowed sources<br>• Allowed mediums<br>• Campaign pattern: objective_offer_yyyymm<br>• Content: format_variant<br>• Lowercase, underscores, no PII | Layout: a taxonomy card builds with four sections and a rules footer; example values type in each. 0-9s sources section; 9-18s mediums section; 18-26s campaign and content patterns; 26-35s rules footer. |
| 6 | 3:01 | 36 | When should you not use UTMs? On internal links on your own site. UTMs on internal links start a new session and overwrite the original source, so use event parameters for internal promotions instead. On Google Ads with auto-tagging enabled, where the g c l i d handles attribution and manual UTMs can conflict unless you know how your setup handles both. And on organic search results, which you don't control anyway. The internal-link mistake is the most common and the most damaging. | When not to use UTMs<br>• Internal links: never<br>• Google Ads auto-tagging: careful<br>• Organic search: not yours | Layout: a site map with an internal banner link carrying a UTM; the user's original 'paid social' source gets overwritten with a red flash. 0-12s site map appears; 12-24s internal UTM link clicked; 24-36s original source overwritten red. |
| 7 | 3:37 | 36 | Special cases. QR codes on packaging or event banners: tag them with medium q r and a descriptive source, or scans appear as Direct. WhatsApp and other messaging apps often strip referrers, so tagged links are the only way to attribute them. Influencer links: give each creator a unique content or source value, plus a discount code for cross-checking. And link shorteners and redirects: test that UTMs survive every hop. A single redirect that drops the query string silently breaks everything downstream. | Special cases<br>• QR codes: medium=qr<br>• Messaging apps strip referrers<br>• Unique values per creator + code<br>• Test redirects | Layout: four small tiles: a QR code, a chat app, a creator avatar silhouette, a redirect chain with one link dropping parameters. 0-9s QR tile; 9-18s chat tile; 18-27s creator tile; 27-36s redirect chain drops parameters. |
| 8 | 4:13 | 37 | First example, a simple one. An events company in Dubai prints QR codes on banners, flyers and badges at an expo. Each placement gets medium q r and its own source: expo banner hall three, flyer booth, badge back. All share one campaign name. After the event, one exploration shows which placement drove registrations. And they test every printed code on three phones before printing, because a typo on five thousand flyers can't be fixed, unless the code points to a short link they control. | Example 1: expo QR codes<br>• medium=qr, distinct sources per placement<br>• One campaign<br>• One exploration after the event<br>• Test before printing | Layout: three placement icons (banner, flyer, badge) each with a QR code; registration bars grow per placement; a phone test icon checks each code. 0-12s placements appear; 12-25s bars grow; 25-37s phone test checks. |
| 9 | 4:50 | 36 | Second example, a business case. An education startup in Lahore finds a large Unassigned channel and dozens of mediums like Facebook, F B, social, paid and Paid dash Social. The fix: export source and medium pairs for ninety days, map each messy pair to the new taxonomy, launch a builder sheet with dropdowns, retire freehand links, create a custom channel group to reclassify historical messy values where the rules allow, and review Unassigned every month. Within a quarter, Unassigned shrinks to a sliver. | Example 2: Lahore education startup<br>• Messy mediums, big Unassigned<br>• Map to taxonomy<br>• Builder with dropdowns<br>• Custom channel group for history<br>• Monthly review | Layout: a pie chart with a large gray Unassigned slice; mapping arrows convert messy labels; over three month markers the gray slice shrinks. 0-12s large gray slice; 12-24s mapping arrows; 24-36s slice shrinks over months. |
| 10 | 5:26 | 37 | Watch me build a builder sheet that refuses bad values. On a lists tab, I keep allowed sources and mediums, protected so only the taxonomy owner can edit them. On the main tab, source and medium are dropdowns with data validation, so nobody can type freehand. Campaign and content are free text, but the formula lowercases them and replaces spaces and symbols with underscores. The final column assembles the URL, adding a question mark or an ampersand depending on whether the landing URL already has parameters. | Watch me do it, part 1<br>• Protected lists tab<br>• Dropdowns for source and medium<br>• Formula cleans free text<br>• Handles existing query strings | Layout: a spreadsheet with dropdown arrows on source and medium columns; a formula bar shows the builder formula with parts highlighting; the final URL cell fills. 0-9s lists tab locks; 9-18s dropdowns open; 18-28s formula parts highlight; 28-37s final URL fills. |
| 11 | 6:03 | 35 | Then I hunt leaks in the BigQuery export. The collected traffic source record holds the UTM values seen on each event. My query takes the last thirty days, lowercases manual source and manual medium, counts events per pair, and keeps only mediums that aren't in my approved list. The output is a ranked list of leaks. The top one is a medium called newsletter from an old email template. I fix the template, and add a note to the change log. | Watch me do it, part 2<br>• collected_traffic_source record<br>• Last 30 days, lowercased<br>• Filter to unapproved mediums<br>• Fix at the source, log it | Layout: SQL editor with the leak query; the NOT IN list highlights; results show 'newsletter' at the top; a template icon is edited and a log entry appears. 0-9s query highlights; 9-18s approved list highlights; 18-26s leak results; 26-35s template fixed and logged. |
| 12 | 6:38 | 23 | Common mistakes. Mixed case, like capital I Instagram and lowercase instagram, creating separate rows. Swapping fields, with the campaign in source and the platform in campaign. UTMs on internal banners. Forgetting UTMs on email signatures, social bios and printed QR codes. And no owner for the taxonomy, so it drifts within months. | Common mistakes<br>• Mixed case<br>• Swapped fields<br>• Internal UTMs<br>• Untagged bios, signatures, QR<br>• No owner | Layout: five warning cards drop; the first shows 'Instagram' and 'instagram' as separate report rows. 0-12s card 1 with split rows; 12-23s cards 2 to 5. |
| 13 | 7:01 | 24 | How do you know your UTM discipline is working? Watch two numbers monthly: the share of sessions in Unassigned, and the count of distinct medium values. Both should fall and then stay low. And watch Direct for unexplained jumps after campaigns launch. If Direct spikes when your newsletter goes out, a link somewhere is untagged. | Is it working?<br>• Unassigned share falls<br>• Distinct mediums fall<br>• No Direct spikes after sends | Layout: two small trend lines declining; a Direct line with a spike aligned to a newsletter icon, labeled 'untagged link'. 0-12s two declining lines; 12-24s Direct spike with newsletter icon. |
| 14 | 7:25 | 25 | Recap. UTMs label links so G A four can attribute traffic that would otherwise be Direct or misclassified. Medium and source values decide the default channel grouping. Enforce a lowercase taxonomy with a builder that uses dropdowns. Never put UTMs on internal links. Test redirects and QR codes. And hunt leaks every month, fixing them at the source. | Recap<br>• Label links<br>• Medium decides channel<br>• Enforced taxonomy and builder<br>• No internal UTMs<br>• Test, then hunt leaks monthly | Layout: recap card with shipping-label icon and checkmarks. 0-12s icon; 12-25s checkmarks. |
| 15 | 7:50 | 23 | Try this now. Build a UTM taxonomy and a builder sheet with dropdowns for your organization or a client. Tag five real upcoming links with it, including one QR code if you use print. If you have the BigQuery export, run the leak query, and fix the top leak at its source this week. | Try this now<br>• Taxonomy and builder<br>• Tag five real links<br>• Include a QR code<br>• Run the leak query and fix one leak | Layout: builder sheet with five filled rows; a QR code icon; a leak list with the top row struck through. 0-8s five rows fill; 8-15s QR icon; 15-23s top leak struck through. |

<details><summary>Narration per scene (paste into ElevenLabs)</summary>

Scene 1 — UTM discipline

```text
Here's a quick quiz. A customer taps a link in your WhatsApp broadcast, lands on your site and buys. Where does G A four put that sale? If the link wasn't tagged, very possibly in Direct, or as a meaningless referral. Your best-performing channel just became invisible. In this lecture you'll learn how UTM parameters label links, how your medium values decide G A four's channel reports, how to build and enforce a taxonomy, when not to use UTMs, and how to find messy values automatically.
```

Scene 2 — Why it matters

```text
Why does this matter? Because some sources are detected automatically, like search engines, referrals and Google Ads with auto-tagging. Many aren't: emails, messaging apps, influencer bios, QR codes, many paid social ads. Untagged clicks often land in Direct or Unassigned. Then budget decisions are made on a picture where your email program, your creators or your WhatsApp list look worthless. UTMs are how you make those channels visible.
```

Scene 3 — The parameters

```text
Here are the parameters. U T M source: who sent the traffic, like instagram, newsletter or partner blog. U T M medium: the type of channel, like paid social, email, affiliate or c p c. U T M campaign: the campaign name. U T M content: which creative or link variant. U T M term: the keyword, mainly for paid search. And U T M id, a campaign id useful for cost data imports. Think of a shipping label: source is the sender, medium is the delivery service, campaign is the order number, content is which box.
```

Scene 4 — Medium decides channel

```text
Now the key idea: your medium values decide your channel reports. G A four's default channel group assigns sessions to channels like Organic Search, Paid Social, Email, Affiliates, Referral and Direct, using rules based mainly on source and medium. A medium of email maps to Email. But if one person uses capital E Email, another e dash mail, and a third newsletter, only some of them map correctly, and the rest land in Unassigned. Check Google's current channel definitions and align your taxonomy to them.
```

Scene 5 — A taxonomy

```text
So build a taxonomy, a short, enforced list of allowed values. Sources are platforms, like google, meta, instagram, tiktok, linkedin, youtube, snapchat, newsletter, whatsapp, or partner names. Mediums are channel types, like c p c, paid social, organic social, email, s m s, affiliate, influencer, display, referral and q r. Campaigns follow a pattern, like objective, offer, year and month. Content follows format and variant. Rules: lowercase only, underscores not spaces, no personal data, no dates in source or medium.
```

Scene 6 — When not to use UTMs

```text
When should you not use UTMs? On internal links on your own site. UTMs on internal links start a new session and overwrite the original source, so use event parameters for internal promotions instead. On Google Ads with auto-tagging enabled, where the g c l i d handles attribution and manual UTMs can conflict unless you know how your setup handles both. And on organic search results, which you don't control anyway. The internal-link mistake is the most common and the most damaging.
```

Scene 7 — Special cases

```text
Special cases. QR codes on packaging or event banners: tag them with medium q r and a descriptive source, or scans appear as Direct. WhatsApp and other messaging apps often strip referrers, so tagged links are the only way to attribute them. Influencer links: give each creator a unique content or source value, plus a discount code for cross-checking. And link shorteners and redirects: test that UTMs survive every hop. A single redirect that drops the query string silently breaks everything downstream.
```

Scene 8 — Example 1: expo QR codes

```text
First example, a simple one. An events company in Dubai prints QR codes on banners, flyers and badges at an expo. Each placement gets medium q r and its own source: expo banner hall three, flyer booth, badge back. All share one campaign name. After the event, one exploration shows which placement drove registrations. And they test every printed code on three phones before printing, because a typo on five thousand flyers can't be fixed, unless the code points to a short link they control.
```

Scene 9 — Example 2: Lahore education startup

```text
Second example, a business case. An education startup in Lahore finds a large Unassigned channel and dozens of mediums like Facebook, F B, social, paid and Paid dash Social. The fix: export source and medium pairs for ninety days, map each messy pair to the new taxonomy, launch a builder sheet with dropdowns, retire freehand links, create a custom channel group to reclassify historical messy values where the rules allow, and review Unassigned every month. Within a quarter, Unassigned shrinks to a sliver.
```

Scene 10 — Watch me do it, part 1

```text
Watch me build a builder sheet that refuses bad values. On a lists tab, I keep allowed sources and mediums, protected so only the taxonomy owner can edit them. On the main tab, source and medium are dropdowns with data validation, so nobody can type freehand. Campaign and content are free text, but the formula lowercases them and replaces spaces and symbols with underscores. The final column assembles the URL, adding a question mark or an ampersand depending on whether the landing URL already has parameters.
```

Scene 11 — Watch me do it, part 2

```text
Then I hunt leaks in the BigQuery export. The collected traffic source record holds the UTM values seen on each event. My query takes the last thirty days, lowercases manual source and manual medium, counts events per pair, and keeps only mediums that aren't in my approved list. The output is a ranked list of leaks. The top one is a medium called newsletter from an old email template. I fix the template, and add a note to the change log.
```

Scene 12 — Common mistakes

```text
Common mistakes. Mixed case, like capital I Instagram and lowercase instagram, creating separate rows. Swapping fields, with the campaign in source and the platform in campaign. UTMs on internal banners. Forgetting UTMs on email signatures, social bios and printed QR codes. And no owner for the taxonomy, so it drifts within months.
```

Scene 13 — Is it working?

```text
How do you know your UTM discipline is working? Watch two numbers monthly: the share of sessions in Unassigned, and the count of distinct medium values. Both should fall and then stay low. And watch Direct for unexplained jumps after campaigns launch. If Direct spikes when your newsletter goes out, a link somewhere is untagged.
```

Scene 14 — Recap

```text
Recap. UTMs label links so G A four can attribute traffic that would otherwise be Direct or misclassified. Medium and source values decide the default channel grouping. Enforce a lowercase taxonomy with a builder that uses dropdowns. Never put UTMs on internal links. Test redirects and QR codes. And hunt leaks every month, fixing them at the source.
```

Scene 15 — Try this now

```text
Try this now. Build a UTM taxonomy and a builder sheet with dropdowns for your organization or a client. Tag five real upcoming links with it, including one QR code if you use print. If you have the BigQuery export, run the leak query, and fix the top leak at its source this week.
```

</details>

### 11. Consent and consent mode concepts

Lesson `consent-mode-concepts` · module “Campaign tracking, consent and privacy” · target 9 min · 15 scenes · 1255 words · planned 9:06 (words ≈ 9:06) · status: **to produce**

| Term | Say |
|---|---|
| GA4 | G A four |
| UTM | U T M |
| UTMs | U T Ms |
| CMP | C M P |
| GDPR | G D P R |
| PII | P I I |
| QR | Q R |
| PECR | peck-er |
| EEA | E E A |
| ePrivacy | e-privacy |

| # | Time | Sec | Narration | On screen | Visual |
|---|---|---|---|---|---|
| 1 | 0:00 | 39 | Here's a scenario that happens every week somewhere. A UK retailer launches a new consent banner on Monday. By Wednesday, G A four users are down sharply, and the marketing director asks what broke. Nothing broke. The site started respecting people's choices. In this lecture you'll learn why consent is a legal requirement, what a consent management platform does, how Google's consent mode works with its four signals, the difference between basic and advanced mode, and exactly what consent does to your reports, your modeling and your BigQuery export. | Consent mode concepts<br>• Consent is a legal requirement<br>• What a CMP does<br>• Consent mode's four signals<br>• Basic versus advanced<br>• What consent does to your data | Layout: a users line chart drops on a Wednesday marker; a question mark 'what broke?' is replaced by a consent banner icon with 'respecting choices'. 0-10s line drops; 10-20s question mark; 20-29s consent banner replaces it; 29-39s title builds. |
| 2 | 0:39 | 38 | Why does this matter? Because many privacy regimes, including the E U's G D P R and e-privacy rules and the U K's G D P R and P E C R, require a lawful basis for processing and, in many cases, prior consent before non-essential cookies. Analytics and advertising cookies are generally treated as non-essential there. Requirements differ by jurisdiction, so this lecture explains concepts, not legal advice. Involve qualified counsel for your situation. But every analyst must understand how consent shapes the data they report. | Why it matters<br>• GDPR, ePrivacy, UK GDPR, PECR<br>• Prior consent for non-essential cookies<br>• Varies by jurisdiction<br>• Concepts, not legal advice | Layout: a map with the EU and UK highlighted and a consent icon over each; a 'not legal advice' badge sits in the corner. 0-10s map appears; 10-19s regions highlight; 19-28s consent icons; 28-38s badge. |
| 3 | 1:17 | 39 | First, the consent management platform, or C M P. It displays the banner, records choices, and exposes them to tags. Good practice includes accept and reject options that are equally easy where the law requires it, granular categories like analytics, advertising and functional, a persistent way to change choices later, and records of consent for accountability. If you advertise with Google in the European Economic Area, Google's E U user consent policy requires you to obtain and pass consent signals, and certified C M Ps integrate with Google's systems. | The CMP<br>• Banner and choices<br>• Equal accept and reject where required<br>• Granular categories<br>• Change later, keep records<br>• Google EU user consent policy | Layout: a consent banner mock with equal-weight accept and reject buttons, category toggles, and a small persistent 'privacy' icon in the corner. 0-10s banner appears; 10-20s equal buttons; 20-29s category toggles; 29-39s persistent icon. |
| 4 | 1:56 | 39 | Now consent mode. It's Google's framework for tags to adjust behavior based on consent. Think of it like traffic lights for data. The C M P sets the lights, and Google tags obey them. There are four signals. Analytics storage: may analytics cookies be stored? Ad storage: may advertising cookies be stored? Ad user data: may user data be sent to Google for advertising? And ad personalization: may data be used for personalized ads, like remarketing? A default is set before any tag fires, then updated when the user chooses. | Consent mode signals<br>• analytics_storage<br>• ad_storage<br>• ad_user_data<br>• ad_personalization<br>• Default first, then update | Layout: four traffic lights in a row labeled with the four signals; they start red (default) and some turn green after a user click on a banner. 0-13s four lights appear red; 13-26s user clicks banner; 26-39s some lights turn green. |
| 5 | 2:35 | 38 | Basic versus advanced. In basic consent mode, Google tags are blocked entirely until the user consents. If they decline, nothing is sent. In advanced mode, tags load with consent denied by default and send cookieless pings, with no identifiers stored, when consent is denied, which lets Google model some of the missing data if you're eligible. Advanced can recover more insight. But some organizations prefer basic, because it sends nothing without consent. It's a risk and policy decision, not just a technical one. Document who decided and why. | Basic versus advanced<br>• Basic: nothing until consent<br>• Advanced: cookieless pings when denied<br>• Advanced enables modeling if eligible<br>• A documented policy decision | Layout: split screen: basic side shows a closed gate with no data passing when denied; advanced side shows small unlabeled pings passing through a filter. 0-13s basic gate closed; 13-25s advanced pings pass; 25-38s decision document stamps. |
| 6 | 3:13 | 38 | Here's what consent does to your data, and this is where most reporting confusion comes from. Behavioral modeling only applies when a property is eligible. Google's documented thresholds include at least a thousand events a day with analytics storage denied for at least seven days, and at least a thousand daily users with it granted for seven of the previous twenty-eight days, with advanced consent mode on all pages. Many small sites never qualify, so their reports show observed data only. That's not a bug. It's the rule. | Modeling eligibility<br>• 1,000+ denied events/day for 7+ days<br>• 1,000+ granted daily users on 7 of 28 days<br>• Advanced mode on all pages<br>• Many small sites never qualify | Layout: a threshold meter with two gauges; a small site's needles fall short and a label says 'observed data only'; a large site's needles pass and a 'modeling' badge appears. 0-10s two gauges appear; 10-19s small site falls short; 19-28s large site passes; 28-38s badges appear. |
| 7 | 3:51 | 40 | And the BigQuery export contains no modeled data at all. In advanced mode, the consent-denied cookieless pings do appear in the export, but without a user pseudo id or session identifiers, and with a privacy info field showing analytics storage as No, capital N. So user and session counts from the export will be lower than the blended numbers in reports. And since the June twenty twenty-six data controls change, Ads identifier collection is governed by consent mode's ad storage, while Google signals affects only behavioral reporting in G A four. | Consent and the export<br>• No modeled data in BigQuery<br>• Denied pings: no user or session IDs<br>• privacy_info.analytics_storage = 'No'<br>• Ads IDs governed by ad_storage (2026) | Layout: a BigQuery-style table with some rows showing empty user_pseudo_id cells and 'No' in a consent column; a label 'no modeled rows' sits above. 0-10s table appears; 10-20s empty ID cells highlight; 20-30s consent column highlights; 30-40s label appears. |
| 8 | 4:31 | 39 | First example, a simple one. A UK online retailer runs a consent audit. In a clean browser profile, with the network tab open, and without touching the banner, they record every request to analytics and ad domains. Their ad pixel fires on page load, before the banner even appears. They set default consent to denied for the relevant regions, link each non-Google tag to the right consent category in the tag manager, and retest: reject all, then accept all. Then they document the setup and schedule a quarterly re-audit. | Example 1: a UK consent audit<br>• Clean profile, network tab<br>• Pixel firing before the banner<br>• Denied defaults, consent categories<br>• Retest reject and accept<br>• Quarterly re-audit | Layout: a browser network panel lists requests; one ad-domain request before the banner highlights red; after the fix, rejecting shows no ad requests. 0-10s network panel; 10-20s early request red; 20-29s fix applied; 29-39s clean reject test. |
| 9 | 5:10 | 40 | Second example, a business case. A Karachi fashion brand starts shipping to the UK, where it previously had no banner at all. It adds a C M P with region-specific defaults, denied for the UK and the European Economic Area. It chooses advanced mode after documenting the decision with legal advice. And it tells stakeholders in advance that UK observed users will fall, and that modeling may not apply until volumes meet the thresholds. The weekly report gains a consent granted share line, so nobody mistakes a consent effect for a demand drop. | Example 2: expanding to the UK<br>• Region-specific defaults<br>• Documented choice of advanced mode<br>• Stakeholders warned in advance<br>• 'Consent granted share' in reports | Layout: a map arrow from Pakistan to the UK; a CMP shield appears over the UK; a weekly report card gains a new line 'consent granted share'. 0-13s map arrow; 13-27s CMP shield; 27-40s report card gains line. |
| 10 | 5:50 | 42 | Watch me read the code, even if your C M P writes it. Before any Google tag loads, the page defines the data layer and the gtag function. Then gtag consent default: all four signals denied, for a list of opt-in regions, with wait for update set to five hundred milliseconds, so the C M P has time to load a returning visitor's choice. A second default applies elsewhere, according to your legal advice. When the visitor chooses, the C M P calls gtag consent update, for example granting analytics storage but denying the three advertising signals. | Watch me do it, part 1<br>• Default before any Google tag<br>• Four signals denied in opt-in regions<br>• wait_for_update: 500 ms<br>• Update on the user's choice | Layout: code editor with the consent default block; lines highlight in order; then an update call appears as a banner button is clicked. 0-10s data layer and gtag lines; 10-21s default block highlights; 21-32s region and wait lines highlight; 32-42s update call appears. |
| 11 | 6:32 | 33 | Then I quantify the effect in the export. I group September's events by privacy info analytics storage and count them. I see three values: Yes, No, and null. Null usually means consent wasn't set, which is itself a finding: maybe some pages load the tag without the consent default. Then I verify with Tag Assistant on those pages. That query turns a vague we lost some data into a precise share you can report every week. | Watch me do it, part 2<br>• Group by consent state<br>• Yes, No, null<br>• Null = consent not set somewhere<br>• Verify pages with Tag Assistant | Layout: a result table with three rows (Yes, No, null); the null row highlights amber and links to a page list being checked. 0-8s query runs; 8-16s three rows appear; 16-25s null row amber; 25-33s page list check. |
| 12 | 7:05 | 43 | Remember that consent mode governs Google tags only. Every other tag, like other ad pixels, heatmap tools and chat widgets, must also respect consent, usually through your tag manager's consent settings or the C M P's blocking features. Audit third-party tags regularly; old pixels from past campaigns are a common compliance leak. And regionally: opt-in is the norm in the E E A and U K, several U S states focus on opt-out rights and some honor Global Privacy Control, and the U A E and Saudi Arabia have personal data protection laws. Check current rules for your markets. | Beyond Google tags and regions<br>• Every tag must respect consent<br>• Audit old pixels<br>• EEA/UK opt-in; US opt-out models<br>• UAE and KSA data protection laws | Layout: a tag list where non-Google tags get consent category badges; a world map shows region labels 'opt-in', 'opt-out', 'check local law'. 0-14s tag list badges; 14-29s old pixel removed; 29-43s world map labels. |
| 13 | 7:48 | 22 | Common mistakes. Tags firing before the banner loads. A reject option hidden on a second screen where the law requires parity. Treating consent mode as a substitute for a C M P. Forgetting non-Google tags. Never re-testing after adding new tags. And reporting a consent-driven drop as a demand problem. | Common mistakes<br>• Tags before the banner<br>• Hidden reject<br>• Consent mode without a CMP<br>• Non-Google tags ignored<br>• No re-tests<br>• Misreading consent drops | Layout: six warning cards in a grid. 0-11s cards 1 to 3; 11-22s cards 4 to 6. |
| 14 | 8:10 | 31 | Recap. Consent obligations depend on jurisdiction, so involve counsel. A C M P records choices and exposes them. Consent mode's four signals tell Google tags what's allowed. Basic mode blocks tags until consent, advanced mode sends cookieless pings and enables modeling if eligible. Modeling has thresholds, and the BigQuery export has no modeled data. Every non-Google tag must respect consent too. And report the consent granted share, so nobody misreads the numbers. | Recap<br>• Counsel for decisions<br>• CMP plus consent mode<br>• Four signals<br>• Basic vs advanced<br>• Thresholds; export has no modeling<br>• Report consent share | Layout: recap card with traffic lights icon and checkmarks. 0-16s traffic lights; 16-31s checkmarks. |
| 15 | 8:41 | 25 | Try this now. Run the clean-browser consent audit from the lesson on a site you manage, and list every request that fires before a consent choice. If you have the BigQuery export, run the consent-state query for last month and calculate the share of events with consent denied or unset. Share both with your team this week. | Try this now<br>• Clean-browser audit<br>• List pre-consent requests<br>• Consent-state query<br>• Share the results | Layout: a checklist with the audit steps; a small pie chart of Yes, No and null; a share icon. 0-8s audit checklist; 8-17s pie chart; 17-25s share icon. |

<details><summary>Narration per scene (paste into ElevenLabs)</summary>

Scene 1 — Consent mode concepts

```text
Here's a scenario that happens every week somewhere. A UK retailer launches a new consent banner on Monday. By Wednesday, G A four users are down sharply, and the marketing director asks what broke. Nothing broke. The site started respecting people's choices. In this lecture you'll learn why consent is a legal requirement, what a consent management platform does, how Google's consent mode works with its four signals, the difference between basic and advanced mode, and exactly what consent does to your reports, your modeling and your BigQuery export.
```

Scene 2 — Why it matters

```text
Why does this matter? Because many privacy regimes, including the E U's G D P R and e-privacy rules and the U K's G D P R and P E C R, require a lawful basis for processing and, in many cases, prior consent before non-essential cookies. Analytics and advertising cookies are generally treated as non-essential there. Requirements differ by jurisdiction, so this lecture explains concepts, not legal advice. Involve qualified counsel for your situation. But every analyst must understand how consent shapes the data they report.
```

Scene 3 — The CMP

```text
First, the consent management platform, or C M P. It displays the banner, records choices, and exposes them to tags. Good practice includes accept and reject options that are equally easy where the law requires it, granular categories like analytics, advertising and functional, a persistent way to change choices later, and records of consent for accountability. If you advertise with Google in the European Economic Area, Google's E U user consent policy requires you to obtain and pass consent signals, and certified C M Ps integrate with Google's systems.
```

Scene 4 — Consent mode signals

```text
Now consent mode. It's Google's framework for tags to adjust behavior based on consent. Think of it like traffic lights for data. The C M P sets the lights, and Google tags obey them. There are four signals. Analytics storage: may analytics cookies be stored? Ad storage: may advertising cookies be stored? Ad user data: may user data be sent to Google for advertising? And ad personalization: may data be used for personalized ads, like remarketing? A default is set before any tag fires, then updated when the user chooses.
```

Scene 5 — Basic versus advanced

```text
Basic versus advanced. In basic consent mode, Google tags are blocked entirely until the user consents. If they decline, nothing is sent. In advanced mode, tags load with consent denied by default and send cookieless pings, with no identifiers stored, when consent is denied, which lets Google model some of the missing data if you're eligible. Advanced can recover more insight. But some organizations prefer basic, because it sends nothing without consent. It's a risk and policy decision, not just a technical one. Document who decided and why.
```

Scene 6 — Modeling eligibility

```text
Here's what consent does to your data, and this is where most reporting confusion comes from. Behavioral modeling only applies when a property is eligible. Google's documented thresholds include at least a thousand events a day with analytics storage denied for at least seven days, and at least a thousand daily users with it granted for seven of the previous twenty-eight days, with advanced consent mode on all pages. Many small sites never qualify, so their reports show observed data only. That's not a bug. It's the rule.
```

Scene 7 — Consent and the export

```text
And the BigQuery export contains no modeled data at all. In advanced mode, the consent-denied cookieless pings do appear in the export, but without a user pseudo id or session identifiers, and with a privacy info field showing analytics storage as No, capital N. So user and session counts from the export will be lower than the blended numbers in reports. And since the June twenty twenty-six data controls change, Ads identifier collection is governed by consent mode's ad storage, while Google signals affects only behavioral reporting in G A four.
```

Scene 8 — Example 1: a UK consent audit

```text
First example, a simple one. A UK online retailer runs a consent audit. In a clean browser profile, with the network tab open, and without touching the banner, they record every request to analytics and ad domains. Their ad pixel fires on page load, before the banner even appears. They set default consent to denied for the relevant regions, link each non-Google tag to the right consent category in the tag manager, and retest: reject all, then accept all. Then they document the setup and schedule a quarterly re-audit.
```

Scene 9 — Example 2: expanding to the UK

```text
Second example, a business case. A Karachi fashion brand starts shipping to the UK, where it previously had no banner at all. It adds a C M P with region-specific defaults, denied for the UK and the European Economic Area. It chooses advanced mode after documenting the decision with legal advice. And it tells stakeholders in advance that UK observed users will fall, and that modeling may not apply until volumes meet the thresholds. The weekly report gains a consent granted share line, so nobody mistakes a consent effect for a demand drop.
```

Scene 10 — Watch me do it, part 1

```text
Watch me read the code, even if your C M P writes it. Before any Google tag loads, the page defines the data layer and the gtag function. Then gtag consent default: all four signals denied, for a list of opt-in regions, with wait for update set to five hundred milliseconds, so the C M P has time to load a returning visitor's choice. A second default applies elsewhere, according to your legal advice. When the visitor chooses, the C M P calls gtag consent update, for example granting analytics storage but denying the three advertising signals.
```

Scene 11 — Watch me do it, part 2

```text
Then I quantify the effect in the export. I group September's events by privacy info analytics storage and count them. I see three values: Yes, No, and null. Null usually means consent wasn't set, which is itself a finding: maybe some pages load the tag without the consent default. Then I verify with Tag Assistant on those pages. That query turns a vague we lost some data into a precise share you can report every week.
```

Scene 12 — Beyond Google tags and regions

```text
Remember that consent mode governs Google tags only. Every other tag, like other ad pixels, heatmap tools and chat widgets, must also respect consent, usually through your tag manager's consent settings or the C M P's blocking features. Audit third-party tags regularly; old pixels from past campaigns are a common compliance leak. And regionally: opt-in is the norm in the E E A and U K, several U S states focus on opt-out rights and some honor Global Privacy Control, and the U A E and Saudi Arabia have personal data protection laws. Check current rules for your markets.
```

Scene 13 — Common mistakes

```text
Common mistakes. Tags firing before the banner loads. A reject option hidden on a second screen where the law requires parity. Treating consent mode as a substitute for a C M P. Forgetting non-Google tags. Never re-testing after adding new tags. And reporting a consent-driven drop as a demand problem.
```

Scene 14 — Recap

```text
Recap. Consent obligations depend on jurisdiction, so involve counsel. A C M P records choices and exposes them. Consent mode's four signals tell Google tags what's allowed. Basic mode blocks tags until consent, advanced mode sends cookieless pings and enables modeling if eligible. Modeling has thresholds, and the BigQuery export has no modeled data. Every non-Google tag must respect consent too. And report the consent granted share, so nobody misreads the numbers.
```

Scene 15 — Try this now

```text
Try this now. Run the clean-browser consent audit from the lesson on a site you manage, and list every request that fires before a consent choice. If you have the BigQuery export, run the consent-state query for last month and calculate the share of events with consent denied or unset. Share both with your team this week.
```

</details>

### 12. Privacy by design and data governance

Lesson `privacy-by-design` · module “Campaign tracking, consent and privacy” · target 8 min · 15 scenes · 1152 words · planned 8:21 (words ≈ 8:21) · status: **to produce**

| Term | Say |
|---|---|
| GA4 | G A four |
| UTM | U T M |
| UTMs | U T Ms |
| CMP | C M P |
| GDPR | G D P R |
| PII | P I I |
| QR | Q R |
| CNIC | C N I C |
| PDPL | P D P L |
| GET | get |
| POST | post |
| IP | I P |

| # | Time | Sec | Narration | On screen | Visual |
|---|---|---|---|---|---|
| 1 | 0:00 | 40 | Here's an uncomfortable truth. Many G A four properties contain personal data that nobody meant to collect. An email address in a thank-you page URL. A customer's name in a page title. A phone number in a search query. Nobody decided to collect it. It leaked. In this lecture you'll learn privacy by design for analytics: the principles, how personal data leaks and how to stop it, the settings that support privacy, the regulatory landscape at a high level, and a monthly scan that finds leaks before a regulator or client does. | Privacy by design<br>• Principles for analytics<br>• How PII leaks<br>• Settings that help<br>• The regulatory landscape<br>• A monthly PII scan | Layout: a URL bar shows a thank-you page with an email address in the query string; the email blurs into a redaction block. 0-10s URL bar appears; 10-20s email highlights red; 20-30s redaction block covers it; 30-40s title builds. |
| 2 | 0:40 | 37 | Why build privacy in rather than bolt it on? Because teams that treat privacy as a legal checkbox discover problems at the worst time: during a regulator query, a client audit or a data breach. Google's terms prohibit sending personally identifiable information to G A four at all. And privacy failures destroy trust fast. Privacy by design means collecting the minimum necessary, protecting it, and respecting user rights from the start. It's cheaper, safer and, frankly, better analytics, because clean data is easier to use. | Why by design<br>• Problems surface at the worst time<br>• Google's terms: no PII in GA4<br>• Trust is fragile<br>• Minimum data, protected, rights respected | Layout: a timeline where a 'bolt-on' approach ends with an alarm icon labeled 'audit', while a 'by design' approach runs smoothly with a shield. 0-18s bolt-on timeline with alarm; 18-37s by-design timeline with shield. |
| 3 | 1:17 | 38 | Here's the analogy. Think of your analytics like a restaurant kitchen with a health inspector. You don't clean only when the inspector calls. You design the kitchen so raw chicken never touches the salad station. Privacy principles are the kitchen layout. Purpose limitation: collect for defined purposes. Data minimization: prefer aggregated or banded values. Transparency: your privacy notice names your tools and purposes. Storage limitation: retention matched to real needs. Security: least privilege and secure exports. And user rights: be able to handle deletion and access requests. | The kitchen layout<br>• Purpose limitation<br>• Data minimization<br>• Transparency<br>• Storage limitation<br>• Security<br>• User rights | Layout: a kitchen floor plan with separated stations labeled with the six principles; an inspector icon walks through with a clipboard. 0-13s floor plan draws; 13-25s stations label; 25-38s inspector walks through. |
| 4 | 1:55 | 34 | How does personal data leak? Rarely on purpose. Through URLs, like a thank-you page with the email in the query string, or search results pages with names in the query. Through page titles, like welcome back followed by a name. Through form values captured by overly broad click tracking. And through custom parameters added by well-meaning developers. Regional identifiers leak too, like Pakistan's C N I C or Saudi Arabia's iqama numbers in a form that uses GET. | How PII leaks<br>• URLs and query strings<br>• Page titles<br>• Broad click tracking<br>• Custom parameters<br>• Regional IDs via GET forms | Layout: four leak paths drawn as pipes into a GA4 tank: URL, title, click capture, parameter; a drip labeled with an ID pattern falls from the URL pipe. 0-8s URL pipe; 8-17s title pipe; 17-26s click and parameter pipes; 26-34s drip falls. |
| 5 | 2:29 | 37 | The defenses, in order. Configure forms to use POST rather than GET, so values don't appear in URLs. Use G A four's data redaction settings for email addresses and specified query parameters in web streams. Strip sensitive query parameters in the tag manager before sending page location, as a second line of defense. Add a personal data check to your QA script. And if personal data does reach G A four, use the data deletion request feature, delete any copies, and fix the source. | Defenses<br>• POST, not GET<br>• GA4 data redaction<br>• Strip in the tag manager<br>• PII check in QA<br>• Deletion requests and source fixes | Layout: a layered shield with five layers labeled with each defense; a leak arrow is stopped at the first or second layer. 0-18s layers build; 18-37s leak arrow stopped. |
| 6 | 3:06 | 35 | Settings that support privacy. Data retention, set deliberately rather than defaulting to maximum. Google signals, which since June twenty twenty-six only affects behavioral reporting, reviewed alongside your privacy notice. Granular location and device data collection, which can be limited per region. Ads personalization controls. And user deletion for individuals when needed. Also note that G A four doesn't log or store IP addresses for reporting, although IPs are still transmitted in requests and used to derive location at collection time. | Supporting settings<br>• Deliberate retention<br>• Google signals (reporting only, 2026)<br>• Granular location/device limits<br>• Ads personalization controls<br>• User deletion | Layout: a settings panel with toggles for each item flipping into deliberate positions; a small IP icon with a note 'not stored for reporting'. 0-18s toggles flip; 18-35s IP note appears. |
| 7 | 3:41 | 48 | The regulatory landscape, at a high level. In the E U and U K, G D P R and U K G D P R require a lawful basis, consent for non-essential cookies under e-privacy rules and P E C R, data subject rights and transfer rules. In the U S, state laws like California's, as amended, focus on opt-out rights and sensitive data, and more states keep passing laws. The U A E has a federal personal data protection law, with separate regimes in some financial free zones. Saudi Arabia has its Personal Data Protection Law. Pakistan's dedicated legislation has been in development. Check the current status with counsel. | Regulatory landscape<br>• EU/UK: GDPR, ePrivacy/PECR<br>• US: state laws, opt-out focus<br>• UAE: federal law + free-zone regimes<br>• KSA: PDPL<br>• Pakistan: legislation in development | Layout: a world map with region badges appearing in sequence and a 'check with counsel' stamp. 0-12s EU/UK badge; 12-24s US badge; 24-36s UAE and KSA badges; 36-48s Pakistan badge and stamp. |
| 8 | 4:29 | 37 | First example, a simple one. A property portal in Dubai lets users search by owner name, so URLs look like search, q equals mohammed ali villa. The team adds q to G A four's query parameter redaction list, or strips it in the tag manager, and sends a separate search event with a search category parameter, like villa or apartment, instead of the raw term. They still learn what categories people search for, without storing names. And search pages join the monthly personal data audit. | Example 1: Dubai property search<br>• Names in ?q=<br>• Redact or strip q<br>• search_category instead<br>• Categories without names | Layout: a search URL with a name in the query blurs out; a clean event chip 'search' with 'search_category: villa' appears. 0-12s URL with name; 12-25s blur applied; 25-37s clean event chip appears. |
| 9 | 5:06 | 37 | Second example, a business case. A UK charity finds donor emails in page location, because its donation form used GET. The team switches the form to POST, adds email and donor to the redaction list, strips them in the tag manager as a second defense, submits a data deletion request for the affected period, deletes matching rows from its BigQuery dataset, and adds the scan to its monthly routine. The incident report is short, because they can show exactly what happened and what was fixed. | Example 2: UK charity donations<br>• Emails in URLs via GET<br>• POST, redaction, GTM stripping<br>• Deletion in GA4 and BigQuery<br>• Monthly scan | Layout: a donation form switches its method label from GET to POST; redaction and strip icons stack; deletion icons hit GA4 and a BigQuery table. 0-12s form method switches; 12-25s redaction and strip icons; 25-37s deletion icons. |
| 10 | 5:43 | 40 | Watch me run the monthly scan. In BigQuery, I pull page location, page referrer and page title from page view events for the last thirty days. Then I search the combined text with a regular expression for anything that looks like an email address, or a query parameter named email, phone, name, C N I C or iqama. I group and count the matches, and sort by hits. The result is a list of exact URLs and titles to investigate. On a clean property, it returns nothing, and that's the goal. | Watch me do it, part 1<br>• Page location, referrer, title<br>• Last 30 days<br>• Regex: emails and sensitive parameters<br>• Ranked list of leaks | Layout: SQL editor with the scan; the regex highlights; result rows show a few masked example URLs sorted by hits. 0-13s columns extract; 13-27s regex highlights; 27-40s results sort. |
| 11 | 6:23 | 33 | One more privacy habit, because this course is about AI too. Before pasting analytics exports into an AI assistant, remove identifiers you don't need, aggregate where possible, and use tools and plans your organization has approved for that data, checking retention and training settings. A table of sessions and key events by channel and week is almost always enough for AI-assisted analysis. You rarely need user-level rows, and you should almost never paste them into a chat. | Privacy with AI tools<br>• Remove unneeded identifiers<br>• Aggregate first<br>• Approved tools and plans<br>• Check retention and training settings | Layout: a user-level table collapses into an aggregated weekly table before flowing into a chat window with an 'approved tool' badge. 0-11s user-level table; 11-22s collapses to aggregate; 22-33s flows into approved chat. |
| 12 | 6:56 | 18 | Common mistakes. Assuming anonymous analytics means no privacy obligations. Exporting user-level data into spreadsheets shared by email. Keeping ex-employees and ex-agencies on the property. Never reading the privacy notice to check it matches reality. And pasting raw exports into unapproved AI tools. | Common mistakes<br>• 'Anonymous' means no obligations<br>• User-level spreadsheets by email<br>• Stale access<br>• Outdated privacy notice<br>• Raw exports into AI tools | Layout: five warning cards drop in. 0-9s cards 1 to 3; 9-18s cards 4 and 5. |
| 13 | 7:14 | 23 | How do you measure privacy health? Monthly: the PII scan returns nothing. Quarterly: access is reviewed, the privacy notice matches the tools actually running, and retention has a documented reason. And for every incident, the time from discovery to fix and deletion. Those are the numbers that let you answer an auditor calmly. | Privacy health<br>• Monthly: clean PII scan<br>• Quarterly: access, notice, retention<br>• Incident: time to fix and delete | Layout: a small dashboard with a green 'scan clean' tile, a quarterly checklist tile, and an incident timer tile. 0-8s scan tile; 8-15s quarterly tile; 15-23s incident timer. |
| 14 | 7:37 | 24 | Recap. Collect the minimum data needed for defined purposes. Personal data usually leaks through URLs, titles and parameters, so use POST, redaction and tag manager stripping. Set retention deliberately and review settings like Google signals. Know the regimes in your markets and review them with counsel. Scan monthly. And keep user-level data out of AI chats. | Recap<br>• Minimum data<br>• Stop leaks at the source<br>• Deliberate settings<br>• Know your regimes<br>• Scan monthly<br>• Aggregate before AI | Layout: recap card with the layered shield and checkmarks. 0-12s shield; 12-24s checkmarks. |
| 15 | 8:01 | 20 | Try this now. Audit one G A four property for personal data. Review page paths with query strings, page titles and custom parameters, or run the BigQuery scan from the lesson. Document every leak, its source and the fix, and add the scan to your monthly routine. | Try this now<br>• Audit for PII<br>• Or run the scan<br>• Document leak, source, fix<br>• Make it monthly | Layout: an audit sheet with three columns (leak, source, fix) filling; a calendar icon with a monthly repeat. 0-10s sheet fills; 10-20s calendar repeat. |

<details><summary>Narration per scene (paste into ElevenLabs)</summary>

Scene 1 — Privacy by design

```text
Here's an uncomfortable truth. Many G A four properties contain personal data that nobody meant to collect. An email address in a thank-you page URL. A customer's name in a page title. A phone number in a search query. Nobody decided to collect it. It leaked. In this lecture you'll learn privacy by design for analytics: the principles, how personal data leaks and how to stop it, the settings that support privacy, the regulatory landscape at a high level, and a monthly scan that finds leaks before a regulator or client does.
```

Scene 2 — Why by design

```text
Why build privacy in rather than bolt it on? Because teams that treat privacy as a legal checkbox discover problems at the worst time: during a regulator query, a client audit or a data breach. Google's terms prohibit sending personally identifiable information to G A four at all. And privacy failures destroy trust fast. Privacy by design means collecting the minimum necessary, protecting it, and respecting user rights from the start. It's cheaper, safer and, frankly, better analytics, because clean data is easier to use.
```

Scene 3 — The kitchen layout

```text
Here's the analogy. Think of your analytics like a restaurant kitchen with a health inspector. You don't clean only when the inspector calls. You design the kitchen so raw chicken never touches the salad station. Privacy principles are the kitchen layout. Purpose limitation: collect for defined purposes. Data minimization: prefer aggregated or banded values. Transparency: your privacy notice names your tools and purposes. Storage limitation: retention matched to real needs. Security: least privilege and secure exports. And user rights: be able to handle deletion and access requests.
```

Scene 4 — How PII leaks

```text
How does personal data leak? Rarely on purpose. Through URLs, like a thank-you page with the email in the query string, or search results pages with names in the query. Through page titles, like welcome back followed by a name. Through form values captured by overly broad click tracking. And through custom parameters added by well-meaning developers. Regional identifiers leak too, like Pakistan's C N I C or Saudi Arabia's iqama numbers in a form that uses GET.
```

Scene 5 — Defenses

```text
The defenses, in order. Configure forms to use POST rather than GET, so values don't appear in URLs. Use G A four's data redaction settings for email addresses and specified query parameters in web streams. Strip sensitive query parameters in the tag manager before sending page location, as a second line of defense. Add a personal data check to your QA script. And if personal data does reach G A four, use the data deletion request feature, delete any copies, and fix the source.
```

Scene 6 — Supporting settings

```text
Settings that support privacy. Data retention, set deliberately rather than defaulting to maximum. Google signals, which since June twenty twenty-six only affects behavioral reporting, reviewed alongside your privacy notice. Granular location and device data collection, which can be limited per region. Ads personalization controls. And user deletion for individuals when needed. Also note that G A four doesn't log or store IP addresses for reporting, although IPs are still transmitted in requests and used to derive location at collection time.
```

Scene 7 — Regulatory landscape

```text
The regulatory landscape, at a high level. In the E U and U K, G D P R and U K G D P R require a lawful basis, consent for non-essential cookies under e-privacy rules and P E C R, data subject rights and transfer rules. In the U S, state laws like California's, as amended, focus on opt-out rights and sensitive data, and more states keep passing laws. The U A E has a federal personal data protection law, with separate regimes in some financial free zones. Saudi Arabia has its Personal Data Protection Law. Pakistan's dedicated legislation has been in development. Check the current status with counsel.
```

Scene 8 — Example 1: Dubai property search

```text
First example, a simple one. A property portal in Dubai lets users search by owner name, so URLs look like search, q equals mohammed ali villa. The team adds q to G A four's query parameter redaction list, or strips it in the tag manager, and sends a separate search event with a search category parameter, like villa or apartment, instead of the raw term. They still learn what categories people search for, without storing names. And search pages join the monthly personal data audit.
```

Scene 9 — Example 2: UK charity donations

```text
Second example, a business case. A UK charity finds donor emails in page location, because its donation form used GET. The team switches the form to POST, adds email and donor to the redaction list, strips them in the tag manager as a second defense, submits a data deletion request for the affected period, deletes matching rows from its BigQuery dataset, and adds the scan to its monthly routine. The incident report is short, because they can show exactly what happened and what was fixed.
```

Scene 10 — Watch me do it, part 1

```text
Watch me run the monthly scan. In BigQuery, I pull page location, page referrer and page title from page view events for the last thirty days. Then I search the combined text with a regular expression for anything that looks like an email address, or a query parameter named email, phone, name, C N I C or iqama. I group and count the matches, and sort by hits. The result is a list of exact URLs and titles to investigate. On a clean property, it returns nothing, and that's the goal.
```

Scene 11 — Privacy with AI tools

```text
One more privacy habit, because this course is about AI too. Before pasting analytics exports into an AI assistant, remove identifiers you don't need, aggregate where possible, and use tools and plans your organization has approved for that data, checking retention and training settings. A table of sessions and key events by channel and week is almost always enough for AI-assisted analysis. You rarely need user-level rows, and you should almost never paste them into a chat.
```

Scene 12 — Common mistakes

```text
Common mistakes. Assuming anonymous analytics means no privacy obligations. Exporting user-level data into spreadsheets shared by email. Keeping ex-employees and ex-agencies on the property. Never reading the privacy notice to check it matches reality. And pasting raw exports into unapproved AI tools.
```

Scene 13 — Privacy health

```text
How do you measure privacy health? Monthly: the PII scan returns nothing. Quarterly: access is reviewed, the privacy notice matches the tools actually running, and retention has a documented reason. And for every incident, the time from discovery to fix and deletion. Those are the numbers that let you answer an auditor calmly.
```

Scene 14 — Recap

```text
Recap. Collect the minimum data needed for defined purposes. Personal data usually leaks through URLs, titles and parameters, so use POST, redaction and tag manager stripping. Set retention deliberately and review settings like Google signals. Know the regimes in your markets and review them with counsel. Scan monthly. And keep user-level data out of AI chats.
```

Scene 15 — Try this now

```text
Try this now. Audit one G A four property for personal data. Review page paths with query strings, page titles and custom parameters, or run the BigQuery scan from the lesson. Document every leak, its source and the fix, and add the scan to your monthly routine.
```

</details>

### 13. Reading standard reports with confidence

Lesson `standard-reports` · module “Reports and explorations” · target 8 min · 15 scenes · 1160 words · planned 8:27 (words ≈ 8:24) · status: **to produce**

| Term | Say |
|---|---|
| GA4 | G A four |
| AOV | A O V |
| UTM | U T M |
| API | A P I |
| ChatGPT | Chat G P T |

| # | Time | Sec | Narration | On screen | Visual |
|---|---|---|---|---|---|
| 1 | 0:00 | 37 | Every Monday, somewhere, a marketing manager opens G A four, clicks around for twenty minutes, takes a few screenshots, and still can't say whether last week was good. Standard reports aren't the problem. Not knowing which report answers which question is. In this lecture you'll learn the shape of G A four's reporting, the crucial difference between user and traffic acquisition, how to compare segments honestly, how to customize the library for each audience, what changed in twenty twenty-six, and how to automate your weekly numbers. | Standard reports<br>• The shape of GA4 reporting<br>• User versus traffic acquisition<br>• Honest comparisons<br>• Custom collections<br>• 2026 updates and automation | Layout: a Monday calendar tile; a clock spins while screenshot icons pile up; then a single clean weekly summary card replaces the pile. 0-9s calendar tile; 9-18s screenshots pile up; 18-28s summary card replaces pile; 28-37s title builds. |
| 2 | 0:37 | 32 | Why does this matter? Because reports are how most people in your organization experience analytics. If they can't find answers quickly, they'll stop looking, or worse, they'll pull the wrong report and draw the wrong conclusion. A channel that looks weak in user acquisition might be strong in traffic acquisition, and vice versa. Knowing the difference, and designing reports around questions, is what turns G A four from a maze into a tool. | Why it matters<br>• Reports are analytics for most people<br>• Wrong report, wrong conclusion<br>• Design around questions | Layout: a maze icon transforms into a straight road with signposts labeled with questions. 0-11s maze appears; 11-21s maze straightens into a road; 21-32s signposts appear. |
| 3 | 1:09 | 34 | Here's the shape. G A four has two analysis surfaces. Reports are pre-built, aggregated, fast, shareable, and good for recurring monitoring. Explorations are flexible canvases for ad-hoc questions, which we'll cover next. Think of reports as the dashboard in your car: speed, fuel, warning lights, always there. Explorations are the mechanic's diagnostic computer: powerful, but you plug it in when something needs investigating. The default collections typically include Realtime, Acquisition, Engagement, Monetization, Retention, and user attributes and technology. | Two surfaces<br>• Reports: monitoring<br>• Explorations: investigation<br>• Car dashboard versus diagnostic computer | Layout: a car dashboard with gauges labeled acquisition, engagement, monetization; beside it a diagnostic laptop plugged into the car labeled 'explorations'. 0-11s dashboard gauges; 11-23s labels appear; 23-34s diagnostic laptop plugs in. |
| 4 | 1:43 | 36 | Now the most misunderstood distinction in G A four. User acquisition uses first user source, medium and channel. It answers: where did new users first come from? Traffic acquisition uses session source, medium and channel. It answers: where did each session come from? A user who first discovers you on Instagram and later returns through Google search appears under Organic Social in user acquisition, and under Organic Search for the later session in traffic acquisition. Neither is wrong. They answer different questions. | User versus traffic acquisition<br>• User: first source of new users<br>• Traffic: source of each session<br>• Same person, two answers<br>• Different questions | Layout: a single user icon's journey: first visit from an Instagram-style icon, later visit from a search icon; two report cards show different channel credit. 0-12s first visit arrow; 12-24s second visit arrow; 24-36s two report cards with different credit. |
| 5 | 2:19 | 35 | So when a stakeholder asks which channel works, clarify: do you mean finding new people, or driving visits and actions? Then the engagement reports: events, key events, pages and screens, and landing pages. Switch the primary dimension to page path when titles are duplicated. Landing pages are critical for evaluating campaigns and search. And monetization and retention: purchases, item performance, and how many users come back over time. For subscription and repeat-purchase businesses, retention often matters more than acquisition volume. | Clarify, then read<br>• New people or visits and actions?<br>• Engagement: pages, landing pages<br>• Page path when titles repeat<br>• Monetization and retention | Layout: a question card splits into two paths; engagement and retention report thumbnails appear; a 'page title' column switches to 'page path'. 0-12s question card splits; 12-23s report thumbnails appear; 23-35s dimension switches to page path. |
| 6 | 2:54 | 35 | Comparisons, filters and date ranges. Comparisons put segments side by side, like mobile versus desktop, or the UK versus the UAE. Filters narrow a report. When comparing date ranges, align weekdays and watch seasonality. Ramadan, Diwali, Black Friday and back-to-school timing differ by market and by year. And check the data quality icon in the report header: data thresholds may withhold rows for small groups when Google signals or demographics are involved, and indicators show when data is sampled or modeled. | Honest comparisons<br>• Comparisons and filters<br>• Align weekdays<br>• Watch seasonality by market<br>• Check thresholds, sampling, modeling icons | Layout: two date ranges align by weekday; seasonal event markers appear on a calendar strip; a data quality icon pulses in a report header. 0-12s date ranges align; 12-23s seasonal markers; 23-35s quality icon pulses. |
| 7 | 3:29 | 36 | And twenty twenty-six brought changes to the reporting surface. Google added dedicated classification for visits from AI assistants, like ChatGPT, Gemini, Copilot, Claude and Perplexity, in default channel reporting, so check your channel definitions. Analytics Advisor, with Ask Advisor in beta, lets you ask plain-language questions inside G A four. Use it to get oriented, then confirm every number in a report before you share it. And Google keeps expanding customizable dashboard-style reporting, so check the what's new page for your property. | 2026 changes<br>• AI assistant traffic classification<br>• Ask Advisor (beta): orient, then verify<br>• More customizable dashboards | Layout: a channel list gains an 'AI assistants' row; a chat bubble labeled 'Ask Advisor' answers a question and a 'verify in report' checkmark follows. 0-12s AI assistants row appears; 12-24s Ask Advisor bubble; 24-36s verify checkmark. |
| 8 | 4:05 | 37 | First example, a simple one. A skincare brand ran creator campaigns across the UAE and Saudi Arabia, with tagged links where medium is influencer and source is each creator's handle. To report results: traffic acquisition, session source and medium, filtered to medium influencer. Add sessions, engagement rate, purchases and revenue. Add a comparison for the UAE versus Saudi Arabia. And cross-check purchases against each creator's discount code redemptions. Illustratively, one creator drives many sessions with low engagement, another drives fewer sessions but far more purchases. | Example 1: creator campaign readout<br>• Traffic acquisition, medium = influencer<br>• Sessions, engagement, purchases, revenue<br>• UAE versus KSA comparison<br>• Cross-check discount codes | Layout: a report table filtered to influencer rows; two creator rows highlight: one with high sessions and low purchases, one with fewer sessions and high purchases. 0-12s filtered table; 12-25s comparison columns; 25-37s two creator rows highlight. |
| 9 | 4:42 | 36 | Second example, a business case. A Lahore software company's growth lead used to screenshot six reports every Monday. Now a script pulls channel sessions, key events and trial starts for two weeks, writes them to a sheet with week-over-week changes, and the analyst spends the saved hour writing the why notes. The leadership collection in G A four still exists for ad-hoc checks, but the Monday meeting runs from the sheet and its commentary. Automation moved time from collecting numbers to explaining them. | Example 2: Lahore SaaS readout<br>• Six screenshots replaced by a script<br>• Two weeks, week-over-week<br>• Saved hour goes to 'why' notes | Layout: six screenshot icons shrink into one script icon that outputs a sheet; a notes panel grows beside the sheet. 0-9s six screenshots; 9-18s shrink into script; 18-27s sheet output; 27-36s notes panel grows. |
| 10 | 5:18 | 37 | Watch me customize the library for leadership. As an admin, I create a collection called Leadership with no more than four reports: an overview with sessions, key events, revenue and key event rate by channel; traffic acquisition by session default channel group; landing pages with key event rate; and a monetization overview. I publish it. Reducing noise in the navigation increases adoption far more than training people to find things in a crowded menu. Then I make a second collection for the content team. | Watch me do it, part 1<br>• Collection: Leadership<br>• Four reports max<br>• Publish<br>• A second collection for content | Layout: a library editor with a new collection card; four report cards drag in; a publish button pulses; a second collection appears. 0-9s collection created; 9-18s four reports drag in; 18-28s publish; 28-37s second collection. |
| 11 | 5:55 | 37 | Then I automate the weekly numbers with the Data A P I. The lesson's Python script creates a client using application default credentials with read-only access. It requests the session default channel group, with sessions, key events and total revenue, for two date ranges: the previous week and last week. It prints a row per channel. Because the A P I returns the same numbers as standard reports, including thresholds and modeling, they'll match what stakeholders see in the interface, unlike raw BigQuery counts. | Watch me do it, part 2<br>• Data API with read-only credentials<br>• Channel group dimension<br>• Sessions, key events, revenue<br>• Two date ranges<br>• Matches the interface | Layout: code editor with the Data API script; lines highlight; a terminal prints channel rows with two columns of values. 0-9s client line; 9-18s dimensions and metrics; 18-28s date ranges; 28-37s output rows. |
| 12 | 6:32 | 30 | Common mistakes. Using user acquisition to judge campaign performance for returning customers. Reporting page titles when several pages share the same title. Comparing a week with a public holiday to a normal week without noting it. Ignoring threshold or sampling indicators on small segments. Reading totals without rates. And forgetting time zones: a property set to one time zone reporting on markets in another can shift daily peaks. | Common mistakes<br>• Wrong acquisition report<br>• Duplicate titles<br>• Unnoted holidays<br>• Ignored quality indicators<br>• Totals without rates<br>• Time zones | Layout: six warning cards in a grid. 0-15s cards 1 to 3; 15-30s cards 4 to 6. |
| 13 | 7:02 | 32 | Here's a weekly reading routine you can adopt. One, key events and revenue versus the previous period and the same period last year. Two, traffic acquisition: which channels moved, and did the key event rate move with them? Three, landing pages: top gainers and losers by key events. Four, technology: any device or browser with a sudden drop, which is often a bug. And five, note anomalies and hypotheses. Don't over-explain small changes. | Weekly routine<br>• Outcomes vs previous and last year<br>• Channel moves and rates<br>• Landing page gainers and losers<br>• Tech drops<br>• Note anomalies, don't over-explain | Layout: a five-step routine card with a small timer showing thirty minutes. 0-11s steps 1 to 3; 11-21s steps 4 and 5; 21-32s timer. |
| 14 | 7:34 | 28 | Recap. Reports are for monitoring, explorations for investigation. User acquisition is where new users first came from; traffic acquisition is where each session came from. Compare honestly, with aligned weekdays, seasonality and quality indicators in mind. Customize collections per audience. Use Ask Advisor to orient, then verify. And automate recurring numbers with the Data A P I, so your time goes into explaining them. | Recap<br>• Monitor versus investigate<br>• User versus traffic acquisition<br>• Honest comparisons<br>• Collections per audience<br>• Verify AI answers; automate | Layout: recap card with car dashboard icon and checkmarks. 0-14s dashboard icon; 14-28s checkmarks. |
| 15 | 8:02 | 25 | Try this now. Design, on paper or in G A four, a leadership collection with no more than four reports, and write the three questions it should answer each week. Then ask Ask Advisor, if you have it, one of those questions, and check its answer against the report. Note where they agree and where they don't. | Try this now<br>• Four-report leadership collection<br>• Three weekly questions<br>• Ask Advisor one question<br>• Verify against the report | Layout: a collection sketch with four cards and three question tags; a chat answer next to a report with matching and mismatching values marked. 0-8s collection sketch; 8-17s question tags; 17-25s comparison marks. |

<details><summary>Narration per scene (paste into ElevenLabs)</summary>

Scene 1 — Standard reports

```text
Every Monday, somewhere, a marketing manager opens G A four, clicks around for twenty minutes, takes a few screenshots, and still can't say whether last week was good. Standard reports aren't the problem. Not knowing which report answers which question is. In this lecture you'll learn the shape of G A four's reporting, the crucial difference between user and traffic acquisition, how to compare segments honestly, how to customize the library for each audience, what changed in twenty twenty-six, and how to automate your weekly numbers.
```

Scene 2 — Why it matters

```text
Why does this matter? Because reports are how most people in your organization experience analytics. If they can't find answers quickly, they'll stop looking, or worse, they'll pull the wrong report and draw the wrong conclusion. A channel that looks weak in user acquisition might be strong in traffic acquisition, and vice versa. Knowing the difference, and designing reports around questions, is what turns G A four from a maze into a tool.
```

Scene 3 — Two surfaces

```text
Here's the shape. G A four has two analysis surfaces. Reports are pre-built, aggregated, fast, shareable, and good for recurring monitoring. Explorations are flexible canvases for ad-hoc questions, which we'll cover next. Think of reports as the dashboard in your car: speed, fuel, warning lights, always there. Explorations are the mechanic's diagnostic computer: powerful, but you plug it in when something needs investigating. The default collections typically include Realtime, Acquisition, Engagement, Monetization, Retention, and user attributes and technology.
```

Scene 4 — User versus traffic acquisition

```text
Now the most misunderstood distinction in G A four. User acquisition uses first user source, medium and channel. It answers: where did new users first come from? Traffic acquisition uses session source, medium and channel. It answers: where did each session come from? A user who first discovers you on Instagram and later returns through Google search appears under Organic Social in user acquisition, and under Organic Search for the later session in traffic acquisition. Neither is wrong. They answer different questions.
```

Scene 5 — Clarify, then read

```text
So when a stakeholder asks which channel works, clarify: do you mean finding new people, or driving visits and actions? Then the engagement reports: events, key events, pages and screens, and landing pages. Switch the primary dimension to page path when titles are duplicated. Landing pages are critical for evaluating campaigns and search. And monetization and retention: purchases, item performance, and how many users come back over time. For subscription and repeat-purchase businesses, retention often matters more than acquisition volume.
```

Scene 6 — Honest comparisons

```text
Comparisons, filters and date ranges. Comparisons put segments side by side, like mobile versus desktop, or the UK versus the UAE. Filters narrow a report. When comparing date ranges, align weekdays and watch seasonality. Ramadan, Diwali, Black Friday and back-to-school timing differ by market and by year. And check the data quality icon in the report header: data thresholds may withhold rows for small groups when Google signals or demographics are involved, and indicators show when data is sampled or modeled.
```

Scene 7 — 2026 changes

```text
And twenty twenty-six brought changes to the reporting surface. Google added dedicated classification for visits from AI assistants, like ChatGPT, Gemini, Copilot, Claude and Perplexity, in default channel reporting, so check your channel definitions. Analytics Advisor, with Ask Advisor in beta, lets you ask plain-language questions inside G A four. Use it to get oriented, then confirm every number in a report before you share it. And Google keeps expanding customizable dashboard-style reporting, so check the what's new page for your property.
```

Scene 8 — Example 1: creator campaign readout

```text
First example, a simple one. A skincare brand ran creator campaigns across the UAE and Saudi Arabia, with tagged links where medium is influencer and source is each creator's handle. To report results: traffic acquisition, session source and medium, filtered to medium influencer. Add sessions, engagement rate, purchases and revenue. Add a comparison for the UAE versus Saudi Arabia. And cross-check purchases against each creator's discount code redemptions. Illustratively, one creator drives many sessions with low engagement, another drives fewer sessions but far more purchases.
```

Scene 9 — Example 2: Lahore SaaS readout

```text
Second example, a business case. A Lahore software company's growth lead used to screenshot six reports every Monday. Now a script pulls channel sessions, key events and trial starts for two weeks, writes them to a sheet with week-over-week changes, and the analyst spends the saved hour writing the why notes. The leadership collection in G A four still exists for ad-hoc checks, but the Monday meeting runs from the sheet and its commentary. Automation moved time from collecting numbers to explaining them.
```

Scene 10 — Watch me do it, part 1

```text
Watch me customize the library for leadership. As an admin, I create a collection called Leadership with no more than four reports: an overview with sessions, key events, revenue and key event rate by channel; traffic acquisition by session default channel group; landing pages with key event rate; and a monetization overview. I publish it. Reducing noise in the navigation increases adoption far more than training people to find things in a crowded menu. Then I make a second collection for the content team.
```

Scene 11 — Watch me do it, part 2

```text
Then I automate the weekly numbers with the Data A P I. The lesson's Python script creates a client using application default credentials with read-only access. It requests the session default channel group, with sessions, key events and total revenue, for two date ranges: the previous week and last week. It prints a row per channel. Because the A P I returns the same numbers as standard reports, including thresholds and modeling, they'll match what stakeholders see in the interface, unlike raw BigQuery counts.
```

Scene 12 — Common mistakes

```text
Common mistakes. Using user acquisition to judge campaign performance for returning customers. Reporting page titles when several pages share the same title. Comparing a week with a public holiday to a normal week without noting it. Ignoring threshold or sampling indicators on small segments. Reading totals without rates. And forgetting time zones: a property set to one time zone reporting on markets in another can shift daily peaks.
```

Scene 13 — Weekly routine

```text
Here's a weekly reading routine you can adopt. One, key events and revenue versus the previous period and the same period last year. Two, traffic acquisition: which channels moved, and did the key event rate move with them? Three, landing pages: top gainers and losers by key events. Four, technology: any device or browser with a sudden drop, which is often a bug. And five, note anomalies and hypotheses. Don't over-explain small changes.
```

Scene 14 — Recap

```text
Recap. Reports are for monitoring, explorations for investigation. User acquisition is where new users first came from; traffic acquisition is where each session came from. Compare honestly, with aligned weekdays, seasonality and quality indicators in mind. Customize collections per audience. Use Ask Advisor to orient, then verify. And automate recurring numbers with the Data A P I, so your time goes into explaining them.
```

Scene 15 — Try this now

```text
Try this now. Design, on paper or in G A four, a leadership collection with no more than four reports, and write the three questions it should answer each week. Then ask Ask Advisor, if you have it, one of those questions, and check its answer against the report. Note where they agree and where they don't.
```

</details>

### 14. Explorations: funnels, paths, segments and cohorts

Lesson `explorations` · module “Reports and explorations” · target 8 min · 15 scenes · 1156 words · planned 8:24 (words ≈ 8:23) · status: **to produce**

| Term | Say |
|---|---|
| GA4 | G A four |
| AOV | A O V |
| UTM | U T M |
| MAU | monthly active users |
| FAQ | F A Q |
| CRM | C R M |

| # | Time | Sec | Narration | On screen | Visual |
|---|---|---|---|---|---|
| 1 | 0:00 | 39 | A standard report tells you that mobile conversion is down. It doesn't tell you where mobile users drop out of checkout, what they do after viewing pricing, or whether customers acquired this month are coming back. For those questions, you need explorations. In this lecture you'll learn the exploration techniques and when to use each one, why segment scope changes the answer, how to build a funnel, how paths and cohorts reveal what reports hide, and how to move a key funnel into BigQuery so it's unsampled and permanent. | Explorations<br>• Techniques and when to use them<br>• Segment scope<br>• Building a funnel<br>• Paths and cohorts<br>• A durable funnel in BigQuery | Layout: a standard report card shows a red down arrow; a magnifying glass slides over it, opening into a funnel diagram, a path tree and a cohort grid. 0-10s report card with down arrow; 10-20s magnifier slides in; 20-29s three technique visuals open; 29-39s title builds. |
| 2 | 0:39 | 33 | Why explorations? Because real business questions are specific. Where do mobile users abandon checkout? Do people who use site search buy more? Are recent customers stickier? Standard reports are built for monitoring, not investigation. Explorations let you pick the technique, the segments and the breakdowns that fit your question. They're private by default, so you can work freely, then share when ready. Just remember they rely on event-level data, so they're limited by your retention setting. | Why explorations<br>• Specific questions<br>• Choose technique, segments, breakdowns<br>• Private by default<br>• Limited by retention | Layout: three question cards float; each drops into a different exploration technique slot; a retention clock sits at the edge showing a limit. 0-11s question cards; 11-22s cards drop into slots; 22-33s retention clock. |
| 3 | 1:12 | 33 | Here are the techniques. Free form: flexible pivot tables and charts, like key event rate by landing page and device. Funnel exploration: step-by-step conversion and drop-off. Path exploration: what happens before or after an event. Segment overlap: how audiences intersect, like purchasers who also used search. Cohort exploration: retention of groups over time. User lifetime: value over the whole relationship. And user explorer: individual event streams, to be used responsibly, mainly for QA of test journeys. | Techniques<br>• Free form<br>• Funnel<br>• Path<br>• Segment overlap<br>• Cohort<br>• User lifetime<br>• User explorer (responsibly) | Layout: a technique menu with seven tiles, each showing a tiny preview chart as it is named. 0-11s free form and funnel tiles; 11-22s path and overlap tiles; 22-33s cohort, lifetime and explorer tiles. |
| 4 | 1:45 | 40 | Now segments, the most powerful feature, and the easiest to get wrong. There are three scopes. User segments include users who met a condition at any time, and all their data. Session segments include only sessions that met the condition. Event segments include only the matching events. Here's an analogy: user scope is inviting everyone who has ever bought a ticket to a concert. Session scope is counting only the nights they actually attended. Users who viewed pricing includes all their sessions. Sessions with a pricing view includes only those sessions. Different answers. | Segment scope<br>• User: all data for qualifying users<br>• Session: only qualifying sessions<br>• Event: only matching events<br>• Choose deliberately | Layout: three nested circles labeled user, session and event; a 'viewed pricing' condition highlights different amounts of data in each. 0-13s user circle highlights broadly; 13-27s session circle highlights less; 27-40s event circle highlights least. |
| 5 | 2:25 | 37 | Building a funnel. Start with the question: where do mobile users drop off between product view and purchase? Steps: view item, add to cart, begin checkout, add payment info, purchase. Then settings. Open or closed: a closed funnel requires entry at step one, an open funnel lets users enter at any step. Directly or indirectly followed: usually indirectly. Breakdown: device category. And optionally, time elapsed between steps. Trended funnel views show whether a step is improving over time, perfect for checking a checkout redesign. | Building a funnel<br>• Question first<br>• Five steps<br>• Open versus closed<br>• Indirectly followed<br>• Breakdown by device<br>• Trended view for changes | Layout: a funnel builder panel: steps added one by one; toggles for open/closed and direct/indirect flip; a breakdown dropdown selects device. 0-9s steps added; 9-18s toggles flip; 18-28s breakdown selected; 28-37s trended view line appears. |
| 6 | 3:02 | 38 | Path exploration is for discovery. Start from an event, like view pricing, and see the next steps. Or start from an ending point, like purchase, and look backward. Use it to find unexpected loops, like users bouncing repeatedly between pricing and the FAQ, which suggests unanswered questions. Cohort exploration groups users by acquisition date or first event and shows how they return over the following weeks. For subscription apps, course platforms and grocery delivery, cohort retention is often a better leading indicator of revenue than monthly visitors. | Paths and cohorts<br>• Paths: forward or backward from an event<br>• Spot loops<br>• Cohorts: return behavior by start week<br>• Retention as a leading indicator | Layout: a path tree branching from 'view_pricing' with a loop between pricing and FAQ highlighted; a cohort triangle grid fills with shades. 0-13s path tree branches; 13-25s loop highlights; 25-38s cohort grid fills. |
| 7 | 3:40 | 40 | First example, a simple one. An online academy notices many pricing views but few purchases. A path exploration from view pricing shows a large share of next steps go to the FAQ and then exit. A free-form table shows FAQ-page sessions have a lower key event rate on mobile. A segment overlap shows most purchasers never visited the FAQ; they came from webinar emails. Hypothesis: pricing lacks clarity about refunds and certificates, which the FAQ answers poorly on mobile. Action: add a concise refund and certificate section to pricing, and test it. | Example 1: the pricing puzzle<br>• Path: pricing to FAQ to exit<br>• Free form: FAQ weak on mobile<br>• Overlap: buyers skip the FAQ<br>• Hypothesis and test | Layout: three small exploration visuals appear in sequence (path, table, Venn), then converge into a hypothesis card and a test card. 0-10s path visual; 10-20s table visual; 20-30s Venn visual; 30-40s hypothesis and test cards. |
| 8 | 4:20 | 36 | Second example, a business case. A grocery delivery app in the UAE sees monthly active users rising and assumes retention is improving. A cohort exploration by first-open week shows the opposite. Recent cohorts return less in weeks two to four than cohorts from three months earlier. Growth is coming from heavy acquisition spend, not stickier customers. The team shifts part of the budget to onboarding improvements and makes week-four retention a KPI. Without the cohort view, they'd have celebrated the wrong thing. | Example 2: UAE grocery app<br>• MAU rising<br>• Cohorts: recent weeks retain worse<br>• Growth from spend, not stickiness<br>• Budget to onboarding; week-4 retention KPI | Layout: a rising MAU line; beside it a cohort grid where newer rows are lighter in weeks two to four; a budget slider moves toward 'onboarding'. 0-12s MAU line rises; 12-24s cohort grid reveals lighter rows; 24-36s budget slider moves. |
| 9 | 4:56 | 43 | Watch me build the funnel. In a new funnel exploration, I add five steps: view item, add to cart, begin checkout, add payment info and purchase. I set it to closed, so users must enter at step one, and indirectly followed. I add a breakdown by device category and a date range of the last twenty-eight days. The chart shows mobile losing far more users between checkout and payment. I switch to the trended view and see the drop started on a specific date. I note it as a hypothesis, not a conclusion, and check the release log. | Watch me do it, part 1<br>• Five steps<br>• Closed, indirectly followed<br>• Breakdown: device<br>• Trended view pinpoints a date<br>• Hypothesis, then release log | Layout: funnel exploration UI mock builds step by step; a device breakdown shows mobile bars shrinking at payment; a trended line shows a drop at a date marker. 0-11s steps added; 11-22s settings toggled; 22-32s device breakdown; 32-43s trended drop at a date. |
| 10 | 5:39 | 37 | Then I make it durable in BigQuery. The lesson's query finds each consented user's first timestamp for each step in September, plus their device. Then it counts users who reached each step after the previous one. It won't match the exploration exactly, because the counting rules differ, there's no modeling, and consent-denied rows are excluded. But it's unsampled, it isn't limited by retention, and I can schedule it. So my key funnel now has a permanent history, not just a view I rebuild every month. | Watch me do it, part 2<br>• First timestamp per step per user<br>• Count ordered progress<br>• Won't match exactly<br>• Unsampled, schedulable, permanent | Layout: SQL editor highlights the firsts CTE and the ordered COUNTIF conditions; a result table by device appears; a calendar icon marks it as scheduled. 0-9s firsts CTE highlights; 9-18s ordered conditions highlight; 18-28s result by device; 28-37s scheduled icon. |
| 11 | 6:16 | 35 | A word on sampling and limits. Explorations may be sampled when queries exceed quota thresholds on standard properties, and an indicator shows the sample size. To reduce sampling, shorten the date range or simplify the query. For heavy or long-range analysis, use the BigQuery export, which provides raw event-level data, and is also the route for joining G A four with C R M or cost data. Remember that standard properties have a daily export limit, which module seven covers. | Sampling and limits<br>• Sampling indicator<br>• Shorter ranges, simpler queries<br>• BigQuery for heavy work and joins<br>• Export limits (module 7) | Layout: a sampling badge shows a percentage; narrowing the date range makes it disappear; a BigQuery icon offers an alternative path with a join icon to a CRM. 0-12s sampling badge; 12-23s range narrows; 23-35s BigQuery path with join. |
| 12 | 6:51 | 24 | Common mistakes. Building explorations for questions a standard report already answers. Choosing the wrong segment scope. Comparing a closed funnel's numbers with an open funnel's. Drawing conclusions from heavily sampled data without saying so. And forgetting that explorations can't look back beyond the retention window, which is why the BigQuery version matters for long-term funnels. | Common mistakes<br>• Explorations for simple questions<br>• Wrong scope<br>• Open vs closed confusion<br>• Unstated sampling<br>• Retention window | Layout: five warning cards drop in. 0-12s cards 1 to 3; 12-24s cards 4 and 5. |
| 13 | 7:15 | 22 | Here's the essence of analysis, and it applies to every exploration you'll build. Observation, then segmentation, then hypothesis, then action. Not screenshots of charts. A good exploration ends with a sentence that starts, we think this is happening because, and a test or fix that will prove it right or wrong. | The essence<br>• Observation<br>• Segmentation<br>• Hypothesis<br>• Action<br>• Not screenshots | Layout: four stepping stones labeled observation, segmentation, hypothesis, action; a camera icon labeled 'screenshots' is crossed out. 0-11s stones appear; 11-22s camera crossed out. |
| 14 | 7:37 | 22 | Recap. Explorations answer specific questions that reports can't. Segment scope, user, session or event, changes the answer, so choose deliberately. Use funnels for drop-off, paths for discovery and cohorts for retention. Watch for sampling and the retention window. And move your key funnel into BigQuery so it's unsampled and permanent. | Recap<br>• Specific questions<br>• Scope changes answers<br>• Funnels, paths, cohorts<br>• Sampling and retention<br>• Durable funnel in BigQuery | Layout: recap card with funnel, path and cohort icons and checkmarks. 0-11s icons; 11-22s checkmarks. |
| 15 | 7:59 | 25 | Try this now. Build a funnel exploration, or specify one in writing, for your main conversion journey. Break it down by device, find the biggest drop-off, and write one hypothesis for it in the form: we think this is happening because. If you have BigQuery, run the lesson's funnel query for the same period and compare the shapes. | Try this now<br>• Funnel for your main journey<br>• Breakdown by device<br>• One hypothesis<br>• Compare with the BigQuery version | Layout: a funnel sketch with a circled drop; a hypothesis card types 'we think this is happening because'; a second funnel from BigQuery overlays. 0-8s funnel sketch; 8-17s hypothesis card; 17-25s BigQuery overlay. |

<details><summary>Narration per scene (paste into ElevenLabs)</summary>

Scene 1 — Explorations

```text
A standard report tells you that mobile conversion is down. It doesn't tell you where mobile users drop out of checkout, what they do after viewing pricing, or whether customers acquired this month are coming back. For those questions, you need explorations. In this lecture you'll learn the exploration techniques and when to use each one, why segment scope changes the answer, how to build a funnel, how paths and cohorts reveal what reports hide, and how to move a key funnel into BigQuery so it's unsampled and permanent.
```

Scene 2 — Why explorations

```text
Why explorations? Because real business questions are specific. Where do mobile users abandon checkout? Do people who use site search buy more? Are recent customers stickier? Standard reports are built for monitoring, not investigation. Explorations let you pick the technique, the segments and the breakdowns that fit your question. They're private by default, so you can work freely, then share when ready. Just remember they rely on event-level data, so they're limited by your retention setting.
```

Scene 3 — Techniques

```text
Here are the techniques. Free form: flexible pivot tables and charts, like key event rate by landing page and device. Funnel exploration: step-by-step conversion and drop-off. Path exploration: what happens before or after an event. Segment overlap: how audiences intersect, like purchasers who also used search. Cohort exploration: retention of groups over time. User lifetime: value over the whole relationship. And user explorer: individual event streams, to be used responsibly, mainly for QA of test journeys.
```

Scene 4 — Segment scope

```text
Now segments, the most powerful feature, and the easiest to get wrong. There are three scopes. User segments include users who met a condition at any time, and all their data. Session segments include only sessions that met the condition. Event segments include only the matching events. Here's an analogy: user scope is inviting everyone who has ever bought a ticket to a concert. Session scope is counting only the nights they actually attended. Users who viewed pricing includes all their sessions. Sessions with a pricing view includes only those sessions. Different answers.
```

Scene 5 — Building a funnel

```text
Building a funnel. Start with the question: where do mobile users drop off between product view and purchase? Steps: view item, add to cart, begin checkout, add payment info, purchase. Then settings. Open or closed: a closed funnel requires entry at step one, an open funnel lets users enter at any step. Directly or indirectly followed: usually indirectly. Breakdown: device category. And optionally, time elapsed between steps. Trended funnel views show whether a step is improving over time, perfect for checking a checkout redesign.
```

Scene 6 — Paths and cohorts

```text
Path exploration is for discovery. Start from an event, like view pricing, and see the next steps. Or start from an ending point, like purchase, and look backward. Use it to find unexpected loops, like users bouncing repeatedly between pricing and the FAQ, which suggests unanswered questions. Cohort exploration groups users by acquisition date or first event and shows how they return over the following weeks. For subscription apps, course platforms and grocery delivery, cohort retention is often a better leading indicator of revenue than monthly visitors.
```

Scene 7 — Example 1: the pricing puzzle

```text
First example, a simple one. An online academy notices many pricing views but few purchases. A path exploration from view pricing shows a large share of next steps go to the FAQ and then exit. A free-form table shows FAQ-page sessions have a lower key event rate on mobile. A segment overlap shows most purchasers never visited the FAQ; they came from webinar emails. Hypothesis: pricing lacks clarity about refunds and certificates, which the FAQ answers poorly on mobile. Action: add a concise refund and certificate section to pricing, and test it.
```

Scene 8 — Example 2: UAE grocery app

```text
Second example, a business case. A grocery delivery app in the UAE sees monthly active users rising and assumes retention is improving. A cohort exploration by first-open week shows the opposite. Recent cohorts return less in weeks two to four than cohorts from three months earlier. Growth is coming from heavy acquisition spend, not stickier customers. The team shifts part of the budget to onboarding improvements and makes week-four retention a KPI. Without the cohort view, they'd have celebrated the wrong thing.
```

Scene 9 — Watch me do it, part 1

```text
Watch me build the funnel. In a new funnel exploration, I add five steps: view item, add to cart, begin checkout, add payment info and purchase. I set it to closed, so users must enter at step one, and indirectly followed. I add a breakdown by device category and a date range of the last twenty-eight days. The chart shows mobile losing far more users between checkout and payment. I switch to the trended view and see the drop started on a specific date. I note it as a hypothesis, not a conclusion, and check the release log.
```

Scene 10 — Watch me do it, part 2

```text
Then I make it durable in BigQuery. The lesson's query finds each consented user's first timestamp for each step in September, plus their device. Then it counts users who reached each step after the previous one. It won't match the exploration exactly, because the counting rules differ, there's no modeling, and consent-denied rows are excluded. But it's unsampled, it isn't limited by retention, and I can schedule it. So my key funnel now has a permanent history, not just a view I rebuild every month.
```

Scene 11 — Sampling and limits

```text
A word on sampling and limits. Explorations may be sampled when queries exceed quota thresholds on standard properties, and an indicator shows the sample size. To reduce sampling, shorten the date range or simplify the query. For heavy or long-range analysis, use the BigQuery export, which provides raw event-level data, and is also the route for joining G A four with C R M or cost data. Remember that standard properties have a daily export limit, which module seven covers.
```

Scene 12 — Common mistakes

```text
Common mistakes. Building explorations for questions a standard report already answers. Choosing the wrong segment scope. Comparing a closed funnel's numbers with an open funnel's. Drawing conclusions from heavily sampled data without saying so. And forgetting that explorations can't look back beyond the retention window, which is why the BigQuery version matters for long-term funnels.
```

Scene 13 — The essence

```text
Here's the essence of analysis, and it applies to every exploration you'll build. Observation, then segmentation, then hypothesis, then action. Not screenshots of charts. A good exploration ends with a sentence that starts, we think this is happening because, and a test or fix that will prove it right or wrong.
```

Scene 14 — Recap

```text
Recap. Explorations answer specific questions that reports can't. Segment scope, user, session or event, changes the answer, so choose deliberately. Use funnels for drop-off, paths for discovery and cohorts for retention. Watch for sampling and the retention window. And move your key funnel into BigQuery so it's unsampled and permanent.
```

Scene 15 — Try this now

```text
Try this now. Build a funnel exploration, or specify one in writing, for your main conversion journey. Break it down by device, find the biggest drop-off, and write one hypothesis for it in the form: we think this is happening because. If you have BigQuery, run the lesson's funnel query for the same period and compare the shapes.
```

</details>

### 15. From data to insight: an analysis workflow

Lesson `from-data-to-insight` · module “Reports and explorations” · target 8 min · 14 scenes · 1115 words · planned 8:05 (words ≈ 8:05) · status: **to produce**

| Term | Say |
|---|---|
| GA4 | G A four |
| AOV | A O V |
| UTM | U T M |
| KPI | K P I |
| KPIs | K P eyes |
| CR | conversion rate |
| LTV | L T V |
| CRM | C R M |

| # | Time | Sec | Narration | On screen | Visual |
|---|---|---|---|---|---|
| 1 | 0:00 | 43 | Sessions rose twelve percent. Is that an insight? No. It's an observation. Here's an insight: sessions rose twelve percent because a creator's video drove new mobile users, but they converted at half the site average because the landing page is slow on mobile, so fixing speed could unlock meaningful revenue. See the difference? It explains, it quantifies, and it points to an action. In this lecture you'll learn a question-first workflow, the decomposition method for diagnosing changes, how to separate signal from noise, how to use AI assistants safely for hypotheses, and how to present insights people act on. | From data to insight<br>• Observation versus insight<br>• Question-first workflow<br>• Decomposition<br>• Signal versus noise<br>• AI for hypotheses, safely<br>• Presenting | Layout: an observation card 'sessions +12%' transforms into a richer insight card with three highlighted parts: explains, quantifies, action. 0-11s observation card; 11-22s transforms; 22-32s three parts highlight; 32-43s title builds. |
| 2 | 0:43 | 33 | Why is this the skill that matters most? Because stakeholders don't need more numbers. They need to know what's happening, why, and what to do. Analysts who deliver insights get invited into decisions. Analysts who deliver screenshots get asked for more screenshots. And with AI tools now summarizing reports in seconds, the value of a human analyst is exactly this: framing the right question, testing explanations against evidence, and making a recommendation someone can own. | Why it matters<br>• Stakeholders need what, why, what next<br>• Insights earn a seat at decisions<br>• AI summarizes; analysts decide what's true and useful | Layout: two analyst icons: one hands over a stack of screenshots and gets more requests; the other hands over a one-line insight and gets a seat at a table. 0-11s screenshot analyst; 11-22s insight analyst; 22-33s seat at the table. |
| 3 | 1:16 | 38 | Here's the question-first workflow. Question: what decision will this inform, who makes it, by when? Context: what changed, like campaigns, releases, seasonality, tracking? Hypotheses: list two to four plausible explanations before opening G A four. Evidence: which report or segment would confirm or refute each? Analysis: run the checks and note data quality caveats. So what: quantify the impact and recommend an action. Follow-up: how will we know if it worked? Writing hypotheses first protects you from confirmation bias, hunting until something supports what you already believed. | Question-first workflow<br>• Question, context<br>• Hypotheses before data<br>• Evidence per hypothesis<br>• Analysis with caveats<br>• So what, follow-up | Layout: a seven-step ladder climbs; the hypotheses rung glows with a shield labeled 'against confirmation bias'. 0-13s ladder builds; 13-25s hypotheses rung glows; 25-38s shield appears. |
| 4 | 1:54 | 40 | Now the decomposition method, my favorite diagnostic tool. When a KPI moves, break it into factors. Revenue equals sessions, times conversion rate, times average order value. Leads equal sessions times lead rate. Ask which factor moved, and within that factor, which segment explains most of the change: channel, device, country, landing page, new or returning. Think of it like a doctor checking vital signs one at a time instead of saying the patient seems unwell. A fifteen percent revenue drop could be one channel's traffic, or a mobile conversion problem. Very different fixes. | Decomposition<br>• Revenue = sessions x CR x AOV<br>• Leads = sessions x lead rate<br>• Which factor moved?<br>• Which segment explains it? | Layout: a revenue bar splits into three multiplied blocks (sessions, conversion rate, AOV); one block shrinks; a stethoscope icon checks each. 0-13s revenue bar splits; 13-27s stethoscope checks each; 27-40s conversion rate block shrinks. |
| 5 | 2:34 | 34 | Separating signal from noise. Small numbers swing randomly. Before reacting, compare against a longer baseline, like the same weekday average over several weeks. Check volume: a conversion rate from thirty sessions is unreliable. Check tracking changes and annotations: did measurement change rather than behavior? And consider seasonality and events: paydays, public holidays, school terms, religious festivals, sales events. Then, where a decision is significant and two things moved together, remember correlation isn't causation. Propose an experiment to confirm. | Signal versus noise<br>• Longer baselines<br>• Enough volume<br>• Tracking changes<br>• Seasonality and events<br>• Correlation isn't causation | Layout: a noisy line with a baseline band; a small-volume point wobbles wildly; an annotation flag marks a tracking change; a calendar shows festival markers. 0-8s noisy line with band; 8-17s small-volume point wobbles; 17-26s annotation flag; 26-34s festival markers. |
| 6 | 3:08 | 40 | First example, with illustrative numbers. An online homeware store compares two equivalent weeks. Sessions went from fifty thousand to forty-nine thousand, minus two percent. Conversion rate from two percent to one point six, minus twenty percent. Average order value sixty to sixty-one, up two percent. Revenue down about twenty percent. So conversion rate explains the drop. By device, desktop is unchanged but mobile fell sharply, starting the day of a site release with a new address lookup. Funnels confirm drop-off at add shipping info on mobile. Recommendation: roll back or fix the component. | Example 1: the revenue dip<br>• Sessions -2%, CR -20%, AOV +2% (illustrative)<br>• Mobile only<br>• Starts on release day<br>• Drop at add_shipping_info<br>• Fix or roll back | Layout: a decomposition table fills; the conversion rate row highlights red; a device split shows mobile down; a release marker on a timeline aligns with the drop. 0-10s table fills; 10-20s CR row red; 20-30s device split; 30-40s release marker aligns. |
| 7 | 3:48 | 33 | Second example, a business case. A Riyadh travel agency's leads fall thirty percent in a week, illustratively. Decomposition shows sessions stable and the lead rate down only on Arabic-language pages. A path exploration shows Arabic users reaching the form and leaving. The cause: the form's Arabic validation message broke after a translation update, blocking submission. Fixing it restored the lead rate. The written insight took five lines, and the recommendation had an owner and a deadline. | Example 2: Riyadh travel leads<br>• Sessions stable, lead rate down on Arabic pages<br>• Path: reach form, leave<br>• Broken Arabic validation<br>• Fixed, with owner and deadline | Layout: a language split chart shows Arabic pages' lead rate collapsing; a form mock shows a broken validation message in Arabic script; after the fix the rate recovers. 0-8s language split chart; 8-16s broken form message; 16-25s fix applied; 25-33s rate recovers. |
| 8 | 4:21 | 39 | Watch me decompose in pandas. I export two comparable weeks with sessions, orders and revenue by device. The lesson's script groups by week, computes conversion rate as orders over sessions, and average order value as revenue over orders, and prints them. Instantly I see conversion rate is the factor that moved. Then it pivots by device and prints the change in conversion rate per device, sorted. Mobile sits at the top of the list. Ten lines of code, and I've done in a minute what used to take twenty clicks. | Watch me do it, part 1<br>• Export two weeks by device<br>• Compute CR and AOV per week<br>• Pivot by device<br>• Sort the CR change | Layout: code editor with the pandas script; the aggregated table prints; then a sorted per-device change list prints with mobile at the top. 0-10s script highlights; 10-20s weekly table prints; 20-29s device changes print; 29-39s mobile highlights. |
| 9 | 5:00 | 44 | Then I use AI for hypotheses, carefully. I give an assistant the context, the aggregated table with no personal data, and the known events: a release on the fourteenth, no campaign changes. I ask for three or four hypotheses ranked by fit, the G A four report or exploration that would confirm or refute each, and a what, so what, now what draft using only the numbers provided, with estimates marked illustrative. Inside G A four, Ask Advisor can do similar work from your property. Either way, I treat the answer as a list of hypotheses, and I verify each one myself. | Watch me do it, part 2<br>• Context, aggregated data, known events<br>• Ranked hypotheses with checks<br>• Summary using only given numbers<br>• Ask Advisor: same rule<br>• Verify every claim | Layout: a chat window receives a structured prompt; a response lists ranked hypotheses; each gets a 'verify' checkbox that is ticked after opening a report. 0-15s prompt pastes in; 15-29s ranked hypotheses appear; 29-44s verify boxes tick. |
| 10 | 5:44 | 32 | Now presenting. Use what, so what, now what. What: mobile conversion rate fell from two point one percent to one point three percent since the fourteenth. So what: illustratively, about one fifth of weekly revenue is at risk, and desktop is unaffected. Now what: roll back the address lookup, the developers, today; re-test the funnel, analytics, tomorrow. Lead with the conclusion. Put charts after the story, and only the charts that support it. | Presenting<br>• What<br>• So what (impact, labeled)<br>• Now what (owner, date)<br>• Conclusion first | Layout: three stacked cards labeled What, So what, Now what fill with the example text; a small chart appears below them, not above. 0-8s What card; 8-16s So what card; 16-24s Now what card; 24-32s supporting chart below. |
| 11 | 6:16 | 36 | Some questions can't be answered in G A four alone. Profit needs cost of goods. Lifetime value across offline channels needs your C R M. Lead quality needs sales outcomes. Good analysts know when to pull in other sources, like C R M exports, ad cost data and BigQuery joins, and they state clearly which numbers came from where. And they timebox. Agree the time with the decision-maker up front. A good-enough answer before the decision beats a perfect one after it. | Beyond GA4 and timeboxing<br>• Profit, LTV, lead quality need other data<br>• State sources clearly<br>• Agree a timebox<br>• Good enough, on time | Layout: GA4 icon joined by CRM, cost and order system icons into one analysis; a timer icon labeled 'agreed timebox'. 0-12s sources join; 12-24s source labels; 24-36s timebox timer. |
| 12 | 6:52 | 27 | Common mistakes. Opening G A four without a question and looking for insights. Reporting every metric that changed instead of the ones that matter. Explaining random noise as meaningful trends. Recommendations without owners or deadlines. Forgetting to check whether tracking changed. Presenting AI-generated explanations as findings. And false precision, like revenue impact to the last unit when the inputs are estimates. | Common mistakes<br>• No question<br>• Every metric<br>• Noise as trend<br>• No owners<br>• Ignored tracking changes<br>• AI output as findings<br>• False precision | Layout: seven warning chips scroll past. 0-14s chips 1 to 4; 14-27s chips 5 to 7. |
| 13 | 7:19 | 23 | Recap. An insight explains, quantifies and points to action. Write hypotheses before opening reports. Decompose K P Is to find the factor and segment that moved. Separate signal from noise. Use AI and Ask Advisor to generate hypotheses, then verify everything. Present as what, so what, now what, conclusion first, with owners and dates. | Recap<br>• Explain, quantify, act<br>• Hypotheses first<br>• Decompose<br>• Signal vs noise<br>• AI for hypotheses, verified<br>• What, so what, now what | Layout: recap card with the stethoscope and three-card icons. 0-12s icons; 12-23s checkmarks. |
| 14 | 7:42 | 23 | Try this now. Take a real K P I change from a site you know. Decompose it into factors and find the segment that moved most. Write three hypotheses before you look any further. Then write a what, so what, now what summary with an owner and a date for the action. | Try this now<br>• Decompose a real change<br>• Three hypotheses first<br>• What, so what, now what<br>• Owner and date | Layout: a decomposition template, three hypothesis cards and a three-part summary card fill in sequence. 0-8s decomposition fills; 8-15s hypotheses fill; 15-23s summary fills. |

<details><summary>Narration per scene (paste into ElevenLabs)</summary>

Scene 1 — From data to insight

```text
Sessions rose twelve percent. Is that an insight? No. It's an observation. Here's an insight: sessions rose twelve percent because a creator's video drove new mobile users, but they converted at half the site average because the landing page is slow on mobile, so fixing speed could unlock meaningful revenue. See the difference? It explains, it quantifies, and it points to an action. In this lecture you'll learn a question-first workflow, the decomposition method for diagnosing changes, how to separate signal from noise, how to use AI assistants safely for hypotheses, and how to present insights people act on.
```

Scene 2 — Why it matters

```text
Why is this the skill that matters most? Because stakeholders don't need more numbers. They need to know what's happening, why, and what to do. Analysts who deliver insights get invited into decisions. Analysts who deliver screenshots get asked for more screenshots. And with AI tools now summarizing reports in seconds, the value of a human analyst is exactly this: framing the right question, testing explanations against evidence, and making a recommendation someone can own.
```

Scene 3 — Question-first workflow

```text
Here's the question-first workflow. Question: what decision will this inform, who makes it, by when? Context: what changed, like campaigns, releases, seasonality, tracking? Hypotheses: list two to four plausible explanations before opening G A four. Evidence: which report or segment would confirm or refute each? Analysis: run the checks and note data quality caveats. So what: quantify the impact and recommend an action. Follow-up: how will we know if it worked? Writing hypotheses first protects you from confirmation bias, hunting until something supports what you already believed.
```

Scene 4 — Decomposition

```text
Now the decomposition method, my favorite diagnostic tool. When a KPI moves, break it into factors. Revenue equals sessions, times conversion rate, times average order value. Leads equal sessions times lead rate. Ask which factor moved, and within that factor, which segment explains most of the change: channel, device, country, landing page, new or returning. Think of it like a doctor checking vital signs one at a time instead of saying the patient seems unwell. A fifteen percent revenue drop could be one channel's traffic, or a mobile conversion problem. Very different fixes.
```

Scene 5 — Signal versus noise

```text
Separating signal from noise. Small numbers swing randomly. Before reacting, compare against a longer baseline, like the same weekday average over several weeks. Check volume: a conversion rate from thirty sessions is unreliable. Check tracking changes and annotations: did measurement change rather than behavior? And consider seasonality and events: paydays, public holidays, school terms, religious festivals, sales events. Then, where a decision is significant and two things moved together, remember correlation isn't causation. Propose an experiment to confirm.
```

Scene 6 — Example 1: the revenue dip

```text
First example, with illustrative numbers. An online homeware store compares two equivalent weeks. Sessions went from fifty thousand to forty-nine thousand, minus two percent. Conversion rate from two percent to one point six, minus twenty percent. Average order value sixty to sixty-one, up two percent. Revenue down about twenty percent. So conversion rate explains the drop. By device, desktop is unchanged but mobile fell sharply, starting the day of a site release with a new address lookup. Funnels confirm drop-off at add shipping info on mobile. Recommendation: roll back or fix the component.
```

Scene 7 — Example 2: Riyadh travel leads

```text
Second example, a business case. A Riyadh travel agency's leads fall thirty percent in a week, illustratively. Decomposition shows sessions stable and the lead rate down only on Arabic-language pages. A path exploration shows Arabic users reaching the form and leaving. The cause: the form's Arabic validation message broke after a translation update, blocking submission. Fixing it restored the lead rate. The written insight took five lines, and the recommendation had an owner and a deadline.
```

Scene 8 — Watch me do it, part 1

```text
Watch me decompose in pandas. I export two comparable weeks with sessions, orders and revenue by device. The lesson's script groups by week, computes conversion rate as orders over sessions, and average order value as revenue over orders, and prints them. Instantly I see conversion rate is the factor that moved. Then it pivots by device and prints the change in conversion rate per device, sorted. Mobile sits at the top of the list. Ten lines of code, and I've done in a minute what used to take twenty clicks.
```

Scene 9 — Watch me do it, part 2

```text
Then I use AI for hypotheses, carefully. I give an assistant the context, the aggregated table with no personal data, and the known events: a release on the fourteenth, no campaign changes. I ask for three or four hypotheses ranked by fit, the G A four report or exploration that would confirm or refute each, and a what, so what, now what draft using only the numbers provided, with estimates marked illustrative. Inside G A four, Ask Advisor can do similar work from your property. Either way, I treat the answer as a list of hypotheses, and I verify each one myself.
```

Scene 10 — Presenting

```text
Now presenting. Use what, so what, now what. What: mobile conversion rate fell from two point one percent to one point three percent since the fourteenth. So what: illustratively, about one fifth of weekly revenue is at risk, and desktop is unaffected. Now what: roll back the address lookup, the developers, today; re-test the funnel, analytics, tomorrow. Lead with the conclusion. Put charts after the story, and only the charts that support it.
```

Scene 11 — Beyond GA4 and timeboxing

```text
Some questions can't be answered in G A four alone. Profit needs cost of goods. Lifetime value across offline channels needs your C R M. Lead quality needs sales outcomes. Good analysts know when to pull in other sources, like C R M exports, ad cost data and BigQuery joins, and they state clearly which numbers came from where. And they timebox. Agree the time with the decision-maker up front. A good-enough answer before the decision beats a perfect one after it.
```

Scene 12 — Common mistakes

```text
Common mistakes. Opening G A four without a question and looking for insights. Reporting every metric that changed instead of the ones that matter. Explaining random noise as meaningful trends. Recommendations without owners or deadlines. Forgetting to check whether tracking changed. Presenting AI-generated explanations as findings. And false precision, like revenue impact to the last unit when the inputs are estimates.
```

Scene 13 — Recap

```text
Recap. An insight explains, quantifies and points to action. Write hypotheses before opening reports. Decompose K P Is to find the factor and segment that moved. Separate signal from noise. Use AI and Ask Advisor to generate hypotheses, then verify everything. Present as what, so what, now what, conclusion first, with owners and dates.
```

Scene 14 — Try this now

```text
Try this now. Take a real K P I change from a site you know. Decompose it into factors and find the segment that moved most. Write three hypotheses before you look any further. Then write a what, so what, now what summary with an owner and a date for the action.
```

</details>

### 16. Attribution concepts and their limits

Lesson `attribution-concepts` · module “Attribution, dashboards and analytics operations” · target 8 min · 15 scenes · 1171 words · planned 8:29 (words ≈ 8:29) · status: **to produce**

| Term | Say |
|---|---|
| GA4 | G A four |
| ROAS | row-as |
| MMM | M M M |
| KPI | K P I |
| KPIs | K P eyes |
| DTC | D T C |
| Meridian | Meridian |
| Robyn | Robin |

| # | Time | Sec | Narration | On screen | Visual |
|---|---|---|---|---|---|
| 1 | 0:00 | 40 | A shopper sees your TikTok video, searches your brand on Google a week later, opens an email, and finally buys after clicking a retargeting ad. Who gets the credit? TikTok says TikTok. Google says Google. Your email tool says email. And your finance team says, why do these add up to three sales when we only made one? In this lecture you'll learn how attribution models work, what G A four actually offers, why platforms over-claim, the limits of click-based attribution, and how incrementality tests and marketing mix models give you better answers. | Attribution<br>• The credit question<br>• Models and what GA4 offers<br>• Platform self-attribution<br>• Limits of click-based attribution<br>• Incrementality and MMM | Layout: a customer journey line with four touchpoint icons; three platform badges each claim the single sale; a finance icon shows '3 claims, 1 sale'. 0-10s journey draws; 10-20s touchpoints appear; 20-30s three badges claim the sale; 30-40s finance icon shows mismatch. |
| 2 | 0:40 | 33 | Why does this matter? Because attribution decides budgets. If you trust last-click alone, you'll pour money into channels that close sales, like branded search and retargeting, and starve the channels that create demand, like video and social prospecting. If you trust every platform's self-reported numbers, you'll believe you made three times the sales you did. Getting attribution roughly right, and knowing its limits, is one of the most valuable things a marketing analyst can do. | Why it matters<br>• Attribution decides budgets<br>• Last-click starves demand creation<br>• Platform totals overstate reality | Layout: a budget pie shifts heavily toward 'closing' channels under a last-click lens, then rebalances under a broader lens. 0-11s pie under last-click; 11-22s lens changes; 22-33s pie rebalances. |
| 3 | 1:13 | 35 | Here's the core idea. Every attribution model is a simplification, and each answers a slightly different question. Think of a football goal. Who deserves credit? The striker who scored? The midfielder who made the pass? The defender who won the ball? Last click credits the striker. First click credits the defender. Linear shares credit equally. Position-based favors the first and last touches. Time decay favors recent touches. And data-driven attribution estimates credit algorithmically by comparing paths that did and didn't convert. | Models answer different questions<br>• Last click, first click<br>• Linear, position-based, time decay<br>• Data-driven<br>• The football goal analogy | Layout: a football pitch diagram: a defender wins the ball, a midfielder passes, a striker scores; credit percentages appear above players for each model in turn. 0-9s play animates; 9-18s last-click credit; 18-26s linear credit; 26-35s data-driven credit. |
| 4 | 1:48 | 38 | What does G A four offer? Data-driven attribution is the default reporting model for key events, with paid and organic last click, and a Google paid channels last-click variant, as alternatives. Google retired first click, linear, time decay and position-based from G A four in twenty twenty-three, but you should still understand them, because other platforms and analysts use them. Acquisition reports use session and first-user dimensions, not multi-touch credit. The attribution area shows model comparison and conversion paths, and lookback windows are configurable in property settings. | What GA4 offers<br>• Default: data-driven<br>• Alternatives: last-click variants<br>• Rule-based models retired in 2023<br>• Acquisition reports aren't multi-touch<br>• Model comparison, paths, lookback windows | Layout: a GA4-style settings card shows the attribution model dropdown with data-driven selected; retired models appear grayed; a model comparison table appears beside it. 0-13s settings card; 13-25s retired models gray; 25-38s comparison table appears. |
| 5 | 2:26 | 35 | Now platform self-attribution. Every ad platform reports conversions using its own rules, typically including view-through conversions and its own click windows. Meta, TikTok, Google Ads and others will often each claim the same sale. So platform totals usually add up to more than your actual orders. Use platform numbers for in-platform optimization, like which ad set or creative performs best within that platform. Use a neutral source, like G A four, your backend, or a modeling approach, for cross-channel budget decisions. | Platform self-attribution<br>• Own rules, view-through, own windows<br>• Totals exceed real orders<br>• Platform data: in-platform optimization<br>• Neutral source: cross-channel budgets | Layout: three platform dashboards each show a conversion count; stacked together they exceed a backend orders bar. 0-12s three dashboards; 12-23s counts stack; 23-35s exceed backend bar. |
| 6 | 3:01 | 30 | And the limits of click-based attribution. It can't see impressions that influenced people without a click: a billboard, a podcast, an unclicked video. It loses users who deny consent, switch devices or browsers. And it confuses correlation with causation. Retargeting may get credit for sales that would have happened anyway. Data-driven attribution is smarter about sharing credit, but it's still built on observed clicks. It isn't proof of causality. | Limits of click-based attribution<br>• Blind to non-click influence<br>• Consent and device gaps<br>• Correlation, not causation<br>• Data-driven isn't causal proof | Layout: a journey map where a billboard and podcast icon have dotted, invisible lines; a consent-denied user fades out; a retargeting icon wears a 'credit' badge with a question mark. 0-10s invisible touchpoints; 10-20s user fades; 20-30s retargeting badge with question mark. |
| 7 | 3:31 | 37 | So what gives better answers? Incrementality testing asks: what would have happened without this activity? Methods include geo holdouts, where you turn a channel off in comparable regions, conversion lift studies offered by platforms, and on and off tests. They measure causal impact. Marketing mix modeling uses aggregated historical data, like spend, sales, seasonality and prices, to estimate each channel's contribution. Open-source tools such as Google's Meridian and Meta's Robyn have made it more accessible, but it needs enough history, variation in spend, and statistical expertise. | Better answers<br>• Incrementality: what if we hadn't?<br>• Geo holdouts, lift studies, on/off tests<br>• MMM: aggregated history<br>• Meridian, Robyn<br>• Needs history and expertise | Layout: a map split into test and control regions with a lift bar between them; a separate MMM panel shows spend lines feeding a contribution chart. 0-12s test and control map; 12-25s lift bar; 25-37s MMM panel. |
| 8 | 4:08 | 40 | Two practical tools inside G A four help you have better attribution conversations. The conversion paths report shows the sequences of channels people touched before a key event, so you can see whether social or video usually appears early and search late. And the model comparison report shows how much credit each channel gets under data-driven versus last click. When a channel gains a lot of credit under data-driven, treat that as a question, not a verdict. Is that influence incremental? That's exactly the kind of question a holdout test can answer. | Paths and model comparison<br>• Conversion paths: early versus late channels<br>• Model comparison: credit shifts<br>• Big shifts are questions, not verdicts | Layout: a conversion paths ribbon shows channel icons in sequence; a model comparison table highlights one channel's credit rising under data-driven with a question mark leading to a test icon. 0-13s paths ribbon; 13-27s comparison table; 27-40s question mark to test icon. |
| 9 | 4:48 | 36 | First example, with illustrative results. A fashion brand sees retargeting with a strong return on ad spend in its ad platform and a solid share of last-click revenue in G A four. The team runs a holdout: a random portion of the retargeting audience is excluded for four weeks. Purchase rates in the holdout are almost identical to the exposed group. The true incremental effect is small. Most of those buyers would have returned anyway. Budget shifts toward prospecting that creates new demand. | Example 1: is retargeting worth it?<br>• Strong platform ROAS<br>• Four-week audience holdout<br>• Similar purchase rates<br>• Small incremental effect; shift budget | Layout: two groups of shopper icons, exposed and holdout; their purchase-rate bars end up nearly equal; a budget arrow moves from retargeting to prospecting. 0-12s two groups; 12-24s bars grow nearly equal; 24-36s budget arrow moves. |
| 10 | 5:24 | 32 | Second example, a business case, also illustrative. A UK coffee subscription brand sees branded search with an outstanding return. It runs a two-week, geo-split pause of branded search in comparable regions, at a time when competitors weren't bidding on the brand name. Most clicks shift to the organic listing, and purchases barely change. The brand reduces branded bids, keeps a small defensive budget, and rechecks quarterly, because competitor behavior can change the answer. | Example 2: branded search<br>• Geo-split pause<br>• Clicks shift to organic<br>• Purchases barely change<br>• Reduce bids, recheck quarterly | Layout: a search results mock where a paid brand ad disappears and the organic listing catches the clicks; a purchases line stays flat; a quarterly calendar icon. 0-8s paid ad disappears; 8-16s organic catches clicks; 16-24s purchases flat; 24-32s quarterly icon. |
| 11 | 5:56 | 31 | Watch me design a geo holdout on one page. Question: does paid social prospecting create incremental purchases in Saudi Arabia? Units: twelve comparable cities or regions. Assignment: randomly split six and six, test with ads on, control with ads paused. Baseline: eight weeks before, check that test and control trends move together. Duration: four to six weeks, enough purchases per group. Metric: purchases per thousand residents from the backend, not platform-reported. | Watch me do it, part 1<br>• Question<br>• 12 comparable regions<br>• Random 6 vs 6<br>• 8-week baseline check<br>• Backend metric | Layout: a one-page test plan fills section by section; a regional map colors six regions blue (test) and six gray (control). 0-8s question and units fill; 8-16s map colors regions; 16-23s baseline and duration fill; 23-31s metric fills. |
| 12 | 6:27 | 36 | Then the guardrails and decision rules. No other big changes in either group, and log promotions and holidays, like Ramadan or White Friday, which fall differently each year. The readout compares the change in test against the change in control versus the pre-period, with an uncertainty range. And the decision, scale, hold or cut, is agreed before the test starts, so nobody reinterprets the result afterward. That last line is the one most teams skip, and it's the one that makes tests trustworthy. | Watch me do it, part 2<br>• Guardrails and event log<br>• Difference-in-differences with a range<br>• Decision rule agreed upfront | Layout: a guardrail fence around both groups; a readout chart with two lines and a shaded uncertainty band; a signed decision card stamped 'agreed before start'. 0-12s guardrail fence; 12-24s readout chart with band; 24-36s decision card stamped. |
| 13 | 7:03 | 22 | Common mistakes. Summing platform-reported conversions and presenting them as total sales. Judging upper-funnel channels by last click alone. Switching attribution models frequently and comparing numbers across the switch. Treating data-driven attribution as proof of causality. And running tests without agreeing the decision rule first, then arguing about what the result means. | Common mistakes<br>• Summing platform conversions<br>• Last click for upper funnel<br>• Frequent model switches<br>• Data-driven as proof<br>• No upfront decision rule | Layout: five warning cards drop. 0-11s cards 1 to 3; 11-22s cards 4 and 5. |
| 14 | 7:25 | 31 | A mature approach triangulates. Platform data for tactics within each platform. G A four attribution for directional cross-channel views. And experiments or marketing mix models for strategic budget decisions. When they disagree, that's not failure. It's information about what each method can and can't see. Use this template for every attribution conversation: which decision, which source, which model and window, what it can't see, and how we'll validate with a test. | Triangulate<br>• Platforms: tactics<br>• GA4: directional cross-channel<br>• Experiments and MMM: strategy<br>• Disagreement is information | Layout: a triangle with three corners labeled platforms, GA4, experiments/MMM; a decision point sits in the center. 0-10s triangle corners; 10-21s decision point; 21-31s template card. |
| 15 | 7:56 | 33 | Recap. All attribution models are simplifications that answer different questions. G A four defaults to data-driven, with last-click alternatives. Platforms self-attribute, so their totals exceed real orders. Click-based attribution can't prove causality. Use incrementality tests and marketing mix modeling to estimate causal impact, and triangulate. Try this now: for one channel you use, write the decision you need to make, the source and model you'd rely on, and a one-page holdout design to validate it. | Recap and try this now<br>• Models are simplifications<br>• GA4: data-driven default<br>• Platforms over-claim<br>• Test for causality; triangulate<br>• Design one holdout | Layout: recap icons then a blank one-page holdout template appears. 0-16s recap icons; 16-33s template appears. |

<details><summary>Narration per scene (paste into ElevenLabs)</summary>

Scene 1 — Attribution

```text
A shopper sees your TikTok video, searches your brand on Google a week later, opens an email, and finally buys after clicking a retargeting ad. Who gets the credit? TikTok says TikTok. Google says Google. Your email tool says email. And your finance team says, why do these add up to three sales when we only made one? In this lecture you'll learn how attribution models work, what G A four actually offers, why platforms over-claim, the limits of click-based attribution, and how incrementality tests and marketing mix models give you better answers.
```

Scene 2 — Why it matters

```text
Why does this matter? Because attribution decides budgets. If you trust last-click alone, you'll pour money into channels that close sales, like branded search and retargeting, and starve the channels that create demand, like video and social prospecting. If you trust every platform's self-reported numbers, you'll believe you made three times the sales you did. Getting attribution roughly right, and knowing its limits, is one of the most valuable things a marketing analyst can do.
```

Scene 3 — Models answer different questions

```text
Here's the core idea. Every attribution model is a simplification, and each answers a slightly different question. Think of a football goal. Who deserves credit? The striker who scored? The midfielder who made the pass? The defender who won the ball? Last click credits the striker. First click credits the defender. Linear shares credit equally. Position-based favors the first and last touches. Time decay favors recent touches. And data-driven attribution estimates credit algorithmically by comparing paths that did and didn't convert.
```

Scene 4 — What GA4 offers

```text
What does G A four offer? Data-driven attribution is the default reporting model for key events, with paid and organic last click, and a Google paid channels last-click variant, as alternatives. Google retired first click, linear, time decay and position-based from G A four in twenty twenty-three, but you should still understand them, because other platforms and analysts use them. Acquisition reports use session and first-user dimensions, not multi-touch credit. The attribution area shows model comparison and conversion paths, and lookback windows are configurable in property settings.
```

Scene 5 — Platform self-attribution

```text
Now platform self-attribution. Every ad platform reports conversions using its own rules, typically including view-through conversions and its own click windows. Meta, TikTok, Google Ads and others will often each claim the same sale. So platform totals usually add up to more than your actual orders. Use platform numbers for in-platform optimization, like which ad set or creative performs best within that platform. Use a neutral source, like G A four, your backend, or a modeling approach, for cross-channel budget decisions.
```

Scene 6 — Limits of click-based attribution

```text
And the limits of click-based attribution. It can't see impressions that influenced people without a click: a billboard, a podcast, an unclicked video. It loses users who deny consent, switch devices or browsers. And it confuses correlation with causation. Retargeting may get credit for sales that would have happened anyway. Data-driven attribution is smarter about sharing credit, but it's still built on observed clicks. It isn't proof of causality.
```

Scene 7 — Better answers

```text
So what gives better answers? Incrementality testing asks: what would have happened without this activity? Methods include geo holdouts, where you turn a channel off in comparable regions, conversion lift studies offered by platforms, and on and off tests. They measure causal impact. Marketing mix modeling uses aggregated historical data, like spend, sales, seasonality and prices, to estimate each channel's contribution. Open-source tools such as Google's Meridian and Meta's Robyn have made it more accessible, but it needs enough history, variation in spend, and statistical expertise.
```

Scene 8 — Paths and model comparison

```text
Two practical tools inside G A four help you have better attribution conversations. The conversion paths report shows the sequences of channels people touched before a key event, so you can see whether social or video usually appears early and search late. And the model comparison report shows how much credit each channel gets under data-driven versus last click. When a channel gains a lot of credit under data-driven, treat that as a question, not a verdict. Is that influence incremental? That's exactly the kind of question a holdout test can answer.
```

Scene 9 — Example 1: is retargeting worth it?

```text
First example, with illustrative results. A fashion brand sees retargeting with a strong return on ad spend in its ad platform and a solid share of last-click revenue in G A four. The team runs a holdout: a random portion of the retargeting audience is excluded for four weeks. Purchase rates in the holdout are almost identical to the exposed group. The true incremental effect is small. Most of those buyers would have returned anyway. Budget shifts toward prospecting that creates new demand.
```

Scene 10 — Example 2: branded search

```text
Second example, a business case, also illustrative. A UK coffee subscription brand sees branded search with an outstanding return. It runs a two-week, geo-split pause of branded search in comparable regions, at a time when competitors weren't bidding on the brand name. Most clicks shift to the organic listing, and purchases barely change. The brand reduces branded bids, keeps a small defensive budget, and rechecks quarterly, because competitor behavior can change the answer.
```

Scene 11 — Watch me do it, part 1

```text
Watch me design a geo holdout on one page. Question: does paid social prospecting create incremental purchases in Saudi Arabia? Units: twelve comparable cities or regions. Assignment: randomly split six and six, test with ads on, control with ads paused. Baseline: eight weeks before, check that test and control trends move together. Duration: four to six weeks, enough purchases per group. Metric: purchases per thousand residents from the backend, not platform-reported.
```

Scene 12 — Watch me do it, part 2

```text
Then the guardrails and decision rules. No other big changes in either group, and log promotions and holidays, like Ramadan or White Friday, which fall differently each year. The readout compares the change in test against the change in control versus the pre-period, with an uncertainty range. And the decision, scale, hold or cut, is agreed before the test starts, so nobody reinterprets the result afterward. That last line is the one most teams skip, and it's the one that makes tests trustworthy.
```

Scene 13 — Common mistakes

```text
Common mistakes. Summing platform-reported conversions and presenting them as total sales. Judging upper-funnel channels by last click alone. Switching attribution models frequently and comparing numbers across the switch. Treating data-driven attribution as proof of causality. And running tests without agreeing the decision rule first, then arguing about what the result means.
```

Scene 14 — Triangulate

```text
A mature approach triangulates. Platform data for tactics within each platform. G A four attribution for directional cross-channel views. And experiments or marketing mix models for strategic budget decisions. When they disagree, that's not failure. It's information about what each method can and can't see. Use this template for every attribution conversation: which decision, which source, which model and window, what it can't see, and how we'll validate with a test.
```

Scene 15 — Recap and try this now

```text
Recap. All attribution models are simplifications that answer different questions. G A four defaults to data-driven, with last-click alternatives. Platforms self-attribute, so their totals exceed real orders. Click-based attribution can't prove causality. Use incrementality tests and marketing mix modeling to estimate causal impact, and triangulate. Try this now: for one channel you use, write the decision you need to make, the source and model you'd rely on, and a one-page holdout design to validate it.
```

</details>

### 17. Dashboards that drive decisions

Lesson `dashboards-that-drive-decisions` · module “Attribution, dashboards and analytics operations” · target 8 min · 15 scenes · 1179 words · planned 8:33 (words ≈ 8:33) · status: **to produce**

| Term | Say |
|---|---|
| GA4 | G A four |
| ROAS | row-as |
| MMM | M M M |
| KPI | K P I |
| KPIs | K P eyes |
| BI | B I |
| CRM | C R M |
| API | A P I |

| # | Time | Sec | Narration | On screen | Visual |
|---|---|---|---|---|---|
| 1 | 0:00 | 37 | I've seen a dashboard with forty-three charts. Nobody could tell me what decision it supported. It was beautiful, it was expensive, and it was ignored. In this lecture you'll learn to build dashboards that drive decisions: how to design for a specific audience, the tools and connectors in twenty twenty-six, including Google's Data Studio, which you may know as Looker Studio, the design principles that make dashboards readable, how to keep them fast with BigQuery, and a one-page spec you can use for every build. | Dashboards that drive decisions<br>• Design for an audience<br>• Tools in 2026: Data Studio and more<br>• Design principles<br>• Speed with BigQuery<br>• A one-page spec | Layout: a crowded dashboard with dozens of tiny charts shrinks into a clean one-page layout with five tiles and a commentary box. 0-9s crowded dashboard; 9-18s shrinks and reorganizes; 18-28s clean layout; 28-37s title builds. |
| 2 | 0:37 | 35 | Why does this matter? Because a dashboard is a decision tool, not a data dump. The best ones answer a small set of recurring questions for a specific audience, at a glance, and show when something needs attention. If nobody would act differently after seeing a chart, it doesn't belong. Dashboards are also how your work is judged. A clear dashboard with honest commentary builds trust. A confusing one makes people doubt the data, even when the data is right. | Why it matters<br>• Decision tool, not data dump<br>• Recurring questions, one audience<br>• Remove charts nobody acts on<br>• Clarity builds trust | Layout: a chart card gets a question label 'would anyone act differently?'; charts that fail fade out; remaining charts align. 0-12s question label; 12-23s failing charts fade; 23-35s remaining charts align. |
| 3 | 1:12 | 38 | Start with the audience. Leadership asks: are we on track, and where should we invest? They need four to six K P Is versus target, trends and a channel summary, weekly or monthly. Channel managers ask what's working in their channel, daily or weekly, with spend, key events, cost per key event and creative breakdowns. Content and product teams need landing pages, engagement and funnels. And agency clients ask: is my money well spent? Outcomes, commentary and next steps, monthly. One dashboard can't serve all of them well. | Audiences<br>• Leadership: on track? invest where?<br>• Channel managers: what's working?<br>• Content/product: which pages and features?<br>• Clients: money well spent? | Layout: four audience icons each with a speech bubble question; each connects to a different dashboard thumbnail. 0-10s leadership; 10-19s channel managers; 19-28s content/product; 28-38s clients. |
| 4 | 1:50 | 40 | Now tools. Google's free dashboarding tool is Data Studio. It was called Looker Studio from twenty twenty-two until Google renamed it back to Data Studio in April twenty twenty-six, and Looker Studio Pro became Data Studio Pro. Existing reports and links carried over automatically. It connects natively to G A four, Google Ads, Search Console, Sheets and BigQuery. Other tools, like Power B I, Tableau or Metabase, can use G A four through connectors or the BigQuery export. The concepts are the same everywhere: data sources, calculated fields, blending, and filters and controls. | Tools in 2026<br>• Data Studio (formerly Looker Studio, renamed April 2026)<br>• Native GA4, Ads, Search Console, Sheets, BigQuery<br>• Power BI, Tableau, Metabase via connectors<br>• Same concepts everywhere | Layout: a name badge 'Looker Studio' flips to 'Data Studio' with an April 2026 date tag; connector lines draw to GA4, Ads, Search Console, Sheets and BigQuery icons. 0-13s badge flips; 13-27s date tag; 27-40s connector lines draw. |
| 5 | 2:30 | 34 | One practical issue: quotas. The G A four Data A P I, which powers the live connector, has quotas. Dashboards with many charts and many viewers can hit them, especially on Monday mornings when everyone opens the report, producing errors instead of numbers. Mitigations include fewer charts per page, cached extracts, or routing through a small pre-aggregated BigQuery table. For any dashboard with more than a handful of regular viewers, the BigQuery route is usually the grown-up option. | Quotas and speed<br>• Live connector uses the Data API<br>• Many viewers can hit quotas<br>• Fewer charts, extracts, or BigQuery summaries<br>• BigQuery for busy dashboards | Layout: many viewer icons hit a dashboard at once; a quota gauge redlines and error tiles appear; switching to a BigQuery summary table makes the gauge calm. 0-8s viewers arrive; 8-17s quota gauge redlines; 17-26s errors appear; 26-34s BigQuery summary calms it. |
| 6 | 3:04 | 37 | Here are the design principles. Top-left is prime real estate, so put the most important K P Is there. Every K P I needs context: versus target, previous period or last year. Show rates and totals together. Use consistent colors and definitions, with a glossary page. Minimize chart types: scorecards, line charts for trends, bar charts for comparisons, tables for detail. And annotate campaign launches, site releases and tracking changes. Think of it like a car dashboard again: few dials, clear warning lights, nothing decorative. | Design principles<br>• Top-left for top KPIs<br>• Context for every KPI<br>• Rates with totals<br>• Consistent definitions<br>• Few chart types<br>• Annotations | Layout: a wireframe builds: scorecards across the top-left, a trend line with annotation markers, a channel table and landing page table below. 0-9s scorecards; 9-18s trend with annotations; 18-28s tables; 28-37s glossary tab. |
| 7 | 3:41 | 28 | And the most important element on any dashboard is the commentary box. What happened, why, and what we're doing next. Numbers without narrative leave stakeholders to invent their own explanations, and they usually invent the wrong ones. Clients read the commentary first. The charts support it. If you only have ten minutes before a meeting, spend them on the commentary, not on another chart. | The commentary box<br>• What happened<br>• Why<br>• What we're doing next<br>• Charts support the story | Layout: a commentary box at the bottom of the wireframe grows and highlights while three bullet lines type in. 0-14s box highlights; 14-28s three lines type. |
| 8 | 4:09 | 41 | Three techniques make dashboards smarter without making them busier. Calculated fields, like key events divided by sessions, or a CASE statement that groups messy campaign names into clean families. Blending, which joins sources, like ad spend from each platform with G A four key events, matched on date and campaign, so you can show cost per key event. And controls, like date, country and channel selectors, so one page serves several questions. Label every blended number with its sources, because a cost per acquisition built from two systems will never match either system exactly. | Smarter, not busier<br>• Calculated fields<br>• Blending spend with outcomes<br>• Controls for viewers<br>• Label blended sources | Layout: a calculated field formula types in; two data source cylinders (ad spend, GA4) join on date and campaign into a cost-per-key-event tile; filter controls appear at the top. 0-10s formula types; 10-20s sources join; 20-31s tile appears with source label; 31-41s controls appear. |
| 9 | 4:50 | 40 | First example, a simple one. A small agency in Manchester manages paid social and search for an online florist. Its monthly client dashboard has four pages. Outcomes: orders, revenue and cost per order versus target, with spend blended from ad platforms. Channels: paid social, organic search and email, each with sessions, key event rate and revenue. Search: Search Console clicks and top landing pages with key events. And a glossary and data notes page with definitions, consent caveats and the attribution model used. Each month, three commentary bullets and one recommendation. | Example 1: an agency client dashboard<br>• Outcomes vs target<br>• Channels<br>• Search<br>• Glossary and data notes<br>• Three bullets, one recommendation | Layout: four page thumbnails fan out; the glossary page shows a 'consent caveats' note; a commentary box with three bullets and a recommendation appears. 0-13s four pages fan out; 13-27s glossary note; 27-40s commentary box. |
| 10 | 5:30 | 36 | Second example, a business case. A Karachi agency's client dashboards were built on the live G A four connector. Every Monday morning, when fifteen account managers opened them at once, charts failed. The team moved each client to a nightly BigQuery summary table plus a Data Studio report per client, with a data freshness note saying updated daily at seven. Monday failures stopped, load times dropped, and they could finally blend ad spend and C R M outcomes on the same page. | Example 2: Karachi agency<br>• Monday quota failures<br>• Nightly BigQuery summaries<br>• Freshness note<br>• Faster, blendable dashboards | Layout: a calendar Monday with error icons; a nightly moon icon runs a query; dashboards load green with a 'updated daily 07:00' tag. 0-12s Monday errors; 12-24s nightly query; 24-36s green dashboards with freshness tag. |
| 11 | 6:06 | 41 | Watch me build the summary table. In BigQuery, I create or replace a table called daily channel K P Is. For each date and channel, where channel comes from the session traffic source last click record's default channel group, I count session starts as sessions, count purchases and generate lead events as key events, and sum purchase revenue. I cover the last four hundred days. Then I schedule it daily. And I note on the dashboard that these export-based counts will differ from the G A four interface, which uses different identity and modeling rules. | Watch me do it, part 1<br>• Create or replace a summary table<br>• Date x channel<br>• Sessions, key events, revenue<br>• 400 days, scheduled daily<br>• Label: export-based counts | Layout: SQL editor highlights the CREATE TABLE, the channel field, and the three aggregates; a schedule clock attaches; a small label 'export-based' appears on a dashboard tile. 0-10s create table highlights; 10-20s channel field; 20-31s aggregates; 31-41s schedule and label. |
| 12 | 6:47 | 36 | Then I write the spec before building a single chart. Audience: leadership. Three questions: are we on track versus target, which channels moved, and what are we doing about it? K P Is with comparisons: revenue versus target, key events versus previous period, key event rate versus last year, cost per acquisition versus target. Filters: date, country and channel. Sources, with the BigQuery table, ad spend exports and C R M. Refresh time. A definitions page. And an owner with a monthly review. | Watch me do it, part 2<br>• Audience and three questions<br>• KPIs with comparisons<br>• Filters and sources<br>• Refresh, definitions, owner | Layout: a one-page spec document fills section by section; a dashboard wireframe assembles to its right following the spec. 0-9s audience and questions; 9-18s KPIs and comparisons; 18-27s filters and sources; 27-36s wireframe assembles. |
| 13 | 7:23 | 24 | Common mistakes. Thirty charts on one page. No targets or comparisons, so nothing signals good or bad. Mixing attribution sources without labeling them. Dashboards that silently break when quotas are reached. Metrics named inconsistently, like users on one page and visitors on another. And no commentary, which leaves the most important part of the dashboard empty. | Common mistakes<br>• Too many charts<br>• No comparisons<br>• Unlabeled sources<br>• Quota breakage<br>• Inconsistent names<br>• No commentary | Layout: six warning cards in a grid. 0-12s cards 1 to 3; 12-24s cards 4 to 6. |
| 14 | 7:47 | 23 | Recap. A dashboard is a decision tool for a specific audience. Every K P I needs context. Label every source and attribution model. Use Data Studio, formerly Looker Studio, or your BI tool of choice, and route busy dashboards through BigQuery summaries. Write a spec first, and never ship without a commentary box. | Recap<br>• One audience, few questions<br>• Context for every KPI<br>• Label sources<br>• BigQuery for busy dashboards<br>• Spec first, commentary always | Layout: recap card with the clean wireframe icon and checkmarks. 0-12s wireframe icon; 12-23s checkmarks. |
| 15 | 8:10 | 23 | Try this now. Sketch a one-page dashboard for a real audience using the wireframe and spec from the lesson. List each K P I, its comparison, and the question it answers. Write a sample commentary box for last month. If a chart doesn't connect to one of your three questions, remove it. | Try this now<br>• Spec and sketch one page<br>• KPI, comparison, question<br>• Sample commentary<br>• Remove unconnected charts | Layout: spec card and sketch appear side by side; one chart without a question link fades out. 0-12s spec and sketch; 12-23s unconnected chart fades. |

<details><summary>Narration per scene (paste into ElevenLabs)</summary>

Scene 1 — Dashboards that drive decisions

```text
I've seen a dashboard with forty-three charts. Nobody could tell me what decision it supported. It was beautiful, it was expensive, and it was ignored. In this lecture you'll learn to build dashboards that drive decisions: how to design for a specific audience, the tools and connectors in twenty twenty-six, including Google's Data Studio, which you may know as Looker Studio, the design principles that make dashboards readable, how to keep them fast with BigQuery, and a one-page spec you can use for every build.
```

Scene 2 — Why it matters

```text
Why does this matter? Because a dashboard is a decision tool, not a data dump. The best ones answer a small set of recurring questions for a specific audience, at a glance, and show when something needs attention. If nobody would act differently after seeing a chart, it doesn't belong. Dashboards are also how your work is judged. A clear dashboard with honest commentary builds trust. A confusing one makes people doubt the data, even when the data is right.
```

Scene 3 — Audiences

```text
Start with the audience. Leadership asks: are we on track, and where should we invest? They need four to six K P Is versus target, trends and a channel summary, weekly or monthly. Channel managers ask what's working in their channel, daily or weekly, with spend, key events, cost per key event and creative breakdowns. Content and product teams need landing pages, engagement and funnels. And agency clients ask: is my money well spent? Outcomes, commentary and next steps, monthly. One dashboard can't serve all of them well.
```

Scene 4 — Tools in 2026

```text
Now tools. Google's free dashboarding tool is Data Studio. It was called Looker Studio from twenty twenty-two until Google renamed it back to Data Studio in April twenty twenty-six, and Looker Studio Pro became Data Studio Pro. Existing reports and links carried over automatically. It connects natively to G A four, Google Ads, Search Console, Sheets and BigQuery. Other tools, like Power B I, Tableau or Metabase, can use G A four through connectors or the BigQuery export. The concepts are the same everywhere: data sources, calculated fields, blending, and filters and controls.
```

Scene 5 — Quotas and speed

```text
One practical issue: quotas. The G A four Data A P I, which powers the live connector, has quotas. Dashboards with many charts and many viewers can hit them, especially on Monday mornings when everyone opens the report, producing errors instead of numbers. Mitigations include fewer charts per page, cached extracts, or routing through a small pre-aggregated BigQuery table. For any dashboard with more than a handful of regular viewers, the BigQuery route is usually the grown-up option.
```

Scene 6 — Design principles

```text
Here are the design principles. Top-left is prime real estate, so put the most important K P Is there. Every K P I needs context: versus target, previous period or last year. Show rates and totals together. Use consistent colors and definitions, with a glossary page. Minimize chart types: scorecards, line charts for trends, bar charts for comparisons, tables for detail. And annotate campaign launches, site releases and tracking changes. Think of it like a car dashboard again: few dials, clear warning lights, nothing decorative.
```

Scene 7 — The commentary box

```text
And the most important element on any dashboard is the commentary box. What happened, why, and what we're doing next. Numbers without narrative leave stakeholders to invent their own explanations, and they usually invent the wrong ones. Clients read the commentary first. The charts support it. If you only have ten minutes before a meeting, spend them on the commentary, not on another chart.
```

Scene 8 — Smarter, not busier

```text
Three techniques make dashboards smarter without making them busier. Calculated fields, like key events divided by sessions, or a CASE statement that groups messy campaign names into clean families. Blending, which joins sources, like ad spend from each platform with G A four key events, matched on date and campaign, so you can show cost per key event. And controls, like date, country and channel selectors, so one page serves several questions. Label every blended number with its sources, because a cost per acquisition built from two systems will never match either system exactly.
```

Scene 9 — Example 1: an agency client dashboard

```text
First example, a simple one. A small agency in Manchester manages paid social and search for an online florist. Its monthly client dashboard has four pages. Outcomes: orders, revenue and cost per order versus target, with spend blended from ad platforms. Channels: paid social, organic search and email, each with sessions, key event rate and revenue. Search: Search Console clicks and top landing pages with key events. And a glossary and data notes page with definitions, consent caveats and the attribution model used. Each month, three commentary bullets and one recommendation.
```

Scene 10 — Example 2: Karachi agency

```text
Second example, a business case. A Karachi agency's client dashboards were built on the live G A four connector. Every Monday morning, when fifteen account managers opened them at once, charts failed. The team moved each client to a nightly BigQuery summary table plus a Data Studio report per client, with a data freshness note saying updated daily at seven. Monday failures stopped, load times dropped, and they could finally blend ad spend and C R M outcomes on the same page.
```

Scene 11 — Watch me do it, part 1

```text
Watch me build the summary table. In BigQuery, I create or replace a table called daily channel K P Is. For each date and channel, where channel comes from the session traffic source last click record's default channel group, I count session starts as sessions, count purchases and generate lead events as key events, and sum purchase revenue. I cover the last four hundred days. Then I schedule it daily. And I note on the dashboard that these export-based counts will differ from the G A four interface, which uses different identity and modeling rules.
```

Scene 12 — Watch me do it, part 2

```text
Then I write the spec before building a single chart. Audience: leadership. Three questions: are we on track versus target, which channels moved, and what are we doing about it? K P Is with comparisons: revenue versus target, key events versus previous period, key event rate versus last year, cost per acquisition versus target. Filters: date, country and channel. Sources, with the BigQuery table, ad spend exports and C R M. Refresh time. A definitions page. And an owner with a monthly review.
```

Scene 13 — Common mistakes

```text
Common mistakes. Thirty charts on one page. No targets or comparisons, so nothing signals good or bad. Mixing attribution sources without labeling them. Dashboards that silently break when quotas are reached. Metrics named inconsistently, like users on one page and visitors on another. And no commentary, which leaves the most important part of the dashboard empty.
```

Scene 14 — Recap

```text
Recap. A dashboard is a decision tool for a specific audience. Every K P I needs context. Label every source and attribution model. Use Data Studio, formerly Looker Studio, or your BI tool of choice, and route busy dashboards through BigQuery summaries. Write a spec first, and never ship without a commentary box.
```

Scene 15 — Try this now

```text
Try this now. Sketch a one-page dashboard for a real audience using the wireframe and spec from the lesson. List each K P I, its comparison, and the question it answers. Write a sample commentary box for last month. If a chart doesn't connect to one of your three questions, remove it.
```

</details>

### 18. Running analytics as an ongoing practice

Lesson `analytics-operating-rhythm` · module “Attribution, dashboards and analytics operations” · target 8 min · 14 scenes · 1151 words · planned 8:22 (words ≈ 8:20) · status: **to produce**

| Term | Say |
|---|---|
| GA4 | G A four |
| ROAS | row-as |
| MMM | M M M |
| KPI | K P I |
| KPIs | K P eyes |
| RACI | racy |
| CMP | C M P |
| PII | P I I |
| UTM | U T M |
| API | A P I |

| # | Time | Sec | Narration | On screen | Visual |
|---|---|---|---|---|---|
| 1 | 0:00 | 37 | Here's a hard truth about analytics. You can build a perfect implementation in January, and by June it will be quietly broken. Sites change, campaigns launch, developers refactor, new tags appear, and people leave. Analytics decays without ownership. In this lecture you'll learn how to run analytics like a product: roles and responsibilities, a daily, weekly, monthly and quarterly rhythm, the change log, how to audit an inherited property, how to automate the boring parts, and how to build a culture where data is trusted. | The analytics operating rhythm<br>• Analytics decays without owners<br>• Roles and responsibilities<br>• The rhythm<br>• Change logs and audits<br>• Automation and culture | Layout: a pristine January implementation diagram slowly accumulates cracks by June as small change icons (release, campaign, new tag, leaver) hit it. 0-9s January diagram; 9-18s change icons hit it; 18-28s cracks appear by June; 28-37s title builds. |
| 2 | 0:37 | 32 | Why does this matter? Because every number your organization trusts depends on dozens of small things staying correct: tags, triggers, consent settings, UTM habits, definitions. Without routines, those things drift, and nobody notices until a big decision is made on a broken number. An operating rhythm turns analytics from a project into a product, with owners, routines and documentation. It's not glamorous. It's the reason some teams trust their data and others don't. | Why it matters<br>• Trust depends on many small things<br>• Drift goes unnoticed<br>• Project versus product | Layout: a tower of small blocks labeled tags, consent, UTMs, definitions; without maintenance it wobbles; with a rhythm icon beside it, it stays upright. 0-11s tower builds; 11-21s wobbles; 21-32s rhythm icon steadies it. |
| 3 | 1:09 | 39 | Start with roles. Think of a simple RACI: who's responsible, accountable, consulted and informed. The measurement plan: the analyst is responsible, the marketing lead accountable. The tracking plan and tags: the analyst or implementer responsible, the analytics owner accountable, developers consulted. The data layer: developers responsible, the tech lead accountable. Consent and privacy: analyst and legal responsible, the data protection lead accountable. Dashboards: the analyst responsible, the marketing lead accountable. In a small business, one person holds several roles. The point is that every activity has a named owner. | Roles<br>• Measurement plan<br>• Tracking plan and tags<br>• Data layer<br>• Consent and privacy<br>• Dashboards<br>• Every activity has an owner | Layout: a RACI grid fills with role initials; one empty cell flashes and then gets a name. 0-13s grid draws; 13-26s cells fill; 26-39s empty cell flashes and fills. |
| 4 | 1:48 | 37 | Now the rhythm itself. Daily, five minutes: a sanity check during active campaigns, and alert review. Weekly, thirty minutes: K P Is versus target, channel and landing page movers, anomaly notes. Monthly, two hours: the QA script on top journeys, the PII audit, the Unassigned review, dashboard commentary, and an access review for leavers. Quarterly, one day: measurement plan review, tracking plan clean-up, consent re-audit, the attribution and experiment roadmap, and stakeholder feedback. And on every release: developer notice, QA script, change log, annotation. | The rhythm<br>• Daily: 5 min<br>• Weekly: 30 min<br>• Monthly: 2 hrs<br>• Quarterly: 1 day<br>• Every release: notice, QA, log | Layout: a circular calendar with four rings (daily, weekly, monthly, quarterly) and a release lightning icon on the edge. 0-9s daily ring; 9-18s weekly ring; 18-28s monthly and quarterly rings; 28-37s release icon. |
| 5 | 2:25 | 36 | The cheapest, highest-value analytics artifact is the change log. Date, change, who, reason, and data impact. For example: the second of March, generate lead moved to a data layer push, by Hassan, because the thank-you page created false positives, and leads dropped as expected. When a number moves unexpectedly, the change log is the first place to look. Think of it like a flight recorder. You hope you never need it, but when something goes wrong, it tells you exactly what happened. | The change log<br>• Date, change, who, reason, impact<br>• First place to look<br>• A flight recorder for data | Layout: a change log table fills three rows; a chart with an unexpected dip draws a dotted line to a matching change log row. 0-12s log rows fill; 12-24s chart dip; 24-36s dotted line connects to log row. |
| 6 | 3:01 | 38 | Agencies and new hires often inherit a messy property. Audit it in six areas. Access and ownership: who owns the account, who has admin, remove stale users. Configuration: retention, filters, referrals, cross-domain, key events, links. Implementation: duplicate tags, the container's state, data layer quality. Data quality: not set rates, Unassigned share, personal data leaks, revenue reconciliation. Consent: pre-consent firing, the C M P setup, non-Google tags. And reporting: which dashboards exist and who uses them. Score each red, amber or green, then prioritize by impact and effort. | Auditing an inherited property<br>• Access and ownership<br>• Configuration<br>• Implementation<br>• Data quality<br>• Consent<br>• Reporting<br>• Score and prioritize | Layout: six audit tiles each get a red, amber or green dot; they then sort into an impact-versus-effort grid. 0-19s six tiles with dots; 19-38s tiles move into impact/effort grid. |
| 7 | 3:39 | 39 | Now automation, which makes the rhythm sustainable. Daily, a BigQuery scheduled health query posts anomalies to a team chat. Weekly, a Data A P I script pulls the K P I table into a sheet, and an AI assistant drafts the first version of the commentary from the aggregated numbers, which the analyst edits and approves. Monthly, the PII scan and UTM leak query run on their own. And quarterly, a simple query lists every event name seen in the last thirty days, which you compare with the tracking plan. | Automate the rhythm<br>• Daily: health query to chat<br>• Weekly: API pull + AI draft commentary, human-approved<br>• Monthly: PII and UTM scans<br>• Quarterly: event inventory vs plan | Layout: four automation lanes (daily, weekly, monthly, quarterly) each with a small robot icon handing off to a human reviewer icon. 0-10s daily lane; 10-20s weekly lane; 20-29s monthly lane; 29-39s quarterly lane. |
| 8 | 4:18 | 37 | First example, a simple one. A solo consultant runs analytics for three small clients. She can't do everything, so she shrinks the rhythm to fit. A weekly twenty-minute review per client, reading from an automated sheet. A monthly thirty-minute check with the QA script on the top journey and the PII scan. And a quarterly one-hour review with each client. Her rule: if a routine keeps getting canceled, shrink it rather than abandon it. A small rhythm that runs beats a big one on paper. | Example 1: a solo consultant<br>• Weekly 20 min per client<br>• Monthly 30 min checks<br>• Quarterly 1-hour reviews<br>• Shrink, don't abandon | Layout: three client cards each with a small routine clock; one oversized routine shrinks to fit a calendar week. 0-12s three client cards; 12-25s routine clocks; 25-37s oversized routine shrinks. |
| 9 | 4:55 | 38 | Second example, a business case. A new analyst at a Dubai e-commerce company inherits duplicate tags, no change log, and a dashboard nobody trusts. Days one to thirty: audit, remove the hard-coded duplicate, fix payment gateway referrals, add the PII scan. Days thirty-one to sixty: rebuild the tracking plan, move purchase to a data layer push, launch the UTM builder, run a consent audit. Days sixty-one to ninety: a leadership dashboard on BigQuery summaries with weekly commentary, and the first geo holdout designed with the paid media team. | Example 2: a 90-day turnaround<br>• Days 1-30: audit and critical fixes<br>• Days 31-60: rebuild and govern<br>• Days 61-90: dashboard and first test | Layout: a 90-day timeline with three phases; icons for each phase's key actions pop onto the timeline. 0-13s phase 1 icons; 13-25s phase 2 icons; 25-38s phase 3 icons. |
| 10 | 5:33 | 31 | How did they know it worked? At day ninety, leadership meetings referenced the dashboard by name, and the change log explained the two numbers that moved unexpectedly that quarter. That's the real measure of an operating rhythm: people use the data in decisions, and surprises have explanations. Other signs: fewer escaped errors found by stakeholders, faster answers to how was this measured, and no single person who holds all the knowledge. | Measuring the rhythm<br>• Dashboard used in decisions<br>• Surprises explained by the log<br>• Fewer escaped errors<br>• No single point of knowledge | Layout: a meeting table icon with a dashboard on screen; a change log row lighting up next to a chart anomaly; a team of several people icons sharing a key. 0-10s meeting icon; 10-21s log explains anomaly; 21-31s shared key. |
| 11 | 6:04 | 40 | Watch me run the quarterly event inventory. In BigQuery, I select event name and count events over the last thirty days, sorted by volume. Then I paste that list next to the tracking plan. Two lists come out of the comparison. Events that are live but undocumented: either document them or remove the tag. And events that are documented but show zero volume: investigate whether they broke, or retire them. This week, I find a leftover test event from a vendor and a signup event that stopped firing after a redesign. | Watch me do it<br>• Event names, last 30 days<br>• Compare with the plan<br>• Undocumented: document or remove<br>• Zero volume: investigate or retire | Layout: a query result list and a tracking plan list side by side; connecting lines match most items; two unmatched items highlight in amber and red. 0-13s two lists appear; 13-27s matching lines draw; 27-40s unmatched items highlight. |
| 12 | 6:44 | 37 | Building culture matters as much as routines. Teach questions, not clicks: train stakeholders to ask good questions, and they'll find the reports. Celebrate decisions made with data, including decisions to stop something. Be honest about uncertainty: say directionally and illustratively when you mean it, because credibility comes from calibrated claims. Keep documentation close to the work, linked from the dashboard. And whenever an analyst, agency or freelancer changes, hand over a pack: plans, container export, UTM taxonomy, consent setup, dashboards, the change log and open issues. | Culture<br>• Teach questions<br>• Celebrate data-informed decisions<br>• Calibrated claims<br>• Docs near the work<br>• Handover packs | Layout: five culture icons appear around a team circle; a handover box fills with document icons. 0-18s culture icons; 18-37s handover box fills. |
| 13 | 7:21 | 27 | Common mistakes. No change log, so historical anomalies become mysteries. One person holding all knowledge and access. Audits that list eighty issues without prioritizing. Stakeholders who only see numbers, never the reasoning. And routines that exist on paper only. Recap: run analytics like a product, with owners, a daily-to-quarterly rhythm, a change log, prioritized audits, automation with human approval, and honest communication. | Mistakes and recap<br>• No log, single owner, unprioritized audits<br>• Numbers without reasoning; paper routines<br>• Owners, rhythm, log, audits, automation, honesty | Layout: warning cards collapse into a recap card with the circular calendar icon. 0-14s warning cards; 14-27s collapse into recap card. |
| 14 | 7:48 | 34 | Try this now. Create a change log and a monthly analytics routine for your organization or a client, and schedule the first review in your calendar. If you have the BigQuery export, run the event inventory query and compare it with your tracking plan. And if you're ready to go deeper, the Privacy-First Measurement course covers consent-aware collection and server-side tagging, and AI for Data Analysis and Decision Making will help you turn these numbers into defensible decisions. | Try this now<br>• Change log and monthly routine<br>• Schedule the first review<br>• Run the event inventory<br>• Next: Privacy-First Measurement; AI for Data Analysis | Layout: a calendar invite is created; a change log template appears; two course badges slide in. 0-11s calendar invite; 11-23s log template; 23-34s two course badges. |

<details><summary>Narration per scene (paste into ElevenLabs)</summary>

Scene 1 — The analytics operating rhythm

```text
Here's a hard truth about analytics. You can build a perfect implementation in January, and by June it will be quietly broken. Sites change, campaigns launch, developers refactor, new tags appear, and people leave. Analytics decays without ownership. In this lecture you'll learn how to run analytics like a product: roles and responsibilities, a daily, weekly, monthly and quarterly rhythm, the change log, how to audit an inherited property, how to automate the boring parts, and how to build a culture where data is trusted.
```

Scene 2 — Why it matters

```text
Why does this matter? Because every number your organization trusts depends on dozens of small things staying correct: tags, triggers, consent settings, UTM habits, definitions. Without routines, those things drift, and nobody notices until a big decision is made on a broken number. An operating rhythm turns analytics from a project into a product, with owners, routines and documentation. It's not glamorous. It's the reason some teams trust their data and others don't.
```

Scene 3 — Roles

```text
Start with roles. Think of a simple RACI: who's responsible, accountable, consulted and informed. The measurement plan: the analyst is responsible, the marketing lead accountable. The tracking plan and tags: the analyst or implementer responsible, the analytics owner accountable, developers consulted. The data layer: developers responsible, the tech lead accountable. Consent and privacy: analyst and legal responsible, the data protection lead accountable. Dashboards: the analyst responsible, the marketing lead accountable. In a small business, one person holds several roles. The point is that every activity has a named owner.
```

Scene 4 — The rhythm

```text
Now the rhythm itself. Daily, five minutes: a sanity check during active campaigns, and alert review. Weekly, thirty minutes: K P Is versus target, channel and landing page movers, anomaly notes. Monthly, two hours: the QA script on top journeys, the PII audit, the Unassigned review, dashboard commentary, and an access review for leavers. Quarterly, one day: measurement plan review, tracking plan clean-up, consent re-audit, the attribution and experiment roadmap, and stakeholder feedback. And on every release: developer notice, QA script, change log, annotation.
```

Scene 5 — The change log

```text
The cheapest, highest-value analytics artifact is the change log. Date, change, who, reason, and data impact. For example: the second of March, generate lead moved to a data layer push, by Hassan, because the thank-you page created false positives, and leads dropped as expected. When a number moves unexpectedly, the change log is the first place to look. Think of it like a flight recorder. You hope you never need it, but when something goes wrong, it tells you exactly what happened.
```

Scene 6 — Auditing an inherited property

```text
Agencies and new hires often inherit a messy property. Audit it in six areas. Access and ownership: who owns the account, who has admin, remove stale users. Configuration: retention, filters, referrals, cross-domain, key events, links. Implementation: duplicate tags, the container's state, data layer quality. Data quality: not set rates, Unassigned share, personal data leaks, revenue reconciliation. Consent: pre-consent firing, the C M P setup, non-Google tags. And reporting: which dashboards exist and who uses them. Score each red, amber or green, then prioritize by impact and effort.
```

Scene 7 — Automate the rhythm

```text
Now automation, which makes the rhythm sustainable. Daily, a BigQuery scheduled health query posts anomalies to a team chat. Weekly, a Data A P I script pulls the K P I table into a sheet, and an AI assistant drafts the first version of the commentary from the aggregated numbers, which the analyst edits and approves. Monthly, the PII scan and UTM leak query run on their own. And quarterly, a simple query lists every event name seen in the last thirty days, which you compare with the tracking plan.
```

Scene 8 — Example 1: a solo consultant

```text
First example, a simple one. A solo consultant runs analytics for three small clients. She can't do everything, so she shrinks the rhythm to fit. A weekly twenty-minute review per client, reading from an automated sheet. A monthly thirty-minute check with the QA script on the top journey and the PII scan. And a quarterly one-hour review with each client. Her rule: if a routine keeps getting canceled, shrink it rather than abandon it. A small rhythm that runs beats a big one on paper.
```

Scene 9 — Example 2: a 90-day turnaround

```text
Second example, a business case. A new analyst at a Dubai e-commerce company inherits duplicate tags, no change log, and a dashboard nobody trusts. Days one to thirty: audit, remove the hard-coded duplicate, fix payment gateway referrals, add the PII scan. Days thirty-one to sixty: rebuild the tracking plan, move purchase to a data layer push, launch the UTM builder, run a consent audit. Days sixty-one to ninety: a leadership dashboard on BigQuery summaries with weekly commentary, and the first geo holdout designed with the paid media team.
```

Scene 10 — Measuring the rhythm

```text
How did they know it worked? At day ninety, leadership meetings referenced the dashboard by name, and the change log explained the two numbers that moved unexpectedly that quarter. That's the real measure of an operating rhythm: people use the data in decisions, and surprises have explanations. Other signs: fewer escaped errors found by stakeholders, faster answers to how was this measured, and no single person who holds all the knowledge.
```

Scene 11 — Watch me do it

```text
Watch me run the quarterly event inventory. In BigQuery, I select event name and count events over the last thirty days, sorted by volume. Then I paste that list next to the tracking plan. Two lists come out of the comparison. Events that are live but undocumented: either document them or remove the tag. And events that are documented but show zero volume: investigate whether they broke, or retire them. This week, I find a leftover test event from a vendor and a signup event that stopped firing after a redesign.
```

Scene 12 — Culture

```text
Building culture matters as much as routines. Teach questions, not clicks: train stakeholders to ask good questions, and they'll find the reports. Celebrate decisions made with data, including decisions to stop something. Be honest about uncertainty: say directionally and illustratively when you mean it, because credibility comes from calibrated claims. Keep documentation close to the work, linked from the dashboard. And whenever an analyst, agency or freelancer changes, hand over a pack: plans, container export, UTM taxonomy, consent setup, dashboards, the change log and open issues.
```

Scene 13 — Mistakes and recap

```text
Common mistakes. No change log, so historical anomalies become mysteries. One person holding all knowledge and access. Audits that list eighty issues without prioritizing. Stakeholders who only see numbers, never the reasoning. And routines that exist on paper only. Recap: run analytics like a product, with owners, a daily-to-quarterly rhythm, a change log, prioritized audits, automation with human approval, and honest communication.
```

Scene 14 — Try this now

```text
Try this now. Create a change log and a monthly analytics routine for your organization or a client, and schedule the first review in your calendar. If you have the BigQuery export, run the event inventory query and compare it with your tracking plan. And if you're ready to go deeper, the Privacy-First Measurement course covers consent-aware collection and server-side tagging, and AI for Data Analysis and Decision Making will help you turn these numbers into defensible decisions.
```

</details>

### 19. The GA4 BigQuery export: setup, schema and SQL you will reuse

Lesson `bigquery-export-and-sql` · module “BigQuery export and AI-assisted analysis” · target 8 min · 15 scenes · 1183 words · planned 8:34 (words ≈ 8:34) · status: **to produce**

| Term | Say |
|---|---|
| GA4 | G A four |
| SQL | sequel |
| BigQuery | Big Query |
| UNNEST | un-nest |
| MCP | M C P |
| API | A P I |
| UTC | U T C |
| 360 | three sixty |
| CRM | C R M |
| CTE | C T E |

| # | Time | Sec | Narration | On screen | Visual |
|---|---|---|---|---|---|
| 1 | 0:00 | 39 | The G A four interface is like a restaurant menu: curated, convenient, and limited to what the kitchen decided to serve. The BigQuery export is the keys to the kitchen: every raw ingredient, every event, with every parameter, in your own cloud project. In this lecture you'll learn what the export gives you, its options and limits, the five ideas you need to read the schema, five reusable SQL queries, why the numbers won't match the interface, and how teams use it to answer questions G A four alone can't. | BigQuery export and SQL<br>• What the export gives you<br>• Options and limits<br>• The schema in five ideas<br>• Five reusable queries<br>• Why numbers differ | Layout: a curated menu card on the left; a kitchen door swings open on the right revealing shelves of raw ingredient jars labeled with event names. 0-10s menu card; 10-20s kitchen door opens; 20-29s ingredient jars appear; 29-39s title builds. |
| 2 | 0:39 | 33 | Why does this matter? Because the interface is aggregated, thresholded, sometimes sampled, and limited by retention. With the export you can keep history beyond fourteen months, build unsampled funnels, join G A four with C R M, cost or order data, and schedule health checks that alert you before stakeholders notice problems. It's the difference between reading reports and owning your data. And since the export isn't retroactive, the most important step is simply linking it early. | Why it matters<br>• Beyond retention and sampling<br>• Joins with CRM, cost, orders<br>• Scheduled health checks<br>• Not retroactive: link early | Layout: a timeline where the export starts only from the link date; data before it is grayed; an arrow labels 'link early'. 0-11s timeline draws; 11-22s grayed section before link; 22-33s link early arrow. |
| 3 | 1:12 | 41 | Here are the options, and please verify current terms. The daily export writes one events table per day. Standard properties have a daily limit of one million events for this export, and consistently exceeding it can pause the export. The streaming export writes near real-time intraday tables with no event limit, but costs apply, and some fields aren't included. Fresh Daily is for G A four three sixty properties. And the user data export adds user-level tables. You link it in admin, under product links, choosing the cloud project, data location and export types. | Export options<br>• Daily: 1M events/day limit (standard)<br>• Streaming: intraday, no limit, costs<br>• Fresh Daily: 360 only<br>• User data tables<br>• Link in Admin > Product links | Layout: four export option cards with small icons; the daily card shows a '1M/day' badge; the link path appears as breadcrumb text. 0-10s daily card; 10-20s streaming card; 20-31s fresh daily and user cards; 31-41s breadcrumb path. |
| 4 | 1:53 | 37 | Costs are manageable if you're careful. BigQuery has a free tier and a sandbox. Beyond that, you pay for storage and for the bytes each query scans. The single biggest cost mistake is querying events star without a date filter, and scanning years of data to answer a question about last week. So always filter with the table suffix, select only the columns you need, and build small summary tables for dashboards. Choose a data location that suits your data-residency obligations when you link. | Cost hygiene<br>• Free tier and sandbox<br>• Pay for storage and bytes scanned<br>• Always filter _TABLE_SUFFIX<br>• Select only needed columns<br>• Summary tables for dashboards | Layout: a query meter shows a huge scan for an unfiltered query; adding a date filter shrinks the meter dramatically. 0-9s unfiltered query; 9-18s huge meter; 18-28s date filter added; 28-37s meter shrinks. |
| 5 | 2:30 | 37 | Now the schema, in five ideas. One: one row per event, with event name, a timestamp in microseconds in UTC, and an event date in the property's time zone. Two: parameters are nested in a repeated field called event params, each a key with a string, integer or double value, so you read one with a subquery over UNNEST. Three: users and sessions are derived. User pseudo id identifies the browser or app instance, and a session is that id plus the ga session id parameter. | Schema ideas 1 to 3<br>• One row per event<br>• Nested event_params, read with UNNEST<br>• user_pseudo_id + ga_session_id = session | Layout: a table row expands to show nested event_params; a key pair 'user_pseudo_id + ga_session_id' snaps together into a 'session' token. 0-12s row expands; 12-25s UNNEST subquery highlights; 25-37s session token forms. |
| 6 | 3:07 | 35 | Four: traffic source has three layers. Traffic source is the user's first source. Collected traffic source holds values collected on each event, like UTMs and click ids. And session traffic source last click holds session-scoped last-click attribution that matches the session acquisition reports; it was added in twenty twenty-four and isn't in intraday tables. Five: privacy fields. Privacy info shows consent state as Yes, No or null. Consent-denied rows have no user pseudo id. And the export contains no modeled data. | Schema ideas 4 and 5<br>• traffic_source: first source<br>• collected_traffic_source: per event<br>• session_traffic_source_last_click: sessions<br>• privacy_info; no modeled data | Layout: three stacked traffic-source layers light up with labels; a privacy_info column shows Yes, No, null values; a 'no modeled data' stamp. 0-12s three layers light up; 12-23s privacy column; 23-35s stamp. |
| 7 | 3:42 | 33 | First example, a simple one. A London publisher needs three-year content trends, but G A four exploration retention is at most fourteen months on a standard property. Because the export was linked early, the analyst builds year-over-year and three-year views of engaged sessions by content group straight from BigQuery, and feeds a Data Studio dashboard from a summary table. The retention setting stopped mattering for trend analysis, because the raw history lives in their own project. | Example 1: London publisher<br>• Needs 3-year trends<br>• Explorations capped at 14 months<br>• Export linked early<br>• Summary table to Data Studio | Layout: a three-year trend line drawn from BigQuery extends well beyond a 14-month marker; a summary table feeds a dashboard icon. 0-11s 14-month marker; 11-22s three-year line extends; 22-33s summary table to dashboard. |
| 8 | 4:15 | 37 | Second example, a business case. A Lahore home-goods brand links BigQuery, schedules a daily summary table with date, channel, sessions, orders and revenue, and joins it with its order system on transaction id, adding delivered revenue for cash on delivery orders. For the first time, the team can report channel performance on delivered revenue rather than orders placed. And it discovers that one marketplace referral channel has a much higher refusal rate than the others, which changes how much it's worth paying for that traffic. | Example 2: Lahore home goods<br>• Daily summary table<br>• Join on transaction_id with orders<br>• Delivered revenue by channel<br>• One channel's refusal rate exposed | Layout: two tables join on a transaction_id key; a new 'delivered revenue' column appears; one channel row highlights with a high refusal rate. 0-9s two tables; 9-18s join on key; 18-28s delivered column; 28-37s row highlights. |
| 9 | 4:52 | 42 | Watch me write the first query. Sessions, users and purchases per day for September. I select event date. Users: count distinct user pseudo id. Sessions: count distinct user pseudo id joined with the ga session id, which I pull from event params with a small UNNEST subquery. Purchases: count if the event name is purchase. I filter the table suffix to September, and exclude rows with a null user pseudo id, because consent-denied rows can't be counted as users or sessions. Group by date, order by date. It runs in seconds and scans only one month. | Watch me do it, part 1<br>• Users: distinct user_pseudo_id<br>• Sessions: user + ga_session_id<br>• Purchases: COUNTIF<br>• September suffix; exclude null IDs | Layout: SQL editor with query one; the session CONCAT line highlights; the suffix filter highlights; a small bytes-scanned indicator shows a modest number. 0-10s select lines; 10-21s session expression; 21-32s filters; 32-42s bytes indicator. |
| 10 | 5:34 | 40 | Next, revenue by channel. I use the session traffic source last click record's default channel group, count distinct transaction ids as orders, and sum purchase revenue, for purchase events in September. Then landing pages. That one is a little longer: for each session, I take the first page view's location as the landing page, strip the query string, and flag whether the session had a purchase or lead. Then I compute sessions and key event rate per landing page, keeping pages with at least a hundred sessions, so small numbers don't mislead me. | Watch me do it, part 2<br>• Revenue by last-click channel<br>• Landing page = first page_view per session<br>• Strip query strings<br>• Key event rate, min 100 sessions | Layout: two query panels: the channel query produces a bar chart; the landing page query's CTEs highlight in order and produce a ranked table with a 'min 100 sessions' filter. 0-10s channel query and bars; 10-20s landing CTE highlights; 20-30s ranked table; 30-40s filter badge. |
| 11 | 6:14 | 35 | Now, why won't these numbers match the interface? Because the interface can include modeled data, applies thresholds, may use different identity like user id or modeling, counts users and sessions with approximation algorithms, and attributes in its own way. So don't try to reconcile to the last unit. Decide which source is authoritative for which purpose, for example the interface for executive trend reporting and BigQuery for funnels and joins, write that down, and label every dashboard with its source. | Why numbers differ<br>• Modeling, thresholds, identity<br>• Approximate counting<br>• Different attribution<br>• Choose an authority per purpose<br>• Label sources | Layout: two numbers side by side, 'interface' and 'export', differ slightly; five reason chips float between them; a label 'source: BigQuery' attaches to a dashboard tile. 0-12s two numbers; 12-23s reason chips; 23-35s source label. |
| 12 | 6:49 | 23 | Common mistakes. Linking the export late, because there's no backfill. Querying events star without a table suffix filter, and paying to scan years of data. Counting sessions without the ga session id, or users without handling consent-denied null ids. And treating the export as wrong because it doesn't match modeled interface numbers. | Common mistakes<br>• Linking late<br>• No suffix filter<br>• Wrong session and user counting<br>• Expecting an exact match | Layout: four warning cards drop in. 0-12s cards 1 and 2; 12-23s cards 3 and 4. |
| 13 | 7:12 | 28 | How do you know you're using the export well? Three signs. Your key funnels and KPIs have scheduled summary tables with history beyond the retention window. At least one decision has been made using a join G A four couldn't do alone, like delivered revenue or lead quality. And your monthly BigQuery bill is predictable, because queries are filtered and dashboards read small tables. | Using it well<br>• Scheduled summaries with long history<br>• Decisions from joins<br>• Predictable costs | Layout: three check tiles: a long history chart, a join icon with a decision stamp, a flat cost line. 0-9s history tile; 9-19s join tile; 19-28s cost tile. |
| 14 | 7:40 | 30 | Recap. The export gives you raw, event-level data in your own project. Know the options and the one million events per day limit for standard daily exports. Remember the five schema ideas: one row per event, nested parameters, derived sessions, three traffic source layers, and privacy fields with no modeling. Filter by date to control cost. Build summary tables. And choose, document and label an authoritative source for each purpose. | Recap<br>• Raw data you own<br>• Options and limits<br>• Five schema ideas<br>• Filter for cost<br>• Summaries and labeled sources | Layout: recap card with the kitchen icon and five checkmarks. 0-15s kitchen icon; 15-30s checkmarks. |
| 15 | 8:10 | 24 | Try this now. If your property isn't linked to BigQuery, link it today, even if you won't query it yet, because there's no backfill. If it is linked, run the lesson's first three queries for last month, compare the results with the interface, and write down the differences and the reasons you think explain them. | Try this now<br>• Link the export today<br>• Or run three queries<br>• Compare with the interface<br>• Explain the differences | Layout: a link button pressed; three query result cards; a comparison note. 0-8s link pressed; 8-16s three result cards; 16-24s comparison note. |

<details><summary>Narration per scene (paste into ElevenLabs)</summary>

Scene 1 — BigQuery export and SQL

```text
The G A four interface is like a restaurant menu: curated, convenient, and limited to what the kitchen decided to serve. The BigQuery export is the keys to the kitchen: every raw ingredient, every event, with every parameter, in your own cloud project. In this lecture you'll learn what the export gives you, its options and limits, the five ideas you need to read the schema, five reusable SQL queries, why the numbers won't match the interface, and how teams use it to answer questions G A four alone can't.
```

Scene 2 — Why it matters

```text
Why does this matter? Because the interface is aggregated, thresholded, sometimes sampled, and limited by retention. With the export you can keep history beyond fourteen months, build unsampled funnels, join G A four with C R M, cost or order data, and schedule health checks that alert you before stakeholders notice problems. It's the difference between reading reports and owning your data. And since the export isn't retroactive, the most important step is simply linking it early.
```

Scene 3 — Export options

```text
Here are the options, and please verify current terms. The daily export writes one events table per day. Standard properties have a daily limit of one million events for this export, and consistently exceeding it can pause the export. The streaming export writes near real-time intraday tables with no event limit, but costs apply, and some fields aren't included. Fresh Daily is for G A four three sixty properties. And the user data export adds user-level tables. You link it in admin, under product links, choosing the cloud project, data location and export types.
```

Scene 4 — Cost hygiene

```text
Costs are manageable if you're careful. BigQuery has a free tier and a sandbox. Beyond that, you pay for storage and for the bytes each query scans. The single biggest cost mistake is querying events star without a date filter, and scanning years of data to answer a question about last week. So always filter with the table suffix, select only the columns you need, and build small summary tables for dashboards. Choose a data location that suits your data-residency obligations when you link.
```

Scene 5 — Schema ideas 1 to 3

```text
Now the schema, in five ideas. One: one row per event, with event name, a timestamp in microseconds in UTC, and an event date in the property's time zone. Two: parameters are nested in a repeated field called event params, each a key with a string, integer or double value, so you read one with a subquery over UNNEST. Three: users and sessions are derived. User pseudo id identifies the browser or app instance, and a session is that id plus the ga session id parameter.
```

Scene 6 — Schema ideas 4 and 5

```text
Four: traffic source has three layers. Traffic source is the user's first source. Collected traffic source holds values collected on each event, like UTMs and click ids. And session traffic source last click holds session-scoped last-click attribution that matches the session acquisition reports; it was added in twenty twenty-four and isn't in intraday tables. Five: privacy fields. Privacy info shows consent state as Yes, No or null. Consent-denied rows have no user pseudo id. And the export contains no modeled data.
```

Scene 7 — Example 1: London publisher

```text
First example, a simple one. A London publisher needs three-year content trends, but G A four exploration retention is at most fourteen months on a standard property. Because the export was linked early, the analyst builds year-over-year and three-year views of engaged sessions by content group straight from BigQuery, and feeds a Data Studio dashboard from a summary table. The retention setting stopped mattering for trend analysis, because the raw history lives in their own project.
```

Scene 8 — Example 2: Lahore home goods

```text
Second example, a business case. A Lahore home-goods brand links BigQuery, schedules a daily summary table with date, channel, sessions, orders and revenue, and joins it with its order system on transaction id, adding delivered revenue for cash on delivery orders. For the first time, the team can report channel performance on delivered revenue rather than orders placed. And it discovers that one marketplace referral channel has a much higher refusal rate than the others, which changes how much it's worth paying for that traffic.
```

Scene 9 — Watch me do it, part 1

```text
Watch me write the first query. Sessions, users and purchases per day for September. I select event date. Users: count distinct user pseudo id. Sessions: count distinct user pseudo id joined with the ga session id, which I pull from event params with a small UNNEST subquery. Purchases: count if the event name is purchase. I filter the table suffix to September, and exclude rows with a null user pseudo id, because consent-denied rows can't be counted as users or sessions. Group by date, order by date. It runs in seconds and scans only one month.
```

Scene 10 — Watch me do it, part 2

```text
Next, revenue by channel. I use the session traffic source last click record's default channel group, count distinct transaction ids as orders, and sum purchase revenue, for purchase events in September. Then landing pages. That one is a little longer: for each session, I take the first page view's location as the landing page, strip the query string, and flag whether the session had a purchase or lead. Then I compute sessions and key event rate per landing page, keeping pages with at least a hundred sessions, so small numbers don't mislead me.
```

Scene 11 — Why numbers differ

```text
Now, why won't these numbers match the interface? Because the interface can include modeled data, applies thresholds, may use different identity like user id or modeling, counts users and sessions with approximation algorithms, and attributes in its own way. So don't try to reconcile to the last unit. Decide which source is authoritative for which purpose, for example the interface for executive trend reporting and BigQuery for funnels and joins, write that down, and label every dashboard with its source.
```

Scene 12 — Common mistakes

```text
Common mistakes. Linking the export late, because there's no backfill. Querying events star without a table suffix filter, and paying to scan years of data. Counting sessions without the ga session id, or users without handling consent-denied null ids. And treating the export as wrong because it doesn't match modeled interface numbers.
```

Scene 13 — Using it well

```text
How do you know you're using the export well? Three signs. Your key funnels and KPIs have scheduled summary tables with history beyond the retention window. At least one decision has been made using a join G A four couldn't do alone, like delivered revenue or lead quality. And your monthly BigQuery bill is predictable, because queries are filtered and dashboards read small tables.
```

Scene 14 — Recap

```text
Recap. The export gives you raw, event-level data in your own project. Know the options and the one million events per day limit for standard daily exports. Remember the five schema ideas: one row per event, nested parameters, derived sessions, three traffic source layers, and privacy fields with no modeling. Filter by date to control cost. Build summary tables. And choose, document and label an authoritative source for each purpose.
```

Scene 15 — Try this now

```text
Try this now. If your property isn't linked to BigQuery, link it today, even if you won't query it yet, because there's no backfill. If it is linked, run the lesson's first three queries for last month, compare the results with the interface, and write down the differences and the reasons you think explain them.
```

</details>

### 20. AI-assisted GA4 analysis: Ask Advisor, Gemini, MCP and a verification ladder

Lesson `ai-assisted-ga4-analysis` · module “BigQuery export and AI-assisted analysis” · target 9 min · 15 scenes · 1228 words · planned 8:55 (words ≈ 8:54) · status: **to produce**

| Term | Say |
|---|---|
| GA4 | G A four |
| SQL | sequel |
| BigQuery | Big Query |
| UNNEST | un-nest |
| MCP | M C P |
| API | A P I |
| CMO | C M O |
| Gemini | Gemini |

| # | Time | Sec | Narration | On screen | Visual |
|---|---|---|---|---|---|
| 1 | 0:00 | 42 | In twenty twenty-six, you can type a question like why did revenue drop last week directly into G A four, and an AI assistant will answer in seconds. That's genuinely useful. It's also a trap, because a confident answer isn't the same as a correct one. In this lecture you'll learn where AI now sits in the analytics workflow, a five-step verification ladder for AI answers, prompt templates that produce checkable work, how to connect an assistant to G A four safely through M C P, and the privacy rules that keep you out of trouble. | AI-assisted GA4 analysis<br>• Where AI sits now<br>• A verification ladder<br>• Prompt templates<br>• Connecting via MCP, read-only<br>• Privacy and governance | Layout: a question typed into a chat bubble inside an analytics-style frame; an instant answer appears; a ladder icon rises beside it labeled 'verify'. 0-10s question types; 10-21s answer appears; 21-32s ladder rises; 32-42s title builds. |
| 2 | 0:42 | 30 | Why does this matter? Because AI shifts where analysts spend time. Drafting queries, summarizing tables and writing first-draft commentary now take minutes. That frees you for the parts AI is bad at: knowing your tracking history, your definitions, your consent changes and your business context. If you use AI well, you'll answer more questions, faster, with the same rigor. If you use it carelessly, you'll spread confident mistakes at scale. | Why it matters<br>• Drafting is now fast<br>• AI lacks your history and context<br>• More answers, same rigor<br>• Or confident mistakes at scale | Layout: a time pie shifts from 'drafting' to 'verifying and explaining'; a warning icon shows a mistake multiplying across several dashboards when unchecked. 0-10s pie shifts; 10-20s mistake multiplies; 20-30s check icon stops it. |
| 3 | 1:12 | 35 | Here's where AI sits. Inside G A four, Analytics Advisor, with Ask Advisor in beta, answers plain-language questions about your property. Inside Google Cloud, Gemini helps draft SQL in BigQuery, and conversational data agents surface in Data Studio. General assistants like Claude, ChatGPT and Gemini can analyze exported, aggregated tables, write Python and SQL, and draft narratives. And through M C P, Google's experimental Analytics M C P server lets an assistant call read-only tools like run report, using your credentials. | Where AI sits<br>• Inside GA4: Ask Advisor (beta)<br>• Google Cloud: Gemini in BigQuery, Data Studio agents<br>• General assistants: Claude, ChatGPT, Gemini<br>• MCP: GA4 read-only tools | Layout: four zones on a map of the workflow: GA4, Google Cloud, general assistants, MCP bridge; each lights with a small icon. 0-9s GA4 zone; 9-18s Cloud zone; 18-26s assistants zone; 26-35s MCP bridge. |
| 4 | 1:47 | 32 | The core principle: AI proposes, you verify. Think of AI like a brilliant new intern who has never seen your company before. They're fast, articulate and tireless. But they don't know that the consent banner changed last Tuesday, that revenue here means net of refunds, or that the thank-you page used to double count. You'd never forward an intern's first draft to the CEO without checking it. Treat AI output exactly the same way. | AI proposes, you verify<br>• A brilliant new intern<br>• Fast, articulate, context-free<br>• Check before it reaches the CEO | Layout: an intern icon hands a polished report to an analyst icon, who checks three sticky notes (consent change, revenue definition, old bug) before passing it on. 0-11s intern hands report; 11-21s three sticky notes; 21-32s analyst passes it on. |
| 5 | 2:19 | 41 | Here's the verification ladder. Step one, source check: which report, query or table did the number come from? If you can't tell, it isn't a finding. Step two, recompute: rerun the key number yourself in a report, the Data A P I or SQL. Step three, definition check: does users mean active users, is revenue net of refunds, which attribution model? Step four, context check: tracking changes, consent changes, releases, holidays, straight from the change log. And step five, plausibility: does the size of the effect make business sense? Would you bet on it? | The verification ladder<br>• 1 Source<br>• 2 Recompute<br>• 3 Definitions<br>• 4 Context<br>• 5 Plausibility | Layout: a five-rung ladder; each rung lights and shows a small icon as named. 0-10s rungs 1 and 2; 10-20s rung 3; 20-31s rung 4; 31-41s rung 5. |
| 6 | 3:00 | 37 | Now prompts that produce checkable work. The secret is giving the model your schema notes and rules. For SQL, tell it the dataset, that parameters live in event params and need UNNEST, that sessions are user pseudo id plus ga session id, which field holds the session channel and revenue, and that it must always filter the table suffix. Then ask for the SQL, a line-by-line explanation, and two ways the result could mislead. That last request turns the model into its own first reviewer. | SQL prompts that work<br>• Dataset and schema notes<br>• Session and channel definitions<br>• Always filter suffix<br>• SQL + explanation + two risks | Layout: a prompt card with schema notes highlighted; output panel shows SQL, an explanation list and a 'could mislead' section. 0-12s prompt card; 12-25s schema notes highlight; 25-37s output three sections. |
| 7 | 3:37 | 43 | For explaining a change, give aggregated data only, with no personal data: week, channel, device, sessions, key events, revenue. Add known context, like a consent banner change on the second of September and a new checkout on the fourteenth. Ask it to decompose the change, name the segment that explains most of it, list three hypotheses with the report or query that would test each, and use only numbers from the table, labeling estimates. And for weekly commentary, ask for what, so what, now what, naming the source and attribution model, with no causal claims without a test. | Explain and summarize prompts<br>• Aggregated data only<br>• Known context<br>• Decompose, segment, hypotheses with tests<br>• Only given numbers<br>• Commentary with sources | Layout: an aggregated table flows into a prompt; a response shows a decomposition, a highlighted segment and three hypothesis cards each with a test icon. 0-11s table flows in; 11-22s decomposition; 22-32s segment highlight; 32-43s hypothesis cards. |
| 8 | 4:20 | 43 | First example, a simple one. A Dubai marketplace's CMO asks Ask Advisor why orders fell. It points to lower paid social sessions. The analyst climbs the ladder. The report confirms the session drop. But the change log shows the UK consent banner changed that week, and BigQuery shows the drop is concentrated in consent-denied traffic, with no change in purchase rates among consented users. The commentary says observed sessions fell mainly because of new consent defaults, purchases from consented users are stable, and no budget change is recommended. The AI found the what. The analyst found the why. | Example 1: Dubai marketplace<br>• Ask Advisor: paid social sessions down<br>• Change log: consent banner changed<br>• BigQuery: drop in denied traffic<br>• No budget change | Layout: a chat answer card; a change-log entry slides in; a BigQuery split by consent state shows the drop in 'No' rows only; a commentary card with 'no budget change'. 0-11s chat answer; 11-22s change-log entry; 22-32s consent split; 32-43s commentary card. |
| 9 | 5:03 | 36 | Second example, a business case. A Karachi agency builds a shared prompt with its schema notes and asks Claude or ChatGPT to draft SQL for client questions. Every query is reviewed by a senior analyst against a checklist: suffix filter, session definition, null ids, revenue field and attribution field, before it touches a dashboard. Drafting time drops sharply. And review keeps catching the typical AI mistakes, like forgetting the date filter or counting sessions inconsistently. AI speed plus human review beats either alone. | Example 2: Karachi agency SQL copilot<br>• Shared schema prompt<br>• Senior review checklist<br>• Faster drafting<br>• Review catches typical AI mistakes | Layout: a drafting lane (AI icon) feeds a review gate with a five-item checklist; one query bounces back with a missing date filter flagged. 0-9s AI drafts; 9-18s review gate checklist; 18-27s query bounces with flag; 27-36s approved query passes. |
| 10 | 5:39 | 42 | Watch me use the SQL template. I paste the schema notes, then the question: key event rate by landing page for mobile users in September twenty twenty-six, pages with at least a hundred sessions. The assistant returns a query, an explanation and two risks: that landing pages with query strings may split, and that consent-denied sessions are excluded. I check it against my review list. The suffix filter is there. Sessions use ga session id. But it filtered mobile using the wrong field, so I fix that, run it, and compare the top rows with the interface. | Watch me do it, part 1<br>• Paste schema notes and question<br>• Get SQL, explanation, risks<br>• Review checklist<br>• Fix the device filter<br>• Compare with the interface | Layout: prompt pasted; response panel with SQL; checklist items tick; one line with a device filter highlights red and is corrected; results table appears. 0-8s prompt pasted; 8-17s SQL returned; 17-25s checklist ticks; 25-34s device line corrected; 34-42s results appear. |
| 11 | 6:21 | 43 | Next, I connect an assistant to G A four through M C P. Google's experimental Analytics M C P server runs locally and uses application default credentials, after I enable the Admin and Data A P Is in a cloud project and install it following the README. I use an account with read-only access, Viewer or Analyst, limited to the properties I need. Now I can ask my assistant to run a report on key events by channel for last week, and it calls run report. I still check the number against the interface before I use it. | Watch me do it, part 2<br>• Local MCP server, default credentials<br>• Enable Admin and Data APIs<br>• Read-only account, limited properties<br>• Assistant calls run_report<br>• Verify before use | Layout: an assistant window connects via a plug icon labeled MCP to a GA4 icon; a lock badge reads 'read-only'; a tool call 'run_report' appears with a result table. 0-11s plug connects; 11-22s read-only badge; 22-32s tool call appears; 32-43s result table. |
| 12 | 7:04 | 30 | Privacy and governance, briefly. Send AI tools aggregated data wherever possible, and never paste user-level exports with identifiers into unapproved tools. Check your organization's approved tools, retention and training settings, and client contracts. Keep a shared prompt library with schema notes, so every analyst gets consistent SQL. And when an AI-drafted query becomes a scheduled production query, have a human review it and record it in the change log. | Privacy and governance<br>• Aggregated data<br>• Approved tools and settings<br>• Shared prompt library<br>• Human review before production | Layout: a user-level table is blocked at a gate while an aggregated table passes; a prompt library book and a change-log entry appear. 0-10s user-level blocked; 10-20s aggregate passes; 20-30s library and log. |
| 13 | 7:34 | 20 | Common mistakes. Presenting an AI answer without a source you can open. Pasting user-level data into unapproved tools. Letting AI-generated SQL run in production without review. Ignoring consent and tracking changes that explain a surprising finding. And asking vague questions, which get vague, confident answers. | Common mistakes<br>• No openable source<br>• User-level data in chats<br>• Unreviewed SQL in production<br>• Ignoring context<br>• Vague questions | Layout: five warning cards drop in. 0-10s cards 1 to 3; 10-20s cards 4 and 5. |
| 14 | 7:54 | 35 | Recap. AI now lives inside G A four, inside Google Cloud, in general assistants, and behind M C P connections. AI proposes, you verify, using the ladder: source, recompute, definitions, context, plausibility. Give models schema notes and rules so their work is checkable. Connect with read-only access. And keep data aggregated and governed. If you want to go deeper on statistics and decisions, AI for Data Analysis and Decision Making is your next course. For consent-aware collection, take Privacy-First Measurement. | Recap<br>• Four places AI lives<br>• AI proposes, you verify<br>• Schema-aware prompts<br>• Read-only connections<br>• Next courses | Layout: recap card with the ladder icon; two course badges slide in. 0-18s ladder icon; 18-35s course badges. |
| 15 | 8:29 | 26 | Try this now. Ask Ask Advisor, or an assistant connected to your data, one real question about last week. Then climb the verification ladder: find the source, recompute the number, check definitions, check the change log, and judge plausibility. Write down which rung, if any, changed the answer. That note is the best argument for keeping humans in the loop. | Try this now<br>• One real question<br>• Climb all five rungs<br>• Note which rung changed the answer | Layout: a question card, the five-rung ladder with ticks, and a note card 'rung that changed the answer'. 0-9s question; 9-17s ladder ticks; 17-26s note card. |

<details><summary>Narration per scene (paste into ElevenLabs)</summary>

Scene 1 — AI-assisted GA4 analysis

```text
In twenty twenty-six, you can type a question like why did revenue drop last week directly into G A four, and an AI assistant will answer in seconds. That's genuinely useful. It's also a trap, because a confident answer isn't the same as a correct one. In this lecture you'll learn where AI now sits in the analytics workflow, a five-step verification ladder for AI answers, prompt templates that produce checkable work, how to connect an assistant to G A four safely through M C P, and the privacy rules that keep you out of trouble.
```

Scene 2 — Why it matters

```text
Why does this matter? Because AI shifts where analysts spend time. Drafting queries, summarizing tables and writing first-draft commentary now take minutes. That frees you for the parts AI is bad at: knowing your tracking history, your definitions, your consent changes and your business context. If you use AI well, you'll answer more questions, faster, with the same rigor. If you use it carelessly, you'll spread confident mistakes at scale.
```

Scene 3 — Where AI sits

```text
Here's where AI sits. Inside G A four, Analytics Advisor, with Ask Advisor in beta, answers plain-language questions about your property. Inside Google Cloud, Gemini helps draft SQL in BigQuery, and conversational data agents surface in Data Studio. General assistants like Claude, ChatGPT and Gemini can analyze exported, aggregated tables, write Python and SQL, and draft narratives. And through M C P, Google's experimental Analytics M C P server lets an assistant call read-only tools like run report, using your credentials.
```

Scene 4 — AI proposes, you verify

```text
The core principle: AI proposes, you verify. Think of AI like a brilliant new intern who has never seen your company before. They're fast, articulate and tireless. But they don't know that the consent banner changed last Tuesday, that revenue here means net of refunds, or that the thank-you page used to double count. You'd never forward an intern's first draft to the CEO without checking it. Treat AI output exactly the same way.
```

Scene 5 — The verification ladder

```text
Here's the verification ladder. Step one, source check: which report, query or table did the number come from? If you can't tell, it isn't a finding. Step two, recompute: rerun the key number yourself in a report, the Data A P I or SQL. Step three, definition check: does users mean active users, is revenue net of refunds, which attribution model? Step four, context check: tracking changes, consent changes, releases, holidays, straight from the change log. And step five, plausibility: does the size of the effect make business sense? Would you bet on it?
```

Scene 6 — SQL prompts that work

```text
Now prompts that produce checkable work. The secret is giving the model your schema notes and rules. For SQL, tell it the dataset, that parameters live in event params and need UNNEST, that sessions are user pseudo id plus ga session id, which field holds the session channel and revenue, and that it must always filter the table suffix. Then ask for the SQL, a line-by-line explanation, and two ways the result could mislead. That last request turns the model into its own first reviewer.
```

Scene 7 — Explain and summarize prompts

```text
For explaining a change, give aggregated data only, with no personal data: week, channel, device, sessions, key events, revenue. Add known context, like a consent banner change on the second of September and a new checkout on the fourteenth. Ask it to decompose the change, name the segment that explains most of it, list three hypotheses with the report or query that would test each, and use only numbers from the table, labeling estimates. And for weekly commentary, ask for what, so what, now what, naming the source and attribution model, with no causal claims without a test.
```

Scene 8 — Example 1: Dubai marketplace

```text
First example, a simple one. A Dubai marketplace's CMO asks Ask Advisor why orders fell. It points to lower paid social sessions. The analyst climbs the ladder. The report confirms the session drop. But the change log shows the UK consent banner changed that week, and BigQuery shows the drop is concentrated in consent-denied traffic, with no change in purchase rates among consented users. The commentary says observed sessions fell mainly because of new consent defaults, purchases from consented users are stable, and no budget change is recommended. The AI found the what. The analyst found the why.
```

Scene 9 — Example 2: Karachi agency SQL copilot

```text
Second example, a business case. A Karachi agency builds a shared prompt with its schema notes and asks Claude or ChatGPT to draft SQL for client questions. Every query is reviewed by a senior analyst against a checklist: suffix filter, session definition, null ids, revenue field and attribution field, before it touches a dashboard. Drafting time drops sharply. And review keeps catching the typical AI mistakes, like forgetting the date filter or counting sessions inconsistently. AI speed plus human review beats either alone.
```

Scene 10 — Watch me do it, part 1

```text
Watch me use the SQL template. I paste the schema notes, then the question: key event rate by landing page for mobile users in September twenty twenty-six, pages with at least a hundred sessions. The assistant returns a query, an explanation and two risks: that landing pages with query strings may split, and that consent-denied sessions are excluded. I check it against my review list. The suffix filter is there. Sessions use ga session id. But it filtered mobile using the wrong field, so I fix that, run it, and compare the top rows with the interface.
```

Scene 11 — Watch me do it, part 2

```text
Next, I connect an assistant to G A four through M C P. Google's experimental Analytics M C P server runs locally and uses application default credentials, after I enable the Admin and Data A P Is in a cloud project and install it following the README. I use an account with read-only access, Viewer or Analyst, limited to the properties I need. Now I can ask my assistant to run a report on key events by channel for last week, and it calls run report. I still check the number against the interface before I use it.
```

Scene 12 — Privacy and governance

```text
Privacy and governance, briefly. Send AI tools aggregated data wherever possible, and never paste user-level exports with identifiers into unapproved tools. Check your organization's approved tools, retention and training settings, and client contracts. Keep a shared prompt library with schema notes, so every analyst gets consistent SQL. And when an AI-drafted query becomes a scheduled production query, have a human review it and record it in the change log.
```

Scene 13 — Common mistakes

```text
Common mistakes. Presenting an AI answer without a source you can open. Pasting user-level data into unapproved tools. Letting AI-generated SQL run in production without review. Ignoring consent and tracking changes that explain a surprising finding. And asking vague questions, which get vague, confident answers.
```

Scene 14 — Recap

```text
Recap. AI now lives inside G A four, inside Google Cloud, in general assistants, and behind M C P connections. AI proposes, you verify, using the ladder: source, recompute, definitions, context, plausibility. Give models schema notes and rules so their work is checkable. Connect with read-only access. And keep data aggregated and governed. If you want to go deeper on statistics and decisions, AI for Data Analysis and Decision Making is your next course. For consent-aware collection, take Privacy-First Measurement.
```

Scene 15 — Try this now

```text
Try this now. Ask Ask Advisor, or an assistant connected to your data, one real question about last week. Then climb the verification ladder: find the source, recompute the number, check definitions, check the change log, and judge plausibility. Write down which rung, if any, changed the answer. That note is the best argument for keeping humans in the loop.
```

</details>
